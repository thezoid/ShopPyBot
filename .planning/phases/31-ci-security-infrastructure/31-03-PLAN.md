---
phase: 31-ci-security-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - requirements.txt
  - pyproject.toml
autonomous: true
requirements: [RH-05]

must_haves:
  truths:
    - ".github/dependabot.yml exists, is valid schema v2, and covers both pip (directory /) and github-actions ecosystems weekly"
    - "cryptography is pinned to ==49.0.0 and pydantic-settings[yaml] to ==2.14.2 in requirements.txt"
    - "jinja2 is pinned to ==3.1.6 in pyproject.toml [web] optional-dependencies"
    - "The full pytest suite stays green after the bumps (>=887 passed baseline)"
  artifacts:
    - path: ".github/dependabot.yml"
      provides: "Automated dependency-update PR config for pip + github-actions"
      contains: "package-ecosystem"
    - path: "requirements.txt"
      provides: "Patched cryptography + pydantic-settings pins clearing 4 of the 7 alerts"
      contains: "cryptography==49.0.0"
    - path: "pyproject.toml"
      provides: "Patched jinja2 pin in [web] extra clearing 3 of the 7 alerts"
      contains: "jinja2==3.1.6"
  key_links:
    - from: ".github/dependabot.yml pip ecosystem (directory /)"
      to: "requirements.txt + pyproject.toml"
      via: "single pip entry covers both root manifests"
      pattern: "package-ecosystem:\\s*\"pip\""
    - from: "the 3 version bumps"
      to: "the 7 open Dependabot alerts"
      via: "patched versions verified against PyPI in research; queue drains post-merge"
      pattern: "cryptography==49.0.0"
---

<objective>
Close RH-05: create the dependabot configuration and remediate the 7 currently-open dependency alerts to a clean queue — without violating any prior-milestone pin constraint.

Purpose: Research pulled all 7 open alerts live via gh api and cross-referenced them to exact pinned versions. All 7 clear with exactly 3 package bumps — cryptography, pydantic-settings, jinja2 — none of which touch the v4.1-pinned FastAPI/uvicorn, none introduce sse-starlette, and none introduce Node. Zero documented-dismissals are needed. Exact-pin style (==) is kept to match the project's established requirements.txt convention (resolved open question: exact-pin, not a range).

Output: A new .github/dependabot.yml (pip + github-actions), three version-pin edits, and a green full-suite regression run.

Pitfall guardrail (research Pitfall 5): jinja2 is NOT in requirements.txt — it lives ONLY in pyproject.toml's [web] extra. The alert's manifest_path mislabels it as requirements.txt. Apply the jinja2 bump in pyproject.toml, not requirements.txt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ci-security-infrastructure/31-RESEARCH.md
@.planning/phases/31-ci-security-infrastructure/31-CONTEXT.md

<interfaces>
<!-- Exact current pins (extracted from the manifests) and their target versions. -->
requirements.txt (current → target):
- cryptography==44.0.2            → cryptography==49.0.0        (clears alerts #6 high, #11 high, #7 low)
- pydantic-settings[yaml]==2.14.0 → pydantic-settings[yaml]==2.14.2  (clears #12 moderate)

pyproject.toml [project.optional-dependencies].web (current → target):
- "jinja2==3.1.4"                 → "jinja2==3.1.6"            (clears #8, #9, #10 moderate)

DO NOT touch (v4.1 locked pins): fastapi==0.115.8, uvicorn[standard]==0.30.6, python-multipart==0.0.32.
platformdirs==4.10.0 appears in BOTH files intentionally (research Pitfall 6) — leave it; it is not an alert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/dependabot.yml (pip + github-actions ecosystems)</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - 31-RESEARCH.md "Code Examples → RH-05 dependabot.yml" (the exact target config) and "Supporting" table (schema v2, dual-manifest note) and "Pitfall 5/6" (single pip entry at directory / covers both root manifests)
    - .github/ directory layout (confirm the workflows/ sibling; dependabot.yml sits at .github/dependabot.yml, NOT under workflows/)
  </read_first>
  <action>
    Create .github/dependabot.yml with version: 2 and an updates: list of two entries. Entry 1: package-ecosystem "pip", directory "/", schedule interval weekly with day monday, open-pull-requests-limit 10, labels ["dependencies", "python"]. Entry 2: package-ecosystem "github-actions", directory "/", schedule interval weekly with day monday, open-pull-requests-limit 5, labels ["dependencies", "github-actions"]. A single pip entry at directory "/" covers BOTH requirements.txt and pyproject.toml in the repo root — do NOT add a second pip entry per manifest. Place the file at .github/dependabot.yml (repo-root .github/, a sibling of workflows/), not inside .github/workflows/.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/dependabot.yml')); assert d['version']==2; ec=[u['package-ecosystem'] for u in d['updates']]; assert 'pip' in ec and 'github-actions' in ec; print('valid', ec)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/dependabot.yml` exists at that exact path (not under workflows/).
    - The file parses as valid YAML and version == 2 (the python assert command prints "valid ['pip', 'github-actions']").
    - Exactly two updates entries exist: one with package-ecosystem "pip" directory "/", one with package-ecosystem "github-actions" directory "/".
    - Both entries use schedule interval "weekly".
    - `grep -nE 'open-pull-requests-limit' .github/dependabot.yml` matches on both entries.
  </acceptance_criteria>
  <done>A valid schema-v2 dependabot.yml at .github/dependabot.yml configures weekly pip and github-actions update PRs, with a single pip entry covering both root manifests.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the 3 version bumps and confirm the full suite stays green</name>
  <files>requirements.txt, pyproject.toml</files>
  <read_first>
    - requirements.txt (lines 3 cryptography==44.0.2, 8 pydantic-settings[yaml]==2.14.0)
    - pyproject.toml (line 14 "jinja2==3.1.4" inside [project.optional-dependencies].web)
    - 31-RESEARCH.md "Standard Stack → Supporting" (exact target versions + which alert each clears), "Pitfall 5" (jinja2 lives in pyproject only), "Pitfall 7" (cryptography 44→49 jump audited; fall back to 48.0.1 ONLY if the suite goes red), and "Alternatives Considered" (49.0.0 over 48.0.1 rationale)
  </read_first>
  <action>
    In requirements.txt: change cryptography==44.0.2 to cryptography==49.0.0 and pydantic-settings[yaml]==2.14.0 to pydantic-settings[yaml]==2.14.2 — preserve the [yaml] extra marker and the exact-pin == style. In pyproject.toml: change the "jinja2==3.1.4" string in the [project.optional-dependencies].web list to "jinja2==3.1.6". Do NOT modify any other pin — fastapi, uvicorn, python-multipart, platformdirs, pydantic, and all requirements.txt lines stay exactly as they are.

    Then install the bumped versions and run the full regression suite: pip install -r requirements.txt then pip install -e .[web] (the test env may also need pip install httpx for the web tests per prior sessions). Run the full pytest suite. It must stay green at the 887-passed / 2-skipped baseline. Per research Pitfall 7: if and only if the cryptography bump turns the suite red with an ImportError/AttributeError on a removed cipher (CAST5/SEED/IDEA/Blowfish/TripleDES/ARC4/SECT curves — none used in this codebase per grep), fall back to cryptography==48.0.1 and record the fallback in the summary. Do not weaken any other pin to force green.
  </action>
  <verify>
    <automated>pytest --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'cryptography==49.0.0' requirements.txt` matches; `grep -nE 'cryptography==44' requirements.txt` returns no lines.
    - `grep -nE 'pydantic-settings\[yaml\]==2.14.2' requirements.txt` matches.
    - `grep -nE 'jinja2==3.1.6' pyproject.toml` matches; `grep -nE 'jinja2==3.1.4' pyproject.toml` returns no lines.
    - `grep -nE 'fastapi==0.115.8|uvicorn\[standard\]==0.30.6' pyproject.toml` still matches (locked pins untouched).
    - jinja2 does NOT appear in requirements.txt (`grep -niE 'jinja2' requirements.txt` returns no lines — it lives only in pyproject.toml).
    - `pytest --tb=short -q` reports 0 failed and >=887 passed (fallback to cryptography==48.0.1 documented in the summary only if a removed-cipher ImportError occurred).
  </acceptance_criteria>
  <done>cryptography, pydantic-settings, and jinja2 are bumped to their patched versions in the correct manifests, no locked pin is touched, and the full pytest suite is green at the >=887-passed baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency manifest → installed runtime | A vulnerable pinned version ships the CVE into production (credential encryption + web templates) |
| dependabot.yml config → GitHub Dependabot service | A wrong/missing ecosystem entry silently leaves a manifest un-monitored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Elevation of Privilege / Info Disclosure | known-CVE dependency (cryptography high alerts #6/#11 on the credential/session encryption path) | mitigate | bump cryptography to 49.0.0 (removes the SECT-curve root cause class, not just validates it); regression suite confirms no crypto-logic break |
| T-31-08 | Information Disclosure | jinja2 template-rendering CVEs (#8/#9/#10) in the web dashboard | mitigate | bump jinja2 to 3.1.6 in the pyproject [web] extra (the correct manifest — Pitfall 5) |
| T-31-09 | Repudiation | dependabot.yml covers the wrong ecosystem → manifest silently unmonitored | mitigate | single pip entry at directory "/" covers both root manifests; github-actions ecosystem added so retired-action drift is auto-flagged; YAML + ecosystem membership asserted |
| T-31-10 | Denial of Service (self-inflicted) | large cryptography major jump (44→49) regresses the suite | mitigate | changelog audited in research (no deprecated-cipher usage in this repo); full pytest gate is the tripwire; documented 48.0.1 fallback if red |
| T-31-SC | Tampering | pip install of bumped packages (supply chain) | mitigate | all three are existing long-established deps (pyca/pydantic/pallets), slopcheck [OK] in research Package Legitimacy Audit; version bump only, no new packages → no blocking-human checkpoint required |
</threat_model>

<verification>
- .github/dependabot.yml is valid schema-v2 with pip + github-actions ecosystems.
- requirements.txt: cryptography==49.0.0, pydantic-settings[yaml]==2.14.2. pyproject.toml [web]: jinja2==3.1.6. Locked pins untouched.
- pytest --tb=short -q green at >=887 passed / 2 skipped.
- CI-verification debt (post-push, per CONTEXT.md live-CI boundary): `gh api /repos/thezoid/ShopPyBot/dependabot/alerts?state=open --paginate` returns empty after the bumps merge. If gh/runner is unavailable this session, record as CI debt — not a code gap.
</verification>

<success_criteria>
- RH-05 local-verifiable half complete: dependabot.yml valid, 3 bumps applied to the correct manifests, no pinned-constraint violation, full suite green.
- Zero documented-dismissals required (research confirmed all 7 alerts clear via the 3 bumps).
</success_criteria>

<output>
Create `.planning/phases/31-ci-security-infrastructure/31-03-SUMMARY.md` when done. Record the pytest pass count, any cryptography fallback taken, and the alert-queue-drain evidence (or CI-verification debt note if gh was unavailable).
</output>
