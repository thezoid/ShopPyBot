---
phase: 30-breakfix-hardening
plan: 06
type: execute
wave: 2
depends_on: ["30-03"]
files_modified:
  - plugins/shopbot_plugin_walmart.py
  - plugins/shopbot_plugin_target.py
  - plugins/shopbot_plugin_gamestop.py
  - plugins/shopbot_plugin_newegg.py
  - plugins/shopbot_plugin_squareenix.py
  - tests/test_plugin_walmart.py
  - tests/test_plugin_target.py
  - tests/test_plugin_gamestop.py
  - tests/test_plugin_newegg.py
  - tests/test_plugin_squareenix.py
autonomous: true
requirements: [BF-03]

must_haves:
  truths:
    - "All 5 community plugin login() methods return bool and False on missing creds / ambiguous signal / exception"
    - "Each community auto_buy sets _checkout_stage='login' before login() and aborts remaining stages on a failed login"
  artifacts:
    - path: "plugins/shopbot_plugin_walmart.py"
      provides: "login(self) -> bool via generic verification + auto_buy abort-on-False"
      contains: "_verify_login_generic"
    - path: "plugins/shopbot_plugin_squareenix.py"
      provides: "login(self) -> bool via generic verification + auto_buy abort-on-False"
      contains: "_verify_login_generic"
  key_links:
    - from: "plugins/shopbot_plugin_walmart.py:login"
      to: "core/plugin_base._verify_login_generic"
      via: "_verify_login_generic(tab, fragment, form_selector) before return True"
      pattern: "_verify_login_generic"
    - from: "community auto_buy"
      to: "self._checkout_stage / login() return"
      via: "_checkout_stage='login' then abort on False"
      pattern: "_checkout_stage = \"login\""
---

<objective>
Convert the 5 community plugins (Walmart, Target, GameStop, NewEgg, SquareEnix) to the BF-03 `login() -> bool` contract using the shared generic verification signal (D-12 — no platform-specific override; live selector tuning stays operator debt), and make each `auto_buy` set `_checkout_stage="login"` and abort on a failed login. Depends on 30-03's `_verify_login_generic` helper; runs in parallel with 30-04 (distinct plugin files).

Purpose: The BF-03 guarantee (ambiguous → not-success) applies uniformly to ALL 7 plugins (D-11), including the 5 community plugins with unverified TODO selectors.
Output: `login(self) -> bool` for all 5 community plugins + `auto_buy` abort/stage signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-breakfix-hardening/30-CONTEXT.md
@.planning/phases/30-breakfix-hardening/30-RESEARCH.md
@.planning/phases/30-breakfix-hardening/30-PATTERNS.md
@.planning/phases/30-breakfix-hardening/30-VALIDATION.md
@.planning/phases/30-breakfix-hardening/30-03-SUMMARY.md

<interfaces>
<!-- Contracts extracted from live source + plan 30-03 output. Do not re-explore. -->

core/plugin_base.py (from plan 30-03):
  async def _verify_login_generic(self, tab, signin_url_fragment: str, form_selector: str) -> bool   # any exception → False

Per-plugin login def + auto_buy login-call sites, and the generic (fragment, form_selector) signal (D-12, selector tuning is operator debt):
  | Plugin      | login def | login() call in auto_buy | signin_url_fragment | form_selector                                          |
  | Walmart     | :118      | :186                     | /account/login      | #email                                                 |
  | Target      | :120      | :188                     | /account/signin     | [data-test="accountNav-signIn"] input[type="email"]    |
  | GameStop    | :120      | :188                     | /login              | input#login-form-email                                 |
  | NewEgg      | :124      | :203                     | /identity/signin    | #labeled-input-signEmail                               |
  | SquareEnix  | :121      | :194                     | /account/login      | [type="email"]                                         |

All 5 call login() MID-FLOW (after add_to_cart.click() → cart navigate → checkout_btn.click()) — Pitfall 3: D-15 = "return False, abort remaining stages", NOT "no add-to-cart occurred".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Walmart + Target + GameStop login() -> bool + auto_buy abort</name>
  <files>plugins/shopbot_plugin_walmart.py, plugins/shopbot_plugin_target.py, plugins/shopbot_plugin_gamestop.py, tests/test_plugin_walmart.py, tests/test_plugin_target.py, tests/test_plugin_gamestop.py</files>
  <read_first>
    - plugins/shopbot_plugin_walmart.py (login :118-146; auto_buy login call :186)
    - plugins/shopbot_plugin_target.py (login :120; auto_buy login call :188)
    - plugins/shopbot_plugin_gamestop.py (login :120; auto_buy login call :188)
    - core/plugin_base.py (_verify_login_generic, from 30-03)
    - .planning/phases/30-breakfix-hardening/30-PATTERNS.md (§community login — representative Walmart body + selector table)
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Pitfall 3 — community plugins call login mid-flow; D-15 uniform abort)
  </read_first>
  <behavior>
    - each login returns False on missing creds / exception
    - each login returns True only after _verify_login_generic(tab, fragment, form_selector) confirms (per table)
    - each auto_buy sets _checkout_stage="login" before login() and aborts (return False) on a failed login
  </behavior>
  <action>
    Convert `login(self) -> None` to `-> bool` for Walmart, Target, GameStop. Missing platform creds → `return False`; the `except Exception` handler returns `False`. After each `writeLog("Signed in to ...", ...)`, call `await self._verify_login_generic(tab, <fragment>, <form_selector>)` per the table (Walmart `/account/login` + `#email`; Target `/account/signin` + `[data-test="accountNav-signIn"] input[type="email"]`; GameStop `/login` + `input#login-form-email`) and `return` its result; move any `save_session()` after verification. In each `auto_buy`, set `self._checkout_stage = "login"` immediately before `await self.login()` (walmart:186, target:188, gamestop:188) and return False (abort remaining stages) when login() is False. Generic signal ONLY (D-12; per-retailer selector tuning is operator debt). D-15 = abort remaining stages, NOT no-add-to-cart (Pitfall 3). Log with `exc.__class__.__name__` only (V7).
  </action>
  <acceptance_criteria>
    - walmart/target/gamestop `login` signatures are `-> bool`; early returns are `return False`; success path calls `_verify_login_generic` with the table fragment/selector
    - each `auto_buy` sets `self._checkout_stage = "login"` before `await self.login()` and aborts on False
    - `pytest tests/test_plugin_walmart.py tests/test_plugin_target.py tests/test_plugin_gamestop.py -x -q` green
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_plugin_walmart.py tests/test_plugin_target.py tests/test_plugin_gamestop.py -x -q</automated>
  </verify>
  <done>Walmart/Target/GameStop logins return bool with generic verification; auto_buy aborts and signals stage=login on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NewEgg + SquareEnix login() -> bool + auto_buy abort</name>
  <files>plugins/shopbot_plugin_newegg.py, plugins/shopbot_plugin_squareenix.py, tests/test_plugin_newegg.py, tests/test_plugin_squareenix.py</files>
  <read_first>
    - plugins/shopbot_plugin_newegg.py (login :124-161; auto_buy login call :203)
    - plugins/shopbot_plugin_squareenix.py (login :121-152; auto_buy login call :194)
    - core/plugin_base.py (_verify_login_generic, from 30-03)
    - .planning/phases/30-breakfix-hardening/30-PATTERNS.md (§community login — selector table)
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Pitfall 3 — D-15 uniform abort)
  </read_first>
  <behavior>
    - each login returns False on missing creds / exception
    - each login returns True only after _verify_login_generic(tab, fragment, form_selector) confirms (per table)
    - each auto_buy sets _checkout_stage="login" before login() and aborts (return False) on a failed login
  </behavior>
  <action>
    Same conversion for NewEgg (`/identity/signin` + `#labeled-input-signEmail`) and SquareEnix (`/account/login` + `[type="email"]`): `login(self) -> bool`, missing creds → `return False`, `except Exception` → `return False`, call `await self._verify_login_generic(tab, <fragment>, <form_selector>)` before returning True, move any `save_session()` after verification. In each `auto_buy`, set `self._checkout_stage = "login"` immediately before `await self.login()` (newegg:203, squareenix:194) and return False (abort remaining stages) on login() False. Generic signal only (D-12). Log with `exc.__class__.__name__` only (V7).
  </action>
  <acceptance_criteria>
    - newegg/squareenix `login` signatures are `-> bool`; early returns are `return False`; success path calls `_verify_login_generic` with the table values
    - each `auto_buy` sets `self._checkout_stage = "login"` before `await self.login()` and aborts on False
    - `pytest tests/test_plugin_newegg.py tests/test_plugin_squareenix.py -x -q` green
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_plugin_newegg.py tests/test_plugin_squareenix.py -x -q</automated>
  </verify>
  <done>NewEgg/SquareEnix logins return bool with generic verification; auto_buy aborts and signals stage=login on failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retail sign-in page → plugin checkout flow | Post-submit DOM/URL state is untrusted on every community plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Spoofing | any community plugin proceeding to checkout on an unconfirmed login | mitigate | `login()` returns True only after `_verify_login_generic` confirms; ambiguous → False (D-13); uniform across all 5 (D-11) |
| T-30-03d | Spoofing | failed community login retried as transient | mitigate | `auto_buy` sets `_checkout_stage="login"` + aborts on False, feeding the orchestrator short-circuit (30-01, Task 3) |
| T-30-V7 | Information Disclosure | credential leak via exception text | mitigate | all handlers log `exc.__class__.__name__` only; never log credentials |

No package-manager installs occur in this plan — no `T-30-SC` checkpoint required.

**Operator debt (out of scope, documented):** per-retailer live login-selector accuracy for these 5 plugins is verifiable only against live storefronts (selector-TODO bucket). The generic verification MECHANISM ships now and still guarantees ambiguous → not-success.
</threat_model>

<verification>
- `pytest tests/test_plugin_walmart.py tests/test_plugin_target.py tests/test_plugin_gamestop.py tests/test_plugin_newegg.py tests/test_plugin_squareenix.py -x -q` green.
- All 5 logins return bool, False on missing creds/ambiguous/exception; each auto_buy aborts and signals stage=login on failure.
</verification>

<success_criteria>
- BF-03 applied uniformly to all 7 plugins (D-11): with 30-05 (Amazon/BestBuy) + this plan, no plugin can report a failed/ambiguous login as successful.
</success_criteria>

<output>
Create `.planning/phases/30-breakfix-hardening/30-06-SUMMARY.md` when done.
</output>
