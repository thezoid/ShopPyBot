---
phase: 30-breakfix-hardening
plan: 04
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - plugins/shopbot_plugin_amazon.py
  - plugins/shopbot_plugin_bestbuy.py
  - tests/test_plugin_amazon.py
  - tests/test_plugin_bestbuy.py
autonomous: true
requirements: [BF-02]

must_haves:
  truths:
    - "Amazon auto_buy durably records the place-order marker before dispatching the place-order click"
    - "BestBuy auto_buy durably records the place-order marker before dispatching the place-order click"
  artifacts:
    - path: "plugins/shopbot_plugin_amazon.py"
      provides: "marker write via run_in_executor immediately before place_order_guarded (place-order stage)"
      contains: "mark_place_order_attempted_sync"
    - path: "plugins/shopbot_plugin_bestbuy.py"
      provides: "marker write via run_in_executor immediately before place_order_guarded (place-order stage)"
      contains: "mark_place_order_attempted_sync"
  key_links:
    - from: "plugins/shopbot_plugin_amazon.py:auto_buy"
      to: "models.mark_place_order_attempted_sync"
      via: "run_in_executor before place_order_guarded(place_order.click)"
      pattern: "mark_place_order_attempted_sync"
    - from: "plugins/shopbot_plugin_bestbuy.py:auto_buy"
      to: "models.mark_place_order_attempted_sync"
      via: "run_in_executor before place_order_guarded(place_order.click)"
      pattern: "mark_place_order_attempted_sync"
---

<objective>
Write the BF-02 durable place-order marker at the exact click-dispatch instant inside the two live-tested plugins' `auto_buy()` — the one site the orchestrator cannot reach (it awaits `auto_buy()` as one atomic call). The platform-agnostic guard that reads this marker back was built in plan 30-01; this plan only adds the write side. This is the first plugin→`models.py` import edge in the codebase.

Purpose: Close the swallowed-`TimeoutError` root cause (amazon:460-469, bestbuy:389-396) — the marker must be on disk before the click so a crash/relaunch mid-place-order latches non-retryable.
Output: A 4-line marker-write addition to Amazon's place-order stage (Task 1) and an independently-droppable parity addition to BestBuy's (Task 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-breakfix-hardening/30-CONTEXT.md
@.planning/phases/30-breakfix-hardening/30-RESEARCH.md
@.planning/phases/30-breakfix-hardening/30-PATTERNS.md
@.planning/phases/30-breakfix-hardening/30-VALIDATION.md
@.planning/phases/30-breakfix-hardening/30-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted from live source + plan 30-01 output. Do not re-explore. -->

models.py (added by plan 30-01):
  def mark_place_order_attempted_sync(link: str, attempted_at: str) -> None   # blocking sqlite3 write; call via run_in_executor

plugins/shopbot_plugin_amazon.py:
  async def auto_buy(self, url: str) -> bool   # :366-463; place-order stage at :460-463 inside `async with asyncio.timeout(step_timeout_secs):`
    #   self._checkout_stage = "place-order"        (:460)
    #   self._last_tab = tab                         (:462)
    #   return await self.place_order_guarded(place_order.click)   (:463)  ← marker write goes IMMEDIATELY before this
    #   outer `except Exception as exc: ... return False` (:464) is the swallowed-TimeoutError → retryable root cause

plugins/shopbot_plugin_bestbuy.py:
  async def auto_buy(self, url: str) -> bool   # :275-396; place-order stage at :389-396 (byte-identical shape to Amazon)
    #   place_order = await tab.select(".button--place-order", timeout=10)
    #   return await self.place_order_guarded(place_order.click)   ← marker write IMMEDIATELY before this
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amazon place-order marker write</name>
  <files>plugins/shopbot_plugin_amazon.py, tests/test_plugin_amazon.py</files>
  <read_first>
    - plugins/shopbot_plugin_amazon.py (auto_buy :366-463, place-order stage :460-463; note existing imports for datetime/timezone/asyncio)
    - models.py (mark_place_order_attempted_sync, added by 30-01)
    - .planning/phases/30-breakfix-hardening/30-PATTERNS.md (§amazon marker write — Pattern 1 + "Blocking external calls off the event loop")
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Pattern 1 — do NOT route through write_queue; ASYNC-05 unaffected)
    - .planning/phases/30-breakfix-hardening/30-01-SUMMARY.md (final accessor name/signature)
  </read_first>
  <behavior>
    - auto_buy writes the marker (mark_place_order_attempted_sync) BEFORE calling place_order_guarded at the place-order stage
    - the write completes (is awaited) before the click; it is not enqueued to write_queue
  </behavior>
  <action>
    In Amazon `auto_buy`, inside the `async with asyncio.timeout(step_timeout_secs)` block at the `"place-order"` stage (amazon:460-463), IMMEDIATELY BEFORE `return await self.place_order_guarded(place_order.click)`, add `from models import mark_place_order_attempted_sync` and write the marker via `await asyncio.get_running_loop().run_in_executor(None, mark_place_order_attempted_sync, url, now_iso)` where `now_iso = datetime.now(timezone.utc).isoformat()`. The write MUST be an awaited synchronous `run_in_executor` write so it durably commits before the click even if the click times out 1ms later (D-01). Do NOT route through `write_queue` (async put does not guarantee on-disk durability — defeats D-01). This is the first plugin→models edge; it follows the existing `increment_checkout_attempts_sync` direct-write precedent and does NOT violate ASYNC-05 (that governs the purchased/confirmed write, which stays owned by write_queue, unchanged). Log with `exc.__class__.__name__` only if any handler is touched (V7).
  </action>
  <acceptance_criteria>
    - shopbot_plugin_amazon.py imports `mark_place_order_attempted_sync` and calls it via `run_in_executor` before `place_order_guarded` in the place-order stage
    - the marker write is NOT routed through `write_queue`
    - `pytest tests/test_plugin_amazon.py -x -q` green, including a test asserting the marker is written before the place-order click (ordering)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_plugin_amazon.py -x -q</automated>
  </verify>
  <done>Amazon writes the durable marker immediately before dispatching the place-order click.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (ISOLATED / DROPPABLE): BestBuy place-order marker write (parity)</name>
  <files>plugins/shopbot_plugin_bestbuy.py, tests/test_plugin_bestbuy.py</files>
  <read_first>
    - plugins/shopbot_plugin_bestbuy.py (auto_buy :275-396, place-order stage :389-396)
    - models.py (mark_place_order_attempted_sync, added by 30-01)
    - .planning/phases/30-breakfix-hardening/30-PATTERNS.md (§bestbuy marker — byte-identical Amazon shape)
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Pitfall 5 + Open Question 1 — BestBuy has the identical swallowed-TimeoutError bug; inclusion recommended)
  </read_first>
  <behavior>
    - BestBuy auto_buy writes the marker (mark_place_order_attempted_sync) BEFORE calling place_order_guarded at the place-order stage
    - the write is awaited (durable) and not enqueued to write_queue
  </behavior>
  <action>
    Apply the identical marker write to BestBuy `auto_buy`'s place-order stage (bestbuy:389-396): immediately BEFORE `return await self.place_order_guarded(place_order.click)`, inside the same `async with asyncio.timeout(...)` block, add the `from models import mark_place_order_attempted_sync` import and the same `await asyncio.get_running_loop().run_in_executor(None, mark_place_order_attempted_sync, url, now_iso)` write. BestBuy is a byte-for-byte identical place-order shape to Amazon (Pitfall 5); the guard mechanism in 30-01 is platform-agnostic, so only the write call site is plugin-local.

    THIS TASK IS INDEPENDENTLY DROPPABLE. BestBuy is a research-surfaced parity fix (Open Question 1), not a CONTEXT.md-locked root-cause site. If the operator vetoes scope expansion beyond Amazon, drop ONLY this task — Amazon (Task 1) plus the platform-agnostic orchestrator guard (30-01) still close BF-02 for the primary path. Per RESEARCH.md Open Question 1's recommendation to include (leaving a now-known symmetric exposure open would be a regression from "not yet investigated").
  </action>
  <acceptance_criteria>
    - shopbot_plugin_bestbuy.py calls `mark_place_order_attempted_sync` via `run_in_executor` before `place_order_guarded` in the place-order stage
    - the marker write is NOT routed through `write_queue`
    - `pytest tests/test_plugin_bestbuy.py -x -q` green, including a `test_bestbuy_place_order_latched` asserting marker-before-click ordering
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_plugin_bestbuy.py -x -q</automated>
  </verify>
  <done>BestBuy writes the durable marker immediately before its place-order click (or the task is explicitly dropped with a dated follow-up note).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin place-order click → SQLite | The marker must cross the crash-durability boundary (on disk before the click fires) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Tampering / Repudiation / Integrity | place-order double-submission after a swallowed TimeoutError re-click | mitigate | durable `mark_place_order_attempted_sync` write via `run_in_executor` immediately before `place_order_guarded` — not routed through the async write_queue (the write half of the 30-01 guard) |
| T-30-02c | Integrity | BestBuy retaining an un-mitigated symmetric double-buy exposure | mitigate | parity marker write in BestBuy's place-order stage (Task 2); explicitly droppable if operator vetoes scope |
| T-30-V7 | Information Disclosure | credential leak via exception text | mitigate | any touched handler logs `exc.__class__.__name__` only |

No package-manager installs occur in this plan — no `T-30-SC` checkpoint required.
</threat_model>

<verification>
- `pytest tests/test_plugin_amazon.py tests/test_plugin_bestbuy.py -x -q` green.
- Both plugins write the marker via run_in_executor before place_order_guarded; neither uses write_queue for it.
</verification>

<success_criteria>
- BF-02 write side complete for both live-tested plugins: combined with 30-01's read-side guard, an injected place-order timeout latches non-retryable (success criterion 1).
- BestBuy parity task is isolated and independently droppable.
</success_criteria>

<output>
Create `.planning/phases/30-breakfix-hardening/30-04-SUMMARY.md` when done.
</output>
