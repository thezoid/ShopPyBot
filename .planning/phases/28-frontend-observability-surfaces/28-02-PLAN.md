---
phase: 28-frontend-observability-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - core/health.py
  - web/static/components.css
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-06]
must_haves:
  truths:
    - "get_snapshot() returns heartbeat_age_secs per plugin (computed monotonic age; None when never heartbeated)"
    - "components.css defines all Phase 28 component classes using only var(--xxx) tokens"
  artifacts:
    - path: "core/health.py"
      provides: "Server-computed heartbeat_age_secs in get_snapshot()"
      contains: "heartbeat_age_secs"
    - path: "web/static/components.css"
      provides: "Health card, badge, heartbeat-age, chart, log-viewer component classes"
      contains: ".health-card"
  key_links:
    - from: "core/health.py get_snapshot()"
      to: "time.monotonic()"
      via: "now - rec['last_heartbeat']"
      pattern: "time\\.monotonic\\(\\)"
    - from: "web/static/components.css"
      to: "tokens.css"
      via: "var(--color-status-*) / var(--space-*)"
      pattern: "var\\(--color-status"
---

<objective>
Land the single backend change (server-computed heartbeat_age_secs) and all new CSS component
classes for the four observability surfaces. These two files are disjoint and have no overlap
with dashboard.html, so they form one Wave 2 plan that turns the Wave 0 health tests GREEN and
provides the styling that the Wave 3/4 HTML work consumes.

Purpose: Staleness must be computed server-side (D — the monotonic clock is process-local; the
browser cannot read the server's monotonic epoch). Placing it in get_snapshot() single-sources
it for both the REST route and the Phase 29 SSE poll loop. CSS is token-only (Phase 25 CI guard).
Output: heartbeat_age_secs field + new component classes in components.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-frontend-observability-surfaces/28-UI-SPEC.md
@.planning/phases/28-frontend-observability-surfaces/28-PATTERNS.md

<interfaces>
From core/health.py (current get_snapshot, lines 78-83):
  def get_snapshot(self) -> dict[str, dict]:
      return {name: {k: v for k, v in rec.items() if not k.startswith("_")}
              for name, rec in self._plugins.items()}
  - `import time` already present (line 8); heartbeat() already uses time.monotonic() (line 33)
  - _ensure seeds last_heartbeat=0.0 (never-heartbeated sentinel)

From web/static/tokens.css (all tokens already declared — use via var()):
  --color-status-ok / --color-status-warn / --color-status-err / --color-status-neutral
  --color-surface / --color-bg / --color-border / --color-text / --color-text-muted
  --color-accent / --color-accent-fg
  --space-xs/sm/md/lg/xl/2xl  --text-sm/body/lg  --weight-normal/semibold  --leading-ui

From web/static/components.css (analogs to copy):
  - .status-dot / .status-dot.running / .status-dot.stopped (lines 64-66) — add .status-dot.error
  - .log-pre (lines 96-107) — direct analog for .log-buffer (same props, height 300px, add monospace)
  - .card / .btn / .btn-accent / .btn-sm / .text-secondary / table th td — reused, no new rules needed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add heartbeat_age_secs to get_snapshot()</name>
  <files>core/health.py</files>
  <read_first>
    - core/health.py (modify — get_snapshot() lines 78-83; _ensure last_heartbeat=0.0 sentinel; import time at line 8)
    - 28-RESEARCH.md (Pattern 1 + Code Examples — exact verified get_snapshot() replacement; Pitfall 4 — must live in get_snapshot(), not the route boundary)
    - 28-PATTERNS.md (core/health.py section — the **{...} merge + extra-key dict-comprehension form)
    - tests/test_health.py (the Wave 0 tests this turns GREEN: test_heartbeat_age_secs_fresh/never, test_snapshot_public_keys_exact)
  </read_first>
  <behavior>
    - test_heartbeat_age_secs_fresh: after heartbeat(), heartbeat_age_secs is not None and >= 0.0
    - test_heartbeat_age_secs_never: registered-but-never-heartbeated plugin reports None
    - test_snapshot_public_keys_exact: snapshot key set now includes heartbeat_age_secs
    - test_snapshot_is_json_serializable: float|None remains JSON-serializable (unchanged)
  </behavior>
  <action>
    Rewrite get_snapshot() to compute now = time.monotonic() once, then for each plugin merge the
    existing public-key dict comprehension and add a heartbeat_age_secs key: None when
    rec["last_heartbeat"] == 0.0 (never heartbeated — per D maps to the client "never" / red band),
    else round(now - rec["last_heartbeat"], 1). Use the verified form in 28-RESEARCH.md Pattern 1.
    Do NOT add the computation at the web/routes/api.py route boundary (Pitfall 4 — the SSE poll
    loop in Phase 29 reads get_snapshot() and would miss the field). No new import (time already
    imported). This implements the server side of OBS-01/02/03.
  </action>
  <verify>
    <automated>rtk pytest tests/test_health.py -x</automated>
  </verify>
  <acceptance_criteria>
    - get_snapshot()['PluginA']['heartbeat_age_secs'] is None when last_heartbeat == 0.0
    - get_snapshot()['PluginA']['heartbeat_age_secs'] is a float >= 0.0 after heartbeat()
    - The full tests/test_health.py suite is GREEN (including the three Wave 0 tests)
    - Computation lives in core/health.py get_snapshot(), not web/routes/api.py
  </acceptance_criteria>
  <done>heartbeat_age_secs computed in get_snapshot(); entire test_health.py GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 28 component classes to components.css (token-only)</name>
  <files>web/static/components.css</files>
  <read_first>
    - web/static/components.css (modify — append after line 178; copy .status-dot modifier pattern at 64-66 and .log-pre at 96-107)
    - 28-UI-SPEC.md (Component Inventory + Code Examples — exact class list and token mapping for health-grid/health-card/badge/heartbeat/chart/log-viewer)
    - 28-RESEARCH.md (Code Examples "New CSS Classes" — the complete token-only CSS block, verbatim source)
    - tests/test_design_system.py (test_no_hardcoded_hex_in_components — the guard every new rule must satisfy)
  </read_first>
  <action>
    Append the Phase 28 component classes to web/static/components.css using ONLY var(--xxx)
    tokens (zero hardcoded hex — Pitfall 7 / Phase 25 CI guard). Add: .health-grid (grid,
    auto-fill minmax(220px,1fr), gap var(--space-md)); .health-card / .health-card-name /
    .health-card-stats / .health-card-error; .badge / .badge-ok / .badge-neutral / .badge-err;
    .status-dot.error (background var(--color-status-err) — completes the OBS-01 dot trio);
    .heartbeat-age / .heartbeat-ok / .heartbeat-warn / .heartbeat-err (the three OBS-02 bands);
    .chart-container / .chart-empty (OBS-05 empty state); .log-viewer / .log-controls /
    .log-buffer (clone .log-pre props, height 300px, add font-family: monospace) / .log-line /
    .log-level-error / .log-level-warn / .log-level-info / .log-level-debug / .log-level-trace
    (OBS-06 per-level colors). Add a `select` rule matching the existing input[type=text] property
    set so the log level dropdown inherits border/height/focus styling. Add the responsive rule
    `@media (max-width: 640px) { .health-grid { grid-template-columns: 1fr; } }`. Map every color
    to --color-status-*/--color-text*/--color-surface/--color-bg/--color-border and every spacing
    to --space-*. The 220px/300px/120px layout dimensions are pixel layout values (not color/spacing
    tokens) and are allowed.
  </action>
  <verify>
    <automated>rtk pytest tests/test_design_system.py -x</automated>
  </verify>
  <acceptance_criteria>
    - components.css contains .health-card, .badge-ok, .heartbeat-warn, .chart-empty, .log-buffer, .log-level-error, .status-dot.error
    - test_no_hardcoded_hex_in_components passes (grep: zero `#` hex literals in any new rule)
    - test_all_required_tokens_declared (if present) and test_no_external_urls_in_static stay GREEN
    - .log-buffer uses font-family: monospace and height: 300px
    - A `select` rule exists so the log level dropdown inherits input styling
  </acceptance_criteria>
  <done>All Phase 28 component classes added token-only; design-system CSS guards GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HealthRegistry → browser snapshot | heartbeat_age_secs is a derived numeric; no raw clock epoch or PII leaves the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Information Disclosure | get_snapshot() heartbeat_age_secs | accept | Derived numeric (monotonic delta or None); no wall-clock epoch, no secret; last_error already scrubbed to class name in Phase 26 |
| T-28-03 | Tampering | components.css color injection | mitigate | All colors via declared var(--xxx) tokens; test_no_hardcoded_hex_in_components blocks hex literals |
| T-28-SC | Tampering | npm/pip installs | accept | No package installs in this plan; uPlot already vendored (Phase 25) |
</threat_model>

<verification>
- `rtk pytest tests/test_health.py -x` GREEN (all 28-01 health tests now pass).
- `rtk pytest tests/test_design_system.py -x` GREEN (zero hardcoded hex; no external URLs).
- `rtk pytest -q` shows no new regressions in unrelated suites.
</verification>

<success_criteria>
- heartbeat_age_secs present in every plugin snapshot; None for never-heartbeated; float otherwise.
- All Phase 28 CSS classes exist, token-only, passing the Phase 25 hex/URL guards.
- No new Python or JS packages added.
</success_criteria>

<output>
Create `.planning/phases/28-frontend-observability-surfaces/28-02-SUMMARY.md` when done
</output>
