---
phase: 15-plugin-ecosystem-registry
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - core/service.py
  - core/cli/plugins.py
  - core/cli/__init__.py
  - tests/test_cli_plugins.py
autonomous: true
requirements: [REG-03]
must_haves:
  truths:
    - "Running shoppybot plugins list prints an aligned table of loaded plugins"
    - "shoppybot plugins list --json prints valid parseable JSON of the same data"
    - "shoppybot plugins list makes no network call"
    - "Bare shoppybot plugins (no leaf) exits 2 and does not start the bot"
  artifacts:
    - path: "core/service.py"
      provides: "BotService.list_plugins() read-only accessor over registry._all_plugins"
      contains: "def list_plugins"
    - path: "core/cli/plugins.py"
      provides: "handle_plugins_list + _format_plugins_table"
      exports: ["handle_plugins_list"]
    - path: "core/cli/__init__.py"
      provides: "plugins nested subparser with list leaf + --json flag"
      contains: "plugins"
    - path: "tests/test_cli_plugins.py"
      provides: "table, --json, no-leaf-exit-2, empty, and no-network tests"
      contains: "list_plugins"
  key_links:
    - from: "core/cli/plugins.py"
      to: "BotService.list_plugins"
      via: "svc.list_plugins() call (MOD-02: never imports core.registry directly)"
      pattern: "svc\\.list_plugins"
    - from: "core/service.py"
      to: "registry._all_plugins"
      via: "fresh PluginRegistry construction, read _all_plugins (not _active_plugins)"
      pattern: "_all_plugins"
---

<objective>
Add the `shoppybot plugins list` CLI command. Adds `BotService.list_plugins()` (reads
`registry._all_plugins`, returns plain dicts, no browser launch, no network), a new
`core/cli/plugins.py` handler module mirroring `core/cli/items.py`, and registers a nested
`plugins` subparser with a `list` leaf and `--json` flag in `core/cli/__init__.py`. Closes REG-03.

Purpose: Lets users and agents inspect all locally loaded plugins (domain patterns,
difficulty, proxy/captcha flags) without running the bot and without any network call.

Output: Modified `core/service.py`, `core/cli/__init__.py`; new `core/cli/plugins.py`;
new `tests/test_cli_plugins.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md

<interfaces>
<!-- Exact seams the executor wires against. Patterns 2/3/4 in RESEARCH are the templates. -->
From core/service.py (modify — add accessor next to list_items, lines 60-74):
- `class BotService` with `self._cfg: AppConfig`
- existing read-only accessor `def list_items(self) -> list` (the delegation model to mirror)
- `main(argv=None)` dispatches `sys.exit(args.func(args, BotService()) or 0)`; handlers receive `(args, svc) -> int`

From core/registry.py (read-only reference — do NOT import this in core/cli/plugins.py per MOD-02):
- `class PluginRegistry.__init__(self, config, plugins_dir, proxy_pool=None, captcha_solver=None)`
- `self._all_plugins: list[RetailerPlugin]` populated eagerly in __init__ (no setup/browser)
- `self._active_plugins` is EMPTY until setup_for_items runs — do NOT read it (RESEARCH Pitfall 1)
- each plugin instance exposes `.domain_patterns`, plus `.difficulty`, `.requires_proxy`, `.requires_captcha` (from Plan 15-01)

From core/cli/items.py (the exact mirror for the new module):
- `_format_items_table(rows) -> str`: max-width column sizing, header + dashed separator, "No items tracked." on empty
- `handle_items_list(args, svc) -> int`: `print(...)`; `return 0`
- imports ONLY `sys` and `BotService` (MOD-02)

From core/cli/__init__.py (modify — mirror the items block, lines 63-92):
- `sub.add_parser("items")` + `items_sub = items_p.add_subparsers(...)` + leaf with `set_defaults(func=...)`
- `_require_subcommand(parent_parser)` helper already defined inside build_parser() — reuse it for bare `plugins`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests/test_cli_plugins.py (Wave 0 scaffold for REG-03)</name>
  <read_first>
    - tests/test_cli_items.py (the exact mirror — MagicMock svc, patch core.service.BotService, main([...]), SystemExit code, capsys)
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md "Test pattern for CLI commands" and Phase Requirements to Test Map (REG-03 rows)
  </read_first>
  <action>
    Create `tests/test_cli_plugins.py` mirroring `tests/test_cli_items.py`. Five tests, each patching
    `core.service.BotService` with a `MagicMock` and calling `from core.service import main` then
    `main([...])` inside `pytest.raises(SystemExit)`:
    (1) test_plugins_list_table — `mock_svc.list_plugins.return_value` = one dict
    {"name","domain_patterns","difficulty","requires_proxy","requires_captcha"}; assert exit code 0 and the
    captured stdout contains the plugin name and a domain pattern.
    (2) test_plugins_list_json — `main(["plugins","list","--json"])`; assert exit 0 and `json.loads(out)`
    returns a list whose first element has key "difficulty".
    (3) test_plugins_no_leaf_exits_2 — `main(["plugins"])`; assert exit code 2 and `mock_svc.run` /
    `mock_svc.start` were not called (mirror test_items_no_leaf_exits_2).
    (4) test_plugins_list_empty — `list_plugins.return_value = []`; assert exit 0 and stdout contains
    "No plugins loaded."
    (5) test_plugins_list_no_network — patch `socket.socket` to raise on instantiation (or assert it is never
    called) while `main(["plugins","list"])` runs with the mocked svc; assert no socket was constructed,
    proving the CLI path makes no network call. Use the existing `tmp_data_dir` fixture where items tests use it.
    These tests MUST fail now (handler + parser do not yet exist) — this is the RED scaffold.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_plugins.py -q || true</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_cli_plugins.py` exists and collects 5 tests
    - Running it now fails (no `plugins` subcommand / handler yet) — confirms the scaffold drives Tasks 2-3
    - The no-network test references `socket` (token `socket` present in the file)
  </acceptance_criteria>
  <done>
    Five failing CLI tests exist covering table, JSON, no-leaf-exit-2, empty, and no-network behavior for REG-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add BotService.list_plugins() reading registry._all_plugins (no network, no browser)</name>
  <read_first>
    - core/service.py (the file being modified — list_items at lines 72-74 is the delegation model; __init__ holds self._cfg)
    - core/registry.py (PluginRegistry.__init__ and _all_plugins — lines 60-71; confirms construction is synchronous and browser-free)
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md Pattern 2 and Pitfalls 1, 2
  </read_first>
  <action>
    Add `def list_plugins(self) -> list[dict]` to `BotService` in the read-only accessors block (after
    `list_items`). Inside it, compute `plugins_dir = Path(__file__).parent.parent / "plugins"`, construct a fresh
    `PluginRegistry(self._cfg, plugins_dir)` (import `PluginRegistry` locally inside the method to keep it off the
    module top-level and out of the CLI import path), iterate `registry._all_plugins`, and return a list of dicts
    with keys `name` (`type(plugin).__name__`), `domain_patterns` (`plugin.domain_patterns`), `difficulty`,
    `requires_proxy`, `requires_captcha` — read the last three via `getattr(plugin, ..., <default>)` as a
    belt-and-suspenders guard per RESEARCH Pattern 2. MUST read `_all_plugins`, NEVER `_active_plugins` (empty at
    CLI time — Pitfall 1). MUST NOT call `setup()` / `setup_for_items()` (no browser, no network). Keep the method
    under 30 lines. `Path` is already imported at module top.
  </action>
  <verify>
    <automated>python -c "from core.service import BotService; rows = BotService().list_plugins(); assert isinstance(rows, list) and all('difficulty' in r for r in rows) and len(rows) >= 7; print('ok', len(rows))"</automated>
  </verify>
  <acceptance_criteria>
    - The inline command prints `ok` with a count >= 7 (matches the 7 real plugins) and exits 0
    - `core/service.py` contains `def list_plugins` and the token `_all_plugins`; it does NOT contain `_active_plugins` or `setup_for_items` in the new method
    - `python -m pytest tests/ -q` shows no new failures vs baseline
  </acceptance_criteria>
  <done>
    BotService.list_plugins() returns one dict per discovered plugin from _all_plugins with all 5 metadata keys, no browser, no network.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add core/cli/plugins.py handler + register the plugins subparser, turning the Task 1 tests green</name>
  <read_first>
    - core/cli/items.py (the exact mirror for the new module — _format_items_table + handle_items_list, sys+BotService imports only)
    - core/cli/__init__.py (the file being modified — items block lines 63-92 and the _require_subcommand helper)
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md Patterns 3, 4, 5 and Pitfall 4 (MOD-02)
  </read_first>
  <action>
    Create `core/cli/plugins.py` importing ONLY `json`, `sys`, and `BotService` from `core.service` (MOD-02 —
    NEVER import `core.registry`). Add `_format_plugins_table(rows: list[dict]) -> str` mirroring
    `_format_items_table`: headers ("Name","Domain Patterns","Difficulty","Proxy","CAPTCHA"), max-width column
    sizing, header + dashed-separator lines, return "No plugins loaded." when rows is empty; render
    `domain_patterns` as `", ".join(...)` and booleans via `str(...)`. Add
    `handle_plugins_list(args, svc: BotService) -> int`: call `rows = svc.list_plugins()`; when
    `getattr(args, "json", False)` print `json.dumps(rows, indent=2)`, else print `_format_plugins_table(rows)`;
    `return 0`. Then in `core/cli/__init__.py`: import `handle_plugins_list`, add a `plugins` block immediately
    after the items block — `plugins_p = sub.add_parser("plugins", ...)`, `plugins_sub = plugins_p.add_subparsers(dest="plugins_command")`,
    `list_p = plugins_sub.add_parser("list", ...)` with `list_p.add_argument("--json", action="store_true", default=False, ...)`
    and `list_p.set_defaults(func=handle_plugins_list)`, then
    `plugins_p.set_defaults(func=_require_subcommand(plugins_p))` so bare `shoppybot plugins` exits 2. Keep each
    function under 30 lines and the new file under 300 lines.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_plugins.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli_plugins.py -q` exits 0 with all 5 tests passing (RED → GREEN)
    - `core/cli/plugins.py` imports do NOT include `core.registry` (grep confirms MOD-02 compliance)
    - `core/cli/__init__.py` contains `handle_plugins_list` and a `plugins` add_parser with a `--json` argument
    - `python -m pytest tests/ -q` shows no new failures vs baseline (479 passed, 2 skipped)
  </acceptance_criteria>
  <done>
    shoppybot plugins list prints an aligned table; --json emits valid JSON; bare plugins exits 2; no network call; all Task 1 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → CLI (`shoppybot plugins list [--json]`) | Only input is the boolean `--json` flag (argparse store_true); no free-text user input reaches the shell or filesystem |
| CLI handler → BotService → registry | CLI reads local in-memory plugin metadata only; no network egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | `list_plugins()` output | accept | Output is non-secret plugin metadata (class name, domain patterns, difficulty, bool flags); no credentials, paths, or env values are emitted |
| T-15-04 | Tampering | MOD-02 layering (`core/cli/plugins.py`) | mitigate | Handler imports only `sys`, `json`, `BotService`; never `core.registry` — enforced by acceptance_criteria grep gate |
| T-15-05 | Denial of Service | network call from CLI | mitigate | `list_plugins()` reads `_all_plugins` only (no `setup()`/browser/socket); test_plugins_list_no_network asserts no socket is constructed |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (stdlib `argparse`/`json` only; no new dependencies) |
</threat_model>

<verification>
- `python -m pytest tests/test_cli_plugins.py -q` exits 0 (5 tests)
- `python -m pytest tests/ -q` shows no new failures vs baseline (479 passed, 2 skipped)
- `core/cli/plugins.py` does not import `core.registry` (MOD-02)
- `shoppybot plugins list` performs no network call (asserted by test)
</verification>

<success_criteria>
- REG-03 satisfied: `shoppybot plugins list` shows all loaded plugins (name, domain patterns, difficulty, proxy/captcha) with a `--json` variant and no network call
- Bare `shoppybot plugins` exits 2 without starting the bot
- No new dependency; MOD-02 layering preserved
</success_criteria>

<output>
Create `.planning/phases/15-plugin-ecosystem-registry/15-02-SUMMARY.md` when done
</output>
