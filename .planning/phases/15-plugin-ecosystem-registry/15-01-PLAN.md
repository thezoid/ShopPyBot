---
phase: 15-plugin-ecosystem-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/plugin_base.py
  - tests/test_plugin_base.py
autonomous: true
requirements: [REG-02]
must_haves:
  truths:
    - "Plugin authors can declare difficulty, requires_proxy, requires_captcha as class attributes"
    - "A plugin omitting these attrs loads with defaults medium / False / False"
    - "All 7 existing shopbot_plugin_*.py plugins still load unchanged"
    - "A plugin declaring an out-of-range difficulty value fails at class-definition time"
  artifacts:
    - path: "core/plugin_base.py"
      provides: "RetailerPlugin difficulty/requires_proxy/requires_captcha class attrs + __init_subclass__ validation"
      contains: "difficulty"
    - path: "tests/test_plugin_base.py"
      provides: "default, override, existing-plugin-smoke, and invalid-difficulty assertions"
      contains: "requires_proxy"
  key_links:
    - from: "core/plugin_base.py"
      to: "subclasses in plugins/"
      via: "class attribute inheritance"
      pattern: "difficulty.*Literal"
---

<objective>
Add three additive registry metadata class attributes to the `RetailerPlugin` ABC
(`difficulty`, `requires_proxy`, `requires_captcha`) with sensible defaults, plus an
`__init_subclass__` hook that validates `difficulty` against the three allowed values
at class-definition time. Closes REG-02.

Purpose: Gives plugin authors a machine-readable way to declare anti-detection
difficulty and infrastructure requirements. These attrs are read by the `plugins list`
CLI (Plan 02) and by the wiki registry doc (Plan 03).

Output: Modified `core/plugin_base.py`; extended `tests/test_plugin_base.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md

<interfaces>
<!-- Current ABC (the file being modified). Add attrs, do NOT touch abstract methods. -->
From core/plugin_base.py:
- `PLUGIN_API_VERSION = 2` (module-level constant; STAYS 2 — additive attrs are non-breaking per RESEARCH Pattern 1)
- `class RetailerPlugin(ABC)` with class attr `domain_patterns: list[str]`
- abstract async methods: `check_availability(self, url) -> bool`, `auto_buy(self, url) -> bool`
- no-op async defaults: `setup`, `teardown`, `login`, `detect_captcha`

From tests/test_plugin_base.py:
- `MinimalPlugin(RetailerPlugin)` module-level stub with `domain_patterns = ["example.com"]` and both abstract methods implemented — reuse it for default assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 3 registry attrs + __init_subclass__ difficulty validation to RetailerPlugin</name>
  <read_first>
    - core/plugin_base.py (the file being modified — current ABC structure, PLUGIN_API_VERSION line)
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md Pattern 1 (additive attrs) and Pitfall 3 (__init_subclass__ validation)
  </read_first>
  <behavior>
    - A subclass that declares nothing inherits difficulty == "medium", requires_proxy is False, requires_captcha is False
    - A subclass declaring difficulty = "easy" reports "easy"
    - A subclass declaring difficulty = "Easy" (wrong case) or any value outside {"easy","medium","hard"} raises ValueError at class-definition time (import-time fail-fast)
    - Existing abstract-method enforcement is unchanged (incomplete subclass still raises TypeError on instantiation)
  </behavior>
  <action>
    Import `Literal` from `typing`. Add three class attributes to `RetailerPlugin` directly under
    the existing `domain_patterns` declaration: `difficulty: Literal["easy", "medium", "hard"] = "medium"`,
    `requires_proxy: bool = False`, `requires_captcha: bool = False`. Add a `__init_subclass__(cls, **kwargs)`
    classmethod that calls `super().__init_subclass__(**kwargs)` then raises `ValueError` (message naming the
    offending class and the allowed set) when `cls.difficulty` is not in the frozenset
    `{"easy", "medium", "hard"}`. This is the enforcement point chosen per RESEARCH Open Question 1 and the
    locked CONTEXT decision — document in a short comment that import-time validation is intentional and that
    plugins omitting `difficulty` inherit the default and are never invalidated. Do NOT bump
    `PLUGIN_API_VERSION` (stays 2; RESEARCH Pattern 1 / Anti-Patterns confirm additive attrs need no bump).
    Do NOT alter the abstract methods or `__init__`. No fenced code in this file beyond the attrs themselves.
  </action>
  <verify>
    <automated>python -m pytest tests/test_plugin_base.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_plugin_base.py -q` exits 0
    - `core/plugin_base.py` contains the literal token `difficulty`, `requires_proxy`, `requires_captcha`, and `__init_subclass__`
    - `PLUGIN_API_VERSION` is still `== 2` (test_version_constant stays green)
    - `python -c "import core.plugin_base"` exits 0 (module imports clean)
  </acceptance_criteria>
  <done>
    RetailerPlugin exposes the 3 attrs with correct defaults; an invalid difficulty raises ValueError at subclass definition; PLUGIN_API_VERSION unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test_plugin_base.py with default, override, invalid, and existing-plugin-smoke assertions</name>
  <read_first>
    - tests/test_plugin_base.py (the file being modified — MinimalPlugin stub and existing assertions)
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md Phase Requirements to Test Map (REG-02 rows)
    - core/registry.py (_discover_plugins signature — for the existing-plugin smoke that loads real plugins via the registry)
  </read_first>
  <behavior>
    - test_defaults: MinimalPlugin(config=None) has difficulty == "medium", requires_proxy is False, requires_captcha is False
    - test_override: a subclass with difficulty = "hard", requires_proxy = True reports those values
    - test_invalid_difficulty_raises: defining a subclass with difficulty = "Easy" raises ValueError
    - test_existing_plugins_load: discovering the real plugins/ dir yields >= 7 plugin classes and every instance has a valid difficulty in {"easy","medium","hard"} (proves non-breaking load)
  </behavior>
  <action>
    Add four tests to `tests/test_plugin_base.py`. Reuse the module-level `MinimalPlugin` for the defaults test.
    For the override test, define a local complete subclass setting `difficulty = "hard"` and `requires_proxy = True`.
    For the invalid test, wrap the class definition in `pytest.raises(ValueError)`. For the existing-plugin smoke,
    call `core.registry._discover_plugins(Path(__file__).parent.parent / "plugins")`, assert the returned list has
    length >= 7, instantiate each with `config=None`, and assert each instance's `difficulty` is in the allowed set
    and `requires_proxy`/`requires_captcha` are bools. This directly proves CONTEXT success-criterion 1
    (existing plugins keep loading unchanged). No production code changes in this task.
  </action>
  <verify>
    <automated>python -m pytest tests/test_plugin_base.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_plugin_base.py -q` exits 0 with the 4 new tests collected and passing
    - The existing-plugin smoke asserts `len(...) >= 7` (matches the 7 shopbot_plugin_*.py files present)
    - No new failures vs the Phase 14 baseline (479 passed, 2 skipped) when the full suite runs
  </acceptance_criteria>
  <done>
    test_plugin_base.py proves defaults, override, import-time validation, and non-breaking load of all 7 existing plugins.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin author source → import | A plugin file's class body executes at discovery; the only new surface is the `difficulty` string compared against an allow-list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `difficulty` class attribute | mitigate | `__init_subclass__` validates against the 3-value allow-list at class-definition time; an invalid value fails the import, it cannot silently mask as a default |
| T-15-02 | Elevation | new class attrs (`requires_proxy`, `requires_captcha`) | accept | Pure boolean metadata read-only by CLI/docs; no privilege, no I/O, no network — no attack surface introduced |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new dependencies; stdlib `typing.Literal` only) |
</threat_model>

<verification>
- `python -m pytest tests/test_plugin_base.py -q` exits 0
- `python -m pytest tests/ -q` shows no new failures vs baseline (479 passed, 2 skipped)
- `core/plugin_base.py` unchanged abstract-method contract: `python -m pytest tests/test_plugin_base.py::test_abstract_methods_enforced -q` passes
</verification>

<success_criteria>
- REG-02 satisfied: difficulty/requires_proxy/requires_captcha declarable with defaults; invalid difficulty rejected at import
- All 7 existing plugins load unchanged
- PLUGIN_API_VERSION stays 2; no new dependency added
</success_criteria>

<output>
Create `.planning/phases/15-plugin-ecosystem-registry/15-01-SUMMARY.md` when done
</output>
