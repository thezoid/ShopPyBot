---
phase: 15-plugin-ecosystem-registry
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PLUGIN_REGISTRY.md
  - CONTRIBUTING.md
  - .github/PULL_REQUEST_TEMPLATE.md
  - plugins/PLUGIN_DEV.md
  - tests/test_docs.py
autonomous: true
requirements: [REG-01, REG-04]
must_haves:
  truths:
    - "docs/PLUGIN_REGISTRY.md specifies the wiki registry table with all 9 required fields"
    - "docs/PLUGIN_REGISTRY.md makes the in-repo-SPEC vs external-GitHub-wiki distinction explicit"
    - "CONTRIBUTING.md requires difficulty, requires_proxy, requires_captcha for plugin submissions"
    - "PR template has a structured difficulty/requires_proxy/requires_captcha checklist replacing the free-text Anti-detection risk field"
    - "plugins/PLUGIN_DEV.md ABC contract table lists the 3 new attributes"
  artifacts:
    - path: "docs/PLUGIN_REGISTRY.md"
      provides: "GitHub wiki registry table SPEC with 9 fields + in-repo-vs-wiki note"
      contains: "last-verified"
    - path: "CONTRIBUTING.md"
      provides: "difficulty/requires_proxy/requires_captcha checklist items"
      contains: "requires_proxy"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "structured difficulty/proxy/captcha checklist (replaces free-text risk field)"
      contains: "requires_captcha"
    - path: "plugins/PLUGIN_DEV.md"
      provides: "ABC table rows for difficulty, requires_proxy, requires_captcha"
      contains: "requires_proxy"
    - path: "tests/test_docs.py"
      provides: "doc-presence assertions for REG-01 and REG-04"
      contains: "PLUGIN_REGISTRY"
  key_links:
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "core/plugin_base.py attrs"
      via: "checklist names the 3 class attributes contributors must declare"
      pattern: "requires_proxy"
---

<objective>
Create the in-repo GitHub wiki registry table SPEC (`docs/PLUGIN_REGISTRY.md`) and update
the three contributor docs (`CONTRIBUTING.md`, `.github/PULL_REQUEST_TEMPLATE.md`,
`plugins/PLUGIN_DEV.md`) to require/document the new `difficulty`, `requires_proxy`,
`requires_captcha` attributes. Add `tests/test_docs.py` to lock the doc content. Closes
REG-01 (in-repo SPEC; live wiki population is manual) and REG-04.

Purpose: Gives maintainers a fixed registry-table spec and forces plugin PRs to supply
the structured metadata that the ABC (Plan 01) and CLI (Plan 02) consume.

Output: New `docs/PLUGIN_REGISTRY.md`, `tests/test_docs.py`; modified `CONTRIBUTING.md`,
`.github/PULL_REQUEST_TEMPLATE.md`, `plugins/PLUGIN_DEV.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md

<interfaces>
<!-- The 9 wiki registry fields (REG-01, fixed by CONTEXT/ROADMAP success-criterion 2): -->
<!-- name, platform, domain patterns, maintainer, anti-detection difficulty, -->
<!-- methods implemented, last-verified date, proxy-required, captcha-required -->

<!-- The 3 ABC attrs contributors must declare (from Plan 15-01): -->
<!-- difficulty (easy|medium|hard), requires_proxy (bool), requires_captcha (bool) -->

<!-- Pre-flagged: GitHub wiki is external; the in-repo file is the SPEC only (STATE.md Pitfall 5.1). -->

From .github/PULL_REQUEST_TEMPLATE.md (modify — current state, RESEARCH Pitfall 6):
- Checklist item "Anti-detection risk declared" (free-text `Anti-detection risk:` + `Rationale:`) at lines 34-42
- "Risk Declaration" section at the bottom (lines 53-58) with the free-text fields — REPLACE this with the structured checklist, do NOT add a second risk section

From CONTRIBUTING.md (modify — "Plugin Submission Checklist", lines 64-98):
- Existing "Anti-detection risk declaration" checklist item (lines 86-92) — augment/replace with the 3 structured fields

From plugins/PLUGIN_DEV.md (modify — section 2 "ABC Contract" table, lines 51-58):
- Method table with columns Method | Required | Signature | Default — add 3 attribute rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/PLUGIN_REGISTRY.md wiki registry table SPEC (REG-01)</name>
  <read_first>
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md Pitfall 5 (in-repo SPEC vs external wiki) and the 9-field list in user_constraints
    - .planning/STATE.md Pitfall 5.1 line (GitHub wiki is directory listing only; contract docs stay in-repo)
    - plugins/PLUGIN_DEV.md (tone/format reference for an in-repo doc)
  </read_first>
  <action>
    Create `docs/PLUGIN_REGISTRY.md`. It is the SPEC for the external GitHub wiki registry table — open with an
    explicit note that this file is the in-repo specification and that the actual wiki page is created/populated
    manually by a maintainer on the GitHub wiki (per STATE.md Pitfall 5.1; the wiki is not part of this repo). Define
    the registry table with exactly these 9 required columns: name, platform, domain patterns, maintainer,
    anti-detection difficulty, methods implemented, last-verified date, proxy-required, captcha-required. Give a
    one-line description of each field (e.g. anti-detection difficulty = one of easy/medium/hard matching the
    plugin's `difficulty` attr; proxy-required mirrors `requires_proxy`; captcha-required mirrors
    `requires_captcha`; methods implemented = which RetailerPlugin methods the plugin overrides). Include one
    illustrative example row using the existing Amazon plugin. State that maintainers populate/update the live wiki
    table when a plugin PR merges. Do NOT claim the wiki page already exists.
  </action>
  <verify>
    <automated>python -c "import re; t=open('docs/PLUGIN_REGISTRY.md',encoding='utf-8').read().lower(); missing=[f for f in ['platform','domain patterns','maintainer','difficulty','methods implemented','last-verified','proxy-required','captcha-required'] if f not in t]; assert not missing, missing; assert 'wiki' in t; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PLUGIN_REGISTRY.md` exists and the inline check prints `ok` (all 9 field names present, lowercased)
    - The file contains the word "wiki" and states the in-repo-SPEC vs external-wiki distinction
    - The file does NOT claim the wiki page already exists (it describes population as a maintainer action)
  </acceptance_criteria>
  <done>
    docs/PLUGIN_REGISTRY.md specifies the 9-field registry table and makes the in-repo-SPEC / external-wiki distinction explicit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CONTRIBUTING.md, PR template, and PLUGIN_DEV.md to require/document the 3 attrs (REG-04)</name>
  <read_first>
    - CONTRIBUTING.md (the file being modified — Plugin Submission Checklist lines 64-98, especially the Anti-detection risk item)
    - .github/PULL_REQUEST_TEMPLATE.md (the file being modified — checklist item lines 34-42 and Risk Declaration lines 53-58; RESEARCH Pitfall 6 says REPLACE the free-text field, do not duplicate)
    - plugins/PLUGIN_DEV.md (the file being modified — ABC Contract table lines 51-58)
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md State of the Art table and Open Question 2 (PLUGIN_DEV.md is in scope)
  </read_first>
  <action>
    Three edits, each naming the literal attribute tokens `difficulty`, `requires_proxy`, `requires_captcha`:
    (1) CONTRIBUTING.md — in the Plugin Submission Checklist, replace the free-text "Anti-detection risk
    declaration" item with a structured item requiring the contributor to declare `difficulty`
    (`easy`/`medium`/`hard`), `requires_proxy` (true/false), and `requires_captcha` (true/false) as class
    attributes on the plugin, and to add the plugin's row to the wiki registry (link to
    `docs/PLUGIN_REGISTRY.md`).
    (2) .github/PULL_REQUEST_TEMPLATE.md — REPLACE the free-text "Anti-detection risk" checklist item (lines 34-42)
    AND the bottom "Risk Declaration" section (lines 53-58) with a single structured checklist block requiring
    `difficulty: easy|medium|hard`, `requires_proxy: true|false`, `requires_captcha: true|false`. Do NOT leave a
    second/duplicate risk section (Pitfall 6).
    (3) plugins/PLUGIN_DEV.md — add three rows to the section-2 ABC Contract table for `difficulty` (Required: no,
    Signature: class attr `Literal["easy","medium","hard"]`, Default: `"medium"`), `requires_proxy` (Default:
    `False`), `requires_captcha` (Default: `False`); add a sentence that an invalid `difficulty` value fails at
    import (matches Plan 15-01 `__init_subclass__`). Keep edits minimal and within existing doc structure; no
    unrelated rewrites.
  </action>
  <verify>
    <automated>python -c "tokens=['difficulty','requires_proxy','requires_captcha']; [exit('MISSING in '+f) for f in ['CONTRIBUTING.md','.github/PULL_REQUEST_TEMPLATE.md','plugins/PLUGIN_DEV.md'] for tok in tokens if tok not in open(f,encoding='utf-8').read()]; assert open('.github/PULL_REQUEST_TEMPLATE.md',encoding='utf-8').read().count('Risk Declaration')<=0 or True; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The inline check prints `ok`: all three files contain `difficulty`, `requires_proxy`, and `requires_captcha`
    - `.github/PULL_REQUEST_TEMPLATE.md` does NOT contain two separate risk sections (the free-text `Anti-detection risk:` / `Rationale:` block is removed, not duplicated — verify by reading the file)
    - `plugins/PLUGIN_DEV.md` ABC table has rows for all three attributes
  </acceptance_criteria>
  <done>
    CONTRIBUTING.md and the PR template require the 3 structured attrs; PLUGIN_DEV.md documents them in the ABC table; no duplicate risk section remains.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests/test_docs.py locking REG-01 and REG-04 doc content</name>
  <read_first>
    - tests/test_cli_items.py (test-file conventions: plain pytest functions, no fixtures needed for file reads)
    - docs/PLUGIN_REGISTRY.md, CONTRIBUTING.md, .github/PULL_REQUEST_TEMPLATE.md, plugins/PLUGIN_DEV.md (the docs being asserted — created/modified in Tasks 1-2)
    - .planning/phases/15-plugin-ecosystem-registry/15-RESEARCH.md Phase Requirements to Test Map (REG-01, REG-04 doc-presence rows)
  </read_first>
  <action>
    Create `tests/test_docs.py` (the file does not currently exist — confirmed). Resolve the repo root as
    `Path(__file__).parent.parent`. Add:
    (1) test_plugin_registry_doc_has_9_fields — read `docs/PLUGIN_REGISTRY.md` lowercased; assert each of the 9
    field names (platform, domain patterns, maintainer, difficulty, methods implemented, last-verified,
    proxy-required, captcha-required, and a name/header reference) is present, and assert "wiki" is present.
    (2) test_contributing_requires_attrs — assert `CONTRIBUTING.md` contains `difficulty`, `requires_proxy`,
    `requires_captcha`.
    (3) test_pr_template_requires_attrs — assert `.github/PULL_REQUEST_TEMPLATE.md` contains the same 3 tokens.
    (4) test_plugin_dev_documents_attrs — assert `plugins/PLUGIN_DEV.md` contains the same 3 tokens.
    Pure file-content reads; no network, no DB, no fixtures.
  </action>
  <verify>
    <automated>python -m pytest tests/test_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_docs.py -q` exits 0 with 4 tests passing
    - `tests/test_docs.py` contains the token `PLUGIN_REGISTRY`
    - `python -m pytest tests/ -q` shows no new failures vs baseline (479 passed, 2 skipped)
  </acceptance_criteria>
  <done>
    tests/test_docs.py asserts the 9 registry fields plus the 3-attr requirement across all three contributor docs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor PR → maintainer review | Docs are advisory text; the only enforcement is human PR review against the checklist. No code path, no runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Repudiation | plugin submission metadata | mitigate | PR template + CONTRIBUTING.md require explicit `difficulty`/`requires_proxy`/`requires_captcha` declaration, creating a reviewable record per plugin PR |
| T-15-07 | Information Disclosure | docs/PLUGIN_REGISTRY.md example row | accept | Example uses only public plugin metadata (Amazon plugin name + public domain); no secrets, no maintainer PII beyond a placeholder |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (Markdown docs + a stdlib-only pytest file; no new dependencies) |
</threat_model>

<verification>
- `python -m pytest tests/test_docs.py -q` exits 0 (4 tests)
- `python -m pytest tests/ -q` shows no new failures vs baseline (479 passed, 2 skipped)
- The 9 registry fields are present in docs/PLUGIN_REGISTRY.md; the 3 attrs are present in all three contributor docs
- No duplicate risk section remains in the PR template
</verification>

<success_criteria>
- REG-01 satisfied (in-repo SPEC): docs/PLUGIN_REGISTRY.md specifies the 9-field wiki registry table; live wiki population flagged manual/human-verify in must_haves
- REG-04 satisfied: CONTRIBUTING.md and the PR template require the 3 structured attrs; PLUGIN_DEV.md documents them
- No new dependency; doc edits are minimal and scoped
</success_criteria>

<output>
Create `.planning/phases/15-plugin-ecosystem-registry/15-03-SUMMARY.md` when done
</output>
