---
phase: 13-anti-detection-layer-1-fingerprint-proxy
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - core/service.py
  - core/orchestrator.py
  - core/registry.py
  - plugins/shopbot_plugin_amazon.py
  - plugins/shopbot_plugin_bestbuy.py
  - plugins/shopbot_plugin_walmart.py
  - plugins/shopbot_plugin_target.py
  - plugins/shopbot_plugin_gamestop.py
  - plugins/shopbot_plugin_squareenix.py
  - plugins/shopbot_plugin_newegg.py
  - tests/test_proxy_wiring.py
autonomous: true
requirements: [ANTI-04, ANTI-05, ANTI-08]
must_haves:
  truths:
    - "BotService logs 'Proxy rotation: enabled, pool_size=N' (no credentials) when cfg.proxy.enabled is True"
    - "When proxy is disabled no ProxyPool is built and no proxy log line is emitted"
    - "Each plugin instance is assigned its own proxy (self._proxy) at setup; proxy rotates only at browser restart, never mid-session"
    - "Every plugin's setup() calls apply_stealth(main_tab) immediately after nodriver.start(), before first navigation"
    - "When proxy is enabled, setup() launches with proxy + WebRTC browser_args and registers Fetch auth for credentialed proxies"
    - "When proxy.enabled is True and the pool is exhausted (all retired), setup() fails loudly instead of launching direct"
    - "Ban signals in check_availability record a failure on self._proxy via the pool (rotation deferred to next restart)"
    - "The plugin ABC (core/plugin_base.py) is unchanged — no PLUGIN_API_VERSION bump"
  artifacts:
    - path: "core/service.py"
      provides: "Exact 'Proxy rotation: enabled, pool_size=N' startup log line"
      contains: "pool_size="
    - path: "core/orchestrator.py"
      provides: "ProxyPool construction from cfg.proxy + pass to registry + assign in _staggered_setup"
      contains: "ProxyPool"
    - path: "core/registry.py"
      provides: "proxy_pool param + assign_proxy(plugin) helper (sets _proxy/_pool/_proxy_required)"
      contains: "def assign_proxy"
    - path: "tests/test_proxy_wiring.py"
      provides: "Unit coverage for startup log, per-instance scoping, disabled-default, fail-loud, stealth-at-setup, ban-detect"
      contains: "def test_proxy_startup_log_no_credentials"
  key_links:
    - from: "core/orchestrator.py async_main"
      to: "ProxyPool.from_urls(cfg.proxy.urls)"
      via: "cfg.proxy.enabled gate"
      pattern: "ProxyPool"
    - from: "registry.assign_proxy / _staggered_setup"
      to: "plugin._proxy = pool.advance()"
      via: "per-instance assignment before plugin.setup()"
      pattern: "_proxy"
    - from: "plugin.setup()"
      to: "apply_stealth(self.driver.main_tab)"
      via: "after nodriver.start()"
      pattern: "apply_stealth"
---

<objective>
Wire the Plan 01 helpers (`apply_stealth`, `ProxyPool`, `build_proxy_browser_args`, `setup_proxy_auth`, `_is_ban_response`) and the Plan 02 `cfg.proxy` config into the live bot. Emit the exact startup log line from BotService, construct the shared `ProxyPool` in `async_main` and pass it to the registry, assign a per-instance `self._proxy` to each plugin at setup time (rotation only at restart), call `apply_stealth` in every plugin's `setup()`, launch with proxy + WebRTC args when enabled, register Fetch auth for credentialed proxies, fail loudly when the pool is exhausted, and record ban-signal failures in `check_availability`. No plugin ABC version bump.

Purpose: Deliver ANTI-08 (stealth at every startup), ANTI-04 (opt-in proxy, per-instance scoping, WebRTC, startup log), ANTI-05 (ban detection + rotate-at-restart + retire/cooldown) end to end.

Output: edits to `core/service.py`, `core/orchestrator.py`, `core/registry.py`, all 8 plugins, and `tests/test_proxy_wiring.py`. Suite stays green (baseline 359 passed, 2 skipped + Plan 01/02 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-CONTEXT.md
@.planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-RESEARCH.md
@core/stealth.py
@core/plugin_base.py

<interfaces>
<!-- Consumed from Plan 01 (core/stealth.py) and Plan 02 (core/config_schema.py). -->

From core/stealth.py (Plan 01):
  async def apply_stealth(tab) -> None
  class ProxyPool:
    @classmethod from_urls(urls, max_failures=3, cooldown_secs=300.0) -> ProxyPool
    def current() -> Optional[_ProxyEntry]
    def advance() -> Optional[_ProxyEntry]   # None when all retired
    def record_failure(entry) / record_success(entry)
    def size() -> int                          # for pool_size=N
  def build_proxy_browser_args(entry) -> list[str]   # [] when entry is None
  async def setup_proxy_auth(tab, username, password) -> None  # no-op if username empty
  def _is_ban_response(status_code, body_text) -> bool
  _ProxyEntry has: .host_port (no creds), .username, .password, .url (NEVER log)

From core/config_schema.py (Plan 02):
  cfg.proxy.enabled: bool         # default False
  cfg.proxy.urls: list[str]
  cfg.proxy.max_failures: int     # default 3
  cfg.proxy.cooldown_secs: float  # default 300.0

EXISTING integration points (source-read — facts, not guesses):
  - core/service.py BotService.__init__ (line 40) calls init_store(self._cfg). Add the proxy startup-log emission right after, gated on self._cfg.proxy.enabled. (BotService does NOT construct the registry — async_main does.)
  - core/orchestrator.py async_main (line 224) constructs `registry = PluginRegistry(cfg, plugins_dir)`. Build the ProxyPool here and pass it in.
  - core/orchestrator.py _staggered_setup (lines 170-191) calls `await plugin.setup()` directly (NOT registry.setup_for_items). This is THE assignment point for live runs — call registry.assign_proxy(plugin) before each plugin.setup() here.
  - core/registry.py PluginRegistry.__init__ (lines 67-71) + setup_for_items (lines 109-126).
  - plugins/*.py setup(): each ends with `self.driver = await nodriver.start(headless=..., [browser_args=...])`. Amazon/BestBuy currently pass NO browser_args; the 5 Phase-6 plugins build a UA browser_args list.
  - RetailerPlugin.__init__ (core/plugin_base.py) sets self.driver=None only. DO NOT edit it (no ABC bump). Plugins read proxy state via getattr(self, "_proxy", None) / getattr(self, "_proxy_required", False) / getattr(self, "_pool", None) — registry sets these as dynamic instance attributes (non-breaking, no ABC change).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProxyPool construction (service log + orchestrator) + registry.assign_proxy</name>
  <files>core/service.py, core/orchestrator.py, core/registry.py, tests/test_proxy_wiring.py</files>
  <read_first>
    - core/service.py (BotService.__init__ lines 32-40; init_store placement is the model)
    - core/orchestrator.py (async_main lines 219-228 registry construction; _staggered_setup lines 170-191 — the live setup path)
    - core/registry.py (PluginRegistry.__init__ lines 67-71; setup_for_items lines 109-126)
    - core/stealth.py (ProxyPool.from_urls, advance, current, size — Plan 01 contract)
    - core/config_schema.py (cfg.proxy fields — Plan 02 contract)
    - tests/test_service.py, tests/test_registry.py, tests/test_orchestrator.py (existing construction + caplog patterns)
  </read_first>
  <behavior>
    - test_proxy_startup_log_no_credentials: cfg.proxy.enabled=True, urls=["http://u:secret@h1:1","http://h2:2"] -> BotService(cfg) emits EXACTLY "Proxy rotation: enabled, pool_size=2"; assert "secret", "u:", "@h1" never appear in caplog.
    - test_proxy_disabled_no_log: cfg.proxy.enabled=False -> no "Proxy rotation" line in caplog.
    - test_registry_assign_proxy_distinct_per_plugin: PluginRegistry(cfg, dir, proxy_pool=pool_of_3) -> assign_proxy(pluginA) then assign_proxy(pluginB) sets distinct host_port on pluginA._proxy / pluginB._proxy (round-robin), proving per-instance scoping (Pitfall 6); both have _proxy_required True and _pool is the pool.
    - test_assign_proxy_noop_when_pool_none: proxy_pool=None -> assign_proxy leaves plugin._proxy None and plugin._proxy_required False; no error.
    - test_assign_proxy_exhausted_sets_required_but_none: pool with all entries retired -> assign_proxy sets plugin._proxy_required True and plugin._proxy None (so setup can fail loud — Pitfall 2).
  </behavior>
  <action>
    `core/service.py`: in BotService.__init__ after `init_store(self._cfg)`, guard — if `self._cfg.proxy.enabled`, compute n via `ProxyPool.from_urls(self._cfg.proxy.urls, ...).size()` (or `len(self._cfg.proxy.urls)`) and `writeLog(f"Proxy rotation: enabled, pool_size={n}", "INFO")` — EXACT string per CONTEXT lock, integer only, NO credentials. Do not store the live pool on BotService (async_main owns it). Import ProxyPool from core.stealth.
    `core/orchestrator.py`: in async_main after constructing `registry`, build `proxy_pool = ProxyPool.from_urls(cfg.proxy.urls, cfg.proxy.max_failures, cfg.proxy.cooldown_secs) if cfg.proxy.enabled else None` and pass it to the registry via the new __init__ param (reconstruct `registry = PluginRegistry(cfg, plugins_dir, proxy_pool=proxy_pool)`). In `_staggered_setup`, before each `await plugin.setup()`, call `registry.assign_proxy(plugin)`. Import ProxyPool from core.stealth. Keep functions <30 lines.
    `core/registry.py`: add `proxy_pool=None` to `PluginRegistry.__init__`, store `self._proxy_pool`. Add `def assign_proxy(self, plugin) -> None`: if `self._proxy_pool is None`, set `plugin._proxy_required = False`, `plugin._proxy = None`, return; else set `plugin._proxy_required = True`, `plugin._pool = self._proxy_pool`, `plugin._proxy = self._proxy_pool.advance() or self._proxy_pool.current()` (may be None on exhaustion — the fail-loud signal). Also call `assign_proxy(plugin)` in `setup_for_items` before `await plugin.setup()`. Never log proxy.url.
    Create `tests/test_proxy_wiring.py` with the 5 named tests.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_proxy_wiring.py tests/test_service.py tests/test_registry.py tests/test_orchestrator.py -q` exits 0.
    - Behavior assertion: BotService(cfg with proxy.enabled) emits exactly "Proxy rotation: enabled, pool_size=N"; no credential substring in caplog.
    - Source assertion: core/registry.py defines `def assign_proxy`; core/orchestrator.py references `ProxyPool`; core/service.py contains the literal `Proxy rotation: enabled, pool_size=`.
    - Behavior assertion: assign_proxy yields distinct _proxy per plugin and sets _proxy_required True only when a pool exists.
    - No ABC bump: PLUGIN_API_VERSION still 2. Full suite green.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_proxy_wiring.py tests/test_service.py tests/test_registry.py tests/test_orchestrator.py -q</automated>
  </verify>
  <done>BotService emits the exact log line (no creds) only when enabled; async_main builds the pool and assigns per-plugin; registry.assign_proxy sets distinct _proxy + _pool + _proxy_required (and None+required on exhaustion). 5 tests green. No ABC change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire amazon + bestbuy setup() (stealth + proxy + WebRTC + auth) and ban-detect</name>
  <files>plugins/shopbot_plugin_amazon.py, plugins/shopbot_plugin_bestbuy.py, tests/test_proxy_wiring.py</files>
  <read_first>
    - plugins/shopbot_plugin_amazon.py (setup lines 54-65; check_availability lines 84-111 — ban-detect goes in result/except path)
    - plugins/shopbot_plugin_bestbuy.py (setup lines 39-50; check_availability)
    - core/stealth.py (apply_stealth, build_proxy_browser_args, setup_proxy_auth, _is_ban_response — Plan 01 contract)
    - .planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-RESEARCH.md (Pitfall 2 fail-loud; Pitfall 8 stealth before first nav; Open Question 1 body-scan ban detection)
    - tests/test_plugin_amazon.py, tests/test_plugin_bestbuy.py (existing setup/check mock patterns)
  </read_first>
  <behavior>
    - test_amazon_setup_applies_stealth: nodriver.start mocked to return a browser whose main_tab is a mock; assert apply_stealth awaited on main_tab AFTER start and BEFORE any tab.get.
    - test_amazon_setup_proxy_enabled_args_and_webrtc: self._proxy set to a credentialed entry, _proxy_required True -> nodriver.start browser_args contains --proxy-server=<host_port> AND --force-webrtc-ip-handling-policy=disable_non_proxied_udp; setup_proxy_auth awaited with entry.username/entry.password.
    - test_amazon_setup_proxy_disabled_no_proxy_args: _proxy None, _proxy_required False -> browser_args has no --proxy-server; apply_stealth still awaited.
    - test_amazon_setup_fails_loud_when_exhausted: _proxy_required True and _proxy None -> setup logs ERROR and raises; assert nodriver.start NOT called (Pitfall 2).
    - test_amazon_ban_signal_records_failure: a check_availability run observing a ban (block-phrase body via _is_ban_response) with self._proxy + self._pool set -> self._pool.record_failure(self._proxy) called once; assert NO teardown/re-setup mid-session (restart-only).
    - bestbuy mirrors the amazon stealth/proxy/auth/fail-loud tests.
  </behavior>
  <action>
    In `shopbot_plugin_amazon.py` and `shopbot_plugin_bestbuy.py` setup(): (1) FIRST, fail-loud guard BEFORE nodriver.start — `if getattr(self, "_proxy_required", False) and getattr(self, "_proxy", None) is None: writeLog("Proxy enabled but pool exhausted -- refusing direct launch", "ERROR"); raise RuntimeError(...)`. (2) `proxy = getattr(self, "_proxy", None)`; `browser_args = build_proxy_browser_args(proxy)` ([] when None). (3) `self.driver = await nodriver.start(headless=headless, browser_args=browser_args or None)`. (4) `await apply_stealth(self.driver.main_tab)` (Pitfall 8 — before any get). (5) `if proxy and proxy.username: await setup_proxy_auth(self.driver.main_tab, proxy.username, proxy.password)`.
    In check_availability of both: after navigation, read `document.body.innerText` (via the tab's existing evaluate/get pattern) and the final URL; on exception or when `_is_ban_response(status_or_0, body_text)` is True, and `getattr(self, "_proxy", None)` and `getattr(self, "_pool", None)` are set, call `self._pool.record_failure(self._proxy)` — do NOT rotate here (restart-only). Body-scan only (RESEARCH Open Question 1); no cdp.network handler. Log only `exc.__class__.__name__` on proxy paths (Pitfall 9). Keep each setup() <30 lines (inline the getattr calls). Add tests to tests/test_proxy_wiring.py.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_proxy_wiring.py tests/test_plugin_amazon.py tests/test_plugin_bestbuy.py -q` exits 0.
    - Behavior assertion: apply_stealth is awaited on main_tab before any tab.get in both plugins' setup().
    - Behavior assertion: when proxy assigned, browser_args includes --proxy-server and the WebRTC flag; setup_proxy_auth called for credentialed proxies.
    - Behavior assertion: when _proxy_required and _proxy is None, nodriver.start is NOT called (fail-loud).
    - Behavior assertion: a ban-signal check calls pool.record_failure exactly once with no mid-session teardown/setup.
    - No regression; full suite green.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_proxy_wiring.py tests/test_plugin_amazon.py tests/test_plugin_bestbuy.py -q</automated>
  </verify>
  <done>Amazon + BestBuy: stealth before first nav, proxy+WebRTC args when assigned, Fetch auth for credentialed proxies, fail-loud on exhaustion, ban failure recorded without mid-session rotation. Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the 5 Phase-6 plugins (walmart, target, gamestop, squareenix, newegg)</name>
  <files>plugins/shopbot_plugin_walmart.py, plugins/shopbot_plugin_target.py, plugins/shopbot_plugin_gamestop.py, plugins/shopbot_plugin_squareenix.py, plugins/shopbot_plugin_newegg.py, tests/test_proxy_wiring.py</files>
  <read_first>
    - plugins/shopbot_plugin_walmart.py (setup lines 39-65 — already builds UA browser_args; proxy args MERGE with UA args)
    - plugins/shopbot_plugin_target.py, _gamestop, _squareenix, _newegg (setup ~lines 41-67; same UA browser_args + nodriver.start pattern)
    - core/stealth.py (apply_stealth, build_proxy_browser_args, setup_proxy_auth — Plan 01 contract)
    - tests/test_plugin_walmart.py + the other 4 per-plugin tests (existing UA/headless setup mock patterns)
  </read_first>
  <behavior>
    - test_<platform>_setup_applies_stealth: each of walmart/target/gamestop/squareenix/newegg awaits apply_stealth(main_tab) after nodriver.start, before navigation.
    - test_<platform>_setup_merges_proxy_with_ua: self._proxy set -> nodriver.start browser_args contains the existing --user-agent=... AND --proxy-server=<host_port> AND the WebRTC flag.
    - test_<platform>_setup_proxy_disabled_keeps_ua_only: self._proxy None -> browser_args has --user-agent and no --proxy-server; apply_stealth still awaited.
    - test_<platform>_fails_loud_when_exhausted: _proxy_required True + _proxy None -> ERROR + raise + nodriver.start NOT called.
  </behavior>
  <action>
    Apply the identical edit shape to all 5 Phase-6 plugin setup() bodies: (1) fail-loud guard BEFORE nodriver.start (same as Task 2). (2) they already build a `browser_args` UA list — extend it: `browser_args = (browser_args or []) + build_proxy_browser_args(getattr(self, "_proxy", None))` (returns [] when no proxy, so the UA-only path is behaviorally unchanged). (3) keep `self.driver = await nodriver.start(headless=headless, browser_args=browser_args)`. (4) after start: `await apply_stealth(self.driver.main_tab)`. (5) `proxy = getattr(self, "_proxy", None); if proxy and proxy.username: await setup_proxy_auth(self.driver.main_tab, proxy.username, proxy.password)`. No ABC change; all proxy state via getattr defaults. Each setup() stays <30 lines. Add per-platform tests to tests/test_proxy_wiring.py.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_proxy_wiring.py tests/test_plugin_walmart.py tests/test_plugin_target.py tests/test_plugin_gamestop.py tests/test_plugin_squareenix.py tests/test_plugin_newegg.py -q` exits 0.
    - Behavior assertion: each of the 5 plugins awaits apply_stealth before navigation.
    - Behavior assertion: with a proxy assigned, browser_args contains BOTH --user-agent and --proxy-server and the WebRTC flag; with no proxy, UA-only and no --proxy-server.
    - Behavior assertion: _proxy_required + _proxy None -> nodriver.start NOT called.
    - No ABC bump; full suite green.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_proxy_wiring.py tests/test_plugin_walmart.py tests/test_plugin_target.py tests/test_plugin_gamestop.py tests/test_plugin_squareenix.py tests/test_plugin_newegg.py -q</automated>
  </verify>
  <done>All 5 Phase-6 plugins: stealth before first nav, proxy+WebRTC merged with UA args when assigned, UA-only when disabled, fail-loud on exhaustion. Tests green. No ABC change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async_main → ProxyPool | Proxy credentials from cfg cross into pool + browser launch; must never reach logs |
| retailer page (untrusted) → check_availability ban-detect | Untrusted status/body scanned; spoofed phrases only inflate a failure count |
| Chrome ← proxy 407 challenge | setup_proxy_auth answers with stored credentials via CDP Fetch |
| WebRTC UDP → network | Real-IP leak around the proxy if the launch flag is missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Information Disclosure | "Proxy rotation: enabled, pool_size=N" startup log | mitigate | Log carries only the integer size; test asserts no credential substring in caplog. host_port-only elsewhere. |
| T-13-10 | Spoofing / Information Disclosure | silent direct-connection fallback when pool exhausted | mitigate | _proxy_required gate + fail-loud RuntimeError BEFORE nodriver.start; tests assert nodriver.start NOT called (Pitfall 2). |
| T-13-07 | Information Disclosure | WebRTC real-IP leak | mitigate | build_proxy_browser_args always appends disable_non_proxied_udp; per-plugin tests assert the flag is present when proxy enabled (ANTI-04 SC4). |
| T-13-02 | Information Disclosure | proxy exception paths in setup/check | mitigate | All proxy-path excepts log exc.__class__.__name__ only (existing plugin convention; Pitfall 9). proxy.url never logged. |
| T-13-11 | Tampering | untrusted body spoofing ban phrases | accept | Worst case: an extra record_failure -> eventual rotate at restart. Restart-only rotation bounds blast radius; no privilege/data exposure. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs. RESEARCH audit: no new packages (nodriver 0.50.3 pinned + approved). |
</threat_model>

<verification>
- `python -m pytest tests/test_proxy_wiring.py -q` exits 0.
- `python -m pytest -q` exits 0, no regression vs baseline (359 passed, 2 skipped + Plan 01/02/03 tests).
- No ABC bump: `python -c "import core.plugin_base as p; assert p.PLUGIN_API_VERSION==2; print('OK')"`.
- Exact startup string present: `python -c "assert 'Proxy rotation: enabled, pool_size=' in open('core/service.py').read(); print('OK')"`.
- `python -m pytest tests/test_no_plaintext.py -q` exits 0 (no credential leak to logs).
</verification>

<success_criteria>
- Stealth applied at every browser startup across all 8 plugins (ANTI-08); no ABC version bump.
- Opt-in proxy: per-instance self._proxy, WebRTC flag, exact startup log, fail-loud on exhaustion (ANTI-04).
- Ban signals recorded; rotation only at restart via pool advance (ANTI-05).
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-03-SUMMARY.md` when done.
</output>
