---
phase: 14-anti-detection-layer-2-captcha-solving
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - plugins/shopbot_plugin_amazon.py
  - plugins/shopbot_plugin_bestbuy.py
  - tests/test_captcha_plugin.py
autonomous: true
requirements: [ANTI-06, ANTI-07]
must_haves:
  truths:
    - "When a CAPTCHA is detected and a usable solver is present, the plugin attempts an automated reCAPTCHA v2 solve before pausing"
    - "The solve call is wrapped in run_in_executor + asyncio.timeout(120) so other plugin poll tasks are not blocked"
    - "On empty sitekey, solve failure, timeout, zero balance, or cap-hit, the plugin falls back to the EXISTING manual-pause path (_wait_user_action) — never a silent skip"
    - "Amazon WAF (window.gokuProps present) gracefully falls back to manual pause (automated WAF injection is deferred)"
    - "The solved token is validated non-empty and contains no quote/newline before JS injection"
  artifacts:
    - path: "plugins/shopbot_plugin_amazon.py"
      provides: "reCAPTCHA solve path in the CAPTCHA branch with manual-pause fallback + WAF graceful fallback"
      contains: "_captcha_solver"
    - path: "plugins/shopbot_plugin_bestbuy.py"
      provides: "best-effort reCAPTCHA solve path with manual-pause fallback"
      contains: "_captcha_solver"
    - path: "tests/test_captcha_plugin.py"
      provides: "plugin solve-path + fallback unit tests (mocked solver + tab)"
  key_links:
    - from: "plugins/shopbot_plugin_amazon.py check_availability"
      to: "self._captcha_solver.solve_recaptcha"
      via: "run_in_executor under asyncio.timeout(120)"
      pattern: "run_in_executor"
    - from: "plugins/shopbot_plugin_amazon.py solve failure"
      to: "self._wait_user_action(self.captcha_event, ...)"
      via: "existing manual-pause fallback"
      pattern: "_wait_user_action"
---

<objective>
Use the injected CaptchaSolver in the plugin CAPTCHA path: extract the reCAPTCHA v2
sitekey, solve it via run_in_executor under asyncio.timeout(120), inject the token,
and on ANY failure (empty sitekey, error, timeout, zero balance, cap-hit, or Amazon
WAF) fall back to the existing manual-pause behavior. Reuse `_wait_user_action`; do
not duplicate it.

Purpose: Deliver the user-visible capability of ANTI-06/07 — opt-in automated solving
that degrades safely to the manual pause that already exists.
Output: solve-path integration in Amazon + BestBuy plugins with full fallback coverage
and unit tests (mocked solver + tab).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-anti-detection-layer-2-captcha-solving/14-CONTEXT.md
@.planning/phases/14-anti-detection-layer-2-captcha-solving/14-RESEARCH.md
@.planning/phases/14-anti-detection-layer-2-captcha-solving/14-02-SUMMARY.md

<interfaces>
<!-- The existing CAPTCHA branch + the solver API the executor uses directly. -->

From plugins/shopbot_plugin_amazon.py — the EXISTING CAPTCHA branch to extend
(check_availability lines 95-107; _wait_user_action lines 35-53; detect_captcha lines 85-93):
```python
captcha_present = await self.detect_captcha()
if captcha_present:
    await self._wait_user_action(
        self.captcha_event,
        "CAPTCHA detected on Amazon. Solve it in the browser, then press Enter.",
    )
```
self._captcha_solver is injected by registry.assign_solver (Plan 02); may be None.
self.captcha_event already exists. detect_captcha matches the TEXT captcha
("Enter the characters you see below") — NOT reCAPTCHA and NOT WAF (Pitfall 5).

From core/captcha.py (Plan 01):
```python
def can_solve(self) -> bool: ...
def solve_recaptcha(self, sitekey, pageurl) -> str: ...   # BLOCKING — wrap in run_in_executor
def solve_amazon_waf(self, key, iv, context, pageurl) -> dict: ...  # deferred — not called this phase
```

reCAPTCHA sitekey extraction + token injection (RESEARCH Pattern 5 / Code Examples):
```python
sitekey = await tab.evaluate("document.querySelector('[data-sitekey]')?.getAttribute('data-sitekey') || ''")
# inject: set #g-recaptcha-response value + fire ___grecaptcha_cfg client callbacks (see RESEARCH)
```

Async solve wrapper (RESEARCH Pattern 4 — timeout wraps ONLY the executor call, Pitfall 3):
```python
async with asyncio.timeout(120):
    token = await loop.run_in_executor(None, solver.solve_recaptcha, sitekey, pageurl)
```

BestBuy plugin (plugins/shopbot_plugin_bestbuy.py): inherits detect_captcha no-op default
(returns False) and has NO captcha_event yet — add an asyncio.Event in __init__ to support
the manual-pause fallback, mirroring the Amazon plugin's event pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amazon plugin reCAPTCHA solve path + manual-pause + WAF graceful fallback</name>
  <files>plugins/shopbot_plugin_amazon.py, tests/test_captcha_plugin.py</files>
  <read_first>
    - plugins/shopbot_plugin_amazon.py (check_availability CAPTCHA branch lines 95-107; _wait_user_action 35-53; detect_captcha 85-93)
    - .planning/phases/14-anti-detection-layer-2-captcha-solving/14-RESEARCH.md (Patterns 4/5/6, Pitfalls 3/4/5, Open Question 1 WAF deferral)
    - core/captcha.py (can_solve, solve_recaptcha contract)
  </read_first>
  <behavior>
    - CAPTCHA detected + solver None -> goes straight to _wait_user_action (manual pause)
    - CAPTCHA detected + solver.can_solve() False (cap/zero balance) -> manual pause
    - solver present + empty sitekey extracted -> manual pause (no solve attempted)
    - solver present + sitekey + solve returns token -> token injected, no manual pause
    - solve raises / times out (asyncio.TimeoutError) -> manual pause (no silent skip)
    - window.gokuProps present (Amazon WAF) -> manual pause (WAF auto-solve deferred), logged
    - injected token containing a quote or newline -> rejected, manual pause (input validation V5)
  </behavior>
  <action>
    In plugins/shopbot_plugin_amazon.py, extract a helper async method
    _solve_or_pause(tab, pageurl) called from the existing CAPTCHA branch in check_availability
    (replace the direct _wait_user_action call). Logic: if self._captcha_solver is None or not
    self._captcha_solver.can_solve(), fall to manual pause. Detect Amazon WAF by evaluating the
    window.gokuProps probe (RESEARCH Pattern 6); if present, log an INFO that WAF auto-solve is
    deferred and fall to manual pause (do NOT call solve_amazon_waf this phase — Open Question 1).
    Otherwise extract sitekey via the data-sitekey evaluate; if empty, manual pause. Wrap the solve
    in `async with asyncio.timeout(120): token = await loop.run_in_executor(None,
    self._captcha_solver.solve_recaptcha, sitekey, pageurl)` — the timeout wraps ONLY the executor
    call (Pitfall 3). Catch (asyncio.TimeoutError, Exception) -> log exc.__class__.__name__ and
    manual pause. Before injecting, reject the token if it is empty or contains "'" or a newline
    (V5 input validation) -> manual pause. Inject via tab.evaluate using the
    g-recaptcha-response + ___grecaptcha_cfg callback JS from RESEARCH Code Examples. Manual-pause
    fallback ALWAYS reuses self._wait_user_action(self.captcha_event, "...") — never duplicated,
    never a silent skip. Add self._captcha_solver = None default in __init__ (or via getattr guard)
    so the plugin works when Plan 02 wiring is absent. Keep each method under 30 lines (extract
    _extract_sitekey / _inject_token helpers if needed). Write tests/test_captcha_plugin.py covering
    every branch in <behavior> with a mocked solver and a mocked tab (AsyncMock for evaluate);
    assert _wait_user_action is called on each fallback path and NOT called on the success path.
    Implements ANTI-06 (automated reCAPTCHA solve) and the non-blocking timeout (success criterion 3).
  </action>
  <verify>
    <automated>python -m pytest tests/test_captcha_plugin.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_captcha_plugin.py -q` exits 0 with all <behavior> branches tested
    - Test asserts solver=None -> _wait_user_action called (manual fallback)
    - Test asserts successful solve injects token AND _wait_user_action NOT called
    - Test asserts solve TimeoutError -> _wait_user_action called (no silent skip)
    - Test asserts empty sitekey -> _wait_user_action called, solve_recaptcha NOT called
    - Test asserts window.gokuProps present -> manual pause, solve_amazon_waf NOT called (WAF deferred)
    - Test asserts a token containing "'" is rejected (not injected) and falls to manual pause
    - `grep -n "asyncio.timeout(120)" plugins/shopbot_plugin_amazon.py` returns a match
    - `grep -n "str(exc)" plugins/shopbot_plugin_amazon.py` returns nothing
  </acceptance_criteria>
  <done>Amazon CAPTCHA branch attempts an automated reCAPTCHA solve (run_in_executor under asyncio.timeout(120)), injects a validated token, and falls back to the existing manual pause on empty sitekey/failure/timeout/cap/zero-balance/WAF; all branch tests pass; WAF auto-solve documented as deferred.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BestBuy best-effort reCAPTCHA solve path + manual-pause fallback</name>
  <files>plugins/shopbot_plugin_bestbuy.py, tests/test_captcha_plugin.py</files>
  <read_first>
    - plugins/shopbot_plugin_bestbuy.py (check_availability lines 70-94; __init__ lines 34-38) — no captcha_event yet
    - plugins/shopbot_plugin_amazon.py (the _wait_user_action + _solve_or_pause pattern from Task 1)
    - .planning/phases/14-anti-detection-layer-2-captcha-solving/14-RESEARCH.md (Open Question 2: BestBuy may not show reCAPTCHA -> empty sitekey -> manual pause)
  </read_first>
  <behavior>
    - BestBuy gains a captcha_event asyncio.Event and a _wait_user_action method (or shared helper)
    - check_availability detects a CAPTCHA challenge page and routes through the same solve-or-pause logic
    - Empty sitekey (BestBuy shows non-reCAPTCHA challenge) -> manual pause (no silent skip)
    - solver None or can_solve False -> manual pause
    - successful reCAPTCHA solve -> token injected, no manual pause
  </behavior>
  <action>
    In plugins/shopbot_plugin_bestbuy.py __init__, add self.captcha_event = asyncio.Event() and
    self._captcha_solver = None (import asyncio). Add a _wait_user_action method and a
    _solve_or_pause(tab, pageurl) method mirroring the Amazon plugin's implementation from Task 1
    (same run_in_executor + asyncio.timeout(120) wrapper, same empty-sitekey/failure/cap fallbacks,
    same token validation). Add a detect_captcha override that returns True when a reCAPTCHA widget
    or a known BestBuy challenge marker is present (data-sitekey query or a challenge-phrase find);
    in check_availability, after the existing ban scan, call detect_captcha and route to
    _solve_or_pause when True. Because BestBuy may present a non-reCAPTCHA challenge (Open Question 2),
    empty sitekey simply falls to manual pause — best-effort, no silent skip. Register captcha_event
    is already handled by the orchestrator stdin listener (_KNOWN_EVENTS includes "captcha_event").
    Keep methods under 30 lines (reuse helpers). Extend tests/test_captcha_plugin.py with BestBuy
    branch tests (mocked solver + tab): empty sitekey -> manual pause; successful solve -> injected,
    no pause; solver None -> manual pause. Implements ANTI-06 best-effort BestBuy coverage.
  </action>
  <verify>
    <automated>python -m pytest tests/test_captcha_plugin.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_captcha_plugin.py -q` exits 0
    - `python -c "import asyncio,plugins.shopbot_plugin_bestbuy as b; p=b.BestBuyPlugin(None); assert hasattr(p,'captcha_event')"` exits 0
    - Test asserts BestBuy empty sitekey -> _wait_user_action called (manual fallback)
    - Test asserts BestBuy successful solve -> token injected, _wait_user_action NOT called
    - Test asserts BestBuy solver None -> _wait_user_action called
    - `grep -n "asyncio.timeout(120)" plugins/shopbot_plugin_bestbuy.py` returns a match
  </acceptance_criteria>
  <done>BestBuy plugin has a captcha_event + solve-or-pause path mirroring Amazon; reCAPTCHA solve is best-effort with manual-pause fallback on empty sitekey/failure; branch tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2captcha → plugin | solver-returned token is untrusted input injected into the page via JS |
| plugin solve call → event loop | blocking solve must not stall other plugin poll tasks |
| Amazon WAF page → plugin | gokuProps params are site-controlled; auto-solve deferred |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-inject | Tampering | tab.evaluate token injection | mitigate | Token rejected if empty or contains `'` / newline before embedding in JS literal (V5); test asserts a quote-bearing token is not injected |
| T-14-block2 | Denial of Service | plugin solve call | mitigate | solve wrapped in run_in_executor under asyncio.timeout(120); timeout scope wraps only the executor call (Pitfall 3); test asserts non-blocking wrapper present |
| T-14-silent | Repudiation / safety | fallback path | mitigate | Every failure (empty sitekey/error/timeout/cap/zero-balance/WAF) routes to existing _wait_user_action; tests assert manual pause on each branch — never a silent skip |
| T-14-waf | Tampering | Amazon WAF auto-solve | accept | WAF injection path site-specific + LOW confidence (RESEARCH Open Question 1); deferred — gracefully falls back to manual pause; documented in SUMMARY |
| T-14-abc2 | Tampering | plugin contract | mitigate | Solver consumed via injected self._captcha_solver attribute; no ABC method added, no PLUGIN_API_VERSION bump |
</threat_model>

<verification>
- `python -m pytest tests/test_captcha_plugin.py -q` green
- Full suite `python -m pytest` no regression vs baseline (419 passed, 2 skipped)
- `grep -rn "str(exc)" plugins/shopbot_plugin_amazon.py plugins/shopbot_plugin_bestbuy.py` returns nothing on the new code paths
- asyncio.timeout(120) wraps only the executor call in both plugins
- No PLUGIN_API_VERSION bump
</verification>

<success_criteria>
Opt-in automated reCAPTCHA v2 solving runs in both plugins without blocking other
poll tasks; every failure mode (empty sitekey, error, timeout, cap, zero balance,
Amazon WAF) degrades to the existing manual-pause path; tokens are validated before
injection; WAF auto-solve is deferred and falls back gracefully.
</success_criteria>

<output>
Create `.planning/phases/14-anti-detection-layer-2-captcha-solving/14-03-SUMMARY.md` when done
</output>
