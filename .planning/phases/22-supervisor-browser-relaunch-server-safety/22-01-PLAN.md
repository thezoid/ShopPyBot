---
phase: 22-supervisor-browser-relaunch-server-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/plugin_base.py
  - tests/test_relaunch.py
  - tests/test_plugin_base.py
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "On browser death the plugin can cold-restart its browser via relaunch() with full stealth/proxy/login restoration"
    - "relaunch() runs teardown then setup then restore_session then login in that fixed order"
    - "apply_stealth is re-applied on the relaunched browser (setup() is the single injection point)"
    - "restore_session() returns False as a no-op stub (Phase 23 replaces it)"
    - "relaunch() and restore_session() are additive concrete methods; PLUGIN_API_VERSION stays 2"
  artifacts:
    - path: "core/plugin_base.py"
      provides: "Concrete relaunch() + restore_session() no-op default on RetailerPlugin ABC"
      contains: "async def relaunch"
    - path: "tests/test_relaunch.py"
      provides: "Sequence-order + apply_stealth-called coverage for relaunch()"
    - path: "tests/test_plugin_base.py"
      provides: "restore_session() no-op + PLUGIN_API_VERSION-stays-2 assertions"
  key_links:
    - from: "core/plugin_base.py relaunch()"
      to: "self.teardown / self.setup / self.restore_session / self.login"
      via: "ordered await calls"
      pattern: "await self\\.(teardown|setup|restore_session|login)"
---

<objective>
Add the concrete `relaunch()` cold-restart method and the no-op `restore_session()` stub to the `RetailerPlugin` ABC so the Phase 22 supervisor (Plan 03) can recover from browser death without operator intervention (REL-03).

Purpose: `relaunch()` is the browser-recovery contract the supervisor calls on a dead/disconnected Chrome process. Stealth must be re-injected because CDP `add_script_to_evaluate_on_new_document` is session-scoped and does NOT survive `Browser.stop()` + `Browser.create()` (verified against nodriver 0.50.3); `setup()` is the single re-injection point. `restore_session()` is a no-op stub returning False that Phase 23 replaces with real Fernet cookie restore (cross-phase contract for REL-04).

Output: Two additive concrete methods on the ABC; two test files covering sequence order, apply_stealth-on-relaunch, and the no-op stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-supervisor-browser-relaunch-server-safety/22-CONTEXT.md
@.planning/phases/22-supervisor-browser-relaunch-server-safety/22-RESEARCH.md
@.planning/phases/22-supervisor-browser-relaunch-server-safety/22-PATTERNS.md

<interfaces>
<!-- Existing additive concrete-method analogs on the ABC the executor must mirror.
     From core/plugin_base.py (read directly; no exploration needed): -->

PLUGIN_API_VERSION = 2  # module-level; MUST stay 2 (additive methods are non-breaking)

class RetailerPlugin(ABC):
    def __init__(self, config) -> None:
        self.config = config
        self.driver = None        # set by setup()
        self._checkout_profile = None
        self._checkout_stage: str = ""

    async def setup(self) -> None: ...        # builds Browser; concrete plugins call apply_stealth(self.driver.main_tab) inside
    async def login(self) -> None: ...        # no-op default; concrete plugins authenticate
    async def teardown(self) -> None: ...     # closes browser (L128-130)

    # Existing additive concrete-method pattern to copy (get_active_tab L87-94):
    def get_active_tab(self):
        """... PLUGIN_API_VERSION stays 2 -- additive concrete method (BUY-03)."""
        return getattr(self.driver, "main_tab", None)

# Error-logging convention (avoid credential leak): use exc.__class__.__name__, never str(exc).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 -- failing relaunch + restore_session tests</name>
  <read_first>
    - tests/test_plugin_base.py L70-80 (existing async no-op default assertions; mirror this style)
    - tests/conftest.py L125-154 (fake_plugin factory) and L217-251 (mock_nodriver_start recorder; main_tab.send is AsyncMock for apply_stealth)
    - 22-RESEARCH.md "Phase Requirements -> Test Map" rows REL-03 (test_relaunch_sequence_order, test_relaunch_apply_stealth_called)
    - 22-PATTERNS.md "core/plugin_base.py -- relaunch() and restore_session()" (target method bodies)
  </read_first>
  <files>tests/test_relaunch.py, tests/test_plugin_base.py</files>
  <behavior>
    - test_relaunch_sequence_order: build a RetailerPlugin subclass whose teardown/setup/restore_session/login are AsyncMocks recording call order into a shared list; await relaunch(); assert recorded order is exactly ["teardown", "setup", "restore_session", "login"].
    - test_relaunch_skips_login_when_session_restored: override restore_session to return True; assert login is NOT awaited and recorded order ends at restore_session.
    - test_relaunch_teardown_error_is_swallowed: make teardown raise RuntimeError; assert relaunch still proceeds to setup/login and does not propagate the teardown exception.
    - test_relaunch_apply_stealth_called (in test_relaunch.py): use mock_nodriver_start so setup() builds the recorder browser; on a plugin whose setup calls apply_stealth(self.driver.main_tab), assert relaunch() results in setup() being awaited (apply_stealth invoked on the new browser). Where a concrete plugin is heavy to construct, assert at minimum that relaunch awaits self.setup() exactly once.
    - test_restore_session_noop_returns_false (in test_plugin_base.py): assert await plugin.restore_session() is False.
    - test_plugin_api_version_unchanged (in test_plugin_base.py): assert core.plugin_base.PLUGIN_API_VERSION == 2.
  </behavior>
  <action>Create tests/test_relaunch.py and add the restore_session/version tests to tests/test_plugin_base.py per the behavior list. Use a local RetailerPlugin subclass that supplies the two abstract methods (check_availability, auto_buy) and AsyncMock teardown/setup/restore_session/login attributes, mirroring the fake_plugin construction in conftest. Record call order via a closure list and side_effect on the mocks. These tests MUST fail initially because relaunch() and restore_session() do not yet exist on the ABC. Do NOT implement the methods in this task.</action>
  <verify>
    <automated>pytest tests/test_relaunch.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_relaunch.py exists with the four relaunch tests; running it fails with AttributeError on relaunch (RED state confirmed)
    - tests/test_plugin_base.py contains test_restore_session_noop_returns_false and test_plugin_api_version_unchanged
    - No production code modified in this task
  </acceptance_criteria>
  <done>Both test files contain the listed tests; the relaunch tests fail because relaunch()/restore_session() are absent (RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement relaunch() + restore_session() on RetailerPlugin ABC</name>
  <read_first>
    - core/plugin_base.py L78-94 (get_price/get_active_tab additive-concrete pattern + docstring convention "PLUGIN_API_VERSION stays 2")
    - core/plugin_base.py L120-130 (login() and teardown() signatures relaunch chains)
    - 22-PATTERNS.md "core/plugin_base.py -- relaunch() and restore_session()" (exact method bodies to mirror)
    - 22-RESEARCH.md Pattern 3 + Pitfall 2 (stealth re-injection rationale; setup() is the single injection point)
  </read_first>
  <files>core/plugin_base.py</files>
  <action>Add two additive concrete async methods to RetailerPlugin after teardown() (place near the other concrete defaults). (1) `async def relaunch(self) -> None`: per REL-03 run the fixed sequence teardown -> setup -> restore_session -> login. Wrap the `await self.teardown()` call in try/except Exception and log a WARNING using exc.__class__.__name__ (browser may already be dead; teardown errors must not abort relaunch). Then `await self.setup()` (this re-injects stealth because every concrete plugin's setup() calls apply_stealth on the new browser -- setup() is the single injection point; do NOT call apply_stealth directly here). Capture `session_restored = await self.restore_session()`; if not session_restored, log INFO and `await self.login()`, else log INFO that session was restored and skip login. Use writeLog with [plugin_name] prefix where plugin_name = self.__class__.__name__. Proxy re-assignment (registry.assign_proxy) is the supervisor's responsibility BEFORE calling relaunch() per Open Question 1 resolution -- relaunch() itself takes no registry reference. (2) `async def restore_session(self) -> bool`: no-op stub returning False; docstring notes Phase 23 (REL-04) replaces it and PLUGIN_API_VERSION stays 2. Do NOT bump PLUGIN_API_VERSION. Keep each method under 30 lines.</action>
  <verify>
    <automated>pytest tests/test_relaunch.py tests/test_plugin_base.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 tests pass (GREEN): sequence order is teardown->setup->restore_session->login; login skipped when restore_session returns True; teardown error swallowed; relaunch awaits setup() once; restore_session returns False
    - core.plugin_base.PLUGIN_API_VERSION == 2 (unchanged)
    - relaunch() and restore_session() log via exc.__class__.__name__ / [plugin_name] prefix; no str(exc) on exception paths
    - No abstractmethod added; existing plugins inherit both methods unchanged
  </acceptance_criteria>
  <done>relaunch() and restore_session() exist as concrete ABC methods; the full Wave 0 relaunch/restore_session suite is green; PLUGIN_API_VERSION stays 2</done>
</task>

</tasks>

<verification>
- `pytest tests/test_relaunch.py tests/test_plugin_base.py` green
- `pytest` full suite green (no regression in existing plugin/ABC tests)
- relaunch sequence order asserted by automated test; apply_stealth-on-relaunch verified via setup() invocation
</verification>

<success_criteria>
- relaunch() executes teardown -> setup -> restore_session -> login in order; setup() re-injects stealth (roadmap criterion 3)
- restore_session() is a no-op returning False (Phase 23 contract for REL-04)
- Both methods are additive; PLUGIN_API_VERSION stays 2
- REL-03 (relaunch sequence portion) covered by automated tests
</success_criteria>

<output>
Create `.planning/phases/22-supervisor-browser-relaunch-server-safety/22-01-SUMMARY.md` when done
</output>
