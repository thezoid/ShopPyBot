---
phase: 25-design-system
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_design_system.py
  - tests/test_web_dashboard.py
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]
nyquist_compliant: true
must_haves:
  truths:
    - "Static-analysis tests assert components.css has zero hardcoded hex and tokens.css declares every required token"
    - "Template tests assert FOUC script is first in <head>, CSS link order, no innerHTML on API data, uPlot served"
    - "All new tests are RED (fail) before implementation lands and become the executable acceptance gate"
  artifacts:
    - path: "tests/test_design_system.py"
      provides: "CSS static analysis + uPlot vendor existence tests"
      contains: "test_no_hardcoded_hex_in_components"
    - path: "tests/test_web_dashboard.py"
      provides: "FOUC/link-order/XSS-regression/uPlot-served template tests"
      contains: "test_no_innerHTML_with_api_data"
  key_links:
    - from: "tests/test_design_system.py"
      to: "web/static/components.css"
      via: "file read + hex regex"
      pattern: "components\\.css"
    - from: "tests/test_web_dashboard.py"
      to: "web/templates/dashboard.html"
      via: "client.get('/') + innerHTML regex on template file"
      pattern: "innerHTML"
---

<objective>
Create the Wave 0 test scaffold that the Phase 25 implementation must satisfy. These
tests encode the five ROADMAP success criteria as executable assertions: token coverage,
zero hardcoded hex in component rules, FOUC script position, CSS link presence, XSS
regression guard, no external URLs in CSS, and uPlot vendor files served.

Purpose: Per the Nyquist rule, every implementation task in waves 1-2 has an `<automated>`
verify that maps to a test created here. Tests are written RED (they fail now because the
CSS files and template changes do not exist yet) and turn GREEN as implementation lands.
Output: `tests/test_design_system.py` (new) and additions to `tests/test_web_dashboard.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-design-system/25-VALIDATION.md
@.planning/phases/25-design-system/25-UI-SPEC.md
@.planning/phases/25-design-system/25-PATTERNS.md

<interfaces>
<!-- Existing test fixtures the new tests reuse. Extracted from tests/test_web_dashboard.py lines 10-21. -->
<!-- The `client` and `mock_svc` fixtures already exist; new tests in this file receive `client` directly. -->

From tests/test_web_dashboard.py:
- `mock_svc` fixture: MagicMock with `get_status() -> {"running": False}`, `list_items() -> []`
- `client` fixture: `TestClient(create_app(mock_svc))`
- Text-presence pattern: `resp = client.get("/"); assert "X" in resp.text`

From web/__init__.py (line 30):
- StaticFiles mounted at `/static` over `web/static/` dir; serves subdirectories (e.g. `/static/vendor/...`) with no extra config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_design_system.py — CSS static analysis + uPlot vendor existence</name>
  <files>tests/test_design_system.py</files>
  <read_first>
    - tests/test_design_system.py (does not exist yet — confirm absence, then create)
    - .planning/phases/25-design-system/25-PATTERNS.md (the `tests/test_design_system.py` section, lines 503-572 — exact target test bodies)
    - .planning/phases/25-design-system/25-UI-SPEC.md (Color Tokens + Typography + Spacing tables — authoritative token name list)
    - tests/test_web_dashboard.py (import style + file-grep test pattern to copy)
  </read_first>
  <action>
    Create a new pure file-read test module (no HTTP client; no fastapi import needed). Use
    `ROOT = pathlib.Path(__file__).parent.parent` to resolve repo-root-relative paths.
    Implement three functions exactly per 25-PATTERNS.md:
    (1) `test_no_hardcoded_hex_in_components` — read `web/static/components.css`, strip CSS
    comments with `re.sub(r'/\*.*?\*/', '', css, flags=re.DOTALL)`, then assert the stripped
    text matches neither `(?<![a-zA-Z0-9_-])#[0-9a-fA-F]{3,6}\b` nor `rgb\(`.
    (2) `test_all_required_tokens_declared` — read `web/static/tokens.css`, assert every token
    name from the UI-SPEC Color Tokens / Typography / Spacing tables is present as a substring:
    the 14 `--color-*` tokens, the 6 `--space-*` tokens (`xs,sm,md,lg,xl,2xl`), the 4 `--text-*`
    sizes, `--weight-normal`, `--weight-semibold`, and `--font-family` (use the exact required
    list in 25-PATTERNS.md lines 550-561).
    (3) `test_uplot_vendor_files_exist` — assert `web/static/vendor/uplot.iife.min.js` and
    `web/static/vendor/uplot.min.css` exist (all-lowercase names per the reconciliation note).
    These tests MUST be RED on first run (the CSS files and vendor dir do not exist yet) — that
    is the intended Wave 0 state.
  </action>
  <verify>
    <automated>python -m pytest tests/test_design_system.py -q 2>&1 | grep -E "3 failed|3 errors|collected 3"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_design_system.py` exists and defines exactly the three named functions.
    - Module imports only `re` and `pathlib` (no fastapi, no TestClient).
    - `test_all_required_tokens_declared` references the full required-token list (>= 27 names) including `--color-bg`, `--color-surface`, `--color-accent`, `--space-md`, `--text-body`, `--font-family`.
    - The hex regex uses the comment-stripping step before scanning (no bare grep on raw file).
    - `python -m pytest tests/test_design_system.py -q` collects 3 tests; all 3 fail/error now (RED) because target files do not yet exist.
  </acceptance_criteria>
  <done>tests/test_design_system.py committed with three RED static-analysis tests; collection succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add FOUC / link-order / XSS-regression / no-external-URL / uPlot-served tests to test_web_dashboard.py</name>
  <files>tests/test_web_dashboard.py</files>
  <read_first>
    - tests/test_web_dashboard.py (existing — reuse the `client` fixture; append new tests, do not modify existing ones)
    - .planning/phases/25-design-system/25-PATTERNS.md ("New tests to add" block, lines 604-639 — exact target bodies)
    - .planning/phases/25-design-system/25-RESEARCH.md (CI Grep Assertion code example, lines 488-499)
  </read_first>
  <action>
    Append five new test functions to the existing file, reusing the `client` fixture. Do NOT
    alter `test_banner_renders_when_non_local` (MC-4) or any existing test.
    (1) `test_fouc_script_first_in_head(client)` — GET `/`, parse with `html.parser.HTMLParser`,
    assert the first element child inside `<head>` is a `<script>` whose text contains
    `localStorage`. (Use a small parser subclass that records the first start tag after `<head>`
    and the script body.)
    (2) `test_css_link_order_in_head(client)` — assert `/static/tokens.css`, `/static/components.css`,
    and `/static/dashboard.css` all appear in `resp.text`, and that `tokens.css`'s index precedes
    `components.css`'s index precedes `dashboard.css`'s index.
    (3) `test_no_innerHTML_with_api_data(client)` — read `web/templates/dashboard.html` from disk,
    compile `re.compile(r'innerHTML\s*=\s*.*\$\{(item\.|cred\.|data\.|cfg\.|resp\.)')`, assert no
    matches. (This is the XSS regression guard; it must catch the two current violations at lines
    241 and 255 — so it is RED until Wave 2 fixes them.)
    (4) `test_no_external_urls_in_static(client)` — glob `web/static/**/*.css` (include subdirs), assert
    no file content contains `url(http` or `@import url(`.
    (5) `test_uplot_served(client)` — `client.get("/static/vendor/uplot.iife.min.js")` returns 200.
    Tests 1-3,5 are RED now; test 4 may pass on the current single CSS file but must keep passing
    after the split.
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_dashboard.py -q -k "fouc or css_link or innerHTML or external_urls or uplot_served" 2>&1 | grep -E "failed|passed"</automated>
  </verify>
  <acceptance_criteria>
    - Five new functions appended; all existing tests in the file unchanged and still collected.
    - `test_no_innerHTML_with_api_data` uses the exact regex `innerHTML\s*=\s*.*\$\{(item\.|cred\.|data\.|cfg\.|resp\.)` and currently FAILS (catches lines 241 + 255).
    - `test_fouc_script_first_in_head` fails now (no FOUC script present yet) and the assertion targets the FIRST child of `<head>` being a `<script>` containing `localStorage`.
    - `test_uplot_served` requests `/static/vendor/uplot.iife.min.js` and asserts status_code == 200.
    - `python -m pytest tests/test_web_dashboard.py::test_banner_renders_when_non_local -q` still PASSES (MC-4 untouched).
  </acceptance_criteria>
  <done>Five RED template tests added; MC-4 and all prior tests remain green; XSS regression guard catches both current violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → repo files | Tests read CSS/HTML/vendor files and assert structure; no untrusted input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | XSS regression in dashboard.html | mitigate | `test_no_innerHTML_with_api_data` greps for `innerHTML =` on API-sourced vars; becomes a permanent CI gate (UI-03) |
| T-25-02 | Spoofing | CDN supply-chain via external CSS URL | mitigate | `test_no_external_urls_in_static` blocks `url(http`/`@import url(` in any committed CSS |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; uPlot is vendored from a pinned GitHub tag (Plan 25-02), not a registry — no slopcheck applicable |
</threat_model>

<verification>
- `python -m pytest tests/test_design_system.py tests/test_web_dashboard.py -q` collects all tests with no collection errors.
- New tests are RED (implementation not yet present); MC-4 `test_banner_renders_when_non_local` and all pre-existing tests are GREEN.
</verification>

<success_criteria>
- `tests/test_design_system.py` defines the three static-analysis/vendor tests.
- `tests/test_web_dashboard.py` gains the five template tests without disturbing existing tests.
- Every Wave 1 / Wave 2 implementation task has a corresponding test here to verify against.
</success_criteria>

<output>
Create `.planning/phases/25-design-system/25-01-SUMMARY.md` when done.
</output>
