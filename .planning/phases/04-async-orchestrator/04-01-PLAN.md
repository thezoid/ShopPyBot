---
phase: 04-async-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/config_schema.py
  - tests/conftest.py
  - tests/test_config_schema.py
autonomous: true
requirements: [ASYNC-01, ASYNC-02, ASYNC-03, ASYNC-05]
must_haves:
  truths:
    - "poll_interval is a typed, defaulted config field accessible as cfg.app.poll_interval"
    - "Tests can spawn a fake async plugin with configurable check_availability / auto_buy results without launching a browser"
    - "Tests can drive an asyncio.Event from a simulated stdin listener and observe a paused coroutine resume"
  artifacts:
    - path: "core/config_schema.py"
      provides: "AppSettingsConfig submodel mounted at cfg.app with poll_interval: int = 30"
      contains: "poll_interval"
    - path: "tests/conftest.py"
      provides: "fake_plugin fixture + stdin/Event shim fixture for orchestrator tests"
      contains: "fake_plugin"
    - path: "tests/test_config_schema.py"
      provides: "poll_interval default + override assertions"
      contains: "poll_interval"
  key_links:
    - from: "tests/conftest.py"
      to: "core.plugin_base.RetailerPlugin"
      via: "fake plugin subclasses RetailerPlugin"
      pattern: "RetailerPlugin"
    - from: "core/config_schema.py"
      to: "cfg.app.poll_interval"
      via: "AppSettingsConfig field mounted on AppConfig"
      pattern: "poll_interval"
---

<objective>
Wave 0 groundwork for the async orchestrator: add the single shared `poll_interval`
config field (locked decision) and the shared test fixtures that every downstream
plan's tests depend on (a fake async plugin and an asyncio.Event/stdin shim).

Purpose: Downstream waves (orchestrator, input()-removal, main wiring) cannot be
test-verified without a no-browser fake plugin and an Event-driving shim. The
orchestrator reads `cfg.app.poll_interval`, so the field must exist first.

Output: `poll_interval` field + config test; reusable `fake_plugin` and Event-shim
fixtures in conftest.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-async-orchestrator/04-RESEARCH.md
@.planning/phases/04-async-orchestrator/04-VALIDATION.md

<interfaces>
<!-- Executor: use these directly. No codebase exploration needed. -->

From core/plugin_base.py (the ABC the fake plugin must subclass):
```python
PLUGIN_API_VERSION = 2

class RetailerPlugin(ABC):
    domain_patterns: list[str]            # class attribute
    def __init__(self, config) -> None:   # sets self.config, self.driver = None
    async def setup(self) -> None: ...
    @abstractmethod
    async def check_availability(self, url: str) -> bool: ...
    @abstractmethod
    async def auto_buy(self, url: str) -> bool: ...
    async def login(self) -> None: return None
    async def detect_captcha(self) -> bool: return False
    async def teardown(self) -> None: ...
```

From core/config_schema.py (current AppConfig has NO `app` submodel; add one):
```python
class AppConfig(BaseSettings):
    model_config = SettingsConfigDict(extra="ignore", env_nested_delimiter="__")
    selenium: SeleniumConfig = SeleniumConfig()
    debug: DebugConfig = DebugConfig()
    available: AvailableConfig = AvailableConfig()
    platforms: PlatformsConfig = PlatformsConfig()
    # constructor accepts yaml_file=<Path> kwarg for test injection
```

Existing conftest.py already provides: tmp_config_yml, tmp_data_dir, fake_browser,
tmp_plugins_dir, and an async smoke test. asyncio_mode=auto is set in pyproject.toml,
so async def tests/fixtures run without a decorator. pytest-asyncio==1.3.0 is already
in requirements.txt (research Open Question 3 RESOLVED — do NOT add it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add poll_interval config field</name>
  <read_first>
    - core/config_schema.py (full file — the submodel + AppConfig pattern)
    - tests/test_config_schema.py (analog: how AppConfig(yaml_file=...) is tested)
    - .planning/phases/04-async-orchestrator/04-RESEARCH.md (Open Questions item 1: poll_interval recommendation)
  </read_first>
  <behavior>
    - cfg.app.poll_interval defaults to 30 when config.yml has no app section
    - cfg.app.poll_interval reads an integer from config.yml app.poll_interval
    - Setting it via YAML to a custom value (e.g. 15) is reflected on the model
  </behavior>
  <action>
    Add a new `AppSettingsConfig(BaseModel)` submodel with a single field
    `poll_interval: int = 30` (the locked single shared interval; per-platform jitter
    is Phase 6, do NOT add jitter fields). Mount it on `AppConfig` as
    `app: AppSettingsConfig = AppSettingsConfig()` so the orchestrator's
    `cfg.app.poll_interval` accessor (per RESEARCH Target 2) resolves. Place the new
    submodel near the other BaseModel submodels (SeleniumConfig, DebugConfig). Do NOT
    rename or touch the existing `_LEGACY_KEYS["app"]` deprecation logic — the legacy
    warning targets old credential keys under `app`, and `poll_interval` is not a legacy
    key, so the two coexist. Keep snake_case; no new dependencies.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_config_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "poll_interval" core/config_schema.py returns a field defined as `poll_interval: int = 30`
    - grep "app: AppSettingsConfig" core/config_schema.py returns the mounted field
    - tests/test_config_schema.py contains a test asserting cfg.app.poll_interval == 30 (default) and one asserting an overridden YAML value
    - .venv\Scripts\python.exe -m pytest tests/test_config_schema.py -x -q exits 0
  </acceptance_criteria>
  <done>cfg.app.poll_interval exists with default 30, reads from YAML, and is covered by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add fake_plugin and Event-shim fixtures to conftest</name>
  <read_first>
    - tests/conftest.py (full file — existing fixture style, fake_browser pattern)
    - tests/test_registry.py (analog: how AsyncMock plugins are built and driven)
    - core/plugin_base.py (the ABC the fake plugin subclasses)
    - .planning/phases/04-async-orchestrator/04-VALIDATION.md (Wave 0 Requirements: fake-plugin + Event/stdin shim)
  </read_first>
  <action>
    Add two reusable fixtures to tests/conftest.py.

    (1) `fake_plugin`: a factory fixture returning a function that builds a concrete
    RetailerPlugin subclass instance with configurable `domain_patterns`,
    `check_availability` return value, and `auto_buy` return value. The instance must
    NOT launch a browser: its `setup` is an AsyncMock that records call ordering/timing,
    and `teardown` is an AsyncMock. `check_availability` and `auto_buy` are async and
    return the configured values. Expose attributes the orchestrator/stdin-listener will
    read (e.g. allow attaching `captcha_event`, `passkey_event`, `otp_event`,
    `test_pause_event` asyncio.Event attributes) so downstream Event tests can use it.

    (2) `event_shim`: a fixture that returns a helper to simulate the stdin listener —
    given a list of plugins and a running loop, it sets a named asyncio.Event via
    `loop.call_soon_threadsafe(event.set)` (the ONLY thread-safe bridge per RESEARCH
    Pitfall 1), mirroring how the real `_stdin_listener_thread` will signal. This lets
    a downstream test pause a coroutine on `event.wait()` and prove it resumes when the
    shim fires. Use asyncio.Event from the stdlib; no new dependencies.

    Keep fixtures small and documented with a one-line docstring each. Do not modify the
    existing fixtures.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "def fake_plugin" tests/conftest.py returns the fixture
    - grep "def event_shim" tests/conftest.py returns the fixture
    - grep "call_soon_threadsafe" tests/conftest.py returns the thread-safe Event set in the shim
    - The fake_plugin instance is an instanceof RetailerPlugin and its setup/teardown are AsyncMocks (no real browser)
    - .venv\Scripts\python.exe -m pytest tests/ -x -q exits 0 (full suite still green)
  </acceptance_criteria>
  <done>conftest.py exposes fake_plugin and event_shim fixtures; full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml -> AppConfig | User-edited YAML parsed into typed model; untrusted only insofar as malformed values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | poll_interval from config.yml | mitigate | Pydantic types poll_interval as int; non-int YAML raises ValidationError at startup (CORE-05 behavior preserved) |
| T-04-02 | DoS | poll_interval set absurdly low (e.g. 0) by user | accept | Local single-user tool; a tight poll loop only affects the user's own machine and target sites they already chose to poll. No remote attacker surface. |
| T-04-03 | Information Disclosure | test fixtures | accept | Fixtures handle only fake plugins and Enter-signal events; no credentials touched. The Event shim sets events, never reads input text. |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/test_config_schema.py -q` green
- `.venv\Scripts\python.exe -m pytest tests/ -q` green (fixtures import-clean, no regressions)
</verification>

<success_criteria>
- cfg.app.poll_interval resolves to 30 by default and reads YAML overrides
- fake_plugin and event_shim fixtures available for Waves 2-4 tests
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/04-async-orchestrator/04-01-SUMMARY.md` when done
</output>
