---
phase: 04-async-orchestrator
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - models.py
  - .gitignore
  - tests/test_models_wal.py
autonomous: true
requirements: [ASYNC-04, ASYNC-05]
must_haves:
  truths:
    - "Every DB connection is opened with journal_mode=WAL, busy_timeout=5000, synchronous=NORMAL"
    - "Every DB connection is opened and closed via a context manager (no leaked connections)"
    - "Sync DB functions exist for run_in_executor use; existing function names still work"
    - "Concurrent writers serialized through the layer produce zero 'database is locked' errors in a stress proxy"
  artifacts:
    - path: "models.py"
      provides: "get_db_connection() context manager + WAL PRAGMAs + *_sync functions"
      contains: "journal_mode=WAL"
    - path: "tests/test_models_wal.py"
      provides: "WAL PRAGMA assertion, context-manager close assertion, concurrent-write stress proxy"
      contains: "busy_timeout"
    - path: ".gitignore"
      provides: "WAL sidecar file ignores"
      contains: "db-wal"
  key_links:
    - from: "models.py get_items_sync / update_item_purchased_sync"
      to: "get_db_connection()"
      via: "with get_db_connection() as conn"
      pattern: "with get_db_connection"
    - from: "get_db_connection()"
      to: "sqlite3 PRAGMA"
      via: "conn.execute PRAGMA on open"
      pattern: "PRAGMA journal_mode=WAL"
---

<objective>
Make the SQLite layer safe under concurrent async polling (ASYNC-04) and provide the
serialized-write-compatible sync API the write queue will drain (ASYNC-05 DB side).

Purpose: Two plugins polling concurrently issue frequent reads and rare writes. WAL +
busy_timeout + a single write queue (built in Plan 03) is the locked design for zero
`database is locked` errors over a 60+ min soak (success criterion 4). This plan lands
the connection layer and `*_sync` functions; the queue that calls them is Plan 03.

Output: `get_db_connection()` context manager with the exact PRAGMA setup, `*_sync`
DB functions, WAL sidecar gitignore entries, and unit + stress-proxy tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-async-orchestrator/04-RESEARCH.md

<interfaces>
<!-- Executor: implement against these. Exact PRAGMA strings are load-bearing. -->

Current models.py exports: initialize_db(delete=False), add_items(items),
update_item_purchased(link), get_items(); DB_PATH = os.path.join('data','shop_py_bot.db').

Required connection layer (RESEARCH Target 4 — verbatim PRAGMA sequence):
  PRAGMA journal_mode=WAL
  PRAGMA busy_timeout=5000
  PRAGMA synchronous=NORMAL
Open with sqlite3.connect(DB_PATH, timeout=5). Context manager commits on success,
rolls back + re-raises on exception, closes in finally.

Callers (Plan 03 orchestrator) will invoke via run_in_executor:
  get_items_sync() -> list of (name, link, auto_buy, quantity, purchased)
  update_item_purchased_sync(link) -> None
Plan 03's write-queue drain calls update_item_purchased_sync; plugins NEVER call it
directly (ASYNC-05). add_items_sync is also needed (main.py seeds items pre-loop).

Existing tests/test_models.py uses tmp_data_dir (monkeypatches models.DB_PATH) and
asserts get_items()/update_item_purchased() behavior. Those names must keep working.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WAL context manager and *_sync DB functions</name>
  <read_first>
    - models.py (full file — current connect/commit/close pattern to replace)
    - tests/test_models.py (analog: tmp_data_dir fixture, get_items/update assertions)
    - .planning/phases/04-async-orchestrator/04-RESEARCH.md (Research Target 4: exact PRAGMA + sync functions)
  </read_first>
  <behavior>
    - Opening a connection applies journal_mode=WAL, busy_timeout=5000, synchronous=NORMAL
    - get_db_connection() closes the connection even when the body raises (finally close)
    - get_items_sync() returns the same shape as the old get_items()
    - update_item_purchased_sync(link) sets purchased=1 for the matching link
  </behavior>
  <action>
    Add a `@contextlib.contextmanager def get_db_connection()` that opens
    `sqlite3.connect(DB_PATH, timeout=5)`, executes the three PRAGMAs in order
    (`journal_mode=WAL`, `busy_timeout=5000`, `synchronous=NORMAL`), yields the
    connection, commits on clean exit, rolls back and re-raises on Exception, and closes
    in `finally`. Rewrite `get_items`, `update_item_purchased`, `add_items` to use this
    context manager. Add `*_sync` aliases: `get_items_sync`, `update_item_purchased_sync`,
    `add_items_sync` (the orchestrator and main.py call the `_sync` names via
    run_in_executor; keep the original names as thin wrappers calling the `_sync` bodies
    so existing tests/imports do not break — single source of truth: implement once,
    alias once). Keep `initialize_db` as-is for schema creation but route its connection
    through the same PRAGMA setup if practical; do NOT change the items table schema.
    No bare except (project rule); catch `Exception` in the context manager. No new deps.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_models.py tests/test_models_wal.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "PRAGMA journal_mode=WAL" models.py and grep "busy_timeout=5000" models.py both return a hit
    - grep "synchronous=NORMAL" models.py returns a hit
    - grep -E "def (get_items_sync|update_item_purchased_sync|add_items_sync)" models.py returns all three
    - grep "with get_db_connection" models.py returns at least the three sync function bodies
    - Existing tests/test_models.py still passes unchanged (backward-compatible names)
  </acceptance_criteria>
  <done>get_db_connection() applies the WAL PRAGMAs; *_sync functions exist; legacy names still work; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WAL/context-manager tests, concurrent-write stress proxy, gitignore</name>
  <read_first>
    - tests/test_models.py (analog fixture: tmp_data_dir + initialize_db(delete=True))
    - models.py (the get_db_connection + *_sync functions from Task 1)
    - .planning/phases/04-async-orchestrator/04-VALIDATION.md (ASYNC-04 / SC4 stress proxy row)
    - .gitignore (current entries; data/* is already ignored but add explicit WAL sidecars)
  </read_first>
  <behavior>
    - test_wal_pragma_applied: after opening via get_db_connection, PRAGMA journal_mode returns 'wal'
    - test_busy_timeout_applied: PRAGMA busy_timeout returns 5000
    - test_connection_closed_on_exit: the connection object is closed after the with-block (operation on it raises ProgrammingError)
    - test_concurrent_writes_no_lock: many writers updating distinct links via run_in_executor (or threads) all succeed with zero OperationalError 'database is locked'
  </behavior>
  <action>
    Create tests/test_models_wal.py using the tmp_data_dir fixture and
    initialize_db(delete=True). Assert the three behaviors above by re-querying the
    PRAGMAs through a fresh get_db_connection (journal_mode is a persistent DB property
    once set to WAL; busy_timeout is per-connection). For the stress proxy (SC4), seed N
    distinct item links, then fire many concurrent `update_item_purchased_sync` calls via
    a ThreadPoolExecutor (proxying the run_in_executor write path), and assert no call
    raised sqlite3.OperationalError and all rows show purchased=1. This is the short
    simulated proxy for the manual 60-min soak — mark in the SUMMARY that the full soak
    is human-verify. Add `.gitignore` lines `data/*.db-wal` and `data/*.db-shm` (WAL
    sidecars; they contain only the same data as the main DB, no credentials — see
    threat model). Note data/* is already ignored, but explicit sidecar entries document
    intent.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_models_wal.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_models_wal.py contains test_wal_pragma_applied, test_connection_closed_on_exit, and a concurrent-write test
    - The concurrent-write test uses multiple workers (ThreadPoolExecutor or asyncio.gather over run_in_executor) and asserts zero 'database is locked' errors
    - grep "db-wal" .gitignore and grep "db-shm" .gitignore both return a hit
    - .venv\Scripts\python.exe -m pytest tests/test_models_wal.py -x -q exits 0
  </acceptance_criteria>
  <done>WAL PRAGMA + close + concurrent-write proxy tests pass; WAL sidecars gitignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async coroutines -> sqlite3 file | Concurrent plugin tasks read/write a shared local DB file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | concurrent writes corrupting the purchased flag | mitigate | WAL serializes via SQLite's writer lock; Plan 03 write queue funnels all writes through one task; update uses parameterized `WHERE link=?` (no injection); stress test proves zero lock errors |
| T-04-05 | Information Disclosure | WAL/-shm sidecar files | mitigate | Sidecars contain only the same non-sensitive item data as the main DB (no credentials, no CVV). Added to .gitignore; data/ already ignored. |
| T-04-06 | Denial of Service | write lock contention under load | mitigate | busy_timeout=5000 retries for 5s before raising; WAL keeps readers non-blocking; serialized write queue (Plan 03) removes write-write contention |
| T-04-07 | Tampering | SQL injection via item link | mitigate | All queries parameterized (`?` placeholders); no string interpolation of user values into SQL (preserved from existing models.py) |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/test_models.py tests/test_models_wal.py -q` green
- Concurrent-write stress proxy reports zero `database is locked`
- WAL sidecars present in .gitignore
</verification>

<success_criteria>
- Connection layer applies WAL + busy_timeout=5000 + synchronous=NORMAL on every open
- All connections use the context manager (open/close guaranteed)
- *_sync functions exist for the write queue and run_in_executor read path
- Concurrent-write proxy demonstrates zero lock errors (full 60-min soak is human-verify)
</success_criteria>

<output>
Create `.planning/phases/04-async-orchestrator/04-02-SUMMARY.md` when done
</output>
