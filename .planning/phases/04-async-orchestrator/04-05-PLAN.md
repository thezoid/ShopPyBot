---
phase: 04-async-orchestrator
plan: 05
type: execute
wave: 5
depends_on: ["04-03", "04-04"]
files_modified:
  - main.py
  - tests/test_main_wiring.py
autonomous: false
requirements: [ASYNC-01, ASYNC-02, ASYNC-03, ASYNC-04, ASYNC-05]
must_haves:
  truths:
    - "main.py delegates to core.orchestrator.async_main and no longer contains the sequential while-True loop"
    - "The pre-loop getpass CVV gate and AppConfig validation are preserved unchanged"
    - "Running the bot with Amazon + BestBuy polls both concurrently (live human-verify)"
    - "60+ min two-platform run produces zero 'database is locked' errors (live human-verify)"
  artifacts:
    - path: "main.py"
      provides: "thin entry point importing async_main from core.orchestrator; CVV gate + validation intact"
      contains: "from core.orchestrator import async_main"
    - path: "tests/test_main_wiring.py"
      provides: "asserts main delegates to orchestrator.async_main; CVV gate logic preserved"
      contains: "async_main"
  key_links:
    - from: "main.py main()"
      to: "core.orchestrator.async_main"
      via: "asyncio.run(async_main(cfg, cvv))"
      pattern: "async_main"
    - from: "main.py main()"
      to: "collect_cvv getpass gate"
      via: "preserved pre-loop CVV collection"
      pattern: "collect_cvv"
---

<objective>
Wire the entry point to the new orchestrator: main.py imports and runs
core.orchestrator.async_main, deleting the obsolete in-file sequential async_main loop,
while preserving the pre-loop getpass CVV gate and AppConfig validation (locked
decisions). This is the integration seam that makes the full concurrent stack runnable.

Purpose: All prior plans built the pieces; this connects them so a real run exercises
TaskGroup concurrency (ASYNC-01), staggered init (ASYNC-02), Event-based intervention
(ASYNC-03), WAL DB (ASYNC-04), and the write queue (ASYNC-05) end to end. Includes the
human-verify checkpoint for the live concurrency + zero-lock soak the success criteria
require.

Output: a thin main.py delegating to the orchestrator; a wiring test; and the
human-verify checkpoint covering SC1 (overlap), SC2 (3-plugin stagger), SC3 (Event
intervention), SC4 (60-min zero-lock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-async-orchestrator/04-RESEARCH.md
@.planning/phases/04-async-orchestrator/04-VALIDATION.md

<interfaces>
<!-- Executor: main.py currently defines its own async_main (the sequential loop).
     Replace the body with a delegation to the orchestrator's async_main. -->

Current main.py structure (preserve the marked parts):
```python
def make_tiny(url): ...                 # KEEP if still referenced; orchestrator logs full link
def collect_cvv(): ...                  # PRESERVE verbatim (getpass gate, SEC-02)
async def async_main(cfg, cvv): ...     # DELETE this in-file body (moved to core/orchestrator.py)
def main():
    # PRESERVE: AppConfig() validation + ValidationError -> SystemExit(1)
    # PRESERVE: initialize_db(); add_items(seed from cfg.available.items)
    # PRESERVE: needs_bb_autobuy gate -> cvv = collect_cvv() if needs_bb_autobuy else None
    asyncio.run(async_main(cfg, cvv))   # now calls the imported orchestrator async_main
```

From core/orchestrator.py (Plan 03): `async def async_main(cfg, cvv) -> None` — builds
the registry, staggers setup, starts the stdin listener, runs the TaskGroup, flushes the
write queue, tears down. It already threads CVV onto the bestbuy plugin internally, so
main.py just passes cvv through.

add_items in main.py seeds the DB pre-loop. After Plan 02, add_items still works (legacy
wrapper). If main.py imports get_items only for the old loop, drop unused imports after
deleting the local async_main.

CVV gate logic to preserve (main.py lines ~110-114): only prompt when NOT test_mode AND
at least one bestbuy.com item has auto_buy. In test_mode, cvv stays None (do not block CI
on getpass — WR-02 contract).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delegate main.py to core.orchestrator.async_main</name>
  <read_first>
    - main.py (full file — collect_cvv, the local async_main to delete, main() pre-flight)
    - core/orchestrator.py (the async_main signature + behavior from Plan 03)
    - tests/test_config_schema.py (analog: AppConfig construction in tests)
    - tests/conftest.py (tmp_config_yml, tmp_data_dir, fake_plugin fixtures)
  </read_first>
  <behavior>
    - main() still validates AppConfig and exits 1 with an actionable message on ValidationError
    - main() still seeds the DB and applies the test_mode/bestbuy CVV gate (cvv None in test_mode)
    - main() calls asyncio.run with the orchestrator's async_main, not a local loop
    - The local sequential async_main loop no longer exists in main.py
  </behavior>
  <action>
    Replace `from core.registry import PluginRegistry` etc. usage by adding
    `from core.orchestrator import async_main`. Delete the local `async def async_main`
    definition (the sequential while-True loop) entirely — the orchestrator owns it now.
    Keep `collect_cvv()` and `make_tiny()` (only delete make_tiny if nothing references
    it after the change; the orchestrator logs the full link, so make_tiny is likely
    unused — confirm and remove its now-dead usage, but do not remove `requests` import if
    other code uses it). Preserve main()'s pre-flight exactly: AppConfig() validation with
    ValidationError -> SystemExit(1); initialize_db(); add_items(seed); the needs_bb_autobuy
    gate; `asyncio.run(async_main(cfg, cvv))`. Remove now-unused imports
    (PluginRegistry, get_items, play_* if only the deleted loop used them). Do NOT change
    the CVV gate condition. Keep functions small.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_main_wiring.py tests/test_no_input.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "from core.orchestrator import async_main" main.py returns a hit
    - grep "async def async_main" main.py returns NO hit (local loop deleted)
    - grep "collect_cvv" main.py still returns the preserved getpass gate
    - grep "AppConfig()" main.py and grep "SystemExit(1)" main.py confirm validation preserved
    - grep "while True" main.py returns NO hit
    - tests/test_main_wiring.py asserts main() calls asyncio.run with orchestrator.async_main (patched) and that the CVV gate yields None in test_mode
    - .venv\Scripts\python.exe -m pytest tests/test_main_wiring.py tests/test_no_input.py -x -q exits 0
  </acceptance_criteria>
  <done>main.py is a thin entry delegating to the orchestrator; CVV gate + validation preserved; sequential loop removed; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live concurrency, stagger, intervention, and zero-lock verification</name>
  <what-built>
    The full async orchestrator stack: per-plugin coroutines under one asyncio.TaskGroup
    (ASYNC-01), 1.5s-staggered driver init (ASYNC-02), asyncio.Event-based CAPTCHA/OTP
    intervention replacing input() (ASYNC-03), WAL SQLite (ASYNC-04), and a single write
    queue (ASYNC-05). main.py now runs it end to end with the CVV gate intact.
  </what-built>
  <how-to-verify>
    Prerequisites: copy sample.config.yml to config.yml; set debug.test_mode: true (skips
    the final purchase click); add at least one Amazon item and one BestBuy item under
    available.items. Run: `.venv\Scripts\python.exe main.py`

    1. SC1 (concurrency): Watch the startup and first ~2 poll cycles. Confirm the log
       shows BOTH plugins checking within the same window (interleaved timestamps), not
       Amazon fully finishing before BestBuy starts. Expected: overlapping
       "Checking Amazon availability" and "Checking BestBuy availability" timestamps.
    2. SC2 (stagger): In the startup logs, confirm each plugin's driver-init STAGGER line
       is >= 1.5 seconds after the previous one, and no ChromeDriver/CDP port-conflict
       error appears. For a stronger check, add a third item on a third configured plugin
       and confirm three staggered inits with no port error.
    3. SC3 (intervention, ASYNC-03): On an Amazon item that triggers a CAPTCHA (or force
       one), confirm: an alert sound plays, a clear actionable WARNING is logged, ONLY the
       Amazon plugin pauses while BestBuy keeps logging its checks, and pressing Enter
       resumes the Amazon plugin.
    4. SC4 (zero-lock soak): Leave the bot running with both platforms for 60+ minutes in
       test_mode. After the run, search the day's log file in logs/ for the string
       "database is locked". Expected: zero occurrences. Confirm any purchase-marking log
       lines ("Marked purchased") correspond to successful writes.

    Report the observed timestamps (concurrency + stagger), the intervention behavior,
    and the zero-lock soak result.
  </how-to-verify>
  <resume-signal>Type "approved" if SC1-SC4 all observed as expected, or describe what deviated (e.g. sequential timestamps, port conflict, both plugins paused, or any "database is locked" occurrence).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getpass CVV (sync, pre-loop) -> bestbuy plugin | CVV collected before asyncio.run, threaded onto the plugin in memory |
| main entry -> orchestrator async runtime | Single delegation seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | CVV collection | mitigate | collect_cvv() uses getpass (no echo), runs in the synchronous scope BEFORE asyncio.run(); CVV is never logged and never passed to the stdin listener. Gate skips collection in test_mode. (SEC-02 preserved) |
| T-04-18 | Spoofing | malformed config.yml | mitigate | AppConfig() validation preserved; ValidationError -> SystemExit(1) with actionable message before any browser launches (CORE-05 preserved) |
| T-04-19 | Denial of Service | orchestrator never tearing down browsers on exit | mitigate | async_main's finally calls registry.teardown_all() inside the loop (Plan 03, Pitfall 2); nodriver atexit handler is the second safety net |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/ -q` full suite green (phase gate)
- grep confirms main.py delegates to orchestrator and has no while-True loop
- Human-verify checkpoint: SC1 overlap, SC2 stagger/no-port-conflict, SC3 intervention, SC4 60-min zero-lock
</verification>

<success_criteria>
- main.py delegates to core.orchestrator.async_main; sequential loop removed (ASYNC-01)
- CVV getpass gate and AppConfig validation preserved
- Live run confirms concurrency, 1.5s stagger, Event intervention, and zero database-lock over 60+ min
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/04-async-orchestrator/04-05-SUMMARY.md` when done
</output>
