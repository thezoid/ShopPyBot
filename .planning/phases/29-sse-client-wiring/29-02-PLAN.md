---
phase: 29-sse-client-wiring
plan: 02
type: execute
wave: 1
depends_on: ["29-01"]
files_modified:
  - web/templates/dashboard.html
  - web/static/components.css
autonomous: true
requirements: [SSE-01]
must_haves:
  truths:
    - "The dashboard behaves identically to Phase 28 after this plan (polling still drives all updates) — zero observable change yet"
    - "renderStatus(data) is a pure function that updates the status dot/label, start/stop buttons, uptime, and health cards"
    - "pollStatus() fetches /api/status and delegates rendering to renderStatus(data)"
    - "appendLogLine() skips a line identical to the immediately-preceding rendered line"
    - "A Live/Reconnecting indicator element exists in the sticky header between #header-uptime and #theme-toggle"
  artifacts:
    - path: "web/templates/dashboard.html"
      provides: "renderStatus(data) shared render fn, no-dup guard in appendLogLine, #sse-indicator header element"
      contains: "function renderStatus"
    - path: "web/static/components.css"
      provides: ".status-dot.reconnecting amber state for the Reconnecting indicator"
      contains: ".status-dot.reconnecting"
  key_links:
    - from: "web/templates/dashboard.html pollStatus()"
      to: "renderStatus(data)"
      via: "fetch /api/status then renderStatus(data)"
      pattern: "renderStatus\\("
    - from: "web/templates/dashboard.html appendLogLine()"
      to: "_lastLogLine module var"
      via: "early-return on duplicate"
      pattern: "_lastLogLine"
---

<objective>
Land the three low-risk, zero-observable-change preparation steps that MUST exist before the
EventSource swap in Plan 03: (1) extract the render body of pollStatus() into a pure
renderStatus(data) shared function; (2) add the no-dup guard to appendLogLine(); (3) add the
Live/Reconnecting indicator element to the sticky header HTML plus a token-driven
.status-dot.reconnecting CSS rule. After this plan the dashboard still polls exactly as in
Phase 28 — nothing in runtime behavior changes — so all existing tests stay GREEN and the
EventSource swap in Plan 03 becomes a trivial, independently-revertable trigger change.

Purpose: Pitfall 3 (skipping renderStatus extraction) and Pitfall 6 (indicator referenced
before it exists in the DOM) from 29-RESEARCH are eliminated up front; the SSE swap then only
changes the trigger, not the render logic. Implements CONTEXT locked decisions: renderStatus
single-source-of-truth, no-dup guard, indicator element with --color-status-warn for Reconnecting.
Output: modified web/templates/dashboard.html and web/static/components.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-sse-client-wiring/29-CONTEXT.md
@.planning/phases/29-sse-client-wiring/29-RESEARCH.md

<interfaces>
<!-- Exact current structure from dashboard.html (read 2026-06-27); reuse identifiers verbatim. -->

pollStatus() body to extract (dashboard.html lines 321-355): the try block fetches /api/status,
then sets status-dot className, status-label textContent, btn-start/btn-stop disabled, calls
renderUptime(data.uptime_secs) and renderHealthCards(data), resets failCount. The catch block
(failCount++, #log-content "Connection lost" message, health-grid error message) stays inside
pollStatus — it is the polling-error path, NOT part of renderStatus.

appendLogLine(lineText) (dashboard.html lines 477-488): creates a span with class
'log-line ' + logLevelClass(lineText), sets textContent, appends to #log-buffer, enforces the
MAX_LOG_LINES (500) DOM cap. No-dup guard goes at the TOP, before any DOM work.

Sticky header (dashboard.html lines 30-35):
```
<header class="app-header">
  <span class="app-header-title">ShopPyBot</span>
  <span id="header-uptime" class="text-secondary"></span>
  <button type="button" id="theme-toggle" ...>...</button>
</header>
```
Indicator inserts between #header-uptime and #theme-toggle.

Existing CSS classes (web/static/components.css):
- `.status-dot.running { background: var(--color-status-ok); }`   (Live)
- `.status-dot.stopped { background: var(--color-status-neutral); }`
- `.status-dot.error   { background: var(--color-status-err); }`
Tokens available in tokens.css: --color-status-ok, --color-status-warn, --color-status-neutral, --color-status-err.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract renderStatus(data) and add appendLogLine no-dup guard</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html (pollStatus lines 321-355; appendLogLine lines 477-488; renderUptime line 314; renderHealthCards line 235)
    - .planning/phases/29-sse-client-wiring/29-RESEARCH.md ("Surgical Change Map" items 2 and 3; Pitfall 3; Pitfall 5)
    - .planning/phases/29-sse-client-wiring/29-CONTEXT.md (renderStatus single-source-of-truth + no-dup guard locked decisions)
  </read_first>
  <action>
    In web/templates/dashboard.html: define a new pure function renderStatus(data) (place it
    immediately before pollStatus). Move the success-path render logic currently inside
    pollStatus into renderStatus(data): read status-dot, status-label, btn-start, btn-stop; if
    data.running set dot className 'status-dot running', label 'Running', btn-start disabled true,
    btn-stop disabled false; else 'status-dot stopped', label 'Stopped', btn-start disabled false,
    btn-stop disabled true; then renderUptime(data.uptime_secs) and renderHealthCards(data).
    renderStatus performs NO fetch and does NOT touch failCount. Rewrite pollStatus as a thin
    wrapper: in the try block, fetch /api/status, parse json, call renderStatus(data), then
    failCount = 0; KEEP the existing catch block exactly as-is (failCount++, the >=3 #log-content
    "Connection lost — retrying..." message, and the health-grid error fallback) — that is the
    polling-error path and must remain in pollStatus per renderStatus(data) being a pure renderer.
    This is a zero-observable-change refactor: polling still calls pollStatus, which now renders
    via renderStatus. Per CONTEXT decision (research suggested either approach), renderStatus is
    the single source of truth shared by the future SSE handler and the polling fallback.

    Add the no-dup guard to appendLogLine: declare a module-scope `var _lastLogLine = '';` near
    the other log-viewer state (alongside logFollowEnabled). At the very top of appendLogLine,
    `if (lineText === _lastLogLine) return;` then `_lastLogLine = lineText;` BEFORE creating any
    DOM. Keep the rest of appendLogLine (span creation, textContent, append, 500-line cap)
    unchanged. Use last-line compare only (Pitfall 5: do not build a hash set of all lines).
    Use textContent only; introduce no innerHTML (Pitfall 8 / safe-DOM guard).
  </action>
  <verify>
    <automated>rtk pytest tests/test_web_dashboard.py tests/test_observability_ui.py tests/test_sse_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.html contains `function renderStatus` and pollStatus calls `renderStatus(`.
    - dashboard.html contains `_lastLogLine` and appendLogLine early-returns on a duplicate of the
      immediately-preceding line.
    - test_sse_wiring W29-A5 (renderStatus present) and W29-A6 (_lastLogLine present) turn GREEN.
    - test_observability_ui.py and test_web_dashboard.py stay GREEN (no observable behavior change;
      polling path still renders health cards, uptime, status dot, buttons).
    - test_no_innerHTML_with_api_data stays GREEN (no new innerHTML introduced).
  </acceptance_criteria>
  <done>
    renderStatus(data) is the shared pure renderer; pollStatus is a thin fetch wrapper around it;
    appendLogLine skips an exact-duplicate immediately-preceding line; the dashboard behaves
    identically to Phase 28 under polling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Live/Reconnecting indicator element + .reconnecting CSS rule</name>
  <files>web/templates/dashboard.html, web/static/components.css</files>
  <behavior>
    - Test (W29-A7): served GET / HTML contains `id="sse-indicator"`.
    - Test (W29-A9 regression): components.css contains zero hardcoded hex/rgb() after the addition
      (test_no_hardcoded_hex_in_components stays green; .status-dot.reconnecting uses var(--color-status-warn)).
    - Test (W29-A10 regression): MC-4 non-local banner test stays green (header edit did not drop the banner).
  </behavior>
  <read_first>
    - web/templates/dashboard.html (sticky header lines 30-35)
    - web/static/components.css (status-dot rules lines 64-66, 231)
    - web/static/tokens.css (--color-status-ok / --color-status-warn declarations)
    - .planning/phases/29-sse-client-wiring/29-RESEARCH.md ("Pattern 4: Live/Reconnecting Indicator"; Open Question 1 recommendation; Pitfall 6, 7)
    - .planning/phases/29-sse-client-wiring/29-CONTEXT.md (indicator: --color-status-ok for Live, --color-status-warn for Reconnecting; zero hardcoded hex)
  </read_first>
  <action>
    In web/templates/dashboard.html sticky header, insert the indicator element between the
    #header-uptime span and the #theme-toggle button: a span with id="sse-indicator" class
    "sse-indicator" and aria-live="polite", containing a child span with id="sse-dot" class
    "status-dot" and a child span with id="sse-label" class "text-secondary" (empty text content,
    populated by JS in Plan 03). Use static markup only — no innerHTML, no API data. This resolves
    Pitfall 6 (element must exist before Plan 03's setIndicator references its ids).
    In web/static/components.css, add exactly one rule resolving Open Question 1 (Reconnecting =
    amber/warn per CONTEXT): `.status-dot.reconnecting { background: var(--color-status-warn); }`.
    The Live state will reuse the existing `.status-dot.running` (--color-status-ok). Use ONLY the
    var(--color-status-warn) token — no `#` hex, no rgb() literal (Pitfall 7; test_no_hardcoded_hex
    guard). Optionally add a minimal layout helper for .sse-indicator (e.g. inline-flex gap) using
    existing --space-* tokens only if needed for alignment; if added it must contain zero hex/rgb.
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py tests/test_design_system.py tests/test_web_security.py -q</automated>
  </verify>
  <acceptance_criteria>
    - GET / HTML contains `id="sse-indicator"` (W29-A7 GREEN), with child #sse-dot and #sse-label.
    - components.css contains `.status-dot.reconnecting` using var(--color-status-warn); no hex/rgb.
    - test_no_hardcoded_hex_in_components stays GREEN (W29-A9).
    - MC-4 non-local banner test stays GREEN (W29-A10) — the banner and CSRF gate are untouched.
    - Indicator label/dot have no text/color set yet (JS wiring is Plan 03); element is inert markup.
  </acceptance_criteria>
  <done>
    The #sse-indicator element (dot + label) sits in the sticky header between #header-uptime and
    #theme-toggle; a token-only .status-dot.reconnecting amber rule exists; all CSS and security
    guards stay green; no runtime behavior change yet (label is empty until Plan 03 wires it).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE/poll data → DOM render | status/plugin/last_error/log strings rendered into the dashboard DOM |
| static asset (components.css) → served CSS | one new color rule served to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02 | Tampering (XSS) | renderStatus / appendLogLine rendering status, plugin name, last_error, log lines | mitigate | renderStatus reuses the existing renderHealthCards/renderUptime safe-DOM (createElement/textContent) paths; appendLogLine uses textContent; no innerHTML introduced; guarded by test_no_innerHTML_with_api_data |
| T-29-03 | Information Disclosure | last_error surfaced via status render | accept | last_error already scrubbed to exception class name at get_status() boundary (Phase 26); this plan introduces no new data path, only refactors the render trigger |
| T-29-04 | Tampering | components.css new color rule with hardcoded hex | mitigate | .status-dot.reconnecting uses var(--color-status-warn) only; enforced by test_no_hardcoded_hex_in_components |
| T-29-05 | Tampering | MC-4 non-local banner / CSRF gate accidentally dropped during header edit | mitigate | Header edit inserts only the indicator span; banner + CSRF gate untouched; verified by MC-4 test (test_web_security) |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (29-RESEARCH Package Legitimacy Gate: skipped) |
</threat_model>

<verification>
- `rtk pytest tests/test_web_dashboard.py tests/test_observability_ui.py tests/test_sse_wiring.py tests/test_design_system.py tests/test_web_security.py -q` — all GREEN except the Plan 03 SSE-block assertions (W29-A1..A4, A11) which remain RED until Plan 03.
- Full suite: `rtk pytest -q` — no regression to Phase 25/27/28 surfaces (this plan changes the render trigger structure and adds inert markup/CSS only; polling behavior unchanged).
</verification>

<success_criteria>
- renderStatus(data) extracted as a pure shared renderer; pollStatus is a thin fetch wrapper (W29-A5 GREEN).
- appendLogLine no-dup guard via _lastLogLine last-line compare (W29-A6 GREEN).
- #sse-indicator element present in sticky header; .status-dot.reconnecting token-only CSS rule added (W29-A7, A9 GREEN).
- Zero observable runtime change: dashboard still polls and renders identically to Phase 28; MC-4 + safe-DOM + hex guards green.
</success_criteria>

<output>
Create `.planning/phases/29-sse-client-wiring/29-02-SUMMARY.md` when done.
</output>
