---
phase: 06-platform-expansion
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - SECURITY.md
  - tests/test_security_md.py
autonomous: true
requirements: [PLG-04, PLG-05, PLG-06, PLG-07, PLG-08]
must_haves:
  truths:
    - "SECURITY.md's platform risk table has one row per new platform: walmart.com, target.com, gamestop.com, store.square-enix-games.com, newegg.com"
    - "The Walmart row documents PerimeterX/HUMAN Security; the Target row documents Akamai + headless blocking (SC4)"
    - "Each row carries an anti-detection risk level and a TOS/legal risk consistent with the plugin docstring"
    - "test_security_md.py asserts all five new rows and the SC4 exact phrases are present"
    - "The full test suite is green with all five plugins discovered by the registry (SC1)"
  artifacts:
    - path: "SECURITY.md"
      provides: "5 appended platform risk rows (single source of truth for risk ratings)"
      contains: "store.square-enix-games.com"
    - path: "tests/test_security_md.py"
      provides: "SC4 row-presence + exact-phrase assertions"
  key_links:
    - from: "SECURITY.md Platform Risk table"
      to: "plugin docstrings"
      via: "consistent per-platform risk levels"
      pattern: "walmart.com"
---

<objective>
Append the five new-platform risk rows to the repo-root SECURITY.md risk table (the single source of
truth created in Phase 3), and add tests/test_security_md.py asserting every new row plus the SC4
exact phrases are present. Run the full suite as the phase gate to confirm SC1 (registry discovers all
five new plugins with no core edits) and zero regressions.

Purpose: SC4 requires the Walmart README/SECURITY risk documents PerimeterX/HUMAN Security and the
Target document records Akamai headless blocking. This plan closes the documentation half of SC4
(the docstring half ships in Plans 06-03/06-04) and provides the final phase verification gate.
Output: 5 SECURITY.md rows + a SECURITY.md test + full-suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-platform-expansion/06-CONTEXT.md
@SECURITY.md
@plugins/shopbot_plugin_walmart.py
@plugins/shopbot_plugin_target.py
@plugins/shopbot_plugin_gamestop.py
@plugins/shopbot_plugin_squareenix.py
@plugins/shopbot_plugin_newegg.py

<interfaces>
<!-- The existing SECURITY.md risk table the executor appends rows to. Verified from file read. -->

From SECURITY.md "## Platform Risk" (existing table, three-tier scale low/medium/high):
| Platform     | Anti-Detection Risk | TOS/Legal Risk | Notes |
|--------------|---------------------|----------------|-------|
| amazon.com   | High   | High   | ... |
| bestbuy.com  | Medium | Medium | ... |
(The table is followed by a sentence instructing that new platforms append a row here.)

Risk levels to mirror from the plugin docstrings (delivered by Plans 06-03/06-04):
- walmart.com: HIGH -- PerimeterX/HUMAN Security (SC4 exact phrase required)
- target.com: HIGH / experimental -- Akamai Bot Manager, headless blocking (SC4: "Akamai" + "headless" required)
- gamestop.com: MEDIUM -- checkout CAPTCHA
- store.square-enix-games.com: MEDIUM (best estimate)
- newegg.com: MEDIUM (best estimate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 5 platform risk rows to SECURITY.md</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (the "## Platform Risk" table format + the new-platform-append instruction)
    - plugins/shopbot_plugin_walmart.py / target / gamestop / squareenix / newegg (docstring risk levels to keep consistent)
  </read_first>
  <action>
    Append five new rows to the existing "## Platform Risk" markdown table in SECURITY.md, one per new
    platform, matching the existing column layout (Platform | Anti-Detection Risk | TOS/Legal Risk | Notes):
      - walmart.com -- Anti-Detection: High; TOS/Legal: High; Notes MUST contain the exact phrase
        "PerimeterX/HUMAN Security" and state automated purchasing violates Walmart TOS (SC4).
      - target.com -- Anti-Detection: High; TOS/Legal: High; Notes MUST contain "Akamai" and the word
        "headless" stating auto-buy is experimental because Akamai blocks headless sessions (SC4).
      - gamestop.com -- Anti-Detection: Medium; TOS/Legal: Medium; Notes mention checkout CAPTCHA.
      - store.square-enix-games.com -- Anti-Detection: Medium; TOS/Legal: Medium; Notes note best-estimate
        risk and that selectors are best-effort.
      - newegg.com -- Anti-Detection: Medium; TOS/Legal: Medium; Notes note best-estimate risk.
    Keep risk levels consistent with each plugin's module docstring. Do NOT use any em dashes and do NOT
    add any --- horizontal rule lines (project markdown rule); use commas/colons or restructure. Do not
    alter the existing amazon/bestbuy rows or any other section.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -c "t=open('SECURITY.md',encoding='utf-8').read(); assert 'walmart.com' in t and 'target.com' in t and 'gamestop.com' in t and 'store.square-enix-games.com' in t and 'newegg.com' in t and 'PerimeterX/HUMAN Security' in t and 'Akamai' in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md contains a table row referencing each of: walmart.com, target.com, gamestop.com, store.square-enix-games.com, newegg.com
    - SECURITY.md contains the exact phrase "PerimeterX/HUMAN Security" (Walmart row)
    - SECURITY.md Target row contains "Akamai" and "headless"
    - grep: no new em dash characters and no "---" horizontal-rule line was added
    - existing amazon.com / bestbuy.com rows unchanged
  </acceptance_criteria>
  <done>Five new-platform risk rows appended with consistent risk levels and the SC4 exact phrases; no em dashes / no --- rules; existing rows intact.</done>
</task>

<task type="auto">
  <name>Task 2: Fill tests/test_security_md.py + run full-suite SC1 gate</name>
  <files>tests/test_security_md.py</files>
  <read_first>
    - tests/test_security_md.py (scaffold from Plan 06-01)
    - SECURITY.md (the appended rows from Task 1)
    - tests/test_registry.py (the discovery test pattern for the SC1 assertion)
  </read_first>
  <behavior>
    - Reading SECURITY.md, all five new platform identifiers are present as table rows
    - The exact phrase "PerimeterX/HUMAN Security" is present (Walmart, SC4)
    - Both "Akamai" and "headless" are present (Target, SC4)
    - The real plugins/ directory yields 7 discovered plugin classes via the registry (2 existing + 5 new) with no import error (SC1)
  </behavior>
  <action>
    Remove the skip marker from tests/test_security_md.py and implement: a test that reads the repo-root
    SECURITY.md and asserts each of the five new platform strings (walmart.com, target.com, gamestop.com,
    store.square-enix-games.com, newegg.com) appears; a test asserting the SC4 exact phrase
    "PerimeterX/HUMAN Security" is present; a test asserting "Akamai" and "headless" both appear. Add one
    SC1 registry test: construct PluginRegistry(config=None, plugins_dir=<repo>/plugins) against the REAL
    plugins directory and assert len(registry._all_plugins) == 7 (Amazon, BestBuy, Walmart, Target,
    GameStop, SquareEnix, Newegg) -- proving all five new files load with no core edits. Resolve the repo
    root via Path(__file__).parent.parent. Then run the full suite to confirm zero regressions.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_security_md.py passes with no skip marker
    - A test asserts all 5 new SECURITY.md rows are present
    - A test asserts "PerimeterX/HUMAN Security" and ("Akamai" + "headless") phrases present
    - An SC1 test asserts the real plugins/ registry discovers exactly 7 plugin classes
    - Full suite `pytest tests/ -q` is green with zero failures and zero remaining scaffold skips
  </acceptance_criteria>
  <done>SECURITY.md row + SC4 phrase tests and the SC1 7-plugin discovery test pass; full suite green; phase gate clear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SECURITY.md docs -> operator | Risk documentation informs the operator's TOS/legal decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Repudiation | per-platform risk documentation | mitigate | SECURITY.md is the single source of truth for risk ratings; rows are HONEST about residual risk and do not claim the bot defeats PerimeterX/Akamai/CAPTCHA (RESEARCH Security Domain) |
| T-06-14 | Information Disclosure | SECURITY.md rows | accept | Rows contain only public protection-vendor names and risk tiers; no credentials, secrets, or operator data |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/ -q` is fully green with no scaffold skips remaining.
- SECURITY.md contains all five rows and the SC4 exact phrases.
- The real plugins/ registry discovers 7 plugin classes (SC1).
</verification>

<success_criteria>
- SC4 documentation half complete: Walmart row documents PerimeterX/HUMAN Security; Target row documents Akamai + headless.
- SC1 proven: all five new plugins load via the registry with no core edits (7 total discovered).
- Full suite green; phase ready for verify-work.
</success_criteria>

<output>
Create `.planning/phases/06-platform-expansion/06-05-SUMMARY.md` when done.
</output>
