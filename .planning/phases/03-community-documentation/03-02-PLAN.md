---
phase: 03-community-documentation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .github/PULL_REQUEST_TEMPLATE.md
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/plugin_request.yml
  - .github/ISSUE_TEMPLATE/config.yml
autonomous: true
requirements: [DOCS-04, DOCS-05]
must_haves:
  truths:
    - "Submitting a bug report or plugin request via GitHub Issues presents a pre-filled template with required fields"
    - "Opening a pull request presents a checklist covering ABC compliance, naming convention, test presence, and risk documentation"
    - "Blank issues are disabled and security reports are routed to SECURITY.md / the private advisory channel"
  artifacts:
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR checklist: ABC compliance, shopbot_plugin_* naming, test present+green, risk declared, no secrets"
      contains: "shopbot_plugin_"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Bug report issue form with required fields"
      contains: "name:"
    - path: ".github/ISSUE_TEMPLATE/plugin_request.yml"
      provides: "New-plugin / platform request issue form with required fields"
      contains: "name:"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "blank_issues_enabled false + security contact link to SECURITY.md"
      contains: "blank_issues_enabled"
  key_links:
    - from: ".github/ISSUE_TEMPLATE/config.yml"
      to: "SECURITY.md"
      via: "contact_links url"
      pattern: "SECURITY\\.md"
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "CONTRIBUTING.md plugin checklist"
      via: "mirrored checklist items"
      pattern: "ABC"
---

<objective>
Create the GitHub contribution templates: a PR template with the submission checklist, two issue forms (bug report, plugin request), and the ISSUE_TEMPLATE config.yml that disables blank issues and routes security reports to SECURITY.md. These mirror the canonical checklist in CONTRIBUTING.md and the disclosure channel in SECURITY.md (created in Plan 01).

Purpose: Contributors filing issues and PRs are guided into the required fields and checks automatically by GitHub's native template system.
Output: .github/PULL_REQUEST_TEMPLATE.md plus three files under .github/ISSUE_TEMPLATE/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-community-documentation/03-CONTEXT.md

<read_first>
- CONTRIBUTING.md (created in Plan 01) — the PR checklist MUST mirror the Plugin Submission Checklist items (ABC compliance, shopbot_plugin_* naming, test present + green, anti-detection risk declared, no secrets).
- SECURITY.md (created in Plan 01) — the config.yml security contact link points reporters here; keep the relative path/URL consistent.
- core/registry.py — confirms the shopbot_plugin_*.py naming convention enforced by the PR checklist.
- core/plugin_base.py — RetailerPlugin ABC referenced by the "ABC compliance" checklist item.
</read_first>

GITHUB ISSUE FORM FORMAT NOTES (for the executor):
- Issue forms are YAML files with top-level keys: name, description, title (optional), labels (optional), body (a list of form elements).
- Each body element has a type (markdown, input, textarea, dropdown, checkboxes) and an id/attributes; required fields use validations: {required: true}.
- config.yml uses blank_issues_enabled and contact_links (a list of {name, url, about}).

PROJECT STYLE RULES (enforced for all generated markdown):
- NO em dashes anywhere. Use colons, commas, or restructure.
- NO horizontal-rule lines (a line containing only ---). (Note: YAML front-matter style document separators are not used in these files; do not add any '---' separator lines in the markdown PR template.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two issue forms and ISSUE_TEMPLATE/config.yml</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/plugin_request.yml, .github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>SECURITY.md (for the contact link target), .planning/phases/03-community-documentation/03-CONTEXT.md (Issue and PR Templates decisions)</read_first>
  <action>
Create the three files under .github/ISSUE_TEMPLATE/ implementing DOCS-04.

bug_report.yml: a GitHub issue form (name: "Bug Report", a description, labels such as ["bug"]). Required body fields: a description of the bug (textarea, required), steps to reproduce (textarea, required), the platform/plugin affected (input or dropdown seeded with Amazon, BestBuy, Other), expected vs actual behavior, environment (OS + Python version, input, required), and a confirmation checkbox that no credentials or secrets are included in logs/screenshots pasted. Add a leading markdown element directing security vulnerabilities to SECURITY.md instead.

plugin_request.yml: a GitHub issue form (name: "New Plugin / Platform Request", description, labels such as ["plugin-request"]). Required body fields: target retailer name and homepage URL (input, required), example product URL (input), whether the contributor intends to implement it themselves (dropdown), anticipated anti-detection risk level (dropdown: low / medium / high, required) with a rationale textarea, and any known TOS/legal concerns (textarea).

config.yml: set blank_issues_enabled: false. Add a contact_links list with one entry routing security reports: name "Report a security vulnerability", a url pointing to SECURITY.md (use the repo-relative blob path or the SECURITY.md file reference consistent with how the repo is hosted), and an about line stating vulnerabilities must not be filed as public issues.

All three must be valid YAML.
  </action>
  <verify>
    <automated>python -c "import yaml; [yaml.safe_load(open('.github/ISSUE_TEMPLATE/'+f,encoding='utf-8')) for f in ['bug_report.yml','plugin_request.yml','config.yml']]; c=yaml.safe_load(open('.github/ISSUE_TEMPLATE/config.yml',encoding='utf-8')); assert c['blank_issues_enabled'] is False, 'blank issues not disabled'; assert any('SECURITY.md' in (l.get('url','')) for l in c.get('contact_links',[])), 'no SECURITY.md contact link'; br=yaml.safe_load(open('.github/ISSUE_TEMPLATE/bug_report.yml',encoding='utf-8')); pr=yaml.safe_load(open('.github/ISSUE_TEMPLATE/plugin_request.yml',encoding='utf-8')); assert any(e.get('validations',{}).get('required') for e in br['body']), 'bug_report has no required field'; assert any(e.get('validations',{}).get('required') for e in pr['body']), 'plugin_request has no required field'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
- All three files exist under .github/ISSUE_TEMPLATE/ and parse as valid YAML.
- bug_report.yml is an issue form with at least one required field and a note routing security reports to SECURITY.md.
- plugin_request.yml is an issue form with at least one required field including an anti-detection risk-level dropdown (low/medium/high).
- config.yml sets blank_issues_enabled: false and contains a contact_links entry whose url references SECURITY.md.
  </acceptance_criteria>
  <done>The three issue-template files satisfy all acceptance criteria and the automated verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write .github/PULL_REQUEST_TEMPLATE.md</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>CONTRIBUTING.md (mirror the Plugin Submission Checklist), core/plugin_base.py (RetailerPlugin ABC), .planning/phases/03-community-documentation/03-CONTEXT.md (PR template checklist decision)</read_first>
  <action>
Create .github/PULL_REQUEST_TEMPLATE.md implementing DOCS-05. Include a short "Summary" section (what the PR changes and why) and a "Checklist" section as a markdown task list mirroring the CONTRIBUTING.md plugin submission checklist:

- RetailerPlugin ABC compliance (implements the required async methods check_availability and auto_buy).
- File follows the shopbot_plugin_*.py naming convention (for plugin PRs).
- domain_patterns class attribute is set.
- A pytest test file is included and the full suite is green (python -m pytest tests/ -q).
- Anti-detection risk level declared (low / medium / high) with rationale, present in this PR and in the plugin module docstring.
- No secrets or credentials committed (env vars only; .env not committed).
- Commits follow Conventional Commits.

Add a "Risk Declaration" prompt asking non-plugin PRs to mark plugin-specific items N/A. Do not add any '---' horizontal-rule separator lines and no em dashes.
  </action>
  <verify>
    <automated>python -c "import re; t=open('.github/PULL_REQUEST_TEMPLATE.md',encoding='utf-8').read(); tl=t.lower(); assert 'abc' in tl, 'no ABC compliance item'; assert 'shopbot_plugin_' in tl, 'no naming convention item'; assert 'pytest' in tl, 'no test item'; assert 'risk' in tl, 'no risk declaration item'; assert 'secret' in tl or 'credential' in tl, 'no no-secrets item'; assert '- [ ]' in t, 'no checklist syntax'; assert '—' not in t, 'em dash found'; assert not re.search(r'(?m)^---\s*$', t), 'horizontal rule found'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
- .github/PULL_REQUEST_TEMPLATE.md exists.
- Contains a markdown task-list checklist covering ABC compliance, shopbot_plugin_* naming, test present + green (pytest), anti-detection risk declared, and no secrets committed.
- Checklist items mirror the CONTRIBUTING.md plugin submission checklist.
- Contains no em dashes and no '---' horizontal-rule lines.
  </acceptance_criteria>
  <done>PULL_REQUEST_TEMPLATE.md satisfies all acceptance criteria and the automated verify passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issue reporter to public repo | Issue-template content is public; a misrouted security report would disclose a vulnerability publicly |
| contributor to repository | PR template enforces the no-secrets / ABC / naming checks before merge review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | ISSUE_TEMPLATE/config.yml | mitigate | blank_issues_enabled: false + contact_links route security reports to SECURITY.md, steering vulnerabilities off public issues |
| T-03-05 | Information Disclosure | issue forms / PR template | mitigate | Bug form note + PR checklist item enforce "no credentials/secrets in logs, screenshots, or commits" |
| T-03-06 | Tampering | low-quality / non-compliant plugin PRs | mitigate | PR checklist requires ABC compliance, naming convention, green test suite, and a declared anti-detection risk before review |
</threat_model>

<verification>
- All four template files exist; the three YAML files parse as valid YAML.
- config.yml disables blank issues and links the security channel to SECURITY.md.
- PR template checklist mirrors CONTRIBUTING.md and covers ABC, naming, tests, risk, secrets.
- No em dashes and no horizontal-rule lines in the markdown template.
</verification>

<success_criteria>
- DOCS-04: GitHub issue templates exist for bug reports and plugin/platform requests with required fields; blank issues disabled; security routed privately.
- DOCS-05: GitHub PR template checklist covers ABC compliance, naming convention, test presence, and risk documentation.
</success_criteria>

<output>
Create `.planning/phases/03-community-documentation/03-02-SUMMARY.md` when done.
</output>
