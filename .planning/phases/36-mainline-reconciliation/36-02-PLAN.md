---
phase: 36-mainline-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - requirements.txt
  - .github/dependabot.yml
  - .planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md
  - .planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md
autonomous: true
requirements: [MAIN-02, MAIN-03]
must_haves:
  truths:
    - "Local HEAD is no longer diverged from PR #11's remote head, and no force-push was needed to achieve it"
    - "Every local commit not yet on PR #11's head has a recorded include-or-exclude decision keyed by SHA"
    - "The merged requirements.txt keeps httpx==0.28.1 and takes the newer cryptography and pydantic-settings pins, one line each"
    - "The merged dependabot.yml keeps master's groups blocks and the branch's labels, monday schedule, and pip limit of 10"
    - "The merged ci.yml installs requirements.txt before the editable install and never references the runner context at job-level env"
    - "The full local pytest suite is green on the merged tree before anything is pushed"
  artifacts:
    - path: "requirements.txt"
      provides: "Union-resolved dependency pins for the merged tree"
      contains: "httpx==0.28.1"
    - path: ".github/dependabot.yml"
      provides: "Union-resolved Dependabot config keeping master's groups and the branch's labels/schedule"
      contains: "minor-and-patch"
    - path: ".planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md"
      provides: "MAIN-03 record: the freshly derived SHA list plus the include-or-exclude decision per SHA"
      contains: "include"
  key_links:
    - from: "local HEAD"
      to: "origin/chore/v4.0-milestone-close"
      via: "no-op merge of the stray content-empty commit e2f2695, avoiding force-push"
      pattern: "git merge origin/chore/v4.0-milestone-close"
    - from: "merged tree .github/workflows/ci.yml"
      to: "master's fixed Install and Test steps"
      via: "clean auto-merge taking master's step-scoped SHOPBOT_DATA_DIR and requirements.txt install"
      pattern: "pip install -r requirements.txt"
---

<objective>
Reconcile PR #11's head locally: absorb the stray remote-only commit without a force-push, record the MAIN-03 include-or-exclude decision against a freshly derived SHA list, resolve the two real merge conflicts to the locked union content, and prove the merged tree green with the full local pytest suite. Nothing is pushed in this plan.

Purpose: 36-CONTEXT.md's verification bar is that a bad conflict resolution must be caught before it reaches master. This plan is that gate. It ends with a resolved, locally proven merge commit sitting on the working branch, ready for plan 03 to push.

Output: a merge commit resolving `.github/dependabot.yml` and `requirements.txt`, a committed `36-COMMIT-DISPOSITION.md`, and a green full-suite run recorded in `36-MERGE-LOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-mainline-reconciliation/36-CONTEXT.md
@.planning/phases/36-mainline-reconciliation/36-RESEARCH.md
@.planning/phases/36-mainline-reconciliation/36-VALIDATION.md
@.planning/phases/36-mainline-reconciliation/36-01-SUMMARY.md

<authorization>
Phase-scoped, pre-authorized by 36-CONTEXT.md "Merge Execution and Authority": the executor
commits to the working branch without pausing on the global commit-approval gate. This plan
performs NO push and NO GitHub mutation.
</authorization>

<tooling_constraint>
Raw `git` only for every ancestry, range, rev-list, and log query. Do NOT prefix with `rtk`.
Reason (36-VALIDATION.md "Tooling constraint"): `rtk git log <range>` was observed reporting an
EMPTY range for `HEAD..origin/chore/v4.0-milestone-close`, which actually contains two commits
(`e2f2695` and `d9ad585`). A filtered log here is a correctness hazard, not a formatting
difference, and the MAIN-03 record is derived directly from a range query.

Python and pip invocations use forward slashes (`.venv/Scripts/python.exe`), valid in both
PowerShell and Git Bash on this machine. 36-VALIDATION.md writes the same path with backslashes;
they are the same interpreter.
</tooling_constraint>

<verified_state>
Live-verified 2026-08-02 immediately before planning. Re-derive at execution time.

- `git merge-tree --write-tree origin/master HEAD` reports EXACTLY two conflicts:
  `.github/dependabot.yml` (add/add) and `requirements.txt` (content).
  `.github/workflows/ci.yml` auto-merges cleanly and takes master's corrected Install and Test
  steps. If a third conflicted path appears at execution time, the premise changed: STOP.
- `git merge-tree --write-tree HEAD origin/chore/v4.0-milestone-close` produces a tree hash
  IDENTICAL to HEAD's own tree, proving the divergence-resolving merge is a content-empty no-op.
- The remote-only commits are `e2f2695` (a web-UI "Merge branch 'master' into
  chore/v4.0-milestone-close" that changed no files) and `d9ad585` (Merge pull request #10).
  Their existence is why a plain push is rejected as non-fast-forward.
- The local-ahead commit count has already drifted from 8 (36-CONTEXT.md) to 9 (36-RESEARCH.md)
  to 11 (planning time). NEVER hardcode a count. Re-derive the list.
- Local `.venv` already carries httpx 0.28.1, cryptography 49.0.0, pydantic-settings 2.14.2,
  pytest 9.0.3, pytest-asyncio 1.3.0, matching the union target.
- Expected suite size on the merged tree: roughly 939 tests (master alone runs 755).
</verified_state>

<union_targets>
The exact target file contents are in 36-RESEARCH.md "Code Examples". Read that section rather
than reconstructing from memory. The decision surface is small:

requirements.txt: 16 pinned lines under a leading `# python_requires >= 3.11` comment. Only THREE
lines differ between the two sides. `httpx==0.28.1` comes from master and MAIN-02 exists
specifically to stop it being dropped. `cryptography==49.0.0` and `pydantic-settings[yaml]==2.14.2`
come from the branch. Every other line is already byte-identical on both sides.

.github/dependabot.yml: two ecosystems, `pip` and `github-actions`. From master keep the
`groups.minor-and-patch.update-types` blocks (minor, patch) on BOTH ecosystems, which is the
reason PR #15 exists. From the branch keep `labels`, `schedule.day: "monday"`, and pip
`open-pull-requests-limit: 10`. The github-actions limit stays at 5 because neither side's diff
touched it.
</union_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Absorb the divergence with a no-op merge and record the MAIN-03 commit disposition</name>
  <files>.planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md, .planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md</files>
  <read_first>
.planning/phases/36-mainline-reconciliation/36-RESEARCH.md (Pitfall 3, the stray content-empty merge commit and why merging it in beats force-pushing; and "Open Questions" item 1, on re-deriving the SHA list fresh)
.planning/phases/36-mainline-reconciliation/36-CONTEXT.md ("Specific Ideas", which requires the record to be keyed by SHA so a reader can check git log master against it)
.planning/phases/36-mainline-reconciliation/36-VALIDATION.md (the MAIN-03 assertion row and the raw-git tooling constraint)
  </read_first>
  <action>
    Confirm the working tree is clean (`git status --porcelain` empty) and the current branch is `chore/v4.0-milestone-close` (`git rev-parse --abbrev-ref HEAD`). If either fails, STOP.

    Run `git fetch origin --prune`. Record the pre-merge divergence with raw git: `git rev-list --left-right --count HEAD...origin/chore/v4.0-milestone-close`. Expect a non-zero right-hand number (the remote-only commits). Confirm the remote-only commits are content-empty by listing them: `git log --oneline HEAD..origin/chore/v4.0-milestone-close`. Expected `e2f2695` and `d9ad585`.

    Resolve the divergence by merging, never by forcing: `git merge origin/chore/v4.0-milestone-close -m "chore(36): absorb remote-only merge commits from PR #11 head"`. 36-RESEARCH.md proved via `git merge-tree --write-tree` that this produces a tree identical to HEAD's own tree, so it must not report conflicts and must not change any file. Verify that: capture the tree hash before with `git rev-parse HEAD^{tree}` and confirm the post-merge `git rev-parse HEAD^{tree}` is the SAME hash. If the tree hash changed, the no-op premise is false: STOP and escalate. `git push --force` and `git push --force-with-lease` are PROHIBITED anywhere in this phase.

    Now derive the MAIN-03 SHA list FRESH, after the no-op merge, with raw git only: `git log --oneline --no-merges origin/chore/v4.0-milestone-close..HEAD` and also the merge-inclusive form `git log --oneline origin/chore/v4.0-milestone-close..HEAD`. Do NOT use `rtk git log` and do NOT assume the count is 8, 9, or 11; record whatever it actually is.

    Write `.planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md` containing:
    (a) the derivation command and the timestamp;
    (b) a table with columns SHA | Subject | Decision | Rationale, one row per commit in the derived list, every Decision being `include` per the locked 36-CONTEXT.md decision (all local commits are included; MAIN-03 is satisfied by RECORDING the decision per SHA, not by excluding any);
    (c) an explicit standing rule stating that the include decision covers every commit reachable from the branch tip at PR #11 push time, which necessarily includes the commit that adds this file itself and any plan-02 execution-artifact commits created before the push;
    (d) an explicit scope exclusion stating that phase-36 execution artifacts created AFTER the PR #11 merge (the SUMMARY files for plans 03, 04, and 05) are out of scope for this record and reach master through a later PR, so a reader comparing `git log master` against this table is not misled by their absence;
    (e) a placeholder line reading `Post-merge ancestry verification: pending (plan 03 task 2)`.

    Append one row to 36-MERGE-LOG.md recording the no-op merge (pre-tree hash, post-tree hash, and the derived commit count). Commit both files together with `docs(36): record MAIN-03 commit disposition and absorb PR #11 head divergence`. This commit MUST land before task 2 starts so that the merge commit in task 2 contains only conflict resolutions.
  </action>
  <acceptance_criteria>
    - `git log --oneline HEAD..origin/chore/v4.0-milestone-close` outputs nothing (zero remote-only commits remain).
    - `git merge-base --is-ancestor origin/chore/v4.0-milestone-close HEAD` exits 0.
    - The tree hash recorded before the no-op merge equals `git rev-parse HEAD^{tree}` taken immediately after it (before the disposition-file commit).
    - No command containing `--force` or `--force-with-lease` was executed.
    - 36-COMMIT-DISPOSITION.md exists, its SHA table row count equals the count returned by `git log --oneline origin/chore/v4.0-milestone-close..HEAD` at derivation time, every Decision cell reads `include`, and the file contains both the standing rule (c) and the post-merge scope exclusion (d).
    - Every SHA in the table is a real commit: `git cat-file -t <sha>` outputs `commit` for each.
    - `git status --porcelain` is empty at task end (both files committed).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git log --oneline HEAD..origin/chore/v4.0-milestone-close)" && git merge-base --is-ancestor origin/chore/v4.0-milestone-close HEAD && test -f .planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md && grep -c "include" .planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md && test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>Local HEAD contains every commit on PR #11's remote head, achieved by merge and not by force, and a per-SHA include record exists that a reader can check against `git log master`.</done>
</task>

<task type="auto">
  <name>Task 2: Merge origin/master and resolve the two conflicts to the locked union content</name>
  <files>requirements.txt, .github/dependabot.yml</files>
  <read_first>
.planning/phases/36-mainline-reconciliation/36-RESEARCH.md ("Code Examples", both target file contents verbatim; "PR #11 Conflict Resolution Sequence"; Pitfall 1 on the runner context; Pitfall 2 on pip install -e not installing requirements.txt)
.planning/phases/36-mainline-reconciliation/36-CONTEXT.md ("Conflict Resolution and PR Disposition", the locked union rules for both files)
requirements.txt (current branch-side content, before the merge)
.github/dependabot.yml (current branch-side content, before the merge)
  </read_first>
  <action>
    Run `git merge origin/master --no-commit` so the resolution is inspected before it is sealed. Immediately assert the conflict set: `git diff --name-only --diff-filter=U` must list EXACTLY `.github/dependabot.yml` and `requirements.txt` and nothing else. A third conflicted path means the merge-tree premise changed: `git merge --abort`, record the extra paths in 36-MERGE-LOG.md, and STOP.

    Resolve `requirements.txt` to the union content in 36-RESEARCH.md "Code Examples". Concretely: remove all conflict markers, keep the leading `# python_requires >= 3.11` comment, keep exactly one line each for `httpx==0.28.1` (from master, this is the pin MAIN-02 exists to protect), `cryptography==49.0.0` (from branch), and `pydantic-settings[yaml]==2.14.2` (from branch), and keep every other pin unchanged since both sides already agree on them. Preserve alphabetical ordering.

    Resolve `.github/dependabot.yml` to the union content in the same section. Concretely: keep master's `groups.minor-and-patch.update-types` block listing `minor` and `patch` under BOTH the `pip` and `github-actions` ecosystems, keep the branch's `labels` lists, keep `schedule.day: "monday"` on both, keep pip `open-pull-requests-limit: 10`, and leave github-actions `open-pull-requests-limit: 5` as-is.

    Verify ci.yml auto-merged correctly rather than assuming it. It is not in the conflict set, but 36-RESEARCH.md Corrections 1 and 2 make it the highest-value silent-regression surface in this merge. Assert all three of:
    (i) the requirements install line survived, ignoring the explanatory comment that mentions the same filename, via `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'pip install -r requirements.txt'` returning 1 or more;
    (ii) no job declares SHOPBOT_DATA_DIR at job-level env, via `.venv/Scripts/python.exe -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert all('SHOPBOT_DATA_DIR' not in (j.get('env') or {}) for j in d['jobs'].values())"` exiting 0 (this is Pitfall 1, the bug that produced the historic zero-job runs);
    (iii) SHOPBOT_DATA_DIR still exists somewhere in the file, so the fix was not simply deleted.

    Validate both resolved files parse. `.venv/Scripts/python.exe -c "import yaml; yaml.safe_load(open('.github/dependabot.yml'))"` must exit 0. Run the resolver dry-run that 36-RESEARCH.md "Don't Hand-Roll" prescribes: `.venv/Scripts/python.exe -m pip install --dry-run -r requirements.txt` must exit 0 with no resolver conflict. Confirm no conflict markers survive anywhere: `grep -rn '^<<<<<<<\|^=======$\|^>>>>>>>' requirements.txt .github/dependabot.yml` returns nothing.

    Stage only the two resolved files (`git add requirements.txt .github/dependabot.yml`) and seal the merge with subject `chore(36): merge master into v4.0 close branch` and a body recording the union resolution decisions and the three decided pins. Keep the subject under 72 characters. Do NOT add any other file to this commit.
  </action>
  <acceptance_criteria>
    - `git rev-list --count --merges HEAD -1` confirms HEAD is a merge commit, and `git cat-file -p HEAD | grep -c '^parent '` outputs `2`.
    - `git merge-base --is-ancestor origin/master HEAD` exits 0.
    - `grep -c '^httpx==0.28.1$' requirements.txt` outputs `1`.
    - `grep -c '^cryptography==49.0.0$' requirements.txt` outputs `1`.
    - `grep -c '^pydantic-settings\[yaml\]==2.14.2$' requirements.txt` outputs `1`.
    - `grep -c '^httpx==' requirements.txt` outputs `1` (no duplicate or stale httpx line survived).
    - `grep -rn '^<<<<<<<\|^=======$\|^>>>>>>>' requirements.txt .github/dependabot.yml` returns no output.
    - `.venv/Scripts/python.exe -c "import yaml; yaml.safe_load(open('.github/dependabot.yml'))"` exits 0, and the parsed document has 2 entries under `updates`, each with a `groups.minor-and-patch.update-types` list equal to `["minor","patch"]`, each with `schedule.day == "monday"`, with pip `open-pull-requests-limit == 10` and github-actions `open-pull-requests-limit == 5`.
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'pip install -r requirements.txt'` outputs 1 or more.
    - The job-level-env assertion command for SHOPBOT_DATA_DIR exits 0, and `grep -c SHOPBOT_DATA_DIR .github/workflows/ci.yml` outputs 1 or more.
    - `.venv/Scripts/python.exe -m pip install --dry-run -r requirements.txt` exits 0.
    - `git show --stat HEAD --name-only --pretty=format:` lists no file outside `requirements.txt` and `.github/dependabot.yml` as changed relative to the first parent.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^httpx==0.28.1$' requirements.txt)" = 1 && test "$(grep -c '^httpx==' requirements.txt)" = 1 && test "$(grep -c '^cryptography==49.0.0$' requirements.txt)" = 1 && test "$(grep -c '^pydantic-settings\[yaml\]==2.14.2$' requirements.txt)" = 1 && ! grep -rqn '^<<<<<<<' requirements.txt .github/dependabot.yml && .venv/Scripts/python.exe -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert all('SHOPBOT_DATA_DIR' not in (j.get('env') or {}) for j in d['jobs'].values())" && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'pip install -r requirements.txt')" -ge 1 && git merge-base --is-ancestor origin/master HEAD</automated>
  </verify>
  <done>A two-parent merge commit exists on the working branch whose only content changes are the union-resolved requirements.txt and dependabot.yml, with master's ci.yml Install and Test fixes verifiably intact.</done>
</task>

<task type="auto">
  <name>Task 3: Full local pytest gate on the merged tree</name>
  <files>.planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md</files>
  <read_first>
.planning/phases/36-mainline-reconciliation/36-CONTEXT.md ("Verification Bar", which makes this gate mandatory before any push)
.planning/phases/36-mainline-reconciliation/36-RESEARCH.md ("Local pre-push gate" under Code Examples, and Pitfall 2 on the required install order)
.planning/phases/36-mainline-reconciliation/36-VALIDATION.md ("Test Infrastructure" and "Sampling Rate")
  </read_first>
  <action>
    Reproduce CI's own install order locally, which is the order Pitfall 2 exists to enforce:
    `.venv/Scripts/python.exe -m pip install -r requirements.txt` then
    `.venv/Scripts/python.exe -m pip install -e ".[web]"`.
    This installs no new package: every pin is already in repo history and 36-RESEARCH.md "Package Legitimacy Audit" records that phase 36 introduces none, so no legitimacy checkpoint applies.

    Run the full suite: `.venv/Scripts/python.exe -m pytest`. Do NOT pass `-x`, `-k`, `--lf`, or any watch-mode flag; the gate is the whole suite. Capture the final summary line and the collected count.

    Expected shape: roughly 939 tests, 0 failed, 0 errors. Master alone runs 755, so a collected count near 755 means the branch's own tests did not merge in and is itself a defect signal worth recording.

    If any test fails, distinguish a merge defect from pre-existing breakage before touching anything: stash nothing, instead check the same test against master alone by running `git stash list` (expect empty) and then `.venv/Scripts/python.exe -m pytest <failing node id>` after `git checkout origin/master -- <the relevant source file>` ONLY if that is non-destructive and reversible via `git checkout HEAD -- <file>`. Simpler and preferred: read the failure, and if it names `requirements.txt`, `dependabot.yml`, or a dependency version, it is a conflict-resolution defect and must be fixed in this plan by correcting the resolution and amending, since nothing has been pushed yet. If the failure is unrelated to the merged files, record it in 36-MERGE-LOG.md as pre-existing and continue. Do NOT push in this plan under any circumstance.

    Append one row to 36-MERGE-LOG.md recording: merged-tree HEAD SHA, collected count, passed count, skipped count, failed count, and the pip dry-run result from task 2. Commit with `docs(36): record merged-tree local suite result`.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest` exits 0.
    - The pytest summary reports 0 failed and 0 errors.
    - The collected test count is 900 or greater (the merged suite, not master's 755 alone).
    - `.venv/Scripts/python.exe -c "import httpx, cryptography, pydantic_settings"` exits 0, confirming the union pins are importable in the gate environment.
    - 36-MERGE-LOG.md gained one row containing the merged-tree HEAD SHA and the four pytest counts.
    - `git log --oneline -1 origin/chore/v4.0-milestone-close..HEAD` shows local commits still unpushed: nothing in this plan reached origin.
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <done>The merged tree passes the full local suite with a collected count matching the combined v4.1 plus v4.2 surface, and the result is recorded. The tree is now safe to push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Two divergent git histories to one merged tree | Conflict resolution is a hand-authored write that can silently drop content from either side |
| requirements.txt to the runtime dependency graph | A dropped or downgraded pin becomes every downstream phase's supply-chain surface (SCAN-01 through SCAN-11 depend on this file) |
| ci.yml auto-merge to the CI execution contract | A clean auto-merge can still take the wrong side and silently reintroduce the zero-job bug, which looks like success until CI runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-02-01 | Tampering | `requirements.txt` conflict resolution silently dropping `httpx==0.28.1`, the named MAIN-02 regression | high | mitigate | Exact-line assertions `grep -c '^httpx==0.28.1$'` equals 1 AND `grep -c '^httpx=='` equals 1 (catches both drop and duplicate); plus `pip install --dry-run -r requirements.txt` and an import check for httpx, cryptography, and pydantic_settings in task 3 |
| T-36-02-02 | Tampering | `ci.yml` auto-merge taking the branch side and reintroducing job-level `runner` context or dropping the requirements install step | high | mitigate | Three positive assertions in task 2: YAML-parsed check that no job has SHOPBOT_DATA_DIR at job-level env, comment-filtered grep for the requirements install line, and a presence check so the variable was not simply deleted |
| T-36-02-03 | Tampering / Data loss | Force-pushing to reconcile the divergence, discarding the two remote-only commits | high | mitigate | `--force` and `--force-with-lease` prohibited by name; divergence resolved by a merge whose no-op property is asserted by comparing tree hashes before and after; this plan performs no push at all |
| T-36-02-04 | Repudiation | Local commits swept onto master as a conflict-resolution side effect with no record, which is exactly what MAIN-03 forbids | medium | mitigate | `36-COMMIT-DISPOSITION.md` derived from a raw-git range query at execution time, one row per SHA, plus an explicit standing rule and post-merge scope exclusion so the record stays checkable against `git log master` |
| T-36-02-05 | Tampering | Unrelated files sneaking into the merge commit, hiding a real change in a 263-commit diff | medium | mitigate | `git merge --no-commit` then `git add` of exactly the two resolved paths; acceptance asserts no other path differs from the first parent |
| T-36-02-06 | Information Disclosure | A secret introduced through conflict resolution into a tracked config file | low | mitigate | Neither resolved file accepts credentials; both are pins and schedule config; gitleaks runs on master after the merge and its status is recorded in plan 03 |
| T-36-02-SC | Tampering | Supply chain: `pip install -r requirements.txt` and `pip install -e ".[web]"` into the local venv | medium | accept | No new package enters the graph. 36-RESEARCH.md "Package Legitimacy Audit" records phase 36 as introducing zero new packages: every name in the union file was vetted and pinned in a prior milestone, and the union file is dry-run resolved before installation. No `[ASSUMED]` or `[SUS]` package exists, so no blocking legitimacy checkpoint applies |

No `high` severity threat is left unmitigated. Spoofing, Denial of Service, and Elevation of
Privilege: accept. This plan performs no network mutation, no privilege change, and no remote
write; its entire blast radius is the local working tree until plan 03 pushes.
</threat_model>

<verification>
- `git log --oneline HEAD..origin/chore/v4.0-milestone-close` outputs nothing.
- HEAD is a two-parent merge commit with `origin/master` as an ancestor.
- `grep -c '^httpx==0.28.1$' requirements.txt` outputs 1 and `grep -c '^httpx==' requirements.txt` outputs 1.
- `.venv/Scripts/python.exe -m pip install --dry-run -r requirements.txt` exits 0.
- The ci.yml job-level-env assertion exits 0 and the comment-filtered requirements-install grep returns 1 or more.
- `.venv/Scripts/python.exe -m pytest` exits 0 with 0 failed, 0 errors, and 900 or more collected.
- 36-COMMIT-DISPOSITION.md row count equals the freshly derived commit count, all decisions `include`.
- `git ls-remote origin refs/heads/chore/v4.0-milestone-close` still points at the pre-plan SHA (nothing was pushed).

Sampling note (36-VALIDATION.md): this is the first of the two mandatory full-suite runs. The
second runs against the final merged master tip in plan 05.
</verification>

<success_criteria>
- The divergence with PR #11's remote head is gone, achieved by merge, with zero force operations anywhere.
- MAIN-03 has a per-SHA record derived fresh at execution time, not a hardcoded count, with an explicit standing rule covering commits created after derivation.
- MAIN-02's union resolution is in place and machine-verified: httpx retained at 0.28.1 exactly once, cryptography at 49.0.0, pydantic-settings[yaml] at 2.14.2.
- dependabot.yml keeps master's groups blocks on both ecosystems alongside the branch's labels, monday schedule, and pip limit of 10.
- master's ci.yml Install and Test fixes are verifiably present in the merged tree.
- The full local suite is green on the merged tree, and nothing has been pushed.
</success_criteria>

<output>
Create `.planning/phases/36-mainline-reconciliation/36-02-SUMMARY.md` when done. Record the merged
HEAD SHA, the exact derived commit count and SHA list, the pytest counts, and confirm no push
occurred, since plan 03 pushes this exact tip and verifies ancestry against that SHA list.
</output>
