---
phase: 36-mainline-reconciliation
plan: 03
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - .planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md
  - .planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md
  - .planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md
autonomous: true
requirements: [MAIN-01, MAIN-02, MAIN-03, MAIN-04]
must_haves:
  truths:
    - "The resolved branch reached PR #11's head via a plain push, with no force operation"
    - "PR #11 is merged into master as a merge commit, preserving the 263-commit trail"
    - "Every SHA recorded in 36-COMMIT-DISPOSITION.md is an ancestor of origin/master"
    - "gitleaks.yml and release-please.yml exist on master"
    - "master's requirements.txt carries httpx==0.28.1, cryptography==49.0.0, and pydantic-settings[yaml]==2.14.2, one line each"
    - "A CI run on the post-merge master HEAD scheduled a non-empty job list and every job concluded success on both ubuntu-latest and windows-latest"
  artifacts:
    - path: ".planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md"
      provides: "MAIN-01 evidence: run id, SHA, per-job conclusions, test counts, plus the non-blocking gitleaks/release-please/CodeQL status handed to Phase 38"
      contains: "ubuntu-latest"
  key_links:
    - from: "local merged branch tip"
      to: "origin/chore/v4.0-milestone-close"
      via: "plain git push, fast-forward safe after the plan-02 no-op merge"
      pattern: "git push origin chore/v4.0-milestone-close"
    - from: "PR #11 merge commit"
      to: "origin/master"
      via: "gh pr merge 11 --merge"
      pattern: "git merge-base --is-ancestor"
    - from: "post-merge master SHA"
      to: "CI workflow run with a non-empty jobs array"
      via: "gh run view --json jobs"
      pattern: "conclusion.*success"
---

<objective>
Land PR #11 on master and prove the merge is correct. Push the locally verified merged tree with a plain push, wait for PR checks, merge as a merge commit, then verify the four things the merge was supposed to deliver: MAIN-04 (the v4.1/v4.2 workflow artifacts exist on master), MAIN-02 (the union pins survived onto master), MAIN-03 (every recorded SHA is genuinely an ancestor of master), and MAIN-01 (a real CI run on the new master HEAD scheduled jobs and passed both runners).

Purpose: this is the phase's highest-blast-radius action and the first moment the merge's correctness is externally observable. 36-RESEARCH.md Correction 1 shows master's CI is already green on the smaller 755-test input, so the open question this plan answers is whether the larger merged suite stays green under the same proven ci.yml.

Output: PR #11 MERGED, `36-CI-EVIDENCE.md` recording the run, and `36-COMMIT-DISPOSITION.md` closed out with the ancestry verification result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-mainline-reconciliation/36-CONTEXT.md
@.planning/phases/36-mainline-reconciliation/36-RESEARCH.md
@.planning/phases/36-mainline-reconciliation/36-VALIDATION.md
@.planning/phases/36-mainline-reconciliation/36-02-SUMMARY.md
@.planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md

<authorization>
Phase-scoped, pre-authorized by 36-CONTEXT.md "Merge Execution and Authority": the executor runs
`git push` to the PR head branch `chore/v4.0-milestone-close`, `gh pr merge`, and `git commit`,
without pausing on the global commit-approval gate. Direct pushes to `master` remain NOT
authorized; master changes only through `gh pr merge`.
</authorization>

<tooling_constraint>
Raw `git` and raw `gh` only. Do NOT prefix with `rtk` (36-VALIDATION.md "Tooling constraint":
`rtk git log <range>` silently drops merge commits, and this plan's ancestry assertions are the
MAIN-03 proof). Pass `--repo thezoid/ShopPyBot` on every `gh` call.
</tooling_constraint>

<fix_forward_protocol>
Shared and phase-wide. Invoked identically from plan 03 task 3, from every task in plan 04, and
from plan 05 task 1. The attempt budget is THREE for the entire phase, shared across all three
plans, NOT three per plan. Plans 03, 04 and 05 run as separate executor invocations with no
shared memory, so the counter lives in a file and MUST be re-read from that file immediately
before every increment.

STEP F0, read before increment. Mandatory, no exceptions, even when the current plan has made no
attempt of its own. Parse the current value and hard-stop if the budget is already spent:

`ATTEMPTS=$(sed -n 's/.*Fix-forward attempts used: \([0-9][0-9]*\).*/\1/p' .planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md | tail -1); echo "attempts=${ATTEMPTS:-0}"; test "${ATTEMPTS:-0}" -lt 3`

That `test` exiting non-zero means the phase-wide cap is already spent: STOP immediately, write
the escalation, and do NOT start a fix. This exact form was verified working in Git Bash on this
machine at planning time; it uses only POSIX basic regular expressions, never `grep -oP`, which
is not available here. PowerShell equivalent if bash is unavailable:
`$m = Select-String -Path '.planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md' -Pattern 'Fix-forward attempts used:\s*(\d+)' | Select-Object -Last 1; [int]$m.Matches[0].Groups[1].Value`
Never restart the count at 0 because the current plan has not yet made an attempt. That file is
the only source of truth, and it is the only phase state that must survive a context boundary.

STEP F1, increment and persist BEFORE acting. Rewrite the `Fix-forward attempts used: N of 3`
line in 36-CI-EVIDENCE.md to N plus 1, and append a row naming the plan, the task, the failing
job, and the master SHA that went red. Persisting first means a context loss mid-fix cannot
silently hand back an attempt.

STEP F2, fix forward, never revert. Reverting is prohibited by 36-CONTEXT.md because reverting a
263-commit merge costs more than it recovers. Force-push is prohibited. Direct pushes to master
are prohibited and are blocked by the harness. Read the failure with
`gh run view <id> --repo thezoid/ShopPyBot --log-failed`, cut a branch off current master named
`fix/36-ci-forward-<attempt number>`, make the minimal fix, `gh pr create --base master`, wait
for its checks with the bounded poll, then `gh pr merge <n> --repo thezoid/ShopPyBot --merge`.
Do NOT improvise a same-branch fix and do NOT amend an already-merged commit.

STEP F3, `--admin` is prohibited in this state too. A red master immediately after a merge is
exactly the condition `--admin` would mask. It is forbidden here as it is everywhere else in
this phase, and the single named exception in plan 03 task 2 does NOT extend to this path.

STEP F4, re-verify. Re-run the invoking task's own CI assertion against the new master SHA:
`.jobs | length` of 2 or greater with zero non-success conclusions. Still red means return to
STEP F0, which hard-stops once the shared budget reaches 3.

STEP F5, on exhaustion. STOP the phase. Write the escalation into 36-CI-EVIDENCE.md naming the
failing job, the log excerpt, and the last-good master SHA. The `pre-v5-mainline` tag from plan
01 remains the rollback point. Do NOT start the next plan. A fourth attempt is prohibited.
</fix_forward_protocol>

<poll_budget>
| Wait | Command | Interval | Timeout | Action on timeout |
|------|---------|----------|---------|-------------------|
| PR #11 checks reach terminal state | `timeout 1800 gh pr checks 11 --repo thezoid/ShopPyBot --watch --interval 30 > /dev/null 2>&1` | 30s | 30 min | Capture `gh pr checks 11 --repo thezoid/ShopPyBot --json name,bucket`, append to the log, do NOT merge, do NOT use `--admin`, STOP for escalation |
| PR #11 mergeStateStatus reaches CLEAN | `gh pr view 11 --repo thezoid/ShopPyBot --json mergeStateStatus --jq .mergeStateStatus` | 30s | 15 min (30 iterations) | Record the terminal status, STOP for escalation |
| A CI run appears for the new master SHA | `gh run list --repo thezoid/ShopPyBot --branch master --workflow CI --commit <sha> --json databaseId` | 15s | 5 min (20 iterations) | No run triggered is itself a MAIN-01 failure signal, not latency: record it in 36-CI-EVIDENCE.md and enter the fix-forward path in task 3 |
| The master CI run completes | `timeout 1800 gh run watch <id> --repo thezoid/ShopPyBot --exit-status --interval 30 > /dev/null 2>&1` | 30s | 30 min | Record the run id and its partial state, re-check once with `gh run view <id> --json status,conclusion,jobs`, then STOP for escalation |

Run every poll loop as a background Bash invocation (`run_in_background: true`) and read its
output; foreground `sleep` may be blocked by the harness. There are no unbounded waits.
</poll_budget>

<verified_state>
- master branch protection: required contexts `CodeQL`, `test (windows-latest)`,
  `test (ubuntu-latest)`; `strict: true`; `enforce_admins: false`. Do NOT change any of it,
  Phase 38 owns branch protection.
- `strict: true` means PR #11 must be up to date with master at merge time. Plan 02 merged
  `origin/master` into the branch, so it should already be up to date unless master moved after
  that merge. Verify rather than assume.
- A required check reporting `skipped` or `neutral` SATISFIES branch protection
  (36-RESEARCH.md Pitfall 4). PR #11 touches Python files so CodeQL is expected to run for real.
- The distinguishing signal for the historic zero-job bug is an EMPTY `jobs` array, with
  `conclusion` of `failure` or `startup_failure`, or no run appearing at all. A non-empty `jobs`
  array with every conclusion `success` is the MAIN-01 pass condition.
- Master CI on `e98ec83` concluded success on both runners with 755 passed and 2 skipped. The
  merged tree should collect roughly 939.
</verified_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plain-push the resolved branch and drive PR #11's checks to terminal state</name>
  <files>.planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md</files>
  <read_first>
.planning/phases/36-mainline-reconciliation/36-02-SUMMARY.md (the merged HEAD SHA this plan pushes, and confirmation the local suite was green)
.planning/phases/36-mainline-reconciliation/36-RESEARCH.md (Pitfall 3, why a plain push works only after the no-op merge; Pitfall 4, why skipping counts as passing)
.planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md (the row format to append to)
  </read_first>
  <action>
    Pre-flight before pushing. Assert the working tree is clean (`git status --porcelain` empty), the branch is `chore/v4.0-milestone-close`, HEAD matches the merged SHA recorded in 36-02-SUMMARY.md, and `git log --oneline HEAD..origin/chore/v4.0-milestone-close` is empty (no remote-only commits remain, which is what makes a plain push legal). If any check fails, STOP.

    Push with exactly `git push origin chore/v4.0-milestone-close`. `--force` and `--force-with-lease` are PROHIBITED. If the push is rejected as non-fast-forward, do NOT escalate to a force push: re-run the plan-02 task-1 no-op merge (`git fetch origin && git merge origin/chore/v4.0-milestone-close`), re-verify the tree hash is unchanged, and push again. If it is rejected a second time, STOP and escalate.

    Pushing re-triggers CI on PR #11. Wait for terminal state with the bounded watch in the poll budget. Then read the terminal state with `gh pr checks 11 --repo thezoid/ShopPyBot --json name,bucket,link`. Buckets `pass` and `skipping` satisfy branch protection; `fail`, `pending`, and `cancel` do not.

    Confirm the checks were REAL, not zero-job phantoms. Find the workflow run for the pushed head SHA with `gh run list --repo thezoid/ShopPyBot --branch chore/v4.0-milestone-close --workflow CI --commit <pushed SHA> --json databaseId,status,conclusion`, then `gh run view <id> --repo thezoid/ShopPyBot --json jobs --jq '.jobs[] | {name, conclusion}'`. An empty jobs array with a failure or startup_failure conclusion is the historic bug reappearing: record it and STOP rather than merging.

    If any check is in bucket `fail`, do NOT merge and do NOT use `--admin`. Read the failing job's log with `gh run view <id> --repo thezoid/ShopPyBot --log-failed`, fix on the branch, commit, push again, and re-run the watch. This pre-merge fix loop uses the shared fix_forward_protocol above and consumes the same phase-wide budget of three attempts: run STEP F0 to read the counter out of 36-CI-EVIDENCE.md before each attempt, and STOP when it reaches 3. If 36-CI-EVIDENCE.md does not exist yet (it is created in task 3), treat the count as 0 and create the file with a `Fix-forward attempts used: N of 3` line as part of the first increment, so the counter exists from the first attempt onward.

    Append one row to 36-MERGE-LOG.md: pushed SHA, run id, per-check buckets, and the jobs-array length. Commit with `docs(36): record PR #11 push and check results`.
  </action>
  <acceptance_criteria>
    - `git ls-remote origin refs/heads/chore/v4.0-milestone-close` outputs the same SHA as local `git rev-parse HEAD` at push time.
    - No executed command contained `--force` or `--force-with-lease`.
    - `gh pr checks 11 --repo thezoid/ShopPyBot --json bucket --jq '[.[] | select(.bucket == "fail" or .bucket == "pending" or .bucket == "cancel")] | length'` outputs `0`.
    - `gh run view <run id> --repo thezoid/ShopPyBot --json jobs --jq '.jobs | length'` outputs 2 or greater (non-empty jobs array, proving the workflow compiled and scheduled).
    - Every entry from `gh run view <run id> --repo thezoid/ShopPyBot --json jobs --jq '.jobs[].conclusion'` outputs `success`.
    - `gh pr view 11 --repo thezoid/ShopPyBot --json state --jq .state` still outputs `OPEN` (this task pushes and verifies, it does not merge).
    - 36-MERGE-LOG.md gained one row containing the pushed SHA and the run id.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git ls-remote origin refs/heads/chore/v4.0-milestone-close | cut -f1)" = "$(git rev-parse HEAD)" && test "$(gh pr checks 11 --repo thezoid/ShopPyBot --json bucket --jq '[.[] | select(.bucket == "fail" or .bucket == "pending" or .bucket == "cancel")] | length')" = 0</automated>
  </verify>
  <done>PR #11's head carries the resolved merge, pushed without any force operation, and its required checks are terminal-green with a provably non-empty jobs array.</done>
</task>

<task type="auto">
  <name>Task 2: Merge PR #11 as a merge commit and verify MAIN-02, MAIN-03, MAIN-04 on master</name>
  <files>.planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md, .planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md</files>
  <read_first>
.planning/phases/36-mainline-reconciliation/36-CONTEXT.md (the locked decision that PR #11 lands as a merge commit, not squash and not rebase, because the 263-commit atomic trail is deliberate)
.planning/phases/36-mainline-reconciliation/36-RESEARCH.md ("gh pr merge Flag Reference", especially why --delete-branch must NOT be used on PR #11; and Pitfall 6 on strict mode)
.planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md (the SHA table whose every row this task verifies against origin/master)
.planning/phases/36-mainline-reconciliation/36-VALIDATION.md (the MAIN-02, MAIN-03, MAIN-04 assertion rows)
  </read_first>
  <action>
    Satisfy `strict: true` first. Run `git fetch origin` and check `git merge-base --is-ancestor origin/master <PR #11 head SHA>`; exit 0 means the PR is up to date. If it exits non-zero (master moved after plan 02's merge), merge master into the branch again locally, re-run the plan-02 task-2 conflict assertions, push, and re-run task 1's check watch before continuing. Also poll `gh pr view 11 --repo thezoid/ShopPyBot --json mergeStateStatus --jq .mergeStateStatus` until it returns `CLEAN`, per the poll budget.

    Merge with exactly `gh pr merge 11 --repo thezoid/ShopPyBot --merge --delete-branch=false`. Do NOT pass `--squash` (it destroys the 263-commit trail the project deliberately built), do NOT pass `--rebase`, do NOT pass `--delete-branch` (PR #11's head IS the local working branch and deleting it out from under the checkout is destructive), and do NOT pass `--admin`. `--admin` is permitted in this phase only under one named condition: a required check that has reached a non-terminal state and stayed there past the full poll timeout while the underlying work is independently proven correct by a green local suite AND a green jobs array for the same SHA. Even then, record the justification in 36-MERGE-LOG.md before using it. Absent that condition, a blocked merge is real signal: STOP and escalate.

    After the merge, `git fetch origin` and capture the new `git rev-parse origin/master`.

    Verify MAIN-04: `git ls-tree origin/master -- .github/workflows/gitleaks.yml .github/workflows/release-please.yml` must return two non-empty lines.

    Verify MAIN-02 on master itself, not on the local tree: `git show origin/master:requirements.txt` and assert exactly one line each for `httpx==0.28.1`, `cryptography==49.0.0`, and `pydantic-settings[yaml]==2.14.2`, and exactly one `^httpx==` line total.

    Verify MAIN-03: for EVERY SHA in 36-COMMIT-DISPOSITION.md's table, run `git merge-base --is-ancestor <sha> origin/master` and require exit 0. Any SHA that is not an ancestor contradicts the recorded `include` decision and is a hard STOP. Then replace the file's `Post-merge ancestry verification: pending (plan 03 task 2)` placeholder with the result: the verification timestamp, the post-merge origin/master SHA, the count of SHAs checked, and the count that passed. Keep the standing rule and the post-merge scope exclusion intact.

    Append one row to 36-MERGE-LOG.md for the PR #11 merge: PR number, head SHA, resulting origin/master SHA, exact command, and the MAIN-02/03/04 assertion outcomes. Commit both files with `docs(36): verify MAIN-02, MAIN-03, MAIN-04 on merged master`.
  </action>
  <acceptance_criteria>
    - `gh pr view 11 --repo thezoid/ShopPyBot --json state --jq .state` outputs `MERGED` and `mergedAt` is non-null.
    - The merge landed as a merge commit: `git cat-file -p origin/master | grep -c '^parent '` outputs `2`.
    - `git ls-remote origin refs/heads/chore/v4.0-milestone-close` still returns a line (the head branch was not deleted).
    - `git ls-tree origin/master -- .github/workflows/gitleaks.yml .github/workflows/release-please.yml | wc -l` outputs `2` (MAIN-04).
    - `git show origin/master:requirements.txt | grep -c '^httpx==0.28.1$'` outputs `1`, and `git show origin/master:requirements.txt | grep -c '^httpx=='` also outputs `1` (MAIN-02).
    - `git show origin/master:requirements.txt | grep -c '^cryptography==49.0.0$'` outputs `1` and `grep -c '^pydantic-settings\[yaml\]==2.14.2$'` outputs `1` (MAIN-02).
    - Every SHA in 36-COMMIT-DISPOSITION.md satisfies `git merge-base --is-ancestor <sha> origin/master` with exit 0; the file records checked-count equal to passed-count (MAIN-03).
    - 36-COMMIT-DISPOSITION.md no longer contains the string `pending (plan 03 task 2)`.
    - If `--admin` was used, 36-MERGE-LOG.md contains a row naming the specific stuck check and the independent green evidence that justified it. If it was not used, no such row exists.
  </acceptance_criteria>
  <verify>
    <automated>git fetch origin && test "$(gh pr view 11 --repo thezoid/ShopPyBot --json state --jq .state)" = MERGED && test "$(git cat-file -p origin/master | grep -c '^parent ')" = 2 && test "$(git ls-tree origin/master -- .github/workflows/gitleaks.yml .github/workflows/release-please.yml | wc -l)" = 2 && test "$(git show origin/master:requirements.txt | grep -c '^httpx==0.28.1$')" = 1 && test "$(git show origin/master:requirements.txt | grep -c '^httpx==')" = 1 && ! grep -q "pending (plan 03 task 2)" .planning/phases/36-mainline-reconciliation/36-COMMIT-DISPOSITION.md</automated>
  </verify>
  <done>PR #11 is merged into master as a two-parent merge commit; gitleaks.yml and release-please.yml exist on master; the three union pins are on master exactly once each; every recorded SHA is a proven ancestor of master. MAIN-02, MAIN-03, MAIN-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Prove MAIN-01 on the post-merge master HEAD, with a capped fix-forward path</name>
  <files>.planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md, .planning/phases/36-mainline-reconciliation/36-MERGE-LOG.md</files>
  <read_first>
.planning/phases/36-mainline-reconciliation/36-RESEARCH.md ("CI Polling Pattern", "Confirming MAIN-01 is real, not just 0 jobs was avoided", Pitfall 1, and Correction 1 which establishes that master's ci.yml is already proven on the smaller input)
.planning/phases/36-mainline-reconciliation/36-CONTEXT.md ("Verification Bar": fix forward not revert, hard cap of 3 attempts, and the rule that gitleaks/release-please/CodeQL status is recorded but does not block)
.planning/phases/36-mainline-reconciliation/36-VALIDATION.md (the MAIN-01 assertion row and "Known Blocking Conditions", which distinguishes an Actions-allowlist failure from a merge defect)
  </read_first>
  <action>
    Capture `NEW_SHA=$(git rev-parse origin/master)` after the merge. Find the CI run for that exact SHA with `gh run list --repo thezoid/ShopPyBot --branch master --workflow CI --commit "$NEW_SHA" --json databaseId,status,conclusion`, polling per the poll budget until a run exists. A run that never appears is not latency past the 5-minute window: it is the workflow failing to compile, which is the MAIN-01 failure mode itself.

    Wait for completion with the bounded `gh run watch` in the poll budget, discarding its streaming output to keep the transcript small, then evaluate with `gh run view <id> --repo thezoid/ShopPyBot --json status,conclusion,jobs --jq '{conclusion, jobcount: (.jobs | length), jobs: [.jobs[] | {name, conclusion}]}'`.

    MAIN-01 passes only when ALL of: the jobs array length is 2 or greater, a job named `test (ubuntu-latest)` concluded `success`, and a job named `test (windows-latest)` concluded `success`. An empty jobs array, or a conclusion of `startup_failure`, is the historic zero-job bug and counts as a failure regardless of what the run summary says.

    Record the non-blocking Phase 38 handoff signals for the same SHA: `gh run list --repo thezoid/ShopPyBot --commit "$NEW_SHA" --json name,conclusion,databaseId`. Capture the status of the gitleaks, release-please, and CodeQL workflows. Per 36-CONTEXT.md these do NOT block phase completion, and per 36-VALIDATION.md "Known Blocking Conditions" a failure at action-resolution time (the un-widened Actions allowlist for `gitleaks/gitleaks-action` and `googleapis/release-please-action`) must be recorded as a Phase 38 item, not treated as a merge defect. State that distinction explicitly in the record.

    Create `.planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md` containing: the master SHA, the CI run id and URL, the jobs array length, the per-job name and conclusion, the pytest counts pulled from the job log summary, a comparison against the 755-passed baseline on `e98ec83`, the gitleaks/release-please/CodeQL statuses with their Phase 38 disposition, and a `Fix-forward attempts used: N of 3` counter initialised to 0.

    Fix-forward path, only if MAIN-01 fails. Invoke the shared fix_forward_protocol above, starting at STEP F0. Do NOT skip STEP F0 on the grounds that this plan created the counter and therefore knows it is 0: task 1's pre-merge fix loop may already have consumed attempts against the same phase-wide budget of three, and this plan may itself be re-entered in a fresh context. Read the file, then increment, then act. On exhaustion, STOP and do NOT start plan 04.

    Append one row to 36-MERGE-LOG.md and commit both files with `docs(36): record MAIN-01 CI evidence on merged master`.
  </action>
  <acceptance_criteria>
    - `gh run list --repo thezoid/ShopPyBot --branch master --workflow CI --commit $(git rev-parse origin/master) --json databaseId --jq 'length'` outputs 1 or greater.
    - `gh run view <run id> --repo thezoid/ShopPyBot --json jobs --jq '.jobs | length'` outputs 2 or greater.
    - `gh run view <run id> --repo thezoid/ShopPyBot --json jobs --jq '[.jobs[] | select(.conclusion != "success")] | length'` outputs `0`.
    - `gh run view <run id> --repo thezoid/ShopPyBot --json jobs --jq '[.jobs[].name] | sort'` includes both `test (ubuntu-latest)` and `test (windows-latest)`.
    - `gh run view <run id> --repo thezoid/ShopPyBot --json conclusion --jq .conclusion` outputs `success`, never `startup_failure`.
    - 36-CI-EVIDENCE.md exists and contains the run id, the master SHA, both runner job names with their conclusions, the reported test counts, and exactly one `Fix-forward attempts used: N of 3` line where N is 3 or less.
    - The counter is machine-readable by the STEP F0 command: `sed -n 's/.*Fix-forward attempts used: \([0-9][0-9]*\).*/\1/p' .planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md | tail -1` outputs a single integer between 0 and 3 inclusive.
    - If any fix-forward attempt was made, 36-CI-EVIDENCE.md contains one row per attempt naming the plan, the task, the failing job, and the red master SHA.
    - 36-CI-EVIDENCE.md records the gitleaks, release-please, and CodeQL statuses with an explicit note that they are Phase 38 scope and non-blocking here.
    - No revert commit exists on master: `git log origin/master --oneline -20 --grep='^Revert'` returns nothing referencing the PR #11 merge.
  </acceptance_criteria>
  <verify>
    <automated>RID=$(gh run list --repo thezoid/ShopPyBot --branch master --workflow CI --commit "$(git rev-parse origin/master)" --json databaseId --jq '.[0].databaseId') && test "$(gh run view "$RID" --repo thezoid/ShopPyBot --json jobs --jq '.jobs | length')" -ge 2 && test "$(gh run view "$RID" --repo thezoid/ShopPyBot --json jobs --jq '[.jobs[] | select(.conclusion != "success")] | length')" = 0 && grep -c "Fix-forward attempts used" .planning/phases/36-mainline-reconciliation/36-CI-EVIDENCE.md</automated>
  </verify>
  <done>A real CI run on the post-merge master HEAD scheduled a non-empty job list and both runner jobs concluded success, recorded with evidence. MAIN-01 satisfied, and the historic zero-job narrative is disproved on the merged tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local clone to PR head branch | A ref write that can destroy remote-only history if forced |
| PR merge command to branch protection | The protection rules are the control; `--admin` is the documented bypass and therefore the abuse path |
| CI run reporting to the phase's pass judgement | A run can report a non-failure conclusion while having scheduled zero jobs, so the reported conclusion alone is not trustworthy evidence |
| Merged master to every downstream phase | Phases 37 through 50 build on this tip; a silently wrong merge propagates to all of them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03-01 | Tampering / Data loss | Force-push to `chore/v4.0-milestone-close` discarding the remote-only commits `e2f2695` and `d9ad585` | high | mitigate | `--force` and `--force-with-lease` prohibited by name in task 1; the plan-02 no-op merge removed the need; a rejected push routes to re-merge and retry, and a second rejection is a STOP, never an escalation to force |
| T-36-03-02 | Elevation of Privilege | `gh pr merge --admin` bypassing a genuinely failing required check on the phase's highest-blast-radius merge | high | mitigate | `--admin` allowed only under one named, written condition (non-terminal check past full timeout PLUS independently green local suite PLUS green jobs array for the same SHA), and its use must be justified in 36-MERGE-LOG.md; acceptance asserts the justification row exists if and only if the flag was used |
| T-36-03-03 | Tampering | `--squash` or `--rebase` collapsing the 263-commit atomic trail, contradicting the locked decision and destroying MAIN-03's checkable evidence | high | mitigate | Both flags prohibited by name; acceptance asserts `origin/master` HEAD has exactly 2 parents, which only a true merge commit produces |
| T-36-03-04 | Spoofing of evidence | A CI run reporting a passing-looking conclusion while having scheduled zero jobs (the historic bug), producing a false MAIN-01 pass | high | mitigate | MAIN-01 judged on `jobs | length` >= 2 AND per-job conclusions AND both named runner jobs present, never on the run-level conclusion alone; `startup_failure` explicitly enumerated as a failure |
| T-36-03-05 | Repudiation | Reverting the merge under pressure, erasing the trail and contradicting the locked fix-forward decision | medium | mitigate | Revert prohibited by name; fix-forward is via a separate branch and PR; acceptance greps master's recent log for a revert of the PR #11 merge |
| T-36-03-06 | Denial of Service (self-inflicted) | Grinding indefinitely on a red master, or waiting unbounded on a stuck run | medium | mitigate | Hard cap of 3 fix-forward attempts shared phase-wide; the counter is persisted in 36-CI-EVIDENCE.md and re-read via the fix_forward_protocol STEP F0 command before every increment, so a fresh-context executor in plan 04 or 05 cannot restart the count at 0; every wait bounded with a defined timeout action |
| T-36-03-07 | Tampering | Changing branch protection to unblock the merge | high | mitigate | Branch protection is Phase 38 scope and out of bounds here; the plan never calls `gh api ... /protection` with a write verb, and plan 05's phase gate re-asserts the protection config is unchanged |
| T-36-03-08 | Information Disclosure | Secrets landing on the public default branch through the 263-commit merge | medium | transfer | gitleaks runs on master after the merge and its conclusion is recorded in 36-CI-EVIDENCE.md; remediation of any finding is Phase 38 (SCAN) scope per 36-CONTEXT.md, and the un-widened Actions allowlist is recorded as the reason a gitleaks failure may be environmental rather than a real finding |
| T-36-03-SC | Tampering | Supply chain (package installs) | low | accept | No package manager runs in this plan. 36-RESEARCH.md "Package Legitimacy Audit" records phase 36 as introducing zero new packages |

No `high` severity threat is left unmitigated. T-36-03-08 is transferred to Phase 38 by an
explicit 36-CONTEXT.md decision, with the signal captured here so the transfer is evidenced
rather than assumed.
</threat_model>

<verification>
- `gh pr view 11 --repo thezoid/ShopPyBot --json state --jq .state` outputs `MERGED`, `mergedAt` non-null.
- `git cat-file -p origin/master | grep -c '^parent '` outputs `2`.
- `git ls-tree origin/master -- .github/workflows/gitleaks.yml .github/workflows/release-please.yml | wc -l` outputs `2`.
- `git show origin/master:requirements.txt | grep -c '^httpx=='` outputs `1` and that line is `httpx==0.28.1`.
- Every SHA in 36-COMMIT-DISPOSITION.md passes `git merge-base --is-ancestor <sha> origin/master`.
- The CI run for the post-merge master SHA has 2 or more jobs, all concluding `success`.
- `gh api repos/thezoid/ShopPyBot/branches/master/protection --jq '.required_status_checks.strict'` still outputs `true` with the same three contexts (protection untouched).
- 36-CI-EVIDENCE.md exists with the run id, both runner conclusions, and the fix-forward counter.

Sampling note (36-VALIDATION.md): this plan runs the MAIN-01, MAIN-02, MAIN-03, and MAIN-04
assertions immediately after the merge that makes them checkable, not deferred to phase end.
</verification>

<success_criteria>
- PR #11 is MERGED as a two-parent merge commit, achieved with no force operation, no squash, no rebase, and no head-branch deletion.
- The 263-commit v4.1 plus v4.2 trail, including gitleaks.yml and release-please.yml, is on master.
- master's requirements.txt carries the three union pins exactly once each, httpx included.
- Every SHA recorded as included in 36-COMMIT-DISPOSITION.md is a proven ancestor of master, and the record is closed out with the verification result.
- A CI run on the post-merge master HEAD scheduled a non-empty job list and passed on both ubuntu-latest and windows-latest, recorded as evidence.
- If master went red, at most three fix-forward PRs were attempted and the counter is persisted; no revert exists and branch protection is unchanged.
</success_criteria>

<output>
Create `.planning/phases/36-mainline-reconciliation/36-03-SUMMARY.md` when done. Record the
post-merge origin/master SHA, the CI run id, the per-job conclusions and test counts, the
fix-forward attempts consumed, and the gitleaks/release-please/CodeQL statuses, since plans 04
and 05 branch on the remaining fix-forward budget and Phase 38 consumes the workflow statuses.
</output>
