---
phase: 18-safety-gate-config-foundation
plan: 04
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - plugins/shopbot_plugin_amazon.py
  - plugins/shopbot_plugin_bestbuy.py
  - plugins/shopbot_plugin_walmart.py
  - plugins/shopbot_plugin_target.py
  - plugins/shopbot_plugin_gamestop.py
  - plugins/shopbot_plugin_newegg.py
  - plugins/shopbot_plugin_squareenix.py
  - tests/test_safety_gate.py
autonomous: true
requirements: [BUY-02]
must_haves:
  truths:
    - "All 7 bundled plugins route their final place-order click through place_order_guarded"
    - "No plugin clicks a place-order element outside place_order_guarded (grep assertion)"
    - "7 plugins x monitor_only=True produce zero ('purchased', ...) writes to the write-queue"
    - "BestBuy's confirmed test_mode gap is closed (no unconditional place_order.click)"
  artifacts:
    - path: "plugins/shopbot_plugin_bestbuy.py"
      provides: "place_order_guarded reroute (was unconditional click — the confirmed gap)"
      contains: "place_order_guarded"
    - path: "tests/test_safety_gate.py"
      provides: "7-plugin monitor_only zero-write integration test + grep enforcement test"
      contains: "place_order_guarded"
  key_links:
    - from: "each plugin auto_buy"
      to: "RetailerPlugin.place_order_guarded"
      via: "return await self.place_order_guarded(place_order.click)"
      pattern: "return await self\\.place_order_guarded\\(place_order\\.click\\)"
    - from: "tests/test_safety_gate.py"
      to: "write_queue"
      via: "asserts no ('purchased', ...) tuple enqueued under monitor_only=True"
      pattern: "purchased"
---

<objective>
Reroute the final place-order DOM click in all 7 bundled plugins through `place_order_guarded()`, and add the CI enforcement tests: a 7-plugin integration test asserting zero purchase-queue writes under `monitor_only=True`, and a grep/static assertion that no plugin clicks a place-order element outside the guard.

Purpose: This is the BUY-02 payload — it closes the confirmed hole where BestBuy and 5 others click place-order unconditionally regardless of `test_mode`. Satisfies success criterion 2 (all 7 routed + CI zero-write test) and success criterion 3 (BestBuy gap closed). Scope discipline (BUY-02 / CLAUDE.md): reroute ONLY the final place-order click; do NOT restructure auto_buy methods, intermediate clicks (add-to-cart, checkout-proceed), or any other logic.
Output: 7 updated plugin files, new `tests/test_safety_gate.py`.

Depends on 18-02 for `place_order_guarded` on the ABC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-safety-gate-config-foundation/18-CONTEXT.md
@.planning/phases/18-safety-gate-config-foundation/18-RESEARCH.md
@.planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md

<interfaces>
<!-- Per-plugin place-order trigger map. From RESEARCH.md lines 252-316 + PATTERNS reroute summary. -->
<!-- In every case the replacement is the SAME line: return await self.place_order_guarded(place_order.click) -->

| Plugin   | File                          | auto_buy | Trigger lines | Selector                              | Current guard |
| Amazon   | shopbot_plugin_amazon.py      | L357     | L406-425      | #submitOrderButtonId                  | PARTIAL (test_mode if/else L412-425) |
| BestBuy  | shopbot_plugin_bestbuy.py     | L249     | L304-311      | .button--place-order                  | NONE (confirmed gap) |
| Walmart  | shopbot_plugin_walmart.py     | L148     | L189-195      | [data-testid="place-order-button"]    | NONE |
| Target   | shopbot_plugin_target.py      | L150     | L191-198      | [data-test="placeOrder"]              | NONE |
| GameStop | shopbot_plugin_gamestop.py    | L150     | L190-199      | button.place-order                    | NONE |
| Newegg   | shopbot_plugin_newegg.py      | L163     | L205-215      | .btn-primary.btn-wide / "Place Order" | NONE |
| SquareEnix| shopbot_plugin_squareenix.py | L154     | L196-206      | [data-testid="place-order-button"] / "Place Order" | NONE |

place_order_guarded contract (from 18-02): `async def place_order_guarded(self, click_fn) -> bool`; returns True on click, False when suppressed, never raises.

_check_and_buy signature (core/orchestrator.py L196): async def _check_and_buy(plugin, name, link, auto_buy, write_queue, dispatcher=None)

conftest fixtures available: fake_browser, fake_plugin, mock_nodriver_start (tests/conftest.py).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reroute the final place-order click in all 7 plugins through place_order_guarded</name>
  <files>plugins/shopbot_plugin_amazon.py, plugins/shopbot_plugin_bestbuy.py, plugins/shopbot_plugin_walmart.py, plugins/shopbot_plugin_target.py, plugins/shopbot_plugin_gamestop.py, plugins/shopbot_plugin_newegg.py, plugins/shopbot_plugin_squareenix.py</files>
  <read_first>
    - .planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md (per-plugin reroute sections lines 291-429 + the reroute summary table lines 570-580 — before/after for every plugin)
    - .planning/phases/18-safety-gate-config-foundation/18-RESEARCH.md (Per-Plugin Place-Order Trigger Map lines 252-316; Pitfall 2 lines 326-330 on Amazon)
    - plugins/shopbot_plugin_amazon.py (read the auto_buy method around L357-428 — the partial test_mode block at L412-425 and the unused test_mode var at L391)
    - plugins/shopbot_plugin_bestbuy.py (read auto_buy around L249-312 — the unconditional click + return True at L304-311; this is the confirmed gap analog for the other 5)
    - plugins/shopbot_plugin_walmart.py, plugins/shopbot_plugin_target.py, plugins/shopbot_plugin_gamestop.py, plugins/shopbot_plugin_newegg.py, plugins/shopbot_plugin_squareenix.py (read each auto_buy method around the trigger lines listed in the interfaces table to confirm the `place_order` variable name and the click + return True it owns)
  </read_first>
  <action>
    For each of the 7 plugins, locate the final place-order step inside `auto_buy` (the `place_order = await tab.select(...)` followed by `await place_order.click()`). After the existing not-found guard (`if not place_order: writeLog(...); return False`), replace the `await place_order.click()` line, any immediately-following SUCCESS `writeLog("Order placed ...")`, and the trailing `return True` with a single line: `return await self.place_order_guarded(place_order.click)`.

    Plugin-specific notes:
    - BestBuy (the confirmed gap): replace `await place_order.click()` + `writeLog("Order placed on BestBuy", "SUCCESS")` + `return True` (L308-311) with the single guarded return.
    - Amazon: replace the entire `if not test_mode: ... else: ... _wait_user_action(...)` block (L412-425) with the single guarded return. INTENTIONALLY DROP the `test_pause_event` wait (RESEARCH Pitfall 2 — the guard replaces the ad-hoc gate; do NOT preserve the pause). Also remove the now-unused `test_mode = self.config.debug.test_mode ...` assignment at ~L391. Do NOT touch the separate pre-buy-now test-mode pause at L393-398.
    - Walmart / Target / GameStop / Newegg / SquareEnix: each replaces `await place_order.click()` + trailing `return True` with the single guarded return (selectors per the interfaces table; the `place_order` element variable already holds the resolved element).

    Scope discipline (BUY-02): change ONLY the final place-order click. Do NOT touch add-to-cart, checkout-proceed, captcha, or any other DOM step (RESEARCH anti-pattern: guard wraps only the final irreversible click). Do NOT restructure the auto_buy methods.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; ds=Path('plugins'); fs=sorted(ds.glob('shopbot_plugin_*.py')); missing=[f.name for f in fs if 'place_order_guarded' not in f.read_text(encoding='utf-8')]; assert not missing, missing; assert len(fs)==7, len(fs); print('all 7 routed')"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 `plugins/shopbot_plugin_*.py` files contain `place_order_guarded`.
    - Each plugin's final place-order step is `return await self.place_order_guarded(place_order.click)` (or equivalent with the resolved element variable).
    - Amazon no longer contains the `test_pause_event` wait inside the place-order block and no longer references the dropped `test_mode` var there.
    - No intermediate DOM click (add-to-cart, checkout) was wrapped or altered.
    - The verify command prints `all 7 routed`.
  </acceptance_criteria>
  <done>All 7 plugins route the final place-order click through place_order_guarded; BestBuy gap closed; Amazon ad-hoc gate removed; no other logic changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_safety_gate.py — 7-plugin zero-write integration + grep enforcement</name>
  <files>tests/test_safety_gate.py</files>
  <read_first>
    - tests/conftest.py (fixtures: fake_browser, fake_plugin factory, mock_nodriver_start — lines ~124-153 for fake_plugin)
    - tests/test_orchestrator.py (lines 1-80 — asyncio + write_queue mock + _check_and_buy import patterns)
    - tests/test_plugin_base.py (MinimalPlugin stub pattern lines 13-23)
    - .planning/phases/18-safety-gate-config-foundation/18-RESEARCH.md (Validation Architecture lines 453-515 — the 7-plugin CI test sketch + grep enforcement test sketch; Pitfall 4 lines 334-336 on the write_queue enforcement point)
    - .planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md ("tests/test_safety_gate.py" section lines 433-513 — imports, fixture usage, and the two test sketches)
  </read_first>
  <behavior>
    - test_no_raw_place_order_click_in_plugins (static/grep): for every `plugins/shopbot_plugin_*.py` that defines `auto_buy`, if the file contains any known place-order selector string (#submitOrderButtonId, .button--place-order, place-order-button, placeOrder, place-order, "Place Order"), the file MUST also contain `place_order_guarded`. Fails loudly naming the offending file + selector.
    - test_all_plugins_monitor_only_no_purchase_write (integration): construct each of the 7 real plugin classes (loaded via importlib from plugins/) with a config where `debug.monitor_only=True` and `debug.test_mode=False`; run their place-order path through place_order_guarded (or drive _check_and_buy with auto_buy=True) and assert no `("purchased", ...)` tuple is ever enqueued to the write_queue.
    - test_monitor_only_gate_via_orchestrator: a fake_plugin (available=True, bought=True) with monitor_only=True through `_check_and_buy(..., auto_buy=True, write_queue)` yields zero `("purchased", ...)` writes (BUY-01 cross-check).
  </behavior>
  <action>
    Create `tests/test_safety_gate.py`. No `@pytest.mark.asyncio` decorator (asyncio_mode=auto).

    Grep enforcement test: iterate `Path(__file__).parent.parent / "plugins"` glob `shopbot_plugin_*.py`; skip files without `auto_buy`; for each known place-order selector present in the source, assert `place_order_guarded` is also present, with an assertion message naming the file and selector (per RESEARCH grep sketch lines 487-515). This is the static guard for BUY-02 success criterion 2.

    7-plugin zero-write integration test: load each plugin class (importlib over the 7 module names, find the RetailerPlugin subclass), build a config exposing `debug.monitor_only=True`, `debug.test_mode=False` (use MagicMock per RESEARCH sketch; isolate the monitor_only gate). Drive the place-order path so that `place_order_guarded` is exercised — call each plugin's `place_order_guarded` directly with an AsyncMock click_fn and assert it returns False and click_fn is not awaited (this proves the guard suppresses for all 7 under monitor_only). Then drain the write_queue and assert no `("purchased", ...)` tuple was enqueued. Use the write_queue drain pattern from PATTERNS lines 506-510.

    Orchestrator cross-check test: build a `fake_plugin(available=True, bought=True, config=cfg)` with `cfg.debug.monitor_only=True`, `asyncio.Queue()` write_queue, call `await _check_and_buy(plugin, "Item", "https://fake.example.com/item", True, write_queue)`, drain the queue, assert no `("purchased", ...)` tuple present (BUY-01/BUY-02 integration cross-check).

    Keep all DOM interaction mocked (no real browser). Mock the `place_order` element as an AsyncMock so `place_order.click` is an awaitable callable.
  </action>
  <verify>
    <automated>pytest tests/test_safety_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_safety_gate.py` exists with: `test_no_raw_place_order_click_in_plugins`, `test_all_plugins_monitor_only_no_purchase_write`, and an orchestrator monitor-only cross-check test.
    - The integration test exercises all 7 real plugin classes (loaded via importlib), not stubs, for the guard suppression assertion.
    - No test enqueues or asserts the presence of a `("purchased", ...)` tuple under monitor_only=True; all assert its absence.
    - `pytest tests/test_safety_gate.py -x -q` passes.
  </acceptance_criteria>
  <done>test_safety_gate.py green: grep assertion passes for all 7 plugins, 7-plugin monitor_only path produces zero purchase writes, orchestrator cross-check confirms the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin auto_buy -> place-order DOM click | The final irreversible action across all 7 retailers |
| plugin source -> CI | A future edit could reintroduce a raw place-order click |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering | a plugin bypasses the guard via a raw place-order .click() | mitigate | `test_no_raw_place_order_click_in_plugins` grep assertion fails CI if any place-order selector appears without `place_order_guarded` |
| T-18-11 | Tampering | a plugin places a live order under monitor_only | mitigate | `test_all_plugins_monitor_only_no_purchase_write` asserts zero `("purchased", ...)` writes for all 7 |
| T-18-12 | Repudiation | BestBuy's unconditional click silently re-added | mitigate | Reroute removes the unconditional click; grep + integration tests guard against regression |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `pytest tests/test_safety_gate.py -x -q` green.
- `pytest tests/test_plugin_amazon.py tests/test_plugin_bestbuy.py tests/test_plugin_walmart.py tests/test_plugin_target.py tests/test_plugin_gamestop.py tests/test_plugin_newegg.py tests/test_plugin_squareenix.py -q` — existing per-plugin tests still green after reroute (regression check).
- Grep sanity: every `plugins/shopbot_plugin_*.py` contains `place_order_guarded`.
</verification>

<success_criteria>
- All 7 bundled plugins route the final place-order click through `place_order_guarded`.
- CI test with all 7 plugins and `monitor_only=True` asserts zero purchase-queue writes.
- Grep/static assertion: no plugin clicks place-order outside the guard.
- BestBuy's `test_mode` gap is closed (no unconditional `place_order.click`).
- `tests/test_safety_gate.py` passes; existing per-plugin tests still pass.
</success_criteria>

<output>
Create `.planning/phases/18-safety-gate-config-foundation/18-04-SUMMARY.md` when done
</output>
