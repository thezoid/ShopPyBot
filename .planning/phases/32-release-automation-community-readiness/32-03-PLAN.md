---
phase: 32-release-automation-community-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - CODE_OF_CONDUCT.md
autonomous: true
requirements: [RH-07]
must_haves:
  truths:
    - "SECURITY.md contains zero occurrences of SECURITY_CONTACT_PLACEHOLDER@example.com"
    - "CODE_OF_CONDUCT.md contains zero occurrences of SECURITY_CONTACT_PLACEHOLDER@example.com"
    - "Both files route vulnerability/conduct reports to GitHub Private Vulnerability Reporting at https://github.com/thezoid/ShopPyBot/security/advisories/new"
    - "No email address is substituted for the placeholder (no personal/business email published)"
    - "git grep over tracked NON-.planning files returns zero placeholder occurrences"
  artifacts:
    - path: "SECURITY.md"
      provides: "vulnerability disclosure routed through GitHub PVR, no email"
      contains: "security/advisories/new"
    - path: "CODE_OF_CONDUCT.md"
      provides: "conduct-report contact routed through GitHub PVR, no email"
      contains: "security/advisories/new"
  key_links:
    - from: "SECURITY.md Reporting a Vulnerability"
      to: "GitHub Private Vulnerability Reporting"
      via: "advisories/new link replaces the Direct email item"
      pattern: "security/advisories/new"
    - from: "CODE_OF_CONDUCT.md Enforcement"
      to: "GitHub Private Vulnerability Reporting"
      via: "advisories/new link replaces the placeholder email"
      pattern: "security/advisories/new"
---

<objective>
Replace the two live `SECURITY_CONTACT_PLACEHOLDER@example.com` occurrences (SECURITY.md and CODE_OF_CONDUCT.md) with GitHub Private Vulnerability Reporting wording (RH-07), achieving zero placeholder occurrences in tracked non-`.planning` files with NO email address published.

Purpose: the placeholder currently misdirects security reports to a dead example.com address. Per the locked CONTEXT.md decision (D-RH-07), the disclosure channel is GitHub Private Vulnerability Reporting exclusively — publishing a real maintainer email on a public repo is a lasting exposure the operator did not authorize.

Output: SECURITY.md + CODE_OF_CONDUCT.md pointing to the PVR flow; a repo-wide grep (excluding `.planning/`) confirming zero remaining placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-release-automation-community-readiness/32-RESEARCH.md
@.planning/phases/32-release-automation-community-readiness/32-CONTEXT.md
@SECURITY.md
@CODE_OF_CONDUCT.md

<interfaces>
<!-- Exact recommended replacement prose is in 32-RESEARCH.md "Code Examples" (lines 259-304).
     Executor uses that wording; PVR URL is the real repo's advisories/new endpoint. -->
- SECURITY.md replacement (single PVR channel, operator note): 32-RESEARCH.md lines 259-285
- CODE_OF_CONDUCT.md replacement: 32-RESEARCH.md lines 287-304
- Scope rule (do NOT touch .planning historical occurrences): 32-RESEARCH.md Pitfall 5 (lines 199-203)
- PVR endpoint: https://github.com/thezoid/ShopPyBot/security/advisories/new
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the SECURITY.md contact with GitHub Private Vulnerability Reporting</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md lines 9-27 (Reporting a Vulnerability section, incl. the "Direct email" item at line 20-23)
    - 32-RESEARCH.md "SECURITY.md contact section (RH-07)" recommended replacement (lines 259-285), including the operator note about enabling PVR once in repo Settings.
  </read_first>
  <action>
    In SECURITY.md, remove the numbered "Direct email" item (currently item 2, lines 20-23) that contains `SECURITY_CONTACT_PLACEHOLDER@example.com` and its "(This is a placeholder...)" note. Rewrite the "Reporting a Vulnerability" section so GitHub Private Vulnerability Reporting is the single supported channel: instruct reporters to use the flow at https://github.com/thezoid/ShopPyBot/security/advisories/new (or the Security tab → "Report a vulnerability"), state plainly that there is no public email address for security reports, and keep the existing guidance about what a report should include and the coordinated-disclosure timeline. Add the single operator note (per 32-RESEARCH lines 281-284): Private Vulnerability Reporting must be enabled once in repository Settings → Security & analysis for the link to work; this is a one-time repo setting, not toggled by automation. Do NOT substitute any email address (personal or business) for the placeholder under any circumstance (D-RH-07 locked).
  </action>
  <acceptance_criteria>
    - `rg -c "SECURITY_CONTACT_PLACEHOLDER@example.com" SECURITY.md` == 0
    - `rg -q "security/advisories/new" SECURITY.md` succeeds
    - No email address published: `rg -qi "[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}" SECURITY.md` fails (0 email-shaped matches)
    - The operator note about enabling Private Vulnerability Reporting in repo Settings is present: `rg -qi "Private [Vv]ulnerability [Rr]eporting" SECURITY.md` succeeds
  </acceptance_criteria>
  <verify>
    <automated>[ "$(rg -c 'SECURITY_CONTACT_PLACEHOLDER@example.com' SECURITY.md || echo 0)" = "0" ] && rg -q "security/advisories/new" SECURITY.md && ! rg -qi "[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}" SECURITY.md && rg -qi "Private vulnerability reporting" SECURITY.md && echo "SECURITY.md OK"</automated>
  </verify>
  <done>SECURITY.md has zero placeholder occurrences, routes reports to the PVR advisories/new flow, publishes no email, and carries the one-time operator-enable note.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the CODE_OF_CONDUCT.md contact and verify zero placeholders repo-wide</name>
  <files>CODE_OF_CONDUCT.md</files>
  <read_first>
    - CODE_OF_CONDUCT.md lines 15-24 (Enforcement section with the placeholder at line 20)
    - 32-RESEARCH.md "CODE_OF_CONDUCT.md enforcement section (RH-07)" recommended replacement (lines 287-304)
    - 32-RESEARCH.md Pitfall 5 (lines 199-203): the 8 remaining `.planning/` occurrences are historical audit trail — do NOT edit them.
  </read_first>
  <action>
    In CODE_OF_CONDUCT.md, replace the Enforcement contact block (the `SECURITY_CONTACT_PLACEHOLDER@example.com` line and its "(This is a placeholder...)" note) with wording that routes conduct reports privately via GitHub's private vulnerability reporting flow at https://github.com/thezoid/ShopPyBot/security/advisories/new (Security tab → "Report a vulnerability"), noting the channel is private by design and reaches the maintainer directly. Keep the surrounding Enforcement/Attribution text. Do NOT publish any email address. After both files are edited, run a repo-wide check that EXCLUDES `.planning/` (whose historical PLAN/SUMMARY/VERIFICATION/CONTEXT/REQUIREMENTS/ROADMAP records intentionally reference the placeholder string as audit trail and must not be rewritten) and confirm zero placeholder occurrences remain in tracked non-`.planning` files.
  </action>
  <acceptance_criteria>
    - `rg -c "SECURITY_CONTACT_PLACEHOLDER@example.com" CODE_OF_CONDUCT.md` == 0
    - `rg -q "security/advisories/new" CODE_OF_CONDUCT.md` succeeds
    - No email published: `rg -qi "[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}" CODE_OF_CONDUCT.md` fails (0 matches)
    - Repo-wide, excluding .planning: `git grep -c "SECURITY_CONTACT_PLACEHOLDER@example.com" -- ':!.planning'` returns no lines (0 total occurrences in tracked non-.planning files)
    - The `.planning/` historical occurrences are untouched (not asserted as a count, but no `.planning/` file appears in this plan's diff)
  </acceptance_criteria>
  <verify>
    <automated>[ "$(rg -c 'SECURITY_CONTACT_PLACEHOLDER@example.com' CODE_OF_CONDUCT.md || echo 0)" = "0" ] && rg -q "security/advisories/new" CODE_OF_CONDUCT.md && ! rg -qi "[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}" CODE_OF_CONDUCT.md && [ -z "$(git grep 'SECURITY_CONTACT_PLACEHOLDER@example.com' -- ':!.planning' 2>/dev/null)" ] && echo "COC + repo-wide OK"</automated>
  </verify>
  <done>CODE_OF_CONDUCT.md has zero placeholders and routes to PVR; a repo-wide grep excluding .planning confirms zero placeholder occurrences in tracked non-.planning files; no .planning historical record was altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SECURITY.md / CODE_OF_CONDUCT.md → external reporter | These docs are the sole routing for vulnerability + conduct reports; wrong contact info misdirects sensitive disclosures |
| public repo → maintainer PII | Any published email becomes a permanent, scrapable exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Information Disclosure | maintainer contact in public docs | mitigate | Use GitHub Private Vulnerability Reporting exclusively; publish NO email (both tasks assert zero email-shaped matches) — implements locked decision D-RH-07 |
| T-32-08 | Repudiation / misdirection | dead placeholder email routes reports nowhere | mitigate | Replace with a working PVR advisories/new link so reports reach the maintainer's private inbox with an audit trail |
| T-32-09 | Tampering | falsifying historical `.planning/` records | accept (out of scope) | The 8 `.planning/` placeholder occurrences are the project's own decision audit trail; RH-07's "zero occurrences" is scoped to live non-`.planning` docs (Pitfall 5). Verification excludes `.planning/`. |
| T-32-SC | Tampering | npm/pip installs | accept | No package installs in this plan (docs only) |
</threat_model>

<verification>
- SECURITY.md and CODE_OF_CONDUCT.md each have zero `SECURITY_CONTACT_PLACEHOLDER@example.com` occurrences and no email-shaped strings; both link to the PVR advisories/new flow.
- `git grep "SECURITY_CONTACT_PLACEHOLDER@example.com" -- ':!.planning'` returns nothing (RH-07 "zero occurrences" success criterion, scoped to live docs).
- `.planning/` historical occurrences remain untouched.

**Operator action item (surface in SUMMARY, do NOT action here):**
- Private Vulnerability Reporting must be enabled once by the operator in repo Settings → Security & analysis for the SECURITY.md / CODE_OF_CONDUCT.md advisories/new link to resolve. One-time repo setting; never toggled autonomously (do not call `gh api` to change repo settings).
</verification>

<success_criteria>
- [ ] Zero `SECURITY_CONTACT_PLACEHOLDER@example.com` in SECURITY.md and CODE_OF_CONDUCT.md (RH-07).
- [ ] Zero placeholder occurrences in tracked non-`.planning` files (RH-07).
- [ ] Both docs route to GitHub PVR (advisories/new); no email published (D-RH-07).
- [ ] `.planning/` historical records unchanged.
- [ ] PVR-enable operator action item recorded in SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/32-release-automation-community-readiness/32-03-SUMMARY.md` when done.
</output>
