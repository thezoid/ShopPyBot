---
context: phase
phase: 11-cross-platform-verification
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-06-05T03:37:14.451Z
---

# Resume: Phase 11 (final) — one plan left, then milestone lifecycle

Paused mid `gsd-autonomous` run (phases 8→11). Phases 8, 9, 10 are **complete + pushed**. Phase 11 plans **11-01..11-04 are done + committed (not pushed)**. Only **plan 11-05** remains, then phase-11 verify/review, then the milestone lifecycle.

<current_state>
Phase 11/11, Plan 5 of 5 (11-05), not started. The 11-05 executor was dispatched then interrupted — no 11-05 work exists on disk yet. Suite baseline: **352 passed, 2 skipped, 0 failures** (run `pytest` from repo root `E:\repos\ShopPyBot`, never a phase subdir). 21 commits unpushed on master.
</current_state>

<completed_work>
- Phase 8 Credential Store — done, pushed. CR-01 secret-leak fix.
- Phase 9 CLI Front-End — done, pushed. CR-01 exit-code + WR-06 footgun fixed.
- Phase 10 Optional Web UI — done, pushed. 5 security/data fixes (credential allowlist bypass, config.yml corruption, CSRF gap, credential 422, quantity bound).
- Phase 11-01 core/paths.py + platformdirs==4.10.0 pin (341 tests).
- Phase 11-02 re-anchor models/config_schema/credentials/logger through core/paths.py; fixed models.py CWD-relative DB bug; logger lazy-imports core.paths.log_dir.
- Phase 11-03 idempotent first-run migration (copy2→unlink) in core/paths.py, wired into service.py main() (345 tests).
- Phase 11-04 env-independent smoke + backend-selection tests (352 tests).
</completed_work>

<remaining_work>
- **11-05 T1**: `.github/workflows/ci.yml` — matrix [ubuntu-latest, windows-latest], Python 3.13, `pip install -e .[web]` then `pytest`; job env `SDL_AUDIODRIVER=dummy` + `PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring`.
- **11-05 T2**: `docs/PLATFORMS.md` — per-OS path table (Windows `%LOCALAPPDATA%\shoppybot`; Linux `~/.local/share/shoppybot` + `~/.local/state/shoppybot/log`), backend-per-env (Windows/desktop→keyring, headless→encrypted-file, env-var fallback), repro steps, and the manual checklist matrix folding in the deferred Phase 8/9/10 live checks.
- **11-05 T3**: delete `.github/workflows/app_linuxBuild.yml`, `app_macBuild.yml`, `app_windowsBuild.yml` (**USER APPROVED** — checkpoint pre-cleared). KEEP `codeql-analysis.yml`.
- Then: gsd-verifier phase 11 → gsd-code-review 11 --fix --auto → milestone lifecycle (gsd-audit-milestone → gsd-complete-milestone v2.0 → gsd-cleanup) → push master.
</remaining_work>

<decisions_made>
- OS-standard user dirs via `platformdirs` (appauthor=False) + first-run `copy2`-then-`unlink` migration; `SHOPBOT_DATA_DIR` env override is the test seam (path accessors re-read it each call).
- `logger.py` lazy-imports `core.paths.log_dir` inside `writeLog` to break the circular import (core/paths imports writeLog for migration logging).
- CLI/web config UI scoped to test_mode, logging_level, notifier enables only (AppConfig has no per-platform `enabled` field).
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full machine-readable state (standing approvals, watch items, env).
2. `.planning/phases/11-cross-platform-verification/11-05-PLAN.md` — the plan to execute.
3. `.planning/phases/11-cross-platform-verification/11-RESEARCH.md` — ci.yml shape + CI headless env vars + per-OS path facts.
4. `.planning/phases/11-cross-platform-verification/11-VALIDATION.md` — the manual-matrix rows to fold into docs/PLATFORMS.md.
5. `.planning/phases/{08,09,10}-*/  *-VERIFICATION.md` — the deferred live checks to absorb into the matrix.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Subagent commit-gate stall | gsd-executor/planner subagents stop on the per-commit approval gate during autonomous runs | advisory | Put an `<autonomous_commit_authorization>` block (no pausing, no push, NO Claude co-author trailer) in every executor/planner subagent prompt |
| Claude co-author trailer | Harness default appends a Claude Co-Authored-By trailer; user global CLAUDE.md forbids naming Claude as a contributor | advisory | Commit via `gsd-sdk query commit` (no trailer) and instruct subagents to omit any AI trailer |
| pytest CWD drift | A `cd` into a phase dir made `pytest` collect 0 tests | advisory | Always run `pytest` from repo root `E:\repos\ShopPyBot` |
| items-list fresh-install DB gap | `BotService.__init__` calls `init_store()` but not `initialize_db()`; `shoppybot items list` on a machine with no DB may crash. 11-04 smoke worked around it by calling `models.initialize_db()` in the subprocess | advisory | Assess in phase-11 code review whether CLI items / web item routes should ensure the DB exists |

## Infrastructure State
- Test deps installed in this env: `platformdirs==4.10.0`, `python-multipart==0.0.32`, plus fastapi/uvicorn/jinja2 (already present).
- Repo: branch `master`, branching_strategy none, `workflow.use_worktrees=false` (executors run sequentially on the main tree).
- 21 commits unpushed (it is past 11pm US Eastern — wrap-up time).

## Standing approvals for this run (do not re-ask)
- Auto-commit all GSD doc+code commits this run (no pushes without separate approval).
- Deps approved: keyring==25.7.0, the [web] bundle (fastapi/uvicorn/jinja2/python-multipart), platformdirs==4.10.0.
- Delete the 3 legacy workflow files; keep codeql-analysis.yml.

<context>
This has been a clean, methodical autonomous run: every phase went discuss → (UI-SPEC) → research → validation → pattern-map → plan → plan-check → execute → verify → code-review+fix → push. Code review caught real security bugs each frontend/secret phase (P10 had 3 blockers: credential allowlist bypass, config corruption, CSRF gap — all fixed). Phase 11 is the wrap-up: it moved data/config/log to OS-standard dirs (platformdirs) with safe migration, added env-independent smoke + a CI matrix, and documents the cross-platform verification matrix that finally closes the human-needed checks deferred from phases 8/9/10. After 11-05 the milestone is code-complete; only the maintainer's real-hardware Ubuntu/Windows matrix run remains as a manual step.
</context>

<next_action>
Start with: execute plan 11-05 via a gsd-executor subagent (sequential, main tree) — prompt it with an `<autonomous_commit_authorization>` block, the pre-cleared legacy-deletion checkpoint, and the constraints from 11-05-PLAN.md. Verify `pytest` stays green (352+) from the repo root. Then run gsd-verifier + gsd-code-review on phase 11, then the milestone lifecycle (audit → complete v2.0 → cleanup), then push master.
</next_action>
