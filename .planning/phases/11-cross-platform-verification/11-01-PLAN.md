---
phase: 11-cross-platform-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/paths.py
  - requirements.txt
  - pyproject.toml
  - tests/test_paths.py
autonomous: true
requirements: [XPLAT-01]
must_haves:
  truths:
    - "data_dir(), config_path(), log_dir() return OS-appropriate absolute Paths"
    - "SHOPBOT_DATA_DIR env var overrides resolution on every call (test seam)"
    - "platformdirs==4.10.0 is pinned in requirements.txt and pyproject.toml core deps"
  artifacts:
    - path: "core/paths.py"
      provides: "data_dir/config_path/log_dir accessors backed by platformdirs"
      contains: "import platformdirs"
    - path: "tests/test_paths.py"
      provides: "resolution + env-override + no-separator guard tests"
    - path: "requirements.txt"
      provides: "platformdirs==4.10.0 pin"
      contains: "platformdirs==4.10.0"
  key_links:
    - from: "core/paths.py"
      to: "platformdirs.PlatformDirs"
      via: "module-level _DIRS = PlatformDirs(_APP_NAME, appauthor=False)"
      pattern: "PlatformDirs\\(.*appauthor=False"
    - from: "core/paths.py:data_dir"
      to: "os.environ SHOPBOT_DATA_DIR"
      via: "_env_override read on every call"
      pattern: "environ.get\\(\"SHOPBOT_DATA_DIR\""
---

<objective>
Create `core/paths.py` — the single source of truth for OS-standard data/config/log directories — and pin the `platformdirs` dependency. This is the foundation every consumer (models, credentials, config_schema, logger) re-anchors onto in Plan 02, and that migration uses in Plan 03.

Purpose: XPLAT-01 requires data/config/log to resolve to OS-appropriate per-user locations with no hardcoded path separators. `platformdirs` solves this across Windows (%LOCALAPPDATA%) and Linux (XDG).
Output: `core/paths.py`, `tests/test_paths.py`, dependency pins in `requirements.txt` + `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cross-platform-verification/11-CONTEXT.md
@.planning/phases/11-cross-platform-verification/11-RESEARCH.md
@.planning/phases/11-cross-platform-verification/11-PATTERNS.md

<interfaces>
From core/credentials.py (the module-level-singleton + Path anchor pattern paths.py mirrors):
```python
from __future__ import annotations
import os
from pathlib import Path
_DEFAULT_STORE_PATH: Path = Path(__file__).parent.parent / "data" / "creds.bin"
```

platformdirs 4.10.0 API (verified live on Windows + from Linux source in RESEARCH.md):
- PlatformDirs("shoppybot", appauthor=False).user_data_dir
  -> Windows: C:\Users\<user>\AppData\Local\shoppybot
  -> Linux:   ~/.local/share/shoppybot
- .user_log_dir
  -> Windows: C:\Users\<user>\AppData\Local\shoppybot\Logs
  -> Linux:   ~/.local/state/shoppybot/log
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test_paths.py (RED) for resolution, env-override, separator guard</name>
  <read_first>
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (Path-override-via-env and SC1 separator-guard test patterns)
    - .planning/phases/11-cross-platform-verification/11-RESEARCH.md (Pattern 1 paths.py shape; SC1 grep guard)
    - tests/test_credentials.py (monkeypatch/import-under-test analog)
  </read_first>
  <behavior>
    - test_data_dir_is_absolute: with SHOPBOT_DATA_DIR unset, data_dir() returns an absolute Path
    - test_config_path_under_data_dir: config_path() == data_dir() / "config.yml"
    - test_log_dir_is_absolute: log_dir() returns an absolute Path
    - test_env_override: setting SHOPBOT_DATA_DIR makes data_dir() return that path (read on every call, not cached at import)
    - test_log_dir_env_override: with SHOPBOT_DATA_DIR set, log_dir() == override / "logs"
    - test_no_hardcoded_separators: no source file in core/ plus logger.py, models.py, utils.py contains the literals "data/ / 'data/ / "logs/ / 'logs/ (the SC1 guard)
  </behavior>
  <action>
    Create tests/test_paths.py. Import accessors inside each test (after monkeypatch.setenv) so the env override is read on the call, per RESEARCH Pitfall 1. Use monkeypatch.delenv("SHOPBOT_DATA_DIR", raising=False) where the real platformdirs path is asserted. For test_no_hardcoded_separators, glob Path("core").rglob("*.py") plus the three root modules, scan each line for the four bad substrings ('"data/', "'data/", '"logs/', "'logs/"), and assert the violations list is empty with a path:line:line-content message. Use a non-comment-stripping scan acceptable here because no source line legitimately contains those substrings outside string literals; do NOT add a bare grep == 0 gate on a comment-bearing file. Tests fail now because core/paths.py does not yet exist.
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -m pytest tests/test_paths.py -q 2>&1 | findstr /C:"ModuleNotFoundError" /C:"failed" /C:"error"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_paths.py exists with the six test functions named above
    - `python -m pytest tests/test_paths.py -q` collects and fails (RED) due to missing core/paths.py
    - test_no_hardcoded_separators scans core/ + logger.py + models.py + utils.py
  </acceptance_criteria>
  <done>tests/test_paths.py committed; runs RED because core/paths.py is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create core/paths.py (GREEN) with platformdirs-backed accessors</name>
  <read_first>
    - .planning/phases/11-cross-platform-verification/11-RESEARCH.md (Pattern 1 full implementation shape)
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (core/paths.py utility section)
    - core/credentials.py (lines 15-40 — import + module-level anchor pattern to mirror)
  </read_first>
  <action>
    Create core/paths.py with: `from __future__ import annotations`, `import os`, `from pathlib import Path`, `from platformdirs import PlatformDirs`. Define _APP_NAME = "shoppybot" and module-level _DIRS = PlatformDirs(_APP_NAME, appauthor=False) (appauthor=False suppresses the redundant vendor subdir on Windows per locked decision D — yields %LOCALAPPDATA%\shoppybot, not \shoppybot\shoppybot). Add _env_override() reading os.environ.get("SHOPBOT_DATA_DIR", "").strip() returning Path(val) or None. Add data_dir() returning _env_override() or Path(_DIRS.user_data_dir); config_path() returning data_dir() / "config.yml"; log_dir() returning override/"logs" when SHOPBOT_DATA_DIR set else Path(_DIRS.user_log_dir). Each accessor re-reads the env on every call (the monkeypatch seam that keeps all 335 tests green). No module-level side effects beyond _DIRS construction (no mkdir, no migration here — migration lands in Plan 03). Keep every function under 30 lines per CLAUDE.md. Do NOT import logger or any consumer at module top (Plan 03 will add migrate_legacy_paths which lazy-imports writeLog inside the function body to avoid the circular import).
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -m pytest tests/test_paths.py -q</automated>
  </verify>
  <acceptance_criteria>
    - core/paths.py exists and `core/paths.py contains import platformdirs` (the `from platformdirs import PlatformDirs` line)
    - core/paths.py defines data_dir, config_path, log_dir, _env_override
    - `python -m pytest tests/test_paths.py -q` exits 0 (all six tests green)
    - `python -c "from core.paths import data_dir, config_path, log_dir; print(data_dir().is_absolute())"` prints True
  </acceptance_criteria>
  <done>core/paths.py resolves OS-appropriate absolute paths; SHOPBOT_DATA_DIR override honored; test_paths.py green.</done>
</task>

<task type="auto">
  <name>Task 3: Pin platformdirs==4.10.0 in requirements.txt and pyproject.toml core deps</name>
  <read_first>
    - requirements.txt (existing exact-pin one-per-line format)
    - pyproject.toml (no [project.dependencies] array yet; web extra at lines 10-16)
    - .planning/phases/11-cross-platform-verification/11-RESEARCH.md (Package Legitimacy Audit — platformdirs Approved/VERIFIED, no checkpoint)
  </read_first>
  <action>
    Add `platformdirs==4.10.0` to requirements.txt in alphabetical position (between nodriver==0.50.3 and pydantic==2.13.3). In pyproject.toml [project] table, add a `dependencies = ["platformdirs==4.10.0"]` array (core dependency per locked decision: pinned in requirements.txt + pyproject core deps) so `pip install -e .` pulls it. Do NOT remove or alter the existing [project.optional-dependencies].web block or the requires-python pin. platformdirs is pre-approved in the Package Legitimacy Audit (tox-dev org, 147M+/week, already transitively present) — no human checkpoint required.
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -c "import platformdirs, tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'platformdirs==4.10.0' in d['project']['dependencies']; assert 'platformdirs==4.10.0' in open('requirements.txt').read(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt contains platformdirs==4.10.0`
    - pyproject.toml [project].dependencies includes "platformdirs==4.10.0" (tomllib parse passes)
    - `python -c "import platformdirs"` succeeds
  </acceptance_criteria>
  <done>platformdirs==4.10.0 pinned in both manifests; import succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env -> path resolution | SHOPBOT_DATA_DIR is user/CI-controlled input that selects the data root |
| package index -> install | platformdirs pulled from PyPI during install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | SHOPBOT_DATA_DIR path traversal | accept | Path(val) normalizes; in CI the value is set by the trusted workflow; in production the user controls their own env. No untrusted network input. Low risk per RESEARCH Security Domain. |
| T-11-02 | Tampering | platformdirs install (supply chain) | mitigate | Exact-pinned ==4.10.0; pre-vetted in RESEARCH Package Legitimacy Audit (tox-dev org, VERIFIED on pypi.org/project/platformdirs); already transitively present. No [ASSUMED]/[SUS] packages -> no blocking checkpoint required. |
| T-11-SC | Tampering | pip installs | mitigate | Only dependency added is the pre-vetted platformdirs pin; no other installs in this plan. |
</threat_model>

<verification>
- `python -m pytest tests/test_paths.py -q` exits 0
- `python -m pytest -q` shows no NEW failures vs the 335-test baseline (this plan only adds a module + tests; no consumer re-anchor yet)
- `python -c "from core.paths import data_dir; print(data_dir())"` prints an absolute OS-standard path
</verification>

<success_criteria>
core/paths.py exposes data_dir/config_path/log_dir backed by platformdirs with a SHOPBOT_DATA_DIR override; the separator guard test passes; platformdirs==4.10.0 is pinned in both manifests; baseline suite stays green.
</success_criteria>

<output>
Create `.planning/phases/11-cross-platform-verification/11-01-SUMMARY.md` when done.
</output>
