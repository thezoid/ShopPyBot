---
phase: 11-cross-platform-verification
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-03", "11-04"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/app_linuxBuild.yml
  - .github/workflows/app_macBuild.yml
  - .github/workflows/app_windowsBuild.yml
  - docs/PLATFORMS.md
autonomous: false
requirements: [XPLAT-02, XPLAT-01]
must_haves:
  truths:
    - "ci.yml runs a matrix over ubuntu-latest + windows-latest on Python 3.13, pip install .[web], full pytest + smoke"
    - "ci.yml sets headless env vars: SDL_AUDIODRIVER=dummy, PYTHON_KEYRING_BACKEND=null, SHOPBOT_DATA_DIR=runner.temp"
    - "docs/PLATFORMS.md documents per-OS paths, expected backend per env, repro steps, and the manual verification matrix"
    - "the 3 EOL Python-3.9 legacy workflow files are deleted ONLY after explicit human confirmation; codeql-analysis.yml is untouched"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "matrix CI workflow with headless guards"
      contains: "matrix"
    - path: "docs/PLATFORMS.md"
      provides: "per-OS path table + backend-per-env + repro + manual matrix folding deferred Phase 8/9/10 checks"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pytest"
      via: "pip install -e .[web] then pytest --tb=short on both OS"
      pattern: "pip install -e .\\[web\\]"
    - from: "docs/PLATFORMS.md"
      to: "the deferred Phase 8/9/10 live checks"
      via: "manual matrix rows for keyring-restart / setup-TTY / dashboard"
      pattern: "Ubuntu headless"
---

<objective>
Add the CI matrix workflow `.github/workflows/ci.yml` (automated half of the verification matrix) and `docs/PLATFORMS.md` (documentation + the manual checklist matrix that folds in the deferred Phase 8/9/10 live checks). Then, behind a human-confirm checkpoint, delete the three EOL Python-3.9 per-OS workflow files that `ci.yml` replaces.

Purpose: XPLAT-02 (automated CI + documented matrix) and XPLAT-01 (per-OS path documentation). This is the milestone-closing plan.
Output: ci.yml, docs/PLATFORMS.md, and (after confirmation) removal of app_linuxBuild.yml / app_macBuild.yml / app_windowsBuild.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cross-platform-verification/11-CONTEXT.md
@.planning/phases/11-cross-platform-verification/11-RESEARCH.md
@.planning/phases/11-cross-platform-verification/11-PATTERNS.md
@.planning/phases/11-cross-platform-verification/11-VALIDATION.md
@.github/workflows/app_windowsBuild.yml

<interfaces>
Headless CI env vars (RESEARCH Pattern 5 — all verified):
- SDL_AUDIODRIVER: dummy        # pygame.mixer.init() at utils.py import fails on headless Linux without this
- SDL_VIDEODRIVER: dummy        # pygame display init guard
- PYTHON_KEYRING_BACKEND: keyring.backends.null.Keyring  # _has_real_keyring() returns False without a D-Bus probe/hang
- SHOPBOT_DATA_DIR: ${{ runner.temp }}/shopbot           # redirects all path resolution to a writable temp dir (both OS)

Verified per-OS resolved paths (RESEARCH Code Examples, live + source-confirmed):
- Windows: data/config %LOCALAPPDATA%\shoppybot ; logs %LOCALAPPDATA%\shoppybot\Logs
- Linux:   data ~/.local/share/shoppybot ; config ~/.local/share/shoppybot/config.yml ; logs ~/.local/state/shoppybot/log
Expected backend per env: Windows -> keyring ; Ubuntu desktop -> keyring ; Ubuntu headless -> encrypted-file.

Deferred live checks to fold into the manual matrix (from VALIDATION.md Manual-Only table):
- Phase 8: OS keyring secret survives process restart; headless Ubuntu auto-selects encrypted-file
- Phase 9: setup masked TTY entry on Windows PowerShell + Ubuntu (no echo, name-only confirm)
- Phase 10: dashboard renders + live Start/Stop + log polling + non-local --host 0.0.0.0 warning banner

KEEP untouched: .github/workflows/codeql-analysis.yml (separate concern, NOT a legacy build file).
DELETE (after confirmation): app_linuxBuild.yml, app_macBuild.yml, app_windowsBuild.yml (Python 3.9, actions @v2, EOL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml matrix workflow</name>
  <read_first>
    - .github/workflows/app_windowsBuild.yml (the analog being replaced — triggers + structure)
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (ci.yml target matrix shape + headless env block)
    - .planning/phases/11-cross-platform-verification/11-RESEARCH.md (Pattern 5 + Pitfalls 3/4/6 explaining each env var)
  </read_first>
  <action>
    Create .github/workflows/ci.yml: name "CI"; on push + pull_request to branches [master, dev]; one job `test` with runs-on ${{ matrix.os }}, strategy.fail-fast=false, matrix.os=[ubuntu-latest, windows-latest]. Set the job-level env block (so vars are active during pytest collection, not just a step): SDL_AUDIODRIVER=dummy, SDL_VIDEODRIVER=dummy, PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring, SHOPBOT_DATA_DIR=${{ runner.temp }}/shopbot. Steps: actions/checkout@v4; actions/setup-python@v5 with python-version "3.13"; "Install" -> pip install -e .[web]; "Test" -> pytest --tb=short. Use @v4/@v5 (Node 20, not the EOL @v2 from the legacy files). Only set SHOPBOT_DATA_DIR in the env block, never in a run: shell command, to avoid Windows path-with-spaces word-splitting (RESEARCH Pitfall 6). Do not add a separate job per OS — single matrix job.
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['test']; assert j['strategy']['matrix']['os']==['ubuntu-latest','windows-latest']; assert j['env']['SDL_AUDIODRIVER']=='dummy'; assert j['env']['PYTHON_KEYRING_BACKEND']=='keyring.backends.null.Keyring'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and parses as valid YAML
    - matrix.os == [ubuntu-latest, windows-latest]; python-version "3.13"
    - env block contains SDL_AUDIODRIVER, SDL_VIDEODRIVER, PYTHON_KEYRING_BACKEND, SHOPBOT_DATA_DIR
    - install step uses `pip install -e .[web]`; uses actions/checkout@v4 and actions/setup-python@v5
  </acceptance_criteria>
  <done>ci.yml matrix workflow parses valid and encodes the headless guards + .[web] install + pytest on both OS.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/PLATFORMS.md — path table, backend-per-env, repro steps, manual matrix</name>
  <read_first>
    - .planning/phases/11-cross-platform-verification/11-CONTEXT.md (docs/PLATFORMS.md required sections + matrix that folds deferred checks)
    - .planning/phases/11-cross-platform-verification/11-VALIDATION.md (Manual-Only Verifications table — the deferred Phase 8/9/10 rows)
    - .planning/phases/11-cross-platform-verification/11-RESEARCH.md (verified per-OS paths in Code Examples)
  </read_first>
  <action>
    Create docs/PLATFORMS.md (new docs/ dir) with these sections per the locked decision: (1) "Resolved Paths per OS" — a table with rows data dir / config path / log dir and columns Windows / Ubuntu, using the verified values (Windows %LOCALAPPDATA%\shoppybot + \Logs; Ubuntu ~/.local/share/shoppybot, .../config.yml, ~/.local/state/shoppybot/log). (2) "Expected Credential Backend per Environment" — Windows -> keyring (Credential Manager), Ubuntu desktop -> keyring (Secret Service), Ubuntu headless -> encrypted-file (no active Secret Service). (3) "Reproduce the Verification Matrix" — step-by-step: pip install -e .[web]; run pytest; what the CI matrix covers (automated half on ubuntu-latest + windows-latest); how SHOPBOT_DATA_DIR redirects paths. (4) "Manual Verification Checklist" — a matrix of {Ubuntu desktop, Ubuntu headless, Windows} x {--help, setup, run smoke, items, config, web} x {keyring, file, env backends} as checkboxes, PLUS explicit rows folding the deferred live checks: Phase 8 (keyring secret survives restart; headless auto-selects encrypted-file), Phase 9 (setup masked TTY no-echo on Windows PowerShell + Ubuntu), Phase 10 (dashboard renders + live Start/Stop + log polling + non-local --host 0.0.0.0 warning banner). Follow CLAUDE.md doc style: no em dashes, no horizontal-rule lines. Use pathlib-style paths in prose; introduce no test-tripping 'data/ or 'logs/ literals are fine here since docs/ is outside the SC1 scan scope (core/ + root modules only) — but prefer the backslash/tilde forms shown.
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -c "t=open('docs/PLATFORMS.md',encoding='utf-8').read(); assert 'shoppybot' in t; assert 'keyring' in t and 'encrypted-file' in t; assert 'Ubuntu headless' in t; assert '0.0.0.0' in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/PLATFORMS.md exists (`test -f docs/PLATFORMS.md` succeeds)
    - contains the per-OS path table (Windows + Ubuntu values), backend-per-env mapping, repro steps, and the manual matrix
    - manual matrix includes the folded deferred Phase 8/9/10 rows (keyring restart, setup TTY, dashboard + non-local banner)
  </acceptance_criteria>
  <done>docs/PLATFORMS.md documents verified paths, expected backends, repro steps, and the milestone-closing manual matrix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm deletion of the 3 EOL legacy workflow files</name>
  <what-built>
    ci.yml (matrix workflow) now supersedes the three legacy per-OS workflows. The legacy files use Python 3.9 and actions/checkout@v2 + setup-python@v2 (EOL Node 12) and would keep running stale checks on every push/PR.
  </what-built>
  <how-to-verify>
    Files proposed for deletion (replaced by ci.yml):
      1. .github/workflows/app_linuxBuild.yml
      2. .github/workflows/app_macBuild.yml
      3. .github/workflows/app_windowsBuild.yml
    NOT deleted (must remain): .github/workflows/codeql-analysis.yml and the new ci.yml.
    Why safe: ci.yml covers ubuntu-latest + windows-latest on Python 3.13 with the full suite + smoke; the legacy files add only redundant, outdated checks. Recovery: they are tracked in git history and restorable via `git checkout HEAD~1 -- .github/workflows/<file>`.
    After you approve, the executor will delete exactly those three files and run `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` to confirm ci.yml still parses and `git status` to show only those three removals.
  </how-to-verify>
  <resume-signal>Type "approved" to delete the three legacy workflow files, or "skip" to keep them.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> repo | the workflow checks out and runs the test suite on hosted runners |
| maintainer -> workflow files | deletion of CI config is a gated change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12 | Information Disclosure | CI workflow leaking secrets | mitigate | ci.yml uses NO repository secrets — only public env vars (SDL/keyring/data-dir redirects). No `${{ secrets.* }}` reference exists. Nothing to leak. |
| T-11-13 | Tampering / Denial of Service | deleting the wrong workflow file (e.g. codeql) | mitigate | Deletion is gated behind a blocking human-verify checkpoint listing the exact three files; codeql-analysis.yml is explicitly excluded; files are recoverable from git history. |
| T-11-14 | Denial of Service | headless CI hang/crash (pygame audio / keyring D-Bus) | mitigate | SDL_AUDIODRIVER=dummy + PYTHON_KEYRING_BACKEND=null in the job env block prevent the pygame.mixer.init() failure and the keyring D-Bus probe hang on ubuntu-latest (RESEARCH Pitfalls 3 + 4). |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` parses without error
- `test -f docs/PLATFORMS.md` succeeds and the doc contains the path table + backend map + manual matrix
- After checkpoint approval: the three legacy files are gone, codeql-analysis.yml + ci.yml remain, ci.yml still parses
</verification>

<success_criteria>
ci.yml encodes the ubuntu+windows matrix with headless guards and .[web] install; docs/PLATFORMS.md documents verified per-OS paths, expected backends, repro steps, and the manual matrix folding the deferred Phase 8/9/10 checks; the three EOL legacy workflows are removed only after explicit human confirmation, codeql untouched.
</success_criteria>

<output>
Create `.planning/phases/11-cross-platform-verification/11-05-SUMMARY.md` when done.
</output>
