---
phase: 20-checkout-profile-form-fill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/cli/setup.py
  - core/cli/__init__.py
  - tests/test_setup_checkout_profile.py
autonomous: true
requirements: [BUY-07]
must_haves:
  truths:
    - "User can run a `setup checkout-profile` action that prompts the 9 address keys"
    - "Address-field prompts echo input (visible), not getpass"
    - "Stored confirmation output prints key NAMES only, never values"
    - "CHECKOUT_ADDRESS_LINE2 can be skipped with empty input"
    - "No card number or CVV is ever prompted or stored by this command"
  artifacts:
    - path: "core/cli/setup.py"
      provides: "handle_setup_checkout_profile + _prompt_visible helper"
      contains: "def handle_setup_checkout_profile"
    - path: "core/cli/__init__.py"
      provides: "parser wiring routing the checkout-profile action to the handler"
    - path: "tests/test_setup_checkout_profile.py"
      provides: "stores-9-keys, name-only output, optional line2 skip"
  key_links:
    - from: "core/cli/__init__.py"
      to: "core/cli/setup.handle_setup_checkout_profile"
      via: "parser routing (sub-action or flag branch)"
      pattern: "checkout.profile"
    - from: "core/cli/setup.handle_setup_checkout_profile"
      to: "core/credentials.get_store"
      via: "store.set(key, val) per CHECKOUT_PROFILE_KEYS"
      pattern: "store\\.set"
---

<objective>
Add a `setup checkout-profile` capability to the CLI that interactively prompts the 9 address keys with VISIBLE input and stores them in the CredentialStore. Confirmation output names keys only. No card number or CVV is ever collected here.

Purpose: BUY-07 success criterion 1 — `shoppybot setup checkout-profile` populates the 9-key profile in the off-disk store.
Output: `handle_setup_checkout_profile` + a `_prompt_visible` helper in `core/cli/setup.py`, parser wiring in `core/cli/__init__.py`, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-checkout-profile-form-fill/20-CONTEXT.md
@.planning/phases/20-checkout-profile-form-fill/20-RESEARCH.md
@.planning/phases/20-checkout-profile-form-fill/20-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly; no exploration needed. -->

From core/cli/setup.py: handle_setup(args, svc) -> int is the existing credential wizard. _prompt_secret(prompt) uses getpass (hidden). _prompt_backend() uses sys.stdin.readline() for VISIBLE input (the analog to copy). handle_setup loops SECRET_KEYS, calls store.set(key, val), and prints "  Stored: {key}" (key NAME only, T-09-04).

From core/cli/__init__.py: build_parser() wires subparsers via set_defaults(func=...). The `setup` subparser is registered at lines 51-57: setup_p = sub.add_parser("setup", ...); setup_p.add_argument("--migrate", ...); setup_p.set_defaults(func=handle_setup). The `items` group (lines 71-110) shows the sub-subparser pattern (items_sub = items_p.add_subparsers(dest="items_command")) and the reusable _require_subcommand helper (lines 59-69). main() in core/service.py dispatches via parse_known_args then args.func(args, svc).

From core/checkout_profile.py (Plan 01): CHECKOUT_PROFILE_KEYS (9 keys); CHECKOUT_ADDRESS_LINE2 is the only optional key.

DECISION (per planning context, RESEARCH §"setup checkout-profile Subcommand Pattern"): prefer the BUY-07 spec'd sub-action form `shoppybot setup checkout-profile` (Option A: sub-subparser) IF achievable without large parser restructuring. The `items`/`config` groups already use add_subparsers; mirror that. If a true sub-subcommand proves invasive against the existing setup tests, fall back to Option B (`setup --checkout-profile` flag). Document the chosen form in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _prompt_visible + handle_setup_checkout_profile and wire the parser</name>
  <files>core/cli/setup.py, core/cli/__init__.py</files>
  <read_first>
    - core/cli/setup.py (handle_setup lines 51-86; _prompt_backend lines 28-35; _prompt_secret lines 15-25)
    - core/cli/__init__.py (setup_p registration lines 51-57; items sub-subparser block lines 71-110; _require_subcommand lines 59-69)
    - core/checkout_profile.py (CHECKOUT_PROFILE_KEYS)
    - .planning/phases/20-checkout-profile-form-fill/20-PATTERNS.md (sections "core/cli/setup.py - add handle_setup_checkout_profile" and "core/cli/__init__.py - add setup checkout-profile sub-subparser")
  </read_first>
  <behavior>
    - Invoking the checkout-profile action prompts each of the 9 keys in CHECKOUT_PROFILE_KEYS using VISIBLE input
    - Empty input for CHECKOUT_ADDRESS_LINE2 skips it (no store.set for that key); empty input for a required key also skips storing that key (store.set is only called for non-empty values)
    - For each stored key the handler prints a confirmation containing the key NAME only, never the value
    - The handler returns 0
    - The existing bare `shoppybot setup` credential wizard (handle_setup over SECRET_KEYS) still works unchanged
  </behavior>
  <action>
    In core/cli/setup.py add a module-level helper `_prompt_visible(prompt: str) -> str | None` modeled exactly on _prompt_backend (lines 28-35): print(prompt, end="", flush=True); read via sys.stdin.readline().strip() inside a try/except (EOFError, OSError) returning None on failure; return the value or None when empty. Add `def handle_setup_checkout_profile(args, svc) -> int`: import get_store from core.credentials and CHECKOUT_PROFILE_KEYS from core.checkout_profile at call time (matching the project's call-time import discipline for testability); get the store; print a brief header; loop CHECKOUT_PROFILE_KEYS, building a per-key prompt that marks CHECKOUT_ADDRESS_LINE2 as "(optional, Enter to skip)" and the others as "(Enter to skip)"; call _prompt_visible; when the returned value is not None, call store.set(key, val) and print a confirmation line containing the key NAME only (mirror handle_setup line 80 "  Stored: {key}" — never the value, T-09-04); track a stored count; after the loop print a completion line with the count and return 0. Do NOT prompt for, accept, or store any card number or CVV. In core/cli/__init__.py wire the chosen form: implement Option A by converting the setup parser to use add_subparsers(dest="setup_command") with a "checkout-profile" sub-parser whose set_defaults(func=handle_setup_checkout_profile), while preserving bare `setup` (no sub-action) routing to the existing handle_setup and preserving the existing setup --migrate behavior; import handle_setup_checkout_profile alongside handle_setup. If converting setup to a subparser group breaks existing setup/--migrate tests in a way that cannot be reconciled by keeping setup_p.set_defaults(func=handle_setup), fall back to Option B: add setup_p.add_argument("--checkout-profile", action="store_true", default=False, dest="checkout_profile", ...) and add a branch at the top of handle_setup that returns handle_setup_checkout_profile(args, svc) when getattr(args, "checkout_profile", False) is true. Record which option was chosen in the SUMMARY.
  </action>
  <verify>
    <automated>python -c "from core.cli import build_parser; from core.cli.setup import handle_setup_checkout_profile, _prompt_visible; p=build_parser(); ns,_=p.parse_known_args(['setup','checkout-profile']) if True else (None,None)" 2>&1 | python -c "import sys; sys.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - handle_setup_checkout_profile and _prompt_visible are importable from core.cli.setup
    - build_parser() routes the checkout-profile action (sub-action OR flag) to handle_setup_checkout_profile
    - The handler uses _prompt_visible (sys.stdin.readline), NOT getpass, for the 9 fields
    - Confirmation output prints key NAMES only; no value is printed
    - No card-number/CVV prompt or store.set exists in the handler
    - Existing setup tests (bare setup + --migrate) still pass
  </acceptance_criteria>
  <done>`setup checkout-profile` (or `setup --checkout-profile` fallback) prompts the 9 keys visibly, stores non-empty values via store.set, prints key-name-only confirmations, and leaves the existing setup wizard intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests for setup checkout-profile (stores keys, name-only output, optional skip)</name>
  <files>tests/test_setup_checkout_profile.py</files>
  <read_first>
    - tests/test_cli_setup.py (existing setup subcommand test patterns: parser parsing, monkeypatched stdin, store assertions)
    - tests/conftest.py (EnvVarBackend fake store; reset_credential_store; monkeypatch on core.credentials._store)
    - core/cli/setup.py (handle_setup_checkout_profile just added)
  </read_first>
  <behavior>
    - test_setup_checkout_profile_stores_keys: feeding 9 non-empty visible inputs results in 9 store.set calls keyed by CHECKOUT_PROFILE_KEYS; the handler returns 0
    - test_setup_output_is_key_name_only: captured stdout contains each stored key NAME and contains NONE of the entered values
    - test_setup_address_line2_optional: feeding an empty string for CHECKOUT_ADDRESS_LINE2 results in that key NOT being stored while the other 8 are stored
    - test_setup_stores_no_card_or_cvv: after the run, the store contains no key named with CVV/CARD/PAN substrings
  </behavior>
  <action>
    Create tests/test_setup_checkout_profile.py. Build a fake store using EnvVarBackend with monkeypatch.setattr on core.credentials._store (so get_store() returns it); isolate by monkeypatch.delenv over CHECKOUT_PROFILE_KEYS at test start. Drive visible input by monkeypatching sys.stdin.readline (or core.cli.setup.sys.stdin) to a side_effect iterator yielding the 9 inputs in CHECKOUT_PROFILE_KEYS order; use distinct sentinel values that are unlikely to collide (e.g. f"input_{key}"). Call handle_setup_checkout_profile(args, svc) with a simple args namespace and a minimal svc stand-in (the handler does not use svc beyond signature; pass a MagicMock). Capture stdout via capsys. Assert: all 9 keys present in the store with their fed values (round-trip via store.get); the handler returned 0; for the name-only test assert each key name appears in capsys output and assert that none of the fed input VALUES appears in the output; for the optional test feed "" for the CHECKOUT_ADDRESS_LINE2 slot and assert store.get("CHECKOUT_ADDRESS_LINE2") is None while the other 8 are set; for the card/cvv test assert no stored key (iterate the keys the handler set) contains "CVV", "CARD", or "PAN". Do not assert on getpass (the handler must not call it).
  </action>
  <verify>
    <automated>pytest tests/test_setup_checkout_profile.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_setup_checkout_profile.py -x` passes all four tests
    - Tests prove 9 keys stored, output is key-NAME-only (no values), address_line2 skippable, and no CVV/CARD/PAN key stored
    - Tests use EnvVarBackend + monkeypatched stdin (no getpass, no real backend)
  </acceptance_criteria>
  <done>Four tests pass proving the setup command stores the 9 keys with name-only output, supports skipping the optional field, and never stores card/CVV data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal stdin → CredentialStore | user-entered PII crosses into off-disk store |
| handler → stdout/logs | confirmation output must not echo entered values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Information Disclosure | handle_setup_checkout_profile stdout | mitigate | Print key NAME only on store (mirror handle_setup line 80); test asserts values absent from output |
| T-20-04 | Information Disclosure | accidental card/CVV capture | mitigate | Handler prompts only CHECKOUT_PROFILE_KEYS; test asserts no CVV/CARD/PAN key stored |
| T-20-SC | Tampering | pip installs | accept | No new packages; RESEARCH audit confirms zero installs |
</threat_model>

<verification>
- `pytest tests/test_setup_checkout_profile.py -x` green
- `pytest tests/test_cli_setup.py -x` still green (existing setup + --migrate unbroken)
- `pytest` full suite green
</verification>

<success_criteria>
- `setup checkout-profile` action wired and routed to handle_setup_checkout_profile
- Visible (echoed) prompts for the 9 keys; key-NAME-only confirmation output
- CHECKOUT_ADDRESS_LINE2 skippable; no card/CVV ever stored
- BUY-07 success criterion 1 covered
</success_criteria>

<output>
Create `.planning/phases/20-checkout-profile-form-fill/20-02-SUMMARY.md` when done
</output>
