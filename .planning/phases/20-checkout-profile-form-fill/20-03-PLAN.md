---
phase: 20-checkout-profile-form-fill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core/orchestrator.py
  - plugins/shopbot_plugin_amazon.py
  - core/cli/run.py
  - tests/test_cvv_threading.py
autonomous: true
requirements: [BUY-07]
must_haves:
  truths:
    - "AmazonPlugin instances have a self._cvv attribute initialized to None in __init__"
    - "When a runtime CVV is present, the orchestrator injects it onto the routed Amazon plugin (mirroring the existing BestBuy injection)"
    - "needs_cvv fires when an amazon.com item has auto_buy enabled (not only bestbuy.com)"
    - "The CVV value is runtime-only: never written to the store, config, or a log"
  artifacts:
    - path: "plugins/shopbot_plugin_amazon.py"
      provides: "self._cvv = None in AmazonPlugin.__init__"
      contains: "self._cvv = None"
    - path: "core/orchestrator.py"
      provides: "amz_plugin._cvv = cvv injection block"
      contains: "amz_plugin._cvv = cvv"
    - path: "core/cli/run.py"
      provides: "needs_cvv extended to amazon.com"
    - path: "tests/test_cvv_threading.py"
      provides: "amazon needs_cvv + amazon injection + _cvv-never-logged behavior"
  key_links:
    - from: "core/cli/run.py"
      to: "BotService.run(cvv)"
      via: "getpass-gated cvv passed through unchanged"
      pattern: "needs_cvv"
    - from: "core/orchestrator.py"
      to: "AmazonPlugin._cvv"
      via: "registry.route(amazon.com) attribute injection"
      pattern: "amz_plugin\\._cvv"
---

<objective>
Thread the runtime CVV to the Amazon plugin, mirroring the existing BestBuy path. Add `self._cvv = None` to AmazonPlugin.__init__, add an Amazon injection block in `orchestrator.async_main`, and extend the `run.py` needs_cvv predicate so the getpass prompt fires for amazon.com auto_buy items. The CVV stays runtime-only and is never persisted or logged.

Purpose: Without this, the Amazon CVV field fill in Plan 04 has no value source and the prompt never appears for Amazon-only configs. The path is fully traced in RESEARCH (Critical findings).
Output: edits to three files + a threading test. NOTE: this plan touches AmazonPlugin.__init__ ONLY; the auto_buy form-fill/CVV-entry body is Plan 04 (Wave 2) to avoid same-file parallel conflict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-checkout-profile-form-fill/20-CONTEXT.md
@.planning/phases/20-checkout-profile-form-fill/20-RESEARCH.md
@.planning/phases/20-checkout-profile-form-fill/20-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly; no exploration needed. -->

From core/orchestrator.py (async_main, lines 411-414) — the EXACT existing BestBuy block to mirror:
  if cvv:
      bb_plugin = registry.route("https://www.bestbuy.com/")
      if bb_plugin:
          bb_plugin._cvv = cvv
This sits after _staggered_setup(registry, items) (line 409) and before poll_interval setup (line 416). registry.route(url) returns the routed plugin instance or None.

From plugins/shopbot_plugin_amazon.py (__init__, lines 72-81): sets captcha_event, passkey_event, otp_event, test_pause_event, and self._captcha_solver = None. There is NO self._cvv (unlike BestBuy __init__ line 46 which sets self._cvv = None). super().__init__(config) is called first.

From core/cli/run.py (lines 29-36) — the EXACT needs_cvv predicate to extend:
  needs_cvv = (
      not cfg.debug.test_mode
      and not cfg.debug.monitor_only
      and any(
          "bestbuy.com" in item.link and item.auto_buy
          for item in cfg.available.items
      )
  )
cvv is collected via getpass.getpass only when needs_cvv, then svc.run(cvv) is called. cvv is a parameter only; never logged or stored (existing SEC-02 posture).

From plugins/shopbot_plugin_bestbuy.py __init__ line 46: `self._cvv = None  # injected by orchestrator ... Never logged` — the pattern to copy into AmazonPlugin.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Init Amazon _cvv, add orchestrator injection, extend needs_cvv</name>
  <files>plugins/shopbot_plugin_amazon.py, core/orchestrator.py, core/cli/run.py</files>
  <read_first>
    - plugins/shopbot_plugin_amazon.py (__init__ lines 72-81)
    - plugins/shopbot_plugin_bestbuy.py (__init__ lines 42-49; the _cvv = None line 46 to mirror)
    - core/orchestrator.py (async_main lines 408-416; the BestBuy _cvv block lines 411-414)
    - core/cli/run.py (needs_cvv lines 29-36)
    - .planning/phases/20-checkout-profile-form-fill/20-PATTERNS.md (sections "core/orchestrator.py - add amz_plugin._cvv injection", "core/cli/run.py - extend needs_cvv predicate", "shopbot_plugin_amazon.py - add _cvv init")
  </read_first>
  <behavior>
    - A freshly constructed AmazonPlugin has _cvv == None (attribute exists; no AttributeError when orchestrator assigns it)
    - With a non-None cvv and an Amazon plugin routable from registry, async_main sets amz_plugin._cvv to the cvv value
    - needs_cvv is True when an item with "amazon.com" in its link has auto_buy True (and test_mode/monitor_only are False), even if no bestbuy.com item exists
    - needs_cvv remains True for bestbuy.com auto_buy items (no regression) and False when test_mode or monitor_only is set
    - cvv is never passed to writeLog, store.set, or written to config in any of the three files
  </behavior>
  <action>
    In plugins/shopbot_plugin_amazon.py __init__, add `self._cvv = None` immediately after super().__init__(config) (before or alongside the existing event attributes), with a comment that it is injected by the orchestrator after setup and never logged — mirroring BestBuy __init__ line 46. In core/orchestrator.py async_main, add an Amazon injection block immediately after the existing BestBuy block (after line 414), at the same indentation: guard on `if cvv:`, route the Amazon plugin via registry.route("https://www.amazon.com/"), and when non-None set amz_plugin._cvv = cvv. Do not log the cvv. In core/cli/run.py extend the needs_cvv any() predicate so the per-item test is `("bestbuy.com" in item.link or "amazon.com" in item.link) and item.auto_buy`, leaving the test_mode/monitor_only guards unchanged. Do not introduce any writeLog/print of the cvv. Keep the getpass collection and svc.run(cvv) call as-is.
  </action>
  <verify>
    <automated>python -c "from plugins.shopbot_plugin_amazon import AmazonPlugin; p=AmazonPlugin(config=None); assert p._cvv is None"</automated>
  </verify>
  <acceptance_criteria>
    - AmazonPlugin(config=None)._cvv is None (attribute initialized)
    - orchestrator.async_main contains an `amz_plugin._cvv = cvv` block mirroring the BestBuy block, after it
    - run.py needs_cvv any() predicate includes `"amazon.com" in item.link`
    - No `_cvv`/`cvv` value appears in any writeLog/print/store.set across the three edited files (grep clean)
    - Existing BestBuy CVV behavior unchanged
  </acceptance_criteria>
  <done>AmazonPlugin initializes _cvv; orchestrator injects cvv onto the routed Amazon plugin; needs_cvv fires for amazon.com auto_buy items; cvv stays runtime-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CVV threading tests (amazon needs_cvv, amazon injection, never-logged)</name>
  <files>tests/test_cvv_threading.py</files>
  <read_first>
    - tests/test_bestbuy_plugin.py (_make_bestbuy_plugin no-browser construction pattern to mirror for Amazon)
    - tests/conftest.py (MagicMock/AsyncMock fixtures; how plugins are built without a browser)
    - core/cli/run.py (handle_run + needs_cvv; how cvv is collected and svc.run called)
    - core/orchestrator.py (async_main injection block)
  </read_first>
  <behavior>
    - test_amazon_cvv_attr_default: AmazonPlugin(config=None)._cvv is None
    - test_needs_cvv_true_for_amazon_item: with one amazon.com item auto_buy=True (test_mode=False, monitor_only=False), handle_run triggers the getpass path; with no auto_buy items it does not
    - test_needs_cvv_false_in_test_mode: amazon.com auto_buy item but test_mode=True → no getpass prompt
    - test_orchestrator_injects_amazon_cvv: a fake registry whose route(amazon url) returns a stub plugin, after the injection block, has stub._cvv == the cvv value
    - test_cvv_never_in_source_logs: the three checkout-threading files contain no writeLog/print whose argument text contains a cvv variable
  </behavior>
  <action>
    Create tests/test_cvv_threading.py. For the attribute test, build AmazonPlugin(config=None) and assert _cvv is None. For the needs_cvv tests, drive core.cli.run.handle_run with a fake svc whose get_config() returns a config object (use a lightweight namespace or the real AppConfig builder if simpler) carrying debug.test_mode/debug.monitor_only and available.items each with .link and .auto_buy; monkeypatch getpass.getpass (in the run module namespace) to a recorder and svc.run to a recorder, then assert getpass was/was not called per the matrix (amazon auto_buy True+test_mode False → called; test_mode True → not called; no auto_buy → not called). For the injection test, exercise the injection logic with a fake registry exposing route(url) -> stub (a MagicMock with a settable _cvv) for the amazon url; if directly unit-testing async_main is heavy, instead extract-and-test the injection by constructing the same registry stub and asserting that after the documented injection (route amazon + set _cvv) stub._cvv equals the cvv — prefer calling the real async_main with all heavy collaborators mocked (registry, write queue, dispatcher, run_plugin) only if that is already the established orchestrator-test pattern; otherwise assert against a focused helper. For the never-logged test, reuse the AST-walk approach (model on tests/test_no_env_secret_reads.py): parse core/orchestrator.py, core/cli/run.py, plugins/shopbot_plugin_amazon.py, and for every writeLog/print Call assert no argument's ast.unparse() contains the token "cvv" as a bare variable reference (allow the literal prompt string "Enter CVV (input hidden): " which is a constant, not a value — match on the variable name pattern, not the static prompt). Keep the never-logged scan additive to (not a replacement for) the dedicated CVV AST test in Plan 04.
  </action>
  <verify>
    <automated>pytest tests/test_cvv_threading.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cvv_threading.py -x` passes
    - Tests cover: Amazon _cvv default None, needs_cvv True for amazon auto_buy, needs_cvv False in test_mode, orchestrator amazon injection sets _cvv, and no cvv variable in source logs
    - getpass is monkeypatched (no real prompt); no real browser launched
  </acceptance_criteria>
  <done>Threading tests prove Amazon _cvv init, amazon-aware needs_cvv, orchestrator injection, and cvv-not-logged in the threading files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal getpass → in-memory cvv → plugin attribute | the live CVV crosses from stdin into process memory and onto the plugin; must never reach disk or logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | cvv in any writeLog/print | mitigate | No cvv passed to writeLog/print/store; AST never-logged scan over the threading files (criterion-3 precursor) |
| T-20-06 | Information Disclosure | cvv persisted | mitigate | cvv is a parameter only; never store.set; never written to config (existing SEC-02 posture preserved) |
| T-20-SC | Tampering | pip installs | accept | No new packages; RESEARCH audit confirms zero installs |
</threat_model>

<verification>
- `pytest tests/test_cvv_threading.py -x` green
- `pytest tests/test_orchestrator.py tests/test_cli_run.py -x` still green (no regression to BestBuy threading)
- `pytest` full suite green
</verification>

<success_criteria>
- AmazonPlugin._cvv initialized; orchestrator injects cvv for Amazon mirroring BestBuy
- needs_cvv fires for amazon.com auto_buy items; test_mode/monitor_only still short-circuit
- cvv never logged or persisted (proven by AST scan over the threading files)
- BUY-07 CVV-threading prerequisite covered (form-fill CVV entry in Plan 04)
</success_criteria>

<output>
Create `.planning/phases/20-checkout-profile-form-fill/20-03-SUMMARY.md` when done
</output>
