---
phase: 12-stability-foundation
plan: 04
type: execute
wave: 2
depends_on: ["12-03"]
files_modified:
  - docs/PLATFORMS.md
autonomous: false
requirements: [STAB-01]
must_haves:
  truths:
    - "All 4 deferred v2.0 manual checks are executed where the platform allows and documented pass/fail in docs/PLATFORMS.md"
    - "Ubuntu-only variants that cannot run on this Windows machine are marked 'pending Ubuntu access', not silently skipped"
    - "docs/PLATFORMS.md MC-1..MC-4 rows contain no bare `[ ]` cells — each cell is PASS, FAIL, N/A, or 'pending Ubuntu access'"
  artifacts:
    - path: "docs/PLATFORMS.md"
      provides: "Phase 8/9/10 deferred-check tables updated with pass/fail/pending results"
      contains: "pending Ubuntu"
  key_links:
    - from: "docs/PLATFORMS.md Phase 8/9/10 deferred-check tables"
      to: "MC-1 / MC-2 / MC-3 / MC-4 repro outcomes"
      via: "checkbox cells filled with PASS / FAIL / pending Ubuntu access"
      pattern: "PASS|FAIL|pending"
---

<objective>
Execute and document the 4 deferred v2.0 cross-OS/UI manual checks (STAB-01):

- MC-1: keyring secret survives process restart (Windows Credential Manager).
- MC-2: masked-TTY passphrase/credential prompt (Windows PowerShell, no echo).
- MC-3: web dashboard live render + Start/Stop + log polling (Windows browser).
- MC-4: `0.0.0.0` bind-warning banner live render (Windows browser) — the automated TestClient
  assertion already exists from Plan 03; this confirms the live render and records the test result.

This Windows dev machine can directly run the Windows variants. Ubuntu variants (headless
encrypted-file restart, Ubuntu TTY, Ubuntu browser render) require a second machine or WSL2; where
unavailable they must be documented "pending Ubuntu access", never silently skipped. Any check that
fails must be recorded as FAIL with a follow-up note (no code fix is expected for these live checks,
but a real failure must surface, not be papered over).

Purpose: Close the STAB-01 deferred-debt requirement with documented pass/fail evidence in
docs/PLATFORMS.md.
Output: Updated docs/PLATFORMS.md deferred-check tables with concrete results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stability-foundation/12-RESEARCH.md
@docs/PLATFORMS.md

<interfaces>
<!-- docs/PLATFORMS.md already contains the deferred-check tables and repro steps. -->
<!-- Phase 8 Deferred Live Checks: line ~131 (keyring restart) — repro at ~141 -->
<!-- Phase 9 Deferred Live Checks: line ~147 (masked TTY) — repro at ~153 -->
<!-- Phase 10 Deferred Live Checks: line ~163 (web dashboard) — incl. 0.0.0.0 banner row ~175, repro ~178 -->
<!-- Each table has per-platform columns with [ ] checkbox cells to fill. -->
<!-- Commands to run on this Windows machine: -->
<!--   shoppybot setup ; shoppybot config show ; shoppybot items list  (MC-1) -->
<!--   shoppybot setup  (MC-2, observe no echo) -->
<!--   shoppybot web    then browser http://localhost:8000  (MC-3) -->
<!--   shoppybot web --host 0.0.0.0  then browser http://localhost:8000  (MC-4) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute MC-1 / MC-2 / MC-3 / MC-4 manual checks on this machine</name>
  <read_first>
    - docs/PLATFORMS.md (Phase 8, 9, 10 Deferred Live Checks sections and their repro steps)
    - .planning/phases/12-stability-foundation/12-RESEARCH.md (MC-1..MC-4 sections, "Platform scope on this machine", and Pitfall 4 — TestClient does not substitute for the live MC-3 browser check)
  </read_first>
  <what-built>
    Plan 03 already added the automated MC-4 banner assertion. No further automation is possible for
    MC-1 (cross-process restart survival), MC-2 (real-TTY no-echo), or MC-3 (live browser render +
    Start/Stop + log polling) — these inherently require real hardware and a human observer.
  </what-built>
  <how-to-verify>
    Run each Windows-runnable check and note PASS or FAIL with a one-line observation. Use a fresh
    PowerShell terminal.

    MC-1 (keyring restart survival, Windows):
      1. `shoppybot setup` — complete credential entry with the keyring backend.
      2. Close the terminal/process entirely.
      3. In a NEW PowerShell, clear credential env vars (e.g. `Remove-Item Env:BB_EMAIL` etc. if set).
      4. `shoppybot config show` (or `shoppybot items list`) — confirm it does NOT prompt for credentials.
      Expected: secret retrieved from Windows Credential Manager without an env var. Record PASS/FAIL.

    MC-2 (masked TTY, Windows PowerShell):
      1. Fresh PowerShell -> `shoppybot setup`.
      2. At each credential prompt, type a value and confirm NO characters are echoed.
      3. At the confirmation prompt, confirm only the key name (not the entered value) is shown.
      Expected: no echo on entry; value never displayed. Record PASS/FAIL.

    MC-3 (web dashboard live render, Windows):
      1. `shoppybot web` — starts without error.
      2. Open http://localhost:8000 in a browser.
      3. Confirm Controls (Start/Stop buttons), Items, Credentials, Config sections are visible.
      4. Click Start Bot / Stop Bot — confirm the status dot/label changes live.
      5. In a separate terminal run `shoppybot run` — confirm the log panel updates without a page reload.
      Expected: live render + live Start/Stop + live log polling. Record PASS/FAIL.

    MC-4 (0.0.0.0 banner live render, Windows):
      1. `shoppybot web --host 0.0.0.0`.
      2. Open http://localhost:8000 — confirm the "reachable beyond localhost" warning banner is visible.
      Expected: banner visible. (The automated TestClient assertion from Plan 03 already passes.)
      Record PASS/FAIL.

    For each check, also note whether the Ubuntu variant was run (only if WSL2 / an Ubuntu host is
    available). If not available, this task's result for the Ubuntu column is "pending Ubuntu access".
  </how-to-verify>
  <resume-signal>
    Reply with the four results, e.g.: "MC-1 PASS, MC-2 PASS, MC-3 PASS, MC-4 PASS; Ubuntu variants
    pending (no WSL2/Ubuntu host)." If any check FAILED, describe the failure so it can be recorded
    and a follow-up logged.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Record MC-1..MC-4 results in docs/PLATFORMS.md</name>
  <read_first>
    - docs/PLATFORMS.md (the file being modified — read the Phase 8/9/10 Deferred Live Checks tables in full so the correct checkbox cells are updated)
  </read_first>
  <what-built>
    The agent transcribes the human-reported MC-1..MC-4 results from Task 1 into the Phase 8/9/10
    Deferred Live Checks tables, replacing every `[ ]` placeholder with PASS / FAIL / N/A / "pending
    Ubuntu access". Because the recorded values are the human's manual-check observations (not a
    machine-verifiable assertion), a human must confirm the transcription faithfully reflects the
    Task 1 results — this is documentation of a manual check, so it is human-gated rather than `auto`.
  </what-built>
  <action>
    Transcribe the human-reported results from Task 1 into the Phase 8, Phase 9, and Phase 10
    Deferred Live Checks tables. For each check row and platform column, replace the `[ ]` placeholder
    with an explicit result: `PASS`, `FAIL` (with a brief parenthetical note), or `pending Ubuntu
    access` for Ubuntu-variant cells that could not be run on this Windows machine. For MC-4, note in
    the row (or an adjacent line) that the banner render is additionally CI-asserted by
    `tests/test_web_dashboard.py` (Plan 03). Add a short dated note under each updated section
    indicating these checks were executed in Phase 12 on the Windows dev machine. Do NOT mark any
    Ubuntu variant as PASS unless it was actually run. Do NOT silently leave `[ ]` cells — every cell
    must be PASS, FAIL, N/A (where the table already marks a column N/A), or "pending Ubuntu access".
    If any check FAILED, ensure the FAIL is recorded verbatim (do not soften it to PASS); a real code
    failure would be handled in a follow-up gap plan, not silently.
  </action>
  <how-to-verify>
    1. Open docs/PLATFORMS.md and inspect the Phase 8/9/10 Deferred Live Checks tables.
    2. Confirm every MC-1..MC-4 row/column cell shows PASS, FAIL, N/A, or "pending Ubuntu access" — no
       bare `[ ]` remains.
    3. Confirm each recorded value matches exactly what Task 1's checkpoint reported (no FAIL upgraded
       to PASS, no Ubuntu cell marked PASS unless actually run).
    Grep aid: `rtk grep -c "pending Ubuntu" docs/PLATFORMS.md` > 0 when any Ubuntu variant was not run.
  </how-to-verify>
  <resume-signal>Type "approved" if the table faithfully records the Task 1 results, or describe corrections.</resume-signal>
  <acceptance_criteria>
    - Source assertion: docs/PLATFORMS.md Phase 8/9/10 Deferred Live Checks rows for MC-1..MC-4 contain no `[ ]` placeholders (every cell shows PASS/FAIL/N/A/pending, not bare `[ ]`).
    - Source assertion: at least one cell contains "pending Ubuntu access" if any Ubuntu variant was not run (grep: `rtk grep -c "pending Ubuntu" docs/PLATFORMS.md` > 0 when applicable).
    - Behavior assertion: the recorded results match exactly what Task 1's human checkpoint reported (no upgrading a FAIL to PASS).
  </acceptance_criteria>
  <done>All 4 deferred manual checks are documented pass/fail/pending in docs/PLATFORMS.md, with Ubuntu-only variants explicitly marked pending and any failures recorded verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyring / OS credential store (MC-1) | Verifies secrets persist in the OS store across restart; documentation-only, no code change |
| 0.0.0.0 network bind (MC-4) | Confirms the non-local exposure warning renders to the operator at the network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | 0.0.0.0 dashboard exposure (MC-4 live) | mitigate | Manual confirmation that the operator-facing non-local warning banner renders; reinforces the existing control. No new exposure. |
| T-12-06 | Spoofing / Repudiation | keyring restart survival (MC-1) | accept | Verifies existing keyring backend behavior; documentation-only. No new credential surface. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs; documentation-only plan. No legitimacy gate required. |

Documentation-and-manual-verification plan. No production code changes. The MC-4 check reinforces
an existing security warning. Threats: none above the already-mitigated non-local exposure.
</threat_model>

<verification>
- Human checkpoint (Task 1) returns explicit PASS/FAIL for MC-1, MC-2, MC-3, MC-4 on Windows.
- docs/PLATFORMS.md Phase 8/9/10 deferred-check tables contain no remaining `[ ]` cells for the
  MC-1..MC-4 rows — every cell is PASS / FAIL / N/A / "pending Ubuntu access".
- `pytest --tb=short -q` still exits 0 (no code changed in this plan; suite baseline unaffected).
</verification>

<success_criteria>
STAB-01 satisfied: all 4 deferred manual checks executed where the platform allows and documented
pass/fail in docs/PLATFORMS.md; Ubuntu-only variants explicitly marked "pending Ubuntu access"; any
failure recorded verbatim with a follow-up note rather than masked.
</success_criteria>

<output>
Create `.planning/phases/12-stability-foundation/12-04-SUMMARY.md` when done
</output>
</content>
</invoke>
