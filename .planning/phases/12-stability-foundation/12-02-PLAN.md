---
phase: 12-stability-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_no_env_secret_reads.py
  - tests/test_paths.py
autonomous: true
requirements: [STAB-02]
must_haves:
  truths:
    - "The SC1 secret-read guard recurses into core/cli/*.py (no coverage gap)"
    - "The hardcoded-separator guard scans an absolute, CWD-independent file list that is never silently empty"
  artifacts:
    - path: "tests/test_no_env_secret_reads.py"
      provides: "SC1 guard with rglob coverage of core/ subdirectories"
      contains: "rglob"
    - path: "tests/test_paths.py"
      provides: "separator guard anchored to __file__ with a non-empty assertion"
      contains: "Path(__file__).parent.parent"
  key_links:
    - from: "tests/test_no_env_secret_reads.py"
      to: "core/cli/*.py"
      via: "rglob recursive scan"
      pattern: "rglob"
    - from: "tests/test_paths.py"
      to: "core/ source tree"
      via: "__file__-anchored absolute path + len(src_files) > 0 assertion"
      pattern: "len\\(src_files\\)"
---

<objective>
Resolve audit tech-debt items TD-2 and TD-3, both test-only guard hardening fixes:

- TD-2: the SC1 secret-read guard in `test_no_env_secret_reads.py` uses `glob("*.py")`, which
  does not recurse, so `core/cli/*.py` (5 files) are never scanned. Switch to `rglob` and add a
  companion assertion proving a `core/cli/` file is now in the scanned set.
- TD-3: the separator guard in `test_paths.py` uses CWD-relative `Path("core")`, which yields an
  empty file list when pytest runs from a subdirectory (silent false-pass). Anchor to
  `Path(__file__).parent.parent / "core"` and assert the list is never empty.

Purpose: Both guards currently have silent coverage holes. These fixes turn potential silent
false-passes into reliable guards.
Output: Two hardened guard tests; no production code change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stability-foundation/12-RESEARCH.md

<interfaces>
<!-- Current guard source-of-truth. Executor uses these directly. -->

From tests/test_no_env_secret_reads.py:38-45 (TD-2 — the non-recursive scan):
```python
dirs_to_scan = [repo_root / "plugins", repo_root / "notifications", repo_root / "core"]
for scan_dir in dirs_to_scan:
    for py_file in sorted(scan_dir.glob("*.py")):   # glob = non-recursive; misses core/cli/*.py
```

From tests/test_paths.py:56-69 (TD-3 — the CWD-relative scan):
```python
def test_no_hardcoded_separators():
    src_files = list(Path("core").rglob("*.py")) + [   # Path("core") is CWD-relative
        Path("logger.py"), Path("models.py"), Path("utils.py"),
    ]
    ...
    assert violations == []
```

core/cli/ contents (must be covered by TD-2 fix): __init__.py, config_cmd.py, items.py, run.py, setup.py, web.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden SC1 secret-read guard to recurse into core/cli (TD-2)</name>
  <files>tests/test_no_env_secret_reads.py</files>
  <read_first>
    - tests/test_no_env_secret_reads.py (the file being modified — read it in full, especially the scan loop at lines 38-70 and the credentials.py exemption at line 50)
    - .planning/phases/12-stability-foundation/12-RESEARCH.md (TD-2 section and Pitfall 2 — rglob does not match .pyc, no __pycache__ risk)
  </read_first>
  <behavior>
    - After the fix, the set of scanned files includes core/cli/config_cmd.py (proving recursion).
    - The guard still reports zero violations against the current clean codebase (no false positive introduced by the wider scan).
    - The core/credentials.py exemption still applies (it must remain excluded from violation checks).
  </behavior>
  <action>
    Change the `core/` directory scan from `glob("*.py")` to `rglob("*.py")`. Per RESEARCH, switching
    all three dirs to rglob is safe and future-proof, but the minimum required fix is the `core/` entry;
    apply rglob to the core scan at least. Keep the existing credentials.py exemption logic and the
    comment-line skip intact. Add a companion assertion: collect the scanned file set (relative paths)
    and assert that `core/cli/config_cmd.py` (or the equivalent core/cli file) is present in it — this
    is the verifiable proof the coverage gap is closed. Do NOT relax or remove any existing violation
    check. No production code changes.
  </action>
  <verify>
    <automated>pytest -q tests/test_no_env_secret_reads.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/test_no_env_secret_reads.py` uses `rglob("*.py")` for the core scan (grep, comment lines excluded: `rtk grep -v '^#' ... | rtk grep -c rglob` > 0).
    - Behavior assertion: the new companion assertion confirms a `core/cli/` path is in the scanned set; the test fails if reverted to non-recursive `glob`.
    - Test command: `pytest -q tests/test_no_env_secret_reads.py` exits 0.
  </acceptance_criteria>
  <done>SC1 guard recurses into core/cli; a companion assertion proves the coverage; no false positive on the clean tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anchor separator guard to __file__ and forbid silent empty scan (TD-3)</name>
  <files>tests/test_paths.py</files>
  <read_first>
    - tests/test_paths.py (the file being modified — read test_no_hardcoded_separators at lines 56-69 in full)
    - .planning/phases/12-stability-foundation/12-RESEARCH.md (TD-3 section and Pitfall 3 — silent empty list)
  </read_first>
  <behavior>
    - src_files resolves to the same repo-root core tree regardless of the process CWD.
    - The guard asserts len(src_files) > 0 (turns a silent false-pass into a loud failure).
    - A known file (e.g. core/paths.py) is present in the scanned list.
    - The guard still reports zero hardcoded-separator violations on the current tree.
  </behavior>
  <action>
    Replace `Path("core")` with `Path(__file__).parent.parent / "core"`, and similarly anchor the
    three top-level files (`logger.py`, `models.py`, `utils.py`) to `Path(__file__).parent.parent /
    "<name>"` so the entire scan list is repo-root-absolute and CWD-independent. Add, as the FIRST
    assertion in the test, `assert len(src_files) > 0, "separator guard scanned zero files"` (Pitfall 3).
    Optionally also assert that the `core/paths.py` path is in src_files. Keep the existing
    `bad_patterns` list and the final `violations == []` assertion unchanged. No production code changes.
  </action>
  <verify>
    <automated>pytest -q tests/test_paths.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/test_paths.py` constructs src_files via `Path(__file__).parent.parent` and contains `len(src_files) > 0`.
    - Behavior assertion: the guard would FAIL (not silently pass) if the anchor were wrong and yielded an empty list — the len assertion enforces this.
    - Test command: `pytest -q tests/test_paths.py` exits 0.
  </acceptance_criteria>
  <done>Separator guard is CWD-independent, asserts a non-empty scan list, and still passes on the clean tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only guard changes; no runtime, no input, no network boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | SC1 / separator guard tests | accept | These changes STRENGTHEN existing security guards (wider secret-read coverage, no silent false-pass). No production surface changes. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure test-file edits. No legitimacy gate required. |

Test-only hardening of existing guards. No new production code, input, or dependency.
Threats: none above informational.
</threat_model>

<verification>
- `pytest -q tests/test_no_env_secret_reads.py` exits 0 (recursion + companion assertion).
- `pytest -q tests/test_paths.py` exits 0 (anchored + non-empty assertion).
- `pytest --tb=short -q` exits 0: 354 passed, 2 skipped (no new test files added here; assertions added to existing tests).
</verification>

<success_criteria>
TD-2 and TD-3 resolved: the SC1 guard recursively covers core/cli/*.py with a proof assertion, and
the separator guard is CWD-independent with a non-empty-scan guard — both turning silent coverage
holes into reliable guards, with no production code change and no suite regression.
</success_criteria>

<output>
Create `.planning/phases/12-stability-foundation/12-02-SUMMARY.md` when done
</output>
