---
phase: 24-health-surface-server-safety
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - core/service.py
  - tests/test_service.py
  - tests/test_web_controls.py
autonomous: true
requirements: [REL-07]
must_haves:
  truths:
    - "get_status() returns {running, uptime_secs, plugins:{...}} with the locked shape"
    - "get_status() is safe to call before start() and returns empty plugins dict"
    - "get_status() is cheap and non-blocking (in-memory reads only)"
    - "BotService owns a single HealthRegistry, created in __init__, passed to async_main"
  artifacts:
    - path: "core/service.py"
      provides: "Expanded get_status(), _start_time, _health_registry, async_main wiring"
      contains: "_health_registry"
    - path: "tests/test_service.py"
      provides: "get_status shape tests before/after registry population"
    - path: "tests/test_web_controls.py"
      provides: "/status endpoint richer-shape + JSON-serializable test"
  key_links:
    - from: "core/service.py"
      to: "core.health.HealthRegistry"
      via: "self._health_registry = HealthRegistry() in __init__"
      pattern: "HealthRegistry\\(\\)"
    - from: "core/service.py get_status()"
      to: "self._health_registry.get_snapshot()"
      via: "plugins key"
      pattern: "get_snapshot\\(\\)"
---

<objective>
Expand `BotService.get_status()` to the locked health-surface shape, add a
`_start_time` for `uptime_secs`, hold the single `HealthRegistry` instance, and
pass it into `async_main` via a keyword arg (REL-07). The existing FastAPI
`/status` endpoint is UNCHANGED — the richer dict flows through; this plan adds a
JSON-serializable assertion on it.

Purpose: BotService is the stable API seam both the CLI (`shoppybot status`) and
the web `/status` endpoint read through.
Output: edited `core/service.py`, get_status shape tests, /status JSON-shape test.

Depends on Plan 01 (HealthRegistry). Touches core/service.py only on the source
side — disjoint from orchestrator.py (Plan 04) and cli (Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-health-surface-server-safety/24-RESEARCH.md
@.planning/phases/24-health-surface-server-safety/24-PATTERNS.md

@core/service.py

<interfaces>
<!-- From core/health.py (Plan 01): -->
<!--   HealthRegistry().get_snapshot() -> dict[str, dict] (JSON-safe, no private keys) -->
<!-- async_main currently: async def async_main(cfg, cvv) -> None  (orchestrator.py L629) -->
<!-- Plan 04 changes async_main signature to accept health_registry=None (keyword, default None). -->
<!-- This plan passes health_registry=self._health_registry at both call sites in service.py. -->
<!-- Locked get_status() target shape: -->
<!--   {"running": bool, "uptime_secs": float, -->
<!--    "plugins": {name: {"status": str, "last_heartbeat": float, -->
<!--      "consecutive_errors": int, "items_checked": int, "orders_confirmed": int}}} -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand get_status() + add _start_time + hold HealthRegistry; wire async_main calls</name>
  <files>core/service.py</files>
  <read_first>
    - core/service.py lines 36-70 (__init__, get_status current shape)
    - core/service.py lines 132-203 (start() L142 sets _running=True; _run_loop._main() L150-164 finally block; run() L197-203 blocking path)
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (section `core/service.py` — exact __init__/start/run/get_status/finally edit pattern)
    - .planning/phases/24-health-surface-server-safety/24-RESEARCH.md (Pattern 6 + Pitfall 4 + Pitfall 5 — keyword default None, registry created in __init__ so get_status is safe before start)
  </read_first>
  <behavior>
    - get_status() before start() returns {"running": False, "uptime_secs": 0.0, "plugins": {}} (registry exists but empty; Pitfall 5).
    - After _health_registry.heartbeat("FakePlugin"), get_status()["plugins"]["FakePlugin"] exists with the five public keys.
    - uptime_secs is 0.0 unless _start_time is not None AND _running is True; otherwise it equals time.monotonic() - _start_time.
    - The returned dict is JSON-serializable (only bool/float/int/str/dict).
    - async_main is called with health_registry=self._health_registry at both the start() daemon-thread path and the run() blocking path.
  </behavior>
  <action>
    Edit core/service.py. In __init__ (after self._task on L41): add self._start_time: Optional[float] = None, and create the single registry instance — import HealthRegistry from core.health at module top (alongside the other core imports near L18-23) and set self._health_registry = HealthRegistry(). Add `import time` at module top if not already present. In start()._run_loop._main() (L154), change the call to `await async_main(self._cfg, cvv, health_registry=self._health_registry)`; set self._start_time = time.monotonic() at the same point _running is set True (L142, before the thread launches); in the finally block (L162-164) add self._start_time = None alongside self._running = False. In run() (L197-203 blocking path), set self._start_time = time.monotonic() before asyncio.run, change the call to `asyncio.run(async_main(self._cfg, cvv, health_registry=self._health_registry))`, and set self._start_time = None after it returns. Replace get_status() (L68-70) to return the locked shape: compute uptime = time.monotonic() - self._start_time only when self._start_time is not None and self._running, else 0.0; return {"running": self._running, "uptime_secs": uptime, "plugins": self._health_registry.get_snapshot()}. Keep get_status under 30 lines and free of any I/O (cheap/non-blocking — it is called from the FastAPI request handler). Do NOT change the /status endpoint in web/routes/api.py (richer dict flows through unchanged).
  </action>
  <verify>
    <automated>pytest tests/test_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - get_status() returns the locked 3-key shape; plugins is a dict.
    - self._health_registry is created in __init__ (get_status safe before start).
    - Both async_main call sites pass health_registry=self._health_registry.
  </acceptance_criteria>
  <done>get_status() returns the locked health-surface shape; uptime + registry wired; async_main receives the registry at both call paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_status shape tests + /status JSON-serializable test</name>
  <files>tests/test_service.py, tests/test_web_controls.py</files>
  <read_first>
    - tests/test_service.py (existing test_get_status_before_start_reports_not_running — fixture and style)
    - tests/test_web_controls.py (existing test_get_status_returns_running_bool ~L45-51 — mock_svc + client pattern, /api/status route)
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (sections `tests/test_service.py (EDIT)` and `tests/test_web_controls.py (EDIT)`)
  </read_first>
  <behavior>
    - test_get_status_shape_before_start: status has keys running, uptime_secs, plugins; plugins is an empty dict; uptime_secs == 0.0.
    - test_get_status_shape_with_registry: after service._health_registry.heartbeat("FakePlugin"), "FakePlugin" appears in status["plugins"] with the five public keys.
    - test_get_status_is_json_serializable: json.dumps(service.get_status()) does not raise (covers the web /status flow-through, REL-07).
    - test_status_endpoint_richer_shape (web): GET /api/status with a mock_svc returning the richer dict yields 200 and resp.json() contains uptime_secs and plugins keys.
  </behavior>
  <action>
    Add the get_status shape tests to tests/test_service.py mirroring the existing get_status test fixture. Add the richer /status shape + serializability assertion to tests/test_web_controls.py mirroring its existing /api/status test: set mock_svc.get_status.return_value to a full health dict (running True, uptime_secs float, one plugin record with the five keys), GET /api/status, assert 200 and that uptime_secs and plugins are present in resp.json(). In test_service.py add test_get_status_is_json_serializable using json.dumps on the real BotService.get_status() after populating one plugin via service._health_registry. Do not introduce watch-mode flags.
  </action>
  <verify>
    <automated>pytest tests/test_service.py tests/test_web_controls.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All four behaviors covered and green.
    - The /status endpoint test proves the richer dict serializes to JSON via FastAPI (endpoint unchanged).
  </acceptance_criteria>
  <done>get_status shape and JSON-serializability verified for both CLI-read path and web /status path; endpoint code unchanged.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_service.py tests/test_web_controls.py -x` green.
- `python -c "import json; from core.service import BotService; print(json.dumps(BotService().get_status()))"` prints `{"running": false, "uptime_secs": 0.0, "plugins": {}}`.
</verification>

<success_criteria>
get_status() returns the locked structured shape; is cheap/non-blocking and JSON-serializable; BotService owns a single HealthRegistry wired into async_main; /status endpoint unchanged (REL-07 success criterion 1).
</success_criteria>

<output>
Create `.planning/phases/24-health-surface-server-safety/24-03-SUMMARY.md` when done.
</output>
