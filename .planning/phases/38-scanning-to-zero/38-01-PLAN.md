---
phase: 38-scanning-to-zero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md
autonomous: true
requirements: [SCAN-01, SCAN-06]
must_haves:
  truths:
    - "A recorded, evidence-backed verdict exists saying whether deleting codeql-analysis.yml can break the CodeQL required status check"
    - "A recorded, evidence-backed verdict exists saying whether the cryptography pin should be upgraded, dismissed, or accepted, and whether master already satisfies it"
    - "Every open alert across all three scanners is inventoried with its number, rule, severity, and location before any change is made"
  artifacts:
    - path: ".planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md"
      provides: "Pre-change alert inventory plus the SCAN-06 and SCAN-01 verdicts"
      contains: "VERDICT-SCAN-06"
      min_lines: 60
  key_links:
    - from: "38-SCAN-BASELINE.md VERDICT-SCAN-06"
      to: "38-03-PLAN.md Task 1 deletion gate"
      via: "executor reads the verdict before deleting the workflow file"
      pattern: "VERDICT-SCAN-06"
    - from: "38-SCAN-BASELINE.md VERDICT-SCAN-01"
      to: "38-02-PLAN.md Task 2 version choice"
      via: "executor reads the verdict before editing requirements.txt"
      pattern: "VERDICT-SCAN-01: "
---

<objective>
Establish the factual baseline this phase is measured against, and settle the two questions that
cannot be answered by reading a diff: what actually produces the `CodeQL` required status check,
and what the correct disposition is for the last Dependabot alert, `#13`, which as of 2026-09-08
already reads `state: fixed`.

Purpose: SCAN-06 deletes a workflow file while `CodeQL` is a required status check. If that
workflow is what reports `CodeQL`, deleting it creates a required check that can never report and
permanently blocks every future merge, including this phase's own pull request. That failure is
self-inflicted and expensive to undo. Nothing in this phase deletes anything until this plan says
it is safe.

Output: `.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md`, committed on a fresh working
branch cut from `origin/master`.

This plan makes ZERO remote mutations. Every GitHub API call in it is a GET: no `-X PATCH`,
`-X POST`, `-X PUT` or `-X DELETE` appears anywhere. It does write locally, and those writes are
its deliverable, not a side effect: it creates the `phase-38-scanning-to-zero` branch, restores the
carry-over paths onto it, writes `38-SCAN-BASELINE.md`, and lands two commits, and its
`git fetch --prune --tags` updates local remote-tracking refs. Nothing is pushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-scanning-to-zero/38-CONTEXT.md
@.github/workflows/codeql-analysis.yml
@requirements.txt

**Tooling notes for every task in this phase:**

1. All GitHub API calls are written as plain `gh api`. The rtk hook may rewrite them. If any
   response looks summarised or truncated, re-run with `rtk proxy gh api ...` and use the raw
   JSON. Never accept a filtered body as evidence for an acceptance criterion.
2. Platform is Windows. `rg` and `grep -oP` are unavailable. POSIX BRE only. Use `python -c` for
   JSON work that `--jq` makes awkward.
3. `rtk git log` drops merge commits and caps at 50 entries. Use `rtk proxy git log` where
   completeness matters. `git rev-list --count` and `git merge-base --is-ancestor` are accurate.
4. The repo owner/name is `thezoid/ShopPyBot`. Write it literally. The `:owner/:repo` placeholders
   only expand when the shell cwd is inside the git repo, and they fail silently otherwise.

**Measured state at planning time (2026-08-03), for comparison not for trust:**

| Fact | Value |
|------|-------|
| master HEAD | `bd70601f3fc79f5a26cf0e98d96b615f9c3320bd` |
| Open code scanning alerts | 31 |
| Open Dependabot alerts | 1 (`#13`, cryptography) |
| Open secret scanning alerts | 0 |
| Required contexts on master | `CodeQL`, `test (windows-latest)`, `test (ubuntu-latest)` |
| `.github/workflows/codeql-analysis.yml` workflow id | `8840986`, state `disabled_manually` |
| Default setup workflow id | `325304050`, path `dynamic/github-code-scanning/codeql` |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut the working branch and inventory every open alert</name>

  <read_first>
    - `.planning/phases/38-scanning-to-zero/38-CONTEXT.md` (the measured-state table in `<decisions>`)
    - `.planning/REQUIREMENTS.md` lines 60 through 74 (the SCAN-01..11 requirement text)
  </read_first>

  <files>.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md</files>

  <action>
The local working tree is on a planning branch (`chore/v4.0-milestone-close` at planning time) at
a commit that predates Phase 36 and Phase 37. Every later plan in this phase assumes a tree that
matches `origin/master`. Fix that first, without losing this phase's own plan files, which were
committed on the planning branch and do not exist on `origin/master`.

```
# GATE. Submit this ENTIRE fenced block as ONE shell invocation. The gate, the branch cut and the
# restore have to be in the same parsed unit: a guard that exits in an earlier invocation cannot
# stop a checkout submitted in a later one, and shell variables do not survive between calls.
set -e

# One source of truth for what the restore below touches. The gate MUST cover every one of these
# paths, not the phase directory alone: `git checkout "$PLANSHA" -- <path>` overwrites the
# working-tree copy of each of them, and uncommitted work in ANY of them is destroyed with no
# stash, no dangling blob and no error to notice.
set -- .planning/phases/38-scanning-to-zero/ \
       .planning/ROADMAP.md \
       .planning/HANDOFF.json \
       .planning/phases/37-distributable-artifact/37-VERIFICATION.md

# PLANSHA is the CURRENT commit. Any edit that is not yet committed is not in PLANSHA, so the
# restore would replace the working-tree files with the older committed content and silently
# discard the revisions.
DIRTY=$(git status --porcelain -- "$@")
if [ -n "$DIRTY" ]; then
  echo "ABORT: uncommitted changes in a carry-over path"
  echo "$DIRTY"
  echo "Commit every phase 38 revision on the source branch BEFORE cutting the working branch."
  echo "Otherwise the checkout of \$PLANSHA restores the stale pre-remediation content."
  exit 1
fi

PLANSHA=$(git rev-parse HEAD)
git fetch origin --prune --tags
if ! git checkout -B phase-38-scanning-to-zero origin/master; then
  echo "ABORT: git checkout -B failed. Still on the source branch; nothing was restored."
  exit 1
fi
# Assert the VALUE, not just an exit status: the restore is only safe on the new branch.
BRANCH=$(git rev-parse --abbrev-ref HEAD)
if [ "$BRANCH" != "phase-38-scanning-to-zero" ]; then
  echo "ABORT: expected branch phase-38-scanning-to-zero, on '$BRANCH'. Nothing was restored."
  exit 1
fi

git checkout "$PLANSHA" -- "$@"
git add "$@"
# 38-RUNBOOK.md is named explicitly, not swept in by the directory add: it is the operator
# run-book for this phase and belongs on master alongside the plans it narrates.
git add .planning/phases/38-scanning-to-zero/38-RUNBOOK.md
git reset .planning/phases/38-scanning-to-zero/.continue-here.md
git commit -m "docs(38-01): carry phase 38 plans and planning state onto the working branch"
```

The gate is not advisory, and it deliberately covers all four carry-over paths rather than the
phase directory alone. At planning time the phase directory held five modified plan files and an
untracked `38-RUNBOOK.md`. Running the carry-over from that state reverts three remediation passes
with no error and no diff to notice. A dirty `.planning/ROADMAP.md`, `.planning/HANDOFF.json` or
`37-VERIFICATION.md` is destroyed exactly the same way and is NOT recoverable: that content was
never staged, so there is no stash entry and no dangling blob to recover it from. If the gate
fires, stop and commit on the source branch first.

`set -e` is load-bearing, not decoration. Without it a failed `git checkout -B` leaves the shell on
the source branch and the very next line restores `$PLANSHA` over the source branch's own working
tree. It also turns `git commit`'s "nothing to commit" into a hard stop instead of a silent one,
which is the only signal that the carry-over produced no content.

The `git reset` line is required. `.continue-here.md` is git-tracked inside the phase directory and
carries `status: paused` / `task: 0`; staging the directory wholesale would commit that stale
handoff, and 38-04's merge would carry it onto master before 38-06 removes it. Unstaging leaves the
file in the working tree for local use while keeping it out of every commit on this branch.

Capture `PLANSHA` BEFORE the checkout. After `git checkout -B`, `HEAD` is `origin/master` and the
plan files are gone from the tree, so there is nothing left to read the SHA from. Using the SHA
rather than the branch name keeps this correct whatever branch the orchestrator planned on.

The carry-over is more than the phase directory. Three further paths exist only on the planning
branch and are lost by `checkout -B` if they are not named: `.planning/ROADMAP.md`, which carries
Phase 38's plan list and all eight planning corrections; `.planning/HANDOFF.json`; and
`.planning/phases/37-distributable-artifact/37-VERIFICATION.md`. Confirm the set before checking
out, from the planning branch, with `git diff --name-status origin/master..HEAD`. At planning time
that returns exactly those three plus the phase directory. If it returns anything more, add that
path to the `set --` list above rather than dropping it silently or restoring it separately: the
gate and the restore read the same list, and a path restored outside that list is un-gated.

Then confirm the tree is master plus exactly that one documentation commit:
```
git rev-list --count HEAD..origin/master
git log --oneline origin/master..HEAD
ls .planning/phases/38-scanning-to-zero/
```
The first must print `0`, meaning nothing on master is missing here. The second must show exactly
one commit. The third must list all six `38-0N-PLAN.md` files plus `38-CONTEXT.md` and
`38-RUNBOOK.md`. It also shows `.continue-here.md`, which is correct: the `git reset` above keeps
that file out of every commit on this branch, not out of the working tree. If any plan file is
missing, stop and report rather than continuing on a tree that cannot execute the rest of the
phase.

Now capture the inventory. Run each of these and keep the raw output:

Code scanning, all open alerts:
`gh api "repos/thezoid/ShopPyBot/code-scanning/alerts?state=open&per_page=100" --jq '[.[] | {n: .number, rule: .rule.id, sev: .rule.security_severity_level, path: .most_recent_instance.location.path, line: .most_recent_instance.location.start_line}] | sort_by(.rule, .path, .line) | .[] | "\(.n) \(.rule) \(.sev) \(.path):\(.line)"'`

Dependabot, all open alerts:
`gh api "repos/thezoid/ShopPyBot/dependabot/alerts?state=open&per_page=100" --jq '.[] | "#\(.number) \(.security_advisory.severity) \(.dependency.package.name) \(.dependency.manifest_path) range=\(.security_vulnerability.vulnerable_version_range) patched=\(.security_vulnerability.first_patched_version.identifier // "NONE") \(.security_advisory.ghsa_id)"'`

Secret scanning, all open alerts:
`gh api "repos/thezoid/ShopPyBot/secret-scanning/alerts?state=open&per_page=100" --jq 'length'`

Repository security settings:
`gh api "repos/thezoid/ShopPyBot" --jq '.security_and_analysis'`

Branch protection required checks:
`gh api "repos/thezoid/ShopPyBot/branches/master/protection/required_status_checks"`

Create `.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md` with a `## Alert Inventory`
section holding the code scanning alerts grouped by rule id, and a `## Requirement Text
Corrections` table reconciling what SCAN-01 through SCAN-05 claim against what is measured. Do not
edit `.planning/REQUIREMENTS.md` in this task. The baseline document is where corrections are
recorded; the requirements file keeps its original wording so the drift stays visible.

Corrections known at planning time, to be confirmed rather than assumed:

| Requirement claim | Measured | Note |
|---|---|---|
| SCAN-01: 7 open Dependabot alerts | 1 at planning time, `0` as of 2026-09-08 | Six closed as a side effect of Phase 37's dependency work. The last one, `#13`, went `state: fixed` on 2026-09-07 when PR `#26` merged, so the live open count is now `0`. Confirm that rather than assuming it |
| SCAN-02: two clear-text alerts in `logger.py` | 7 total, only 4 in `logger.py` | Alerts `#27`, `#28`, `#29` are in `core/cli/setup.py`, which SCAN-02 does not name at all |
| SCAN-03: 3 production url-sanitization | 3 | Exact match: `#3` `main.py:51`, `#4` and `#5` `core/cli/run.py:33` |
| SCAN-04: 19 test-file url-sanitization | 19 | Exact match: `#6` through `#24` |
| SCAN-05: `ci.yml` permissions, "CodeQL alert #30" | alerts `#33` and `#34` | Alert number in the requirement text is wrong; the rule is `actions/missing-workflow-permissions` and it fires once per job |

Do not commit yet. Task 3 commits the finished document.
  </action>

  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -c "^phase-38-scanning-to-zero$"</automated>
  </verify>

  <acceptance_criteria>
    - The gate ran before `PLANSHA` was captured, and `git status --porcelain --` over ALL FOUR
      carry-over paths (`.planning/phases/38-scanning-to-zero/`, `.planning/ROADMAP.md`,
      `.planning/HANDOFF.json`, `.planning/phases/37-distributable-artifact/37-VERIFICATION.md`)
      printed nothing. A non-empty result is a hard stop, not a warning: it means the carry-over
      checkout would have restored stale pre-remediation content over the revised content,
      unrecoverably.
    - The gate, the `git checkout -B` and the `git checkout "$PLANSHA" --` restore ran in ONE shell
      invocation under `set -e`, and the branch-name assertion between them passed. If the block
      was split across invocations, the gate did not guard the restore and the task must be redone
      from a clean source branch.
    - `git rev-list --count HEAD..origin/master` prints `0`, proving nothing on master is missing.
    - `git log --oneline origin/master..HEAD` shows exactly one commit, the plan carry-over.
    - `ls .planning/phases/38-scanning-to-zero/` lists `38-01-PLAN.md` through `38-06-PLAN.md`,
      `38-CONTEXT.md` and `38-RUNBOOK.md`. `.continue-here.md` also appears in that directory
      listing and that is expected, not a failure: the `git reset` keeps it out of the commit, not
      out of the working tree.
    - `git diff --name-status origin/master..HEAD` lists `.planning/ROADMAP.md`,
      `.planning/HANDOFF.json` and `.planning/phases/37-distributable-artifact/37-VERIFICATION.md`
      alongside the phase directory, proving the carry-over dropped nothing from the planning
      branch, and carries NO `.continue-here.md` entry. That absence is a statement about the
      STAGED and committed set only. The file stays untracked in the working tree.
    - `git rev-parse --abbrev-ref HEAD` prints `phase-38-scanning-to-zero`.
    - `38-SCAN-BASELINE.md` exists and its `## Alert Inventory` section lists one line per open
      code scanning alert, with a total count that equals the count returned by
      `gh api "repos/thezoid/ShopPyBot/code-scanning/alerts?state=open&per_page=100" --jq 'length'`.
    - The `## Requirement Text Corrections` table has one row per SCAN-01 through SCAN-05, each row
      naming the measured value.
  </acceptance_criteria>

  <done>The working branch carries `origin/master` plus this phase's plan files, and a complete pre-change alert inventory is written to disk.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the SCAN-06 trap and record VERDICT-SCAN-06</name>

  <read_first>
    - `.planning/phases/38-scanning-to-zero/38-CONTEXT.md`, the `### TRAP the plan must verify before acting` section
    - `.github/workflows/codeql-analysis.yml` (all 40 lines)
  </read_first>

  <files>.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md</files>

  <action>
Determine which producer emits the `CodeQL` status check, and which emits `Analyze (python)` and
`Analyze (actions)`. Branch protection requires the context named exactly `CodeQL`. There are two
candidate producers and they are easy to confuse because BOTH are named "CodeQL": the repository
has a default-setup workflow named `CodeQL` at path `dynamic/github-code-scanning/codeql`, and a
file-based workflow also named `CodeQL` at `.github/workflows/codeql-analysis.yml`.

Run all four probes:

1. Workflow inventory with state:
   `gh api "repos/thezoid/ShopPyBot/actions/workflows?per_page=50" --jq '.workflows[] | "\(.id) | \(.name) | \(.path) | \(.state)"'`

2. Check runs on master HEAD, with the producing app:
   `MSHA=$(gh api repos/thezoid/ShopPyBot/branches/master --jq .commit.sha)`
   `gh api "repos/thezoid/ShopPyBot/commits/$MSHA/check-runs?per_page=100" --jq '.check_runs[] | "\(.name) | app=\(.app.slug) | \(.html_url)"'`

3. Check runs on the head of a recent merged pull request against master, which is where the
   `CodeQL` context actually reports. Use pull request 24:
   `PSHA=$(gh api repos/thezoid/ShopPyBot/pulls/24 --jq .head.sha)`
   `gh api "repos/thezoid/ShopPyBot/commits/$PSHA/check-runs?per_page=100" --jq '.check_runs[] | "\(.name) | app=\(.app.slug) | \(.html_url)"'`

4. For each check run named `Analyze (python)` or `Analyze (actions)` found in probe 3, take the
   numeric run id out of its `html_url` (the value after `/actions/runs/`) and resolve the
   workflow that produced it:
   `gh api "repos/thezoid/ShopPyBot/actions/runs/<RUN_ID>" --jq '"name=\(.name) path=\(.path) workflow_id=\(.workflow_id)"'`

Then apply this decision rule and write the result into `38-SCAN-BASELINE.md` under a heading
literally spelled `## VERDICT-SCAN-06`:

Record `VERDICT-SCAN-06: SAFE-TO-DELETE` only if ALL THREE hold:
  a. The check run named exactly `CodeQL` in probe 3 has `app.slug` equal to
     `github-advanced-security`, meaning it is the code scanning results check and not any
     workflow job.
  b. Every `Analyze (*)` check run resolves in probe 4 to a run whose `.path` is
     `dynamic/github-code-scanning/codeql`, meaning default setup, not the file.
  c. The workflow whose `.path` is `.github/workflows/codeql-analysis.yml` reports `state` as
     `disabled_manually` in probe 1.

Otherwise record `VERDICT-SCAN-06: DEFER` and, under it, which of a, b, or c failed and the raw
evidence line that failed it.

Under either verdict, record the raw output of probes 1 through 4 verbatim in a fenced block so a
later reader can re-derive the conclusion without re-running anything.

Expected at planning time, to be confirmed and not assumed: `CodeQL` came from
`app=github-advanced-security`; `Analyze (python)` and `Analyze (actions)` came from run
`30860216102` whose path is `dynamic/github-code-scanning/codeql`; the file-based workflow is id
`8840986` in state `disabled_manually`. That combination yields SAFE-TO-DELETE. If the executor
measures anything different, the measurement wins.

Do not delete anything in this task. Deletion is 38-03 Task 1 and it reads this verdict.
  </action>

  <verify>
    <automated>grep -c "^## VERDICT-SCAN-06$" .planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md</automated>
  </verify>

  <acceptance_criteria>
    - `38-SCAN-BASELINE.md` contains exactly one heading line `## VERDICT-SCAN-06`.
    - The line immediately following it reads either `VERDICT-SCAN-06: SAFE-TO-DELETE` or
      `VERDICT-SCAN-06: DEFER`, with no third option.
    - The section quotes verbatim output for all four probes, and the quoted probe 3 output
      contains a line whose check name is `CodeQL` together with its `app=` value.
    - If the verdict is SAFE-TO-DELETE, the section states the `app.slug` of the `CodeQL` check,
      the `.path` of the workflow that produced each `Analyze (*)` check, and the `state` of
      workflow id `8840986`.
    - If the verdict is DEFER, the section names which of conditions a, b, c failed.
  </acceptance_criteria>

  <done>The question "does deleting codeql-analysis.yml break the CodeQL required check" is answered from live evidence and written down as a machine-greppable verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the cryptography disposition, record VERDICT-SCAN-01, commit the baseline</name>

  <read_first>
    - `requirements.txt` (15 lines)
    - `pyproject.toml` lines 1 through 50 (the `dependencies` and `optional-dependencies` blocks)
    - `core/credentials.py` lines 25 through 35 and lines 215 through 270 (this module's
      cryptography call sites: the imports, `Scrypt` at `:222`, `Fernet` at `:258` and `:268`).
      They are NOT the only ones in the tree: `core/session_store.py` imports `Fernet` and
      `InvalidToken` at `:24` and constructs `Fernet` at `:59` and `:89`. Both modules must appear
      in the import list this task records.
  </read_first>

  <files>.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md</files>

  <action>
SCAN-01's last alert is Dependabot `#13`: `cryptography` in `requirements.txt`, high severity,
GHSA-g6cj-pr64-35w5 / CVE-2026-69247, "PKCS#7 EnvelopedData decryption exposes a Bleichenbacher
oracle through distinguishable errors and timing". Measured 2026-09-08: that alert reads
`state: fixed` and the open Dependabot queue is empty, so this task records a settled outcome
rather than choosing one. Run every probe anyway. Do not assume the fix is still in place, do not
assume the project was ever exposed, and establish both facts from the API rather than from this
paragraph.

1. Read the alert's state, the advisory and its patched version off the alert itself. `state`
   and `fixed_at` are in the projection because the disposition now turns on them:
   `gh api "repos/thezoid/ShopPyBot/dependabot/alerts/13" --jq '"state=\(.state) fixed_at=\(.fixed_at // "null") pkg=\(.dependency.package.name) manifest=\(.dependency.manifest_path) range=\(.security_vulnerability.vulnerable_version_range) patched=\(.security_vulnerability.first_patched_version.identifier // "NONE") ghsa=\(.security_advisory.ghsa_id) cve=\(.security_advisory.cve_id)"'`
   Measured 2026-09-08 this returns `state=fixed fixed_at=2026-09-07T04:02:21Z ... range=>= 44.0.0, < 50.0.0 patched=50.0.0`.
   Take the open-queue count in the same step, because it is the SCAN-01 headline number:
   `gh api "repos/thezoid/ShopPyBot/dependabot/alerts?state=open&per_page=100" --jq 'length'`
   Measured 2026-09-08 this returns `0`.

2. Confirm the patched version is a real, installable release rather than an advisory-only claim:
   `python -c "import json,urllib.request; d=json.load(urllib.request.urlopen('https://pypi.org/pypi/cryptography/json')); print('latest', d['info']['version'], 'requires_python', d['info']['requires_python']); [print(v, len(d['releases'].get(v,[])), 'files') for v in ['49.0.0','50.0.0']]"`

3. Establish reachability. The advisory is specific to PKCS#7 EnvelopedData decryption. Search the
   production tree for any use of that API surface:
   `grep -rn "pkcs7\|PKCS7\|EnvelopedData\|smime\|SMIME\|load_pem_pkcs7\|load_der_pkcs7" --include=*.py core plugins notifications web main.py logger.py models.py utils.py config.py`
   Then enumerate every cryptography import that does exist:
   `grep -rn "from cryptography\|import cryptography" --include=*.py .`

Write a `## VERDICT-SCAN-01` heading into `38-SCAN-BASELINE.md`. The line IMMEDIATELY following
that heading is the machine-readable verdict and nothing else, written exactly the way
`VERDICT-SCAN-06` is written: one line, no leading whitespace, no trailing prose, matching
`^VERDICT-SCAN-01: \(UPGRADE to <version>\|DISMISS\|ACCEPT\)$`, and the only line in the file that
matches it. Downstream plans and the run-book gate on THAT line, not on the heading; a heading
with an empty body must fail the check rather than pass it.

Pick exactly one of these three values. There is no fourth, and the enum does not grow: 38-02
Task 2, 38-04 Task 3 and `38-RUNBOOK.md` all branch on exactly these three literals.

  - `VERDICT-SCAN-01: UPGRADE to <version>` when a patched release exists and installs on
    `requires-python >=3.11`. `<version>` is the advisory's `first_patched_version.identifier` as a
    bare release string, e.g. `UPGRADE to 50.0.0`. Read it as a FLOOR, not as an exact target: it
    asserts the pin must be at or above `<version>`, and any later release in that line satisfies
    it. It never authorises a downgrade from a higher pin back down to `<version>`.
  - `VERDICT-SCAN-01: DISMISS` when no patched release exists AND the vulnerable API is provably
    absent from the tree. State the exact `dismissed_reason` enum value to use.
  - `VERDICT-SCAN-01: ACCEPT` when no patched release exists AND the vulnerable API is reachable.
    State what compensating control makes that tolerable.

**Measured 2026-09-08: SCAN-01 is already SATISFIED on master.** The verdict this task writes is a
record of that, not a decision that is still open:

| Fact | Measured value, 2026-09-08 |
|---|---|
| Dependabot alert `#13` | `state: fixed`, `fixed_at: 2026-09-07T04:02:21Z` |
| Open Dependabot alerts, all severities | `0` |
| GHSA-g6cj-pr64-35w5 vulnerable range | `>= 44.0.0, < 50.0.0` |
| GHSA-g6cj-pr64-35w5 `first_patched_version.identifier` | `50.0.0` |
| `cryptography` pin on `origin/master` (`be613ee`) | `50.0.1`, identical in `requirements.txt` and `pyproject.toml` |

PR `#26` (cryptography 49.0.0 to 50.0.0) merged 2026-09-07T04:01:17Z and PR `#32` carried the pin
on to 50.0.1, which is why the pin sits above the floor rather than exactly on it. The verdict is
therefore `VERDICT-SCAN-01: UPGRADE to 50.0.0`, read as the already-met floor defined above, and
38-02 Task 2 becomes a verification of the satisfied state rather than a manifest edit. Re-run
steps 1 through 3 and confirm all five rows before writing the line. If any value has moved again,
the fresh measurement wins and the disposition is re-derived from the same three values.

BELOW that verdict line, record the supporting evidence in this order: the current pin, the
vulnerable range, the first patched version and whether it exists on PyPI with distribution files,
the complete list of cryptography APIs the project imports with file and line number, and whether
any PKCS#7 or EnvelopedData symbol appears anywhere. Then append two further items that the
measured state makes load-bearing: alert `#13`'s `state` and `fixed_at` verbatim from step 1 plus
the open-queue count, and the `cryptography` pin as it stands in BOTH `requirements.txt` and
`pyproject.toml` on `origin/master`. Those two are what let a later reader see the floor was
already met without re-querying anything.

Expected at planning time, to be confirmed: patched version `50.0.0` exists on PyPI with 46
distribution files and `requires_python !=3.9.0,!=3.9.1,>=3.9`; the project imports only
`cryptography.fernet.Fernet`, `cryptography.fernet.InvalidToken` and
`cryptography.hazmat.primitives.kdf.scrypt.Scrypt`, at `core/credentials.py:29-30` and
`core/session_store.py:24`; no PKCS#7 or EnvelopedData symbol appears anywhere in the tree. That
combination yields `UPGRADE to 50.0.0`, with the unreachability recorded as the reason there was
never a live exposure rather than as a reason to skip the upgrade. Re-measured 2026-09-08 the same
combination still holds and master already carries 50.0.1, so the floor is met and no manifest edit
is left for 38-02 to make.

Also record, as a scope note measured 2026-09-08, that pull request `#25` (the Dependabot
minor-and-patch group) is CLOSED UNMERGED as of 2026-08-17T02:43:40Z and never touched the
`cryptography` pin, so there is no collision left for 38-02 to resolve. It stayed out of scope for
this phase because it carried the FastAPI 0.115 to 0.141 jump that unregisters `/api/events`. Its
safe half landed instead through PRs `#31`, `#32` and `#36`, all merged 2026-09-07, which also
added two `ignore:` rules to `.github/dependabot.yml` holding fastapi/uvicorn and
`starlette>=0.46.0`.

Commit the finished document:
`git add .planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md`
`git commit -m "docs(38-01): record scanner baseline, SCAN-06 trap verdict, cryptography disposition"`
  </action>

  <verify>
    <automated>grep -cE "^VERDICT-SCAN-01: (UPGRADE to [0-9][0-9.]*|DISMISS|ACCEPT)$" .planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md && git log -1 --format=%s | grep -c "^docs(38-01):"</automated>
  </verify>

  <acceptance_criteria>
    - `38-SCAN-BASELINE.md` contains exactly one heading line `## VERDICT-SCAN-01`.
    - The line IMMEDIATELY following that heading is the verdict line, and
      `grep -cE "^VERDICT-SCAN-01: (UPGRADE to [0-9][0-9.]*|DISMISS|ACCEPT)$"` over the file prints
      exactly `1`. The heading alone is not evidence: this criterion fails on an empty section, and
      it is the criterion 38-02 Task 2 and 38-04 branch on.
    - The section lists every `from cryptography` and `import cryptography` line found in the tree,
      with file and line number.
    - The section states explicitly whether the string `PKCS7`, `pkcs7`, or `EnvelopedData` appears
      anywhere under `core`, `plugins`, `notifications`, `web`, or the top-level modules, and the
      grep exit status that proves it.
    - If the disposition is UPGRADE, the named version appears in the PyPI probe output with a
      nonzero file count.
    - The section records alert `#13`'s `state` and `fixed_at` verbatim from step 1, the open
      Dependabot count, and the `cryptography` pin as it stands in both `requirements.txt` and
      `pyproject.toml`. Expected 2026-09-08: `state=fixed`, `fixed_at=2026-09-07T04:02:21Z`, open
      count `0`, pin `50.0.1` in both manifests, which is at or above the `50.0.0` floor. If step 1
      instead returns `state=open`, the fix was reverted between then and execution: say so
      plainly, and 38-02 Task 2 takes its remediation branch instead of its verification branch.
    - `git log -1 --format=%s` starts with `docs(38-01):` and `git status --porcelain` is empty.
  </acceptance_criteria>

  <done>Both blocking questions are answered from evidence, and the baseline document is committed on the phase branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API to local decision | Alert and workflow metadata read here becomes the authority for later destructive actions. A misread here propagates into a deletion |
| Advisory text to upgrade decision | The advisory names an API surface. Deciding it is unreachable is a judgement that must rest on a grep, not on recollection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Denial of Service | `CodeQL` required status check | mitigate | Task 2 forbids deletion until a three-condition rule proves the check comes from `github-advanced-security` rather than the workflow file. The verdict is written as a greppable literal so 38-03 gates on it mechanically |
| T-38-02 | Information Disclosure | Dependabot alert `#13` | mitigate | Task 3 requires a reachability grep over the production tree plus a PyPI existence probe before any disposition. Dismissal is only permitted when no patched release exists AND the API is provably absent |
| T-38-03 | Repudiation | Alert inventory | mitigate | Task 1 records every open alert number and location before any mutation, so a later "it was already closed" claim can be checked against a committed artifact |
| T-38-04 | Tampering | Working tree | mitigate | Task 1 asserts `HEAD == origin/master` before writing anything. Executing this phase against the stale `chore/v4.0-milestone-close` tree would produce fixes against code that no longer exists |
| T-38-05 | Spoofing | Filtered command output | mitigate | The context block forbids accepting a summarised rtk response as evidence, and every acceptance criterion names raw JSON fields rather than prose |
</threat_model>

<verification>
1. `git rev-list --count HEAD..origin/master` is `0`, the branch is `phase-38-scanning-to-zero`,
   and all six plan files plus `38-CONTEXT.md` and `38-RUNBOOK.md` are present in the phase
   directory.
2. `38-SCAN-BASELINE.md` exists, is committed, and contains exactly one `## VERDICT-SCAN-06` and
   exactly one `## VERDICT-SCAN-01` heading, each followed immediately by its single verdict line.
   `grep -cE "^VERDICT-SCAN-01: (UPGRADE to [0-9][0-9.]*|DISMISS|ACCEPT)$"` prints `1`.
3. `git status --porcelain` is empty.
4. No mutating API call was issued. Confirm by searching the session for `-X PATCH`, `-X POST`,
   `-X DELETE`, or `-X PUT`; there must be none.
</verification>

<success_criteria>
- Both verdicts are recorded as machine-greppable literals that 38-02 and 38-03 gate on.
- The alert inventory count matches the live API count at the moment it was taken.
- Zero GitHub state was changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/38-scanning-to-zero/38-01-SUMMARY.md` when done. Include both verdicts
verbatim, the alert inventory totals per rule, and any place where the measurement contradicted
this plan's expected values.
</output>
