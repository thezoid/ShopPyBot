---
phase: 38-scanning-to-zero
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - core/urls.py
  - main.py
  - core/cli/run.py
  - tests/test_url_host_match.py
  - tests/test_secret_names_not_values.py
  - requirements.txt
  - pyproject.toml
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03]
must_haves:
  truths:
    - "A URL whose host is not bestbuy.com cannot trigger the BestBuy CVV prompt, even when the string bestbuy.com appears in its path or query"
    - "The installed cryptography version is outside the vulnerable range declared by GHSA-g6cj-pr64-35w5"
    - "Running the credential migration with real secret values in the environment writes the key names and never the values to stdout or the log file"
  artifacts:
    - path: "core/urls.py"
      provides: "host_matches(url, domain) exact-or-suffix host comparison"
      exports: ["host_matches"]
      min_lines: 12
    - path: "tests/test_url_host_match.py"
      provides: "Regression coverage for the three production url-sanitization alerts"
      min_lines: 40
    - path: "tests/test_secret_names_not_values.py"
      provides: "The invariant the SCAN-02 false-positive dismissals rest on"
      min_lines: 40
  key_links:
    - from: "main.py"
      to: "core/urls.host_matches"
      via: "import and call in the needs_bb_autobuy gate"
      pattern: "host_matches"
    - from: "core/cli/run.py"
      to: "core/urls.host_matches"
      via: "import and call in the needs_cvv gate"
      pattern: "host_matches"
    - from: "requirements.txt"
      to: "pyproject.toml dependencies"
      via: "byte-identical cryptography pin"
      pattern: "cryptography=="
---

<objective>
Fix the production code findings: the three `py/incomplete-url-substring-sanitization` alerts that
let a hostile URL spoof a retailer match, and the `cryptography` dependency alert. Then pin the
invariant that the SCAN-02 clear-text dismissals will rest on, so dismissing those alerts is backed
by a failing-on-regression test rather than by an assertion in a comment.

Purpose: `"bestbuy.com" in item.link` is a substring test over a whole URL. `https://evil.example/
?ref=bestbuy.com` passes it. Today that only gates a CVV prompt, which is not catastrophic, but the
same shape is what routes items to checkout plugins, and this phase is the one that is allowed to
fix it. The cryptography alert is the last open Dependabot finding for SCAN-01.

Output: one new module, two new test files, three edited source files, two edited manifests. All
committed on `phase-38-scanning-to-zero`. Nothing reaches master in this plan; 38-04 lands it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md
@main.py
@core/cli/run.py
@core/credentials.py
@requirements.txt
@pyproject.toml

<interfaces>
Contracts the executor needs. Use these directly. No codebase exploration required.

Existing hostname parsing already in the tree, at `core/registry.py:121`, `:137`, `:150`. The new
helper parses the host the same way:

```python
from urllib.parse import urlparse
host = urlparse(url).hostname or ""
```

Where the two diverge, and this matters: after parsing, the registry still applies a plain
substring test of the pattern against the host, `any(p in host ...)` at `core/registry.py:123`,
`:144`, `:152`. So `registry.route()` returns the BestBuy plugin for
`https://bestbuy.com.attacker.net/x`, while `host_matches` requires exact-or-dot-suffix and
rejects that same host. The new helper is strictly stricter than the registry, not a copy of the
convention already in place.

**Deliberate non-change:** those three registry call sites stay exactly as they are in this plan.
SCAN-03 names alerts `#3`, `#4` and `#5`, all in `main.py` and `core/cli/run.py`;
`core/registry.py` carries no open alert and is not in this plan's `files_modified`. Residual
exposure, stated plainly: a config link on a host such as `bestbuy.com.attacker.net` will still
route to the BestBuy plugin and be driven through its checkout flow, even though the CVV gate this
plan hardens will no longer fire for it. Closing that means routing `route()`, `platform_of()` and
`_route_all()` through `host_matches` too, which changes plugin routing behaviour and needs its own
phase and its own regression tests.

The two production sites to change, quoted exactly as they stand today.

`main.py:49-52`:
```python
    needs_bb_autobuy = (
        not cfg.debug.test_mode
        and any("bestbuy.com" in item.link and item.auto_buy for item in cfg.available.items)
    )
```

`core/cli/run.py:29-36`:
```python
    needs_cvv = (
        not cfg.debug.test_mode
        and not cfg.debug.monitor_only
        and any(
            ("bestbuy.com" in item.link or "amazon.com" in item.link) and item.auto_buy
            for item in cfg.available.items
        )
    )
```

`core/credentials.py:47` defines the taint source that produces all five current clear-text
alerts. It is a list of key NAMES, not values:
```python
SECRET_KEYS: list[str] = [
    "DISCORD_WEBHOOK_URL",
    "SMTP_PASSWORD",
    ...
]
```

`core/credentials.py:404-422` is `migrate_from_env(store)`. It appends key NAMES to `migrated` and
logs only names. Its signature and behaviour must not change:
```python
def migrate_from_env(store) -> list[str]:
    migrated: list[str] = []
    for key in SECRET_KEYS:
        val = os.environ.get(key)
        ...
    return migrated
```

Test fixtures already available: `tmp_data_dir` (used by `tests/test_cli_run.py`) redirects path
resolution. `tests/test_cvv_threading.py` and `tests/test_no_cvv_in_logs.py` are AST scanners for a
different invariant and must not be modified.
</interfaces>

**Running the suite on this machine** (from prior session experience, do not rediscover):
`.venv\Scripts\python.exe -m pytest --tb=short`. Not `rtk pytest`. If the suite errors on missing
imports, `pip install -e ".[web]"` and `pip install httpx` first. The Phase 37 baseline is
969 passed / 2 skipped.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace substring URL matching with host matching at the three production sites</name>

  <read_first>
    - `main.py` lines 40 through 60
    - `core/cli/run.py` lines 1 through 50
    - `core/registry.py` lines 114 through 155 (the existing hostname-matching convention)
    - `.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md`, the `## Alert Inventory` section, to confirm alerts `#3`, `#4`, `#5` are still open and still at these locations
  </read_first>

  <files>core/urls.py, main.py, core/cli/run.py, tests/test_url_host_match.py</files>

  <behavior>
    - `host_matches("https://www.bestbuy.com/site/x", "bestbuy.com")` is True (subdomain)
    - `host_matches("https://bestbuy.com/x", "bestbuy.com")` is True (exact host)
    - `host_matches("https://BestBuy.COM/x", "bestbuy.com")` is True (case insensitive)
    - `host_matches("https://evil.example/p?ref=bestbuy.com", "bestbuy.com")` is False (the alert's own attack: literal in the query)
    - `host_matches("https://evil.example/bestbuy.com/item", "bestbuy.com")` is False (literal in the path)
    - `host_matches("https://bestbuy.com.attacker.net/x", "bestbuy.com")` is False (suffix confusion)
    - `host_matches("https://notbestbuy.com/x", "bestbuy.com")` is False (no dot boundary)
    - `host_matches("", "bestbuy.com")` and `host_matches(None, "bestbuy.com")` are False (no raise)
    - `host_matches("not a url at all", "bestbuy.com")` is False (no raise)
    - A config whose only item link is `https://evil.example/?ref=bestbuy.com` with `auto_buy=True` and `test_mode=False` does NOT trigger the CVV prompt in either `main.main()` or `core.cli.run.handle_run()`
  </behavior>

  <action>
Create `core/urls.py` with a single module-level function. Keep it under 30 lines and give it a
docstring naming the CodeQL rule it exists to satisfy.

The function is `host_matches(url: str | None, domain: str) -> bool`. It parses the URL with
`urllib.parse.urlparse`, takes `.hostname or ""`, lowercases both sides, and returns True only when
the host equals the domain exactly or ends with `"." + domain`. Wrap the parse so a malformed input
returns False rather than raising: `urlparse` raises `ValueError` on some malformed IPv6 literals.

Then edit the two call sites so they call it, per SCAN-03.

`main.py`: add `from core.urls import host_matches` to the import block and change the
`needs_bb_autobuy` generator so the condition reads
`host_matches(item.link, "bestbuy.com") and item.auto_buy`.

`core/cli/run.py`: add `from core.urls import host_matches` and change the `needs_cvv` generator so
the condition reads
`(host_matches(item.link, "bestbuy.com") or host_matches(item.link, "amazon.com")) and item.auto_buy`.

Do not change the surrounding gate logic, the `test_mode` or `monitor_only` short circuits, or the
getpass handling. This is a matcher swap only. `core/cli/run.py:14-19`'s docstring says the CVV
gate "mirrors main.py:main() logic"; that mirroring must survive, so both sites change together or
neither does.

Create `tests/test_url_host_match.py` covering every case in `<behavior>`. Use
`pytest.mark.parametrize` for the pure `host_matches` cases. For the two integration cases, follow
the mocking shape already used in `tests/test_cli_run.py`: a `MagicMock` config with
`debug=MagicMock(test_mode=False, monitor_only=False)` and
`available=MagicMock(items=[MagicMock(link="https://evil.example/?ref=bestbuy.com", auto_buy=True)])`,
then patch `getpass.getpass` and assert it was never called.

Do not change `core/registry.py`. Its three `pattern in host` call sites are the deliberate
non-change described in `<interfaces>`; carry that paragraph's residual-exposure sentence into the
summary so a later reader sees it was a decision and not an oversight.

Do not add a `paths-ignore` entry, a CodeQL config file, or a `# lgtm` / `# nosec` comment
anywhere. The fix is the host parse. Suppression is not a fix.
  </action>

  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_url_host_match.py tests/test_cli_run.py tests/test_main_wiring.py tests/test_cvv_threading.py tests/test_no_cvv_in_logs.py --tb=short -q</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_url_host_match.py` passes with at least 10 assertions, one per `<behavior>` line.
    - Negative control: temporarily revert `core/urls.py::host_matches` to
      `return domain in (url or "")`, re-run `tests/test_url_host_match.py`, and confirm at least
      three cases FAIL. Restore the real implementation and confirm green again. Record both
      outputs in the summary. A test that cannot fail is not a gate.
    - `grep -n "bestbuy.com\" in \|amazon.com\" in " main.py core/cli/run.py` returns no matches.
    - `grep -c "host_matches" main.py` is at least 2 (import plus call) and
      `grep -c "host_matches" core/cli/run.py` is at least 3 (import plus two calls).
    - `grep -rn "lgtm\|nosec\|paths-ignore" core/urls.py main.py core/cli/run.py` returns nothing.
    - The full suite passes: `.venv\Scripts\python.exe -m pytest --tb=short -q` reports 0 failures
      and a passed count strictly greater than 969.
  </acceptance_criteria>

  <done>Both production CVV gates route through a parsed host comparison, and a test proves the query-string spoof no longer matches.</done>
</task>

<task type="auto">
  <name>Task 2: Move the cryptography pin out of the vulnerable range</name>

  <read_first>
    - `.planning/phases/38-scanning-to-zero/38-SCAN-BASELINE.md`, the `## VERDICT-SCAN-01` section. This task is gated on that verdict
    - `requirements.txt` (all 15 lines)
    - `pyproject.toml` lines 14 through 24 (the `dependencies` list and the comment above it that says pins match `requirements.txt` byte for byte)
  </read_first>

  <files>requirements.txt, pyproject.toml</files>

  <action>
<!-- POSTURE-PENDING: 38-02 Task 2 duplicates open PR #26 (bump cryptography 49.0.0 to 50.0.0), which reads MERGEABLE but has never been validated by CI (zero workflow runs on its branch). The operator must choose ONE path -- merge #26, or land the manual pin here and close #26 -- before this task executes. Do not do both. -->

Read `VERDICT-SCAN-01` from `38-SCAN-BASELINE.md` and branch on it.

**If the verdict is `UPGRADE to <version>`** (expected: `50.0.0`):

1. Prove the target installs cleanly and the two APIs this project actually uses still exist,
   before touching any manifest:
   `python -m venv "$TEMP/cryptoprobe"`
   `"$TEMP/cryptoprobe/Scripts/python.exe" -m pip install --quiet --upgrade pip`
   `"$TEMP/cryptoprobe/Scripts/python.exe" -m pip install --quiet "cryptography==<version>"`
   `"$TEMP/cryptoprobe/Scripts/python.exe" -c "import cryptography; from cryptography.fernet import Fernet, InvalidToken; from cryptography.hazmat.primitives.kdf.scrypt import Scrypt; print(cryptography.__version__)"`
   The last command must print the target version and exit 0. This matters because 49 to 50 is a
   major bump and major bumps remove deprecated APIs.

2. Edit `requirements.txt` line 3 from `cryptography==49.0.0` to `cryptography==<version>`.

3. Edit the matching entry in `pyproject.toml`'s `[project] dependencies` list. Both files must
   carry the identical pin: the comment above that list states they match byte for byte, and the
   `wheel` CI job installs from the wheel alone, so a `pyproject.toml` left at 49.0.0 would ship a
   vulnerable wheel while `requirements.txt` looked fixed.

4. Reinstall locally and run the full suite:
   `.venv\Scripts\python.exe -m pip install -r requirements.txt`
   `.venv\Scripts\python.exe -m pytest --tb=short -q`
   Pay attention to `tests/` touching `core/credentials.py` and `core/session_store.py`, which are
   the only Fernet and Scrypt consumers. A Fernet token written by 49 must still decrypt under 50;
   if any credential or session test fails, stop and report rather than deleting the test.

**If the verdict is `DISMISS`**: make no file edit. Record in the summary that the manifests are
unchanged and that the dismissal executes in 38-04 Task 3 with the reason string the verdict names.

**If the verdict is `ACCEPT`**: make no file edit. Record the compensating control the verdict
names, and note that Dependabot alert `#13` will remain open and that SCAN-01 therefore closes as
accepted risk rather than as zero findings. Flag this to the operator in the summary, because it
changes the phase's headline claim.

Do not merge or rebase open pull request `#25` in this task. It is the Dependabot minor-and-patch
group and it carries the FastAPI 0.115 to 0.141 jump that unregisters `/api/events`. It does not
touch the `cryptography` line, so there is no collision to resolve.
  </action>

  <verify>
    <automated>.venv\Scripts\python.exe -m pytest --tb=short -q</automated>
  </verify>

  <acceptance_criteria>
    - Under the UPGRADE branch: `grep -n "^cryptography==" requirements.txt` and
      `grep -n "cryptography==" pyproject.toml` both print the same version string, and that
      version is the `first_patched_version.identifier` returned by
      `gh api "repos/thezoid/ShopPyBot/dependabot/alerts/13" --jq '.security_vulnerability.first_patched_version.identifier'`.
    - The clean-venv probe printed the target version and exited 0, with `Fernet`, `InvalidToken`
      and `Scrypt` all importable. Paste the output in the summary.
    - `.venv\Scripts\python.exe -m pytest --tb=short -q` reports 0 failures.
    - Under DISMISS or ACCEPT: `git diff --stat requirements.txt pyproject.toml` is empty and the
      summary states which branch was taken and why.
  </acceptance_criteria>

  <done>The cryptography pin reflects the verdict from 38-01, proven against a clean install rather than against the manifest text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the secret-name-not-value invariant the SCAN-02 dismissals will rest on</name>

  <read_first>
    - `core/credentials.py` lines 44 through 80 (`SECRET_KEYS`) and lines 394 through 423 (`migrate_from_env`)
    - `core/cli/setup.py` lines 100 through 130 (the three flagged `print` calls at `:108`, `:122`, `:126`)
    - `logger.py` (all 72 lines, specifically `writeLog` at `:49` and its `print` at `:65` and `logFile.write` at `:72`)
    - `tests/test_no_cvv_in_logs.py` lines 1 through 41, to see the existing AST-scanner style and avoid duplicating it
  </read_first>

  <files>tests/test_secret_names_not_values.py</files>

  <behavior>
    - With `SMTP_PASSWORD` set to a unique sentinel in `os.environ`, `migrate_from_env(store)` returns `["SMTP_PASSWORD"]` and the sentinel string appears in neither captured stdout nor the log file
    - The returned list contains key NAMES only: every element is a member of `SECRET_KEYS`
    - `writeLog(f"migrate_from_env: {key} set failed", "WARNING")` writes the key name and no value
    - When the backend read-back returns None, the WARNING path fires and still leaks no value
    - Feeding a sentinel value through `writeLog` directly DOES appear in the output, proving the test can detect a leak and is not vacuous
  </behavior>

  <action>
CodeQL's five current clear-text alerts (`#27`, `#28`, `#29` in `core/cli/setup.py` and `#31`,
`#32` in `logger.py`) all trace to one source: `SECRET_KEYS` at `core/credentials.py:47`. That is a
list of 20 credential key NAMES, not values. The analyser taints it because the identifiers look
like secrets. Those five are genuine false positives and 38-05 dismisses them.

A dismissal with no guard is exactly the failure mode the phase is supposed to prevent: someone
later changes `print(f"  Stored: {key}")` to `print(f"  Stored: {key}={val}")` and the alert stays
dismissed. This test is the guard. Write it before writing anything else, and confirm it can fail.

Create `tests/test_secret_names_not_values.py`. Do not modify `core/credentials.py`,
`core/cli/setup.py`, or `logger.py` in this task. Nothing about them is broken; the test pins the
behaviour they already have.

Structure:

1. A fake store class with `set(key, val)` recording into a dict and `get(key)` reading it back.
2. `test_migrate_from_env_returns_names_never_values`: monkeypatch `os.environ` with two
   `SECRET_KEYS` entries set to unique sentinels such as `SENTINEL_VALUE_A_9f3c` and
   `SENTINEL_VALUE_B_2b71`. Call `core.credentials.migrate_from_env(fake_store)`. Assert the return
   value is exactly the two key names, that every element is in `SECRET_KEYS`, and that neither
   sentinel appears in `capsys.readouterr().out` or `.err`.
3. `test_migrate_from_env_warning_path_leaks_no_value`: make the fake store's `get` return None so
   the read-back failure branch at `core/credentials.py:412-416` fires. Assert the WARNING line
   contains the key name and neither sentinel.
4. `test_writelog_file_output_contains_no_secret_value`: point `SHOPBOT_DATA_DIR` at `tmp_path` via
   monkeypatch so `core.paths.log_dir()` resolves inside the temp tree, run the same migration,
   then read every file under the resolved log directory and assert the sentinels are absent while
   at least one key name is present. Reading the file matters: `logger.writeLog` writes to disk
   independently of stdout, and CodeQL alert `#31` is specifically the disk sink at `logger.py:72`.
5. `test_guard_is_not_vacuous`: call `writeLog("leak-probe SENTINEL_VALUE_C_7d10", "ERROR")`
   directly and assert the sentinel IS present in captured stdout. Without this, a broken capture
   fixture would make tests 2 through 4 pass unconditionally.

Do not import `core.cli.setup`'s interactive `handle_setup` in these tests; it calls `getpass` and
`_prompt_backend`. The `migrate_from_env` path is the same taint flow and is testable without
prompting.
  </action>

  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_secret_names_not_values.py --tb=short -q</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_secret_names_not_values.py` passes with at least 4 test functions.
    - `test_guard_is_not_vacuous` passes, proving the capture and file-read machinery can observe a
      leak when one exists.
    - Negative control: temporarily edit `core/credentials.py:411` to append `f"{key}={val}"`
      instead of `key`, re-run the file, and confirm EXACTLY ONE test fails,
      `test_migrate_from_env_returns_names_never_values`. One failure is the PASS condition here,
      not a broken control: `:411` is the success branch of `migrate_from_env` and it only feeds
      the returned list. It prints nothing, so the stdout assertions still hold; it never executes
      in the read-back-failure branch, so `test_migrate_from_env_warning_path_leaks_no_value` is
      unaffected; and it writes no log line, so the log-file test is unaffected. If a second test
      also fails, stop and find out why before reverting. Revert and confirm green. Record both
      outputs in the summary. `git diff --stat core/credentials.py` must be empty afterwards.
    - `git status --porcelain core/credentials.py core/cli/setup.py logger.py` is empty. This task
      adds a test and changes no production file.
    - Full suite: `.venv\Scripts\python.exe -m pytest --tb=short -q` reports 0 failures.
  </acceptance_criteria>

  <done>The claim that the five clear-text alerts are false positives is now enforced by a test that fails if it ever stops being true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file to CVV gate | `item.link` comes from user-editable `config.yml`. A URL that spoofs a retailer match crosses into a code path that prompts for and holds a card verification value |
| Environment to log file | `migrate_from_env` reads secret values out of `os.environ` and writes to stdout and to a log file on disk. Only key names are meant to cross |
| PyPI to installed environment | A dependency pin change pulls a new artifact into every install and into the published wheel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | Spoofing | `needs_bb_autobuy` in `main.py:49`, `needs_cvv` in `core/cli/run.py:29` | mitigate | Replace substring containment with `core/urls.host_matches`, which parses the host and compares exact-or-dot-suffix. Test cases include the query-string, path-segment, and suffix-confusion spoofs by name |
| T-38-07 | Information Disclosure | `logger.py:65` print and `logger.py:72` file write | mitigate | `tests/test_secret_names_not_values.py` asserts sentinel values never reach stdout or the log file, including the WARNING branch. Non-vacuity is proven by a positive-control test |
| T-38-08 | Information Disclosure | `core/cli/setup.py:108`, `:122`, `:126` | mitigate | Same test pins the `migrate_from_env` taint flow that feeds `:108`. The remaining two print `key` and a prefix derived from `key`, both names by construction |
| T-38-09 | Tampering | `cryptography` pin in two manifests | mitigate | Pin only to the `first_patched_version` reported by the advisory API, verified installable in a throwaway venv with the three symbols this project imports. Both manifests updated together so the wheel cannot ship a version the requirements file claims to have fixed |
| T-38-10 | Repudiation | Suppression instead of fix | mitigate | Acceptance criteria grep for `lgtm`, `nosec`, and `paths-ignore` in the changed files and fail if present. The negative-control step proves each new test can fail |
| T-38-11 | Elevation of Privilege | Major dependency bump 49 to 50 | accept | The only consumed APIs are `Fernet`, `InvalidToken`, and `Scrypt`, all stable. Residual risk is caught by the full suite plus the clean-venv import probe, which run before the pin is committed |
</threat_model>

<verification>
1. `.venv\Scripts\python.exe -m pytest --tb=short -q` reports 0 failures and more than 969 passed.
2. `grep -rn "\"bestbuy.com\" in \|\"amazon.com\" in " main.py core/cli/run.py` returns nothing.
3. `grep -n "cryptography==" requirements.txt pyproject.toml` shows the same version on both lines.
4. Both negative controls were run and both produced failures before being reverted.
5. `git status --porcelain` is empty after committing.
</verification>

<success_criteria>
- The three production url-sanitization alert locations no longer contain a substring URL test.
- The cryptography pin matches the advisory's first patched version, or the summary states which
  non-upgrade branch was taken and why.
- Two new test files exist, both proven capable of failing.
- Every change is committed on `phase-38-scanning-to-zero` with conventional commit messages
  scoped `38-02`. Nothing is pushed to master.
</success_criteria>

<output>
Create `.planning/phases/38-scanning-to-zero/38-02-SUMMARY.md` when done. Include the before and
after pytest counts, both negative-control outputs, the clean-venv cryptography probe output, the
exact diff hunks for `main.py` and `core/cli/run.py`, and the deliberate non-change on
`core/registry.py:123`, `:144`, `:152` with its one-sentence residual exposure.
</output>
