---
phase: 23-encrypted-session-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/config_schema.py
  - tests/test_config_schema.py
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "Each of the 7 platform config models exposes session_persistence: bool defaulting to False (opt-in, off by default)"
    - "A YAML config that sets platforms.amazon.session_persistence: true round-trips to True without being dropped by extra='ignore'"
    - "Existing platform configs with no session_persistence key still load (default False) — additive, non-breaking"
  artifacts:
    - path: "core/config_schema.py"
      provides: "session_persistence: bool = False on all 7 platform models"
      contains: "session_persistence: bool = False"
    - path: "tests/test_config_schema.py"
      provides: "Default + override coverage for session_persistence across all 7 models"
      contains: "session_persistence"
  key_links:
    - from: "core/config_schema.py PlatformsConfig"
      to: "AmazonPlatformConfig.session_persistence + BestBuyPlatformConfig.session_persistence"
      via: "declared field so extra='ignore' cannot drop it from YAML"
      pattern: "session_persistence: bool = False"
---

<objective>
Add `session_persistence: bool = False` as a declared field to all 7 platform config models in `core/config_schema.py` so any plugin can opt into encrypted cookie persistence (default off). Extend `tests/test_config_schema.py` with default + override coverage.

Purpose: REL-04 requires per-platform opt-in. The flag is read by the ABC `save_session`/`restore_session` hooks (Plan 23-04). It must be a declared field (not an extra) so `extra="ignore"` does not silently drop it from YAML, matching the existing `headless` field treatment.
Output: 7 modified models + extended schema tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-encrypted-session-persistence/23-CONTEXT.md
@.planning/phases/23-encrypted-session-persistence/23-PATTERNS.md

<interfaces>
<!-- The 7 platform models and their container, VERIFIED in core/config_schema.py. -->
<!-- Each already declares `headless: bool` — the exact opt-in-bool analog to copy. -->

core/config_schema.py models to modify (each gets one new line `session_persistence: bool = False`):
- AmazonPlatformConfig (line 67) — fields: delay_seconds, delay_jitter, headless, user_agents
- BestBuyPlatformConfig (line 78) — fields: delay_seconds, delay_jitter, headless, user_agents
- WalmartPlatformConfig (line 87) — fields: min_delay, max_delay, headless, user_agents
- TargetPlatformConfig (line 96)
- GameStopPlatformConfig (line 105)
- SquareEnixPlatformConfig (line 114) — config key `squareenix` (no underscore)
- NeweggPlatformConfig (line 126)

Container: PlatformsConfig (line 135) holds amazon + bestbuy (+ others); AppConfig.platforms (line 288).

Decision (RESEARCH Open Question 2): add the field individually to each of the 7 models. Do NOT introduce a shared PlatformBaseConfig mixin — the 7 models share no base today; a mixin is an unneeded abstraction (YAGNI).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session_persistence: bool = False to all 7 platform models</name>
  <read_first>
    - core/config_schema.py lines 67-133 (the 7 platform models; copy the `headless: bool` opt-in pattern)
    - 23-PATTERNS.md "core/config_schema.py" section (lists all 7 target classes + line numbers)
  </read_first>
  <files>core/config_schema.py</files>
  <action>
    Append a `session_persistence: bool = False` field to each of the 7 platform config models: AmazonPlatformConfig, BestBuyPlatformConfig, WalmartPlatformConfig, TargetPlatformConfig, GameStopPlatformConfig, SquareEnixPlatformConfig, NeweggPlatformConfig. Place it after the existing `user_agents` field in each model, mirroring the existing `headless: bool = True` declared-field pattern. Add a short inline comment on each: `# REL-04: opt-in encrypted cookie persistence (default off)`.

    Do NOT add a shared base class or mixin (RESEARCH Open Question 2 — add individually; no new abstraction). Do NOT change any other field, default, or the PlatformsConfig container. This is purely additive (per CLAUDE.md scope discipline).
  </action>
  <verify>
    <automated>pytest tests/test_config_schema.py -k session_persistence -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nv '^#' core/config_schema.py | grep -c "session_persistence: bool = False"` equals 7.
    - `python -c "from core.config_schema import PlatformsConfig; p=PlatformsConfig(); assert p.amazon.session_persistence is False and p.bestbuy.session_persistence is False"` exits 0.
    - No other field defaults changed (diff limited to 7 added lines + comments).
  </acceptance_criteria>
  <done>All 7 platform models declare session_persistence defaulting to False; defaults instantiate to False; no other schema change.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_config_schema.py with session_persistence default + override coverage</name>
  <read_first>
    - tests/test_config_schema.py (existing structure; how platform configs are instantiated / loaded from dict)
    - 23-VALIDATION.md "config" row (session_persistence default False on all 7 models)
  </read_first>
  <files>tests/test_config_schema.py</files>
  <action>
    Add tests to the existing `tests/test_config_schema.py` (extend, do not create a new file):
    - `test_session_persistence_defaults_false_all_platforms`: build a default `PlatformsConfig()` and assert `session_persistence is False` for amazon, bestbuy, walmart, target, gamestop, squareenix, newegg (all 7).
    - `test_session_persistence_override_true`: construct an `AppConfig` (or PlatformsConfig) from a dict that sets `platforms.amazon.session_persistence: true` and assert the loaded value is `True` (proves the declared field survives `extra="ignore"`, mirroring the existing headless-not-dropped assertion).
    - `test_session_persistence_legacy_config_loads`: build a platform config dict WITHOUT a session_persistence key and assert it loads with the field defaulting to False (additive non-breaking).

    Mark the tests so `pytest -k session_persistence` selects them (the function names contain `session_persistence`).
  </action>
  <verify>
    <automated>pytest tests/test_config_schema.py -k session_persistence -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_config_schema.py -k session_persistence -x` collects and passes >= 3 tests.
    - The default test asserts all 7 platforms (not just amazon/bestbuy).
    - The override test proves `true` in YAML/dict survives to `True` (not dropped).
  </acceptance_criteria>
  <done>session_persistence default-False (7 platforms), override-True (not dropped), and legacy-load behaviors are covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml → process | User-supplied per-platform opt-in flag crosses into AppConfig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering | session_persistence silently dropped by extra='ignore' | mitigate | Declared field (not extra), matching headless treatment; `test_session_persistence_override_true` asserts the value survives |
| T-23-06 | Elevation of Privilege | persistence on-by-default | mitigate | Default False on all 7 models; `test_session_persistence_defaults_false_all_platforms` |
| T-23-SC | Tampering | npm/pip installs | accept | No new packages this phase |
</threat_model>

<verification>
- `pytest tests/test_config_schema.py -k session_persistence` green.
- `grep -c "session_persistence: bool = False"` (comments filtered) equals 7 in core/config_schema.py.
</verification>

<success_criteria>
All 7 platform models expose `session_persistence: bool = False`; the flag is overridable from YAML and never dropped; legacy configs without the key still load.
</success_criteria>

<output>
Create `.planning/phases/23-encrypted-session-persistence/23-02-SUMMARY.md` when done
</output>
