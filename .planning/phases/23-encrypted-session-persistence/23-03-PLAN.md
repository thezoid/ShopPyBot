---
phase: 23-encrypted-session-persistence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_no_committed_sessions.py
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "No data/sessions/*.bin file is tracked by git"
    - "data/sessions/ is covered by .gitignore (via the existing data/* rule)"
  artifacts:
    - path: "tests/test_no_committed_sessions.py"
      provides: "CI guard: no committed session files + sessions dir gitignored"
      contains: "def test_no_committed_session_files"
  key_links:
    - from: "tests/test_no_committed_sessions.py"
      to: "git ls-files data/sessions/"
      via: "subprocess.run checks no .bin tracked"
      pattern: "git.*ls-files"
    - from: "tests/test_no_committed_sessions.py"
      to: ".gitignore"
      via: "asserts data/* (or data/sessions) present"
      pattern: "data/\\*"
---

<objective>
Create `tests/test_no_committed_sessions.py` — a CI guard asserting (1) no `data/sessions/*.bin` session file is git-tracked and (2) `data/sessions/` is gitignored. This is REL-04 criterion 3: encrypted cookies must never be committed.

Purpose: Auth-bearing cookie files are the most sensitive artifact this milestone produces. A CI assertion makes accidental commits a failing build, not a silent leak.
Output: `tests/test_no_committed_sessions.py` (test-only; no source change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-encrypted-session-persistence/23-CONTEXT.md
@.planning/phases/23-encrypted-session-persistence/23-PATTERNS.md

<interfaces>
<!-- Analog: tests/test_no_plaintext.py (file-scan/skip pattern). VERIFIED. -->
<!-- .gitignore line 8 is `data/*` — already covers data/sessions/. VERIFIED. -->

Established CI-guard pattern (tests/test_no_plaintext.py):
- Use `subprocess.run([...], capture_output=True, text=True)` for git queries.
- Use `pathlib.Path(".gitignore").read_text()` for the ignore-rule assertion.
- Skip gracefully when running outside a git checkout (CalledProcessError / FileNotFoundError) so the suite stays green in non-git environments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI guard — no committed session files + sessions dir gitignored</name>
  <read_first>
    - tests/test_no_plaintext.py lines 33-86 (file-scan + skip-when-absent pattern)
    - 23-PATTERNS.md "tests/test_no_committed_sessions.py" section (git ls-files + gitignore assertions)
    - .gitignore line 8 (`data/*` rule that covers data/sessions/)
  </read_first>
  <files>tests/test_no_committed_sessions.py</files>
  <action>
    Create `tests/test_no_committed_sessions.py` modeled on `tests/test_no_plaintext.py`'s CI-guard style.

    `test_no_committed_session_files`: run `subprocess.run(["git", "ls-files", "data/sessions/"], capture_output=True, text=True)` (do NOT pass check=True so a clean empty result is handled uniformly). Filter stdout lines ending in `.bin`; assert the filtered list is empty with a message naming any offending tracked files. Wrap the subprocess call in a try/except for `(FileNotFoundError, subprocess.CalledProcessError)` and `pytest.skip("git not available")` so the test does not fail outside a git checkout (matching the skip-when-absent convention in test_no_plaintext.py).

    `test_sessions_dir_gitignored`: read `.gitignore` via `Path(".gitignore").read_text()`; if `.gitignore` is absent, `pytest.skip`. Assert that `"data/*"` is present OR `"data/sessions"` appears in the file (the existing `data/*` rule on line 8 covers `data/sessions/`). Add a comment that `data/*` is the source of coverage so a future reordering does not silently break the guarantee.

    Test-only plan: do NOT create `data/sessions/` and do NOT modify `.gitignore` (already covered by `data/*`).
  </action>
  <verify>
    <automated>pytest tests/test_no_committed_sessions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_no_committed_sessions.py -x` passes (2 tests; or skips cleanly if git/.gitignore absent — never errors).
    - `test_no_committed_session_files` uses `git ls-files data/sessions/` and asserts no `.bin` tracked.
    - `test_sessions_dir_gitignored` asserts `data/*` (or `data/sessions`) present in .gitignore.
    - No source file or .gitignore modified by this plan.
  </acceptance_criteria>
  <done>CI guard proves no session .bin is git-tracked and the sessions directory is gitignored; both tests pass (or skip gracefully outside git).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | An encrypted (or worse, plaintext) session file could be accidentally committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Information Disclosure | session .bin committed to git | mitigate | `test_no_committed_session_files` fails CI if any data/sessions/*.bin is tracked |
| T-23-08 | Information Disclosure | data/sessions/ accidentally un-ignored | mitigate | `test_sessions_dir_gitignored` asserts the data/* rule covers it |
| T-23-SC | Tampering | npm/pip installs | accept | No new packages this phase |
</threat_model>

<verification>
- `pytest tests/test_no_committed_sessions.py` green (or skips outside git).
</verification>

<success_criteria>
A CI test asserts no `data/sessions/*.bin` is git-tracked and `data/sessions/` is gitignored (REL-04 criterion 3).
</success_criteria>

<output>
Create `.planning/phases/23-encrypted-session-persistence/23-03-SUMMARY.md` when done
</output>
