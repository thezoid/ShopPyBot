---
phase: 27-sse-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sse.py
  - tests/test_log_reader.py
autonomous: true
requirements: [SSE-02]
must_haves:
  truths:
    - "A failing test file tests/test_sse.py exists asserting the retry line, a status frame, a keep-alive comment, running-flip on bot start/stop, hub-empty-after-disconnect, and no credential pattern in a frame"
    - "A failing test file tests/test_log_reader.py exists asserting tail_log_lines returns only new lines, advances the cursor, and resets to total on after_line > total (midnight rollover)"
    - "All new tests FAIL on missing implementation (AttributeError / ImportError / 404), NOT on collection or syntax errors"
    - "The httpx streaming-read chunking question (A2) is resolved: frame assertions join the first few chunks rather than indexing a single chunk"
  artifacts:
    - path: "tests/test_sse.py"
      provides: "Wave 0 isolation spike — 6 TestClient streaming tests, each using `with TestClient(app) as client:` so the lifespan/poll-loop runs"
      min_lines: 80
      contains: "with TestClient"
    - path: "tests/test_log_reader.py"
      provides: "tail_log_lines cursor tests including midnight-rollover reset"
      min_lines: 30
      contains: "tail_log_lines"
  key_links:
    - from: "tests/test_sse.py"
      to: "/api/events"
      via: "client.stream GET"
      pattern: "client\\.stream\\(\"GET\", \"/api/events\""
    - from: "tests/test_sse.py"
      to: "web.sse_hub module-level interval constants"
      via: "monkeypatch of _POLL_INTERVAL_SECS / _KEEPALIVE_SECS"
      pattern: "_POLL_INTERVAL_SECS"
---

<objective>
Create the Wave 0 RED test scaffold for the SSE infrastructure — the "spike" that validates
the cross-thread bridge in complete isolation before any implementation or browser wiring.
Two new test files: tests/test_sse.py (six TestClient streaming tests, one per ROADMAP
success criterion plus the SSE-03 credential carryover) and tests/test_log_reader.py
(tail_log_lines cursor + midnight-rollover tests).

Every test MUST fail on missing implementation (AttributeError / ImportError / 404), never
on a collection or syntax error. This is the Nyquist gate: tests exist before the code that
satisfies them (Plans 02 and 03 turn these GREEN).

Purpose: De-risk the highest-risk phase by locking the exact observable contract (retry line,
status frame, keepalive comment, running-flip, hub-empty-after-disconnect, no credential leak)
in executable form, and resolve research open question A2 (httpx iter_text chunking) so later
plans assert correctly.
Output: tests/test_sse.py, tests/test_log_reader.py (both RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-sse-infrastructure/27-RESEARCH.md
@.planning/phases/27-sse-infrastructure/27-PATTERNS.md
@.planning/phases/27-sse-infrastructure/27-VALIDATION.md

<interfaces>
<!-- Contracts the tests must assert against. The implementation (Plans 02/03) MUST match these. -->

Module-level injectable constants (defined in Plan 02 at top of web/sse_hub.py;
the route generator default reads _KEEPALIVE_SECS from sse_hub):
  web.sse_hub._POLL_INTERVAL_SECS = 1.0    # override in tests to 0.05
  web.sse_hub._KEEPALIVE_SECS = 15.0       # override in tests to 0.05 (keepalive) or 60.0 (suppress)

SseHub (Plan 02, web/sse_hub.py):
  class SseHub:
      def subscribe(self) -> asyncio.Queue        # adds a maxsize=100 queue, returns it
      def unsubscribe(self, q: asyncio.Queue)      # set.discard
      def broadcast(self, event: str, payload: dict)  # f"event: {event}\ndata: {json}\n\n" to every queue
      _queues: set[asyncio.Queue]                  # introspected by the disconnect test

Route (Plan 03, web/routes/sse.py):
  GET /api/events -> StreamingResponse(media_type="text/event-stream")
  Stream opens with "retry: 3000\n\n", then status/log frames or ": keep-alive\n\n" on idle.

create_app (Plan 03, web/__init__.py):
  create_app(svc, is_non_local=False) gains lifespan= and app.state.sse_hub = SseHub().
  app.state.sse_hub is only populated when the app runs under `with TestClient(app) as c:`.

tail_log_lines (Plan 02, web/log_reader.py):
  def tail_log_lines(after_line: int) -> tuple[list[str], int]
  Returns (lines[after_line:], total). If after_line > total -> returns (lines, total) [rollover reset].

BotService.get_status() shape (already exists, core/service.py):
  {"running": bool, "uptime_secs": float, "plugins": {name: {...}}}
</interfaces>

<analog_fixtures>
<!-- Copy these patterns verbatim. Source: tests/test_api_observability.py, tests/test_web_dashboard.py -->

importorskip guard (lines 1-2 of every web test):
  import pytest
  pytest.importorskip("fastapi")

mock_svc fixture (tests/test_api_observability.py lines 28-33):
  @pytest.fixture
  def mock_svc():
      svc = MagicMock()
      svc.get_status.return_value = {"running": False, "uptime_secs": 0.0, "plugins": {}}
      svc.list_items.return_value = []
      return svc

CRED_PATTERN (tests/test_api_observability.py line 20):
  CRED_PATTERN = re.compile(r'(@|password|token|key=|cvv)', re.IGNORECASE)

FLAG — the existing `client` fixture (test_web_dashboard.py lines 18-21) does NOT use a
context manager, so the lifespan never runs. SSE tests MUST NOT reuse it. Each SSE test
opens its own `with TestClient(create_app(mock_svc)) as client:` inline.
</analog_fixtures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED test scaffold — tests/test_sse.py (6 isolation tests)</name>
  <read_first>
    - .planning/phases/27-sse-infrastructure/27-RESEARCH.md (Pattern 3, TestClient SSE test skeleton, Credential-pattern assertion, Open Questions A2 and A3)
    - .planning/phases/27-sse-infrastructure/27-PATTERNS.md (tests/test_sse.py section, the context-manager FLAG, Imports block)
    - .planning/phases/27-sse-infrastructure/27-VALIDATION.md (Wave 0 Requirements — one test per criterion)
    - tests/test_api_observability.py (mock_svc fixture lines 28-33, CRED_PATTERN line 20, importorskip lines 12-13)
    - tests/test_web_dashboard.py (the NON-context-manager client fixture — the anti-pattern to avoid, lines 18-21)
  </read_first>
  <files>tests/test_sse.py</files>
  <action>
    Create tests/test_sse.py with the importorskip("fastapi") guard as lines 1-2, then import re,
    MagicMock from unittest.mock, and TestClient from fastapi.testclient. Define the mock_svc
    fixture verbatim from tests/test_api_observability.py (get_status returns running=False,
    uptime_secs=0.0, plugins={}; list_items returns []). Define CRED_PATTERNS as the list
    ["@", "password", "token", "key=", "cvv"].

    Write exactly six test functions, each opening its OWN `with TestClient(create_app(mock_svc))
    as client:` block (NEVER a shared client fixture — the lifespan must run, per the PATTERNS FLAG).
    Before each request, monkeypatch the module-level interval constants on the web.sse_hub module
    (import web.sse_hub as a module and set _POLL_INTERVAL_SECS and _KEEPALIVE_SECS) and restore
    them in a try/finally so tests stay fast and isolated. Read each streaming response via
    `with client.stream("GET", "/api/events") as resp:` then `resp.iter_text()`.

    Per-test contract (one ROADMAP criterion each):
      - test_sse_retry_line_on_open: poll=0.05, keepalive=60.0; assert "retry: 3000" appears in the
        joined first few chunks (criterion 4).
      - test_sse_status_frame_arrives: poll=0.05, keepalive=60.0; assert "event: status" and "data:"
        appear in the joined first several chunks (criterion 1).
      - test_sse_keepalive_comment: poll=60.0 (suppress status), keepalive=0.05; assert ": keep-alive"
        appears in the joined first several chunks (criterion 1 keepalive half).
      - test_sse_bot_start_stop_reflected: configure mock_svc.get_status to return running=False then
        running=True on successive calls (use side_effect or a mutable holder); assert a frame
        containing '"running": true' (or running True after json round-trip) arrives in the stream
        within the fast interval (criterion 3).
      - test_sse_disconnect_cleans_hub: open the stream, read one frame, exit the `with
        client.stream(...)` context; then assert the hub on app.state has zero queues
        (len(app.state.sse_hub._queues) == 0). Assert via the finally-cleanup path, NOT
        request.is_disconnected() (unreliable in TestClient in-process transport — RESEARCH A3).
        Reach the app via the TestClient instance (client.app) inside the still-open `with
        TestClient(...) as client:` block, after the inner stream context has exited (criterion 2).
      - test_sse_no_credential_patterns: poll=0.05; collect the first ~3 chunks, join them, assert
        none of "password", "key=", "cvv" appear (case-insensitive) and that the email-like regex
        r'[a-zA-Z0-9._%+-]+@[a-zA-Z0-9.-]+' does not match the combined frames (SSE-03 carryover).

    Resolve open question A2 inline: assert on `"<token>" in "".join(chunks[:N])` for a small N,
    NOT on chunks[0] exactly — httpx may chunk a single SSE frame across reads. Do not commit any
    test that indexes a single chunk for a multi-line frame.

    Do NOT add a shared context-manager client fixture; keep the `with` inline per test so the
    disconnect test can observe the hub after the stream context exits but before the app context exits.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && python -m pytest tests/test_sse.py -q 2>&1 | grep -E "6 (failed|error)|collected 6"</automated>
  </verify>
  <acceptance_criteria>
    - File begins with `import pytest` then `pytest.importorskip("fastapi")` (lines 1-2).
    - `python -m pytest tests/test_sse.py --collect-only -q` collects exactly 6 tests with NO
      collection/import errors (failures must be runtime, not collection).
    - All 6 tests FAIL when run (RED), failing on missing app.state.sse_hub / 404 / AttributeError —
      verified by: `python -m pytest tests/test_sse.py -q` reports 6 failed (or errored), 0 passed.
    - Every test body contains `with TestClient(create_app(mock_svc)) as client:` (grep count == 6).
    - No shared `client` fixture is defined (grep for `def client(` returns 0 matches in the file).
    - The disconnect test asserts `len(...sse_hub._queues) == 0` (grep finds `_queues`).
    - Interval constants are monkeypatched and restored in try/finally (grep finds both
      `_POLL_INTERVAL_SECS` and `_KEEPALIVE_SECS`).
  </acceptance_criteria>
  <done>tests/test_sse.py exists; pytest collects exactly 6 tests with no import/collection errors; all 6 fail at runtime on missing implementation.</done>
</task>

<task type="auto">
  <name>Task 2: RED test scaffold — tests/test_log_reader.py (tail_log_lines cursor)</name>
  <read_first>
    - .planning/phases/27-sse-infrastructure/27-RESEARCH.md (Pattern 5: tail_log_lines Cursor with Midnight Rollover; Pitfall 6)
    - .planning/phases/27-sse-infrastructure/27-PATTERNS.md (web/log_reader.py modification — function signature and return type)
    - web/log_reader.py (current functions: _read_today_lines, read_recent_logs, read_logs_filtered; _LOG_DIR derivation)
  </read_first>
  <files>tests/test_log_reader.py</files>
  <action>
    Create tests/test_log_reader.py importing pytest. tail_log_lines reads today's log file via
    _read_today_lines() which resolves _LOG_DIR / "%Y%B%d.log". To test deterministically without a
    real log file, monkeypatch web.log_reader._read_today_lines to return a controlled list (use
    monkeypatch.setattr or unittest.mock.patch on "web.log_reader._read_today_lines"). This avoids
    filesystem and date coupling.

    Write three test functions asserting the (new_lines, new_cursor) tuple contract:
      - test_tail_returns_new_lines_from_cursor: _read_today_lines returns 5 lines; call
        tail_log_lines(2); assert the result is (lines[2:], 5) — exactly the 3 trailing lines and a
        cursor of 5.
      - test_tail_advances_cursor_no_new_lines: _read_today_lines returns 5 lines; call
        tail_log_lines(5); assert the result is ([], 5) — nothing new, cursor unchanged.
      - test_tail_rollover_resets_cursor: _read_today_lines returns 2 lines (a fresh post-midnight
        file) while cursor is 10; call tail_log_lines(10); assert the result is (all 2 lines, 2) —
        i.e. after_line > total triggers a reset that returns every line of the new file and a cursor
        equal to the new total (Pitfall 6).

    Import tail_log_lines with `from web.log_reader import tail_log_lines` at the top so the file
    fails to import (ImportError) until Plan 02 adds the function — this is the intended RED state.
    Do NOT guard the import with a try/except; the ImportError IS the RED signal.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && python -m pytest tests/test_log_reader.py -q 2>&1 | grep -E "error|failed|no tests ran"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_log_reader.py defines exactly 3 test functions named test_tail_returns_new_lines_from_cursor,
      test_tail_advances_cursor_no_new_lines, test_tail_rollover_resets_cursor.
    - The file imports `tail_log_lines` from web.log_reader at module top (grep finds
      `from web.log_reader import tail_log_lines`).
    - `python -m pytest tests/test_log_reader.py -q` reports a collection ImportError (RED) because
      tail_log_lines does not exist yet — NOT a syntax error in the test file itself
      (`python -c "import ast; ast.parse(open('tests/test_log_reader.py').read())"` exits 0).
    - Each test monkeypatches web.log_reader._read_today_lines (grep finds `_read_today_lines`).
    - The rollover test asserts the returned cursor equals the new total (2), not the stale cursor (10).
  </acceptance_criteria>
  <done>tests/test_log_reader.py exists, parses as valid Python, and fails to collect with an ImportError on the missing tail_log_lines symbol (the intended RED state).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot daemon thread -> uvicorn event loop | Health/log data crosses from the bot thread's private loop to uvicorn's loop. asyncio.Queue is NOT thread-safe; the bridge must keep the bot thread away from the queues. Tests here lock the contract that uvicorn's _poll_loop is the sole producer. |
| server -> browser (SSE frame) | Status/log frames sent to any localhost client; could leak secrets if get_status() last_error were unscrubbed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | /api/events data frames | mitigate | test_sse_no_credential_patterns asserts no @/password/token/key=/cvv and no email-like string in live frames (SSE-03 carryover); locks the scrub contract before implementation exists |
| T-27-02 | Denial of Service | SseHub per-client queues / generator leak | mitigate | test_sse_disconnect_cleans_hub asserts SseHub._queues is empty after the stream context exits, locking the try/finally unsubscribe contract |
| T-27-03 | Tampering / Availability | cross-loop asyncio.Queue corruption | mitigate | This plan creates only tests; tests exercise the bridge exclusively through /api/events on uvicorn's loop — never touching queues from a bot thread — encoding the sole-producer invariant in the spike |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; RESEARCH.md Package Legitimacy Audit confirms zero new packages. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `python -m pytest tests/test_sse.py --collect-only -q` collects exactly 6 tests, zero collection errors.
- `python -m pytest tests/test_sse.py -q` reports 6 failed/errored, 0 passed (RED).
- `python -m pytest tests/test_log_reader.py -q` reports an ImportError on tail_log_lines (RED).
- Both files parse as valid Python (ast.parse exits 0).
</verification>

<success_criteria>
- tests/test_sse.py: 6 isolation tests, each using `with TestClient(app) as client:`; all RED on missing implementation; no shared client fixture; A2 chunking resolved via joined-chunk assertions.
- tests/test_log_reader.py: 3 tail_log_lines tests including the midnight-rollover reset; RED via ImportError.
- No new Python packages introduced.
</success_criteria>

<output>
Create `.planning/phases/27-sse-infrastructure/27-01-SUMMARY.md` when done. Record: the resolved
A2 chunking decision (which iter_text join-window the assertions use), and confirm both files are RED.
</output>
