---
phase: 26-read-only-api-endpoints
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - core/health.py
  - core/orchestrator.py
autonomous: true
requirements: [SSE-03]
must_haves:
  truths:
    - "Per-plugin health snapshot carries a last_error field containing only exc.__class__.__name__ (or None), never str(exc)"
    - "supervise()'s except block records the scrubbed last_error alongside the existing record_error call"
    - "A CI test asserts /api/status JSON and get_status() payload contain no credential-pattern strings (@, password, token, key=, cvv, case-insensitive)"
  artifacts:
    - path: "core/health.py"
      provides: "record_last_error(name, exc) method + last_error in _ensure init dict"
      contains: "def record_last_error"
    - path: "core/orchestrator.py"
      provides: "health.record_last_error(plugin_name, exc) call in the supervise except block"
      contains: "record_last_error"
  key_links:
    - from: "core/orchestrator.py"
      to: "core.health.HealthRegistry.record_last_error"
      via: "health.record_last_error(plugin_name, exc) in except Exception block"
      pattern: "health\\.record_last_error\\(plugin_name, exc\\)"
    - from: "core/health.py record_last_error"
      to: "per-plugin snapshot last_error"
      via: "self._plugins[name]['last_error'] = exc.__class__.__name__"
      pattern: "exc\\.__class__\\.__name__"
---

<objective>
Establish the SSE-03 secret-leak invariant: add a scrubbed `last_error` field (exception class name
only) to the HealthRegistry per-plugin snapshot, record it at the orchestrator's `supervise()`
exception boundary, and make the Wave 0 credential/scrub tests go GREEN. `str(exc)` must never enter
the snapshot — only `exc.__class__.__name__`, which is always a Python identifier and cannot carry
proxy creds or API keys.

Purpose: Guarantee error surfaces exposed via get_status()/`/api/status` (and, in Phase 27, SSE
frames) never leak credential-pattern strings to the browser. Forward-prep for the Phase 28 status
badge.
Output: 1 new HealthRegistry method + _ensure dict update; 1-line orchestrator call-site addition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-read-only-api-endpoints/26-RESEARCH.md
@.planning/phases/26-read-only-api-endpoints/26-PATTERNS.md
@.planning/phases/26-read-only-api-endpoints/26-CONTEXT.md

<interfaces>
<!-- Contracts to implement. Extracted from source + PATTERNS. Use directly — no exploration. -->

core/health.py current _ensure dict (lines 19-28) — keys: status, last_heartbeat,
consecutive_errors, items_checked, orders_confirmed, _degraded_armed. NO last_error today.
get_snapshot() (lines 71-76) strips ONLY keys starting with "_". So `last_error` (no underscore) is
automatically included once added to _ensure.
record_error(name) (lines 38-40) is the method shape to mirror: _ensure(name) then mutate the dict.

core/orchestrator.py supervise() except block (lines 125-155):
- `except Exception as exc:` at line 125
- line 132 already uses `exc.__class__.__name__` in a writeLog call (the scrubbing pattern is established)
- lines 136-138: `if health is not None:` → `plugin_name = plugin.__class__.__name__` → `health.record_error(plugin_name)`
- INSERT the new call immediately after line 138's record_error(plugin_name), before the
  degraded-threshold logic (line 139+). Do not touch the degraded/park/relaunch logic.

Scrubbing invariant (CONTEXT.md + RESEARCH lines 215-237):
- store `exc.__class__.__name__` ONLY (e.g. "ConnectionError", "TimeoutError")
- NEVER store `str(exc)` (can contain proxy URLs, API keys from tracebacks)

CI guard contract (RESEARCH lines 239-273, Pitfall 4 lines 356-359):
- regex `(@|password|token|key=|cvv)` IGNORECASE over json.dumps(get_status()) and raw /api/status text
- the credential test MUST use a REAL HealthRegistry, not a MagicMock (already authored in Plan 01 Task 3)
- do NOT scrub /api/logs content (operator's own log files — CONTEXT.md decision)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add record_last_error + last_error init to HealthRegistry</name>
  <files>core/health.py</files>
  <read_first>
    - core/health.py (lines 19-28 _ensure dict; lines 38-40 record_error shape to mirror; lines 71-76 get_snapshot — confirm it strips only "_"-prefixed keys)
    - .planning/phases/26-read-only-api-endpoints/26-PATTERNS.md (lines 205-264 health.py contract: _ensure update, record_last_error method, get_snapshot unchanged)
    - .planning/phases/26-read-only-api-endpoints/26-RESEARCH.md (lines 215-237 Pattern 4; lines 350-355 Pitfall 3 _ensure must include last_error; lines 399-411 _ensure init dict)
  </read_first>
  <behavior>
    - record_last_error("P", RuntimeError("proxy http://user:pass@host token=abc")) -> get_snapshot()["P"]["last_error"] == "RuntimeError" (class name only); "@" not in that value
    - a never-crashed plugin (only heartbeat called) -> get_snapshot()[name]["last_error"] is None (no KeyError) because _ensure initializes it
    - get_snapshot() includes last_error for every plugin and still strips _degraded_armed (underscore key)
  </behavior>
  <action>
    In core/health.py `_ensure`, add `"last_error": None,` to the initialized dict (alongside the
    existing keys, before `_degraded_armed`). Add a new method `record_last_error(self, name, exc)`
    immediately after `record_error` (line 40), mirroring its shape: call `self._ensure(name)` then
    set `self._plugins[name]["last_error"] = exc.__class__.__name__`. NEVER assign `str(exc)`. Do NOT
    change get_snapshot — it already includes non-underscore keys. No fenced code in this action;
    implement via the editor.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && rtk pytest tests/test_api_observability.py -k "last_error or scrubbed" -x 2>&1 | grep -Ei "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_api_observability.py -k "last_error or scrubbed"` passes (test_health_last_error_scrubbed GREEN)
    - `grep -n "def record_last_error" core/health.py` returns one line
    - `grep -n "exc.__class__.__name__" core/health.py` returns a match; `grep -n "str(exc)" core/health.py` returns NO match
    - `grep -n "\"last_error\": None" core/health.py` confirms _ensure initializes the field
    - `python -c "from core.health import HealthRegistry; r=HealthRegistry(); r.heartbeat('P'); print(r.get_snapshot()['P']['last_error'])"` prints `None` (no KeyError)
  </acceptance_criteria>
  <done>record_last_error stores class name only; _ensure initializes last_error=None; snapshot includes it for all plugins; scrub test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record scrubbed last_error in orchestrator supervise() except block</name>
  <files>core/orchestrator.py</files>
  <read_first>
    - core/orchestrator.py (lines 125-155 supervise except block; line 132 existing exc.__class__.__name__ usage; lines 136-138 record_error call site — insertion point)
    - core/health.py (record_last_error added in Task 1 — the method being called)
    - .planning/phases/26-read-only-api-endpoints/26-PATTERNS.md (lines 268-300 orchestrator one-line addition + exact insertion point)
    - .planning/phases/26-read-only-api-endpoints/26-RESEARCH.md (lines 229-237 supervise call-site; line 278 anti-pattern: no str(exc) in the recording path)
  </read_first>
  <behavior>
    - When a plugin raises in supervise(), health.record_last_error(plugin_name, exc) is called once, alongside the existing health.record_error(plugin_name)
    - The degraded-threshold / park / relaunch logic is unchanged
    - No str(exc) is introduced into the health recording path
  </behavior>
  <action>
    In core/orchestrator.py, inside the `except Exception as exc:` block, immediately after the
    existing `health.record_error(plugin_name)` (line 138) and still within the `if health is not None:`
    guard, add one line: `health.record_last_error(plugin_name, exc)`. Place it before the
    degraded-threshold comment/logic at line 139. Change nothing else — do not alter the writeLog,
    failure_times window, degraded/park/relaunch branches. The exc.__class__.__name__ scrub happens
    inside record_last_error (Task 1), so the call passes the raw exc object; that is correct and the
    scrubbing boundary is the registry method. No fenced code in this action; implement via the editor.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && rtk pytest tests/ -k "orchestrator or supervise or health or credential or last_error or scrubbed" -x 2>&1 | grep -Ei "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "health.record_last_error(plugin_name, exc)" core/orchestrator.py` returns exactly one line, inside the except block, after record_error
    - `grep -n "str(exc)" core/orchestrator.py` shows no NEW str(exc) introduced into the health-recording path (any pre-existing writeLog uses exc.__class__.__name__)
    - `rtk pytest tests/ -k "orchestrator or supervise or health"` passes (no regression to supervisor/degraded/park tests)
  </acceptance_criteria>
  <done>supervise() records scrubbed last_error alongside record_error; degraded/park/relaunch logic untouched; supervisor tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Confirm credential-leak guard is GREEN end-to-end</name>
  <files>core/health.py, core/orchestrator.py</files>
  <read_first>
    - tests/test_api_observability.py (the credential/leak tests authored in Plan 01 Task 3 — the GREEN target)
    - .planning/phases/26-read-only-api-endpoints/26-RESEARCH.md (lines 239-273 Pattern 5 credential assertion; lines 356-359 Pitfall 4 real registry)
    - .planning/phases/26-read-only-api-endpoints/26-CONTEXT.md (lines 44-49 CI guard targets status/SSE JSON only, NOT /api/logs content)
  </read_first>
  <behavior>
    - test_get_status_no_credential_leak GREEN: json.dumps(get_status() payload with real-registry snapshot) has no CRED_PATTERN match
    - test_api_status_no_credential_leak GREEN: GET /api/status response text has no CRED_PATTERN match
    - The guard does NOT touch /api/logs content (operator log files are not scrubbed)
  </behavior>
  <action>
    No production code change should be needed here if Tasks 1-2 are correct; this task is the
    end-to-end credential-guard gate. Run the credential tests; if any fail, the failure points to a
    last_error that stored more than the class name OR a snapshot leaking a value — trace it back to
    record_last_error / _ensure in core/health.py and fix THERE (never by scrubbing /api/logs or by
    post-processing the response). Verify by inspection that no code path stores str(exc) into the
    snapshot. If the test seeds a credential-shaped last_error and still passes, the invariant holds.
    Do not modify the test assertions to make them pass. No fenced code in this action.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && rtk pytest tests/test_api_observability.py -k "credential or leak or scrubbed" -x 2>&1 | grep -Ei "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_api_observability.py -k "credential or leak or scrubbed"` passes (all credential/scrub tests GREEN)
    - `rtk pytest tests/ -x` full suite GREEN
    - No assertion in tests/test_api_observability.py was weakened to pass (git diff of the test file shows no change vs Plan 01)
    - `grep -rn "str(exc)" core/health.py` returns no match (scrub invariant intact)
  </acceptance_criteria>
  <done>Credential-leak guard GREEN end-to-end; full suite passes; scrub invariant verified (no str(exc) in the snapshot path); test assertions unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exception object → health snapshot | str(exc) may carry proxy creds / CAPTCHA API keys; only the class name may cross into the snapshot |
| get_status()/`/api/status` JSON → browser | Snapshot is serialized to the browser; must contain no credential-pattern strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | Information Disclosure | last_error in health snapshot (PRIMARY SSE-03 threat) | mitigate | Store exc.__class__.__name__ ONLY at the record_last_error boundary; never str(exc); acceptance_criteria greps confirm no str(exc) in core/health.py |
| T-26-09 | Information Disclosure | get_status()/`/api/status` JSON to browser | mitigate | CI credential-pattern assertion (re `(@|password|token|key=|cvv)` IGNORECASE) over json.dumps(get_status()) and raw /api/status text using a REAL HealthRegistry (Pitfall 4) |
| T-26-10 | Repudiation | over-mocked registry hides a real leak | mitigate | Credential test constructs a real HealthRegistry and exercises get_snapshot(); test assertions must not be weakened (git-diff check) |
| T-26-11 | Information Disclosure | accidental scrubbing of operator log content | accept | CONTEXT.md decision: do NOT scrub /api/logs (operator's own files); guard targets status/SSE JSON only |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs; stdlib (re, json) + existing pytest only; RESEARCH Package Legitimacy Audit: not applicable |
</threat_model>

<verification>
- `rtk pytest tests/test_api_observability.py -k "credential or leak or last_error or scrubbed"` GREEN.
- `rtk pytest tests/ -x` full suite GREEN (no supervisor/health regression).
- `grep -rn "str(exc)" core/health.py` returns nothing (scrub invariant).
- No new Python packages (requirements.txt unchanged).
</verification>

<success_criteria>
- ROADMAP criterion 4 satisfied: CI test asserts no credential-pattern strings in /api/status JSON or get_status() payload; last_error scrubbed to exc.__class__.__name__ only.
- supervise() records the scrubbed last_error at the exception boundary without disturbing the supervisor logic.
- Operator log content is never scrubbed.
</success_criteria>

<output>
Create `.planning/phases/26-read-only-api-endpoints/26-03-SUMMARY.md` when done.
</output>
