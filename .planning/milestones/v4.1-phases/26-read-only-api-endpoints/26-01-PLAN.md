---
phase: 26-read-only-api-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_api_observability.py
  - tests/test_models.py
autonomous: true
requirements: [OBS-08, SSE-03]
must_haves:
  truths:
    - "A failing test exists for every Phase 26 ROADMAP success criterion before any implementation runs"
    - "Test file imports succeed and the suite collects without errors (RED state, not import errors)"
  artifacts:
    - path: "tests/test_api_observability.py"
      provides: "TestClient tests for /api/history, /api/price-history, /api/logs filtering, and credential-leak guard"
      min_lines: 90
      contains: "def test_"
    - path: "tests/test_models.py"
      provides: "get_confirmed_orders_sync model test (added or created)"
      contains: "get_confirmed_orders_sync"
  key_links:
    - from: "tests/test_api_observability.py"
      to: "web.create_app"
      via: "TestClient(create_app(mock_svc))"
      pattern: "TestClient\\(create_app"
    - from: "tests/test_api_observability.py"
      to: "web.routes.api.get_confirmed_orders_sync"
      via: "unittest.mock.patch on the import name in web.routes.api"
      pattern: "patch\\(\"web.routes.api"
---

<objective>
Create the Wave 0 RED test scaffold for Phase 26. Every ROADMAP success criterion gets a failing
pytest test before any implementation. The tests reference symbols that do not yet exist
(`get_confirmed_orders_sync`, `/api/history`, `/api/price-history/{link_b64}`, `read_logs_filtered`,
`last_error`), so the suite must fail on assertions/404s — NOT on collection/import errors.

Purpose: Establish the Nyquist verification baseline so Plans 02 and 03 implement against a fixed
contract. Tests are the executable spec for both implementation plans.
Output: `tests/test_api_observability.py` (new) and additions to `tests/test_models.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-read-only-api-endpoints/26-RESEARCH.md
@.planning/phases/26-read-only-api-endpoints/26-PATTERNS.md
@.planning/phases/26-read-only-api-endpoints/26-VALIDATION.md

<interfaces>
<!-- Contracts the tests encode. Extracted from codebase + RESEARCH. Executor uses these directly. -->

Existing test fixture pattern — tests/test_web_dashboard.py lines 1-21:
- `import pytest; pytest.importorskip("fastapi")`
- `from unittest.mock import MagicMock, patch`
- `from fastapi.testclient import TestClient`
- `mock_svc` fixture: `MagicMock()` with `svc.get_status.return_value` and `svc.list_items.return_value = []`
- `client` fixture: `from web import create_app; return TestClient(create_app(mock_svc))`

Router mount (web/__init__.py): api_router mounted at prefix="/api" → routes are
`/api/status`, `/api/logs`, `/api/history`, `/api/price-history/{link_b64}`.

Endpoint contracts (from 26-CONTEXT.md locked decisions):
- GET /api/history → {"confirmed_orders": [{name, order_id, confirmed_at, checkout_attempts}]}; [] when none.
- GET /api/price-history/{link_b64} → {"series": [{"t": <iso>, "price": <float>}]}; {"series": []} for non-Amazon / bad b64.
- GET /api/logs?level=&search=&n= → {"logs": [...]}; AND-combined filters; no params == read_recent_logs(50) behavior.
- get_status() plugins snapshot per-plugin dict gains "last_error" key (class name or None).

Model contract (RESEARCH lines 311-324):
- get_confirmed_orders_sync() → list of (name, order_id, confirmed_at, checkout_attempts) tuples for purchased=1 rows; [] when none.

Patch target rule (26-PATTERNS.md lines 346-349): patch the IMPORT NAME inside web.routes.api,
not the source module — e.g. patch("web.routes.api.get_confirmed_orders_sync", ...).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing TestClient tests for the three read endpoints</name>
  <files>tests/test_api_observability.py</files>
  <read_first>
    - tests/test_web_dashboard.py (lines 1-21: mock_svc + client fixtures to copy verbatim; lines 77-82: patch() usage)
    - .planning/phases/26-read-only-api-endpoints/26-PATTERNS.md (lines 304-385: full test scaffold patterns + patch targets)
    - .planning/phases/26-read-only-api-endpoints/26-VALIDATION.md (lines 47-58: Wave 0 required test list)
    - .planning/phases/26-read-only-api-endpoints/26-RESEARCH.md (lines 466-502: per-requirement test map + fixture pattern)
  </read_first>
  <behavior>
    - test_get_history_empty: patch web.routes.api.get_confirmed_orders_sync -> [], GET /api/history is 200 and json == {"confirmed_orders": []}
    - test_get_history_with_orders: patch returns one tuple ("Widget A","ORD-001","2026-01-01T00:00:00+00:00",1); json["confirmed_orders"][0] has keys name/order_id/confirmed_at/checkout_attempts with order_id == "ORD-001"
    - test_get_price_history_with_data: patch web.routes.api.get_price_history_sync -> newest-first rows [(2599,"USD","2026-01-02T00:00:00+00:00"),(1999,"USD","2026-01-01T00:00:00+00:00")]; GET /api/price-history/<valid b64 of an amazon link> is 200; json["series"] oldest-first so series[0]["t"] <= series[-1]["t"]; price is cents/100 (19.99 first)
    - test_get_price_history_empty: patch get_price_history_sync -> []; GET /api/price-history/<valid b64> is 200 and json == {"series": []}
    - test_get_price_history_bad_link: GET /api/price-history/!!!notbase64!!! is 200 and json == {"series": []} (no error, no 400)
    - test_logs_filtered_level_and_search: patch web.routes.api.read_logs_filtered (or web.log_reader.read_logs_filtered as imported into api) to assert it is called with (min(n,500), level, search) and GET /api/logs?level=ERROR&search=captcha&n=50 returns its output under "logs"
    - test_logs_no_params_default_behavior: GET /api/logs with no params returns {"logs": [...]} equivalent to read_recent_logs(50) (assert the filtered call receives n=50, level=None, search=None)
  </behavior>
  <action>
    Create tests/test_api_observability.py. Copy the `mock_svc` and `client` fixtures verbatim from
    tests/test_web_dashboard.py lines 10-21, but extend `mock_svc.get_status.return_value` to
    {"running": False, "uptime_secs": 0.0, "plugins": {}} so the credential test (Task 3) and the
    status route have a realistic payload shape. Use `base64.urlsafe_b64decode`/`urlsafe_b64encode`
    from stdlib `base64` to build the valid `link_b64` fixtures (encode an `amazon.com` product URL).
    Patch the DB/file functions at their import name inside web.routes.api per 26-PATTERNS.md lines
    346-349: patch("web.routes.api.get_confirmed_orders_sync"),
    patch("web.routes.api.get_price_history_sync"), patch("web.routes.api.read_logs_filtered").
    Write all seven test functions named exactly per the behavior block. These MUST currently fail
    with 404 (routes absent), AttributeError on patch (symbol not yet imported into api), or assertion
    mismatch — NOT with a collection/import error. If patch raises AttributeError because the symbol
    is not yet importable into web.routes.api, that is an acceptable RED signal; do NOT add the symbol
    here. Do NOT place fenced code in this file beyond what pytest needs; this IS the test file.
    No fenced code blocks in this action — write the actual test module via the editor.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && rtk pytest tests/test_api_observability.py -x 2>&1 | grep -Eiv "PASSED" | grep -Ei "failed|error|collected"</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_api_observability.py --collect-only -q` lists all 7 test functions (collection succeeds — no ImportError)
    - `rtk pytest tests/test_api_observability.py -x` exits non-zero (RED): failures are 404 / assertion / patch-AttributeError, NOT a module-collection error
    - File contains the `client` fixture using `TestClient(create_app(mock_svc))`
    - File contains `patch("web.routes.api.get_confirmed_orders_sync"` and `patch("web.routes.api.get_price_history_sync"`
  </acceptance_criteria>
  <done>Seven failing endpoint tests collected cleanly; suite is RED on assertions/404s, not on imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing model test for get_confirmed_orders_sync</name>
  <files>tests/test_models.py</files>
  <read_first>
    - tests/test_models.py (existing structure, initialize_db / get_db_connection usage, fixture for a temp DB if present)
    - models.py (lines 93-145: get_items_sync, update_item_confirmed_sync, increment_checkout_attempts_sync — used to seed a confirmed row)
    - .planning/phases/26-read-only-api-endpoints/26-PATTERNS.md (lines 170-201: get_confirmed_orders_sync contract + placement)
    - .planning/phases/26-read-only-api-endpoints/26-VALIDATION.md (lines 55-56: model test requirement)
  </read_first>
  <behavior>
    - test_get_confirmed_orders_sync_empty: fresh DB with no purchased rows -> get_confirmed_orders_sync() returns []
    - test_get_confirmed_orders_sync_returns_confirmed: seed one item, mark confirmed via update_item_confirmed_sync(link, "ORD-7", "2026-01-01T00:00:00+00:00"), then get_confirmed_orders_sync() returns one tuple whose [1]=="ORD-7" and [2]=="2026-01-01T00:00:00+00:00" and length 4 (name, order_id, confirmed_at, checkout_attempts)
    - test_get_confirmed_orders_sync_excludes_unpurchased: seed an item that is never purchased -> it does NOT appear in the result
  </behavior>
  <action>
    Add three test functions to tests/test_models.py following the existing DB-setup convention in that
    file (use the same fixture/initialize_db(delete=True) pattern the file already uses for hermetic
    SQLite). Import `get_confirmed_orders_sync` from models. Seed rows with the existing
    `add_items_sync` and confirm with `update_item_confirmed_sync` so the test exercises the real
    items table columns order_id/confirmed_at/checkout_attempts. These tests MUST fail now with
    ImportError on `get_confirmed_orders_sync` (function not yet defined) — that is the RED signal for
    Plan 02. If tests/test_models.py uses a module-level DB fixture, integrate cleanly so the new
    tests do not break existing ones. No fenced code in this action — write the test functions via the
    editor.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && rtk pytest tests/test_models.py -k confirmed_orders 2>&1 | grep -Ei "failed|error|passed|collected"</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_models.py -k confirmed_orders` selects 3 tests and they FAIL (RED), with the failure attributable to missing `get_confirmed_orders_sync`
    - Existing tests in tests/test_models.py still collect (no syntax/import breakage introduced)
    - The three new test function names contain `confirmed_orders`
  </acceptance_criteria>
  <done>Three failing model tests for get_confirmed_orders_sync added; existing model tests still collect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing credential-leak + last_error scrub tests</name>
  <files>tests/test_api_observability.py</files>
  <read_first>
    - core/health.py (lines 19-28 _ensure dict; lines 71-76 get_snapshot — confirm last_error NOT yet present)
    - .planning/phases/26-read-only-api-endpoints/26-PATTERNS.md (lines 360-385: credential-pattern test using a REAL HealthRegistry)
    - .planning/phases/26-read-only-api-endpoints/26-RESEARCH.md (lines 239-273 Pattern 5; lines 356-359 Pitfall 4 — must use real HealthRegistry, not MagicMock)
  </read_first>
  <behavior>
    - test_get_status_no_credential_leak: build a REAL HealthRegistry, _ensure("TestPlugin"), set its last_error to "ConnectionError"; wrap registry.get_snapshot() inside a get_status() payload; json.dumps the payload; assert CRED_PATTERN (re.compile(r'(@|password|token|key=|cvv)', re.IGNORECASE)) finds no match
    - test_api_status_no_credential_leak: GET /api/status via the client (mock_svc.get_status returns a plugins snapshot from a real HealthRegistry with a seeded last_error); assert the raw response text has no CRED_PATTERN match
    - test_health_last_error_scrubbed: REAL HealthRegistry; call record_last_error("P", RuntimeError("proxy http://user:pass@host token=abc")); assert get_snapshot()["P"]["last_error"] == "RuntimeError" (class name only, never str(exc)); assert "@" not in get_snapshot()["P"]["last_error"]
  </behavior>
  <action>
    Append three tests to tests/test_api_observability.py. Define module-level
    CRED_PATTERN = re.compile(r'(@|password|token|key=|cvv)', re.IGNORECASE) (the `re`/`json` imports
    mirror test_web_dashboard.py line 231-233 usage). Per Pitfall 4 (RESEARCH lines 356-359) use a
    REAL `from core.health import HealthRegistry` instance — NEVER a MagicMock registry — so the test
    exercises the actual get_snapshot scrubbing path. test_health_last_error_scrubbed seeds a
    deliberately credential-shaped exception message to prove only the class name is stored. These
    tests MUST fail now: record_last_error does not exist yet (AttributeError) and last_error is not
    in the snapshot. That is the RED signal for Plan 03. No fenced code in this action — author the
    tests in the editor.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && rtk pytest tests/test_api_observability.py -k "credential or last_error or scrubbed" 2>&1 | grep -Ei "failed|error|passed|collected"</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_api_observability.py -k "credential or last_error or scrubbed"` selects 3 tests and they FAIL (RED) due to missing `record_last_error` / absent `last_error` key
    - CRED_PATTERN regex literal is present exactly as `r'(@|password|token|key=|cvv)'` with `re.IGNORECASE`
    - The scrub test constructs a REAL `HealthRegistry()` (grep shows `HealthRegistry()` not a MagicMock for the registry under test)
  </acceptance_criteria>
  <done>Three failing credential/scrub tests added using a real HealthRegistry; suite RED on missing record_last_error/last_error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test author → test suite | Tests are trusted code; risk is false-confidence (tests that pass without exercising the real path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Repudiation | credential-leak test uses MagicMock registry | mitigate | Force a REAL HealthRegistry in the credential/scrub tests (Pitfall 4); acceptance_criteria greps for `HealthRegistry()` |
| T-26-02 | Information Disclosure | scrub test only checks class name, not str(exc) leakage | mitigate | test_health_last_error_scrubbed seeds a credential-shaped exception message and asserts only the class name is stored and "@" is absent |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (stdlib + existing pytest/fastapi only); RESEARCH Package Legitimacy Audit: "not applicable" |
</threat_model>

<verification>
- `rtk pytest tests/test_api_observability.py --collect-only -q` collects all 10 tests with no ImportError.
- `rtk pytest tests/test_api_observability.py tests/test_models.py -k "confirmed_orders or history or price or logs or credential or last_error or scrubbed"` is RED (every Phase 26 behavior has a failing test).
- No new Python packages added (no requirements.txt change).
</verification>

<success_criteria>
- All 10 Phase 26 behaviors (7 endpoint + 3 scrub/leak) plus 3 model tests exist and FAIL for the right reason (missing implementation, not broken collection).
- Tests encode the locked CONTEXT.md contracts verbatim (envelope keys, empty-series, scrub-to-class-name).
- The credential/scrub tests use a real HealthRegistry.
</success_criteria>

<output>
Create `.planning/phases/26-read-only-api-endpoints/26-01-SUMMARY.md` when done.
</output>
