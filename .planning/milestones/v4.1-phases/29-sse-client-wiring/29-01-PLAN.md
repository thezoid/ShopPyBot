---
phase: 29-sse-client-wiring
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_sse_wiring.py
autonomous: true
requirements: [SSE-01]
must_haves:
  truths:
    - "Running pytest on tests/test_sse_wiring.py fails (RED) because the SSE wiring is not yet present in dashboard.html"
    - "Every automatable Phase 29 assertion (W29-A1..A11) has a corresponding test"
  artifacts:
    - path: "tests/test_sse_wiring.py"
      provides: "Static-template RED scaffold for SSE client wiring (W29-A1..A11)"
      contains: "def test_"
  key_links:
    - from: "tests/test_sse_wiring.py"
      to: "GET / served dashboard.html"
      via: "TestClient(create_app(mock_svc)).get('/')"
      pattern: "client.get\\(.?/.?\\)"
---

<objective>
Create the Wave 0 RED test scaffold `tests/test_sse_wiring.py` that asserts the static
SSE-wiring surface of the served dashboard HTML (W29-A1 through W29-A11 from
29-RESEARCH.md). These tests MUST fail now (the wiring does not exist yet) and turn GREEN
as Plan 02 and Plan 03 land the renderStatus refactor, the indicator element, the no-dup
guard, and the EventSource block.

Purpose: Establishes the automated feedback surface for SSE-01 before any implementation,
per the Nyquist rule. The 5 live behaviors (DevTools one-stream, 1-2s updates, no-dup,
reconnect, no-EventSource fallback) are inherently manual UAT and are NOT faked as
automated tests here — only the static template surface and regression guards are automated.
Output: tests/test_sse_wiring.py (new file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-sse-client-wiring/29-RESEARCH.md
@.planning/phases/29-sse-client-wiring/29-VALIDATION.md

<interfaces>
<!-- Test fixture pattern to copy verbatim — established in tests/test_observability_ui.py -->
From tests/test_observability_ui.py (lines 10-29):
```python
import pytest
pytest.importorskip("fastapi")
from unittest.mock import MagicMock
from fastapi.testclient import TestClient

@pytest.fixture
def mock_svc():
    svc = MagicMock()
    svc.get_status.return_value = {"running": False, "uptime_secs": 0, "plugins": {}}
    svc.list_items.return_value = []
    return svc

@pytest.fixture
def client(mock_svc):
    from web import create_app
    return TestClient(create_app(mock_svc))
```

W29 automatable assertion table (29-RESEARCH.md, "Automatable vs Manual UAT Split"):
- W29-A1: `new EventSource('/api/events')` substring present in resp.text
- W29-A2: `addEventListener('status'` present
- W29-A3: `addEventListener('log'` present
- W29-A4: `typeof EventSource` present
- W29-A5: `renderStatus` present
- W29-A6: `_lastLogLine` present
- W29-A7: `id="sse-indicator"` present (indicator element)
- W29-A11: both `setInterval(pollStatus` and `setInterval(pollLogs` appear only inside the `else` fallback branch of the `typeof EventSource` feature-detect
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write static-template RED scaffold tests/test_sse_wiring.py</name>
  <files>tests/test_sse_wiring.py</files>
  <read_first>
    - tests/test_observability_ui.py (copy the mock_svc + client fixtures verbatim; same create_app(mock_svc) pattern)
    - .planning/phases/29-sse-client-wiring/29-RESEARCH.md ("Automatable vs Manual UAT Split" table W29-A1..A11; "Surgical Change Map")
    - .planning/phases/29-sse-client-wiring/29-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    Create tests/test_sse_wiring.py. Copy the `pytest.importorskip("fastapi")`, `mock_svc`,
    and `client` fixtures from tests/test_observability_ui.py exactly (same shape). Add one
    test function per automatable assertion, each issuing `resp = client.get("/")`, asserting
    `resp.status_code == 200`, then a substring/regex check:
    - test_eventsource_constructed: assert `new EventSource('/api/events')` in resp.text (W29-A1).
    - test_named_status_listener: assert `addEventListener('status'` in resp.text (W29-A2).
    - test_named_log_listener: assert `addEventListener('log'` in resp.text (W29-A3).
    - test_no_onmessage_for_named_events: assert `.onmessage` NOT in resp.text (anti-pattern guard
      from 29-RESEARCH Pitfall 1; named events require addEventListener).
    - test_feature_detect_present: assert `typeof EventSource` in resp.text (W29-A4).
    - test_render_status_extracted: assert `renderStatus` in resp.text (W29-A5; shared render fn).
    - test_no_dup_guard_present: assert `_lastLogLine` in resp.text (W29-A6).
    - test_indicator_element_present: assert `id="sse-indicator"` in resp.text (W29-A7).
    - test_setinterval_only_in_fallback (W29-A11): regex-extract the substring of resp.text from
      the first occurrence of `typeof EventSource` onward; assert that both `setInterval(pollStatus`
      and `setInterval(pollLogs` occur only AFTER an `else` token within that feature-detect block
      (i.e. find the index of `} else {` or `else {` after `typeof EventSource`, and assert each
      `setInterval(poll...` index is greater than that else index). This proves no polling fires on
      the SSE-active path (criterion 1).
    Add a module docstring stating this is the Phase 29 Wave 0 RED scaffold (SSE-01) and that
    the 5 live criteria are manual UAT (not automated here). Add inline comments mapping each
    test to its W29-Ax id. Do NOT add tests for the live/timing/reconnect criteria — those are
    operator UAT per 29-VALIDATION.md.
    Do NOT duplicate `test_no_innerHTML_with_api_data` or `test_no_hardcoded_hex_in_components`
    (W29-A8/A9 live in test_web_dashboard.py / test_design_system.py — reference them, do not copy).
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_sse_wiring.py exists and is collected by pytest (no import/collection errors).
    - The static-wiring tests FAIL (RED) because dashboard.html does not yet contain
      EventSource/renderStatus/_lastLogLine/sse-indicator wiring. (test_no_onmessage_for_named_events
      may already pass since `.onmessage` is currently absent — that is acceptable; it guards Plan 03.)
    - No new packages installed (pytest + fastapi already present).
  </acceptance_criteria>
  <done>
    tests/test_sse_wiring.py committed; running `rtk pytest tests/test_sse_wiring.py -q` shows
    the SSE-wiring assertions failing (RED), confirming the scaffold targets behavior not yet built.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → create_app | In-process TestClient; no untrusted input; mock service only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | tests/test_sse_wiring.py asserting wrong surface | mitigate | Assertions derived verbatim from 29-RESEARCH W29-A1..A11 table; no live/timing tests faked |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (29-RESEARCH Package Legitimacy Gate: skipped); pytest + fastapi already installed |
</threat_model>

<verification>
- `rtk pytest tests/test_sse_wiring.py -q` collects and runs (RED expected at Wave 0).
- `rtk pytest tests/test_observability_ui.py tests/test_web_dashboard.py tests/test_design_system.py -q`
  remains GREEN (this plan adds a new test file only; touches no production code).
</verification>

<success_criteria>
- New file tests/test_sse_wiring.py present with one test per W29-A1..A7 + W29-A11 + onmessage guard.
- SSE-wiring assertions are RED (target behavior not yet implemented).
- No production files modified; no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/29-sse-client-wiring/29-01-SUMMARY.md` when done.
</output>
