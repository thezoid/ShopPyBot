---
phase: 29-sse-client-wiring
plan: 03
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - web/templates/dashboard.html
autonomous: false
requirements: [SSE-01]
must_haves:
  truths:
    - "When EventSource is available, the dashboard opens one persistent EventSource('/api/events') and starts NO setInterval polls (manual UAT: criterion 1)"
    - "SSE 'status' events render via renderStatus(data); SSE 'log' events append via appendLogLine (manual UAT: criteria 2, 3)"
    - "onopen sets the indicator to Live; onerror sets it to Reconnecting; native EventSource auto-reconnect handles reconnection (manual UAT: criterion 4)"
    - "When EventSource is unavailable, the existing setInterval pollStatus/pollLogs fallback runs and no JavaScript error is thrown (manual UAT: criterion 5)"
    - "On load, a one-shot /api/status paint and /api/logs backfill run before SSE takes over"
  artifacts:
    - path: "web/templates/dashboard.html"
      provides: "EventSource feature-detect block, named status/log listeners, onopen/onerror indicator, backfill one-shots, polling fallback"
      contains: "typeof EventSource"
  key_links:
    - from: "web/templates/dashboard.html SSE 'status' listener"
      to: "renderStatus(data)"
      via: "es.addEventListener('status', e => renderStatus(JSON.parse(e.data)))"
      pattern: "addEventListener\\('status'"
    - from: "web/templates/dashboard.html SSE 'log' listener"
      to: "appendLogLine(line)"
      via: "es.addEventListener('log', e => appendLogLine(JSON.parse(e.data).line))"
      pattern: "addEventListener\\('log'"
    - from: "web/templates/dashboard.html onopen/onerror"
      to: "#sse-indicator (setIndicator)"
      via: "setIndicator('live'|'reconnecting')"
      pattern: "setIndicator\\("
---

<objective>
Replace the two `setInterval` polls (dashboard.html lines 571-574) with a single
feature-detected EventSource('/api/events') connection that drives live status and log updates,
keeping the existing polling as a fallback when EventSource is unavailable. This is the
rollback-last step: it changes only the update TRIGGER (poll → SSE), reusing the renderStatus,
appendLogLine, renderLogLines, and #sse-indicator artifacts that Plan 02 already landed.
Reverting this single block restores Phase 28 polling behavior.

Purpose: Delivers SSE-01 — the dashboard receives live status + log updates over one SSE stream
replacing the 2s polling loop, with a clean fallback and a Live/Reconnecting indicator.
Implements the remaining CONTEXT locked decisions: named-event listeners, feature-detect
fallback, native auto-reconnect (no manual reconnect loop), onopen→Live / onerror→Reconnecting,
one-shot backfill then SSE drives updates, no setInterval when SSE active.
Output: modified web/templates/dashboard.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-sse-client-wiring/29-CONTEXT.md
@.planning/phases/29-sse-client-wiring/29-RESEARCH.md

<interfaces>
<!-- Contracts already in place from Phase 27/28 + Plan 02. Reuse verbatim; do not re-implement. -->

Server SSE frame format (web/sse_hub.py / web/routes/sse.py, Phase 27):
  event: status\ndata: {"running": true, "uptime_secs": 42, "plugins": {...}}\n\n
  event: log\ndata: {"line": "[INFO] ... — Bot started"}\n\n
  : keep-alive   (every ~15s idle)
  retry: 3000    (browser waits 3s before native reconnect)
Named events require addEventListener('status'|'log'), NOT es.onmessage (Pitfall 1).
e.data is the post-`data:` string; always JSON.parse it.

Functions/elements available after Plan 02 (do not redefine):
- renderStatus(data)            — pure shared renderer (status dot/label, start/stop buttons, uptime, health cards)
- appendLogLine(lineText)       — appends one line, has the _lastLogLine no-dup guard + 500-line cap
- renderLogLines(lines)         — one-shot batch render (innerHTML='' then re-render) — backfill ONLY
- maybeScrollToBottom()         — respects logFollowEnabled
- pollStatus() / pollLogs()     — fetch wrappers; pollStatus now delegates to renderStatus
- #sse-indicator / #sse-dot / #sse-label — indicator element in sticky header
- POLL_MS = 2000                — fallback interval constant
Current replace target (dashboard.html lines 571-574): setInterval(pollStatus, POLL_MS);
setInterval(pollLogs, POLL_MS); pollStatus(); pollLogs();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setIndicator(state) helper</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html (the #sse-indicator markup added in Plan 02; .status-dot.running / .status-dot.reconnecting classes)
    - .planning/phases/29-sse-client-wiring/29-RESEARCH.md ("Pattern 4: Live/Reconnecting Indicator"; Pitfall 8 textContent-only)
    - .planning/phases/29-sse-client-wiring/29-CONTEXT.md (--color-status-ok for Live via .running, --color-status-warn for Reconnecting via .reconnecting)
  </read_first>
  <action>
    In web/templates/dashboard.html, add a pure helper setIndicator(state) that takes the string
    'live' or 'reconnecting'. It reads #sse-dot and #sse-label via getElementById. For 'live':
    set the dot className to 'status-dot running' (resolves to --color-status-ok) and the label
    textContent to 'Live'. For 'reconnecting' (anything not 'live'): set the dot className to
    'status-dot reconnecting' (resolves to --color-status-warn, added in Plan 02) and the label
    textContent to 'Reconnecting'. Use textContent only — never innerHTML (Pitfall 8 / safe-DOM
    guard). The label strings are static literals, not API data. Place setIndicator near the other
    helpers, before the wiring block in Task 2.
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py tests/test_design_system.py -q</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.html contains `function setIndicator` toggling #sse-dot class between
      'status-dot running' and 'status-dot reconnecting' and #sse-label between 'Live'/'Reconnecting'.
    - No innerHTML introduced (test_no_innerHTML_with_api_data stays GREEN).
    - No hardcoded color in JS (colors come from the CSS token classes added in Plan 02).
  </acceptance_criteria>
  <done>
    setIndicator('live') / setIndicator('reconnecting') updates the header indicator dot color and
    label text via textContent and the token-driven status-dot classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace setInterval polls with feature-detected EventSource wiring</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html (lines 571-574 setInterval/immediate calls; pollStatus/pollLogs; renderStatus/appendLogLine/maybeScrollToBottom)
    - .planning/phases/29-sse-client-wiring/29-RESEARCH.md ("Pattern 1: Named Event Listener", "Pattern 2: Feature Detect + Fallback", "Surgical Change Map" item 4; Pitfalls 1, 2, 4; Open Question 2 recommendation)
    - .planning/phases/29-sse-client-wiring/29-CONTEXT.md (all SSE wiring locked decisions: named listeners, feature-detect, native reconnect only, onopen/onerror indicator, backfill-then-SSE, no setInterval when SSE active)
  </read_first>
  <action>
    In web/templates/dashboard.html, replace the four lines `setInterval(pollStatus, POLL_MS);`
    / `setInterval(pollLogs, POLL_MS);` / `pollStatus();` / `pollLogs();` with this structure:
    First run the one-shot backfill regardless of path — call pollStatus() once and pollLogs()
    once (Open Question 2 recommendation: reuse the already-tested fetch wrappers for the initial
    /api/status paint and /api/logs backfill; do NOT inline fresh fetches). Then branch on
    `if (typeof EventSource !== 'undefined') { ... } else { ... }`.
    SSE branch: create `var es = new EventSource('/api/events');`. Register
    es.addEventListener('status', function(e) { renderStatus(JSON.parse(e.data)); }); and
    es.addEventListener('log', function(e) { var p = JSON.parse(e.data); appendLogLine(p.line);
    maybeScrollToBottom(); }); Set es.onopen = function() { setIndicator('live'); }; and
    es.onerror = function() { setIndicator('reconnecting'); };. Do NOT use es.onmessage (Pitfall 1:
    named events never fire onmessage). Do NOT create a manual setTimeout/new EventSource reconnect
    loop inside onerror (Pitfall 2: native reconnect via server retry:3000 already handles it;
    onerror is indicator-only). Do NOT call renderLogLines on log events (Pitfall 4: that clears
    and re-renders the buffer; SSE log events use appendLogLine only). Do NOT start any setInterval
    in the SSE branch (criterion 1: zero polling when SSE connected).
    Fallback (else) branch: `setInterval(pollStatus, POLL_MS); setInterval(pollLogs, POLL_MS);`
    — the existing Phase 28 polling behavior, with no JS error (criterion 5). The backfill
    pollStatus()/pollLogs() already ran above, so the else branch only needs the intervals.
    Leave pollStatus, pollLogs, the filter-control IIFE (which still calls pollLogs for manual
    refresh/level/search), and the polling catch block all unchanged. This is the rollback-last
    change: reverting this single block restores Phase 28 polling.
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py tests/test_web_dashboard.py tests/test_observability_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.html contains `new EventSource('/api/events')` (W29-A1), `addEventListener('status'`
      (W29-A2), `addEventListener('log'` (W29-A3), `typeof EventSource` (W29-A4).
    - Both `setInterval(pollStatus` and `setInterval(pollLogs` appear ONLY inside the `else` branch
      of the feature-detect (W29-A11 GREEN): no setInterval on the SSE-active path.
    - `.onmessage` does NOT appear anywhere in dashboard.html (named-event guard).
    - es.onerror sets the indicator only — no `new EventSource` or setTimeout reconnect inside it.
    - SSE log events call appendLogLine (not renderLogLines).
    - All tests in tests/test_sse_wiring.py are GREEN; test_web_dashboard.py + test_observability_ui.py
      stay GREEN (renderStatus/appendLogLine/renderLogLines unchanged).
  </acceptance_criteria>
  <done>
    The two setInterval polls are replaced by a feature-detected EventSource block: named
    status/log listeners drive renderStatus/appendLogLine, onopen/onerror drive the indicator,
    native reconnect is relied upon, a one-shot backfill runs on load, and polling remains as the
    no-EventSource fallback. SSE-01 static surface fully wired; reverting this block restores polling.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT — verify the 5 live SSE-01 criteria (DevTools / live bot)</name>
  <what-built>
    SSE client wiring is complete: the dashboard opens one EventSource('/api/events'), renders
    live status via renderStatus and live log lines via appendLogLine (with the no-dup guard),
    shows a Live/Reconnecting indicator in the header, and falls back to polling when EventSource
    is unavailable. The 5 SSE-01 success criteria are live-behavior / DevTools observations that
    TestClient cannot exercise (synchronous HTTP, no real browser), so they require operator UAT.
  </what-built>
  <how-to-verify>
    Run the dashboard: `python main.py`, open http://localhost:8000 in a browser.
    1. Criterion 1 (one stream replaces two polls): open DevTools → Network. Confirm a single
       persistent `text/event-stream` request to `/api/events`; confirm there are NO repeating
       `/api/status` or `/api/logs` requests every 2s while SSE is connected.
    2. Criterion 2 (live health within 1-2s): start the bot (Start Bot), then stop it (Stop Bot).
       Confirm the status dot/label and health cards update within ~1-2s WITHOUT a page refresh.
    3. Criterion 3 (no duplicate log lines): reload the page; confirm backfilled log lines appear,
       then as the bot runs confirm new lines append and no line is duplicated at the
       backfill/stream boundary.
    4. Criterion 4 (clean reconnect): close the browser tab, reopen http://localhost:8000.
       Confirm DevTools shows one new `/api/events` stream, the indicator returns to "Live", and
       live updates resume with no page-reload-needed errors.
    5. Criterion 5 (fallback, no JS error): in DevTools console run `window.EventSource = undefined`
       then reload. Confirm the Network tab shows the `/api/status` + `/api/logs` polls resume
       (every 2s) and the Console shows NO JavaScript error.
    Per the autonomous live-UAT policy (MEMORY: defer live-environment checks as UAT debt): if a
    live browser/bot environment is not available right now, record these 5 as Phase 29 UAT debt
    in the SUMMARY and continue — do not block the phase.
  </how-to-verify>
  <files>web/templates/dashboard.html (served at GET /; verified live in a browser)</files>
  <action>Operator-only live verification of the 5 SSE-01 criteria via DevTools + a running bot; no code change. Claude has already automated all static-template assertions (W29-A1..A11) in prior tasks.</action>
  <verify><human-check>Operator confirms the 5 live criteria in a browser (steps in how-to-verify), or records them as Phase 29 UAT debt.</human-check></verify>
  <done>All 5 live SSE-01 criteria verified by the operator, OR explicitly recorded as Phase 29 UAT debt in the SUMMARY per the autonomous live-UAT policy.</done>
  <resume-signal>Type "approved" if all 5 verify, "debt" to record as deferred UAT, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream (/api/events) → DOM | server-pushed status JSON + log line strings cross into renderStatus/appendLogLine and into the DOM |
| EventSource connection | same-origin (localhost) read-only GET; browser sends only automatic headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Tampering (XSS) | SSE 'status'/'log' data rendered via renderStatus / appendLogLine | mitigate | SSE handlers reuse the Plan 02 safe-DOM renderers (createElement/textContent); appendLogLine uses textContent; setIndicator uses textContent; no innerHTML in the SSE path; guarded by test_no_innerHTML_with_api_data |
| T-29-07 | Information Disclosure | last_error delivered in SSE status frames | accept | SSE frames already scrubbed at get_status() (last_error = exception class name, Phase 26) and CI-asserted credential-free (Phase 26 SSE-03); this plan adds no new data, only consumes the existing scrubbed stream |
| T-29-08 | Denial of Service | tight client reconnect loop | mitigate | Rely on native EventSource auto-reconnect with server retry:3000; onerror is indicator-only, NO manual setTimeout/new EventSource (Pitfall 2) — prevents reconnect storms |
| T-29-09 | Spoofing/Repudiation | new endpoint or CSRF surface | accept | No new endpoint; /api/events is an existing read-only GET with no check_origin (matches /api/status posture, verified web/routes/sse.py); MC-4 banner + CSRF gate on POST/DELETE unchanged |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (29-RESEARCH Package Legitimacy Gate: skipped) |
</threat_model>

<verification>
- `rtk pytest tests/test_sse_wiring.py -q` — ALL GREEN (W29-A1..A7, A11 + onmessage guard).
- `rtk pytest tests/test_web_dashboard.py tests/test_observability_ui.py tests/test_design_system.py tests/test_web_security.py -q` — GREEN (no regression: safe-DOM, hex, MC-4 banner, observability surfaces intact).
- `rtk pytest tests/test_sse.py -q` — GREEN (Phase 27 SSE infra unaffected).
- Full suite `rtk pytest -q` — GREEN.
- MANUAL UAT (criteria 1-5): operator checkpoint above; deferrable as Phase 29 UAT debt per the autonomous live-UAT policy.
</verification>

<success_criteria>
- One feature-detected EventSource('/api/events') replaces the two setInterval polls; named
  status/log listeners drive renderStatus/appendLogLine; onopen/onerror drive the Live/Reconnecting
  indicator; native reconnect relied upon; one-shot backfill on load; polling fallback when
  EventSource is absent (W29-A1..A11 GREEN).
- No innerHTML in the SSE path; no manual reconnect loop; no setInterval on the SSE-active path;
  no renderLogLines on log events; safe-DOM + hex + MC-4 + Phase 27/28 guards all green.
- The 5 live SSE-01 criteria verified via operator UAT or recorded as Phase 29 UAT debt.
</success_criteria>

<output>
Create `.planning/phases/29-sse-client-wiring/29-03-SUMMARY.md` when done.
</output>
