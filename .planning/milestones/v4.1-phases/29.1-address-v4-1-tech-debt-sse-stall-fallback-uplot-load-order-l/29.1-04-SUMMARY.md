---
phase: 29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l
plan: 04
subsystem: ui
tags: [sse, eventsource, watchdog, polling-fallback, dashboard]

requires:
  - phase: 29-sse-client-wiring
    provides: EventSource block, setIndicator, pollStatus, pollLogs
  - phase: 29.1
    provides: RED test_sse_stall_ms_constant + test_watchdog_setinterval_in_sse_branch
provides:
  - client-side idle watchdog + REST polling safety net for stalled-but-open SSE streams
affects: []

tech-stack:
  added: []
  patterns: [nested setInterval watchdog -> guarded fallback timer with recovery teardown]

key-files:
  created: []
  modified: [web/templates/dashboard.html]

key-decisions:
  - "8s stall threshold (SSE_STALL_MS=8000); server emits status every 1s, so 8 missed ticks = real stall, under the 15s keepalive"
  - "Fallback timer uses anonymous-wrapper setInterval(function(){pollStatus();pollLogs();}) so the regression guard test_setinterval_only_in_fallback stays GREEN"
  - "No es.close()+reopen: if the server poll loop died there is no producer to reconnect to; REST polling is the resilient path"

patterns-established:
  - "Idle watchdog distinct from EventSource.onerror (which fires only on close, never on idle)"

requirements-completed: [SSE-01, SSE-02]

duration: 4min
completed: 2026-06-30
---

# Phase 29.1 Plan 04 Summary

**Client-side idle watchdog flips the indicator to Reconnecting and starts a REST polling safety net when the SSE stream stalls open without closing, recovering to Live on the next status event**

## Performance
- **Duration:** ~4 min
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Declared `SSE_STALL_MS = 8000` adjacent to `POLL_MS`
- Watchdog `setInterval` (every POLL_MS) inside the EventSource branch: on >8s status gap, `setIndicator('reconnecting')` + guarded fallback timer (`if (!_sseFallbackTimer)`, Pitfall 3)
- Fallback timer = `setInterval(function(){ pollStatus(); pollLogs(); }, POLL_MS)` (anonymous wrapper — regression guard stays GREEN)
- status listener resets `_sseLastStatusTs` and tears down fallback (`clearInterval` + `setIndicator('live')`); onopen also resets the timestamp; onerror unchanged; es.close() not called
- Full file 14/14 GREEN incl. regression guard; full suite 807 passed / 2 skipped

## Task Commits
1. **Task 1: Declare SSE_STALL_MS and wire the idle watchdog + REST fallback** - `63b47a2` (fix)

## Files Created/Modified
- `web/templates/dashboard.html` - SSE_STALL_MS + watchdog/fallback in the EventSource branch, tagged SSE-01/SSE-02

## Decisions Made
None beyond the locked CONTEXT.md approach.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None. Regression guard verified GREEN: the bare `setInterval(pollStatus`/`setInterval(pollLogs` strings appear only in the else fallback branch.

## Next Phase Readiness
All three v4.1 audit warnings (OBS-05, OBS-07, SSE-01/02) closed. Live-browser behaviors (stall->fallback, cold-load chart, repeated-line visibility) remain deferred UAT per v4.1 posture.

---
*Phase: 29.1*
*Completed: 2026-06-30*
