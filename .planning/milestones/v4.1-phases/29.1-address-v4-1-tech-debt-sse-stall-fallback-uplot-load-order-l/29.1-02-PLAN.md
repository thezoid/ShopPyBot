---
phase: 29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l
plan: 02
type: execute
wave: 2
depends_on: ["29.1-01"]
files_modified:
  - web/templates/dashboard.html
autonomous: true
requirements: [OBS-05]

must_haves:
  truths:
    - "The uPlot vendored <script> loads synchronously in <head> before any inline <script> runs"
    - "No cold-load ReferenceError: uPlot is not defined can occur (global defined before inline parse)"
    - "uPlot loading stays vendored, zero-Node, no CDN, no bundler, no defer attribute"
  artifacts:
    - path: "web/templates/dashboard.html"
      provides: "uplot.iife.min.js <script> relocated from </body> to </head>"
      contains: "uplot.iife.min.js"
  key_links:
    - from: "<head> of dashboard.html"
      to: "uPlot global"
      via: "synchronous <script src> before inline block"
      pattern: "uplot\\.iife\\.min\\.js"
---

<objective>
Fix the OBS-05 cold-load race: move the vendored uPlot loader
`<script src="/static/vendor/uplot.iife.min.js"></script>` from the end of `<body>`
(currently line 894) to the end of `<head>` (after the CSS links at lines 17-20, before
`</head>` at line 21). This is a pure tag relocation — no logic change, no `defer` — so the
`uPlot` global is defined before the inline `<script>` block (begins line 190, calls
`new uPlot()` at line 481) parses at cold load.

Purpose: Eliminate the cold-load `ReferenceError: uPlot is not defined` (lowest-risk fix, done first per RESEARCH risk ordering).
Output: dashboard.html with the uPlot loader in <head>; test_uplot_script_in_head turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md
@web/templates/dashboard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate uPlot loader to head, synchronous, no defer</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html lines 14-21 (head CSS block + </head>) and lines 888-896 (end of inline script, current uPlot <script> at 894, </body></html>)
    - .planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md "Fix 1 — uPlot Load Order (OBS-05)" — exact source/target anchors; and "Common Pitfalls" Pitfall 5 (NO defer)
  </read_first>
  <action>
    Delete the line `<script src="/static/vendor/uplot.iife.min.js"></script>` from its current
    position at the end of `<body>` (line 894). Insert it inside `<head>` immediately after the
    last CSS link `<link rel="stylesheet" href="/static/vendor/uplot.min.css">` (line 20) and
    before `</head>` (line 21). Load it SYNCHRONOUSLY — do NOT add a `defer` or `async` attribute
    (per Pitfall 5: defer would execute at DOMContentLoaded, after the synchronous inline block at
    line 190 has already called new uPlot()). Tag the relocated line with the comment
    `<!-- OBS-05: move from body end to head for guaranteed load order -->` per the project's
    comment-tag convention. Leave the uPlot CSS `<link>` at line 20 unchanged. Do not touch the
    inline `<script>` block, the `new uPlot()` call at line 481, or anything else.
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py::test_uplot_script_in_head -x</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_sse_wiring.py::test_uplot_script_in_head -x` PASSES (uplot.iife.min.js
      now before </head> and before the first <script> after </head>).
    - The literal string `uplot.iife.min.js` appears exactly once in dashboard.html (relocated, not duplicated).
    - The relocated `<script>` has NO `defer` and NO `async` attribute.
    - The comment `OBS-05` appears on/adjacent to the relocated line.
    - The old end-of-body position no longer contains `uplot.iife.min.js` (string is before </head>, not in <body>).
    - All other tests still pass: `rtk pytest tests/test_sse_wiring.py` shows no NEW failures vs Wave 0
      (the 4 still-unbuilt dedup/watchdog assertions remain RED; test_uplot_script_in_head flips to GREEN).
  </acceptance_criteria>
  <done>uPlot loader is in <head>, synchronous, tagged OBS-05; test_uplot_script_in_head GREEN; no duplicate script tag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static asset | Loads the SAME vendored /static/vendor/uplot.iife.min.js as before, only repositioned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-02 | Tampering | uplot.iife.min.js <script> relocation | accept | Same vendored file, same path, no CSP change, no new origin; relocation only (RESEARCH Security Domain) |
| T-29.1-SC | Tampering | pip/npm installs | accept | No packages installed; zero-Node constraint; asset already vendored |

No new attack surface. Nothing to block on.
</threat_model>

<verification>
`rtk pytest tests/test_sse_wiring.py::test_uplot_script_in_head -x` GREEN; full suite shows no new
failures beyond the still-RED dedup/watchdog assertions.
</verification>

<success_criteria>
- uPlot loader synchronously in <head>, tagged OBS-05, no defer, single occurrence.
- test_uplot_script_in_head GREEN.
</success_criteria>

<output>
Create `.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-02-SUMMARY.md` when done
</output>
