---
phase: 29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sse_wiring.py
autonomous: true
requirements: [SSE-01, SSE-02, OBS-05, OBS-07]

must_haves:
  truths:
    - "Five new RED assertions exist in tests/test_sse_wiring.py and FAIL against current dashboard.html"
    - "All 8 existing assertions in tests/test_sse_wiring.py stay GREEN (untouched)"
    - "The regression-guard test test_setinterval_only_in_fallback is present and GREEN"
  artifacts:
    - path: "tests/test_sse_wiring.py"
      provides: "5 new static-content assertions (W29.1-A1..A5) + existing 8 + regression guard"
      contains: "def test_sse_stall_ms_constant"
  key_links:
    - from: "tests/test_sse_wiring.py"
      to: "rendered dashboard.html (GET /)"
      via: "TestClient(create_app(mock_svc)).get('/')"
      pattern: "client\\.get\\(\"/\"\\)"
---

<objective>
Add the five Wave-0 RED assertions (W29.1-A1 through A5) to tests/test_sse_wiring.py. These
assert the target state of all three fixes and MUST FAIL against the current dashboard.html
(none of the three fixes have landed yet). The existing 8 assertions and the regression guard
must remain untouched and GREEN.

Purpose: Establish RED-before-GREEN coverage so Waves 1-3 each have an automated target to turn
GREEN, and so the critical regression guard is exercised on every subsequent commit.
Output: tests/test_sse_wiring.py extended with 5 new test functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md
@.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-VALIDATION.md
@tests/test_sse_wiring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 RED assertions to tests/test_sse_wiring.py</name>
  <files>tests/test_sse_wiring.py</files>
  <read_first>
    - tests/test_sse_wiring.py (existing 8 assertions + test_setinterval_only_in_fallback; the `client` and `mock_svc` fixtures; the `import re` already present at top)
    - .planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md section "New Test Assertions Required (Phase 29.1)" — the EXACT bodies for all 5 tests (W29.1-A1..A5)
  </read_first>
  <action>
    Append five new test functions to tests/test_sse_wiring.py, copying the exact assertion
    bodies from the RESEARCH.md "New Test Assertions Required (Phase 29.1)" section (do NOT
    re-derive them). The five functions, by name:
    - test_sse_stall_ms_constant (W29.1-A1 / SSE-01): asserts the literal "SSE_STALL_MS" in resp.text.
    - test_watchdog_setinterval_in_sse_branch (W29.1-A2 / SSE-01): positional — "SSE_STALL_MS" appears
      after the "typeof EventSource" index but BEFORE the first `else` token after it.
    - test_uplot_script_in_head (W29.1-A3 / OBS-05): positional — "uplot.iife.min.js" appears before
      "</head>" and before the first "<script>" found after "</head>".
    - test_dedup_next_line_flag (W29.1-A4 / OBS-07): asserts the literal "_dedupNextLine" in resp.text.
    - test_dedup_flag_armed_after_render (W29.1-A5 / OBS-07): positional — "_dedupNextLine = true"
      appears after the ".forEach(function(line)" call inside "function renderLogLines".
    Reuse the existing `client` fixture and the module-level `import re`. Do NOT modify, rename, or
    delete any of the 8 existing tests or test_setinterval_only_in_fallback. Keep each new test's
    docstring referencing its W29.1-Ax id and requirement, consistent with existing style.
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py -k "sse_stall_ms_constant or watchdog_setinterval_in_sse_branch or uplot_script_in_head or dedup_next_line_flag or dedup_flag_armed_after_render"</automated>
  </verify>
  <acceptance_criteria>
    - The 5 new tests are collected by pytest (names match exactly those listed above).
    - All 5 new tests FAIL (RED) against current dashboard.html — confirming they target unbuilt state:
      `rtk pytest tests/test_sse_wiring.py -k "sse_stall_ms_constant or watchdog_setinterval_in_sse_branch or uplot_script_in_head or dedup_next_line_flag or dedup_flag_armed_after_render"`
      reports 5 failed, 0 passed.
    - All 8 pre-existing assertions + test_setinterval_only_in_fallback still PASS:
      `rtk pytest tests/test_sse_wiring.py -k "not (sse_stall_ms_constant or watchdog_setinterval_in_sse_branch or uplot_script_in_head or dedup_next_line_flag or dedup_flag_armed_after_render)"`
      reports 9 passed, 0 failed.
    - `import re` is present and unduplicated; no fixture redefinition.
  </acceptance_criteria>
  <done>5 new RED tests collected and failing; existing 9 tests green; file edits confined to additions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Test-only change; no production code touched; no runtime input crosses a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-01 | Tampering | tests/test_sse_wiring.py | accept | Test-only edit, no production surface; CI runs the suite, no secrets or input handling involved |
| T-29.1-SC | Tampering | pip installs | accept | No packages installed this phase (RESEARCH.md "Package Legitimacy Audit: Not applicable"); pytest already vendored |

No new attack surface. Nothing to block on (per planning_context security_threat_model).
</threat_model>

<verification>
Run `rtk pytest tests/test_sse_wiring.py`: 5 new tests RED, 9 existing tests GREEN. This is the
expected Wave-0 state and gates Waves 1-3.
</verification>

<success_criteria>
- 5 new assertions added verbatim from RESEARCH.md, all RED.
- 8 existing assertions + regression guard untouched and GREEN.
</success_criteria>

<output>
Create `.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-01-SUMMARY.md` when done
</output>
