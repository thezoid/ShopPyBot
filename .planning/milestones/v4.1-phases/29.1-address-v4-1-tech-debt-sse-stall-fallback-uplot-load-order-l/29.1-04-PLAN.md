---
phase: 29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l
plan: 04
type: execute
wave: 4
depends_on: ["29.1-03"]
files_modified:
  - web/templates/dashboard.html
autonomous: true
requirements: [SSE-01, SSE-02]

must_haves:
  truths:
    - "SSE_STALL_MS = 8000 constant is declared at top of the inline script scope near POLL_MS"
    - "A watchdog setInterval inside the EventSource if-branch detects an 8s status gap and starts REST fallback polling"
    - "On the next status SSE event (or onopen) the fallback timer is cleared and the indicator returns to live"
    - "The fallback timer uses the anonymous-wrapper shape so test_setinterval_only_in_fallback stays GREEN"
  artifacts:
    - path: "web/templates/dashboard.html"
      provides: "SSE_STALL_MS constant + watchdog/fallback wiring in the EventSource if-branch"
      contains: "SSE_STALL_MS"
  key_links:
    - from: "watchdog setInterval (if-branch)"
      to: "pollStatus() + pollLogs() REST fallback"
      via: "setInterval(function() { pollStatus(); pollLogs(); }, POLL_MS)"
      pattern: "setInterval\\(function\\(\\)\\s*\\{\\s*pollStatus\\(\\);\\s*pollLogs\\(\\)"
    - from: "status SSE event / onopen"
      to: "fallback timer teardown"
      via: "clearInterval(_sseFallbackTimer) + setIndicator('live')"
      pattern: "clearInterval\\("
---

<objective>
Fix SSE-01/02: EventSource onerror fires only on close, never on idle. Add a client-side idle
watchdog so a stalled-but-open stream falls back to REST polling. Declare `SSE_STALL_MS = 8000`
adjacent to `POLL_MS` (line 208). Inside the existing EventSource if-branch (lines 601-615), add a
watchdog setInterval (every POLL_MS) that, when `Date.now() - _sseLastStatusTs > SSE_STALL_MS`,
calls `setIndicator('reconnecting')` and starts a guarded REST fallback timer. In the existing
`status` listener (line 604) record `_sseLastStatusTs` and, if a fallback is active, clear it and
return to 'live'. Reset `_sseLastStatusTs` in onopen (line 610) too. Do NOT call es.close()+reopen.

CRITICAL REGRESSION CONSTRAINT: the fallback timer MUST be written as the anonymous-function
wrapper `setInterval(function() { pollStatus(); pollLogs(); }, POLL_MS)` — NEVER the bare
`setInterval(pollStatus, POLL_MS)` / `setInterval(pollLogs, POLL_MS)` form inside the if-branch.
The existing test_setinterval_only_in_fallback greps for the literal `setInterval(pollStatus` /
`setInterval(pollLogs` and requires them ONLY after the `else`; the bare form inside the if-branch
turns it RED.

Purpose: Keep dashboard data fresh when SSE stalls open without closing (highest-surface fix, done last per RESEARCH risk ordering).
Output: dashboard.html with watchdog/fallback; test_sse_stall_ms_constant + test_watchdog_setinterval_in_sse_branch GREEN; test_setinterval_only_in_fallback STAYS GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md
@web/templates/dashboard.html
@tests/test_sse_wiring.py

<interfaces>
<!-- Existing project functions the executor reuses directly (do NOT reimplement) -->
From web/templates/dashboard.html:
  const POLL_MS = 2000;                       // line 208 — declare SSE_STALL_MS right after this
  function setIndicator(state)                // line 347 — 'live' | any other string => 'reconnecting'
  async function pollStatus()                 // line 359 — fetch /api/status, renderStatus(data)
  async function pollLogs()                   // line 562 — fetch /api/logs (reads current filter from DOM)
  function renderStatus(data)                 // line 326 — shared pure renderer
  // SSE if-branch (601-615): es = new EventSource('/api/events');
  //   es.addEventListener('status', ...)  line 604
  //   es.onopen = function(){ setIndicator('live'); }  line 610
  //   es.onerror = function(){ setIndicator('reconnecting'); }  line 611
  //   else { setInterval(pollStatus, POLL_MS); setInterval(pollLogs, POLL_MS); }  lines 612-614
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare SSE_STALL_MS and wire the idle watchdog + REST fallback</name>
  <files>web/templates/dashboard.html</files>
  <behavior>
    - test_sse_stall_ms_constant: rendered HTML contains the literal "SSE_STALL_MS".
    - test_watchdog_setinterval_in_sse_branch: "SSE_STALL_MS" appears after "typeof EventSource"
      and BEFORE the first `else` token after it (i.e., inside the if-branch).
    - test_setinterval_only_in_fallback (REGRESSION): literal "setInterval(pollStatus" and
      "setInterval(pollLogs" appear ONLY after the `else` — so the if-branch fallback timer MUST
      use the anonymous-wrapper shape, never the bare form.
  </behavior>
  <read_first>
    - web/templates/dashboard.html lines 206-210 (POLL_MS = 2000 at 208) and lines 598-616 (SSE wiring if/else block; status listener at 604; onopen 610; onerror 611; bare fallback setInterval at 613-614)
    - .planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md "Fix 3 — SSE Stall Watchdog (SSE-01/02)" (full target-code block + named symbols) AND "Common Pitfalls" Pitfalls 2 (init _sseLastStatusTs at construction), 3 (guard inner setInterval with if (!_sseFallbackTimer)), 6 (SSE_STALL_MS at top-level near POLL_MS) AND "Regression risk on test_setinterval_only_in_fallback"
    - tests/test_sse_wiring.py lines 170-227 (test_setinterval_only_in_fallback — the exact strings it greps and the only-after-else rule)
  </read_first>
  <action>
    Declare `const SSE_STALL_MS = 8000;` immediately after `const POLL_MS = 2000;` at line 208
    (top-level inline-script scope for grep-discoverability per Pitfall 6); tag it `// SSE-01: stall threshold (8 missed 1s ticks)`.

    Inside the EventSource if-branch (after `var es = new EventSource('/api/events');` at line 603),
    declare `var _sseLastStatusTs = Date.now();` (init at construction, Pitfall 2) and
    `var _sseFallbackTimer = null;`. Add a watchdog: `setInterval(function() { ... }, POLL_MS)` that
    checks `if (Date.now() - _sseLastStatusTs > SSE_STALL_MS)`; on stall call `setIndicator('reconnecting')`
    and, guarded by `if (!_sseFallbackTimer)` (Pitfall 3), start the fallback timer.

    CRITICAL — the fallback timer MUST be the anonymous-wrapper form:
    `_sseFallbackTimer = setInterval(function() { pollStatus(); pollLogs(); }, POLL_MS);`
    NEVER `setInterval(pollStatus, POLL_MS)` or `setInterval(pollLogs, POLL_MS)` inside the if-branch
    (that turns test_setinterval_only_in_fallback RED). The bare-form setInterval calls must remain
    ONLY in the existing `else` branch (lines 613-614) — leave those untouched.

    In the existing `status` listener (line 604), after `renderStatus(JSON.parse(e.data))`, add
    `_sseLastStatusTs = Date.now();` and, if `_sseFallbackTimer` is set, `clearInterval(_sseFallbackTimer); _sseFallbackTimer = null; setIndicator('live');`
    — all inside the existing try/catch (CR-01). In `es.onopen` (line 610) add `_sseLastStatusTs = Date.now();`
    after `setIndicator('live')`. Leave `es.onerror` (line 611) unchanged. Do NOT call es.close() or
    reopen the stream. Tag new lines `SSE-01` / `SSE-02` per comment-tag convention.
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py::test_sse_stall_ms_constant tests/test_sse_wiring.py::test_watchdog_setinterval_in_sse_branch tests/test_sse_wiring.py::test_setinterval_only_in_fallback -x</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_sse_wiring.py::test_sse_stall_ms_constant -x` PASSES (SSE_STALL_MS present).
    - `rtk pytest tests/test_sse_wiring.py::test_watchdog_setinterval_in_sse_branch -x` PASSES
      (SSE_STALL_MS appears inside the if-branch, before the else).
    - REGRESSION GUARD: `rtk pytest tests/test_sse_wiring.py::test_setinterval_only_in_fallback -x`
      STAYS GREEN — the if-branch fallback uses `setInterval(function() { pollStatus(); pollLogs(); }, POLL_MS)`;
      the literal `setInterval(pollStatus` / `setInterval(pollLogs` appear ONLY in the else branch.
    - `SSE_STALL_MS` value is exactly `8000`; declared adjacent to `POLL_MS` at top-level (not inside the if-block).
    - Inner fallback setInterval is guarded by `if (!_sseFallbackTimer)` (no compounding timers, Pitfall 3).
    - `_sseLastStatusTs` initialized at EventSource construction AND reset in onopen (Pitfall 2).
    - On status event the fallback is torn down via `clearInterval` + `setIndicator('live')`.
    - es.close() is NOT called anywhere new; es.onerror is unchanged.
    - FULL FILE GREEN: `rtk pytest tests/test_sse_wiring.py` — all 13 assertions pass (8 original + 5 new), zero RED.
  </acceptance_criteria>
  <done>SSE_STALL_MS=8000 declared near POLL_MS; watchdog + guarded anonymous-wrapper fallback wired; status/onopen reset _sseLastStatusTs; recovery clears fallback; all 13 tests GREEN including the regression guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/status, /api/logs | Fallback timer hits the SAME endpoints the no-EventSource else-branch already polls in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-04 | Denial of Service (self) | _sseFallbackTimer compounding intervals | mitigate | Guard the inner setInterval with `if (!_sseFallbackTimer)` so only one fallback timer runs (Pitfall 3); cleared on recovery |
| T-29.1-05 | Information Disclosure | fallback polling of /api/status, /api/logs | accept | Same endpoints + same CSRF posture as the existing production no-EventSource fallback; no new data exposure (RESEARCH Security Domain) |
| T-29.1-SC | Tampering | pip/npm installs | accept | No packages installed; browser built-ins (setInterval/clearInterval/Date.now) + existing project functions only |

No new attack surface. Nothing to block on.
</threat_model>

<verification>
`rtk pytest tests/test_sse_wiring.py` — all 13 assertions GREEN, including the regression guard
test_setinterval_only_in_fallback. Then `rtk pytest` (full suite) green before /gsd:verify-work.
</verification>

<success_criteria>
- SSE_STALL_MS=8000 near POLL_MS; watchdog + guarded anonymous-wrapper REST fallback; recovery teardown.
- test_sse_stall_ms_constant + test_watchdog_setinterval_in_sse_branch GREEN.
- test_setinterval_only_in_fallback STAYS GREEN (regression guard).
- All 13 SSE-wiring assertions GREEN.
</success_criteria>

<output>
Create `.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-04-SUMMARY.md` when done
</output>
