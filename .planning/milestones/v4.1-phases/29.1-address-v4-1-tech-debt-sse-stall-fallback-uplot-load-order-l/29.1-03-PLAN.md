---
phase: 29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l
plan: 03
type: execute
wave: 3
depends_on: ["29.1-02"]
files_modified:
  - web/templates/dashboard.html
autonomous: true
requirements: [OBS-07]

must_haves:
  truths:
    - "A repeated, legitimate consecutive bot log line is always shown (blanket equality drop removed)"
    - "Only the first SSE line after a backfill repaint is dropped if it equals the last backfilled line"
    - "An empty-filter repaint resets both _lastLogLine and _dedupNextLine (flag NOT armed on empty path)"
  artifacts:
    - path: "web/templates/dashboard.html"
      provides: "_dedupNextLine one-shot boundary flag in appendLogLine + renderLogLines"
      contains: "_dedupNextLine"
  key_links:
    - from: "renderLogLines (after non-empty forEach)"
      to: "appendLogLine (first post-backfill call)"
      via: "_dedupNextLine one-shot flag"
      pattern: "_dedupNextLine = true"
---

<objective>
Fix OBS-07: replace the blanket content-equality drop in appendLogLine (current line 501,
`if (lineText === _lastLogLine) return;`) with a one-shot boundary dedup using a new
`_dedupNextLine` boolean flag. The flag is reset to false at the top of renderLogLines (alongside
the existing `_lastLogLine = ''` reset at line 525), and armed to true ONLY after a non-empty
backfill forEach completes (after line 534, before maybeScrollToBottom). The first subsequent
appendLogLine consumes the flag (clears it, drops only if the line matches `_lastLogLine`); all
later lines append unconditionally so legitimately repeated bot messages always show.

Purpose: Stop suppressing legitimately repeated consecutive bot log lines while still de-duping the single backfill→SSE boundary overlap.
Output: dashboard.html with _dedupNextLine; test_dedup_next_line_flag + test_dedup_flag_armed_after_render GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md
@web/templates/dashboard.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _dedupNextLine one-shot boundary dedup</name>
  <files>web/templates/dashboard.html</files>
  <behavior>
    - test_dedup_next_line_flag: rendered HTML contains the literal "_dedupNextLine".
    - test_dedup_flag_armed_after_render: "_dedupNextLine = true" appears AFTER the
      ".forEach(function(line)" call inside "function renderLogLines".
    - Behavioral (encoded as acceptance criteria, manual-UAT for live): repeated identical bot
      lines after the first appended line are all shown; only the first post-backfill SSE line is
      dropped if it equals the last backfilled line; empty-filter repaint does NOT arm the flag.
  </behavior>
  <read_first>
    - web/templates/dashboard.html lines 485-536 (var _lastLogLine at 486; appendLogLine 500-513 with the blanket drop at 501; renderLogLines 522-536 with the _lastLogLine reset at 525, empty-path early return at 526-533, and the forEach at 534)
    - .planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-RESEARCH.md "Fix 2 — Log Duplicate Guard (OBS-07)" (target-code block) AND "Common Pitfalls" Pitfall 1 (empty-filter must NOT arm) and Pitfall 4 (keep _lastLogLine reset order)
  </read_first>
  <action>
    Declare a new module-scope flag `var _dedupNextLine = false;` adjacent to `var _lastLogLine = '';`
    at line 486; tag it `// OBS-07: one-shot backfill→SSE boundary dedup flag`.

    In appendLogLine (line 500): REMOVE the blanket guard `if (lineText === _lastLogLine) return;`
    (line 501). Replace it with a one-shot consume block: if `_dedupNextLine` is true, set
    `_dedupNextLine = false` FIRST (always clear on the first call), then `if (lineText === _lastLogLine) return;`.
    Keep `_lastLogLine = lineText;` (line 502) immediately after, unchanged in position. Do not alter the
    DOM-cap loop (lines 510-512). Tag the new block `OBS-07`.

    In renderLogLines (line 522): KEEP the existing `_lastLogLine = '';` reset at line 525 in place.
    Add `_dedupNextLine = false;` immediately after it (BEFORE the empty-path early-return check at
    line 526), so ALL paths — empty and non-empty — clear the flag first (Pitfall 1). Do NOT arm the
    flag inside the empty branch (lines 526-533). After the non-empty `lines.forEach(...)` at line 534
    and BEFORE `maybeScrollToBottom()` at line 535, add `_dedupNextLine = true;` tagged
    `// OBS-07: arm one-shot dedup for first post-backfill SSE line`. Preserve the existing reset
    order (Pitfall 4): _lastLogLine reset at top → forEach builds it up → arm flag after.

    Do NOT reintroduce the blanket equality drop anywhere (State of the Art: it is deprecated).
  </action>
  <verify>
    <automated>rtk pytest tests/test_sse_wiring.py::test_dedup_next_line_flag tests/test_sse_wiring.py::test_dedup_flag_armed_after_render -x</automated>
  </verify>
  <acceptance_criteria>
    - `rtk pytest tests/test_sse_wiring.py::test_dedup_next_line_flag -x` PASSES (_dedupNextLine present).
    - `rtk pytest tests/test_sse_wiring.py::test_dedup_flag_armed_after_render -x` PASSES
      (`_dedupNextLine = true` appears after the forEach in renderLogLines).
    - The blanket guard `if (lineText === _lastLogLine) return;` is NO LONGER present at the TOP of
      appendLogLine (it now lives only inside the `if (_dedupNextLine)` block, gated by the flag).
    - `_dedupNextLine = false` appears BEFORE the empty-path early-return (so empty-filter repaint
      clears it); `_dedupNextLine = true` does NOT appear inside the empty branch.
    - `test_no_dup_guard_present` still PASSES (`_lastLogLine` variable retained).
    - Full suite shows no NEW failures vs prior wave: `rtk pytest tests/test_sse_wiring.py`
      (uPlot + both dedup tests GREEN; the 2 watchdog assertions remain RED until Wave 4).
  </acceptance_criteria>
  <done>_dedupNextLine flag added; blanket drop removed; both dedup tests GREEN; empty path does not arm; _lastLogLine retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE/backfill data → log DOM | Log line text rendered via appendLogLine, which already uses textContent (not innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.1-03 | Tampering (XSS) | appendLogLine output path | accept | Output path unchanged: still span.textContent (RESEARCH ASVS V5); the flag only gates whether a line is appended, not how it is rendered |
| T-29.1-SC | Tampering | pip/npm installs | accept | No packages installed; client-side flag only, no data leaves browser |

No new attack surface. Nothing to block on.
</threat_model>

<verification>
`rtk pytest tests/test_sse_wiring.py::test_dedup_next_line_flag tests/test_sse_wiring.py::test_dedup_flag_armed_after_render -x`
GREEN; test_no_dup_guard_present still GREEN; no new suite failures.
</verification>

<success_criteria>
- _dedupNextLine one-shot flag implemented per RESEARCH; blanket drop removed; empty path not armed.
- Both dedup assertions GREEN; _lastLogLine retained.
</success_criteria>

<output>
Create `.planning/phases/29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l/29.1-03-SUMMARY.md` when done
</output>
