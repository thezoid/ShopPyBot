---
phase: 29.1-address-v4-1-tech-debt-sse-stall-fallback-uplot-load-order-l
plan: 03
subsystem: ui
tags: [dashboard, logs, dedup, sse]

requires:
  - phase: 29.1
    provides: RED test_dedup_next_line_flag + test_dedup_flag_armed_after_render
provides:
  - one-shot _dedupNextLine boundary dedup replacing the blanket content-equality drop
affects: []

tech-stack:
  added: []
  patterns: [one-shot flag for detecting a producer boundary exactly once]

key-files:
  created: []
  modified: [web/templates/dashboard.html]

key-decisions:
  - "Dedup only the first SSE line after a backfill repaint; legitimately repeated bot messages always render"
  - "Flag reset on ALL renderLogLines paths (incl. empty-filter early return); armed only after a non-empty forEach"

patterns-established:
  - "One-shot _dedupNextLine flag: set by producer (renderLogLines) after a batch, consumed once by appendLogLine"

requirements-completed: [OBS-07]

duration: 3min
completed: 2026-06-30
---

# Phase 29.1 Plan 03 Summary

**Replaced the blanket consecutive-equality log drop with a one-shot backfill->SSE boundary dedup, so legitimately repeated bot messages are no longer suppressed**

## Performance
- **Duration:** ~3 min
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Declared `_dedupNextLine` flag adjacent to `_lastLogLine`
- appendLogLine: blanket `if (lineText === _lastLogLine) return;` now gated inside `if (_dedupNextLine)`, cleared on first call
- renderLogLines: flag reset before the empty-path early return (Pitfall 1); armed after the non-empty forEach (Pitfall 4 order preserved)
- `_lastLogLine` retained (test_no_dup_guard_present stays GREEN); suite 12 passed / 2 RED

## Task Commits
1. **Task 1: Add _dedupNextLine one-shot boundary dedup** - `a0a0f0d` (fix)

## Files Created/Modified
- `web/templates/dashboard.html` - _dedupNextLine flag in appendLogLine + renderLogLines, tagged OBS-07

## Decisions Made
None - followed plan as specified.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## Next Phase Readiness
OBS-07 closed; proceed to SSE-01/02 watchdog (highest surface, done last).

---
*Phase: 29.1*
*Completed: 2026-06-30*
