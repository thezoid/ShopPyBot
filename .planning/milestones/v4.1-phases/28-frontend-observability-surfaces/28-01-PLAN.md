---
phase: 28-frontend-observability-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_health.py
  - tests/test_observability_ui.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-04, OBS-05, OBS-06, OBS-07, OBS-09]
must_haves:
  truths:
    - "heartbeat_age_secs guard expects the new key (RED until 28-02 lands the field)"
    - "Static-scaffold tests assert the three new dashboard sections + header uptime + log controls + uPlot assets exist (RED until 28-03/28-04 land the HTML)"
    - "The existing no-innerHTML XSS guard still runs and passes against dashboard.html"
  artifacts:
    - path: "tests/test_health.py"
      provides: "Updated snapshot-key guard + heartbeat_age_secs fresh/never unit tests"
      contains: "heartbeat_age_secs"
    - path: "tests/test_observability_ui.py"
      provides: "Static server-rendered scaffold assertions for OBS surfaces"
      contains: "section-health"
  key_links:
    - from: "tests/test_observability_ui.py"
      to: "GET /"
      via: "TestClient(create_app(mock_svc)).get('/')"
      pattern: "client\\.get\\(\"/\"\\)"
---

<objective>
Wave 0 RED scaffold for Phase 28. Add the backend unit tests and static server-rendered
scaffold tests that downstream waves turn GREEN. No production code changes — only tests.

Purpose: Establish automated verification surface BEFORE implementation. Per 28-VALIDATION.md,
the automatable surface is (1) the heartbeat_age_secs snapshot field, (2) static HTML
scaffolding presence via TestClient GET /, and (3) the existing XSS/CSS CI guards. The
JS-rendered visuals are manual UAT and are NOT faked here.
Output: Updated tests/test_health.py and new tests/test_observability_ui.py (both RED
until 28-02/28-03/28-04 land).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-frontend-observability-surfaces/28-VALIDATION.md
@.planning/phases/28-frontend-observability-surfaces/28-PATTERNS.md

<interfaces>
From core/health.py HealthRegistry (current — get_snapshot to be extended in 28-02):
  - _ensure(name): seeds record with last_heartbeat=0.0 (never-heartbeated sentinel)
  - heartbeat(name): sets last_heartbeat = time.monotonic()
  - get_snapshot() -> dict[name -> {status, last_heartbeat, consecutive_errors,
                                    items_checked, orders_confirmed, last_error}]
    (28-02 ADDS heartbeat_age_secs to this dict)

From tests/test_health.py (existing, lines 18-22 and 101-106 — analogs to copy):
  - test_heartbeat_sets_last_heartbeat: create registry, heartbeat, assert snapshot field
  - test_snapshot_public_keys_exact: expected_keys = {"status","last_heartbeat",
    "consecutive_errors","items_checked","orders_confirmed","last_error"}

From tests/test_web_dashboard.py (existing — fixture + scaffold patterns to copy):
  - mock_svc fixture: MagicMock with get_status.return_value={"running": False},
    list_items.return_value=[]
  - client fixture: TestClient(create_app(mock_svc))
  - test_no_innerHTML_with_api_data: reads dashboard.html from disk, regex-scans for
    interpolated inner/outerHTML/insertAdjacentHTML sinks (runs unchanged; do NOT duplicate)
  - test_uplot_served: GET /static/vendor/uplot.iife.min.js -> 200 (do NOT duplicate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update snapshot-key guard and add heartbeat_age_secs unit tests</name>
  <files>tests/test_health.py</files>
  <read_first>
    - tests/test_health.py (modify — test_snapshot_public_keys_exact at lines 101-106; test_heartbeat_sets_last_heartbeat at lines 18-22 is the analog)
    - core/health.py (source of truth — _ensure last_heartbeat=0.0 sentinel; heartbeat() uses time.monotonic())
    - 28-PATTERNS.md (tests/test_health.py section — exact new-test structure)
    - 28-RESEARCH.md (Pitfall 3 — exact-set guard must be updated in the same change as the field)
  </read_first>
  <action>
    In tests/test_health.py: add "heartbeat_age_secs" to the expected_keys set in
    test_snapshot_public_keys_exact (per OBS-01/02). Add two new tests following the
    test_heartbeat_sets_last_heartbeat pattern: test_heartbeat_age_secs_fresh creates a
    HealthRegistry, calls heartbeat("PluginA"), gets the snapshot, and asserts
    heartbeat_age_secs is not None and >= 0.0; test_heartbeat_age_secs_never creates a
    HealthRegistry, calls _ensure("PluginA") only (registered but never heartbeated), gets
    the snapshot, and asserts heartbeat_age_secs is None (the last_heartbeat==0.0 sentinel
    maps to None per D — "never heartbeated"). Do NOT monkeypatch time.monotonic — monotonic
    advances naturally between heartbeat and get_snapshot. These tests are RED until 28-02
    lands the field in get_snapshot().
  </action>
  <verify>
    <automated>rtk pytest tests/test_health.py::test_snapshot_public_keys_exact tests/test_health.py::test_heartbeat_age_secs_fresh tests/test_health.py::test_heartbeat_age_secs_never -x</automated>
  </verify>
  <acceptance_criteria>
    - test_snapshot_public_keys_exact expected_keys set contains "heartbeat_age_secs"
    - test_heartbeat_age_secs_fresh and test_heartbeat_age_secs_never exist in the file
    - test_heartbeat_age_secs_never asserts `is None` for the never-heartbeated case
    - The three tests FAIL (RED) against the current get_snapshot() (expected — turns GREEN in 28-02)
    - No production code edited in this task
  </acceptance_criteria>
  <done>Snapshot-key guard updated and two heartbeat_age_secs unit tests added; all three RED pending 28-02.</done>
</task>

<task type="auto">
  <name>Task 2: Create static-scaffold tests for the three new observability sections</name>
  <files>tests/test_observability_ui.py</files>
  <read_first>
    - tests/test_web_dashboard.py (modify-free analog — copy mock_svc/client fixtures and the GET / section-assertion pattern; do NOT duplicate test_no_innerHTML_with_api_data or test_uplot_served)
    - 28-UI-SPEC.md (Section Layout Contract — #section-health, #section-buys, #section-log-viewer ids; Component Specification — #header-uptime, #log-level-filter, #log-search, #btn-log-follow, buys-table headers, MAX_LOG_LINES = 500)
    - 28-VALIDATION.md (Wave 0 Requirements — exact list of scaffold assertions)
    - web/templates/dashboard.html (current — confirms #header-uptime slot exists at line 32 and uPlot script at line 456; new ids are RED until 28-03/28-04)
  </read_first>
  <action>
    Create tests/test_observability_ui.py with `import pytest; pytest.importorskip("fastapi")`
    at the top, then copy the mock_svc and client fixtures verbatim from
    tests/test_web_dashboard.py (mock_svc must set get_status.return_value to
    {"running": False, "uptime_secs": 0, "plugins": {}} so the Jinja render and any header
    rendering have a valid status). Add static GET / assertions (each: fetch "/", assert 200,
    assert substring in resp.text): test_dashboard_renders_health_section asserts
    'id="section-health"' and "Plugin Health" present (OBS-01/02/03); test_header_uptime_slot_present
    asserts 'id="header-uptime"' present (OBS-09); test_dashboard_renders_buys_section asserts
    'id="section-buys"', "Confirmed Orders", and the four buys headers "Order ID" and
    "Checkout Attempts" present (OBS-04); test_dashboard_renders_log_viewer_section asserts
    'id="section-log-viewer"', 'id="log-buffer"', 'id="log-level-filter"', and 'id="log-search"'
    present (OBS-06/08-UI); test_log_dom_cap_constant asserts the literal "MAX_LOG_LINES = 500"
    appears in resp.text (OBS-07); test_uplot_script_and_css_present asserts both
    "/static/vendor/uplot.iife.min.js" and "/static/vendor/uplot.min.css" appear in resp.text
    (OBS-05 — assets must be linked for charts). Do NOT add a duplicate of
    test_no_innerHTML_with_api_data (it already lives in test_web_dashboard.py and covers the
    whole file). All these tests are RED until 28-03/28-04 land the HTML.
  </action>
  <verify>
    <automated>rtk pytest tests/test_observability_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_observability_ui.py exists and begins with pytest.importorskip("fastapi")
    - mock_svc fixture returns get_status with running/uptime_secs/plugins keys
    - Tests assert exact ids: section-health, header-uptime, section-buys, section-log-viewer, log-buffer, log-level-filter, log-search
    - test_log_dom_cap_constant asserts the string "MAX_LOG_LINES = 500"
    - test_uplot_script_and_css_present asserts both uplot.iife.min.js and uplot.min.css
    - No duplicate of test_no_innerHTML_with_api_data or test_uplot_served
    - The new tests FAIL (RED) against the current dashboard.html (expected — turn GREEN in 28-03/28-04)
  </acceptance_criteria>
  <done>Static-scaffold test file created with section/control/asset presence assertions; all RED pending 28-03/28-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | TestClient renders the template; no live network, localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | New JS render functions (XSS via API strings) | mitigate | This plan preserves the existing test_no_innerHTML_with_api_data guard (no duplication, no weakening); 28-03/28-04 implementations must pass it |
| T-28-SC | Tampering | npm/pip installs | accept | No new packages installed in Phase 28 — uPlot already vendored (Phase 25); no install tasks exist |
</threat_model>

<verification>
- `rtk pytest tests/test_health.py tests/test_observability_ui.py -q` runs; new tests are RED
  (expected at Wave 0). The rest of the health suite stays GREEN.
- `rtk pytest tests/test_web_dashboard.py::test_no_innerHTML_with_api_data -x` stays GREEN.
</verification>

<success_criteria>
- Snapshot-key guard updated to include heartbeat_age_secs; two heartbeat_age_secs tests added.
- New static-scaffold test file asserts all three new sections, header uptime, log controls,
  the 500-line cap constant, and uPlot assets.
- No production code modified. No new packages. Existing XSS/CSS guards untouched.
</success_criteria>

<output>
Create `.planning/phases/28-frontend-observability-surfaces/28-01-SUMMARY.md` when done
</output>
