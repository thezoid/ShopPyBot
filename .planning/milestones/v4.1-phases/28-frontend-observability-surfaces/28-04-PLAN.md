---
phase: 28-frontend-observability-surfaces
plan: 04
type: execute
wave: 4
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - web/templates/dashboard.html
autonomous: true
requirements: [OBS-04, OBS-05, OBS-06, OBS-07, OBS-08, OBS-09]
must_haves:
  truths:
    - "Confirmed-buys table renders rows (name, order_id, confirmed_at, checkout_attempts); shows an empty row when no orders (manual-UAT visual + present in DOM)"
    - "Each item shows a uPlot price chart when data exists; non-Amazon items show 'No price history available for this plugin' (manual-UAT visual)"
    - "Log viewer color-codes by level, level+search filters narrow lines, Follow auto-scrolls and pauses on scroll-up, DOM buffer capped at 500 (manual-UAT visual)"
    - "The 500-line cap constant MAX_LOG_LINES = 500 is present in the template"
  artifacts:
    - path: "web/templates/dashboard.html"
      provides: "loadConfirmedBuys(), loadPriceChart(), renderLogLines()/appendLogLine(), extended pollLogs(), follow logic"
      contains: "loadConfirmedBuys"
  key_links:
    - from: "loadPriceChart(itemLink)"
      to: "GET /api/price-history/{link_b64}"
      via: "URL-safe b64 + Date.parse(p.t)/1000 -> new uPlot"
      pattern: "Date\\.parse\\(.*\\)\\s*/\\s*1000"
    - from: "pollLogs()"
      to: "GET /api/logs?level=&search=&n=500"
      via: "URLSearchParams read from filter controls"
      pattern: "URLSearchParams"
    - from: "loadConfirmedBuys()"
      to: "GET /api/history"
      via: "makeCell textContent rows into #buys-tbody"
      pattern: "/api/history"
---

<objective>
Implement the three data-rendering surfaces in dashboard.html: the confirmed-buys table (OBS-04),
per-item uPlot price charts with explicit empty state (OBS-05), and the full log viewer with
level/search filter, Follow/pause-on-scroll, and the 500-line DOM cap (OBS-06/07/08-UI). These
fill the section scaffolds added in 28-03 and complete Phase 28.

Purpose: These are the remaining observability surfaces. The chart path has two verified traps
that MUST be honored: (1) the API "t" field is an ISO string — convert Date.parse(p.t)/1000 to
Unix seconds before uPlot (scales.x time:true silently misrenders otherwise); (2) link_b64 must
reuse removeItem()'s exact URL-safe encoding keeping '=' padding (the server's urlsafe_b64decode
requires it). Render functions stay pure for Phase 29 reuse.
Output: dashboard.html with loadConfirmedBuys(), loadPriceChart(), renderLogLines()/appendLogLine(),
extended pollLogs(), follow logic, and the page-load call wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-frontend-observability-surfaces/28-UI-SPEC.md
@.planning/phases/28-frontend-observability-surfaces/28-RESEARCH.md
@.planning/phases/28-frontend-observability-surfaces/28-PATTERNS.md

<interfaces>
GET /api/history (web/routes/api.py) -> { confirmed_orders: [ { name, order_id,
  confirmed_at (ISO string), checkout_attempts (number) } ] }  (empty list when none)
GET /api/price-history/{link_b64} -> { series: [ { t: "<ISO 8601 string>", price: <float> } ] }
  (oldest-first; {series: []} for non-Amazon or bad encoding — never 400)
GET /api/logs?level=&search=&n= -> { logs: [ "<line>" ] }  (n clamped 1..500 server-side)
  Log line format (logger.py line 50): "[LEVEL][YYYYMonthDD@HH:MM:SS] message" — the
  /^\[(\w+)\]/ regex matches the leading [LEVEL] prefix.

From web/templates/dashboard.html (current — analogs and integration points):
  - removeItem() (line 261): canonical link_b64 =
      btoa(unescape(encodeURIComponent(link))).replace(/\+/g,'-').replace(/\//g,'_')
      — copy VERBATIM for loadPriceChart; KEEP '=' padding
  - loadItems() (lines 280-306): builds #items-tbody rows from objects (item.name, item.link).
      Add per-item chart wiring here: give each <tr> a data-link attribute (link_b64) and, after
      the row is appended, call loadPriceChart(item.link) targeting a sibling chart container.
  - makeCell(text) (274-278): textContent cell helper — reuse for buys rows
  - pollLogs() (187-193): currently fetch('/api/logs') -> #log-content textContent join.
      EXTEND in-place (executor discretion confirmed) to read filter controls, pass
      ?level=&search=&n=500, and call renderLogLines(data.logs || []). Keep the existing
      #log-content pre updated too (both panels coexist).
  - Page-load calls (lines 452-454): loadItems(); loadCredentials(); loadConfig();
      ADD loadConfirmedBuys();
  - uPlot global `uPlot` available (script tag line 456). new uPlot(opts, [xArr, yArr], el).
  - CSS from 28-02: .chart-container .chart-empty .log-buffer .log-line .log-level-* .btn-accent

JS contract (28-UI-SPEC — pure render functions for Phase 29 reuse):
  loadConfirmedBuys() -> Promise<void> ; loadPriceChart(itemLink) -> Promise<void>
  renderLogLines(lines) -> void ; appendLogLine(lineText) -> void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement loadConfirmedBuys() and per-item loadPriceChart() with ISO->Unix conversion</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html (modify — makeCell 274-278; loadItems 280-306; removeItem b64 at line 261; page-load block 452-454; uPlot script line 456)
    - 28-UI-SPEC.md (28-C3 price chart placement + uPlot config + 28-C4 buys table + Empty/Error state copy)
    - 28-RESEARCH.md (Code Examples loadPriceChart with ISO conversion; Pitfall 1 ISO->Unix; Pitfall 2 link_b64; Pitfall 5 clientWidth||300; Pattern 3 encoding)
    - 28-PATTERNS.md (Safe DOM + link_b64 patterns)
  </read_first>
  <action>
    Add loadConfirmedBuys(): GET /api/history; clear #buys-tbody via innerHTML = '' (clear-only);
    if confirmed_orders empty append one <tr> with a <td colspan="4" class="text-secondary">
    "No confirmed orders yet." (createElement); else for each order append a <tr> with four
    makeCell cells (name, order_id, confirmed_at as-is ISO string, String(checkout_attempts)) —
    all textContent. On fetch error, render a single colspan-4 row "Failed to load order history.
    Check server logs.". Call loadConfirmedBuys() once in the page-load block alongside loadItems().
    Add loadPriceChart(itemLink): compute b64 with the EXACT removeItem encoding
    (btoa(unescape(encodeURIComponent(itemLink))).replace(/\+/g,'-').replace(/\//g,'_') — keep '='
    padding); locate the per-item chart container (id 'chart-' + b64); GET
    /api/price-history/{b64}; on error render a .chart-empty div "Failed to load price history.";
    if series.length === 0 render a .chart-empty div "No price history available for this plugin."
    (role="status"); else build the uPlot chart: CRITICAL convert ts = series.map(p =>
    Date.parse(p.t)/1000) (ISO string -> Unix seconds — Pitfall 1) and prices = series.map(p =>
    p.price); read stroke colors from getComputedStyle(document.documentElement).getPropertyValue
    ('--color-accent'/'--color-text-muted').trim() (no hardcoded hex); opts width =
    containerEl.clientWidth || 300, height 120, series[1].points.show = ts.length < 2 (markers when
    <2 points), scales.x={time:true}; then new uPlot(opts, [ts, prices], containerEl). In
    loadItems(), set each row's data-link to the b64 and create/append a per-item
    <div id="chart-{b64}" class="chart-container"> (executor discretion: sibling div grouped by
    data-link, avoiding nested-div-in-tr); after building all rows, call loadPriceChart(item.link)
    for each item. No innerHTML on API data anywhere (charts use the uPlot array API).
  </action>
  <verify>
    <automated>rtk pytest tests/test_web_dashboard.py::test_no_innerHTML_with_api_data tests/test_observability_ui.py::test_uplot_script_and_css_present tests/test_observability_ui.py::test_dashboard_renders_buys_section -q</automated>
  </verify>
  <acceptance_criteria>
    - loadConfirmedBuys() exists; renders an empty colspan-4 "No confirmed orders yet." row when confirmed_orders is empty; data rows via makeCell/textContent
    - loadConfirmedBuys() is called in the page-load block
    - loadPriceChart() uses the verbatim removeItem b64 encoding and keeps '=' padding
    - loadPriceChart() converts timestamps with Date.parse(p.t)/1000 BEFORE new uPlot (grep: `Date.parse` and `/ 1000` present in chart path)
    - series.length === 0 path renders the literal "No price history available for this plugin."
    - uPlot stroke colors read via getComputedStyle().getPropertyValue('--color-...') (no hex passed to uPlot)
    - test_no_innerHTML_with_api_data passes (no interpolated HTML sinks added)
    - MANUAL-UAT (deferred): open dashboard with an Amazon item (line chart renders, correct x-axis) and a non-Amazon item (empty message, no JS error); confirmed-buys table populates / shows empty row
  </acceptance_criteria>
  <done>Confirmed-buys table + per-item price charts render with ISO->Unix conversion and correct b64; XSS guard GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Implement log viewer render, extend pollLogs() with filters, add Follow + 500-line cap</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html (modify — pollLogs 187-193; the #section-log-viewer scaffold from 28-03)
    - 28-UI-SPEC.md (28-C5 log viewer — renderLogLines/appendLogLine/logLevelClass JS, MAX_LOG_LINES trim, follow/pause-on-scroll, filter wiring with ~300ms search debounce, Empty/Error copy)
    - 28-RESEARCH.md (Pattern 4 follow toggle; Pattern 5 DOM cap; Code Examples pollLogs extension + logLevelClass; Pitfall 6 read filter values from DOM each call)
    - logger.py (line 50 — "[LEVEL][ts] message" format the /^\[(\w+)\]/ regex parses)
  </read_first>
  <action>
    Add appendLogLine(lineText): create a <span class="log-line " + logLevelClass(lineText)> with
    textContent = lineText (never innerHTML) and append to #log-buffer. Add logLevelClass(line):
    match /^\[(\w+)\]/ ; map ERROR->log-level-error, WARNING->log-level-warn, INFO->log-level-info,
    DEBUG->log-level-debug, TRACE->log-level-trace; default log-level-info on no match. Add
    renderLogLines(lines): clear #log-buffer via innerHTML = '' (clear-only); if lines empty append
    a <p class="text-secondary"> "No log lines match the current filter."; else appendLogLine each;
    enforce the DOM cap with `const MAX_LOG_LINES = 500; while (buffer.children.length >
    MAX_LOG_LINES) buffer.removeChild(buffer.firstChild);` (OBS-07 — the literal MAX_LOG_LINES = 500
    must appear); then call maybeScrollToBottom(). Extend pollLogs() in-place: read level =
    #log-level-filter.value and search = #log-search.value from the DOM each call (Pitfall 6),
    build URLSearchParams({n:500}) adding level/search only when non-empty, fetch '/api/logs?'+params,
    call renderLogLines(data.logs || []); keep updating the existing #log-content pre too; on error
    render the "Failed to load logs. Retrying..." text-secondary into #log-buffer. Add follow logic:
    module-scoped logFollowEnabled=true; a scroll listener on #log-buffer that sets
    logFollowEnabled=false and removes .btn-accent + sets aria-pressed=false when the user scrolls
    up (atBottom = scrollHeight - scrollTop <= clientHeight + 8); a click listener on #btn-log-follow
    that toggles logFollowEnabled, toggles .btn-accent, sets aria-pressed, and scrolls to bottom when
    re-enabled; maybeScrollToBottom() that scrolls to bottom only when logFollowEnabled. Wire the
    level <select> change and the search <input> input (debounced ~300ms) and #btn-log-refresh click
    to call pollLogs(). Do NOT add a new setInterval — reuse the existing pollLogs interval.
  </action>
  <verify>
    <automated>rtk pytest tests/test_observability_ui.py::test_dashboard_renders_log_viewer_section tests/test_observability_ui.py::test_log_dom_cap_constant tests/test_web_dashboard.py::test_no_innerHTML_with_api_data -q</automated>
  </verify>
  <acceptance_criteria>
    - appendLogLine sets span.textContent (never innerHTML); logLevelClass maps the five levels via /^\[(\w+)\]/
    - renderLogLines clears via clear-only innerHTML='' and shows "No log lines match the current filter." on empty
    - The literal "MAX_LOG_LINES = 500" appears and a trim loop removes firstChild beyond 500 (test_log_dom_cap_constant passes)
    - pollLogs() reads level+search from the DOM each call and builds URLSearchParams with n=500
    - Follow logic: pause on scroll-up (remove .btn-accent, aria-pressed=false), resume on click (add .btn-accent, scroll to bottom)
    - test_no_innerHTML_with_api_data passes; full tests/test_observability_ui.py passes
    - MANUAL-UAT (deferred): generate logs; confirm per-level colors, level filter narrows, search debounces, Follow auto-scrolls then pauses on scroll-up, buffer stays <=500 lines
  </acceptance_criteria>
  <done>Log viewer renders color-coded, filterable, follow-capable, 500-capped; all 28-01 scaffold tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/logs lines → DOM | log lines are API-sourced strings that may contain attacker-influenced content |
| GET /api/history fields → DOM | item name / order_id are API-sourced strings |
| GET /api/price-history → uPlot | numeric arrays only; no string interpolation into chart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Tampering | appendLogLine (DOM XSS via log content) | mitigate | span.textContent = lineText (never innerHTML); guarded by test_no_innerHTML_with_api_data |
| T-28-08 | Tampering | loadConfirmedBuys (XSS via item name/order_id) | mitigate | makeCell sets td.textContent; clear-only innerHTML='' for tbody reset |
| T-28-09 | Tampering | loadPriceChart (chart container id / chart data) | mitigate | id uses link_b64 (URL-safe chars only); uPlot receives numeric arrays; colors via getComputedStyle (declared tokens, not page content) |
| T-28-10 | Input Validation | level/search query params | accept | Sanitized server-side by read_logs_filtered (Phase 26); client only forwards values |
| T-28-SC | Tampering | npm/pip installs | accept | No package installs; uPlot already vendored |
</threat_model>

<verification>
- `rtk pytest tests/test_observability_ui.py -q` — full file GREEN (all four surfaces' scaffold + cap tests pass).
- `rtk pytest tests/test_web_dashboard.py -q` — XSS guard + MC-4 banner GREEN.
- `rtk pytest -q` — full suite GREEN before /gsd:verify-work.
</verification>

<success_criteria>
- Confirmed-buys table, per-item price charts (ISO->Unix, correct b64, empty state), and the full
  log viewer (color/filter/follow/500-cap) all implemented via pure render functions.
- Zero innerHTML-with-data; uPlot fed numeric arrays with theme colors from CSS tokens.
- Phase 28 complete: all OBS-01..09 surfaces render; no new packages.
</success_criteria>

<output>
Create `.planning/phases/28-frontend-observability-surfaces/28-04-SUMMARY.md` when done
</output>
