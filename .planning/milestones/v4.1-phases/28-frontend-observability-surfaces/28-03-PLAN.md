---
phase: 28-frontend-observability-surfaces
plan: 03
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - web/templates/dashboard.html
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-09]
must_haves:
  truths:
    - "Each plugin renders a health card with status badge, colored heartbeat age, error/checked/orders-confirmed stats (manual-UAT visual)"
    - "Heartbeat age is colored green <30s / amber 30-60s / red >=60s or null (manual-UAT visual)"
    - "Bot uptime appears humanized in #header-uptime when running, blank when stopped (manual-UAT visual)"
    - "#section-health and #section-buys and #section-log-viewer scaffolds exist in the rendered HTML"
  artifacts:
    - path: "web/templates/dashboard.html"
      provides: "Three new section scaffolds + health-card render + uptime render wired into pollStatus()"
      contains: "section-health"
  key_links:
    - from: "dashboard.html pollStatus()"
      to: "renderHealthCards(data) + renderUptime(data.uptime_secs)"
      via: "calls appended after existing status-dot updates"
      pattern: "renderHealthCards"
    - from: "renderHealthCards"
      to: "data.plugins (from GET /api/status)"
      via: "createElement/textContent per plugin"
      pattern: "createElement"
---

<objective>
Add the three new section scaffolds to dashboard.html and implement the health-cards + uptime
surfaces (OBS-01/02/03/09), wired into the existing pollStatus() loop. The empty buys and
log-viewer section shells are placed in this plan so all three new sections exist in one HTML
edit; their data-rendering JS lands in 28-04. This turns the section-presence and header-uptime
scaffold tests GREEN.

Purpose: Health cards are the primary observability surface. Render functions must be pure
(take data, update DOM) so Phase 29 can call them from an SSE handler unchanged. Uptime reuses
the #header-uptime slot built in Phase 25.
Output: dashboard.html with #section-health/#section-buys/#section-log-viewer scaffolds,
renderHealthCards(), renderUptime(), and helper functions, all called from pollStatus().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-frontend-observability-surfaces/28-UI-SPEC.md
@.planning/phases/28-frontend-observability-surfaces/28-RESEARCH.md
@.planning/phases/28-frontend-observability-surfaces/28-PATTERNS.md

<interfaces>
GET /api/status (web/routes/api.py) returns svc.get_status() directly:
  { running: bool, uptime_secs: number, plugins: { <name>: {
      status, last_heartbeat, consecutive_errors, items_checked,
      orders_confirmed, last_error, heartbeat_age_secs } } }
  - heartbeat_age_secs: float (seconds) or null (never heartbeated) — added in 28-02

From web/templates/dashboard.html (current — analogs and insertion points):
  - #header-uptime slot exists at line 32 (set via textContent only)
  - Section card pattern: <section class="card" id="section-{name}"><h2>...</h2>...</section>
    Insert order per 28-UI-SPEC: #section-health BEFORE #section-items;
    #section-buys AFTER #section-items; #section-log-viewer AFTER #section-config
  - pollStatus() (lines 159-185): try/fetch('/api/status')/json; update status-dot/label/buttons.
    APPEND renderUptime(data.uptime_secs) and renderHealthCards(data) inside the try after
    the existing button updates (do NOT replace the try/catch structure).
  - loadCredentials() (lines 309-353): the multi-element createElement chain to copy for cards
  - CSS classes available from 28-02: .health-grid .health-card .health-card-name
    .health-card-stats .health-card-error .badge .badge-ok/neutral/err .status-dot.error
    .heartbeat-age .heartbeat-ok/warn/err .text-secondary

JS contract (28-UI-SPEC JS Function Architecture — must stay pure for Phase 29 reuse):
  renderHealthCards(statusPayload) -> void ; renderUptime(uptimeSecs) -> void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three section scaffolds to dashboard.html</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html (modify — section card pattern at lines 41-57; insertion points per 28-UI-SPEC Section Layout Contract)
    - 28-UI-SPEC.md (Section Layout Contract + 28-C4 buys-table HTML + 28-C5 log-viewer HTML — exact ids, headings, control markup, ARIA attrs)
    - 28-VALIDATION.md (Wave 0 Requirements — the scaffold ids the tests assert)
    - tests/test_observability_ui.py (the 28-01 scaffold tests this turns GREEN)
  </read_first>
  <action>
    Insert three new <section class="card"> blocks into the container per the 28-UI-SPEC layout:
    (1) #section-health with <h2>Plugin Health</h2> and an empty <div class="health-grid"
    id="health-grid"></div>, placed immediately before #section-items; (2) #section-buys with
    <h2>Confirmed Orders</h2> and the full buys-table skeleton from 28-C4 (thead with the four
    headers Item / Order ID / Confirmed At / Checkout Attempts, and <tbody id="buys-tbody">),
    placed immediately after #section-items; (3) #section-log-viewer with <h2>Log Viewer</h2> and
    the log-controls + log-buffer markup from 28-C5 (label-for-associated <select id="log-level-filter">
    with options All levels/ERROR/WARNING/INFO/DEBUG/TRACE, <input type="text" id="log-search"
    placeholder="Search logs...">, <button id="btn-log-follow" class="btn btn-accent"
    aria-pressed="true">Follow</button>, <button id="btn-log-refresh" class="btn btn-sm">Refresh</button>,
    and <div id="log-buffer" class="log-buffer" role="log" aria-live="polite" aria-label="Log output">),
    placed after #section-config. Keep the existing inline #log-content pre in #section-controls
    (both panels coexist in Phase 28 per UI-SPEC). Do NOT touch the MC-4 banner block. The
    buys-tbody and log-buffer stay empty here — 28-04 fills them.
  </action>
  <verify>
    <automated>rtk pytest tests/test_observability_ui.py::test_dashboard_renders_health_section tests/test_observability_ui.py::test_dashboard_renders_buys_section tests/test_observability_ui.py::test_dashboard_renders_log_viewer_section tests/test_observability_ui.py::test_header_uptime_slot_present -x</automated>
  </verify>
  <acceptance_criteria>
    - GET / contains 'id="section-health"', "Plugin Health", and 'id="health-grid"'
    - GET / contains 'id="section-buys"', "Confirmed Orders", "Order ID", "Checkout Attempts", 'id="buys-tbody"'
    - GET / contains 'id="section-log-viewer"', 'id="log-buffer"', 'id="log-level-filter"', 'id="log-search"', 'id="btn-log-follow"'
    - The existing #log-content pre and the MC-4 banner block remain present and unchanged
    - test_banner_renders_when_non_local and test_banner_absent_when_local still pass
  </acceptance_criteria>
  <done>Three section scaffolds inserted; section-presence + header-uptime scaffold tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Implement renderHealthCards() + renderUptime() and wire into pollStatus()</name>
  <files>web/templates/dashboard.html</files>
  <read_first>
    - web/templates/dashboard.html (modify — loadCredentials createElement chain at 309-353; pollStatus at 159-185; makeCell at 274-278)
    - 28-UI-SPEC.md (28-C1 health card HTML + heartbeatClass/badgeClass/dotClass JS + 28-C2 humanizeUptime JS; Empty/Error states copy)
    - 28-RESEARCH.md (Code Examples — heartbeatClass/heartbeatText; Anti-Patterns — no innerHTML on API data)
    - 28-PATTERNS.md (Safe DOM + Poll extension patterns)
    - tests/test_web_dashboard.py (test_no_innerHTML_with_api_data — the guard new code must satisfy)
  </read_first>
  <action>
    Add pure render functions to the inline script. renderHealthCards(statusPayload): read
    statusPayload.plugins; clear #health-grid via `grid.innerHTML = ''` (clear-only, no
    interpolation — allowed); if plugins is empty/absent append a <p class="text-secondary"> with
    "No plugins registered."; else for each [name, rec] build a <div class="health-card"
    data-plugin="{name}"> via createElement with: a .health-card-name span (textContent = name),
    a .badge span whose class adds badgeClass(rec.status) containing a .status-dot whose class adds
    dotClass(rec.status) plus a .badge-label span (textContent = a human status text), a
    .heartbeat-age span whose class adds heartbeatClass(rec.heartbeat_age_secs) with textContent
    from heartbeatText (per 28-C1: "Heartbeat: {N}s" or "Heartbeat: never"), a .health-card-stats
    div with three spans textContent "Errors: {consecutive_errors}", "Checked: {items_checked}",
    "Orders confirmed: {orders_confirmed}", and — only when rec.last_error is truthy — a
    .health-card-error span (textContent = rec.last_error, already scrubbed to class name in Phase 26).
    All API values via textContent ONLY (UI-03). Add helper functions heartbeatClass(ageSecs)
    (null/undefined or >=60 -> 'heartbeat-err'; <30 -> 'heartbeat-ok'; else 'heartbeat-warn'),
    heartbeatText(ageSecs) (null/undefined -> 'Heartbeat: never'; else `Heartbeat: ${ageSecs}s`),
    badgeClass(status) ('running'->'badge-ok','error'->'badge-err',else 'badge-neutral'),
    dotClass(status) ('running'->'running','error'->'error',else 'stopped'). Add renderUptime(secs):
    set #header-uptime textContent to "Up {h}h {m}m" / "Up {m}m" when secs>0, else '' (28-C2
    humanizeUptime). In pollStatus(), inside the existing try after the button updates, call
    renderUptime(data.uptime_secs) and renderHealthCards(data). On the pollStatus catch path after
    3 failures, set #health-grid to a single <p class="text-secondary"> "Failed to load plugin
    status. Check server logs." (createElement, not innerHTML interpolation). Do NOT add a
    setInterval — reuse the existing pollStatus interval.
  </action>
  <verify>
    <automated>rtk pytest tests/test_web_dashboard.py::test_no_innerHTML_with_api_data tests/test_observability_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - renderHealthCards and renderUptime exist; both are pure (signature: data in, DOM out, no fetch inside)
    - heartbeatClass returns 'heartbeat-err' for null AND for >=60; 'heartbeat-ok' for <30; 'heartbeat-warn' for 30-59 (OBS-02 bands)
    - All plugin/error/stat values set via textContent; no innerHTML/outerHTML/insertAdjacentHTML with ${} interpolation
    - pollStatus() calls renderUptime(data.uptime_secs) and renderHealthCards(data) inside the existing try
    - test_no_innerHTML_with_api_data passes; full tests/test_observability_ui.py passes
    - MANUAL-UAT (deferred per autonomous policy): open dashboard with an active plugin; verify the card shows badge + colored heartbeat age cycling green→amber→red, the three stat lines, and the header shows "Up Nh Mm"
  </acceptance_criteria>
  <done>Health cards + uptime render from pollStatus() via pure functions; XSS guard GREEN; scaffold tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/status JSON → DOM | plugin names, status, last_error are API-sourced strings crossing into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | renderHealthCards (XSS via plugin name / last_error) | mitigate | All API values set via textContent; only clear-only `innerHTML = ''` used; guarded by test_no_innerHTML_with_api_data |
| T-28-05 | Information Disclosure | last_error sub-label | accept | Renders the already-scrubbed exc.__class__.__name__ from Phase 26; no further client scrubbing needed |
| T-28-06 | Tampering | staleness band classification | mitigate | Client only numerically compares the server value (<30/<60/else); no eval, no string-to-code path |
| T-28-SC | Tampering | npm/pip installs | accept | No package installs; uPlot already vendored |
</threat_model>

<verification>
- `rtk pytest tests/test_observability_ui.py -q` — section-presence + header-uptime tests GREEN
  for the surfaces this plan owns (buys/log data tests still pass on scaffold presence).
- `rtk pytest tests/test_web_dashboard.py -q` — XSS guard + MC-4 banner tests GREEN.
- `rtk pytest -q` — no regressions.
</verification>

<success_criteria>
- Three section scaffolds present in rendered HTML; #header-uptime populated by renderUptime.
- renderHealthCards/renderUptime are pure functions called from pollStatus().
- Zero innerHTML-with-data; OBS-02 three-band logic correct; MC-4 banner intact.
</success_criteria>

<output>
Create `.planning/phases/28-frontend-observability-surfaces/28-03-SUMMARY.md` when done
</output>
