---
milestone: v4.2
name: Release Readiness
audited: 2026-07-02T23:59:00Z
status: tech_debt
scores:
  requirements: 20/20
  phases: 6/6
  integration: 6/6   # all checked cross-phase boundaries WIRED, no breaks found
  flows: 8/9          # 1 flow (BF-02 double-buy guard) incomplete for 5/7 plugins
gaps: {}              # no unsatisfied/orphaned requirements; no cross-phase BLOCKER found
nyquist:
  compliant_phases: []
  partial_phases: [30, 31, 32, 33, 34, 35]
  missing_phases: []
  overall: PARTIAL     # all 6 VALIDATION.md exist but nyquist_compliant:false / status:draft (never flipped post-execution)
tech_debt:
  - phase: 30-breakfix-hardening
    items:
      - "WARNING (MED-01, code-level, confirmed still open in current source): BF-02's write-ahead place-order marker (mark_place_order_attempted_sync / order_marker_link) is wired into place_order_guarded for Amazon and BestBuy ONLY. The 5 community plugins (Walmart, Target, GameStop, NewEgg, SquareEnix) call place_order_guarded(place_order.click) with no order_marker_link, so a swallowed timeout/exception at their place-order stage is fully retryable -- the exact double-buy mechanism BF-02 (HIGH) was created to close. This was a deliberate, pre-declared scope decision in 30-CONTEXT.md (BF-02's HIGH concern is the Amazon place-order path) and is re-confirmed live by 30-REVIEW.md's deep code review. All 5 affected plugins are independently EXPERIMENTAL/selector-unverified (lowering real-world likelihood of reaching place-order), but the residual risk is real and identical in mechanism/severity to the pre-fix Amazon bug. Suggested fix (from 30-REVIEW.md): move the marker write into place_order_guarded itself so all 7 (and future) plugins inherit it for free instead of per-plugin copy-paste."
      - "Resolved-but-notable: 30-REVIEW.md found and fixed a genuine CRITICAL bug (CR-01) post-verification -- the place-order marker was originally written unconditionally BEFORE the test_mode/monitor_only suppression check, which would have permanently latched (poisoned) any item reached under the documented test_mode:true default and fired a false possibly_placed alert. Confirmed fixed in current source (marker write now lives inside place_order_guarded, gated on a real click) via order_marker_link kwarg in core/plugin_base.py. VERIFICATION.md's own cited evidence reflects the PRE-fix shape and was not re-run after the review fix landed -- stale citation only, not a functional gap."
      - "Deferred (LOW-02, acknowledged no-action): _checkout_stage doubles as informal state + retry-control signal with no reset-on-entry guarantee; documented as an acceptable convention, not regression-tested."
      - "Deferred UAT: Amazon WAF live-challenge acceptance (2captcha injection payload shape undocumented, only confirmable against a real AWS-WAF challenge); BF-02 live double-buy edge proof against a real slow-drop; community-plugin (5) live login-selector accuracy; BestBuy post-login landing-page signal is URL-only (no live-verified DOM selector)."
  - phase: 31-ci-security-infrastructure
    items:
      - "CI-verification debt, confirmed still open by a live check during this audit: gitleaks.yml Actions runs are ALL startup_failure (12/12 runs checked live via gh run list) because the repo's Actions permissions are still allowed_actions:selected with gitleaks/gitleaks-action not yet allowlisted. codeql-analysis.yml shows zero registered runs. Both are blocked on the operator widening the Actions allowlist, not a code gap -- YAML/config correctness for both workflows was independently verified."
      - "CI-verification debt, confirmed still open by a live check: all 7 Dependabot alerts (#6-#12, cryptography/pydantic-settings/jinja2) still report state:open on GitHub. Each vulnerable_version_range was cross-checked against the already-applied local version bumps and all 7 are satisfied by the new pins -- the fix is code-complete; the queue only drains after this branch's PR (#11, open, targeting master) merges and GitHub rescans."
      - "31-REVIEW.md WR-01 (gitleaks.yml missing explicit permissions block) and WR-02 (dependabot.yml referenced labels that did not exist) were both fixed in a follow-up commit (8f69d1d) -- confirmed live: gitleaks.yml now has an explicit permissions block, and python/github-actions labels now exist on the GitHub repo. Note: 31-REVIEW.md's own frontmatter/status was never updated to resolved -- a minor doc-hygiene lag, not a functional gap."
      - "Info-only, no action needed (31-REVIEW.md IN-01/IN-02/IN-03): config.yml secret-guard pathspec is root-only by design; gitleaks/CI trigger footprint is push/PR to master+dev only (matches pre-existing ci.yml convention); cryptography 49.0.0 drops 32-bit Windows/x86_64 macOS wheels (irrelevant to this project's 64-bit CI matrix)."
  - phase: 32-release-automation-community-readiness
    items:
      - "Operator-action, confirmed still open by a live check: GitHub Private Vulnerability Reporting is disabled (gh api repos/.../private-vulnerability-reporting -> enabled:false). SECURITY.md/CODE_OF_CONDUCT.md's advisories/new links will 404 until an operator flips this in repo Settings."
      - "Operator-action, confirmed still open by a live check: Actions allowlist (allowed_actions:selected) still blocks googleapis/release-please-action; release-please.yml is not yet registered on GitHub's default branch (404 from the workflow API) because this branch's changes have not merged to master yet (PR #11 open, mergeable, targeting master)."
      - "Policy decision made autonomously in the operator's absence (documented override in 32-VERIFICATION.md): RH-07 resolved to GitHub PVR-only (no published email) instead of a maintainer email. Needs explicit operator sign-off per CLAUDE.md's approval-gate rule for decisions made while the operator was away."
      - "32-REVIEW.md WR-04 (CODE_OF_CONDUCT.md routes conduct/harassment reports through the vulnerability-disclosure-specific GitHub feature, not a dedicated conduct channel) -- resolved as operator debt flagged for a dedicated conduct-report channel rather than code-fixed; still effectively open pending an operator decision."
      - "Deferred UAT: none new beyond the operator-action items above (this phase produces static config/docs artifacts)."
  - phase: 33-config-refactor
    items:
      - "Deferred UAT: live Amazon/BestBuy availability-poll cadence is now jittered (30-40s, was flat 30s) as a deliberate, pre-accepted behavior change (Option A) -- code path is fully proven by test_amazon_config_activates_poll_jitter; only the live-cadence/no-new-WAF-friction observation itself is operator debt."
      - "33-REVIEW.md IN-01 (typo'd platform key silently falls back to defaults with no diagnostic) acknowledged-pre-existing, no action -- documented CFG-02 tradeoff."
  - phase: 34-feature-completion
    items:
      - "Deferred UAT (visual/browser-only, both themes): plugin log-filter dropdown correctness and live-tail SSE filter behavior; Analytics stat-cards + per-plugin table rendering incl. N/A state. Code/JS wiring independently confirmed correct via static analysis and integration tests; only pixel/theme rendering is unconfirmed."
  - phase: 35-audit-fixes-doc-hygiene-cleanup
    items:
      - "Info (IN-01, acknowledged by design, no action): two independent remove-item code paths now exist (SSR POST /items/remove vs. the pre-existing JS DELETE /api/items/link) that must be kept in sync by any future maintainer touching either one."
      - "Optional, non-blocking: a live JS-disabled-browser manual spot-check of the SSR remove button is available but judged redundant with existing FastAPI TestClient proof (a real unmodified HTTP POST with no JS)."
      - "35-REVIEW.md found and fixed a genuine CRITICAL bug (CR-01) post-verification: POST /items/remove crashed with an unhandled 500 when link was submitted as a multipart file field instead of plain text (unauthenticated, no Origin header needed). Confirmed fixed in current source (isinstance str guard) with a dedicated regression test. IN-02 (missing .inline-remove-form CSS rule) also fixed. Both fixes post-date 35-VERIFICATION.md, which does not reflect them -- functionally resolved, doc-lag only."
  - phase: cross-milestone
    items:
      - "Nyquist doc-hygiene gap (found by this audit, not previously flagged): ALL 6 v4.2 phase VALIDATION.md files (30-35) still read status:draft / nyquist_compliant:false / wave_0_complete:false / Approval:pending, despite every phase's own VERIFICATION.md independently confirming a passing test suite and satisfied requirements. This is the same category of staleness that DH-01/DH-03 (this milestone's own Phase 35) reconciled for OLDER phases (18-24, 25-27) -- but the fix was never applied to v4.2's own 6 phases. Recommend a small follow-up before/at milestone close."
      - "STATE.md schema drift (pre-existing, explicitly out-of-scope per 35-CONTEXT.md/35-RESEARCH.md): custom Last-action/Next-action prose fields are not machine-parseable by GSD SDK verbs; STATE.md also has visibly corrupted/duplicated table fragments mid-file."
      - "Stale project CLAUDE.md Architecture section (pre-existing, explicitly out-of-scope per 35-CONTEXT.md/35-RESEARCH.md/32-REVIEW.md): still describes the pre-refactor single-process while-True / amazon_bot.py / bestbuy_bot.py design; the live codebase is the plugin/orchestrator/web-dashboard architecture. Confirmed still present verbatim in CLAUDE.md at time of this audit."
      - "ROADMAP.md's own Progress table footer is stale: shows v4.2 as 0/TBD Active and not yet planned, both predating all 6 phases actually completing -- cosmetic doc lag, not a functional issue."
      - "Deferred UAT carried forward, unrelated to v4.2 code but part of the accumulated operator backlog: 17 items from the v4.0 close (STATE.md) + 8 items from the v4.1 close (STATE.md) -- all pre-accepted live-environment UAT debt, none are code gaps, none re-verified in this audit (out of v4.2 scope)."
---

# Milestone v4.2 -- Release Readiness -- Audit Report

**Audited:** 2026-07-02
**Phases:** 30 (Breakfix Hardening), 31 (CI & Security Infrastructure), 32 (Release Automation & Community Readiness), 33 (Config Refactor), 34 (Feature Completion), 35 (Audit-Fixes & Doc-Hygiene Cleanup)
**Requirements:** 20 (RH-01..07, AF-01..03, BF-01..03, CFG-01..02, FC-01..02, DH-01..03)
**Status:** tech_debt -- all 20 requirements satisfied, no cross-phase blocker found, but the accumulated deferred/operator debt (including one code-level completeness gap, BF-02's 5-plugin propagation) warrants explicit operator review before/at milestone close.

## Test Suite

.venv\Scripts\python.exe -m pytest -q (run independently during this audit, not copied from a phase claim): **940 passed, 2 skipped, 49.05s** -- matches the milestone brief exactly.

---

## 1. Requirements: 3-Source Cross-Reference (20/20 satisfied)

Cross-checked (a) .planning/REQUIREMENTS.md traceability table, (b) each phase's VERIFICATION.md Requirements Coverage table, (c) each plan's SUMMARY.md requirements-completed frontmatter.

| Req | Description (short) | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|-----|----------------------|-------|:---:|:---:|:---:|:---:|
| BF-01 | Amazon WAF auto-solve via 2captcha, manual-pause fallback | 30 | [x] Complete | SATISFIED | 30-02: [BF-01] | satisfied |
| BF-02 (HIGH) | Place-order-timeout double-buy idempotency guard | 30 | [x] Complete | SATISFIED (non-blocking alert-cadence caveat) | 30-01: [BF-02], 30-04: [BF-02] | satisfied (guard mechanism proven; see tech-debt note -- 5/7 plugins do not yet call it) |
| BF-03 | Plugin login verified via real post-login signals | 30 | [x] Complete | SATISFIED | 30-01/03/05/06: [BF-03] | satisfied |
| RH-01 | No tracked secret/credential artifact; clean scan | 31 | [x] Complete | SATISFIED (local half); CI-run-green is CI debt | 31-01: [RH-01] | satisfied |
| RH-04 | CodeQL workflow on supported action versions | 31 | [x] Complete | SATISFIED (local half); CI-run-green is CI debt | 31-02: [RH-04] | satisfied |
| RH-05 | dependabot.yml exists; alerts reviewed/remediated | 31 | [x] Complete | SATISFIED (local half); queue-drain is CI debt | 31-03: [RH-05] | satisfied |
| RH-02 | release-please seeded at 2.0.0, python release-type | 32 | [x] Complete | SATISFIED (code-complete); Actions-run-green is operator debt | 32-01: [RH-02] | satisfied |
| RH-03 | pyproject.toml canonical version = 2.0.0 | 32 | [x] Complete | SATISFIED | 32-01: [RH-03] | satisfied |
| RH-06 | README accuracy (platforms, install, badges, URL) | 32 | [x] Complete | SATISFIED | 32-02: [RH-06] | satisfied |
| RH-07 | Real maintainer security contact, zero placeholder | 32 | [x] Complete | SATISFIED via documented override (PVR instead of email) | 32-03: [RH-07] | satisfied (override recorded; operator sign-off pending -- tech debt, not a gap) |
| CFG-01 | Canonical delay-field names + back-compat shim | 33 | [x] Complete | SATISFIED | 33-01: [CFG-01] | satisfied |
| CFG-02 | Plugin declares own config section, zero core edits | 33 | [x] Complete | SATISFIED | 33-02: [CFG-02] | satisfied |
| FC-01 | [plugin] log tag + /api/logs plugin filter | 34 | [x] Complete | SATISFIED | 34-01: [FC-01], 34-02: [FC-01] | satisfied |
| FC-02 | Outcome analytics (success-rate, time-to-checkout) | 34 | [x] Complete | SATISFIED | 34-03: [FC-02] | satisfied |
| AF-01 | SSR items-table remove works without JS | 35 | [x] Complete | SATISFIED | 35-01: [AF-01] | satisfied |
| AF-02 | Raw last_heartbeat scrubbed from both surfaces | 35 | [x] Complete | SATISFIED | 35-02: [AF-02] | satisfied |
| AF-03 | Dead escHtml() removed | 35 | [x] Complete | SATISFIED | 35-01: [AF-03] | satisfied |
| DH-01 | v4.1 VALIDATION.md (25/26/27) status/wave flags fixed | 35 | [x] Complete | SATISFIED | 35-03: [DH-01] | satisfied |
| DH-02 | v4.1 SUMMARY.md (27/28/29) requirements: frontmatter added | 35 | [x] Complete | SATISFIED | 35-03: [DH-02] | satisfied |
| DH-03 | v4.0 VALIDATION.md (18-24) nyquist_compliant fixed | 35 | [x] Complete | SATISFIED | 35-03: [DH-03] | satisfied |

**Score: 20/20 satisfied. 0 unsatisfied. 0 orphaned.** Every REQ-ID in the traceability table appears in exactly one phase's VERIFICATION.md Requirements Coverage table and at least one plan's SUMMARY.md requirements-completed frontmatter; no REQ-ID is claimed-but-unverified or verified-but-unclaimed.

**Notable nuance (not a status downgrade):** BF-02 and RH-07 are both "satisfied" via a documented, explicit scope/override decision rather than a literal 1:1 match to the ROADMAP wording. Both are called out prominently in the tech-debt section above and should be reviewed by the operator, but neither rises to "partial" or "unsatisfied" under the workflow's status matrix: the underlying mechanism in both cases is implemented, tested, and verified as designed; what remains is either (a) propagation to more call sites (BF-02) or (b) an operator policy sign-off on an already-working substitute channel (RH-07).

---

## 2. Phase Verification Summary

| Phase | Status | Score | Critical Gaps | Non-Critical / Deferred |
|-------|--------|-------|:---:|---|
| 30 -- Breakfix Hardening | human_needed | 19/20 truths (1 partial, non-blocking) | 0 | 4 live-UAT items; MED-01 (5-plugin BF-02 gap, see tech debt) |
| 31 -- CI & Security Infrastructure | human_needed | 11/14 must-haves local; 3 CI-verification debt | 0 | gitleaks/CodeQL live-green-run + Dependabot queue-drain all confirmed still pending (live-checked this audit) |
| 32 -- Release Automation & Community Readiness | human_needed | 5/5 local scope (1 via override) | 0 | PVR toggle + Actions allowlist confirmed still pending (live-checked this audit); RH-07 channel sign-off pending |
| 33 -- Config Refactor | human_needed | 9/9 | 0 | 1 live-cadence UAT item (jitter) |
| 34 -- Feature Completion | human_needed | 16/16 | 0 | 2 live-browser visual-rendering UAT items (both themes) |
| 35 -- Audit-Fixes & Doc-Hygiene Cleanup | passed | 13/13 | 0 | 1 acknowledged design note (dual remove pathways); post-verification review found+fixed 1 critical bug (CR-01) |

All 6 phases have a VERIFICATION.md (no "unverified phase" blocker). human_needed status on 5/6 phases is exclusively live-environment/CI-execution verification that is structurally impossible to complete in this sandbox (per each VERIFICATION.md's own frontmatter and this milestone's "code-complete + CI-green, live-UAT stays operator debt" definition of done) -- not a code gap. Phase 35 is the only phase that reached passed because it required no live-browser/live-retailer observation.

Every phase's REVIEW.md (post-verification deep code review) was also read for this audit. Phase 30's review found and the executor fixed one genuine CRITICAL regression (CR-01: BF-02 marker permanently poisoning items under the documented test_mode:true default) and Phase 35's review found and fixed a second CRITICAL bug (CR-01: unhandled 500 on a malformed link form field in the new SSR remove route) -- both confirmed present and fixed in current source by this audit, both covered by dedicated regression tests. No phase has an open, unresolved CRITICAL review finding.

---

## 3. Cross-Phase Integration Findings

All boundaries below were verified by reading current source (not summary claims) and confirming the wiring is real, not just present.

| From (phase) | To (phase) | What was checked | Status | Evidence |
|---|---|---|---|---|
| 30 (BF-02 marker, BF-03 login ABC) | 33 (delay-field rename, get_platform_config) | core/plugin_base.py and all 7 plugin files carry both phases' changes without conflict | WIRED | login(self) -> bool ABC (30) and get_platform_config() (33) coexist cleanly in core/plugin_base.py; all 7 plugins retain async def login() -> bool plus _verify_login_generic calls after the CFG-01 refactor touched the same files' delay-field usage |
| 30 (marker write via order_marker_link) | 34 (analytics reads place_order_attempted_at) | core/analytics.py / models.get_order_analytics_rows_sync() correctly consume the Phase-30 DB column | WIRED | models.py:119-133 selects place_order_attempted_at; core/analytics.py:77-88 uses it as the attempted denominator and time_to_checkout anchor, exactly matching Phase 30's column semantics |
| 30 (platform_key per plugin, login verification) | 34 (set_log_plugin / log tagging by platform_key) | core/orchestrator.py:supervise() and core/registry.py:_plugin_tag resolve the same platform_key both phases rely on | WIRED | core/orchestrator.py:112 calls set_log_plugin(_plugin_tag(plugin)); core/service.py:172 (list_plugins) uses the identical _plugin_tag helper for the dashboard dropdown -- single source of truth, no drift |
| 33 (delay_seconds/delay_jitter canonical fields) | 30 (orchestrator retry/marker logic) | core/orchestrator.py both reads the new canonical config fields (_get_plugin_sleep) and the Phase-30 _pre_attempt_check/_PossiblyPlaced guard in the same module without interference | WIRED | Both functions live side-by-side in core/orchestrator.py (lines ~293-320 and ~420-506); zero references to legacy min_delay/max_delay remain in the orchestrator |
| 31 (CI workflows: gitleaks, CodeQL, ci.yml) | 32 (release-please workflow) | All 4 .github/workflows/*.yml files parse, use consistent action-version conventions, and do not collide on triggers/permissions | WIRED | All 4 workflows independently valid YAML; release-please.yml correctly scoped to contents:write,pull-requests:write only, consistent with codeql-analysis.yml's explicit scoping; no duplicate job/workflow names. Both gitleaks.yml and release-please.yml are equally blocked by the same root cause today (Actions allowlist), confirmed live -- operator debt, not a wiring break |
| 34 (dashboard.html: log-plugin-filter dropdown, analytics section) | 35 (dashboard.html: SSR remove-item form, dead escHtml() removal) | Both phases edit web/templates/dashboard.html; checked for ID collisions / JS interference | WIRED | #log-plugin-filter, #section-analytics, and action="/items/remove" all present with no ID collisions; escHtml grep-confirmed 0 occurrences repo-wide; no JS listener from Phase 34's additions targets the Phase 35 SSR form |

**Integration score: 6/6 checked boundaries WIRED.** No cross-phase import/export break, no contract mismatch, no silent overwrite found between any of the 6 phases' changes.

### E2E Flow Verification

| # | Flow | Status | Detail |
|---|------|--------|--------|
| 1 | Amazon/BestBuy place-order timeout -> marker written -> retry aborted, possibly_placed alert fires | COMPLETE | order_marker_link wired through place_order_guarded for both plugins; _pre_attempt_check raises _PossiblyPlaced; proven by dedicated tests, and by this audit's direct source read (CR-01 fix confirmed present) |
| 2 | 5 community plugins place-order timeout -> marker written -> retry aborted | BROKEN (WARNING, not BLOCKER) | None of Walmart/Target/GameStop/NewEgg/SquareEnix pass order_marker_link to place_order_guarded (confirmed via grep against current source) -- a swallowed timeout at their place-order stage is fully retryable. Pre-declared deferred scope (30-CONTEXT.md), re-surfaced at MEDIUM by 30-REVIEW.md, still open. See tech_debt above |
| 3 | Amazon WAF challenge detected -> 2captcha solve -> token injected, OR any failure -> manual-pause fallback | COMPLETE | _solve_or_pause traced end-to-end; 5 distinct fallback paths each proven by a dedicated test |
| 4 | All 7 plugins: login() -> _verify_login_generic -> auto_buy aborts on False | COMPLETE | Confirmed in all 7 plugin files' current source, not just tests |
| 5 | Legacy min_delay/max_delay config -> shim -> canonical delay_seconds/delay_jitter -> orchestrator jitter sleep | COMPLETE | Shim, orchestrator consumer, and sample.config.yml all consistent; README carries no stale field references |
| 6 | New/community plugin declares an undeclared platforms.key section -> validates via its own model, zero core edits | COMPLETE | PlatformsConfig(extra="allow") + RetailerPlugin.get_platform_config(), proven by a fixture-plugin test |
| 7 | Plugin runs -> set_log_plugin tags lines -> /api/logs?plugin= filters (snapshot AND live SSE tail) -> dashboard dropdown | COMPLETE | Dropdown populated from list_plugins()'s real platform_keys; SSE 'log' listener gated through the same lineMatchesFilters guard as the one-shot snapshot -- closes the live-tail-bypasses-filter gap the plan explicitly called out |
| 8 | Confirmed-order DB rows -> compute_analytics -> /api/analytics -> dashboard stat cards + per-plugin table | COMPLETE | Pure function, fixture-exact assertions, no link/credential leakage in the response (tested) |
| 9 | Dashboard SSR form -> POST /items/remove -> CSRF check -> remove_item -> redirect, safe on malformed input | COMPLETE | Functional + CSRF + malformed-input (CR-01) tests all present and passing; confirmed hardened in current source |

**Flow score: 8/9 complete.** Flow #2 is the one genuine, currently-open E2E break -- classified WARNING (not BLOCKER) because it is pre-declared, deliberately-scoped debt from phase-discussion time (not a silently-missed connection), the guard mechanism itself is proven and platform-agnostic (only the 5 call sites are missing), and the affected plugins are independently flagged EXPERIMENTAL with unverified selectors, making it unlikely in practice they currently reach the place-order stage at all. It is, however, a real residual double-buy exposure identical in mechanism to the bug BF-02 (HIGH) was created to close, and should not be left open indefinitely.

Release-automation and CI-security-scan "flows" (conventional commit -> release-please PR; push -> gitleaks/CodeQL scan result) are code-complete and correctly wired but currently blocked from live execution by operator-gated GitHub Settings (Actions allowlist, confirmed live via gh api) -- these are tracked as operator-action items, not counted against the flow score, consistent with this milestone's own "done = code-complete + CI-green-pending-operator-action" definition.

---

## 4. Nyquist Compliance (Discovery Only)

| Phase | VALIDATION.md | nyquist_compliant | status | wave_0_complete | Classification |
|-------|:---:|:---:|:---:|:---:|---|
| 30 | exists | false | draft | false | PARTIAL |
| 31 | exists | false | draft | false | PARTIAL |
| 32 | exists | false | draft | false | PARTIAL |
| 33 | exists | false | draft | false | PARTIAL |
| 34 | exists | false | draft | false | PARTIAL |
| 35 | exists | false | draft | false | PARTIAL |

**Overall: PARTIAL (0/6 compliant).** Every VALIDATION.md has a fully-populated per-requirement verification map and a passing test suite behind it (confirmed via each phase's VERIFICATION.md and this audit's independent full-suite run), but none of the 6 phases had their frontmatter flipped to nyquist_compliant:true / status:validated at execution close -- the exact same staleness pattern that this milestone's own Phase 35 (DH-01, DH-03) reconciled for the older v4.0/v4.1 phases, but never applied to v4.2's own phases. This is a recommended, low-cost follow-up (frontmatter-only, mirrors the DH-01/03 pattern exactly) rather than a re-validation need -- no phase shows red/pending test evidence.

---

## 5. Tech Debt & Operator-Action Aggregate

### Deferred live-UAT items

| Source | Count | Items |
|--------|:---:|---|
| v4.0 milestone close (carried, STATE.md) | 17 | Phases 18-24 live-monitor/confirmation/form-fill/timeout/supervisor/session/headless-audio UAT + 7 phase human_needed verification statuses + WAF auto-solve TODO + SEED-001/002 dormant |
| v4.1 milestone close (carried, STATE.md) | 8 | Phases 27-29.1 live-SSE/browser-render UAT + WAF/SEED items carried into v4.2 scope |
| v4.2 new -- Phase 30 | 4 | Amazon WAF live-challenge acceptance; BF-02 live double-buy edge proof; 5-community-plugin live login-selector accuracy; BestBuy post-login landing-page DOM signal |
| v4.2 new -- Phase 33 | 1 | Live Amazon/BestBuy poll-cadence jitter observation (30-40s vs. flat 30s), no new WAF/rate friction |
| v4.2 new -- Phase 34 | 2 | Plugin log-filter dropdown visual/theme check; Analytics stat-cards + table visual/theme check |
| Total | 32 | |

### Operator-action items (repo/GitHub Settings, confirmed live during this audit)

| Item | Live-confirmed status |
|------|---|
| Enable GitHub Private Vulnerability Reporting | Confirmed disabled (gh api repos/.../private-vulnerability-reporting -> enabled:false) |
| RH-07 channel-choice sign-off (PVR-only vs. maintainer email) | Pending -- autonomous decision made in operator's absence, needs explicit approval |
| Widen Actions allowlist for gitleaks/gitleaks-action + googleapis/release-please-action | Confirmed still selected-only (allowed_actions:"selected"); gitleaks confirmed 12/12 live runs = startup_failure; release-please not yet registered on default branch |
| release-please first-release trigger (contingent on the allowlist item + merging this branch's PR #11 to master) | Pending -- PR #11 open, mergeable, targeting master |
| Add a LICENSE file, if open-sourcing | Confirmed absent (ls LICENSE* -> not found); README correctly omits a license badge to match |
| CI-verification debt (contingent on the allowlist item above): gitleaks live-green run, CodeQL live-green run, Dependabot alert-queue drain (7 alerts, all still open, all confirmed satisfied by the already-applied version pins) | Confirmed still open via live gh queries this audit |
| A dedicated conduct-report channel (CODE_OF_CONDUCT.md currently routes through the vulnerability-disclosure-specific GitHub feature, per 32-REVIEW.md WR-04) | Open, operator decision needed |

### Code-level tech debt (highest priority for a future closure phase)

- BF-02 5-plugin propagation gap (see Section 3, Flow #2) -- the single item in this audit closest to requirement-relevant risk. Recommend folding into a small follow-up phase: thread order_marker_link through the 5 community plugins' place_order_guarded calls (mechanical, mirrors the existing Amazon/BestBuy pattern exactly).
- Nyquist frontmatter lag across all 6 v4.2 phases (Section 4) -- low-cost, frontmatter-only fix.
- STATE.md schema drift (custom Last-action/Next-action prose fields not SDK-verb-parseable; visibly corrupted/interleaved table fragments mid-file) -- pre-existing, explicitly flagged out-of-scope by Phase 35's own research, carried forward again here.
- Stale CLAUDE.md Architecture section -- still describes the pre-refactor amazon_bot.py/bestbuy_bot.py/while-True design; confirmed still present verbatim. Pre-existing, explicitly flagged out-of-scope by Phase 35's own research and Phase 32's review, carried forward again here.
- ROADMAP.md Progress-table footer staleness -- shows "0/TBD -- Active" and "not yet planned" for v4.2 despite all 6 phases being complete; cosmetic only.

---

## 6. Verdict

**Status: tech_debt.**

All 20 v4.2 requirements are satisfied by an independent 3-source cross-reference, with zero unsatisfied or orphaned requirements. All 6 cross-phase integration boundaries checked in this audit are genuinely WIRED -- no broken import, no silent overwrite, no contract mismatch found between any two phases' changes. 8 of 9 traced E2E flows are code-complete; the 9th (community-plugin BF-02 propagation) is a real, currently-open, HIGH-adjacent risk that was nonetheless a deliberate, pre-declared scope decision at planning time, not a silent miss, and does not by itself invalidate BF-02's "satisfied" status as scoped.

This does not clear the bar for a clean passed, because the accumulated deferred/operator debt is substantial and includes items an operator should explicitly see and decide on before treating v4.2 as fully closed: the BF-02 5-plugin gap, 5 unresolved live GitHub-Settings actions (PVR, Actions allowlist x2, license, conduct channel) all independently reconfirmed still open by live checks during this audit, an RH-07 policy decision made in the operator's absence, and a Nyquist-frontmatter lag across all 6 of this milestone's own phases that mirrors the exact pattern this milestone's own Phase 35 fixed for older phases but did not apply to itself.

No BLOCKER-level finding exists. Nothing here requires reopening a phase or forcing gaps_found.

---

_Audited: 2026-07-02_
_Auditor: integration-checker (autonomous milestone audit run)_
