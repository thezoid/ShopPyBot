---
phase: 10-optional-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - core/cli/web.py
  - core/cli/__init__.py
  - web/__init__.py
  - web/routes/__init__.py
  - web/routes/api.py
  - web/routes/credentials.py
  - web/routes/config.py
  - web/routes/pages.py
  - web/security.py
  - web/log_reader.py
  - web/config_web.py
  - tests/test_web_app.py
  - tests/test_web_security.py
  - tests/test_web_items.py
  - tests/test_web_controls.py
  - tests/test_web_credentials.py
  - tests/test_web_config.py
  - tests/test_web_dashboard.py
  - tests/test_web_mod02.py
autonomous: false
requirements: [GUI-05, GUI-06]
user_setup:
  - service: "[web] optional extra (fastapi, uvicorn, jinja2, python-multipart)"
    why: "Web dashboard runtime dependencies; ASSUMED legitimacy (slopcheck unavailable at research time)"
    install: "pip install .[web]"

must_haves:
  truths:
    - "pip install .[web] installs fastapi, uvicorn, jinja2, python-multipart (exact-pinned)"
    - "create_app(svc) returns a FastAPI app with StaticFiles mounted and routers included"
    - "shoppybot web --host 0.0.0.0 prints a stderr security warning before serving"
    - "shoppybot web prints the pip-install hint when fastapi is absent (CLI-04 intact)"
    - "is_localhost detects loopback for 127.0.0.1, localhost, ::1, [::1]:port, 127.0.0.1:port"
    - "Every web test module skips cleanly when fastapi is absent (pytest.importorskip)"
  artifacts:
    - path: "pyproject.toml"
      provides: "[project.optional-dependencies] web + web* in packages.find include"
      contains: "fastapi==0.115.8"
    - path: "web/__init__.py"
      provides: "create_app(svc, is_non_local=False) factory"
      contains: "def create_app"
    - path: "web/security.py"
      provides: "is_localhost + check_origin Depends"
      contains: "def is_localhost"
    - path: "web/log_reader.py"
      provides: "read_recent_logs(n=50) matching logger.py %Y%B%d format"
      contains: "%Y%B%d"
    - path: "web/config_web.py"
      provides: "WEB_ALLOWLIST + read/write reusing config_cmd ALLOWLIST + _atomic_yaml_write"
      contains: "from core.cli.config_cmd import"
    - path: "core/cli/web.py"
      provides: "handle_web body: lazy import + warning + uvicorn.run"
      contains: "from web import create_app"
    - path: "tests/test_web_mod02.py"
      provides: "AST guard scanning web/**/*.py for forbidden imports"
      contains: "rglob"
  key_links:
    - from: "core/cli/web.py:handle_web"
      to: "web.create_app"
      via: "lazy import inside function body"
      pattern: "from web import create_app"
    - from: "web/__init__.py:create_app"
      to: "web.routes.api/credentials/config/pages routers"
      via: "include_router inside factory body"
      pattern: "include_router"
    - from: "web/config_web.py"
      to: "core.cli.config_cmd"
      via: "import ALLOWLIST + _atomic_yaml_write (no duplication)"
      pattern: "from core.cli.config_cmd import"
---

<objective>
Lay the Phase 10 foundation: the `[web]` optional extra, the lazy-imported `web/` package skeleton with a working `create_app(svc)` factory, the security layer (`is_localhost` + `check_origin`), the log reader, the config-web helper, the filled `handle_web` body, and the complete Wave 0 test scaffold for every downstream plan.

Purpose: Establish the contracts (create_app signature, app.state.svc, check_origin Depends, route module layout) that Plans 02-05 build against without exploring the codebase. Lock GUI-05 (optional extra + CLI-04) and GUI-06 (non-local warning + is_localhost) here. Create all test files so downstream plans turn red tests green.
Output: pyproject `[web]` extra; `web/` package with factory, security, log_reader, config_web, and stub route modules; filled handle_web; eight Wave 0 test files (skip-guarded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-optional-web-ui/10-RESEARCH.md
@.planning/phases/10-optional-web-ui/10-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Use directly; no codebase exploration. -->

From core/cli/web.py (Phase 9 stub — module top imports ONLY `sys`; keep that invariant):
```
def handle_web(args, svc=None) -> int
```

From core/cli/__init__.py build_parser(), the web subparser (lines 117-121) currently has NO --host/--port args. They must be added:
```
web_p = sub.add_parser("web", help="...")
web_p.set_defaults(func=handle_web)
```

From core/service.py:
```
class BotService:
    def __init__(self, cfg: Optional[AppConfig] = None) -> None
    def get_config(self) -> AppConfig
    def get_status(self) -> dict          # {"running": bool}
    def list_items(self) -> list          # list of 5-tuples (name, link, auto_buy, qty, purchased)
    def add_item(self, name, link, auto_buy, quantity) -> None
    def remove_item(self, link) -> None
    def start(self, cvv: Optional[str] = None) -> None
    def stop(self) -> None
```

From core/cli/config_cmd.py (import, do not duplicate):
```
ALLOWLIST: dict[str, tuple[str, type]] = {"test_mode": ("debug", bool), "logging_level": ("debug", int)}
def _atomic_yaml_write(path: Path, data: dict) -> None
```

From core/config_schema.py:
```
from core.config_schema import _DEFAULT_YAML_PATH    # Path to config.yml
# AppConfig.debug.{logging_level:int, test_mode:bool}
# AppConfig.notifications.{sound:bool, discord.enabled:bool, email.enabled:bool, sms.enabled:bool}
# Platforms have NO `enabled` field — see config-scope note below.
```

From logger.py line 45 — log filename format (MUST match exactly):
```
f"{datetime.now().strftime('%Y%B%d')}.log"   # e.g. 2026June04.log (title-case month)
```

From tests/conftest.py: fixture `tmp_data_dir(tmp_path, monkeypatch)` redirects the DB to tmp_path.
</interfaces>

<config_scope_note>
RESOLVED (research open question #2): Platforms (amazon, bestbuy, ...) have NO `enabled` field in AppConfig. The UI-SPEC Section 4 lists per-platform toggles, but only notifier toggles are schema-backed. Scope WEB_ALLOWLIST to: test_mode, logging_level, notifications.sound, notifications.discord.enabled, notifications.email.enabled, notifications.sms.enabled. Do NOT add a platform `enabled` schema field (YAGNI / out of scope).
</config_scope_note>

<verified_skeletons>
RESEARCH.md "Code Examples" and PATTERNS.md contain the live-verified create_app, handle_web, is_localhost, check_origin, and read_recent_logs implementations. Follow those exactly.
</verified_skeletons>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — eight skip-guarded web test files</name>
  <read_first>
    - tests/test_cli_no_fastapi.py (importorskip / sys.modules pattern)
    - tests/test_cli_mod02.py (AST scan to copy for test_web_mod02.py)
    - tests/conftest.py (tmp_data_dir fixture)
    - .planning/phases/10-optional-web-ui/10-RESEARCH.md (Code Examples: TestClient skip guard, SC3, SC2, SC6 snippets)
    - .planning/phases/10-optional-web-ui/10-PATTERNS.md (tests/test_web.py + tests/test_web_mod02.py sections)
    - .planning/phases/10-optional-web-ui/10-VALIDATION.md (per-task verification map)
  </read_first>
  <behavior>
    - Each of the eight test files starts with `import pytest` then `pytest.importorskip("fastapi")` so the whole module skips when fastapi is absent (GUI-05).
    - test_web_app.py: asserts create_app(MagicMock()) returns an app, GET / returns 200, static mount exists, GET /api/status returns 200 with "running" key.
    - test_web_security.py: is_localhost matrix (127.0.0.1, localhost, ::1, [::1]:8000, 127.0.0.1:8000 -> True; 0.0.0.0, 192.168.1.5 -> False); non-local handle_web warning (monkeypatch uvicorn.run to no-op, host=0.0.0.0, assert "WARNING" in stderr); non-local banner flag (create_app(svc, is_non_local=True) sets app.state.is_non_local True); CSRF 403 (POST /api/items with origin http://evil.com -> 403).
    - test_web_items.py: GET /api/items serializes BotService.list_items 5-tuples; POST /api/items calls svc.add_item; DELETE /api/items/{b64} calls svc.remove_item; SC2 parity test uses real BotService + tmp_data_dir.
    - test_web_controls.py: POST /api/bot/start calls svc.start (no cvv); POST /api/bot/stop calls svc.stop; GET /api/status returns running bool; GET /api/logs returns list.
    - test_web_credentials.py: GET /api/credentials returns name + is_set only (no "value" field); POST /api/credentials returns {"status":"ok"} and secret value absent from resp.text (SC3); POST calls get_store().set.
    - test_web_config.py: GET /api/config returns test_mode + logging_level + notifier toggles; POST /api/config writes allowlisted key; unknown key -> 422.
    - test_web_dashboard.py: GET / renders all four section headings (Controls, Items, Credentials, Config) and the CTA copy from UI-SPEC.
    - test_web_mod02.py: AST scan of web/**/*.py (rglob) for forbidden names {orchestrator, registry, models, add_items_sync, get_items_sync, remove_item_sync, add_items}; asserts no violations.
  </behavior>
  <action>
    Create all eight test files under tests/. Copy the importorskip skip-guard from tests/test_cli_no_fastapi.py lines 1-6 to the top of each web test module. Copy tests/test_cli_mod02.py verbatim into tests/test_web_mod02.py, change cli_dir to `Path(__file__).parent.parent / "web"`, and change `.glob("*.py")` to `.rglob("*.py")` so web/routes/ is scanned. Use MagicMock-based svc fixtures (svc.get_status.return_value = {"running": False}, svc.list_items.return_value = []) for unit tests and real BotService + tmp_data_dir for the SC2 parity test. For SC6 use monkeypatch.setattr(uvicorn, "run", lambda *a, **kw: None) then call handle_web with a fabricated args object having host="0.0.0.0" and port=8000 and assert "WARNING" in capsys.readouterr().err. Use header origin "http://127.0.0.1:8000" on all valid state-changing POSTs in tests. These tests reference routes that Plans 02-05 implement; some will fail RED until those plans run — that is expected. This task's verify only confirms the files exist and are import-clean (collect-only) plus the MOD-02 guard and security tests (which target this plan's own files) pass. The importorskip guard at the top of each new web test module must NOT alter the no-fastapi path: tests/test_cli_no_fastapi.py must still pass once all eight files exist (CLI-04 / GUI-05 seam intact).
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_app.py tests/test_web_security.py tests/test_web_items.py tests/test_web_controls.py tests/test_web_credentials.py tests/test_web_config.py tests/test_web_dashboard.py tests/test_web_mod02.py --collect-only -q</automated>
    <automated>python -m pytest tests/test_cli_no_fastapi.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_web_*.py --collect-only -q` exits 0 (all eight modules import-clean)
    - Each web test file's first two non-docstring lines are `import pytest` and `pytest.importorskip("fastapi")`
    - tests/test_web_mod02.py uses `rglob("*.py")` and scans the `web` directory
    - With fastapi blocked (`monkeypatch.setitem(sys.modules,"fastapi",None)` equivalent), the modules skip rather than error
    - tests/test_cli_no_fastapi.py still passes after the web test files exist (CLI-04 seam intact): the importorskip guard must not break the no-fastapi path
  </acceptance_criteria>
  <done>Eight skip-guarded web test files exist; pytest collection succeeds; MOD-02 guard scans web/ via rglob; tests/test_cli_no_fastapi.py still passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>About to install the [web] optional extra. Per the Package Legitimacy Audit in 10-RESEARCH.md, slopcheck was unavailable at research time, so fastapi, uvicorn, jinja2, and python-multipart are tagged [ASSUMED]. They are mainstream PyPI packages but were not slopcheck-verified.</what-built>
  <how-to-verify>
    1. Confirm each package on its registry page:
       - https://pypi.org/project/fastapi/0.115.8/
       - https://pypi.org/project/uvicorn/0.30.6/
       - https://pypi.org/project/jinja2/3.1.4/
       - https://pypi.org/project/python-multipart/0.0.32/
    2. Confirm the source repos match (fastapi/fastapi, encode/uvicorn, pallets/jinja, andrew-d/python-multipart).
    3. Approve to allow the pyproject `[web]` extra and `pip install .[web]`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the [web] extra, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: pyproject [web] extra + web/ package skeleton + security + log_reader + config_web</name>
  <read_first>
    - pyproject.toml (current [project] + [tool.setuptools.packages.find])
    - core/cli/config_cmd.py (ALLOWLIST + _atomic_yaml_write to import)
    - core/config_schema.py lines 54-56, 194-200 (debug + notifications fields)
    - logger.py line 45 (log filename format)
    - .planning/phases/10-optional-web-ui/10-PATTERNS.md (web/__init__.py, web/security.py, web/log_reader.py, web/config_web.py, pyproject sections)
    - .planning/phases/10-optional-web-ui/10-RESEARCH.md (verified skeletons)
  </read_first>
  <action>
    Edit pyproject.toml: add `[project.optional-dependencies]` with `web = ["fastapi==0.115.8", "uvicorn[standard]==0.30.6", "jinja2==3.1.4", "python-multipart==0.0.32"]`, and add `"web*"` to the `[tool.setuptools.packages.find]` include list. Run pip install .[web] (approved in the checkpoint).
    Create web/__init__.py with `create_app(svc, is_non_local: bool = False) -> FastAPI`: FastAPI(title="ShopPyBot Dashboard", docs_url=None, redoc_url=None); set app.state.svc = svc and app.state.is_non_local = is_non_local; mount StaticFiles at /static from web/static; inside the factory body import the four routers (web.routes.api, web.routes.credentials, web.routes.config, web.routes.pages) and include_router each (api/credentials/config under prefix "/api", pages with no prefix). All fastapi imports live only in web/ files.
    Create web/routes/__init__.py (empty package marker).
    Create web/security.py with is_localhost(host) (strip IPv6 brackets, strip IPv4 port, accept {"127.0.0.1","localhost","::1"}, else ipaddress.ip_address(h).is_loopback) and check_origin(request) Depends (None origin -> allow; compare urlparse(origin).hostname against request.url.hostname and the local-hosts set; raise HTTPException(403, "CSRF: origin rejected") on mismatch) exactly per the RESEARCH/PATTERNS verified code.
    Create web/log_reader.py with read_recent_logs(n=50) using `datetime.datetime.now().strftime("%Y%B%d") + ".log"` under `Path(__file__).parent.parent / "logs"`; return [] if file absent; else splitlines()[-n:].
    Create web/config_web.py importing `from core.cli.config_cmd import ALLOWLIST, _atomic_yaml_write` and `from core.config_schema import _DEFAULT_YAML_PATH`; define WEB_ALLOWLIST = {**ALLOWLIST, "notifications.sound": (...), "notifications.discord.enabled": (...), "notifications.email.enabled": (...), "notifications.sms.enabled": (...)} per the config-scope note (notifier toggles only; NO platform enables). Provide read_web_config(svc) returning the six allowlisted current values and write_web_config(key, value) that gates on WEB_ALLOWLIST, coerces to bool/int, then read-modify-writes config.yml via _atomic_yaml_write (handling nested notifications.* paths). Keep functions under 30 lines; split helpers if needed.
    Do NOT implement route bodies here — Plan 01 only needs config_web helpers and security; the route bodies are filled by later plans. Create the four web/routes/*.py files as importable stubs each defining `router = APIRouter()` with NO endpoints yet (so create_app's include_router succeeds). Later plans add endpoints to api.py (Plan 02/03), credentials.py (Plan 04), config.py + pages.py (Plan 05).
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_security.py tests/test_web_mod02.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml,tomllib;d=tomllib.load(open('pyproject.toml','rb'));assert d['project']['optional-dependencies']['web'][0]=='fastapi==0.115.8'"` exits 0
    - pyproject `[tool.setuptools.packages.find]` include list contains `"web*"`
    - `python -c "from web import create_app; from unittest.mock import MagicMock; create_app(MagicMock())"` exits 0
    - `python -c "from web.security import is_localhost; assert is_localhost('127.0.0.1:8000') and is_localhost('[::1]:8000') and not is_localhost('0.0.0.0')"` exits 0
    - `python -c "from web.log_reader import read_recent_logs; read_recent_logs(5)"` exits 0
    - web/config_web.py contains `from core.cli.config_cmd import` (no duplicated ALLOWLIST/writer)
    - `python -m pytest tests/test_web_security.py tests/test_web_mod02.py -q` exits 0
  </acceptance_criteria>
  <done>[web] extra installs; create_app importable and constructs an app; is_localhost matrix correct; config_web reuses config_cmd; security + mod02 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Fill handle_web body + add --host/--port to the web subparser</name>
  <read_first>
    - core/cli/web.py (Phase 9 stub — keep module-top `import sys` only)
    - core/cli/__init__.py lines 117-123 (web subparser to extend)
    - tests/test_cli_no_fastapi.py (CLI-04 guard that must stay green)
    - .planning/phases/10-optional-web-ui/10-PATTERNS.md (core/cli/web.py replacement body)
    - .planning/phases/10-optional-web-ui/10-RESEARCH.md (handle_web verified body)
  </read_first>
  <action>
    Fill handle_web(args, svc=None): keep the existing lazy `import fastapi` ImportError guard that prints the EXACT string "Web UI is not installed. Run: pip install .[web]" to stderr and returns 1 (CLI-04). This copy MUST match the 10-UI-SPEC Copywriting Contract row "Web extra not installed (CLI)" verbatim — do NOT use "FastAPI is not installed." After the guard, do `from web import create_app`, `import uvicorn`, `from core.service import BotService`, `from web.security import is_localhost` — all inside the function body, never at module top. Read host = getattr(args, "host", "127.0.0.1") and port = getattr(args, "port", 8000). Compute is_non_local = not is_localhost(host). If is_non_local, print the exact warning to stderr: "WARNING: ShopPyBot dashboard is binding to a non-local interface. Credential management is exposed on a non-local interface. Use only on a trusted private network." Then resolved_svc = svc if svc is not None else BotService(); app = create_app(resolved_svc, is_non_local=is_non_local); uvicorn.run(app, host=host, port=port); return 0.
    In core/cli/__init__.py extend the web subparser with `web_p.add_argument("--host", default="127.0.0.1", help="Bind address (default 127.0.0.1; non-local prints a security warning).")` and `web_p.add_argument("--port", type=int, default=8000, help="Bind port (default 8000).")`. Keep `web_p.set_defaults(func=handle_web)`. Note: core/cli/__init__.py imports handle_web at module top already — this is fine because handle_web's body is lazy; the function reference does not import fastapi.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_no_fastapi.py tests/test_web_security.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli_no_fastapi.py -q` exits 0 (CLI-04 intact; no top-level fastapi leak)
    - core/cli/web.py module-level imports are limited to `sys` (no `import fastapi`, no `from web` at top)
    - When fastapi is absent, the exact string "Web UI is not installed. Run: pip install .[web]" appears on stderr (matches 10-UI-SPEC Copywriting Contract verbatim; not "FastAPI is not installed.")
    - `python -c "from core.cli import build_parser; p=build_parser(); a=p.parse_args(['web','--host','0.0.0.0','--port','9000']); assert a.host=='0.0.0.0' and a.port==9000"` exits 0
    - SC6 test `tests/test_web_security.py::test_non_local_host_warning` (or equivalent name) exits green
    - `python -m pytest tests/test_web_security.py -q` exits 0
  </acceptance_criteria>
  <done>handle_web serves create_app via uvicorn; --host/--port wired; non-local warning fires; CLI-04 guard green; ImportError copy matches UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package install → environment | pip pulls [web] extra packages ([ASSUMED] legitimacy) |
| CLI args → bind interface | --host controls network exposure of the credential-managing UI |
| browser → FastAPI routes | untrusted HTTP crosses here (mitigations in Plans 02-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | pip install fastapi/uvicorn/jinja2/python-multipart | mitigate | Blocking human-verify checkpoint before the [web] extra is added/installed; verify each on pypi.org per audit |
| T-10-01 | Elevation of Privilege | non-localhost bind exposes credential UI (GUI-06) | mitigate | is_localhost loopback detection; stderr warning before uvicorn.run; is_non_local flag set on app.state for the in-page banner (rendered in Plan 05) |
| T-10-02 | Information Disclosure | fastapi import leak breaks CLI-04 / could load web stack on every CLI call | mitigate | All fastapi imports confined to web/ package; handle_web body-only lazy import; test_cli_no_fastapi.py guard kept green |
| T-10-03 | Tampering | config_web writes arbitrary keys to config.yml | mitigate | WEB_ALLOWLIST gate (test_mode, logging_level, four notifier toggles only); unknown key rejected (422 in Plan 05) |
</threat_model>

<verification>
- `python -m pytest tests/test_web_security.py tests/test_web_mod02.py tests/test_cli_no_fastapi.py -q` green
- `pip install .[web]` succeeds (Windows: uvicorn falls back to h11; uvloop/httptools skipped)
- Full suite with fastapi present: `python -m pytest -q` green; web route tests for Plans 02-05 may be RED until those plans land (expected at this wave)
- Default-install regression: `python -m pytest tests/test_cli_no_fastapi.py -q` green
</verification>

<success_criteria>
- [web] extra exact-pinned and installable; web* discoverable as a package (GUI-05)
- create_app(svc) factory constructs an app with StaticFiles + four routers (GUI-01 foundation)
- is_localhost correct across IPv4/IPv6/port forms; non-local bind warns on stderr before serve (GUI-06)
- CLI-04 invariant intact (no top-level fastapi import outside web/)
- All eight Wave 0 web test files exist, skip-guarded, collect cleanly
</success_criteria>

<output>
Create `.planning/phases/10-optional-web-ui/10-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
