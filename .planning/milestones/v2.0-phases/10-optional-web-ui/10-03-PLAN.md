---
phase: 10-optional-web-ui
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - web/routes/credentials.py
requirements: [GUI-03]
autonomous: true

must_haves:
  truths:
    - "GET /api/credentials returns each SECRET_KEY as {name, is_set} — never a value field"
    - "POST /api/credentials stores via get_store().set and returns {'status':'ok'} only"
    - "The submitted secret value never appears in the HTTP response body or any log (SC3)"
    - "Credential POST is origin-checked (403 on cross-origin)"
  artifacts:
    - path: "web/routes/credentials.py"
      provides: "GET + POST /credentials routes; status-only responses"
      contains: "is_set"
  key_links:
    - from: "web/routes/credentials.py"
      to: "core.credentials.get_store / SECRET_KEYS"
      via: "get_store().get(k) for status; get_store().set(k,v) for write"
      pattern: "get_store\\(\\)"
    - from: "web/routes/credentials.py POST"
      to: "web.security.check_origin"
      via: "dependencies=[Depends(check_origin)]"
      pattern: "Depends\\(check_origin\\)"
---

<objective>
Implement the credentials routes in `web/routes/credentials.py` (GUI-03). GET returns key NAME + is_set boolean only; POST writes via CredentialStore.set and returns a status only. The secret value must never appear in any response body, HTML, localStorage, or server log (SC3).

Purpose: Manage credentials over HTTP with zero secret exposure to the client side — the highest-risk requirement of the phase.
Output: filled `web/routes/credentials.py`; test_web_credentials.py green including the SC3 leak assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-optional-web-ui/10-PATTERNS.md
@.planning/phases/10-optional-web-ui/10-RESEARCH.md

<interfaces>
web/routes/credentials.py is included under prefix "/api" by create_app (Plan 01), so declare @router.get("/credentials") -> served at /api/credentials.

From core/credentials.py:
```
SECRET_KEYS: list[str]                 # 19 canonical secret key names
def get_store() -> CredentialStore     # process-wide store; lazy EnvVarBackend fallback
# CredentialStore.get(key) -> str | None
# CredentialStore.set(key, value) -> None
```

From web/security.py (Plan 01):
```
def check_origin(request: Request) -> None
```
</interfaces>

<route_contract>
- GET  /credentials  -> {"credentials": [{"name": k, "is_set": get_store().get(k) is not None} for k in SECRET_KEYS]}.  NO value field, ever.
- POST /credentials  [Depends(check_origin)] -> body {"key","value"}; validate key ∈ SECRET_KEYS (else 422 {"status":"error","detail":"unknown key"}); get_store().set(key, value); return {"status":"ok"}. NEVER echo value, even in error responses. Do NOT log the value.
</route_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Credentials GET (name + is_set) and POST (set, status-only)</name>
  <read_first>
    - web/routes/credentials.py (Plan 01 stub: `router = APIRouter()`)
    - core/credentials.py (SECRET_KEYS, get_store, CredentialStore.get/set)
    - web/security.py (check_origin)
    - tests/test_web_credentials.py (SC3 leak assertions + set-calls-store test)
    - .planning/phases/10-optional-web-ui/10-PATTERNS.md (credential route — secret never in response)
    - .planning/phases/10-optional-web-ui/10-UI-SPEC.md (Section 3 Credentials: "Set"/"Not set", status-only responses)
  </read_first>
  <behavior>
    - GET /api/credentials returns one entry per SECRET_KEY with keys "name" and "is_set" only; no "value" key on any entry.
    - POST /api/credentials {"key":"AMZ_EMAIL","value":"super-secret-12345"} with valid origin: returns 200 {"status":"ok"}; the literal secret value is absent from resp.text.
    - POST with key not in SECRET_KEYS returns 422 {"status":"error","detail":...} and the value is absent from the response.
    - POST calls get_store().set(key, value) exactly once (assert via patched get_store).
    - Cross-origin POST (origin http://evil.com) returns 403.
  </behavior>
  <action>
    Fill web/routes/credentials.py. Import APIRouter, Depends, Request from fastapi; JSONResponse from fastapi.responses; `from core.credentials import SECRET_KEYS, get_store`; `from web.security import check_origin`. Keep `router = APIRouter()`. Add GET /credentials: build `[{"name": k, "is_set": get_store().get(k) is not None} for k in SECRET_KEYS]` and return JSONResponse({"credentials": [...]}). NEVER include the value. Add POST /credentials with `dependencies=[Depends(check_origin)]`: `body = await request.json()`; `key = body.get("key")`; if key not in SECRET_KEYS return JSONResponse({"status":"error","detail":"unknown key"}, status_code=422) — do not echo the value; else `get_store().set(key, body["value"])` and return JSONResponse({"status":"ok"}). Do NOT call writeLog with the value; do not include the value in any branch's response. Keep handlers under 30 lines.
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_credentials.py tests/test_web_mod02.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_web_credentials.py -q` exits 0
    - GET /api/credentials response: no entry contains a "value" key (SC3)
    - POST /api/credentials response body does not contain the submitted secret value in any branch (ok, 422, 403)
    - POST calls get_store().set once with (key, value) (assert via patched get_store)
    - web/routes/credentials.py contains no `writeLog(` call that includes the value, and no `import models/orchestrator/registry`
    - `python -m pytest tests/test_web_mod02.py -q` exits 0
  </acceptance_criteria>
  <done>Credential GET exposes only name+is_set; POST sets via store and returns status only; secret never leaks; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/credentials | plaintext secret crosses into the server (one direction only) |
| server → GET /api/credentials response | must carry status metadata only, never secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Information Disclosure | secret value echoed in any HTTP response/HTML (GUI-03/SC3) | mitigate | Routes return {"status":"ok"} / {"status":"error","detail":...} only; GET returns name+is_set; SC3 test asserts `secret not in resp.text` for every branch |
| T-10-09 | Information Disclosure | secret value written to a log line | mitigate | No writeLog call includes the value; CRED-06 log-reader stays clean |
| T-10-10 | Spoofing | local-malware CSRF POSTs a credential to 127.0.0.1 | mitigate | dependencies=[Depends(check_origin)] on POST /credentials; 403 on origin mismatch |
| T-10-11 | Tampering | arbitrary key write (path/key injection) | mitigate | key ∈ SECRET_KEYS gate; unknown key -> 422, no store write |
</threat_model>

<verification>
- `python -m pytest tests/test_web_credentials.py tests/test_web_mod02.py -q` green
- SC3 assertions: secret value absent from GET body, POST ok body, POST 422 body
- `python -m pytest -q` green
</verification>

<success_criteria>
- Credential management over HTTP exposes zero secret values to the client (GUI-03 / SC3)
- POST persists via CredentialStore.set; key-membership gate blocks injection
- Origin check mitigates local CSRF; MOD-02 guard green
</success_criteria>

<output>
Create `.planning/phases/10-optional-web-ui/10-03-SUMMARY.md` when done.
</output>
