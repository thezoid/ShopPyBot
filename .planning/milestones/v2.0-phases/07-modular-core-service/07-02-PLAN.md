---
phase: 07-modular-core-service
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - pyproject.toml
autonomous: true
requirements: [MOD-03]
must_haves:
  truths:
    - "pip install -e . exits 0"
    - "shoppybot --help exits 0 and the entry point resolves to core.service:main"
    - "the existing [tool.pytest.ini_options] block is preserved verbatim (testpaths + asyncio settings)"
    - "the existing test suite still passes unmodified after install"
  artifacts:
    - path: "pyproject.toml"
      provides: "package metadata + packages + console entry point shoppybot = core.service:main"
      contains: "shoppybot ="
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "core.service:main"
      via: "console entry point"
      pattern: "core\\.service:main"
    - from: "pyproject.toml packages"
      to: "core, plugins, notifications"
      via: "setuptools package discovery (no src/ move)"
      pattern: "core"
---

<objective>
Make the package pip-installable with a `shoppybot` console entry point, declaring `core`, `plugins`, and `notifications` as packages WITHOUT moving them into `src/`. Extend the existing `pyproject.toml` — do not break its `[tool.pytest.ini_options]`.

Purpose: MOD-03 — `pip install -e .` works on Ubuntu and Windows; `shoppybot` resolves to the service entry point.
Output: an extended `pyproject.toml` with `[build-system]`, `[project]` metadata, package list, and `[project.scripts] shoppybot = "core.service:main"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modular-core-service/07-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Current pyproject.toml (already exists for pytest). Extend, do not replace the pytest block. -->
```toml
[project]
name = "shoppybot"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
```
<!-- Entry-point target created in Plan 01: core/service.py defines a module-level main(). -->
<!-- Existing top-level packages to declare (no src/ move): core, plugins, notifications. -->
<!-- Top-level single-file modules that must remain importable: models.py, logger.py, config.py, utils.py, main.py. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pyproject.toml with build backend, packages, and console entry point</name>
  <read_first>pyproject.toml (whole file — preserve the existing [project] and [tool.pytest.ini_options] exactly), core/service.py (confirm a module-level main() exists as the entry-point target — from Plan 01), requirements.txt (so [project].dependencies, if added, match the pinned versions and introduce NO new deps)</read_first>
  <files>pyproject.toml</files>
  <action>Extend pyproject.toml in place. Add a `[build-system]` table using setuptools (`requires = ["setuptools>=61"]`, `build-backend = "setuptools.build_meta"`). Keep the existing `[project]` keys (name, version, requires-python) and the existing `[tool.pytest.ini_options]` block verbatim — do NOT remove or reorder the pytest settings. Add `[project.scripts]` with `shoppybot = "core.service:main"`. Configure setuptools package discovery so `core`, `plugins`, and `notifications` are installed as packages and the top-level single-file modules (`models`, `logger`, `config`, `utils`, `main`) remain importable — use `[tool.setuptools] py-modules = [...]` for the single-file modules and `[tool.setuptools.packages.find]` (or an explicit `packages` list) for the three packages, whichever cleanly avoids pulling in `tests` or stray dirs. Do NOT add any new third-party dependency (locked: no new deps this phase); if you add a `[project].dependencies` list, it must mirror requirements.txt exactly. Do NOT create a src/ layout.</action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['scripts']['shoppybot']=='core.service:main'; assert d['tool']['pytest']['ini_options']['testpaths']==['tests']; print('ok')"</automated>
    <automated>pip install -e .</automated>
    <automated>shoppybot --help</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml parses; `[project.scripts] shoppybot` equals `core.service:main`.
    - The `[tool.pytest.ini_options]` block is unchanged (testpaths=["tests"], asyncio_mode="auto", asyncio_default_fixture_loop_scope="function").
    - `pip install -e .` exits 0.
    - `shoppybot --help` exits 0 (the entry point resolves and core.service:main runs its arg handling).
    - `python -m pytest tests/ -q` still passes with no test file modified.
  </acceptance_criteria>
  <done>Editable install succeeds, shoppybot console script resolves to core.service:main, pytest config preserved, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install → local environment | build metadata installs the package + console script into the active venv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | pyproject build config | mitigate | setuptools backend only; explicit package list avoids accidentally packaging tests/, config.yml, or data/. No new dependencies introduced (locked). |
| T-07-06 | Information Disclosure | packaged files | mitigate | Package discovery excludes tests/, config.yml (gitignored secrets), data/ (SQLite), logs/ — only code packages/modules are declared. |
| T-07-SC | Tampering | pip install -e . | accept | Installs only THIS local package in editable mode; introduces no third-party packages (no requirements change). No external package legitimacy gate needed because no new dependency is added. |
</threat_model>

<verification>
- pyproject.toml parses via tomllib; entry point + pytest block both correct.
- `pip install -e .` exits 0; `shoppybot --help` exits 0.
- Full suite green, no test file modified.
</verification>

<success_criteria>
- SC3 (partial): `pip install -e .` succeeds and `shoppybot --help` shows the entry point. (Cross-OS Ubuntu+Windows confirmation: Windows confirmed here; Ubuntu confirmation is a documented expectation pending Phase 11 matrix — no Ubuntu-specific paths are introduced this phase.)
- SC4: existing suite passes unmodified after install.
- MOD-03 (package + entry point) satisfied; the main.py shim half of MOD-03 lands in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/07-modular-core-service/07-02-SUMMARY.md` when done.
</output>
