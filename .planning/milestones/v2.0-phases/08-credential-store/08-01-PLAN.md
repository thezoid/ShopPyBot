---
phase: 08-credential-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_credentials.py
  - tests/conftest.py
  - core/credentials.py
  - core/config_schema.py
autonomous: true
requirements: [CRED-01, CRED-04]
must_haves:
  truths:
    - "from core.credentials import CredentialStore, EnvVarBackend, SECRET_KEYS, get_store succeeds"
    - "EnvVarBackend().get(KEY) returns the value set via monkeypatch.setenv (existing test behavior preserved)"
    - "get_store() returns an EnvVarBackend when no store has been initialized (lazy fallback)"
    - "config.yml may contain a credentials section with backend and data_dir; secrets never live there"
  artifacts:
    - path: "core/credentials.py"
      provides: "CredentialStore ABC, EnvVarBackend, SECRET_KEYS, get_store/init_store accessors"
      contains: "class CredentialStore"
      min_lines: 60
    - path: "core/config_schema.py"
      provides: "CredentialsConfig sub-model wired into AppConfig"
      contains: "class CredentialsConfig"
    - path: "tests/test_credentials.py"
      provides: "Wave 0 test scaffold for CRED-01..CRED-07"
      contains: "def test_env_backend_get"
    - path: "tests/conftest.py"
      provides: "isolated_keyring + reset_credential_store fixtures"
      contains: "def reset_credential_store"
  key_links:
    - from: "core/config_schema.py AppConfig"
      to: "CredentialsConfig"
      via: "credentials field default"
      pattern: "credentials: CredentialsConfig"
    - from: "core/credentials.py get_store"
      to: "EnvVarBackend"
      via: "lazy fallback when _store is None"
      pattern: "return EnvVarBackend\\(\\)"
---

<objective>
Establish the credential-store contract: the `CredentialStore` ABC, the identity-preserving `EnvVarBackend`, the canonical `SECRET_KEYS` list, and the module-level `get_store()`/`init_store()` accessors — plus the `CredentialsConfig` config section and the Wave 0 test scaffold that downstream plans (02, 03, 04) verify against.

Purpose: All later work (keyring/file backends, selection logic, 16 call-site migrations) builds against this interface. Shipping the contract first prevents downstream scavenger hunts. The `EnvVarBackend` is the fallback that keeps all 227 existing `monkeypatch.setenv` tests green without modification (CRED-04).
Output: `core/credentials.py` (ABC + EnvVarBackend + SECRET_KEYS + accessors), `CredentialsConfig` in `core/config_schema.py`, `tests/test_credentials.py` scaffold, conftest fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-credential-store/08-CONTEXT.md
@.planning/phases/08-credential-store/08-RESEARCH.md
@.planning/phases/08-credential-store/08-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans implement against these. -->

CredentialStore (ABC) — core/credentials.py:
  get(key: str) -> str | None
  set(key: str, value: str) -> None
  delete(key: str) -> None
  list() -> list[str]

SECRET_KEYS (list[str]) — the 19 canonical keys (see 08-RESEARCH.md "SECRET_KEYS Canonical List"):
  DISCORD_WEBHOOK_URL, SMTP_PASSWORD, TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM,
  AMZ_EMAIL, AMZ_PASSWORD, BB_EMAIL, BB_PASSWORD, WALMART_EMAIL, WALMART_PASSWORD,
  TARGET_EMAIL, TARGET_PASSWORD, GAMESTOP_EMAIL, GAMESTOP_PASSWORD,
  SQUAREENIX_EMAIL, SQUAREENIX_PASSWORD, NEWEGG_EMAIL, NEWEGG_PASSWORD

Module accessors — core/credentials.py:
  init_store(cfg: AppConfig) -> CredentialStore   # sets module-global _store
  get_store() -> CredentialStore                  # returns _store, or EnvVarBackend() when _store is None

CredentialsConfig (pydantic BaseModel) — core/config_schema.py:
  backend: str = "auto"   # auto | keyring | file | env
  data_dir: str = ""      # empty = data/creds.bin (project default)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold + conftest fixtures for credentials</name>
  <files>tests/test_credentials.py, tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (existing fixtures: tmp_data_dir uses monkeypatch.setattr; add new fixtures alongside)
    - tests/test_notifications.py (monkeypatch.setenv / monkeypatch.delenv test pattern)
    - .planning/phases/08-credential-store/08-PATTERNS.md (section "tests/test_credentials.py" — file header, monkeypatch usage, tmp_path file-backend pattern, reset_credential_store + isolated_keyring fixtures)
    - .planning/phases/08-credential-store/08-RESEARCH.md (section "Phase Requirements to Test Map" — exact test names; Pattern 8 DictKeyring)
  </read_first>
  <behavior>
    - test_env_backend_get: monkeypatch.setenv("AMZ_EMAIL","x@example.com"); EnvVarBackend().get("AMZ_EMAIL") == "x@example.com"
    - test_env_backend_monkeypatch: same shape, confirms existing setenv pattern works through the backend
    - test_get_store_lazy_fallback: with module _store is None, get_store() returns an EnvVarBackend instance
    - test_secret_keys_canonical: SECRET_KEYS contains all 19 names; no duplicates (len == len(set))
    - test_credentials_config_default: AppConfig().credentials.backend == "auto"
    - Stub (xfail or skeleton) tests named per 08-RESEARCH Test Map for CRED-02/03/05/06/07: test_keyring_backend, test_has_real_keyring_fail, test_file_backend, test_encrypted_file_is_not_plaintext, test_file_backend_wrong_passphrase, test_auto_select_keyring, test_auto_select_file, test_startup_log_backend_name, test_no_plaintext_secrets_in_config_yml, test_migrate_from_env — these reference symbols built in plans 02/03/04 and may be marked pytest.mark.xfail(reason="implemented in plan 08-0N") until then.
  </behavior>
  <action>
    Create tests/test_credentials.py with the test functions named in the behavior block (and in 08-RESEARCH "Phase Requirements to Test Map"). Tests for symbols not yet built (KeyringBackend, EncryptedFileBackend, init_store selection, migrate_from_env, no-plaintext) MUST be present as named stubs decorated with pytest.mark.xfail(reason="...plan 08-02/03/04") so the file imports and the test names exist now for downstream plans to un-xfail. The five CRED-01/CRED-04 tests listed first are REAL and must pass against this plan's code.
    Add two fixtures to tests/conftest.py: reset_credential_store (saves core.credentials._store, yields, restores it — prevents singleton bleed per 08-RESEARCH Pitfall 1) and isolated_keyring (the _DictKeyring in-memory backend from 08-RESEARCH Pattern 8 / 08-PATTERNS isolated_keyring; install via keyring.set_keyring, teardown resets keyring.core._keyring_backend = None). Guard the isolated_keyring import of keyring behind a module-level try/except or pytest.importorskip("keyring") so the file still imports before keyring is installed in plan 02.
    Do not place fenced code in production files. Use the patterns referenced verbatim.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && python -m pytest tests/test_credentials.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_credentials.py contains def test_env_backend_get and def test_secret_keys_canonical
    - tests/conftest.py contains def reset_credential_store and def isolated_keyring
    - `python -m pytest tests/test_credentials.py -q` exits 0 (real tests pass; stubs xfail, not error)
    - File imports cleanly even though keyring is not yet installed (importorskip or guarded import)
  </acceptance_criteria>
  <done>Test file and fixtures exist; the five CRED-01/CRED-04 real tests pass; all downstream stubs present as xfail with named functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CredentialStore ABC + EnvVarBackend + SECRET_KEYS + accessors</name>
  <files>core/credentials.py</files>
  <read_first>
    - core/config_schema.py (lines 1-37 import block + module-level path/threading patterns; the existing sub-model style)
    - models.py (lines 1-28: DB_PATH project-relative data-dir convention; get_db_connection explicit-exception style)
    - logger.py (writeLog(message, type) signature for later use)
    - .planning/phases/08-credential-store/08-PATTERNS.md (section "core/credentials.py" — imports pattern, module-level singleton, _DEFAULT_STORE_PATH, ABC interface, EnvVarBackend, init_store/get_store accessors)
    - .planning/phases/08-credential-store/08-RESEARCH.md (Pattern 1 ABC, Pattern 4 EnvVarBackend, Pattern 5 module init/accessor, "SECRET_KEYS Canonical List")
  </read_first>
  <behavior>
    - EnvVarBackend.get("AMZ_EMAIL") returns os.environ value or None
    - EnvVarBackend.set writes os.environ[key]; delete pops it; list() returns [k for k in SECRET_KEYS if k in os.environ]
    - get_store() returns EnvVarBackend() when module global _store is None
    - init_store(cfg) sets the module global _store under _store_lock and returns it (selection logic itself is filled in plan 03 — here init_store may default to EnvVarBackend so the symbol exists)
  </behavior>
  <action>
    Create core/credentials.py with: `from __future__ import annotations`; stdlib imports (os, threading, abc.ABC/abstractmethod, pathlib.Path, typing.TYPE_CHECKING) plus the TYPE_CHECKING-guarded `from core.config_schema import AppConfig`. Define the abstract base class CredentialStore with four abstractmethods get(key:str)->str|None, set(key:str,value:str)->None, delete(key:str)->None, list()->list[str]. Define SECRET_KEYS as the 19-entry list (exact names from 08-RESEARCH "SECRET_KEYS Canonical List", grouped by Notifications/Amazon/BestBuy/Walmart/Target/GameStop/SquareEnix/Newegg). Define module globals `_store: CredentialStore | None = None` and `_store_lock = threading.Lock()`, and `_DEFAULT_STORE_PATH = Path(__file__).parent.parent / "data" / "creds.bin"`. Implement EnvVarBackend(CredentialStore) exactly per 08-PATTERNS (get/set/delete/list over os.environ). Implement get_store() returning EnvVarBackend() under the lock when _store is None else _store, and init_store(cfg) that builds a store (stub to EnvVarBackend for this plan; selection logic lands in plan 03) and assigns _store under _store_lock, returning it. Keep functions under 30 lines; no secret values ever logged.
    Note for plan 03: a `_build_store(backend_name, cfg)` seam may be stubbed here to be fleshed out in plan 03; if so, document it as the selection hook.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && python -m pytest tests/test_credentials.py -k "env_backend or secret_keys or lazy_fallback" -q</automated>
  </verify>
  <acceptance_criteria>
    - core/credentials.py contains "class CredentialStore" and "class EnvVarBackend" and "SECRET_KEYS"
    - `python -c "from core.credentials import CredentialStore, EnvVarBackend, SECRET_KEYS, get_store, init_store"` exits 0
    - len(SECRET_KEYS) == 19 and contains "DISCORD_WEBHOOK_URL" and "NEWEGG_PASSWORD"
    - `python -m pytest tests/test_credentials.py -k "env_backend or secret_keys or lazy_fallback" -q` exits 0
    - `grep -nE "writeLog\(.*get\(|writeLog\(.*os\.environ" core/credentials.py` returns no secret-value log lines
  </acceptance_criteria>
  <done>ABC, EnvVarBackend, SECRET_KEYS, get_store, init_store all importable; env-backend + lazy-fallback tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CredentialsConfig sub-model wired into AppConfig</name>
  <files>core/config_schema.py</files>
  <read_first>
    - core/config_schema.py (lines 148-153 DiscordConfig one-field sub-model; lines 188-212 NotificationsConfig + AppConfig field declarations; lines 166-185 SmsConfig validator — the deliberate os.environ exception to LEAVE in place)
    - .planning/phases/08-credential-store/08-PATTERNS.md (section "core/config_schema.py (add CredentialsConfig)")
    - .planning/phases/08-credential-store/08-RESEARCH.md (Pattern 6 CredentialsConfig)
  </read_first>
  <behavior>
    - AppConfig().credentials is a CredentialsConfig instance
    - AppConfig().credentials.backend == "auto" and AppConfig().credentials.data_dir == ""
    - Setting credentials: {backend: file} in config.yml yields AppConfig().credentials.backend == "file"
    - SmsConfig.require_creds_if_enabled STILL reads os.environ (unchanged) with an explanatory comment
  </behavior>
  <action>
    Add a CredentialsConfig(BaseModel) sub-model alongside DiscordConfig/EmailConfig (after the SmsConfig/NotificationsConfig block), with fields backend: str = "auto" and data_dir: str = "", and a one-line docstring stating secrets are NEVER stored here (CRED-06). Add the field `credentials: CredentialsConfig = CredentialsConfig()` to AppConfig immediately after the existing `notifications: NotificationsConfig = NotificationsConfig()` line. Add a code comment above SmsConfig.require_creds_if_enabled (lines ~172-185) marking the os.environ reads there as a DELIBERATE exception: the validator runs during AppConfig() construction, BEFORE init_store(), so it must stay reading os.environ (per 08-RESEARCH Pitfall 7). Do NOT migrate that validator.
  </action>
  <verify>
    <automated>cd E:/repos/ShopPyBot && python -m pytest tests/test_credentials.py -k "credentials_config" tests/test_config_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - core/config_schema.py contains "class CredentialsConfig" and "credentials: CredentialsConfig = CredentialsConfig()"
    - `python -c "from core.config_schema import AppConfig; c=AppConfig(); assert c.credentials.backend=='auto'"` exits 0
    - SmsConfig.require_creds_if_enabled still contains os.environ.get with an adjacent comment containing "DELIBERATE" or "init_store"
    - `python -m pytest tests/test_config_schema.py -q` exits 0 (no regression)
  </acceptance_criteria>
  <done>CredentialsConfig present with auto default; AppConfig exposes credentials; SmsConfig exception documented and unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml → AppConfig | User-supplied YAML parsed into pydantic models; must never carry secrets |
| os.environ → EnvVarBackend | Process environment is the secret source for the env backend (today's behavior) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | CredentialsConfig in config.yml | mitigate | CredentialsConfig holds only backend selector + data_dir; no secret fields exist on the model; CRED-06 no-plaintext test (plan 03) asserts no secret values in config.yml |
| T-08-02 | Information Disclosure | get_store()/EnvVarBackend logging | mitigate | No writeLog call in this plan emits a key value; backend-name-only logging deferred to plan 03; acceptance grep confirms no secret-value log lines |
| T-08-03 | Tampering | core/credentials.py singleton bleed across tests | accept | reset_credential_store fixture restores _store between tests; low severity (test-only) |
</threat_model>

<verification>
- `python -m pytest tests/test_credentials.py tests/test_config_schema.py -q` exits 0
- `python -m pytest -q` baseline suite unaffected (no consumers migrated yet)
- All four interface symbols importable: CredentialStore, EnvVarBackend, SECRET_KEYS, get_store/init_store
</verification>

<success_criteria>
- CredentialStore ABC, EnvVarBackend, SECRET_KEYS (19 keys), get_store, init_store importable from core.credentials
- AppConfig().credentials.backend defaults to "auto"; secrets never on the model
- Wave 0 test scaffold present with downstream stubs as xfail
- Existing 227-test suite stays green
</success_criteria>

<output>
Create `.planning/phases/08-credential-store/08-01-SUMMARY.md` when done
</output>
