---
phase: 09-cli-front-end
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - core/cli/web.py
  - tests/test_cli_no_fastapi.py
autonomous: true
requirements: [CLI-04]
must_haves:
  truths:
    - "`shoppybot web` lazy-imports fastapi inside the handler; when fastapi is absent it prints a `pip install .[web]` hint to stderr and exits non-zero"
    - "No core/cli module imports fastapi at module level"
    - "With fastapi absent (sys.modules['fastapi']=None), `shoppybot run`, `setup`, and `items` are fully functional — no ImportError"
  artifacts:
    - path: "core/cli/web.py"
      provides: "handle_web with lazy try/except ImportError fastapi seam"
      contains: "import fastapi"
    - path: "tests/test_cli_no_fastapi.py"
      provides: "CLI-04 guard: web prints install hint + run works with fastapi poisoned"
      contains: "setitem(sys.modules"
  key_links:
    - from: "core/cli/web.py:handle_web"
      to: "fastapi (optional)"
      via: "try: import fastapi / except ImportError"
      pattern: "import fastapi"
    - from: "tests/test_cli_no_fastapi.py"
      to: "core.service.main + core.cli.web.handle_web"
      via: "monkeypatch.setitem(sys.modules,'fastapi',None)"
      pattern: "sys\\.modules"
---

<objective>
Ship the lazy-import seam that guarantees the CLI is fully functional with no web UI installed
(CLI-04). The `web` handler imports fastapi only inside its body; if fastapi is missing it prints
a clear `pip install .[web]` hint and exits non-zero. Tests poison sys.modules['fastapi'] to prove
run/setup/items keep working and that web degrades gracefully. The actual web server is Phase 10.

Purpose: CLI-04 — uninstalling FastAPI leaves run/setup/items fully functional, no import errors.
Output: implemented core/cli/web.py lazy seam and the no-fastapi guard tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cli-front-end/09-RESEARCH.md
@.planning/phases/09-cli-front-end/09-PATTERNS.md
@.planning/phases/09-cli-front-end/09-01-SUMMARY.md
@.planning/phases/09-cli-front-end/09-02-SUMMARY.md
@.planning/phases/09-cli-front-end/09-03-SUMMARY.md

<interfaces>
<!-- core/cli/web.py module-level imports MUST be stdlib only (sys). NO top-level `import fastapi`. -->
handle_web(args, svc=None) -> int   # try: import fastapi / except ImportError: print hint to stderr, return 1

<!-- Test idiom (RESEARCH Pattern 4):
     monkeypatch.setitem(sys.modules, "fastapi", None)  → `import fastapi` raises ImportError
     del sys.modules["core.cli.web"] before re-import so the lazy import fires fresh -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement web lazy-import seam + CLI-04 guard tests</name>
  <read_first>
    - core/cli/web.py (stub from plan 09-01; module imports `sys` only)
    - tests/test_cli_no_fastapi.py (skip-marked stubs from plan 09-01 — unskip and implement)
    - tests/test_no_env_secret_reads.py (sentinel/guard test idiom)
    - .planning/phases/09-cli-front-end/09-PATTERNS.md (web.py block + test_cli_no_fastapi.py block)
    - .planning/phases/09-cli-front-end/09-RESEARCH.md (Pattern 4 lazy fastapi seam, anti-pattern: no top-level fastapi import)
  </read_first>
  <files>core/cli/web.py, tests/test_cli_no_fastapi.py</files>
  <behavior>
    - handle_web tries `import fastapi` inside the function; on ImportError prints
      "FastAPI is not installed. Run: pip install .[web]" to stderr and returns 1.
    - When fastapi imports successfully, prints a "web UI not yet implemented (Phase 10)" message and returns 0.
    - test_web_no_fastapi: with sys.modules['fastapi']=None, handle_web returns 1 and stderr contains
      "pip install .[web]".
    - test_run_works_without_fastapi: with sys.modules['fastapi']=None, main(["run"]) reaches mocked
      BotService.run with no ImportError.
  </behavior>
  <action>
    Implement handle_web(args, svc=None) in core/cli/web.py per the PATTERNS analog: try `import fastapi`
    (with `# noqa: F401 -- lazy; never at module level`); except ImportError print the
    "FastAPI is not installed. Run: pip install .[web]" line to sys.stderr and return 1; otherwise print
    the Phase-10 placeholder line and return 0. Confirm the module's only top-level import remains `sys` —
    no `import fastapi` at module scope.
    Unskip the test_cli_no_fastapi.py tests: test_web_no_fastapi uses monkeypatch.setitem(sys.modules,
    "fastapi", None), deletes core.cli.web from sys.modules if present, imports handle_web fresh, asserts
    return 1 and "pip install .[web]" in capsys.readouterr().err. test_run_works_without_fastapi poisons
    sys.modules['fastapi']=None, patches core.service.BotService with a mock, calls main(["run"]), and
    asserts mock_svc.run was called once (no fastapi ImportError leaked). Optionally extend coverage to
    main(["items","list"]) and main(["setup"]) under the same poisoning to prove the full CLI core is
    fastapi-free.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_no_fastapi.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli_no_fastapi.py -q` exits 0.
    - core/cli/web.py has NO top-level `import fastapi` (the only top-level import is `sys`); the import is inside handle_web.
    - With sys.modules['fastapi']=None, handle_web returns 1 and stderr contains "pip install .[web]".
    - With sys.modules['fastapi']=None, main(["run"]) calls the mocked BotService.run once with no ImportError.
  </acceptance_criteria>
  <done>web command degrades gracefully without fastapi; run/setup/items proven functional with fastapi poisoned; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional dependency → CLI import graph | fastapi may be absent; CLI must not hard-depend on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Denial of Service | top-level fastapi import breaks the whole CLI when web extra is uninstalled | mitigate | fastapi imported only inside handle_web (try/except ImportError); test_run_works_without_fastapi proves run/items work with fastapi poisoned (CLI-04) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages added this phase (RESEARCH Package Legitimacy Audit: none); the `[web]` extra and fastapi are introduced and audited in Phase 10 |
</threat_model>

<verification>
- `python -m pytest tests/test_cli_no_fastapi.py tests/test_cli_mod02.py -q` green.
- `python -m pytest -q` green (full suite; ZERO skip markers remain — every CLI test now active).
- `python -c "import core.cli.web, ast; t=ast.parse(open('core/cli/web.py').read()); assert not any(getattr(n,'module',None)=='fastapi' or any(a.name=='fastapi' for a in getattr(n,'names',[])) for n in t.body if isinstance(n,(ast.Import,ast.ImportFrom))); print('no top-level fastapi')"` prints ok.
</verification>

<success_criteria>
- CLI-04 satisfied: uninstalling fastapi leaves run/setup/items fully functional; web prints the install hint and exits non-zero.
- No core/cli module imports fastapi at module level.
- Full test suite (255 baseline + all new CLI tests) green with no remaining skip markers.
</success_criteria>

<output>
Create `.planning/phases/09-cli-front-end/09-04-SUMMARY.md` when done.
</output>
