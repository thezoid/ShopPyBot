---
phase: 09-cli-front-end
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - core/cli/items.py
  - tests/test_cli_items.py
  - tests/test_cli_mod02.py
autonomous: true
requirements: [CLI-03, MOD-02]
must_haves:
  truths:
    - "`shoppybot items list` prints an aligned text table of the tracked items (or a no-items message)"
    - "`shoppybot items add --name ... --url ...` calls BotService.add_item with auto_buy=False, quantity=1 defaults"
    - "`shoppybot items remove --url ...` looks up the item name, calls BotService.remove_item, and prints the removed name"
    - "`shoppybot items remove` with an unknown URL exits 1 without calling remove_item"
    - "No core/cli module imports orchestrator, registry, or models — every item operation routes through BotService (MOD-02)"
  artifacts:
    - path: "core/cli/items.py"
      provides: "handle_items_list/add/remove over BotService; _format_items_table"
      contains: "_format_items_table"
    - path: "tests/test_cli_mod02.py"
      provides: "AST grep guard asserting CLI modules never bypass BotService"
      contains: "forbidden_names"
  key_links:
    - from: "core/cli/items.py:handle_items_add"
      to: "BotService.add_item"
      via: "svc.add_item(name, url, auto_buy, quantity)"
      pattern: "\\.add_item\\("
    - from: "core/cli/items.py:handle_items_remove"
      to: "BotService.list_items + remove_item"
      via: "list_items() lookup then remove_item(url)"
      pattern: "\\.remove_item\\("
    - from: "tests/test_cli_mod02.py"
      to: "core/cli/*.py"
      via: "ast.walk import scan"
      pattern: "ast\\.parse"
---

<objective>
Implement the items command group (list/add/remove) entirely over BotService, and lock the
MOD-02 boundary with an AST grep-guard test that fails if any core/cli module imports the
orchestrator, registry, or models directly. items list prints an aligned text table; add maps
argparse flags straight to BotService.add_item; remove looks up the name before deleting and
exits 1 when the URL is not found.

Purpose: the items half of CLI-03, plus the MOD-02 invariant for the whole CLI layer.
Output: implemented core/cli/items.py and an enforced MOD-02 guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cli-front-end/09-RESEARCH.md
@.planning/phases/09-cli-front-end/09-PATTERNS.md
@.planning/phases/09-cli-front-end/09-01-SUMMARY.md

<interfaces>
<!-- BotService (core/service.py) — the only seam items.py may use -->
.list_items() -> list[tuple]            # (name:str, link:str, auto_buy:int, quantity:int, purchased:int)
.add_item(name, link, auto_buy: bool, quantity: int) -> None
.remove_item(link: str) -> None         # silent no-op if link absent (SQLite DELETE)

<!-- argparse flag → attr mapping: --auto-buy → args.auto_buy (bool), --quantity → args.quantity (int),
     --name → args.name, --url → args.url -->

<!-- MOD-02 forbidden import names for the grep guard:
     orchestrator, registry, models, add_items_sync, get_items_sync, remove_item_sync, add_items -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement items list/add/remove over BotService</name>
  <read_first>
    - core/cli/items.py (stubs from plan 09-01; imports sys + BotService only)
    - core/service.py (BotService.list_items/add_item/remove_item signatures + row tuple shape)
    - tests/test_cli_items.py (skip-marked stubs from plan 09-01 — unskip and implement)
    - .planning/phases/09-cli-front-end/09-PATTERNS.md (items.py block: _format_items_table, handle_items_add, handle_items_remove)
    - .planning/phases/09-cli-front-end/09-RESEARCH.md (Pattern 6 table, Pattern 7 remove name lookup, Pitfall 5 silent remove)
  </read_first>
  <files>core/cli/items.py, tests/test_cli_items.py</files>
  <behavior>
    - handle_items_list with rows prints a header line (Name URL Auto-Buy Qty Purchased), a separator,
      and one aligned row per item; with no rows prints "No items tracked."
    - handle_items_add calls svc.add_item(args.name, args.url, args.auto_buy, args.quantity) and prints
      "Added: {name}"; defaults flow from argparse (auto_buy False, quantity 1).
    - handle_items_remove calls svc.list_items(), finds the row whose link == args.url; if missing prints
      a stderr message and returns 1 (no remove_item call); else svc.remove_item(args.url) and prints
      "Removed: {name}" (name only).
  </behavior>
  <action>
    Implement _format_items_table(rows) per the PATTERNS analog: headers ("Name","URL","Auto-Buy","Qty",
    "Purchased"); empty rows → "No items tracked."; compute per-column widths from max(header, cell)
    lengths; build a left-justified format string; emit header, a dashed separator, then one line per row
    rendering bool(r[2]) for auto_buy, r[3] for qty, bool(r[4]) for purchased. handle_items_list(args, svc):
    print(_format_items_table(svc.list_items())); return 0. handle_items_add(args, svc):
    svc.add_item(args.name, args.url, args.auto_buy, args.quantity); print(f"Added: {args.name}"); return 0.
    handle_items_remove(args, svc): rows = svc.list_items(); match = next((r for r in rows if r[1] ==
    args.url), None); if match is None: print to stderr "No item found with URL: {url}" and return 1; else
    svc.remove_item(args.url); print(f"Removed: {match[0]}"); return 0. items.py imports ONLY `sys` and
    `from core.service import BotService` — never models/orchestrator/registry. Unskip the
    test_cli_items.py tests and make them pass (table, add args, remove name, remove-not-found exits 1).
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_items.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli_items.py -q` exits 0 (list table, add delegation, remove name, not-found exit 1).
    - core/cli/items.py contains `_format_items_table` and calls `.add_item(` and `.remove_item(`.
    - test_items_add asserts add_item called with ("Widget","https://ex.com", False, 1).
    - items remove on an absent URL exits 1 and does NOT call remove_item.
  </acceptance_criteria>
  <done>items list/add/remove work over BotService with correct defaults and name-on-remove; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce MOD-02 grep guard for core/cli modules</name>
  <read_first>
    - tests/test_no_env_secret_reads.py (AST-walk scan structure to copy)
    - tests/test_cli_mod02.py (skip-marked stub from plan 09-01)
    - core/cli/ (all modules created in plans 09-01/02/03 — the scan target)
    - .planning/phases/09-cli-front-end/09-PATTERNS.md (test_cli_mod02.py block: forbidden_names set + ast scan)
    - .planning/phases/09-cli-front-end/09-RESEARCH.md (Pattern 8 MOD-02 grep guard)
  </read_first>
  <files>tests/test_cli_mod02.py</files>
  <action>
    Implement test_cli_no_direct_model_imports as a structural copy of test_no_env_secret_reads.py's
    AST-walk: define forbidden_names = {"orchestrator","registry","models","add_items_sync",
    "get_items_sync","remove_item_sync","add_items"}; glob core/cli/*.py (resolve via
    Path(__file__).parent.parent / "core" / "cli"); for each file ast.parse the source and ast.walk it;
    for ast.Import nodes flag any alias.name in forbidden_names; for ast.ImportFrom nodes flag node.module
    in forbidden_names OR any imported alias.name in forbidden_names; assert no violations with a message
    listing the offending file + name. Unskip this test (remove the plan-09-03 skip marker). The test must
    pass against the modules built in plans 09-01/02/03 (all route through core.service.BotService and
    core.credentials/config_schema seams only).
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_mod02.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli_mod02.py -q` exits 0 (no forbidden imports anywhere in core/cli/).
    - tests/test_cli_mod02.py contains the forbidden_names set with orchestrator/registry/models entries.
    - The test scans every core/cli/*.py file via ast.parse (not a substring grep).
  </acceptance_criteria>
  <done>MOD-02 invariant enforced by an active AST guard test that passes against the full core/cli/ package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → items add/remove | name/url/quantity flags that become DB writes via BotService |
| core/cli modules → core internals | architectural boundary: CLI must call BotService only, never DB/orchestrator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Elevation of Privilege | CLI bypassing BotService to touch models/orchestrator directly | mitigate | test_cli_mod02.py AST guard fails the build if any core/cli module imports models/orchestrator/registry (MOD-02) |
| T-09-11 | Tampering | items remove silently no-ops on an unknown URL | mitigate | list_items lookup before delete; unknown URL returns 1 with a stderr message (Pitfall 5) |
| T-09-12 | Spoofing | item name/url come from local CLI args | accept | single-user local tool; input validation is BotService/DB concern, not a CLI trust boundary |
</threat_model>

<verification>
- `python -m pytest tests/test_cli_items.py tests/test_cli_mod02.py -q` green.
- `python -m pytest -q` green (full suite; only plan-04 web tests remain skip-marked).
</verification>

<success_criteria>
- CLI-03 (items half) satisfied: list/add/remove all route through BotService with correct defaults and remove-name behavior.
- MOD-02 satisfied for the CLI layer: an active AST guard test enforces no orchestrator/registry/DB bypass.
</success_criteria>

<output>
Create `.planning/phases/09-cli-front-end/09-03-SUMMARY.md` when done.
</output>
