---
phase: 11-cross-platform-verification
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - tests/test_smoke.py
  - tests/test_backend_selection.py
autonomous: true
requirements: [XPLAT-02]
must_haves:
  truths:
    - "package imports cleanly and shoppybot --help / items list / config show run without error (env-independent)"
    - "backend auto-selection picks keyring when _has_real_keyring True, encrypted-file when False+passphrase, env when False+no-passphrase"
    - "smoke tests never invoke a real bot loop, browser, or keyring"
  artifacts:
    - path: "tests/test_smoke.py"
      provides: "import + --help + items list + config show smoke (subprocess, SHOPBOT_DATA_DIR-redirected)"
    - path: "tests/test_backend_selection.py"
      provides: "mocked _has_real_keyring -> keyring vs file vs env selection assertions"
  key_links:
    - from: "tests/test_backend_selection.py"
      to: "core.credentials._has_real_keyring"
      via: "patch(..., return_value=True/False) around _build_store"
      pattern: "_has_real_keyring"
    - from: "tests/test_smoke.py"
      to: "core.service.main"
      via: "subprocess invocation with SHOPBOT_DATA_DIR set to a temp dir"
      pattern: "SHOPBOT_DATA_DIR"
---

<objective>
Add the env-independent automated half of the verification matrix: a smoke module proving the package imports and the safe CLI commands run without error, and a backend-selection module proving the auto-selection logic chooses the right credential backend per environment via a mocked `_has_real_keyring()`.

Purpose: XPLAT-02 — the automated smoke that runs on any OS (and feeds the CI matrix in Plan 05), confirming import + CLI + backend selection without a real browser, keyring, or audio device.
Output: tests/test_smoke.py, tests/test_backend_selection.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cross-platform-verification/11-CONTEXT.md
@.planning/phases/11-cross-platform-verification/11-RESEARCH.md
@.planning/phases/11-cross-platform-verification/11-PATTERNS.md
@.planning/phases/11-cross-platform-verification/11-02-SUMMARY.md
@.planning/phases/11-cross-platform-verification/11-03-SUMMARY.md

<interfaces>
From core/credentials.py:
```python
def _has_real_keyring() -> bool        # the headless-vs-desktop selection guard to mock
def _build_store(mode: str, cfg) -> CredentialStore   # mode "auto" exercises selection
# precedence: explicit config > real keyring > encrypted-file(passphrase in env) > env-var
# EncryptedFileBackend requires SHOPBOT_STORE_PASSPHRASE in env (else falls to EnvVarBackend)
```
From core/service.py:
```python
def main(argv=None) -> None   # supports ['--help'], ['items','list'], ['config','show']
```
From core/config_schema.py:
```python
class AppConfig:  # AppConfig(yaml_file=tmp_config_yml) injects a config without touching defaults
```
Fixtures available (tests/conftest.py): tmp_config_yml, tmp_data_dir (now also sets SHOPBOT_DATA_DIR),
reset_credential_store. Backend mock pattern: patch("core.credentials._has_real_keyring", return_value=...).
SAFE smoke commands (RESEARCH Pitfall 7): --help, items list, config show. NEVER smoke `run` without --help.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_backend_selection.py — mocked _has_real_keyring drives selection</name>
  <read_first>
    - core/credentials.py (_has_real_keyring + _build_store precedence; EncryptedFileBackend passphrase requirement)
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (Backend-selection monkeypatch pattern; reset_credential_store / tmp_config_yml fixtures)
    - .planning/phases/11-cross-platform-verification/11-RESEARCH.md (Pattern 4 backend-per-OS test)
  </read_first>
  <action>
    Create tests/test_backend_selection.py with three tests using patch("core.credentials._has_real_keyring", ...): (1) test_auto_selects_keyring_when_available — return_value=True, build _build_store("auto", AppConfig(yaml_file=tmp_config_yml)), assert store.__class__.__name__ == "KeyringBackend"; (2) test_auto_selects_file_when_no_keyring — monkeypatch.setenv("SHOPBOT_STORE_PASSPHRASE", "test-passphrase"), return_value=False, assert "EncryptedFileBackend"; (3) test_auto_falls_back_to_env — monkeypatch.delenv("SHOPBOT_STORE_PASSPHRASE", raising=False), return_value=False, assert "EnvVarBackend". Use the reset_credential_store and tmp_config_yml fixtures so the process-wide store singleton is isolated. Import _build_store / AppConfig inside each test after the patch/env setup. This is the env-independent proof of SC3 (XPLAT-02) — no real keyring or D-Bus is touched.
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -m pytest tests/test_backend_selection.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_backend_selection.py exists with the three named tests
    - `tests/test_backend_selection.py contains _has_real_keyring`
    - `python -m pytest tests/test_backend_selection.py -q` exits 0
  </acceptance_criteria>
  <done>Backend auto-selection proven for keyring / file / env via mocked _has_real_keyring; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_smoke.py — env-independent import + CLI smoke</name>
  <read_first>
    - core/service.py (main() dispatch — supported subcommands)
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (Smoke test pattern — subprocess CLI invocation)
    - .planning/phases/11-cross-platform-verification/11-RESEARCH.md (Pitfall 7 — never run `run` without --help; safe command list)
  </read_first>
  <action>
    Create tests/test_smoke.py. test_import_smoke: `import core.service`, `import core.paths`, `import models`, `import logger` all succeed (no exception). test_help_smoke: subprocess.run([sys.executable, "-m", "core.service", "--help"], capture_output=True, text=True) returns returncode 0 (note: if `-m core.service` is not runnable, invoke via [sys.executable, "-c", "from core.service import main; main(['--help'])"] and treat SystemExit(0) as success). test_items_list_smoke and test_config_show_smoke: run main(['items','list']) and main(['config','show']) in a subprocess with env = {**os.environ, "SHOPBOT_DATA_DIR": str(tmp_path), "SHOPBOT_STORE_PASSPHRASE":"x", "PYTHON_KEYRING_BACKEND":"keyring.backends.null.Keyring", "SDL_AUDIODRIVER":"dummy"} so resolution writes to a temp dir and no real keyring/audio is touched; assert returncode 0. Do NOT smoke `run` except via --help (it would block in the asyncio bot loop). Keep assertions to returncode == 0; do not assert on exact stdout text (varies by OS/config).
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -m pytest tests/test_smoke.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_smoke.py exists with test_import_smoke, test_help_smoke, test_items_list_smoke, test_config_show_smoke
    - `tests/test_smoke.py contains SHOPBOT_DATA_DIR`
    - no test invokes `run` without --help
    - `python -m pytest tests/test_smoke.py -q` exits 0
  </acceptance_criteria>
  <done>Import + --help + items list + config show smoke green, env-independent, no real bot/keyring/audio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test subprocess -> CLI | smoke spawns the CLI with a controlled env; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information Disclosure | smoke output capturing a secret | mitigate | Smoke runs `items list` / `config show` only — neither prints secret values (config show is allowlist-gated from Phase 9/10; credential values are name+is_set only). No secret is set in the smoke env beyond a throwaway passphrase. Assertions check returncode, not stdout content. |
| T-11-11 | Denial of Service | smoke invoking the real bot loop | mitigate | `run` is never invoked except via --help (RESEARCH Pitfall 7); items/config subcommands return synchronously. Prevents a hanging test. |
</threat_model>

<verification>
- `python -m pytest tests/test_smoke.py tests/test_backend_selection.py -q` exits 0
- `python -m pytest -q` exits 0 (full suite green including the new smoke + selection modules)
</verification>

<success_criteria>
Env-independent smoke proves import + safe CLI commands succeed and backend auto-selection picks keyring/file/env correctly via a mocked _has_real_keyring — the automated half of the XPLAT-02 verification matrix, runnable on any OS.
</success_criteria>

<output>
Create `.planning/phases/11-cross-platform-verification/11-04-SUMMARY.md` when done.
</output>
