---
phase: 11-cross-platform-verification
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - models.py
  - core/config_schema.py
  - core/credentials.py
  - logger.py
  - tests/conftest.py
autonomous: true
requirements: [XPLAT-01]
must_haves:
  truths:
    - "models.DB_PATH resolves under core.paths.data_dir() — no longer CWD-relative"
    - "credentials._DEFAULT_STORE_PATH, config_schema._DEFAULT_YAML_PATH, logger log dir all anchor through core/paths.py"
    - "credentials.data_dir config override still wins over _DEFAULT_STORE_PATH"
    - "logger.py imports core.paths.log_dir lazily inside writeLog (no circular import)"
    - "all 335 existing tests stay green after re-anchor"
  artifacts:
    - path: "models.py"
      provides: "DB_PATH anchored to data_dir()"
      contains: "from core.paths import data_dir"
    - path: "logger.py"
      provides: "log dir via core.paths.log_dir, lazy-imported inside writeLog"
    - path: "tests/conftest.py"
      provides: "tmp_data_dir extended to also set SHOPBOT_DATA_DIR"
  key_links:
    - from: "models.py"
      to: "core.paths.data_dir"
      via: "DB_PATH = str(data_dir() / 'shop_py_bot.db')"
      pattern: "from core.paths import data_dir"
    - from: "logger.py:writeLog"
      to: "core.paths.log_dir"
      via: "lazy import inside writeTofile branch"
      pattern: "from core.paths import log_dir"
---

<objective>
Re-anchor the four legacy path anchors (models DB, creds.bin, config.yml, log dir) onto `core/paths.py` from Plan 01. Fix the `models.py` CWD-relative DB bug. Keep all 335 existing tests green by preserving the monkeypatch seams and extending `tmp_data_dir` to also redirect `SHOPBOT_DATA_DIR`.

Purpose: XPLAT-01 — paths must resolve to OS-standard locations regardless of launch directory; `models.py:5 DB_PATH = os.path.join('data', ...)` is CWD-relative and breaks when `shoppybot` is launched from anywhere but the repo root.
Output: re-anchored models.py, core/config_schema.py, core/credentials.py, logger.py; extended conftest.py fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cross-platform-verification/11-CONTEXT.md
@.planning/phases/11-cross-platform-verification/11-PATTERNS.md
@.planning/phases/11-cross-platform-verification/11-RESEARCH.md
@.planning/phases/11-cross-platform-verification/11-01-SUMMARY.md

<interfaces>
From core/paths.py (created in Plan 01):
```python
def data_dir() -> Path        # OS-standard user data dir (or SHOPBOT_DATA_DIR override)
def config_path() -> Path     # data_dir() / "config.yml"
def log_dir() -> Path         # OS-standard user log dir (or override / "logs")
```

Current anchors to replace:
- models.py:5            DB_PATH = os.path.join('data', 'shop_py_bot.db')        # CWD-relative bug
- core/config_schema.py:17  _DEFAULT_YAML_PATH = Path(__file__).parent.parent / "config.yml"
- core/credentials.py:40 _DEFAULT_STORE_PATH = Path(__file__).parent.parent / "data" / "creds.bin"
- logger.py:41-45        _scriptdir = ...; log_dir = os.path.join(_scriptdir, "logs"); ...

CRITICAL circular-import seam (locked decision): core/paths.py (Plan 03) imports writeLog FROM logger.py.
Therefore logger.py must NOT import core.paths at module top — it must lazy-import `from core.paths import log_dir`
INSIDE the writeTofile branch of writeLog(). Follow 11-PATTERNS.md "Lazy import to break circular dependency".

Existing test seams that MUST keep working:
- core/config_schema.py __init__ accepts yaml_file=Path|str|None (tests pass yaml_file=tmp_config_yml)
- core/credentials.py cfg.credentials.data_dir override path (lines ~343-347) takes precedence over _DEFAULT_STORE_PATH
- tests/conftest.py:53-58 tmp_data_dir monkeypatches models.DB_PATH directly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor models.py DB_PATH and config_schema/credentials defaults through core/paths.py</name>
  <read_first>
    - models.py (lines 1-34 — DB_PATH + initialize_db makedirs)
    - core/config_schema.py (lines 1-17 — _DEFAULT_YAML_PATH)
    - core/credentials.py (lines 15-45, ~340-350 — _DEFAULT_STORE_PATH + data_dir override)
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (models / config_schema / credentials re-anchor sections)
  </read_first>
  <action>
    In models.py: add `from core.paths import data_dir as _paths_data_dir` near the top imports and change line 5 to `DB_PATH = str(_paths_data_dir() / "shop_py_bot.db")`. Leave initialize_db's os.makedirs(os.path.dirname(DB_PATH), exist_ok=True) unchanged — dirname of an absolute path returns the correct parent. In core/config_schema.py: add `from core.paths import config_path as _paths_config_path` and change line 17 to `_DEFAULT_YAML_PATH: Path = _paths_config_path()`. In core/credentials.py: add `from core.paths import data_dir as _paths_data_dir` and change line 40 to `_DEFAULT_STORE_PATH: Path = _paths_data_dir() / "creds.bin"`. Do NOT touch the cfg.credentials.data_dir override block (~lines 343-347) — that config override must still win over _DEFAULT_STORE_PATH. Use pathlib `/` operator only; introduce no hardcoded "/" or "\\" or 'data/' / 'logs/' string literals (the SC1 guard from Plan 01 must stay green).
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -m pytest tests/test_paths.py tests/test_models.py tests/test_credentials.py tests/test_config_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `models.py contains from core.paths import data_dir`
    - `core/config_schema.py contains from core.paths import config_path`
    - `core/credentials.py contains from core.paths import data_dir`
    - models.py line 5 no longer contains os.path.join('data'
    - test_paths.py::test_no_hardcoded_separators still passes (no new 'data/ or 'logs/ literals)
    - `python -m pytest tests/test_models.py tests/test_credentials.py tests/test_config_schema.py -q` exits 0
  </acceptance_criteria>
  <done>DB/creds/config anchors route through core/paths.py; CWD-relative DB bug fixed; config override preserved; those test modules green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor logger.py log dir via lazy core.paths import inside writeLog</name>
  <read_first>
    - logger.py (lines 1-47 — _CONFIG_PATH at line 10, writeTofile branch at lines 40-47)
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (logger.py re-anchor section + "Lazy import to break circular dependency")
    - .planning/phases/11-cross-platform-verification/11-CONTEXT.md (CIRCULAR IMPORT locked decision)
  </read_first>
  <action>
    In logger.py writeLog(), replace the writeTofile branch (lines 41-45) so it lazy-imports inside the branch: `from core.paths import log_dir as _paths_log_dir` (lazy — avoids the circular import because core/paths.py's migrate_legacy_paths imports writeLog from logger), then `_log_dir = _paths_log_dir(); _log_dir.mkdir(parents=True, exist_ok=True); log_file_path = _log_dir / f"{datetime.now().strftime('%Y%B%d')}.log"`, then open(log_file_path, "a", encoding="utf-8"). Do NOT add `from core.paths import ...` at logger.py module top — it must stay inside the function body. Remove the now-unused os.path.join/_scriptdir lines in that branch. Leave logger.py's separate _CONFIG_PATH logging-level read (line 10) as-is: it is a pre-existing repo-root config read outside this phase's locked scope and degrades gracefully to level 5 on miss; note this in the SUMMARY as an observed follow-up, do not change it. Introduce no hardcoded 'logs/ string literal.
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -c "import logger; logger.writeLog('migrate-test', 'INFO')" && python -m pytest tests/test_paths.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `logger.py contains from core.paths import log_dir` AND that import sits inside writeLog (not at module top)
    - `import logger; logger.writeLog('x','INFO')` runs without ImportError or circular-import error
    - test_paths.py::test_no_hardcoded_separators still passes
    - `grep -v '^#' logger.py | grep -c "os.path.join.*logs"` returns 0 (the old hardcoded log join is gone)
  </acceptance_criteria>
  <done>logger writes to core.paths.log_dir() via lazy import; no circular import; writeLog runs clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tmp_data_dir fixture and run full suite to confirm 335 stay green</name>
  <read_first>
    - tests/conftest.py (lines 53-58 — tmp_data_dir fixture)
    - .planning/phases/11-cross-platform-verification/11-PATTERNS.md (conftest.py extend tmp_data_dir section)
  </read_first>
  <action>
    In tests/conftest.py, extend the tmp_data_dir fixture to ALSO set the env override so core/paths.data_dir() agrees with the monkeypatched models.DB_PATH: add `monkeypatch.setenv("SHOPBOT_DATA_DIR", str(tmp_path))` as the first line of the fixture body, keep the existing `monkeypatch.setattr(models, "DB_PATH", str(tmp_path / "shop_py_bot.db"))`, and keep `return tmp_path`. This keeps both seams consistent so tests that go through models.DB_PATH and tests that go through core/paths resolve to the same temp dir. Then run the full suite to confirm no regression from the re-anchors.
  </action>
  <verify>
    <automated>cd E:\repos\ShopPyBot && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py contains monkeypatch.setenv("SHOPBOT_DATA_DIR"` inside tmp_data_dir
    - `python -m pytest -q` exits 0 with no NEW failures vs the 335-test baseline (baseline + new test_paths.py tests all green)
  </acceptance_criteria>
  <done>tmp_data_dir redirects both seams; full suite green after the re-anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> credential store | _DEFAULT_STORE_PATH now points at the OS-standard user dir for creds.bin |
| import graph | logger <-> core.paths circular-import risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | creds.bin relocation to OS-standard dir | mitigate | Re-anchor only changes the DEFAULT path string; no file is moved in this plan (Plan 03 migrates). creds.bin stays Fernet-encrypted; new location (%LOCALAPPDATA% / ~/.local/share) is user-owned, strictly more protected than a repo-relative data/ dir. cfg.credentials.data_dir override still wins. |
| T-11-04 | Denial of Service | logger <-> core.paths circular import | mitigate | logger.py lazy-imports core.paths.log_dir INSIDE writeLog (locked decision); verified by `import logger; writeLog(...)` smoke in Task 2. Prevents an import-time deadlock that would break every module that imports logger. |
| T-11-05 | Tampering | hardcoded separator regression | mitigate | All re-anchors use pathlib `/`; Plan 01's test_no_hardcoded_separators guard re-runs in Tasks 1-2 acceptance criteria. |
</threat_model>

<verification>
- `python -m pytest -q` exits 0 (335 baseline + Plan 01 path tests, no new failures)
- `python -c "import models; print(models.DB_PATH)"` prints an absolute path under the OS data dir (not a bare 'data/shop_py_bot.db')
- `import logger; logger.writeLog('x','INFO')` runs with no circular-import error
</verification>

<success_criteria>
All four anchors route through core/paths.py; the CWD-relative DB bug is fixed; logger avoids the circular import via lazy import; the config override still wins; the full existing suite stays green.
</success_criteria>

<output>
Create `.planning/phases/11-cross-platform-verification/11-02-SUMMARY.md` when done.
</output>
