---
milestone: v3.0
name: Resilience + Ecosystem
status: tech_debt
audited: 2026-06-09
method: parallel adversarial audit (3 auditors — traceability, integration, intent+debt)
blockers: 0
concerns: 2
debt: 6
suite: 548 passed, 2 skipped
coverage: ~99% on v3.0 modules
---

# v3.0 Milestone Audit — Resilience + Ecosystem

## Verdict

**tech_debt** — no implementation blockers. All six phases delivered; 17/18 requirements are genuinely implemented with code AND passing tests, and the two documented scope deferrals (ANTI-06 Amazon WAF, PRICE-02 non-Amazon get_price) are honestly recorded, not hidden. Both milestone pillars land end-to-end:

- **Resilience:** JS fingerprint stealth (ANTI-08), opt-in proxy rotation + ban detection + WebRTC leak prevention + per-instance scoping (ANTI-04/05), 2captcha reCAPTCHA-v2 solving + balance gate + spend cap + executor wrapping (ANTI-06/07), v2.0 debt paydown + hardened tests (STAB-01/02/03).
- **Ecosystem:** plugin ABC metadata + `shoppybot plugins list` (REG-02/03), wiki registry SPEC + contributor docs (REG-01/04), per-item price targets + append-only `price_history` + fan-out `price_drop` alerts with separate dedup + price-history CLI (PRICE-01..06).

Cross-phase integration auditor: **PASSED** — proxy/stealth/captcha/get_price compose in one setup/poll path with no conflict, `PLUGIN_API_VERSION` stayed 2 throughout, stock vs price_drop dedup use disjoint DB columns (regression-proven), full suite re-run 548 passed / 2 skipped.

## Concerns (2)

| ID | Area | Detail |
|----|------|--------|
| STAB-01 | Cross-OS manual checks | The `[x]` checkbox overstates the literal criterion ("4 manual checks EXECUTED and documented pass/fail"). Reality: all are recorded **PENDING** in `12-HUMAN-UAT.md` / `docs/PLATFORMS.md` — honestly documented as deferred (not falsely passed), and this matches the headless-CI capability constraint the user already chose to defer. Only MC-4's banner conditional is CI-asserted. **Accepted deferral, not a hidden failure.** |
| F6 / SEED-001 | Public-repo history scrub | The repo **IS public** (`gh` confirms `visibility: PUBLIC`). [[SEED-001]] (a full gitleaks/trufflehog history audit + squash before release/announce) has not been run. No leak is evident — planning docs reference secret **names only** (verified, never values); `config.yml`/`data/*.db`/`.env` are gitignored. **Highest-priority pre-release item**; tracked, not a milestone blocker. |

## Acknowledged Debt (6)

| ID | Area | Disposition |
|----|------|-------------|
| F3 | 13 deferred live-environment manual UAT checks (phases 12-16) | Intentional headless-CI deferrals, tracked per-phase in `{N}-HUMAN-UAT.md`. Run on real Windows/Ubuntu + funded accounts before trusting in production. |
| F4 | ANTI-06 Amazon WAF auto-solve | Deferred by user decision; graceful manual-pause fallback ships; `solve_amazon_waf()` stub retained + test-covered. Tracked in `todos/pending/waf-auto-solve-followup.md`. |
| F5 | PRICE-02 non-Amazon get_price | Amazon get_price() is real; other 6 plugins inherit default-None as designed → price monitoring is effectively **Amazon-only** at ship time. Matches the documented contract. |
| F7 / SEED-002 | release-please version tagging | Not yet set up; paired with SEED-001 as release-hardening. No impact on v3.0 intent. |
| F8 | `core/stealth.py` 325 lines (>300 guideline) | The overage is the embedded ~54-line `STEALTH_JS` data constant; Python logic is ~271 lines. Acceptable as-is. |
| ENV-NOTE | Fresh checkout needs `pip install -r requirements.txt` | One auditor hit a transient missing `platformdirs` (it IS in requirements.txt) → 0 failures after install. Environmental, not a code defect. The Windows host platform-gates more tests as skipped (POSIX/keyring/Ubuntu); no regressions. |

## Requirement Traceability (18/18 checkbox; 17 fully delivered, STAB-01 accepted-deferral)

STAB-02/03 ✅ · ANTI-04/05/06/07/08 ✅ · REG-01/02/03/04 ✅ · PRICE-01..06 ✅ · STAB-01 ⚠ (automated portion done; manual cross-OS checks deferred per standing policy).

## Recommendation

Complete the milestone — the debt is honest, tracked, and contains 0 blockers. Action [[SEED-001]] (public-repo history scrub) before any release/tag/announce, and close the per-phase HUMAN-UAT checks on real hardware when available.
