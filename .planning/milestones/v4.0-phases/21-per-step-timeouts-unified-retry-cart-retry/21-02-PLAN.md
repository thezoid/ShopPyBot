---
phase: 21-per-step-timeouts-unified-retry-cart-retry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - models.py
  - tests/test_models.py
autonomous: true
requirements: [BUY-05]

must_haves:
  truths:
    - "checkout_attempts can be incremented by exactly 1 per call via increment_checkout_attempts_sync(link)"
    - "get_item_order_state_sync(link) returns (purchased as bool, order_id) — the idempotency anchor for cart-retry"
    - "get_item_order_state_sync returns (False, None) when the link is not present"
  artifacts:
    - path: "models.py"
      provides: "increment_checkout_attempts_sync + get_item_order_state_sync helpers"
      contains: "checkout_attempts = checkout_attempts + 1"
    - path: "tests/test_models.py"
      provides: "unit coverage for both new helpers (increment-by-one, order-state read, missing-row)"
  key_links:
    - from: "models.py:increment_checkout_attempts_sync"
      to: "items.checkout_attempts column"
      via: "parameterized UPDATE"
      pattern: "checkout_attempts = checkout_attempts \\+ 1"
    - from: "models.py:get_item_order_state_sync"
      to: "items.purchased + items.order_id columns"
      via: "parameterized SELECT"
      pattern: "SELECT purchased, order_id FROM items WHERE link"
---

<objective>
Add two additive `models.py` sync helpers that the orchestrator cart-retry (Plan 03) needs: `increment_checkout_attempts_sync(link)` (resolves the P19 carry-forward — increments the attempt counter by exactly 1) and `get_item_order_state_sync(link)` (the targeted idempotency read returning `(purchased, order_id)`). Both follow the existing parameterized-SQL helper pattern. No schema changes — `checkout_attempts`, `purchased`, and `order_id` columns already exist from Phase 19.

Purpose: The attempt counter gives an accurate per-attempt count; the order-state read is the no-double-buy guard (BUY-05). Keeping both as standalone sync helpers matches the project's DB pattern (orchestrator calls them via run_in_executor).
Output: two new functions in `models.py`, tests in `tests/test_models.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-CONTEXT.md
@.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-RESEARCH.md
@.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-PATTERNS.md

<interfaces>
<!-- Existing models.py helpers the new ones mirror (verified from source). -->

From models.py (existing — copy these patterns exactly):
- get_item_notification_state_sync(link: str) -> tuple[bool, str | None]  (lines 141-150; template for get_item_order_state_sync — SELECT two cols, fetchone, None-row → (False, None), `return bool(row[0]), row[1]`)
- update_item_purchased_sync(link)  (lines 101-104; template for increment_checkout_attempts_sync — single parameterized UPDATE inside `with get_db_connection() as conn:`)
- update_item_confirmed_sync(link, order_id, confirmed_at)  (lines 107-117; confirms order_id column exists — no schema change needed)

Columns already present (Phase 19): items.checkout_attempts (INTEGER DEFAULT 0), items.order_id (TEXT), items.confirmed_at (TEXT), items.purchased.
DB connection helper: get_db_connection() context manager (models.py lines 10-30).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: models.py — increment_checkout_attempts_sync + get_item_order_state_sync</name>
  <files>models.py, tests/test_models.py</files>
  <read_first>
    - models.py lines 101-117 (PATTERNS analog A/B: update_item_purchased_sync single UPDATE, update_item_confirmed_sync confirming order_id column)
    - models.py lines 141-150 (PATTERNS analog: get_item_notification_state_sync — exact two-column read template)
    - models.py lines 10-30 (get_db_connection context manager)
    - 21-RESEARCH.md Pattern 5 + Pattern 6 (illustrative helper shapes)
  </read_first>
  <behavior>
    - increment_checkout_attempts_sync(link) raises checkout_attempts from 0 to 1 on first call, 1 to 2 on second call (increments by exactly 1, never resets)
    - get_item_order_state_sync(link) returns (False, None) for a freshly added item (purchased=0, order_id NULL)
    - After update_item_confirmed_sync(link, "ORDER123", ts), get_item_order_state_sync(link) returns (True, "ORDER123")
    - get_item_order_state_sync("https://no-such-item") returns (False, None) (missing row)
  </behavior>
  <action>
    Add increment_checkout_attempts_sync(link: str) -> None to models.py: docstring references BUY-05/REL-08; body is a single `with get_db_connection() as conn:` block executing the parameterized UPDATE with SQL `checkout_attempts = checkout_attempts + 1` (relative increment, never an absolute set) WHERE link=? — mirror update_item_purchased_sync exactly. Add get_item_order_state_sync(link: str) -> tuple[bool, str | None] to models.py: docstring references the BUY-05 idempotency check; body copies get_item_notification_state_sync's structure exactly, changing the SELECT to `SELECT purchased, order_id FROM items WHERE link=?`, returning (False, None) when the row is None and `bool(row[0]), row[1]` otherwise. Place both helpers near the existing update_item_purchased_sync / get_item_notification_state_sync helpers. Keep each function under 30 lines.
  </action>
  <verify>
    <automated>pytest tests/test_models.py -x</automated>
  </verify>
  <acceptance_criteria>
    - increment_checkout_attempts_sync uses relative SQL `checkout_attempts = checkout_attempts + 1` (grep confirms; not an absolute SET)
    - get_item_order_state_sync returns a (bool, str | None) tuple and handles the missing-row case as (False, None)
    - No ALTER TABLE / schema change introduced (columns already exist)
    - `pytest tests/test_models.py -x` passes the increment-by-one, order-state read, and missing-row tests
  </acceptance_criteria>
  <done>Both helpers added to models.py; tests/test_models.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| link (str) → SQLite | item link used as the WHERE-clause key for read/update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | SQL injection via link | mitigate | Both helpers use parameterized queries (`WHERE link=?` with tuple binding); no string interpolation — matches existing models.py convention |
| T-21-04 | Repudiation | inaccurate attempt count enabling double-buy detection failure | mitigate | Relative `+ 1` increment guarantees a monotonic per-attempt count; never resets |
| T-21-SC | Tampering | pip installs | accept | No new packages; stdlib sqlite3 only (RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `pytest tests/test_models.py -x` green
- grep confirms `checkout_attempts = checkout_attempts + 1` (relative) in models.py
- grep confirms `SELECT purchased, order_id FROM items WHERE link` parameterized read
</verification>

<success_criteria>
- increment_checkout_attempts_sync increments by exactly 1 per call (BUY-05 attempt accounting)
- get_item_order_state_sync returns (purchased, order_id) as the cart-retry idempotency anchor
- Missing-row returns (False, None) without raising
- No schema changes; parameterized SQL throughout
</success_criteria>

<output>
Create `.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-02-SUMMARY.md` when done
</output>
