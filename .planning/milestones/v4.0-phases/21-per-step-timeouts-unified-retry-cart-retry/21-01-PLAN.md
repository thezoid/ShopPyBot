---
phase: 21-per-step-timeouts-unified-retry-cart-retry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/retry.py
  - tests/test_retry.py
  - tests/test_no_retry_loops.py
autonomous: true
requirements: [REL-08]

must_haves:
  truths:
    - "core/retry.py is the single home for exponential backoff (RetryPolicy + with_retry + compute_delay)"
    - "compute_delay returns deterministic values when passed a seeded rng"
    - "with_retry stops after policy.max_attempts and sleeps compute_delay between attempts"
    - "with_retry calls on_attempt(attempt) before each attempt and should_retry(result) to decide retry"
    - "No `for attempt in range(` retry loop exists anywhere outside core/retry.py (CI-enforced)"
  artifacts:
    - path: "core/retry.py"
      provides: "RetryPolicy dataclass, with_retry async helper, pure compute_delay"
      contains: "class RetryPolicy"
      min_lines: 40
    - path: "tests/test_retry.py"
      provides: "compute_delay determinism, with_retry max_attempts + backoff sleep, callback wiring"
    - path: "tests/test_no_retry_loops.py"
      provides: "AST CI guard forbidding `for attempt in range(` outside core/retry.py"
  key_links:
    - from: "core/retry.py:with_retry"
      to: "core/retry.py:compute_delay"
      via: "delay computation between attempts"
      pattern: "compute_delay\\("
    - from: "tests/test_no_retry_loops.py"
      to: "core/, plugins/, models.py, main.py"
      via: "ast.For walk targeting id == 'attempt' iterating range"
      pattern: "ast\\.For"
---

<objective>
Create `core/retry.py` as the SINGLE source of exponential backoff for the whole project: a generic `RetryPolicy` dataclass, a pure `compute_delay(attempt, policy, rng)` function, and an `async def with_retry(...)` helper. Add the CI AST guard that forbids any new `for attempt in range(` retry loop outside this module. This is the architectural keystone for REL-08: Phase 22's supervisor restart will import the SAME `RetryPolicy` + `compute_delay`, and the cart-retry in Plan 03 will use `with_retry`.

Purpose: One backoff implementation prevents supervisor restart and cart-retry from compounding into a runaway loop (REL-08). Generic max_attempts + backoff keeps it reusable by P22's supervisor.
Output: `core/retry.py`, `tests/test_retry.py`, `tests/test_no_retry_loops.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-CONTEXT.md
@.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-RESEARCH.md
@.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-PATTERNS.md

<interfaces>
<!-- RetryPolicy fields are sourced from CheckoutConfig (core/config_schema.py lines 264-272). -->
<!-- Plan 03 (orchestrator) will construct RetryPolicy from these config fields. -->

From core/config_schema.py (CheckoutConfig — already exists, P18):
- max_cart_retries: int (default 3, ge=0)
- backoff_base: float (default 2.0, ge=0.0)
- backoff_jitter: float (default 0.5, ge=0.0)

Module-header analog — core/confirmation.py lines 1-11 (stdlib-only module, `from __future__ import annotations`).
Dataclass analog — core/stealth.py lines 151-157 (`@dataclass` with typed fields + docstring).
AST-test analog — tests/test_no_cvv_in_logs.py lines 27-83 (repo_root resolution, file-existence guard, ast.walk, violation accumulation, single final assert).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core/retry.py — RetryPolicy + compute_delay + with_retry</name>
  <files>core/retry.py, tests/test_retry.py</files>
  <read_first>
    - core/stealth.py lines 151-162 (PATTERNS analog: @dataclass with typed fields + docstring)
    - core/confirmation.py lines 1-11 (PATTERNS analog: stdlib-only module header, `from __future__ import annotations`)
    - core/config_schema.py lines 264-272 (CheckoutConfig field names/defaults that map to RetryPolicy)
    - 21-RESEARCH.md Pattern 1 + Pattern 2 (compute_delay and with_retry illustrative shapes)
  </read_first>
  <behavior>
    - compute_delay(0, RetryPolicy(max_attempts=3, backoff_base=2.0, backoff_jitter=0.0)) returns 1.0 (backoff_base**0 with zero jitter)
    - compute_delay(1, policy_base2_jitter0) returns 2.0; compute_delay(2, ...) returns 4.0
    - compute_delay(attempt, policy, rng=random.Random(42)) returns identical values across two calls with two fresh Random(42) instances (deterministic with seeded rng)
    - compute_delay jitter is bounded: result is in [backoff_base**attempt, backoff_base**attempt + backoff_jitter]
    - with_retry calls fn() exactly max_attempts times when should_retry always returns True; returns the last result
    - with_retry returns early (fewer calls) the first time should_retry(result) is False
    - with_retry awaits on_attempt(attempt) before each fn() call, with attempt 0-indexed
    - with_retry sleeps compute_delay(attempt, policy, rng) between attempts but NOT after the final attempt
    - with_retry does NOT catch CancelledError raised from asyncio.sleep (propagates to caller)
  </behavior>
  <action>
    Create core/retry.py. Module header: stdlib-only docstring stating this is the single backoff source (REL-08), `from __future__ import annotations`, import asyncio, import random, from dataclasses import dataclass. Define `@dataclass class RetryPolicy` with three typed fields: max_attempts: int, backoff_base: float, backoff_jitter: float (mirror the CheckoutConfig knobs; document in the docstring that Phase 22's supervisor reuses this same policy). Define module-level pure function `compute_delay(attempt: int, policy: RetryPolicy, rng=random) -> float`: return `policy.backoff_base ** attempt + rng.uniform(0, policy.backoff_jitter)`; attempt is 0-indexed; `rng` defaults to the random module and accepts a random.Random instance for deterministic tests (do not assert on the random module directly per Pitfall 3). Define `async def with_retry(fn, policy: RetryPolicy, should_retry=None, on_attempt=None, rng=random)`: loop `for attempt in range(policy.max_attempts)` (this is the ONLY allowed `for attempt in range(` in the codebase), await on_attempt(attempt) when on_attempt is not None BEFORE calling fn, call `result = await fn()`, return result when should_retry is None or `should_retry(result)` is False, and when retrying and attempt is not the last, `await asyncio.sleep(compute_delay(attempt, policy, rng))`; never wrap asyncio.sleep in try/except — CancelledError must propagate (Pitfall 6). Keep each function under 30 lines. Document the max_attempts semantic in with_retry docstring: max_attempts is the TOTAL number of attempts (so the caller in Plan 03 passes max_cart_retries + 1).
  </action>
  <verify>
    <automated>pytest tests/test_retry.py -x</automated>
  </verify>
  <acceptance_criteria>
    - core/retry.py defines RetryPolicy (dataclass), compute_delay (module-level function, not a method), and with_retry (async)
    - `pytest tests/test_retry.py -x` passes all compute_delay determinism, bounds, and with_retry max_attempts/backoff/callback tests
    - compute_delay is callable without instantiating with_retry (importable by Phase 22 supervisor directly)
    - No try/except around asyncio.sleep in with_retry
  </acceptance_criteria>
  <done>RetryPolicy + compute_delay + with_retry implemented; tests/test_retry.py green.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_no_retry_loops.py — AST CI guard (REL-08)</name>
  <files>tests/test_no_retry_loops.py</files>
  <read_first>
    - tests/test_no_cvv_in_logs.py lines 1-83 (PATTERNS analog: repo_root, file-existence guard, ast.walk, violation list, final assert)
    - 21-RESEARCH.md "CI Assertion Design" (AST walk targeting ast.For with target id == "attempt" iterating range)
    - core/captcha.py line 59 (`for _ in range(_MAX_POLLS)` — must NOT be flagged; uses `_`)
    - core/stealth.py line 244 (`for _ in range(len(self._entries))` — must NOT be flagged; uses `_`)
  </read_first>
  <action>
    Create tests/test_no_retry_loops.py mirroring the AST-walk structure of test_no_cvv_in_logs.py. Resolve repo_root = Path(__file__).parent.parent. Scan all `.py` files under `core/` and `plugins/` (recursively) plus the direct files `models.py` and `main.py`, EXCLUDING `core/retry.py` (the one permitted location). For each scanned file, assert it parses, then `ast.walk` for `ast.For` nodes where `node.target` is an `ast.Name` with `id == "attempt"` AND `node.iter` is an `ast.Call` whose function resolves to the name `range`. Accumulate violations as `f"{path.name}:{node.lineno}"`. Final single assert: `assert not violations` with a message naming the offending file:line list and stating that `for attempt in range(` is forbidden outside core/retry.py. Add an inline comment documenting that poll loops using `for _ in range(` (captcha _MAX_POLLS, stealth proxy advance) are structurally exempt because the loop variable is `_`, not `attempt` — no whitelist needed.
  </action>
  <verify>
    <automated>pytest tests/test_no_retry_loops.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_no_retry_loops.py -x` passes against the current codebase (core/retry.py from Task 1 is excluded; captcha/stealth `for _ in range(` loops are not flagged)
    - The scan list includes core/, plugins/, models.py, main.py and excludes core/retry.py
    - Test fails (assertion message lists file:line) if a `for attempt in range(` loop is introduced outside core/retry.py — verifiable by a temporary loop in a scanned file
  </acceptance_criteria>
  <done>tests/test_no_retry_loops.py green; REL-08 single-backoff-source statically enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → RetryPolicy | max_cart_retries / backoff_base / backoff_jitter cross from CheckoutConfig into retry math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Denial of Service | with_retry loop | mitigate | Loop bounded by policy.max_attempts; with_retry always terminates; max_cart_retries upper-bounded by CheckoutConfig Field(ge=0) |
| T-21-02 | Tampering | duplicate backoff implementations | mitigate | tests/test_no_retry_loops.py AST guard forbids new `for attempt in range(` outside core/retry.py |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase; all stdlib (asyncio, random, dataclasses) per RESEARCH Package Legitimacy Audit (N/A) |
</threat_model>

<verification>
- `pytest tests/test_retry.py tests/test_no_retry_loops.py -x` green
- core/retry.py exports RetryPolicy, compute_delay, with_retry
- No new external dependencies introduced
</verification>

<success_criteria>
- core/retry.py is the single backoff implementation (RetryPolicy + with_retry + compute_delay)
- compute_delay deterministic under a seeded rng; jitter bounded by backoff_jitter
- with_retry stops after max_attempts, sleeps compute_delay between attempts, propagates CancelledError
- CI AST guard forbids `for attempt in range(` outside core/retry.py; poll loops exempt
- REL-08 satisfied: generic policy reusable by Phase 22 supervisor
</success_criteria>

<output>
Create `.planning/phases/21-per-step-timeouts-unified-retry-cart-retry/21-01-SUMMARY.md` when done
</output>
