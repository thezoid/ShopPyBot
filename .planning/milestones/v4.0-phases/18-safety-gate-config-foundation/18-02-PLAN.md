---
phase: 18-safety-gate-config-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/plugin_base.py
  - tests/test_plugin_base.py
autonomous: true
requirements: [BUY-02]
must_haves:
  truths:
    - "place_order_guarded returns False and does NOT call click_fn when test_mode=True"
    - "place_order_guarded returns False and does NOT call click_fn when monitor_only=True"
    - "place_order_guarded calls click_fn once and returns True when both flags are False"
    - "place_order_guarded never raises and logs an INFO line when suppressed"
  artifacts:
    - path: "core/plugin_base.py"
      provides: "place_order_guarded concrete async method on RetailerPlugin ABC"
      contains: "async def place_order_guarded"
    - path: "tests/test_plugin_base.py"
      provides: "3 unit tests: suppressed-by-test_mode, suppressed-by-monitor_only, allowed"
  key_links:
    - from: "RetailerPlugin.place_order_guarded"
      to: "self.config.debug.test_mode / monitor_only"
      via: "getattr-safe read of debug flags"
      pattern: "place_order_guarded"
    - from: "place_order_guarded"
      to: "logger.writeLog"
      via: "INFO log on suppression"
      pattern: "from logger import writeLog"
---

<objective>
Add the single `place_order_guarded()` concrete method to the `RetailerPlugin` ABC. It honors `test_mode`/`monitor_only` (reading `self.config.debug`), invoking the place-order click only when both are False, returning `bool`, never raising. This is the central guard all 7 plugins (Plan 04) will route through.

Purpose: Closes the confirmed safety hole where 6 of 7 plugins ignore `test_mode`. Centralizing the gate on the ABC means every current and future community plugin gets it for free. Satisfies BUY-02 (the ABC half) and success criterion 3 (no place-order click when monitor_only or test_mode is active).
Output: Updated `core/plugin_base.py`, extended `tests/test_plugin_base.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-safety-gate-config-foundation/18-CONTEXT.md
@.planning/phases/18-safety-gate-config-foundation/18-RESEARCH.md
@.planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md

<interfaces>
<!-- Existing ABC structure the executor builds against. Extracted from core/plugin_base.py. -->

RetailerPlugin __init__ stores typed config (line 38-41):
    def __init__(self, config) -> None:
        self.config = config   # typed AppConfig
        self.driver = None

Concrete-method analogs already on the ABC (read these for the getattr-safe + concrete-async style):
    - _handle_ban (lines 43-55): concrete, getattr-safe reads of self attributes
    - get_price (lines 71-78): concrete async no-op, docstring notes PLUGIN_API_VERSION stays 2

PLUGIN_API_VERSION = 2 (line 6). This method is ADDITIVE — do NOT bump the version.

Contract (locked in CONTEXT.md):
    async def place_order_guarded(self, click_fn) -> bool:
        # returns True when click_fn() is awaited; False (no call) when suppressed; never raises
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add place_order_guarded() to RetailerPlugin ABC</name>
  <files>core/plugin_base.py</files>
  <read_first>
    - core/plugin_base.py (the file being modified — read the imports block at top, `_handle_ban` lines 43-55, and `get_price` lines 71-78 as the concrete-method analogs)
    - .planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md (analog: "place_order_guarded concrete method" section lines 102-151 — note: writeLog is NOT yet imported in plugin_base.py and must be added)
    - .planning/phases/18-safety-gate-config-foundation/18-RESEARCH.md (Pattern 3 lines 188-212; Pitfall 1 lines 320-324 — the method MUST be `async def` because click_fn is an async coroutine)
  </read_first>
  <behavior>
    - Test: with `cfg.debug.test_mode=True, monitor_only=False` -> `await plugin.place_order_guarded(click_fn)` returns False; `click_fn.assert_not_called()`.
    - Test: with `cfg.debug.test_mode=False, monitor_only=True` -> returns False; click_fn not called.
    - Test: with `cfg.debug.test_mode=False, monitor_only=False` -> returns True; `click_fn.assert_awaited_once()`.
  </behavior>
  <action>
    Add `from logger import writeLog` to the imports at the top of `core/plugin_base.py` (it is not currently imported there; mirror how plugin files import it).

    Add a concrete `async def place_order_guarded(self, click_fn) -> bool` method on `RetailerPlugin`, placed after `get_price` and before `login`. It MUST be `async` (RESEARCH Pitfall 1: click_fn is an async coroutine; a sync method breaks `await self.place_order_guarded(...)`). Read the debug flags getattr-safe following the `_handle_ban` style: `debug = getattr(self.config, "debug", None) if self.config else None`; `test_mode = getattr(debug, "test_mode", True)` (safe default True = suppressed); `monitor_only = getattr(debug, "monitor_only", False)`. If `test_mode or monitor_only`: call `writeLog("place-order suppressed (monitor_only/test_mode)", "INFO")` and `return False` (no exception, no interactive pause). Otherwise `await click_fn()` then `return True`. Docstring must state it returns True on click / False when suppressed, never raises, and PLUGIN_API_VERSION stays 2 (additive). This is per CONTEXT decision (place_order_guarded Contract) and BUY-02.
  </action>
  <verify>
    <automated>python -c "import inspect; from core.plugin_base import RetailerPlugin; assert inspect.iscoroutinefunction(RetailerPlugin.place_order_guarded); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `core/plugin_base.py` imports `writeLog` from `logger`.
    - `RetailerPlugin.place_order_guarded` exists, is `async`, and reads `test_mode`/`monitor_only` getattr-safe.
    - The suppression branch calls `writeLog(..., "INFO")` and returns False with no `await click_fn()`.
    - `PLUGIN_API_VERSION` is still `2` (no bump).
    - The inspect command prints `ok`.
  </acceptance_criteria>
  <done>place_order_guarded is a concrete async method on the ABC; suppression path returns False + INFO log; click path awaits click_fn + returns True.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add place_order_guarded unit tests</name>
  <files>tests/test_plugin_base.py</files>
  <read_first>
    - tests/test_plugin_base.py (the file being extended — read the `MinimalPlugin` stub pattern lines 13-23 and existing async test style; asyncio_mode=auto means no `@pytest.mark.asyncio` decorator needed)
    - .planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md (test analog: "Async test structure" lines 477-491 showing the suppressed-by-test_mode test sketch with MagicMock cfg + AsyncMock click_fn)
  </read_first>
  <behavior>
    - test_place_order_guarded_suppressed_test_mode: test_mode=True, monitor_only=False -> result is False, click_fn not called.
    - test_place_order_guarded_suppressed_monitor_only: test_mode=False, monitor_only=True -> result is False, click_fn not called.
    - test_place_order_guarded_allows_click: both False -> result is True, click_fn awaited exactly once.
  </behavior>
  <action>
    Add three async tests to `tests/test_plugin_base.py` reusing the existing `MinimalPlugin` (or equivalent RetailerPlugin subclass stub) in that file. For each test construct a `cfg = MagicMock()`, set `cfg.debug.test_mode` and `cfg.debug.monitor_only` to the case values, instantiate the plugin with `config=cfg`, create `click_fn = AsyncMock()`, await `plugin.place_order_guarded(click_fn)`, and assert the result plus `click_fn.assert_not_called()` / `click_fn.assert_awaited_once()`. Import `AsyncMock, MagicMock` from `unittest.mock` if not already imported. No `@pytest.mark.asyncio` decorator (asyncio_mode=auto). Tests named exactly as the three behaviors above (per VALIDATION.md test map lines 447-449).
  </action>
  <verify>
    <automated>pytest tests/test_plugin_base.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_plugin_base.py` contains the three named test functions.
    - Each asserts the correct return value AND the correct click_fn call state (not-called vs awaited-once).
    - `pytest tests/test_plugin_base.py -x -q` passes.
  </acceptance_criteria>
  <done>Three place_order_guarded unit tests pass green, covering both suppression gates and the allowed path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin auto_buy -> place-order DOM click | The final irreversible action; must be gated centrally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | place_order_guarded suppression bypass | mitigate | Method returns False without awaiting click_fn when either flag set; unit-tested both gates |
| T-18-05 | DoS | Exception from guard breaks buy-result flow | mitigate | Contract is return-False + INFO log, never raise; getattr-safe reads avoid AttributeError on partial configs |
| T-18-06 | Tampering | safe-default when config/debug missing | mitigate | test_mode defaults to True (suppressed) via getattr fallback — fail-safe toward NOT placing an order |
</threat_model>

<verification>
- `pytest tests/test_plugin_base.py -x -q` green.
- `python -c "from core.plugin_base import PLUGIN_API_VERSION; assert PLUGIN_API_VERSION==2; print('ok')"` confirms no version bump.
</verification>

<success_criteria>
- `place_order_guarded` is a concrete async method on `RetailerPlugin`.
- Suppressed (test_mode OR monitor_only) -> returns False, no click, INFO log, no raise.
- Allowed (both False) -> awaits click_fn, returns True.
- `PLUGIN_API_VERSION` unchanged at 2.
- `tests/test_plugin_base.py` passes.
</success_criteria>

<output>
Create `.planning/phases/18-safety-gate-config-foundation/18-02-SUMMARY.md` when done
</output>
