---
phase: 18-safety-gate-config-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/config_schema.py
  - tests/test_config_schema.py
autonomous: true
requirements: [BUY-01, BUY-02]
must_haves:
  truths:
    - "AppConfig().debug.monitor_only is False by default"
    - "AppConfig().checkout exists with all 6 numeric tuning fields at documented defaults"
    - "CheckoutConfig rejects a negative timeout / retry / backoff value at construction"
  artifacts:
    - path: "core/config_schema.py"
      provides: "DebugConfig.monitor_only field + CheckoutConfig model + AppConfig.checkout wiring"
      contains: "class CheckoutConfig"
    - path: "tests/test_config_schema.py"
      provides: "CheckoutConfig defaults + ge-bound rejection tests; monitor_only default False test"
  key_links:
    - from: "AppConfig"
      to: "CheckoutConfig"
      via: "checkout: CheckoutConfig = CheckoutConfig() declared class attribute"
      pattern: "checkout:\\s*CheckoutConfig\\s*=\\s*CheckoutConfig\\(\\)"
    - from: "DebugConfig"
      to: "monitor_only"
      via: "monitor_only: bool = False field"
      pattern: "monitor_only:\\s*bool\\s*=\\s*False"
---

<objective>
Add the config foundation for the safety gate: a `monitor_only` flag on `DebugConfig` (default `False`) and a new `CheckoutConfig` sub-model wired into `AppConfig`, providing the numeric tuning fields that downstream phases (19/21/22/24) will consume without further schema churn.

Purpose: Every other Phase 18 deliverable (orchestrator gate, place_order_guarded, CLI flag) reads `debug.monitor_only`; downstream phases read `checkout.*`. This plan ships the schema so those consumers exist. Satisfies success criterion 4 (CheckoutConfig present) and the config half of BUY-01.
Output: Updated `core/config_schema.py`, extended `tests/test_config_schema.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-safety-gate-config-foundation/18-CONTEXT.md
@.planning/phases/18-safety-gate-config-foundation/18-RESEARCH.md
@.planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md

<interfaces>
<!-- Existing schema patterns the executor builds against. Extracted from core/config_schema.py. -->

DebugConfig today (lines 61-63):
    class DebugConfig(BaseModel):
        logging_level: int = 5
        test_mode: bool = True

Field(ge=) numeric-bound idiom to copy (WalmartPlatformConfig lines 89-92):
    min_delay: float = Field(default=8.0, ge=0.0)
    max_delay: float = Field(default=15.0, ge=0.0)

AppConfig default-instance field declaration pattern (sub-models declared as `field: Model = Model()`):
    proxy: ProxyConfig = ProxyConfig()
    captcha: CaptchaConfig = CaptchaConfig()

`Field`, `BaseModel`, `BaseSettings` are already imported at the top of core/config_schema.py. No new imports needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add monitor_only to DebugConfig and define CheckoutConfig</name>
  <files>core/config_schema.py</files>
  <read_first>
    - core/config_schema.py (the file being modified — read DebugConfig at lines 61-63, WalmartPlatformConfig ge-bound idiom at lines 86-92, and CaptchaConfig + AppConfig field declarations)
    - .planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md (analog: "DebugConfig field addition" and "CheckoutConfig new model" sections, lines 28-79)
  </read_first>
  <behavior>
    - Test: `AppConfig().debug.monitor_only is False` (default False — per CONTEXT.md, NOT True; STATE.md line 120 is stale).
    - Test: `CheckoutConfig()` yields item_timeout_secs=120, step_timeout_secs=30, max_cart_retries=3, backoff_base=2.0, backoff_jitter=0.5, alert_on_errors=3.
    - Test: `CheckoutConfig(item_timeout_secs=0)` raises pydantic ValidationError (ge=1 lower bound).
    - Test: `CheckoutConfig(max_cart_retries=-1)` raises pydantic ValidationError (ge=0 lower bound).
  </behavior>
  <action>
    Add one field to `DebugConfig`: `monitor_only: bool = False` immediately after `test_mode: bool = True`. The default MUST be `False` per the locked CONTEXT.md decision (RESEARCH Pitfall 6 / PATTERNS P6 — STATE.md line ~120 says True but is stale; CONTEXT.md wins).

    Define a new `class CheckoutConfig(BaseModel)` placed after the `CaptchaConfig` class definition and before `AppConfig`. Six fields, all numeric scalars using the `Field(default=..., ge=...)` idiom (copy the WalmartPlatformConfig pattern; do NOT add a `@field_validator` — scalar `ge` bounds are sufficient): `item_timeout_secs: int = Field(default=120, ge=1)`, `step_timeout_secs: int = Field(default=30, ge=1)`, `max_cart_retries: int = Field(default=3, ge=0)`, `backoff_base: float = Field(default=2.0, ge=0.0)`, `backoff_jitter: float = Field(default=0.5, ge=0.0)`, `alert_on_errors: int = Field(default=3, ge=0)`. Add a one-line docstring noting the fields are consumed by Phases 21/22/24. This is per D (CONTEXT Decisions: CheckoutConfig Schema) and BUY-01/BUY-02 foundation.
  </action>
  <verify>
    <automated>python -c "from core.config_schema import AppConfig, CheckoutConfig; c=AppConfig(); assert c.debug.monitor_only is False; ck=CheckoutConfig(); assert (ck.item_timeout_secs,ck.step_timeout_secs,ck.max_cart_retries,ck.backoff_base,ck.backoff_jitter,ck.alert_on_errors)==(120,30,3,2.0,0.5,3); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `core/config_schema.py` contains `monitor_only: bool = False` inside `DebugConfig`.
    - `core/config_schema.py` contains `class CheckoutConfig(BaseModel):` with all six fields at the exact defaults and `ge` bounds above.
    - No `@field_validator` added to `CheckoutConfig`.
    - The one-line command above prints `ok`.
  </acceptance_criteria>
  <done>monitor_only field and CheckoutConfig model exist with correct defaults and bounds; verify command prints ok.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CheckoutConfig into AppConfig and add schema tests</name>
  <files>core/config_schema.py, tests/test_config_schema.py</files>
  <read_first>
    - core/config_schema.py (AppConfig class — read the sub-model field declarations to find `captcha: CaptchaConfig = CaptchaConfig()` and insert after it)
    - tests/test_config_schema.py (the file being extended — read existing AppConfig/DebugConfig test patterns to match style: pydantic.ValidationError import, AppConfig() construction)
    - .planning/phases/18-safety-gate-config-foundation/18-PATTERNS.md (analog: "AppConfig wiring" section lines 83-99 — RESEARCH Pitfall 5: checkout MUST be a declared class attribute or extra="ignore" silently drops the YAML key)
  </read_first>
  <behavior>
    - Test: `AppConfig().checkout` is a `CheckoutConfig` instance with all defaults.
    - Test: a YAML/dict override of `checkout.item_timeout_secs` is honored (not silently dropped), proving the field is declared.
    - Test: `AppConfig().debug.monitor_only is False`.
    - Test: negative timeout and negative retry are rejected with ValidationError.
  </behavior>
  <action>
    In `AppConfig`, add `checkout: CheckoutConfig = CheckoutConfig()` as a declared class attribute placed immediately after the `captcha: CaptchaConfig = CaptchaConfig()` line (per CONTEXT decision: "placed after captcha"). It MUST be a declared attribute — `extra="ignore"` on AppConfig would otherwise silently drop the `checkout:` YAML key (RESEARCH Pitfall 5).

    Extend `tests/test_config_schema.py` with tests covering the behaviors above. Match the existing test style in that file (construct `AppConfig()` directly for defaults; use `pytest.raises(ValidationError)` for bound violations, importing `ValidationError` from `pydantic`). Cover: monitor_only default False; all six CheckoutConfig defaults; item_timeout_secs=0 rejected; step_timeout_secs=0 rejected; max_cart_retries=-1 rejected; backoff_base=-0.1 rejected; and a `checkout` override-not-dropped assertion (construct CheckoutConfig with a non-default value and assert it sticks).
  </action>
  <verify>
    <automated>pytest tests/test_config_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `AppConfig` declares `checkout: CheckoutConfig = CheckoutConfig()` directly after the captcha field.
    - `tests/test_config_schema.py` has new tests asserting: monitor_only default False, the six CheckoutConfig defaults, and ValidationError on at least one negative timeout AND one negative retry/backoff.
    - `pytest tests/test_config_schema.py -x -q` passes with no failures.
  </acceptance_criteria>
  <done>AppConfig.checkout wired; new schema tests pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml / env -> AppConfig | Operator-supplied numeric tuning values cross into the running bot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering/DoS | CheckoutConfig numeric fields | mitigate | `Field(ge=1)` on timeouts, `Field(ge=0)` on retries/backoff reject negative values at model instantiation; tested in Task 2 |
| T-18-02 | Tampering | DebugConfig.monitor_only default | mitigate | Default fixed to `False` per CONTEXT.md; test asserts `AppConfig().debug.monitor_only is False` |
| T-18-03 | Tampering | checkout YAML key silently dropped | mitigate | `checkout` declared as explicit AppConfig class attribute (extra="ignore" cannot drop a declared field) |
</threat_model>

<verification>
- `pytest tests/test_config_schema.py -x -q` green.
- `python -c "from core.config_schema import CheckoutConfig; print(CheckoutConfig())"` shows all six fields.
- No secrets introduced: CheckoutConfig holds only numeric tuning knobs.
</verification>

<success_criteria>
- `AppConfig().debug.monitor_only is False`.
- `AppConfig().checkout` is a `CheckoutConfig` with item_timeout_secs=120, step_timeout_secs=30, max_cart_retries=3, backoff_base=2.0, backoff_jitter=0.5, alert_on_errors=3.
- Negative numeric values are rejected by pydantic at construction.
- `tests/test_config_schema.py` passes.
</success_criteria>

<output>
Create `.planning/phases/18-safety-gate-config-foundation/18-01-SUMMARY.md` when done
</output>
