---
phase: 24-health-surface-server-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/health.py
  - tests/test_health.py
autonomous: true
requirements: [REL-07]
must_haves:
  truths:
    - "HealthRegistry records per-plugin status, last_heartbeat, consecutive_errors, items_checked, orders_confirmed"
    - "get_snapshot() returns a JSON-safe dict-of-dicts with private keys stripped"
    - "health_degraded arm/disarm dedup state is held in-memory per plugin"
  artifacts:
    - path: "core/health.py"
      provides: "HealthRegistry class with record/snapshot API"
      contains: "class HealthRegistry"
      min_lines: 60
    - path: "tests/test_health.py"
      provides: "HealthRegistry unit tests"
      contains: "from core.health import HealthRegistry"
  key_links:
    - from: "core/health.py"
      to: "time.monotonic"
      via: "heartbeat() timestamp"
      pattern: "time\\.monotonic"
---

<objective>
Create `core/health.py` with the `HealthRegistry` class: the read-side per-plugin
health store that the orchestrator (Plan 04) writes to and `BotService.get_status()`
(Plan 03) reads from. This is the foundational artifact for REL-07; every other
Phase 24 plan consumes it.

Purpose: Provide a lock-free, JSON-safe per-plugin counter store with in-memory
armed/disarmed dedup for the `health_degraded` alert.
Output: `core/health.py` (HealthRegistry) + `tests/test_health.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-health-surface-server-safety/24-RESEARCH.md
@.planning/phases/24-health-surface-server-safety/24-PATTERNS.md

<interfaces>
<!-- HealthRegistry is the contract consumed by Plans 03, 04, 05. -->
<!-- Mutators (written by orchestrator tasks on the bot event loop): -->
<!--   heartbeat(name), set_status(name, status), record_error(name), -->
<!--   reset_errors(name), inc_items_checked(name), inc_orders_confirmed(name), -->
<!--   is_degraded_armed(name)->bool, arm_degraded(name), disarm_degraded(name) -->
<!-- Reader (called from BotService.get_status, possibly on a different thread): -->
<!--   get_snapshot()->dict[str, dict]  (deep copy of inner dicts; private keys stripped) -->
<!-- status enum values: running | degraded | parked | relaunching | idle -->
<!-- per-plugin record keys: status, last_heartbeat, consecutive_errors, items_checked, orders_confirmed -->
<!-- private dedup key (stripped from snapshot): _degraded_armed -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HealthRegistry class in core/health.py</name>
  <files>core/health.py</files>
  <read_first>
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (section `core/health.py` — class skeleton, mutator names, snapshot pattern)
    - .planning/phases/24-health-surface-server-safety/24-RESEARCH.md (Pattern 1 — data model; Pitfall 2 — thread-safe snapshot via deep inner-dict copy)
    - core/orchestrator.py lines 111-125 (analog: failure-deque per-plugin local state being lifted into a class)
  </read_first>
  <behavior>
    - Fresh HealthRegistry().get_snapshot() returns {} (no plugins seen yet).
    - heartbeat("PluginA") creates the record and sets last_heartbeat > 0.0 (time.monotonic).
    - set_status("PluginA", "degraded") sets status; default status before any set_status is "idle".
    - record_error increments consecutive_errors by 1 each call; reset_errors sets it to 0.
    - inc_items_checked and inc_orders_confirmed each increment their counter by 1.
    - is_degraded_armed is False on a new record; arm_degraded sets it True; disarm_degraded sets it False.
    - get_snapshot() returns a record with exactly these keys: status, last_heartbeat, consecutive_errors, items_checked, orders_confirmed — and NO key beginning with "_" (the _degraded_armed flag is stripped).
    - get_snapshot() returns deep-copied inner dicts: mutating a returned inner dict does not affect the registry.
  </behavior>
  <action>
    Create core/health.py per the PATTERNS skeleton. Add a module docstring noting this is the read-side complement to the Phase 22 supervisor (REL-07). Import only the time stdlib module. Define class HealthRegistry with a private _plugins dict[str, dict] in __init__ and a module-level _IDLE_STATUS = "idle" class constant. Implement _ensure(name) to lazily create a record with keys status (=_IDLE_STATUS), last_heartbeat (0.0), consecutive_errors (0), items_checked (0), orders_confirmed (0), _degraded_armed (False). Implement the nine mutators named in the interfaces block (snake_case verbs per CLAUDE.md): heartbeat sets last_heartbeat = time.monotonic(); set_status writes the status string; record_error/reset_errors/inc_items_checked/inc_orders_confirmed mutate the integer counters; is_degraded_armed/arm_degraded/disarm_degraded read/write the private _degraded_armed flag. Implement get_snapshot() returning a NEW outer dict whose values are NEW inner dicts containing only keys that do not start with "_" (per Pitfall 2: copy each inner dict so a concurrent web-thread read cannot observe partial mutation, and strip private keys in the same comprehension). Keep every method under 30 lines (most are 2-3 lines). No try/except inside the registry — callers own exception handling.
  </action>
  <verify>
    <automated>pytest tests/test_health.py -x</automated>
  </verify>
  <acceptance_criteria>
    - core/health.py exists and `python -c "from core.health import HealthRegistry; HealthRegistry()"` succeeds.
    - All nine mutators plus get_snapshot exist and behave per the behavior block.
    - get_snapshot output contains no key starting with "_".
  </acceptance_criteria>
  <done>HealthRegistry class implemented; all behaviors verifiable; snapshot is JSON-safe (primitives only) and private-key-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HealthRegistry unit tests</name>
  <files>tests/test_health.py</files>
  <read_first>
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (section `tests/test_health.py` — test structure and analog)
    - tests/test_supervisor.py lines 1-91 (analog: helper factories, plain assertions; asyncio_mode=auto means no decorators)
  </read_first>
  <behavior>
    - test_registry_idle_before_use: get_snapshot() == {} on a fresh instance.
    - test_heartbeat_sets_last_heartbeat: after heartbeat("A"), snapshot["A"]["last_heartbeat"] > 0.0.
    - test_default_status_is_idle: after _ensure via any mutator, status == "idle" until set_status called.
    - test_record_and_reset_errors: record_error twice -> consecutive_errors == 2; reset_errors -> 0.
    - test_inc_counters: inc_items_checked / inc_orders_confirmed increment correctly.
    - test_arm_disarm_degraded: is_degraded_armed False -> arm_degraded -> True -> disarm_degraded -> False.
    - test_snapshot_strips_private_keys: "_degraded_armed" not in snapshot["A"].
    - test_snapshot_is_deep_copy: mutating a returned inner dict does not change the registry's next snapshot.
    - test_snapshot_is_json_serializable: json.dumps(reg.get_snapshot()) does not raise after populating one plugin.
  </behavior>
  <action>
    Create tests/test_health.py mirroring the plain-function style of tests/test_supervisor.py (no class, no async needed — HealthRegistry is synchronous and tested as the real class, not mocked). Import HealthRegistry from core.health and json. Write one test function per behavior listed above. For test_snapshot_is_json_serializable, populate a plugin via heartbeat + set_status + the counter mutators, then assert json.dumps(reg.get_snapshot()) returns a non-empty string. For test_snapshot_is_deep_copy, capture a snapshot, mutate snap["A"]["items_checked"] = 999, then assert reg.get_snapshot()["A"]["items_checked"] reflects only the registry's real value.
  </action>
  <verify>
    <automated>pytest tests/test_health.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_health.py runs green; at least the nine behaviors above are covered.
    - test_snapshot_is_json_serializable proves the snapshot is JSON-safe (REL-07 web /status flow-through).
  </acceptance_criteria>
  <done>All HealthRegistry behaviors covered by passing unit tests including JSON-serializability and deep-copy isolation.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_health.py -x` green.
- `python -c "import json; from core.health import HealthRegistry; r=HealthRegistry(); r.heartbeat('X'); print(json.dumps(r.get_snapshot()))"` prints valid JSON with no underscore-prefixed keys.
</verification>

<success_criteria>
HealthRegistry exists with the full record/snapshot API; snapshot is JSON-serializable and strips private dedup keys; armed/disarmed state is in-memory only.
</success_criteria>

<output>
Create `.planning/phases/24-health-surface-server-safety/24-01-SUMMARY.md` when done.
</output>
