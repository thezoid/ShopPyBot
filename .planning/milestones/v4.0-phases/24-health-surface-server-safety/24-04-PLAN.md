---
phase: 24-health-surface-server-safety
plan: 04
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - core/orchestrator.py
  - tests/test_supervisor.py
  - tests/test_orchestrator.py
autonomous: true
requirements: [REL-07]
must_haves:
  truths:
    - "async_main creates/receives one HealthRegistry and threads it to supervise + run_plugin"
    - "run_plugin bumps last_heartbeat and items_checked each poll cycle"
    - "supervise sets per-plugin status (running/parked/relaunching) and records errors"
    - "health_degraded fires ONCE when consecutive_errors first reaches alert_on_errors, re-arms on recovery"
    - "health_degraded is distinct from plugin_parked (consecutive-error signal vs windowed-budget escalation)"
    - "orders_confirmed increments on both confirmed and legacy-purchased paths"
  artifacts:
    - path: "core/orchestrator.py"
      provides: "health=None kwarg wiring + supervise/run_plugin/orders_confirmed hooks + health_degraded dispatch"
      contains: "health_degraded"
    - path: "tests/test_supervisor.py"
      provides: "health_degraded fire-once / dedup / re-arm tests + status-transition tests"
      contains: "health_degraded"
    - path: "tests/test_orchestrator.py"
      provides: "run_plugin heartbeat/items_checked + orders_confirmed increment tests"
  key_links:
    - from: "core/orchestrator.py supervise()"
      to: "dispatcher.notify(health_degraded event)"
      via: "_build_event(..., 'health_degraded') guarded by consecutive_errors>=alert_on_errors + not armed"
      pattern: "health_degraded"
    - from: "core/orchestrator.py async_main()"
      to: "supervise(... health=health_registry)"
      via: "create_task kwarg"
      pattern: "health=health_registry"
---

<objective>
Wire the `HealthRegistry` through `async_main` -> `supervise` -> `run_plugin` and
the confirmation path, using the established `=None` keyword-default convention so
no existing call site breaks. Add the per-plugin heartbeat, items_checked,
status-transition, orders_confirmed, and the deduped `health_degraded` alert
(REL-07).

Purpose: The orchestrator is the write side of the health surface; this plan makes
the registry that Plan 03 reads actually get populated, and fires the degradation
alert through the existing dispatcher.
Output: edited `core/orchestrator.py` + supervisor and orchestrator tests.

Depends on Plan 01 (HealthRegistry). Touches core/orchestrator.py only on the
source side — disjoint from service.py (Plan 03) and cli (Plan 05). All hook edits
are within one file and applied sequentially.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-health-surface-server-safety/24-RESEARCH.md
@.planning/phases/24-health-surface-server-safety/24-PATTERNS.md

@core/orchestrator.py

<interfaces>
<!-- From core/health.py (Plan 01) — mutators used here: -->
<!--   heartbeat(name), set_status(name, status), record_error(name), reset_errors(name), -->
<!--   inc_items_checked(name), inc_orders_confirmed(name), -->
<!--   is_degraded_armed(name)->bool, arm_degraded(name), disarm_degraded(name) -->
<!-- status enum values: running | degraded | parked | relaunching | idle -->
<!-- Existing orchestrator hook sites (verified): -->
<!--   async_main(cfg, cvv) at L629; supervise create_task at L667-670 (currently passes registry=registry) -->
<!--   supervise(plugin, write_queue, poll_interval, dispatcher, cfg, registry=None) at L90 -->
<!--     healthy-run reset at L117 (attempt=0); except Exception block L120-152; -->
<!--     failure_times deque L111; n_budget=alert_on_errors L105; park at L131-133; -->
<!--     browser-dead relaunch at L135-144; run_plugin call at L116 -->
<!--   run_plugin(plugin, write_queue, poll_interval, dispatcher=None, cfg=None) at L278; -->
<!--     items fetch L284; per-cycle top before for-loop L292; domain-filter pass L295-296 -->
<!--   _enqueue_buy_result(name, link, platform, order_id, write_queue, dispatcher) at L351; -->
<!--     _try_auto_buy(plugin, name, link, write_queue, dispatcher) at L395, calls _enqueue_buy_result at L420 -->
<!--   _build_event(name, link, plugin_name, action) at L155 — reuse with action="health_degraded" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread health=None through async_main/supervise/run_plugin; heartbeat, items_checked, status transitions, orders_confirmed; health_degraded dispatch with re-arm dedup</name>
  <files>core/orchestrator.py</files>
  <read_first>
    - core/orchestrator.py lines 90-152 (supervise loop: healthy-run reset L117, except block L120-152, park L131-133, relaunch L135-144)
    - core/orchestrator.py lines 278-310 (run_plugin loop: items fetch L284, per-cycle top L292, domain filter L295-296)
    - core/orchestrator.py lines 351-420 (_enqueue_buy_result + _try_auto_buy; orders_confirmed hook goes in _try_auto_buy after _enqueue_buy_result returns)
    - core/orchestrator.py lines 629-670 (async_main signature + supervise create_task wiring)
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (section `core/orchestrator.py` — every exact hook-edit snippet)
    - .planning/phases/24-health-surface-server-safety/24-RESEARCH.md (Pattern 2 + Pitfall 1 — threshold strategy; Pattern 4 — orders_confirmed site)
  </read_first>
  <behavior>
    - async_main accepts health_registry=None (keyword, default) and passes health=health_registry into each supervise create_task.
    - supervise accepts health=None (after registry=None); on a healthy run_plugin return it sets status "running", reset_errors, disarm_degraded for the plugin.
    - supervise threads health=health into its run_plugin call.
    - On each crash: record_error; when consecutive_errors >= alert_on_errors AND not already armed AND dispatcher present, dispatch _build_event("", "", plugin_name, "health_degraded"), then arm_degraded — fires exactly once per degradation episode.
    - health_degraded is the consecutive-error signal; plugin_parked remains the windowed-budget escalation at len(failure_times) >= n_budget. The two are distinct checks and fire on distinct conditions (see threshold note in action).
    - On park: set_status "parked". On browser-dead relaunch: set_status "relaunching".
    - run_plugin accepts health=None; at each poll-cycle top (after items fetch, before the for-loop) it calls heartbeat(name) and set_status(name, "running"); for each item passing the domain filter it calls inc_items_checked(name).
    - orders_confirmed: in _try_auto_buy, after _enqueue_buy_result returns, inc_orders_confirmed(platform) is called for BOTH the confirmed (order_id present) and legacy-purchased paths (a purchase happened either way — research rec A3). Thread health through _check_and_buy -> _try_auto_buy the same way dispatcher is threaded; default None.
    - All existing call sites that omit health continue to work (default None makes every hook a no-op).
  </behavior>
  <action>
    Edit core/orchestrator.py applying the PATTERNS hook snippets. THRESHOLD RESOLUTION (research Open Q1, per CONTEXT/key_constraints): health_degraded uses HealthRegistry.consecutive_errors as a dedicated monotonic counter — call health.record_error(name) on each crash and health.reset_errors(name)+disarm_degraded(name) on each healthy run. Fire health_degraded when consecutive_errors >= alert_on_errors (n_budget) AND not is_degraded_armed AND dispatcher is not None, then arm_degraded — this is the consecutive-error degradation signal. Keep the EXISTING windowed failure_times deque + park-at-n_budget logic untouched: plugin_parked stays the windowed-budget escalation. Document in a short comment that degraded = consecutive-error signal (armed-once, re-arms when a healthy run resets the counter and disarms) and parked = windowed-budget escalation; the dedup guarantees one health_degraded per degradation episode (no per-cycle spam). Add health=None to supervise (after registry=None), run_plugin (after cfg=None), async_main (after cvv as keyword default), _check_and_buy (after dispatcher=None), and _try_auto_buy (after dispatcher). In async_main pass health=health_registry into the supervise create_task (L667-670). In supervise thread health=health into the run_plugin call (L116) and add the healthy-run hook (status running + reset_errors + disarm), the except-block hook (record_error + the armed health_degraded dispatch), the park hook (set_status parked before _park_plugin), and the relaunch hook (set_status relaunching inside the _is_browser_dead_exc branch) — all guarded by `if health is not None`. In run_plugin add the per-cycle heartbeat+status hook and the per-item inc_items_checked hook. In _check_and_buy pass health into the _try_auto_buy call; in _try_auto_buy call health.inc_orders_confirmed(platform) after _enqueue_buy_result returns (covers both confirmed and legacy paths). Resolve plugin_name = plugin.__class__.__name__ once per hook. Keep each hook block tight; do not refactor unrelated code.
  </action>
  <verify>
    <automated>pytest tests/test_supervisor.py tests/test_orchestrator.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All five health=None params added with default None; no existing call site broken (run the full supervisor/orchestrator suites).
    - health_degraded dispatched via the existing dispatcher using action="health_degraded" (no NotificationEvent/dispatcher dataclass change).
    - orders_confirmed increments on both confirmed and legacy-purchased paths.
    - status transitions (running/parked/relaunching) wired at the verified hook sites.
  </acceptance_criteria>
  <done>HealthRegistry fully wired through the orchestrator; heartbeat/items_checked/status/orders_confirmed populate; health_degraded fires once per episode and re-arms on recovery, distinct from plugin_parked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: health_degraded + status-transition + counter tests</name>
  <files>tests/test_supervisor.py, tests/test_orchestrator.py</files>
  <read_first>
    - tests/test_supervisor.py lines 1-91 (helper factories _make_cfg/_make_dispatcher/_make_plugin) and ~L196-214 (test_failure_budget_parks_plugin — dispatcher.notify actions assertion pattern)
    - tests/test_orchestrator.py (existing run_plugin / _try_auto_buy test patterns and fixtures)
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (section `tests/test_supervisor.py (EDIT)` — fires_once/dedup/rearms test skeletons)
  </read_first>
  <behavior>
    - test_health_degraded_fires_once: a plugin crashing up to alert_on_errors yields exactly one "health_degraded" action in dispatcher.notify call list (armed after first crossing).
    - test_health_degraded_dedup: continued crashes after arming do NOT add more "health_degraded" notifications (count stays 1).
    - test_health_degraded_rearms: crash -> recover (healthy run resets errors + disarms) -> crash again => "health_degraded" fires twice total.
    - test_health_degraded_distinct_from_parked: when the plugin parks, the dispatcher sees both a health_degraded (from the degradation crossing) and a plugin_parked action, asserting they are separate actions (distinct signals).
    - test_run_plugin_heartbeat_and_items_checked: after one poll cycle over a matching item, the registry snapshot shows last_heartbeat > 0 and items_checked >= 1 for the plugin.
    - test_orders_confirmed_increments_on_confirmed_and_legacy: _try_auto_buy with a confirmed order_id increments orders_confirmed; a legacy (order_id None) successful buy also increments it.
  </behavior>
  <action>
    Add the health_degraded tests to tests/test_supervisor.py reusing its _make_cfg(alert_on_errors=...) / _make_dispatcher / _make_plugin helpers and a real HealthRegistry from core.health. Drive crashes by making run_plugin (or the plugin call it wraps) raise the configured number of times then block/cancel, mirroring the existing test_failure_budget_parks_plugin control flow; assert via `actions = [c.args[0].action for c in dispatcher.notify.call_args_list]` and actions.count("health_degraded"). For re-arm, interleave a healthy run_plugin return between crash groups. For the distinct-from-parked test, assert both "health_degraded" and "plugin_parked" appear in actions. Add the run_plugin heartbeat/items_checked test and the orders_confirmed test to tests/test_orchestrator.py: construct a real HealthRegistry, run one poll cycle (or call _try_auto_buy) with health=registry, then assert registry.get_snapshot() reflects the expected counter values. Use existing fixtures; no watch-mode flags.
  </action>
  <verify>
    <automated>pytest tests/test_supervisor.py tests/test_orchestrator.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All six behaviors covered and green.
    - fire-once, dedup, and re-arm are each independently asserted (REL-07 success criterion 2).
    - distinct-from-parked test proves health_degraded and plugin_parked are separate actions.
  </acceptance_criteria>
  <done>health_degraded dedup/re-arm, status transitions, heartbeat/items_checked, and orders_confirmed all verified by automated tests.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_supervisor.py tests/test_orchestrator.py -x` green.
- `pytest` full suite green (no existing call site broken by the new health=None kwargs).
</verification>

<success_criteria>
The orchestrator populates the HealthRegistry (heartbeat, items_checked, status, orders_confirmed) and dispatches health_degraded once per degradation episode through the existing fan-out, distinct from plugin_parked (REL-07 success criterion 2).
</success_criteria>

<output>
Create `.planning/phases/24-health-surface-server-safety/24-04-SUMMARY.md` when done.
</output>
