---
phase: 24-health-surface-server-safety
plan: 05
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - core/cli/status.py
  - core/cli/__init__.py
  - tests/test_cli_status.py
autonomous: true
requirements: [REL-07]
must_haves:
  truths:
    - "shoppybot status prints a per-plugin health table from get_status()"
    - "shoppybot status --json emits raw JSON matching the get_status() shape"
    - "the status command makes no network call (in-process state only)"
  artifacts:
    - path: "core/cli/status.py"
      provides: "handle_status + _format_status_table"
      contains: "def handle_status"
    - path: "core/cli/__init__.py"
      provides: "status subparser with --json registered via set_defaults(func=handle_status)"
      contains: "handle_status"
    - path: "tests/test_cli_status.py"
      provides: "status table + --json CLI tests (no network)"
  key_links:
    - from: "core/cli/status.py handle_status"
      to: "svc.get_status()"
      via: "read-only in-memory call"
      pattern: "svc\\.get_status\\(\\)"
    - from: "core/cli/__init__.py"
      to: "core.cli.status.handle_status"
      via: "import + status_p.set_defaults(func=handle_status)"
      pattern: "set_defaults\\(func=handle_status\\)"
---

<objective>
Add the `shoppybot status` CLI subcommand: a read-only per-plugin health table
(plus a `--json` raw-output flag) sourced from `BotService.get_status()`, with no
network call. Mirrors the `shoppybot plugins list` precedent exactly (REL-07).

Purpose: Give operators a local CLI surface for the health data; complements the
web `/status` endpoint (which serves the live running-process state).
Output: new `core/cli/status.py`, edited `core/cli/__init__.py`, `tests/test_cli_status.py`.

Depends on Plan 01 (HealthRegistry) only transitively via get_status()'s shape;
functionally depends on Plan 03's expanded get_status() at runtime but the CLI
reads through whatever get_status() returns, so the file edits are disjoint and the
handler tests use a mocked svc. Touches cli files only — disjoint from service.py
(Plan 03) and orchestrator.py (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-health-surface-server-safety/24-RESEARCH.md
@.planning/phases/24-health-surface-server-safety/24-PATTERNS.md

@core/cli/plugins.py

<interfaces>
<!-- Precedent: core/cli/plugins.py handle_plugins_list(args, svc) -> int; _format_plugins_table(rows). -->
<!-- core/cli/__init__.py imports handle_plugins_list at L15; build_parser() registers subparsers; -->
<!--   main() in core/service.py dispatches via args.func(args, BotService()). -->
<!-- get_status() shape (from Plan 03): -->
<!--   {"running": bool, "uptime_secs": float, -->
<!--    "plugins": {name: {"status": str, "last_heartbeat": float, -->
<!--      "consecutive_errors": int, "items_checked": int, "orders_confirmed": int}}} -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core/cli/status.py handle_status + _format_status_table; register status subparser</name>
  <files>core/cli/status.py, core/cli/__init__.py</files>
  <read_first>
    - core/cli/plugins.py (full — exact structural template: imports, _format_plugins_table aligned-column logic, handle_plugins_list)
    - core/cli/__init__.py lines 10-16 (handler imports) and the plugins subparser registration block (analog: add_parser + --json + set_defaults)
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (sections `core/cli/status.py` and `core/cli/__init__.py` — exact formatter, handler, and subparser snippets)
    - .planning/phases/24-health-surface-server-safety/24-RESEARCH.md (Pattern 8 — handler + in-process-state limitation note for help text)
  </read_first>
  <behavior>
    - handle_status(args, svc) calls svc.get_status(); with args.json True it prints json.dumps(status, indent=2); else prints _format_status_table(status); always returns 0.
    - _format_status_table renders a header line showing running and uptime, then a per-plugin aligned table with columns Name, Status, Last Heartbeat, Errors, Checked, Orders; when plugins is empty it prints the header plus a "No plugin data yet." line.
    - build_parser() registers a leaf `status` subcommand with a --json store_true flag defaulting False and set_defaults(func=handle_status).
    - The handler performs no network/IO beyond svc.get_status() (in-memory only).
  </behavior>
  <action>
    Create core/cli/status.py modeled exactly on core/cli/plugins.py. Module docstring: handle_status reads svc.get_status() only (MOD-02, no network); note in the docstring that this shows in-process BotService state, so a separate `shoppybot status` invocation while the bot runs in another process shows running=False (use the web /status endpoint for the live running process — research Pattern 8 limitation). Import json and BotService. Implement _format_status_table(status: dict) -> str using the same left-justified column-width alignment approach as _format_plugins_table: build a header string `running={running}  uptime={uptime:.1f}s`; if plugins is empty return header + "No plugin data yet."; otherwise build data rows from each plugin record (status, last_heartbeat formatted to 1 decimal, consecutive_errors, items_checked, orders_confirmed as strings) under headers ("Name","Status","Last Heartbeat","Errors","Checked","Orders"), compute per-column widths, and join. Implement handle_status(args, svc) -> int returning 0, branching on getattr(args, "json", False). Then edit core/cli/__init__.py: add `from core.cli.status import handle_status` next to the other handler imports (after L15), and register the status subparser in build_parser() mirroring the plugins block — add_parser("status", help="Show per-plugin health status (in-process state; use /status for the live running process)."), add_argument("--json", action="store_true", default=False, help="Emit output as JSON instead of a text table."), set_defaults(func=handle_status). No _require_subcommand (status is a leaf). Keep functions under 30 lines; no try/except (all reads in-memory).
  </action>
  <verify>
    <automated>pytest tests/test_cli_status.py -x</automated>
  </verify>
  <acceptance_criteria>
    - core/cli/status.py exists with handle_status and _format_status_table.
    - core/cli/__init__.py imports and registers the status subparser with --json.
    - The handler reads only svc.get_status() (no network).
  </acceptance_criteria>
  <done>shoppybot status subcommand implemented (table + --json) mirroring plugins list; no network call; in-process-state limitation documented in help/docstring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI status tests in tests/test_cli_status.py</name>
  <files>tests/test_cli_status.py</files>
  <read_first>
    - tests/test_cli_plugins.py lines 1-114 (exact analog: main dispatch via patch("core.service.BotService"), SystemExit code assertion, capsys output assertions, tmp_data_dir fixture)
    - .planning/phases/24-health-surface-server-safety/24-PATTERNS.md (section `tests/test_cli_status.py (NEW)` — _STATUS_PAYLOAD + table/json test skeletons)
  </read_first>
  <behavior>
    - test_status_table: main(["status"]) with a mocked BotService.get_status() returning a populated payload exits 0 and prints the plugin name and header in the table.
    - test_status_json: main(["status","--json"]) prints valid JSON; json.loads(out)["running"] matches the payload and the plugin record is present.
    - test_status_empty: with an empty plugins dict, the table prints the "No plugin data yet." line and exits 0.
    - test_status_no_network: the handler path is exercised without any network/registry import (mocked svc only) — asserts get_status was called and nothing else.
  </behavior>
  <action>
    Create tests/test_cli_status.py mirroring tests/test_cli_plugins.py. Define a module-level _STATUS_PAYLOAD dict matching the get_status() shape (running True, uptime_secs 42.0, one plugin record with the five keys). In each test, patch core.service.BotService to return a MagicMock whose get_status.return_value is the payload (or {"running":False,"uptime_secs":0.0,"plugins":{}} for the empty case), call main(["status"]) / main(["status","--json"]) inside pytest.raises(SystemExit), assert exc.value.code == 0, and assert on capsys output (plugin name present for the table; json.loads succeeds and matches for --json; "No plugin data yet." for the empty case). For test_status_no_network, assert mock_svc.get_status was called and assert the handler did not import core.registry (consistent with the plugins-list no-network precedent — assert via the mocked-svc-only path). Reuse the tmp_data_dir fixture pattern from test_cli_plugins.py.
  </action>
  <verify>
    <automated>pytest tests/test_cli_status.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All four behaviors covered and green.
    - --json output is asserted to be valid JSON matching the get_status() shape (REL-07 CLI surface).
    - No-network behavior asserted (mocked svc only).
  </acceptance_criteria>
  <done>shoppybot status CLI verified: table render, --json raw output, empty-state, and no-network behavior all pass.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_cli_status.py -x` green.
- `python -c "from core.cli import build_parser; p=build_parser(); print('status' in [a.dest for a in p._subparsers._actions if hasattr(a,'choices') and a.choices and 'status' in a.choices])"` confirms the status subcommand is registered.
</verification>

<success_criteria>
`shoppybot status` displays the per-plugin health table and `--json` emits the raw structured dict, with no network call (REL-07 success criterion 1, CLI surface).
</success_criteria>

<output>
Create `.planning/phases/24-health-surface-server-safety/24-05-SUMMARY.md` when done.
</output>
