---
phase: 20-checkout-profile-form-fill
plan: 04
type: execute
wave: 2
depends_on: ["20-01", "20-03"]
files_modified:
  - core/plugin_base.py
  - plugins/shopbot_plugin_bestbuy.py
  - plugins/shopbot_plugin_amazon.py
  - tests/test_checkout_form_fill.py
  - tests/test_no_cvv_in_logs.py
autonomous: true
requirements: [BUY-07]
must_haves:
  truths:
    - "BestBuy and Amazon load self._checkout_profile at setup() time (None when unconfigured)"
    - "BestBuy fills the 7 required shipping fields from the profile before place_order_guarded"
    - "A missing required shipping selector logs a WARNING naming the selector and returns False without submitting"
    - "CHECKOUT_ADDRESS_LINE2 is filled only when present and its selector exists; absence is not an error"
    - "Amazon fills the CVV field from self._cvv when the field exists; an absent CVV field is skipped (not an abort)"
    - "No _cvv value appears in any writeLog() argument on checkout code paths (AST-proven)"
  artifacts:
    - path: "core/plugin_base.py"
      provides: "self._checkout_profile = None default in RetailerPlugin.__init__"
      contains: "_checkout_profile"
    - path: "plugins/shopbot_plugin_bestbuy.py"
      provides: "_fill_field helper + shipping form-fill before CVV/place_order + setup() profile load"
      contains: "_fill_field"
    - path: "plugins/shopbot_plugin_amazon.py"
      provides: "CVV field fill (skip-if-absent) + setup() profile load + optional address fill"
      contains: "_checkout_profile"
    - path: "tests/test_checkout_form_fill.py"
      provides: "fill-field, missing-selector WARN+False, profile-None guard, CVV skip-if-absent"
    - path: "tests/test_no_cvv_in_logs.py"
      provides: "AST scan: no _cvv in any writeLog() arg on checkout paths"
  key_links:
    - from: "plugins/shopbot_plugin_bestbuy.py setup()"
      to: "core.checkout_profile.load_checkout_profile"
      via: "self._checkout_profile assignment"
      pattern: "load_checkout_profile"
    - from: "plugins/shopbot_plugin_bestbuy.py auto_buy"
      to: "tab.select shipping selectors"
      via: "_fill_field per required field before place_order_guarded"
      pattern: "_fill_field"
    - from: "plugins/shopbot_plugin_amazon.py auto_buy"
      to: "self._cvv"
      via: "cvv_field.send_keys when field present"
      pattern: "cvv_field"
---

<objective>
Implement the form-fill behavior for BestBuy and Amazon: load `self._checkout_profile` at setup() time, add a `_fill_field` helper, fill the shipping address fields from the profile BEFORE `place_order_guarded`, and enter the CVV from the runtime `self._cvv`. Enforce the safety rules: a missing REQUIRED shipping selector logs a WARNING (selector name) and returns False without submitting; the optional ADDRESS_LINE2 and an absent CVV field are skipped gracefully. Add the dedicated CI AST assertion proving `_cvv` never appears in any `writeLog()` argument on checkout paths (BUY-07 criterion 3).

Purpose: Completes BUY-07 — the bot fills the configured shipping profile and enters the runtime CVV, with DOM-drift safety and the no-CVV-in-logs security guard.
Output: edits to `core/plugin_base.py` and both plugins, plus `tests/test_checkout_form_fill.py` and `tests/test_no_cvv_in_logs.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-checkout-profile-form-fill/20-CONTEXT.md
@.planning/phases/20-checkout-profile-form-fill/20-RESEARCH.md
@.planning/phases/20-checkout-profile-form-fill/20-PATTERNS.md
@.planning/phases/20-checkout-profile-form-fill/20-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly; no exploration needed. -->

From core/checkout_profile.py (Plan 01): CheckoutProfile has fields first_name, last_name, address_line1, address_line2 (Optional), city, state, zip_code, country, phone. load_checkout_profile() -> CheckoutProfile | None (None when unconfigured; logs WARNING with key NAMES only).

From core/plugin_base.py (RetailerPlugin.__init__ lines 39-43): sets self.config and self.driver = None. setup() (lines 58-60) is a no-op `...`; both BestBuy and Amazon override it. Add self._checkout_profile = None here as the additive safe default (PLUGIN_API_VERSION stays 2; no ABC bump).

From plugins/shopbot_plugin_bestbuy.py: __init__ lines 42-49 (self._cvv = None at 46). setup() lines 158-179 ends after the proxy-auth block (line 179). auto_buy lines 249-319: await self.login() at line 303; the existing CVV block is lines 305-309 (cvv_field = await tab.select("#credit-card-cvv", timeout=10); if cvv_field and self._cvv: await cvv_field.send_keys(self._cvv)); place_order select + guarded click at lines 311-316; self._last_tab = tab at line 315 before place_order_guarded(place_order.click). The form-fill insertion point is AFTER line 303 (login) and BEFORE line 305 (CVV).

From plugins/shopbot_plugin_amazon.py: __init__ lines 72-81 (self._cvv = None added by Plan 03). setup() lines 193-214 ends after the proxy-auth block (line 214). auto_buy lines 357-425: place_order select at lines 416-419; self._last_tab = tab at line 421; place_order_guarded(place_order.click) at line 422. CVV fill insertion point is AFTER line 419 (place_order found) and BEFORE line 421.

From plugins/shopbot_plugin_bestbuy.py (existing CVV pattern lines 305-309) — preserve verbatim semantics: never log self._cvv.

nodriver fill API (verified, 0.50.3): el = await tab.select(selector, timeout=N) returns Element | None; await el.clear_input() clears; await el.send_keys(text) types char-by-char. Use clear_input()+send_keys() (not set_value) for SPA inputs.

BestBuy shipping selectors (MEDIUM/LOW confidence — UAT debt, do not block): first-name #first-name, last-name #last-name, address1 #street, address2 #street2 (optional), city #city, state #state, zip #zip, phone #phone. CVV #credit-card-cvv (HIGH, existing). place order .button--place-order (HIGH, existing).
Amazon CVV selector: #addCreditCardCvvInput (MEDIUM — UAT debt).

CONTEXT decision: country (#country) is in the profile but BestBuy's selector set above does not include a country field; fill the 7 required text fields listed (first/last/street/city/state/zip/phone). Country is stored in the profile for completeness and may be needed for Amazon/future; do not invent a BestBuy country selector — note as UAT debt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _checkout_profile default + BestBuy form-fill + Amazon CVV fill + setup loads</name>
  <files>core/plugin_base.py, plugins/shopbot_plugin_bestbuy.py, plugins/shopbot_plugin_amazon.py</files>
  <read_first>
    - core/plugin_base.py (RetailerPlugin.__init__ lines 39-43; setup() no-op lines 58-60)
    - plugins/shopbot_plugin_bestbuy.py (setup() lines 158-179; auto_buy lines 249-319, esp. login line 303 and CVV block 305-309 and place_order 311-316)
    - plugins/shopbot_plugin_amazon.py (setup() lines 193-214; auto_buy lines 357-425, esp. place_order found 416-419 and place_order_guarded 422)
    - core/checkout_profile.py (CheckoutProfile fields; load_checkout_profile)
    - .planning/phases/20-checkout-profile-form-fill/20-PATTERNS.md (sections "core/plugin_base.py - add _checkout_profile load", "shopbot_plugin_bestbuy.py - add shipping form-fill", "shopbot_plugin_amazon.py - add _cvv init + CVV field fill", and "Shared Patterns")
    - .planning/phases/20-checkout-profile-form-fill/20-RESEARCH.md (sections "Form-Fill Placement in auto_buy", "nodriver Form-Fill API", "Common Pitfalls 3/4/6/7")
  </read_first>
  <behavior>
    - A new BestBuy or Amazon plugin has self._checkout_profile defaulting to None (no AttributeError)
    - After setup(), self._checkout_profile equals load_checkout_profile() (None when unconfigured)
    - In BestBuy auto_buy with self._checkout_profile is None: a WARNING is logged and auto_buy returns False BEFORE any place_order_guarded call
    - In BestBuy auto_buy with a complete profile and all required selectors present: each required field is filled via clear_input()+send_keys() and flow proceeds to the existing CVV block then place_order_guarded
    - In BestBuy auto_buy when any REQUIRED shipping selector returns None: a WARNING naming that selector is logged and auto_buy returns False (no place_order_guarded, no partial submit)
    - CHECKOUT_ADDRESS_LINE2 is filled only when profile.address_line2 is truthy AND its selector exists; an absent #street2 with a present line2 is skipped silently (optional field, not an error)
    - In Amazon auto_buy: when self._cvv is set, the CVV field (#addCreditCardCvvInput) is selected with a short timeout; if present, self._cvv is sent; if absent, the step is skipped and auto_buy continues to place_order_guarded (absent CVV field is a valid state, Pitfall 4)
    - No field value and no self._cvv is ever passed to writeLog; only selector NAMES are logged
  </behavior>
  <action>
    In core/plugin_base.py RetailerPlugin.__init__, add `self._checkout_profile = None` after self.driver assignment (additive default; comment that it is set by setup() via load_checkout_profile). Do NOT change the abstract setup() no-op and do NOT bump PLUGIN_API_VERSION.

    In plugins/shopbot_plugin_bestbuy.py: at the END of setup() (after the proxy-auth block at line 179), import load_checkout_profile (call-time import from core.checkout_profile) and assign self._checkout_profile = load_checkout_profile(). Add an async instance method _fill_field(self, tab, selector, value) -> bool that calls el = await tab.select(selector, timeout=10); if el is None, writeLog a WARNING naming the selector (use the selector repr; the class name prefix like "[BestBuyPlugin]") and return False; else await el.clear_input(); await el.send_keys(value); return True. (For SPA onChange robustness per Pitfall 7, after send_keys optionally dispatch an input event via el.apply with a bubbling Event; gate this behind UAT — implement the dispatch but keep it best-effort inside a try/except that never raises into the flow.) In auto_buy, insert the shipping form-fill AFTER await self.login() (line 303) and BEFORE the existing CVV block (line 305): first guard `if self._checkout_profile is None:` → writeLog WARNING that the checkout profile is not configured (no value) and return False (Pitfall 6); bind profile = self._checkout_profile; iterate the required (selector, value) pairs [(#first-name, first_name), (#last-name, last_name), (#street, address_line1), (#city, city), (#state, state), (#zip, zip_code), (#phone, phone)] calling self._fill_field; if any returns False, return False immediately (WARNING already logged); then if profile.address_line2: el = await tab.select("#street2", timeout=5); if el is not None: await el.clear_input(); await el.send_keys(profile.address_line2) (absent selector for optional field is not an error). Leave the existing CVV block (305-309), place_order select, self._last_tab assignment, and place_order_guarded call unchanged so form-fill precedes them.

    In plugins/shopbot_plugin_amazon.py: at the END of setup() (after the proxy-auth block at line 214), import load_checkout_profile (call-time) and assign self._checkout_profile = load_checkout_profile(). In auto_buy, insert the CVV fill AFTER the place_order found check (line 419) and BEFORE self._last_tab = tab (line 421): if self._cvv: cvv_field = await tab.select("#addCreditCardCvvInput", timeout=5); if cvv_field: await cvv_field.send_keys(self._cvv) (comment: SEC-02, CVV from self._cvv, never logged); if cvv_field is None, skip silently (do NOT return False — absent CVV field is valid, Pitfall 4). Do not add Amazon shipping address form-fill in this phase (Open Question 1: Amazon typically uses the saved default address; address form-fill is UAT-gated and out of scope for this plan — note in SUMMARY). Never pass self._cvv or any profile value to writeLog.
  </action>
  <verify>
    <automated>python -c "from plugins.shopbot_plugin_bestbuy import BestBuyPlugin; from plugins.shopbot_plugin_amazon import AmazonPlugin; b=BestBuyPlugin(config=None); a=AmazonPlugin(config=None); assert b._checkout_profile is None and a._checkout_profile is None and hasattr(b,'_fill_field')"</automated>
  </verify>
  <acceptance_criteria>
    - RetailerPlugin.__init__ sets self._checkout_profile = None; PLUGIN_API_VERSION unchanged
    - Both plugins assign self._checkout_profile = load_checkout_profile() at end of setup()
    - BestBuy auto_buy fills the 7 required fields via _fill_field before the existing CVV/place_order blocks; profile-None guard returns False before place_order_guarded
    - A None required selector → WARNING naming the selector + return False (no place_order_guarded)
    - ADDRESS_LINE2 filled only when present and selector exists; absent optional selector is not an error
    - Amazon CVV fill uses #addCreditCardCvvInput with skip-if-absent; never returns False on absent CVV field
    - No self._cvv or profile value passed to writeLog (grep clean)
  </acceptance_criteria>
  <done>Both plugins load the profile at setup, BestBuy fills shipping before place_order_guarded with missing-selector abort and optional-field skip, Amazon enters CVV with skip-if-absent, and no value is logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Form-fill behavior tests (fill, missing-selector WARN+False, profile-None, CVV skip-if-absent)</name>
  <files>tests/test_checkout_form_fill.py</files>
  <read_first>
    - tests/test_bestbuy_plugin.py (_make_bestbuy_plugin no-browser construction; MagicMock tab/element patterns)
    - tests/conftest.py (fake_browser fixture: fake_tab.select AsyncMock; fake_element has click/send_keys; NOTE: add clear_input AsyncMock on the element in your test, the fixture does not set it)
    - plugins/shopbot_plugin_bestbuy.py (_fill_field + auto_buy form-fill just added)
    - plugins/shopbot_plugin_amazon.py (CVV fill just added)
    - core/checkout_profile.py (CheckoutProfile construction for a complete fixture profile)
  </read_first>
  <behavior>
    - test_bestbuy_fill_field_success: _fill_field with a tab whose select returns an element (clear_input + send_keys AsyncMocks) returns True and calls clear_input then send_keys(value)
    - test_bestbuy_fill_field_missing_selector_warns_false: _fill_field with tab.select returning None returns False and logs a WARNING containing the selector text
    - test_autobuy_returns_false_no_profile: BestBuy auto_buy with self._checkout_profile None (and monitor_only False, test_mode handling per existing flow) returns False without calling place_order_guarded
    - test_missing_required_selector_aborts: a profile present but one required field selector returns None → auto_buy returns False and place_order_guarded is not awaited
    - test_address_line2_optional_skip: profile with address_line2 None → no #street2 fill attempted; profile with address_line2 set but selector None → no error raised
    - test_amazon_cvv_skip_when_field_absent: Amazon auto_buy reaching the CVV step with self._cvv set but the CVV select returning None continues (does not return False at the CVV step)
  </behavior>
  <action>
    Create tests/test_checkout_form_fill.py. Build no-browser plugins mirroring tests/test_bestbuy_plugin.py::_make_bestbuy_plugin (BestBuyPlugin(config=None); set instance.driver as needed). For _fill_field tests, construct a MagicMock tab whose select is an AsyncMock; the returned element is a MagicMock with clear_input and send_keys as AsyncMocks; assert return value and that clear_input/send_keys were awaited with the value. For missing-selector, set tab.select AsyncMock return_value=None and assert _fill_field returns False and (via caplog or a writeLog monkeypatch/recorder) that the selector text was logged at WARNING. For auto_buy-level tests, you must control the full select sequence: set tab.select as an AsyncMock with a side_effect list mapping each selector call to an element or None in flow order, and stub self.login (AsyncMock), self.place_order_guarded (AsyncMock recorder) so you can assert it was/was not awaited; set self.config to a minimal object with debug.monitor_only False and debug.test_mode False and available.items empty (so the monitor_only entry guard passes and quantity defaults). Provide a complete CheckoutProfile via load helper or by constructing CheckoutProfile(...) directly with all fields. For the profile-None test, leave self._checkout_profile = None and assert auto_buy returns False and place_order_guarded was not awaited. For the missing-required-selector test, make the #street select return None mid-sequence and assert auto_buy returns False and place_order_guarded not awaited. For address_line2 tests cover both None value (no #street2 select expected) and value-set-but-selector-None (no raise). For the Amazon CVV-skip test, build AmazonPlugin(config=None), set self._cvv to a sentinel, drive the select sequence so the place_order element is found and the CVV select returns None, stub place_order_guarded, and assert auto_buy did not return False at the CVV step (place_order_guarded is awaited). Do NOT assert any CVV/profile value appears in logs. Keep selector-sequence construction readable with a small dict-driven select side_effect.
  </action>
  <verify>
    <automated>pytest tests/test_checkout_form_fill.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_checkout_form_fill.py -x` passes all behaviors listed
    - Tests stub login + place_order_guarded and assert place_order_guarded is NOT awaited on profile-None and missing-required-selector paths
    - Element mocks include clear_input + send_keys AsyncMocks
    - Amazon CVV-absent path proven to continue (no False at CVV step)
    - No CVV/profile value asserted present in any log
  </acceptance_criteria>
  <done>Behavior tests prove fill success, missing-selector WARN+False with no submit, profile-None guard, optional line2 skip, and Amazon CVV skip-if-absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CVV-not-in-logs AST CI assertion (BUY-07 criterion 3)</name>
  <files>tests/test_no_cvv_in_logs.py</files>
  <read_first>
    - tests/test_no_env_secret_reads.py (AST/grep CI assertion structure to model on)
    - .planning/phases/20-checkout-profile-form-fill/20-RESEARCH.md (section "CVV-Not-In-Logs CI Test Pattern" lines 481-519 — verified exact pattern)
    - .planning/phases/20-checkout-profile-form-fill/20-PATTERNS.md (section "tests/test_checkout_profile.py (NEW)" → "Exact CVV-not-in-logs assertion")
  </read_first>
  <behavior>
    - test_cvv_not_in_writelog_args: an AST walk of plugins/shopbot_plugin_bestbuy.py, plugins/shopbot_plugin_amazon.py, and core/checkout_profile.py finds zero writeLog() Call nodes whose any argument's source text contains "_cvv"; on violation, the assertion message lists file:lineno:arg for each offender
    - The test fails loudly (collects all violations) rather than passing silently if the file set is empty (guard: assert the three target files exist and were parsed)
  </behavior>
  <action>
    Create tests/test_no_cvv_in_logs.py implementing the verified pattern from RESEARCH lines 487-519: repo_root = Path(__file__).parent.parent; checkout_files = the three paths [plugins/shopbot_plugin_bestbuy.py, plugins/shopbot_plugin_amazon.py, core/checkout_profile.py]; for each, read_text(encoding="utf-8"), ast.parse, ast.walk; for every ast.Call whose func resolves to the name "writeLog" (ast.Name.id or ast.Attribute.attr), iterate node.args and check `"_cvv" in ast.unparse(arg)`; collect violations as f"{path.name}:{node.lineno}: writeLog arg contains '_cvv': {arg_src!r}"; assert not violations with the joined message. Add a coverage guard: assert each of the three checkout_files exists (path.is_file()) so an accidental path typo cannot make the scan vacuously pass (model on the companion-assertion guard in test_no_env_secret_reads.py). Scope the token check to "_cvv" exactly (the runtime attribute/variable name) so the literal prompt string "Enter CVV ..." in run.py is not in these scanned files and the constant is not flagged.
  </action>
  <verify>
    <automated>pytest tests/test_no_cvv_in_logs.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_no_cvv_in_logs.py -x` passes (zero violations against the implemented plugins)
    - The test parses all three checkout files via AST and asserts each file exists (no vacuous pass)
    - Introducing a writeLog(f"...{self._cvv}...") into any scanned file would make the test fail (verifiable by reasoning over the assertion)
  </acceptance_criteria>
  <done>The AST CI assertion enforces no `_cvv` in any writeLog() argument across the BestBuy/Amazon plugins and checkout_profile.py, with a non-vacuous file-existence guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile (memory) → DOM inputs | PII address values typed into retailer form fields via CDP key events |
| runtime cvv → CVV DOM input | the live CVV typed into the retailer CVV field; must never reach logs/disk |
| checkout DOM state → place_order_guarded | a partially-filled form must never reach the guarded order click |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Information Disclosure | _cvv in writeLog on checkout paths | mitigate | Dedicated AST assertion (Task 3) scanning both plugins + checkout_profile.py for "_cvv" in any writeLog arg (BUY-07 criterion 3) |
| T-20-08 | Information Disclosure | profile field value logged during fill | mitigate | _fill_field logs selector NAME only, never the value (Pitfall 2 / Shared Pattern) |
| T-20-09 | Tampering | partial-form submit on DOM drift | mitigate | _fill_field returns False on a None required selector; auto_buy returns False before place_order_guarded (Pitfall 3, BUY-07 criterion 4) |
| T-20-10 | Denial of Service | absent Amazon CVV field aborts a valid checkout | accept | Skip-if-absent: absent CVV field is a valid state (payment pre-verified); do not return False (Pitfall 4) |
| T-20-SC | Tampering | pip installs | accept | No new packages; RESEARCH Package Legitimacy Audit confirms zero installs |

## UAT Debt (tracked, non-blocking)
- BestBuy shipping selectors (#first-name/#last-name/#street/#street2/#city/#state/#zip/#phone) are MEDIUM/LOW confidence; Amazon CVV selector #addCreditCardCvvInput is MEDIUM. Live form-fill + CVV entry against the real checkout DOM (and SPA onChange behavior per Pitfall 7) must be verified in a test_mode buy and selectors corrected if drifted. Amazon shipping address form-fill is UAT-gated and deferred (Open Question 1). Record in STATE.md Deferred Items.
</threat_model>

<verification>
- `pytest tests/test_checkout_form_fill.py tests/test_no_cvv_in_logs.py -x` green
- `pytest tests/test_bestbuy_plugin.py tests/test_plugin_bestbuy.py tests/test_amazon_plugin.py -x` still green (no regression to get_active_tab / existing flow)
- `pytest` full suite green
</verification>

<success_criteria>
- _checkout_profile loaded at setup() for both plugins; default None on the ABC
- BestBuy fills the required shipping fields before place_order_guarded; optional ADDRESS_LINE2 skip
- Missing required selector → WARNING(selector) + return False, no submit (criterion 4)
- Amazon enters CVV from self._cvv with skip-if-absent (no abort on absent field)
- AST assertion proves no _cvv in any writeLog() on checkout paths (criterion 3)
- BUY-07 fully covered; live form-fill/CVV recorded as UAT debt
</success_criteria>

<output>
Create `.planning/phases/20-checkout-profile-form-fill/20-04-SUMMARY.md` when done
</output>
