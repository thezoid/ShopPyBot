---
phase: 06-platform-expansion
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - core/config_schema.py
  - tests/conftest.py
  - tests/test_config_schema.py
  - tests/test_plugin_walmart.py
  - tests/test_plugin_target.py
  - tests/test_plugin_gamestop.py
  - tests/test_plugin_squareenix.py
  - tests/test_plugin_newegg.py
  - tests/test_orchestrator_jitter.py
  - tests/test_security_md.py
autonomous: true
requirements: [ANTI-01, ANTI-02, ANTI-03]
must_haves:
  truths:
    - "Setting platforms.walmart.min_delay/max_delay/headless/user_agents in config.yml loads as typed values (no validation error)"
    - "Setting platforms.amazon.headless: false in config.yml loads as False (no longer silently dropped); amazon and bestbuy headless default True (SC3 config surface)"
    - "A negative min_delay or max_delay raises a Pydantic validation error at startup"
    - "A module-level global default UA pool constant exists in config_schema.py for plugins to fall back to when user_agents is empty"
    - "A mock-nodriver fixture lets a test assert headless and browser_args kwargs without launching Chrome"
    - "Seven new test files exist as scaffolds so downstream plugin/orchestrator tasks have a Nyquist target"
  artifacts:
    - path: "core/config_schema.py"
      provides: "5 new *PlatformConfig submodels + headless on Amazon/BestBuy + global UA pool constant"
      contains: "WalmartPlatformConfig"
    - path: "tests/conftest.py"
      provides: "mock_nodriver_start fixture for headless/UA assertions"
      contains: "mock_nodriver_start"
    - path: "tests/test_config_schema.py"
      provides: "tests for new per-platform anti-detection fields + Amazon/BestBuy headless + UA pool constant"
  key_links:
    - from: "core/config_schema.py PlatformsConfig"
      to: "config.platforms.walmart.min_delay"
      via: "WalmartPlatformConfig field"
      pattern: "min_delay"
    - from: "core/config_schema.py AmazonPlatformConfig"
      to: "config.platforms.amazon.headless"
      via: "headless field on AmazonPlatformConfig (SC3)"
      pattern: "headless"
---

<objective>
Lay the Wave 0 foundation for Phase 6: extend the Pydantic config schema with per-platform
anti-detection fields for the five new platforms, add a headless flag to the existing Amazon and
BestBuy platform configs (SC3), add a global default user-agent pool constant, add a mock-nodriver
test fixture, and scaffold the seven new test files that downstream tasks satisfy.

Purpose: Every downstream plan (orchestrator jitter, 5 plugins, SECURITY.md rows) depends on the
config fields and the test scaffolds existing first. SC3 names Amazon explicitly ("platforms.amazon.headless:
false launches an Amazon browser session in visible mode"), so the existing Amazon/BestBuy configs MUST
gain a headless field here, otherwise extra="ignore" silently drops the key. This plan ships nothing
user-visible on its own; it is the contract layer (ANTI-01/02/03 config surface + SC3 headless) the
rest of the phase builds against.
Output: extended config_schema.py (5 new submodels + Amazon/BestBuy headless), a mock-nodriver conftest
fixture, config-schema tests, and 7 empty-but-importable test scaffolds with explicit MISSING markers
for downstream waves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-platform-expansion/06-RESEARCH.md
@.planning/phases/06-platform-expansion/06-CONTEXT.md

<interfaces>
<!-- Existing config_schema.py contracts the executor extends. Do NOT re-derive. -->

From core/config_schema.py (existing, verified):
- Existing submodel pattern (Amazon/BestBuy use delay_seconds/delay_jitter -- DO NOT rename or remove these):
    class AmazonPlatformConfig(BaseModel): delay_seconds: float = 30.0; delay_jitter: float = 10.0
    class BestBuyPlatformConfig(BaseModel): delay_seconds: float = 30.0; delay_jitter: float = 10.0
  This task ADDS a headless: bool = True field to BOTH of these (SC3). It does NOT touch their
  delay_seconds/delay_jitter fields.
- class PlatformsConfig(BaseModel): amazon: AmazonPlatformConfig = ...; bestbuy: BestBuyPlatformConfig = ...
- AppConfig.model_config has extra="ignore" (unknown keys silently dropped) -- this is exactly why
  platforms.amazon.headless must be a DECLARED field, not an extra key.
- imports already present: from pydantic import BaseModel, model_validator

From tests/conftest.py (existing fixtures, do NOT duplicate): tmp_config_yml, fake_browser,
tmp_plugins_dir, fake_plugin. The new mock_nodriver_start fixture is ADDED to this file.

From tests/test_config_schema.py (existing): uses AppConfig(yaml_file=...) + yaml.dump pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config_schema with 5 platform submodels + Amazon/BestBuy headless + global UA pool constant</name>
  <files>core/config_schema.py, tests/test_config_schema.py</files>
  <read_first>
    - core/config_schema.py (AmazonPlatformConfig / BestBuyPlatformConfig / PlatformsConfig pattern to mirror; note extra="ignore")
    - tests/test_config_schema.py (AppConfig(yaml_file=...) + yaml.dump test pattern to mirror)
    - .planning/phases/06-platform-expansion/06-RESEARCH.md "Pattern 1: Config Schema Extension",
      "Default UA pool", and Security Domain "Field validation recommendation"
  </read_first>
  <behavior>
    - platforms.walmart.min_delay=8 / max_delay=15 loads as float 8.0 / 15.0
    - platforms.walmart.headless defaults to True; set to False loads as False
    - platforms.walmart.user_agents defaults to empty list; a YAML list loads as list[str]
    - min_delay=-1 (or max_delay=-1) raises pydantic ValidationError (Field ge=0.0 guard)
    - The same field set exists for target, gamestop, squareenix, newegg (config key "squareenix" -- no underscore)
    - A module-level constant holding the global default UA pool is importable from core.config_schema
    - platforms.amazon.headless and platforms.bestbuy.headless default to True (SC3 config surface)
    - Setting platforms.amazon.headless: false in test YAML yields cfg.platforms.amazon.headless == False
      (proving the key is a declared field and is no longer dropped by extra="ignore")
  </behavior>
  <action>
    Add five Pydantic BaseModel submodels to core/config_schema.py: WalmartPlatformConfig,
    TargetPlatformConfig, GameStopPlatformConfig, SquareEnixPlatformConfig, NeweggPlatformConfig.
    Each carries exactly four fields (ANTI-01/02/03): min_delay: float = 8.0 with Field(ge=0.0),
    max_delay: float = 15.0 with Field(ge=0.0), headless: bool = True, user_agents: list[str] using
    Field(default_factory=list). Register all five on PlatformsConfig using these attribute names:
    walmart, target, gamestop, squareenix (NO underscore -- per resolved open question), newegg --
    each defaulting to its submodel instance, matching the existing amazon/bestbuy lines.
    SC3 REQUIREMENT: add a headless: bool = True field to BOTH the existing AmazonPlatformConfig and
    BestBuyPlatformConfig submodels (do NOT rename or remove their delay_seconds/delay_jitter fields).
    This is mandatory because AppConfig uses extra="ignore", so an undeclared platforms.amazon.headless
    key would be silently dropped and SC3 ("setting platforms.amazon.headless: false launches Amazon in
    visible mode") would be undeliverable. Optionally add a user_agents: list[str] = Field(default_factory=list)
    field to Amazon/BestBuy ONLY if it is a trivial one-line addition consistent with the new submodels;
    headless on Amazon/BestBuy is the mandatory part, user_agents on them is optional.
    Add a module-level constant DEFAULT_USER_AGENTS (a list of 4-5 plausible recent Chrome/Firefox
    desktop UA strings from RESEARCH "Default UA pool") placed near the top of the module; this is
    the fallback pool plugins use when a platform user_agents list is empty (ANTI-02). Do NOT
    harmonize the existing Amazon/BestBuy delay_seconds/delay_jitter fields (out of scope per
    resolved open question 2); add a one-line comment noting the field-name difference is intentional.
    Import Field from pydantic. Add tests to tests/test_config_schema.py mirroring the existing
    AppConfig(yaml_file=...) pattern: one asserting walmart min/max/headless/user_agents load from
    YAML, one asserting a negative min_delay raises ValidationError, one asserting DEFAULT_USER_AGENTS
    is a non-empty list[str], one asserting config.platforms.squareenix is reachable by that key, one
    asserting cfg.platforms.amazon.headless and cfg.platforms.bestbuy.headless exist and default True,
    and one asserting that a test YAML setting platforms.amazon.headless: false yields
    cfg.platforms.amazon.headless == False (SC3 -- proving the key is no longer dropped).
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_config_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_config_schema.py passes including the new tests
    - grep: core/config_schema.py contains "WalmartPlatformConfig", "TargetPlatformConfig", "GameStopPlatformConfig", "SquareEnixPlatformConfig", "NeweggPlatformConfig"
    - grep: core/config_schema.py AmazonPlatformConfig and BestBuyPlatformConfig each contain a "headless" field
    - grep: core/config_schema.py contains "DEFAULT_USER_AGENTS" at module level
    - grep: core/config_schema.py contains "squareenix" (no underscore) as a PlatformsConfig attribute
    - A config with platforms.walmart.min_delay=-1 raises ValidationError
    - cfg.platforms.amazon.headless defaults to True; a YAML with platforms.amazon.headless: false yields False
  </acceptance_criteria>
  <done>Five platform submodels with min_delay/max_delay/headless/user_agents registered on PlatformsConfig; headless added to Amazon/BestBuy (SC3); DEFAULT_USER_AGENTS constant present; new config tests green; Amazon/BestBuy delay fields untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add mock-nodriver conftest fixture + scaffold 7 downstream test files</name>
  <files>tests/conftest.py, tests/test_plugin_walmart.py, tests/test_plugin_target.py, tests/test_plugin_gamestop.py, tests/test_plugin_squareenix.py, tests/test_plugin_newegg.py, tests/test_orchestrator_jitter.py, tests/test_security_md.py</files>
  <read_first>
    - tests/conftest.py (fake_browser fixture pattern; how AsyncMock/MagicMock are used)
    - tests/test_plugin_amazon.py (importlib.util.spec_from_file_location module-load pattern to mirror in each scaffold)
    - .planning/phases/06-platform-expansion/06-VALIDATION.md "Wave 0 Requirements" and per-requirement map
  </read_first>
  <action>
    Add a fixture named mock_nodriver_start to tests/conftest.py: it patches nodriver.start so calling
    it never launches Chrome and instead records the headless and browser_args keyword arguments,
    returning the existing fake_browser-style mock. The fixture must expose the captured kwargs so a
    plugin test can assert plugin setup() passed headless=True/False (ANTI-03) and browser_args
    containing "--user-agent=" (ANTI-02). Use unittest.mock.patch on the "nodriver.start" target and
    yield an object holding .calls or .last_kwargs. Do NOT duplicate the existing fake_browser, tmp_config_yml,
    tmp_plugins_dir, or fake_plugin fixtures.
    Create seven test files as importable scaffolds. Each must import cleanly and contain one xfail or
    skip-marked placeholder plus a top-of-file comment stating which downstream plan fills it and which
    requirement it covers, so the Nyquist gate is explicit:
      - tests/test_plugin_walmart.py    -> filled by Plan 06-03 (PLG-04, ANTI-02, ANTI-03, SC4)
      - tests/test_plugin_target.py     -> filled by Plan 06-03 (PLG-05, SC4)
      - tests/test_plugin_gamestop.py   -> filled by Plan 06-03 (PLG-06, SC4)
      - tests/test_plugin_squareenix.py -> filled by Plan 06-04 (PLG-07)
      - tests/test_plugin_newegg.py     -> filled by Plan 06-04 (PLG-08)
      - tests/test_orchestrator_jitter.py -> filled by Plan 06-02 (ANTI-01)
      - tests/test_security_md.py       -> filled by Plan 06-05 (SC4 SECURITY.md rows)
    Use pytest.mark.skip(reason="scaffold -- filled by Plan 06-0X") on each placeholder so the suite
    stays green but the intent is recorded. Each scaffold MUST be syntactically valid and collectable
    by pytest (no import errors).
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest collects all 7 new test files with zero collection/import errors
    - grep: tests/conftest.py contains "mock_nodriver_start"
    - All 7 scaffold files exist and contain a skip marker referencing their downstream plan number
    - Full suite remains green (skips allowed, no failures)
  </acceptance_criteria>
  <done>mock_nodriver_start fixture added; 7 collectable test scaffolds with skip markers + downstream-plan attribution present; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml -> AppConfig | Untrusted YAML values (delay numbers, UA strings, headless flag) cross into the runtime config model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | min_delay/max_delay YAML values | mitigate | Field(ge=0.0) on both delay fields rejects negative sleep values at startup (V5 input validation) |
| T-06-02 | Information Disclosure | new platform config sections | mitigate | Schema adds only non-secret fields (delays, headless, UA list); no credential field is added -- platform creds stay env-only per SEC-01 |
| T-06-03 | Tampering | user_agents YAML list | accept | UA strings are non-secret cosmetic config; malformed values at worst yield an ineffective UA override, no security impact |
| T-06-04 | Tampering | platforms.amazon/bestbuy.headless YAML flag | accept | headless is a non-secret boolean; declaring it (vs extra="ignore" drop) only changes the documented visible/headless launch mode, no security impact |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit: none); no install task present |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/ -q` is green (skips allowed for scaffolds).
- config_schema exposes the 5 new submodels, the squareenix key, DEFAULT_USER_AGENTS, and headless on Amazon/BestBuy.
- A negative delay value is rejected by Pydantic.
- platforms.amazon.headless: false in YAML yields False (SC3 config surface, no longer dropped).
</verification>

<success_criteria>
- ANTI-01/02/03 config surface (min_delay, max_delay, headless, user_agents) exists for all five new platforms with validation guards.
- SC3 config surface: headless field present on Amazon/BestBuy (default True), settable to False via YAML.
- Global UA pool constant available for plugin fallback.
- mock_nodriver_start fixture + 7 test scaffolds ready for downstream waves.
</success_criteria>

<output>
Create `.planning/phases/06-platform-expansion/06-01-SUMMARY.md` when done.
</output>
</objective>
</content>
</invoke>
