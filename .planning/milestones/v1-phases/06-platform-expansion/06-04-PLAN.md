---
phase: 06-platform-expansion
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - plugins/shopbot_plugin_squareenix.py
  - plugins/shopbot_plugin_newegg.py
  - tests/test_plugin_squareenix.py
  - tests/test_plugin_newegg.py
autonomous: true
requirements: [PLG-07, PLG-08, ANTI-02, ANTI-03]
must_haves:
  truths:
    - "SquareEnixPlugin and NeweggPlugin load via the registry as RetailerPlugin v2 subclasses with no core edits"
    - "Square Enix domain_patterns matches na.store.square-enix-games.com via the broad 'store.square-enix-games.com' pattern; platform_key == squareenix"
    - "NewEgg domain_patterns includes newegg.com (and newegg.ca); platform_key == newegg"
    - "Each setup() passes its config headless flag to nodriver.start and a --user-agent browser_args entry (ANTI-02/ANTI-03)"
    - "auto_buy is implemented for both and returns True on success; neither calls update_item_purchased"
    - "Each module docstring declares its anti-detection risk level + reason"
  artifacts:
    - path: "plugins/shopbot_plugin_squareenix.py"
      provides: "SquareEnixPlugin (PLG-07)"
      contains: "store.square-enix-games.com"
    - path: "plugins/shopbot_plugin_newegg.py"
      provides: "NeweggPlugin (PLG-08)"
      contains: "newegg.com"
  key_links:
    - from: "plugins/shopbot_plugin_squareenix.py setup()"
      to: "nodriver.start(headless=...)"
      via: "config.platforms.squareenix.headless"
      pattern: "nodriver.start"
    - from: "plugins/shopbot_plugin_newegg.py"
      to: "config.platforms.newegg"
      via: "platform_key = newegg"
      pattern: "platform_key"
---

<objective>
Deliver the two lighter-protection platform plugins (Square Enix, NewEgg). Each is a single
self-contained plugins/shopbot_plugin_<name>.py mirroring the Amazon/BestBuy template and the
anti-detection setup() established in Plan 06-03: async nodriver, self.driver in setup(),
domain_patterns + platform_key, ANTI-02 UA rotation + ANTI-03 headless, best-effort TODO-marked
selectors, auto_buy returning True on success, and a risk-level module docstring. Neither calls
update_item_purchased (orchestrator write queue owns the write).

Purpose: PLG-07/PLG-08 + SC1 (registry loads them, no core edits) + ANTI-02/ANTI-03. Grouped
separately from Plan 06-03 because these two have no SC4-named protection requirement and share no
files with 06-03, so both plugin plans run in the same wave in parallel.
Output: 2 plugin files + their 2 filled test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-platform-expansion/06-RESEARCH.md
@.planning/phases/06-platform-expansion/06-CONTEXT.md
@plugins/shopbot_plugin_bestbuy.py

<interfaces>
<!-- Same contracts as Plan 06-03. The bestbuy plugin is the structural template. -->

From core/plugin_base.py (RetailerPlugin v2):
- class attribute domain_patterns: list[str]; __init__(config) sets self.config + self.driver=None
- abstract async check_availability(url)->bool / auto_buy(url)->bool; optional setup/teardown/login/detect_captcha

From core/config_schema.py (delivered by Plan 06-01):
- config.platforms.squareenix / .newegg each: min_delay, max_delay, headless: bool, user_agents: list[str]
- module constant DEFAULT_USER_AGENTS: list[str] (fallback pool)
- NOTE the config key is "squareenix" (no underscore) -- platform_key must match exactly.

From core/orchestrator.py (delivered by Plan 06-02):
- platform_key class attribute convention drives ANTI-01 jitter lookup.

nodriver API (RESEARCH, VERIFIED): await nodriver.start(headless=bool, browser_args=["--user-agent=<ua>"]).
NEVER pass a "--headless" string in browser_args (raises ValueError).

Square Enix domain note (RESEARCH Pitfall 1): the NA store host is na.store.square-enix-games.com
(order is na.store.*, NOT store.na.*). Use the broad substring "store.square-enix-games.com" so the
registry's hostname substring match covers na.store.* and other regional prefixes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Square Enix plugin (PLG-07) with correct broad domain pattern</name>
  <files>plugins/shopbot_plugin_squareenix.py, tests/test_plugin_squareenix.py</files>
  <read_first>
    - plugins/shopbot_plugin_bestbuy.py (structural template)
    - .planning/phases/06-platform-expansion/06-RESEARCH.md "Full setup() with all three anti-detection features wired", Square Enix Platform section, and Pitfall 1 (domain order)
    - tests/test_plugin_amazon.py (importlib load + ABC test pattern); tests/conftest.py (mock_nodriver_start)
  </read_first>
  <behavior>
    - SquareEnixPlugin subclasses RetailerPlugin; instantiating with config=None succeeds; driver None before setup
    - domain_patterns contains "store.square-enix-games.com" (broad) so a na.store.square-enix-games.com URL routes to it; platform_key == "squareenix"
    - setup() applies headless from config + UA rotation (ANTI-02/03), captured by mock_nodriver_start
    - auto_buy implemented; returns True on success; does not call update_item_purchased
    - check_availability returns bool, never raises
    - module docstring declares risk level (MEDIUM best estimate) + reason
  </behavior>
  <action>
    Create plugins/shopbot_plugin_squareenix.py (class SquareEnixPlugin) mirroring the BestBuy plugin
    and the setup() anti-detection pattern from Plan 06-03. domain_patterns =
    ["store.square-enix-games.com"] (broad substring covering na.store.* per RESEARCH Pitfall 1);
    platform_key = "squareenix" (no underscore -- must match the config key). setup() reads headless +
    user_agents from getattr(self.config.platforms, self.platform_key, None) (guard None), selects a UA
    via random.choice from user_agents or DEFAULT_USER_AGENTS (imported from core.config_schema), and
    passes headless=<flag> + browser_args=["--user-agent=<ua>"] to nodriver.start. check_availability
    uses best-effort generic e-commerce selectors from RESEARCH each with a
    "# TODO: verify selectors against live na.store.square-enix-games.com" marker; try/except -> False.
    auto_buy implemented, returns True on success, does NOT call update_item_purchased. login() env-var
    only (SQUAREENIX_EMAIL/SQUAREENIX_PASSWORD), never logs values. Module docstring states risk MEDIUM +
    reason + credential env var names. No em dashes / no --- rules in the docstring. Fill
    tests/test_plugin_squareenix.py (remove skip): importlib ABC/domain/platform_key tests; a routing
    assertion that a na.store.square-enix-games.com URL hostname matches domain_patterns; setup()
    headless+UA assertions via mock_nodriver_start; a no-update_item_purchased source-grep test.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_plugin_squareenix.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_plugin_squareenix.py passes with no skip marker
    - grep: plugins/shopbot_plugin_squareenix.py contains "store.square-enix-games.com" and platform_key = "squareenix"
    - a test proves a "https://na.store.square-enix-games.com/..." hostname matches a domain_patterns entry
    - grep: plugin source does not contain "update_item_purchased"
    - grep: plugin contains "nodriver.start" with headless kwarg + "--user-agent=" + "# TODO: verify selectors against live"
  </acceptance_criteria>
  <done>Square Enix plugin implemented with correct broad domain pattern, ANTI-02/03 setup, auto_buy, no direct DB write; test file green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NewEgg plugin (PLG-08)</name>
  <files>plugins/shopbot_plugin_newegg.py, tests/test_plugin_newegg.py</files>
  <read_first>
    - plugins/shopbot_plugin_squareenix.py (the pattern just written in Task 1 -- mirror it)
    - .planning/phases/06-platform-expansion/06-RESEARCH.md NewEgg Platform section (selectors, newegg.com + newegg.ca, medium risk)
    - tests/test_plugin_squareenix.py (the test shape just written in Task 1 -- mirror it)
  </read_first>
  <behavior>
    - NeweggPlugin subclasses RetailerPlugin; instantiating with config=None succeeds; driver None before setup
    - domain_patterns includes "newegg.com" and "newegg.ca"; platform_key == "newegg"
    - setup() applies headless + UA rotation identically (ANTI-02/03), captured by mock_nodriver_start
    - auto_buy implemented; returns True on success; does not call update_item_purchased
    - check_availability returns bool, never raises
    - module docstring declares risk level (MEDIUM best estimate) + reason
  </behavior>
  <action>
    Create plugins/shopbot_plugin_newegg.py (class NeweggPlugin) mirroring the Square Enix plugin from
    Task 1. domain_patterns = ["newegg.com", "newegg.ca"]; platform_key = "newegg". Reuse the same
    setup() anti-detection pattern (headless + UA rotation via browser_args from config/DEFAULT_USER_AGENTS).
    check_availability uses NewEgg best-effort selectors from RESEARCH each with a
    "# TODO: verify selectors against live newegg.com" marker; try/except -> False. auto_buy implemented,
    returns True on success, does NOT call update_item_purchased. login() env-var only
    (NEWEGG_EMAIL/NEWEGG_PASSWORD), never logs values. Module docstring states risk MEDIUM + reason +
    credential env var names. No em dashes / no --- rules in docstring. Fill tests/test_plugin_newegg.py
    (remove skip) mirroring the Task 1 shape: importlib ABC/domain/platform_key tests, setup() headless+UA
    assertions via mock_nodriver_start, and a no-update_item_purchased source-grep test.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_plugin_newegg.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_plugin_newegg.py passes with no skip marker
    - grep: plugins/shopbot_plugin_newegg.py contains "newegg.com", "newegg.ca", and platform_key = "newegg"
    - grep: plugin source does not contain "update_item_purchased"
    - grep: plugin contains "nodriver.start" with headless kwarg + "--user-agent=" + "# TODO: verify selectors against live"
  </acceptance_criteria>
  <done>NewEgg plugin implemented with multi-region domains, ANTI-02/03 setup, auto_buy, no direct DB write; test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retail site DOM -> plugin | Untrusted page content read via selectors |
| env vars -> plugin login | Credentials cross from environment into the browser session |
| config user_agents -> nodriver browser_args | Config-supplied UA string passed to Chrome launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | login() credential handling | mitigate | os.environ.get only; missing-cred log message is credential-free; values never interpolated into any writeLog call (SEC-01) |
| T-06-11 | Spoofing | UA rotation / headless toggle (dual-use) | accept | In-scope anti-detection for personal use; docstrings are HONEST that risk remains and protections are not defeated (RESEARCH Security Domain) |
| T-06-12 | Tampering | config user_agents -> --user-agent browser_args | accept | UA strings non-secret cosmetic config; single Chrome flag, no broader injection |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/test_plugin_squareenix.py tests/test_plugin_newegg.py -q` green.
- Square Enix broad domain pattern routes a na.store.square-enix-games.com URL (Pitfall 1 avoided).
</verification>

<success_criteria>
- PLG-07/PLG-08 plugins are self-contained RetailerPlugin v2 subclasses (SC1).
- ANTI-02 UA rotation + ANTI-03 per-platform headless wired and asserted via mock.
- auto_buy implemented for both; neither calls update_item_purchased.
</success_criteria>

<output>
Create `.planning/phases/06-platform-expansion/06-04-SUMMARY.md` when done.
</output>
