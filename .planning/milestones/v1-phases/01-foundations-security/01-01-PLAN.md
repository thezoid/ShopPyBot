---
phase: 01-foundations-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/test_utils.py
  - tests/test_models.py
  - tests/test_config.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running pytest from the repo root collects and runs the existing suite without FileNotFoundError or import errors"
    - "Test database writes go to a temp directory, not the missing data/ directory"
    - "pytest-asyncio runs in auto mode with no deprecation warning"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest + asyncio config and requires-python pin"
      contains: "[tool.pytest.ini_options]"
    - path: "tests/conftest.py"
      provides: "Shared tmp_config_yml and tmp_data_dir fixtures"
      contains: "def tmp_config_yml"
  key_links:
    - from: "tests/conftest.py"
      to: "models.DB_PATH"
      via: "monkeypatch.setattr"
      pattern: "monkeypatch.setattr.*models"
---

<objective>
Repair the broken test suite and add pytest configuration so that every subsequent Phase 1 plan can attach automated verification. The existing suite is broken three ways: `tests/test_utils.py` imports `make_tiny` from `utils` (it lives in `main.py`), `tests/test_models.py` writes to a missing `data/` directory, and `tests/test_config.py` depends on `config.py` reading a `config.yml` that does not exist in the test CWD. There is no pytest config file, so `pytest-asyncio` emits a deprecation warning.

Purpose: Wave 0 test scaffold. Nyquist compliance for all other Phase 1 plans depends on a green-collecting suite and shared fixtures.
Output: `pyproject.toml`, `tests/conftest.py`, and repaired existing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundations-security/01-RESEARCH.md
@.planning/phases/01-foundations-security/01-PATTERNS.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly. -->

From models.py (DB path the tests collide with):
  DB_PATH points at data/shop_py_bot.db; the data/ directory does not exist at test CWD.
  initialize_db(delete: bool = False), add_items(items), get_items() are the public functions.

From main.py (where make_tiny actually lives — NOT utils.py):
  def make_tiny(url) -> str   # builds a tinyurl via requests.get

From tests/test_models.py existing fixture pattern (lines 6-18):
  @pytest.fixture(scope='module')
  def setup_db(): initialize_db(delete=True); ... ; yield; os.remove(DB_PATH)

From tests/test_config.py existing pattern (lines 5-37):
  tmp_path fixture writes a config.yml via write_text, then calls load_config(path)

From the verified worktree pyproject.toml (.claude/worktrees/gallant-ritchie-fa629c/pyproject.toml):
  [tool.pytest.ini_options] with testpaths, asyncio_mode = "auto"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pyproject.toml with pytest and asyncio config</name>
  <read_first>
    - .claude/worktrees/gallant-ritchie-fa629c/pyproject.toml (the verified analog to copy)
    - .planning/phases/01-foundations-security/01-PATTERNS.md (pyproject.toml section, lines 363-380)
  </read_first>
  <files>pyproject.toml</files>
  <action>Create `pyproject.toml` at repo root with a `[project]` table (name "shoppybot", version "0.1.0", `requires-python = ">=3.11"` to satisfy INFRA-01's python_requires constraint at project level) and a `[tool.pytest.ini_options]` table with `testpaths = ["tests"]`, `asyncio_mode = "auto"`, and `asyncio_default_fixture_loop_scope = "function"`. Copy the structure from the verified worktree analog. Do not add dependencies here — requirements.txt remains the dependency source of truth (handled in plan 04).</action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['tool']['pytest']['ini_options']['asyncio_mode']=='auto'; assert d['project']['requires-python']=='>=3.11'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists at repo root
    - `[tool.pytest.ini_options]` contains `asyncio_mode = "auto"` and `testpaths = ["tests"]`
    - `[project]` contains `requires-python = ">=3.11"`
    - Command above prints `ok`
  </acceptance_criteria>
  <done>pyproject.toml exists; tomllib loads it; asyncio_mode is "auto" and requires-python is ">=3.11".</done>
</task>

<task type="auto">
  <name>Task 2: Add shared fixtures in tests/conftest.py</name>
  <read_first>
    - tests/test_models.py (fixture + DB_PATH teardown pattern, lines 6-18)
    - tests/test_config.py (tmp_path + yaml write fixture pattern, lines 5-37)
    - models.py (to confirm the DB_PATH module attribute name)
    - .planning/phases/01-foundations-security/01-PATTERNS.md (conftest.py section, lines 383-429)
  </read_first>
  <files>tests/conftest.py</files>
  <action>Create `tests/conftest.py` with two fixtures. `tmp_config_yml(tmp_path)`: writes a minimal valid config dict (`debug.logging_level=3`, `debug.test_mode=True`, `available.timeout=10`, `available.items=[]`, `platforms.amazon.delay_seconds=30.0`, `platforms.bestbuy.delay_seconds=30.0`) to `tmp_path / "config.yml"` via `yaml.dump` and returns the Path. `tmp_data_dir(tmp_path, monkeypatch)`: imports `models`, monkeypatches `models.DB_PATH` to `str(tmp_path / "shop_py_bot.db")`, returns tmp_path. Use guard clauses; keep each fixture under 30 lines. Confirm the exact `DB_PATH` attribute name by reading models.py before writing the monkeypatch target.</action>
  <verify>
    <automated>python -m pytest tests/conftest.py --collect-only -q 2>&1 | head -5; python -c "import models; assert hasattr(models,'DB_PATH'), 'DB_PATH attr name mismatch — fix conftest monkeypatch target'; print('DB_PATH ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` exists and imports without error
    - `tmp_config_yml` writes a config.yml containing a `platforms` section and returns a Path
    - `tmp_data_dir` monkeypatches the real attribute on `models` (verified `hasattr(models,'DB_PATH')` is True)
    - No fixture exceeds 30 lines
  </acceptance_criteria>
  <done>conftest.py collects cleanly; both fixtures defined; monkeypatch targets the confirmed models DB path attribute.</done>
</task>

<task type="auto">
  <name>Task 3: Repair broken existing tests (test_utils, test_models, test_config)</name>
  <read_first>
    - tests/test_utils.py (the broken import on line 2)
    - tests/test_models.py (the data/ directory dependency)
    - tests/test_config.py (config.py CWD dependency)
    - main.py (confirms make_tiny lives here, line 30)
    - models.py (confirms DB_PATH and CRUD signatures)
  </read_first>
  <files>tests/test_utils.py, tests/test_models.py, tests/test_config.py</files>
  <action>Fix three broken tests so the suite collects and runs green. (1) `tests/test_utils.py`: `make_tiny` is defined in `main.py`, not `utils.py` — change the import to `from main import make_tiny`. Because `make_tiny` does a live network call to tinyurl, mark the test with `@pytest.mark.skipif` keyed on an env flag (e.g. `RUN_NETWORK_TESTS`) OR monkeypatch `main.requests.get` to return a stub response whose `.text` starts with `http://tinyurl.com/`; prefer the monkeypatch approach so the test runs offline deterministically. (2) `tests/test_models.py`: use the new `tmp_data_dir` fixture from conftest so the DB writes to tmp_path instead of the missing `data/` directory; remove any hardcoded reliance on a real `data/` dir. (3) `tests/test_config.py`: isolate `config.py`'s module-level load — either pass an explicit fixture config path to `load_config(path)` or use `monkeypatch.chdir(tmp_path)` after writing a config.yml so the import-time read finds a file. Do not change production code (main.py, models.py, config.py) in this plan — only the test files.</action>
  <verify>
    <automated>python -m pytest tests/test_utils.py tests/test_models.py tests/test_config.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_utils.py tests/test_models.py tests/test_config.py -q` exits 0 (all pass or deterministically skip)
    - No `ImportError`, no `FileNotFoundError`, no `sqlite3.OperationalError: unable to open database file`
    - `test_make_tiny` either runs offline via monkeypatch or skips cleanly; it does NOT fail on a network error
    - No production source file (main.py, models.py, config.py) was modified
  </acceptance_criteria>
  <done>The three previously-broken test files pass or skip deterministically; full collection succeeds; production code untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | Tests must not read/write outside tmp_path; no real credentials or DB touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | test_make_tiny live network call | mitigate | Monkeypatch requests.get so tests run offline and deterministically; no external dependency in CI |
| T-01-02 | Information Disclosure | test fixtures writing config | accept | Fixtures write only synthetic non-secret config to tmp_path; no real credentials present |
</threat_model>

<verification>
Run `python -m pytest tests/ -q` after all three tasks. The full suite must collect without errors. New test files added in plans 02/03/04 will extend coverage; this plan only guarantees the existing suite is repaired and pytest config is in place.
</verification>

<success_criteria>
- `python -m pytest tests/ --collect-only -q` succeeds with zero collection errors
- `python -m pytest tests/test_utils.py tests/test_models.py tests/test_config.py -q` exits 0
- pyproject.toml present with asyncio_mode=auto and requires-python ">=3.11"
- conftest.py exposes tmp_config_yml and tmp_data_dir fixtures
</success_criteria>

<output>
Create `.planning/phases/01-foundations-security/01-01-SUMMARY.md` when done.
</output>
