---
phase: 05-notification-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/config_schema.py
  - models.py
  - notifications/__init__.py
  - notifications/base.py
  - tests/conftest.py
  - tests/test_notifications.py
  - tests/test_models.py
autonomous: true
requirements: [NOTIF-02, NOTIF-06]
must_haves:
  truths:
    - "AppConfig exposes a notifications section with sound/discord/email/sms submodels"
    - "Enabling SMS without Twilio env credentials fails at startup with an actionable error, not a silent no-op"
    - "items table carries last_seen_available and last_notified columns; adding them is idempotent across restarts"
    - "Notifier ABC and NotificationEvent are importable so all channel plans build against a fixed contract"
  artifacts:
    - path: "core/config_schema.py"
      provides: "NotificationsConfig submodel + SMS startup validator"
      contains: "class NotificationsConfig"
    - path: "models.py"
      provides: "dedup columns + read/write sync functions"
      contains: "last_seen_available"
    - path: "notifications/base.py"
      provides: "Notifier ABC + NotificationEvent dataclass"
      contains: "class Notifier"
    - path: "tests/test_notifications.py"
      provides: "failing test scaffold for all 14 notification behaviors"
      contains: "def test_"
  key_links:
    - from: "core/config_schema.py"
      to: "os.environ"
      via: "SMS model_validator checking TWILIO_* presence"
      pattern: "model_validator"
    - from: "models.py"
      to: "items table"
      via: "ALTER TABLE ADD COLUMN guard in initialize_db"
      pattern: "last_seen_available"
---

<objective>
Lay the Wave 0 foundation for the notification system: the config schema, the dedup
data model, the notifier contract, and the failing test scaffold every later plan
verifies against.

Purpose: Channel notifiers (Plans 02/03), the dispatcher (Plan 04), and orchestrator
wiring (Plan 05) all build against the `Notifier` ABC, `NotificationEvent`, the new
`notifications` config section, and the dedup columns. This plan defines those contracts
first so no downstream executor has to explore the codebase to discover them.

Output: `NotificationsConfig` submodel on `AppConfig`; two new idempotent columns plus
their `*_sync` read/write functions on `models.py`; the `notifications/` package with
`base.py` (`Notifier` + `NotificationEvent`); and `tests/test_notifications.py` carrying
the 14 named behavior tests (failing/skipped until later plans implement them), plus the
SMS startup-gate and dedup-column tests which pass in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-notification-system/05-RESEARCH.md
@.planning/phases/05-notification-system/05-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor builds on. Use directly; no exploration needed. -->

From core/config_schema.py (existing AppConfig pattern to mirror):
- `AppConfig(BaseSettings)` with submodels assigned as defaulted fields:
  `selenium`, `debug`, `available`, `platforms`, `app`.
- Submodels are plain `pydantic.BaseModel` with defaulted fields (see `PlatformsConfig`).
- `from pydantic import BaseModel, model_validator` already imported.
- env override of YAML is already wired in `settings_customise_sources`; secrets stay
  in env and are NOT config fields.

From models.py (existing patterns to mirror):
- `get_db_connection()` context manager (WAL PRAGMAs) — use for all DB access.
- `initialize_db(delete=False)` runs `CREATE TABLE IF NOT EXISTS items (...)`.
- `*_sync` functions (`get_items_sync`, `update_item_purchased_sync`,
  `add_items_sync`) are the single source of truth; legacy non-`_sync` aliases delegate.

From tests/conftest.py (existing fixtures available):
- `tmp_data_dir` — monkeypatches `models.DB_PATH` to a temp DB (use for models tests).
- `tmp_config_yml` — writes a minimal config.yml and returns its Path; construct
  `AppConfig(yaml_file=<path>)` to test config.
- Add new notification fixtures here (fake notifier, event factory).

From logger.py:
- `writeLog(message: str, type: str, writeTofile: bool = True) -> None` — levels include
  "INFO", "ERROR", "WARNING", "SUCCESS", "DEBUG".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NotificationsConfig submodel with SMS startup validator</name>
  <read_first>
    - core/config_schema.py (mirror PlatformsConfig submodel pattern + existing imports)
    - 05-RESEARCH.md "Pattern 7: Config Schema Extension" (DiscordConfig/EmailConfig/SmsConfig/NotificationsConfig shapes + require_creds_if_enabled validator)
    - 05-RESEARCH.md Open Question 1 (smtp_username optional, defaults to sender)
  </read_first>
  <action>
    Add four BaseModel submodels to core/config_schema.py per RESEARCH Pattern 7:
    `DiscordConfig` (enabled: bool = False), `EmailConfig` (enabled=False, smtp_host
    default "smtp.gmail.com", smtp_port=587, smtp_ssl=False, sender="", recipients:
    list[str]=[], plus an optional `smtp_username: str = ""` that resolves to `sender`
    when empty — per RESEARCH Open Question 1), `SmsConfig` (enabled=False,
    to_number="") and `NotificationsConfig` (sound: bool = True, discord, email, sms).
    Add a `notifications: NotificationsConfig = NotificationsConfig()` field to AppConfig.
    On `SmsConfig`, add a `@model_validator(mode="after")` named `require_creds_if_enabled`
    that, when `enabled` is True, raises `ValueError` listing any missing env vars among
    TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM (read via os.environ). This is the
    NOTIF-06 startup gate: it must fire at AppConfig construction, never at send time.
    Do NOT add any secret (webhook URL, SMTP password, Twilio token/SID) as a config field
    — secrets are env-only.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_notifications.py::test_sms_misconfigured_raises tests/test_notifications.py::test_sms_disabled_by_default -x -q</automated>
  </verify>
  <acceptance_criteria>
    - core/config_schema.py contains "class NotificationsConfig" and "class SmsConfig".
    - AppConfig has a `notifications` field defaulting to NotificationsConfig().
    - Constructing AppConfig with sms.enabled=true and no TWILIO_* env vars raises a
      pydantic ValidationError whose message names the missing env vars.
    - Default construction (no notifications key) does NOT raise (SMS disabled by default).
    - No secret field names (DISCORD_WEBHOOK_URL, SMTP_PASSWORD, TWILIO_*) appear in the schema.
  </acceptance_criteria>
  <done>NotificationsConfig wired into AppConfig; SMS startup validator gates accidental activation; the two named SMS config tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedup columns and read/write sync functions to models.py</name>
  <read_first>
    - models.py (initialize_db CREATE TABLE; get_items_sync / update_item_purchased_sync as the *_sync analog)
    - 05-RESEARCH.md "Pattern 6: Dedup Edge-Trigger Logic" (column types + the three sync function signatures)
    - tests/conftest.py (tmp_data_dir fixture)
  </read_first>
  <action>
    Extend models.py for NOTIF-02 dedup state. Add the two columns to the items table:
    `last_seen_available INTEGER NOT NULL DEFAULT 0` and `last_notified TEXT` (nullable
    ISO-8601 UTC). In initialize_db, after CREATE TABLE IF NOT EXISTS, add an idempotent
    guard: read existing column names via `PRAGMA table_info(items)` and run
    `ALTER TABLE items ADD COLUMN ...` only for columns not already present (handles
    pre-existing DBs without recreating). Add three sync functions mirroring the existing
    `*_sync` style and using `get_db_connection()`:
    `get_item_notification_state_sync(link) -> tuple[bool, str | None]` returning
    (last_seen_available as bool, last_notified); `set_item_available_sync(link, notified_at: str)`
    setting last_seen_available=1 and last_notified=notified_at; and
    `clear_item_available_sync(link)` setting last_seen_available=0 (leave last_notified).
    Keep functions under 30 lines; no bare except.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - models.py contains "last_seen_available" and "last_notified" and the three named functions.
    - initialize_db on a fresh temp DB creates items with both new columns.
    - Calling initialize_db twice (simulating a restart) does not error and does not duplicate columns (idempotent ALTER guard).
    - set_item_available_sync then get_item_notification_state_sync returns (True, the timestamp); clear_item_available_sync returns last_seen_available False.
  </acceptance_criteria>
  <done>Dedup columns exist, ALTER guard is idempotent, and the three state functions round-trip via the WAL connection; models tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create notifications package, Notifier ABC + NotificationEvent, and the failing test scaffold</name>
  <read_first>
    - core/plugin_base.py (ABC + abstractmethod pattern to mirror for Notifier)
    - 05-RESEARCH.md "Pattern 1: Notifier ABC" (NotificationEvent dataclass fields + async send signature)
    - 05-VALIDATION.md "Per-Requirement Verification Map" + 05-RESEARCH.md "Phase Requirements -> Test Map" (the 14 test names)
    - tests/conftest.py (where to add the fake notifier + event factory fixtures)
  </read_first>
  <behavior>
    - NotificationEvent carries item_name, item_url, platform, timestamp (datetime), action ("detected"|"purchased").
    - Notifier is an ABC with one abstractmethod: async send(event) -> None; a non-overriding subclass cannot be instantiated.
    - conftest provides a fake notifier fixture (records calls; can be configured to raise) and a NotificationEvent factory fixture.
    - test_notifications.py declares all 14 named tests from the test map; tests for behavior not yet implemented are marked xfail/skip so the suite stays green at this wave.
  </behavior>
  <action>
    Create `notifications/__init__.py` exporting `Notifier`, `NotificationEvent`, and
    (forward) `NotificationDispatcher` (import-guarded so it does not fail before Plan 04;
    export the names available at this wave). Create `notifications/base.py` with a
    `@dataclass NotificationEvent` (fields per RESEARCH Pattern 1) and a `Notifier(ABC)`
    exposing `@abstractmethod async def send(self, event: NotificationEvent) -> None`.
    In tests/conftest.py add: a `fake_notifier` factory fixture returning a Notifier subclass
    whose `send` records each event and optionally raises a configured exception; and a
    `notification_event` factory fixture building a NotificationEvent with sensible defaults.
    Create tests/test_notifications.py declaring the 14 tests named in the VALIDATION test map
    (test_failing_notifier_does_not_block, test_all_notifiers_called, test_dedup_*,
    test_sound_notifier_*, test_discord_*, test_email_*, test_sms_*). The two SMS-config tests
    and any test that depends only on base.py/config/models should be real and pass now;
    tests depending on notifier/dispatcher classes from Plans 02-04 must be marked
    `@pytest.mark.xfail(reason="implemented in plan 0X", strict=False)` or skipped so the suite
    stays green. Do NOT inline notifier implementations here.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_notifications.py -q && .venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - notifications/base.py contains "class Notifier" and "class NotificationEvent" (or @dataclass NotificationEvent).
    - `from notifications.base import Notifier, NotificationEvent` succeeds; instantiating a Notifier subclass that does not override send raises TypeError.
    - tests/test_notifications.py defines all 14 named test functions from the test map.
    - Full suite (`pytest tests/ -q`) is green: passing real tests + xfail/skip markers on not-yet-implemented behavior; the 72 existing tests still pass.
    - conftest exposes a fake_notifier factory (can raise) and a notification_event factory.
  </acceptance_criteria>
  <done>The notifier contract and event shape are fixed and importable; the full named test scaffold exists and the suite is green via xfail/skip on pending behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml -> AppConfig | Operator-supplied YAML; non-secret settings only |
| environment -> AppConfig validator | Secrets (TWILIO_*) read at startup for the SMS gate |
| poll coroutine -> SQLite | Dedup state writes cross into the DB (serialized later via write queue) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | SmsConfig (accidental SMS charges) | mitigate | model_validator fires at AppConfig construction; sms.enabled=true without TWILIO_* env raises a clear ValueError (NOTIF-06); SMS disabled by default |
| T-05-02 | Information Disclosure | NotificationsConfig fields | mitigate | No secret is a config field; webhook URL / SMTP password / Twilio token are env-only, never serialized into the schema |
| T-05-03 | Tampering | dedup columns / ALTER guard | accept | DB is local single-user; idempotent ALTER guard prevents accidental column duplication; no external write path introduced here |
| T-05-SC | Tampering | npm/pip installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit confirms all stdlib/existing); no install task |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/ -q` is green (existing 72 + new scaffold).
- SMS startup gate: enabled+no-creds raises; disabled default does not.
- Dedup columns present and idempotent across repeated initialize_db.
- Notifier/NotificationEvent importable from notifications.base.
</verification>

<success_criteria>
- NotificationsConfig on AppConfig with the four submodels; SMS validator gates activation (NOTIF-06).
- last_seen_available + last_notified columns and three state functions on models.py (NOTIF-02 foundation).
- Notifier ABC + NotificationEvent fixed contract; full 14-test scaffold present; suite green.
</success_criteria>

<output>
Create `.planning/phases/05-notification-system/05-01-SUMMARY.md` when done.
</output>
