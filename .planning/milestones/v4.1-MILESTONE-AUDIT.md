---
milestone: v4.1
name: Dashboard & Observability
audited: 2026-06-30T17:06:00Z
status: tech_debt
scores:
  requirements: 16/16
  phases: 6/6
  integration: 6/6   # cross-phase boundaries WIRED (5 design boundaries + 29.1 fixes)
  flows: 5/5          # E2E flows complete
gaps: {}              # no critical blockers; no unsatisfied or orphaned requirements
nyquist:
  compliant_phases: [25, 26, 27, 28, 29, 29.1]
  partial_phases: []
  missing_phases: []
  overall: COMPLIANT   # all 6 VALIDATION.md frontmatter nyquist_compliant: true
tech_debt:
  - phase: 25-design-system
    items:
      - "Warning (UI-03): Jinja2 SSR items-table remove buttons have no click handler; non-functional if loadItems() fetch fails (graceful-degradation gap, not XSS; data-link attr is safe). OPEN since 2026-06-28 audit; not in 29.1 scope."
      - "Doc-status lag: 25-VALIDATION.md frontmatter status:planned / wave_0_complete:false despite passing verification (nyquist_compliant flag itself is true)."
      - "Benign deviation: plan specified CSS @import; executor used HTML <link> (documented, functionally equivalent). escHtml() helper defined but currently unused (reserved for SVG edge cases)."
  - phase: 26-read-only-api-endpoints
    items:
      - "Doc-status lag: 26-VALIDATION.md frontmatter status:planned / wave_0_complete:false (nyquist flag true)."
      - "Scoped deferral (by design): OBS-08 plugin-tag log filter deferred (logger.py emits no [PLUGIN_NAME] tag); level+search filtering delivered per REQUIREMENTS wording."
  - phase: 27-sse-infrastructure
    items:
      - "Doc-status lag: 27-VALIDATION.md frontmatter status:planned / wave_0_complete:false (nyquist flag true)."
      - "Warning (SSE-03 spirit): get_status() / SSE status frames include raw last_heartbeat monotonic float (key lacks _ prefix so get_snapshot() does not strip it). Frontend ignores it; no credentials exposed; credential-scrub letter fully met. Cosmetic: rename to _last_heartbeat or add to strip list in core/health.py."
  - phase: 28-frontend-observability-surfaces
    items:
      - "Metadata-traceability gap: 28-01..28-04 SUMMARY frontmatter carry NO requirements field; OBS-01/02/03/04/06/09 are covered + VERIFIED in 28-VERIFICATION coverage table but not echoed in SUMMARY frontmatter (doc-consistency nit, not a delivery gap)."
  - phase: cross-milestone
    items:
      - "16 deferred live-UAT items (browser/live-socket only): P27 (3) keepalive timing, real bot-thread running-flip, real TCP disconnect; P28 (5) health cards, uptime, confirmed table, price chart, log viewer behavior; P29 (5) one-stream-replaces-polls, live health <2s, no-dup at backfill boundary, clean reconnect, polling fallback; P29.1 (3) cold-load chart no ReferenceError, stall->fallback timing, repeated-msg after filter repaint. Pre-accepted UAT debt (same posture as v4.0)."
---

# Milestone v4.1 — Dashboard & Observability — Audit Report

**Audited:** 2026-06-30 (refresh; supersedes 2026-06-28 audit)
**Status:** tech_debt (no blockers; accumulated deferred debt for review)
**Phases:** 25-29 + inserted 29.1 (6/6 complete)
**Auditor:** Claude (gsd-audit-milestone orchestrator) + gsd-integration-checker + verification aggregator

## Verdict

All 16 milestone requirements are satisfied and verified end-to-end. Cross-phase
integration is fully wired (6/6 boundaries, 0 broken, 0 orphaned, 0 missing). All 5
E2E flows complete. No critical blockers.

This refresh supersedes the 2026-06-28 audit. The prior audit listed 4 integration
warnings; the inserted Phase 29.1 (completed 2026-06-30) closed 3 of them (OBS-05 uPlot
load order, OBS-07 log-dup guard, SSE-01/02 SSE-stall watchdog plus REST fallback), each
re-verified with file:line evidence and 5 new GREEN tests. The prior audit's stale-Nyquist
finding for phases 28/29 is also resolved: both VALIDATION.md files were flipped to
`nyquist_compliant: true` / `status: validated` on 2026-06-30.

Status remains `tech_debt` rather than `passed` because of remaining non-blocking debt:
1 open low-severity warning (UI-03 SSR remove-button), 1 new cosmetic warning
(`last_heartbeat` in the status payload), 16 deferred live-UAT items, and minor doc-status
lag on three VALIDATION.md files.

## Requirements Coverage (3-source cross-reference)

16/16 satisfied. Sources: phase VERIFICATION.md requirements tables (evidence-backed),
SUMMARY.md `requirements` frontmatter, REQUIREMENTS.md traceability `[x]`, plus the
integration checker's per-requirement wiring map. Zero unsatisfied. Zero orphaned.

| REQ | Phase | VERIFICATION | SUMMARY frontmatter | Integration | Final |
|-----|-------|--------------|---------------------|-------------|-------|
| UI-01 | 25 | passed | yes | WIRED | satisfied |
| UI-02 | 25 | passed | yes | WIRED | satisfied |
| UI-03 | 25 | passed | yes | WIRED (Warn A) | satisfied |
| UI-04 | 25 | passed | yes | WIRED | satisfied |
| OBS-01 | 28 | passed (auto) | no* | WIRED | satisfied |
| OBS-02 | 28 | passed (auto) | no* | WIRED | satisfied |
| OBS-03 | 28 | passed (auto) | no* | WIRED | satisfied |
| OBS-04 | 28 | passed (auto) | no* | WIRED | satisfied |
| OBS-05 | 28 + 29.1 | passed (29.1) | yes (29.1) | WIRED | satisfied |
| OBS-06 | 28 | passed (auto) | no* | WIRED | satisfied |
| OBS-07 | 28 + 29.1 | passed (29.1) | yes (29.1) | WIRED | satisfied |
| OBS-08 | 26 | passed | yes | WIRED | satisfied |
| OBS-09 | 28 | passed (auto) | no* | WIRED | satisfied |
| SSE-01 | 29 + 29.1 | passed (29.1) | yes (29.1) | WIRED | satisfied |
| SSE-02 | 27 + 29.1 | passed (29.1) | yes (29.1) | WIRED | satisfied |
| SSE-03 | 26 | passed | yes | WIRED (Warn B) | satisfied |

> *OBS-01/02/03/04/06/09: Phase 28 SUMMARY frontmatter (28-01..28-04) carries no
> `requirements` field. Each is independently confirmed SATISFIED by 28-VERIFICATION.md's
> requirements-coverage table (with file:line evidence) and by the integration checker's
> wiring map. This is a doc-consistency nit, not a coverage gap.

## Phase Verification Summary

| Phase | Status | Auto Score | Critical Gaps | Notes |
|-------|--------|-----------|---------------|-------|
| 25 Design System | passed | 5/5 | none | token-driven CSS, FOUC guard, XSS fix, uPlot vendored; @import to <link> benign deviation |
| 26 Read-Only API Endpoints | passed | 10/10 | none | MOD-02 compliant; credential-leak guard green; OBS-08 plugin-tag scoped-deferred |
| 27 SSE Infrastructure | human_needed | 4/4 | none | _poll_loop sole-producer bridge; 3 live-socket UAT items deferred |
| 28 Frontend Observability | human_needed | 9/9 | none | 4 surfaces rendered; 5 live-browser UAT items deferred |
| 29 SSE Client Wiring | human_needed | 9/9 | none | EventSource replaces polling; 5 live-browser UAT items deferred |
| 29.1 v4.1 Tech-Debt Cleanup | human_needed | 9/9 | none | closes 3 prior warnings; 14 wiring tests + full suite (807 passed, 2 skipped); 3 live-runtime UAT items deferred |

`human_needed` on 27/28/29/29.1 reflects un-automatable live-browser / live-socket checks
only. Every automated assertion passed and no automated gap was found.

## Cross-Phase Integration

Integration checker (gsd-integration-checker) read the actual source (not summaries) and
verified every boundary. 12 exports/bindings wired, 7 routes consumed, 0 orphaned routes.

- **28 to 26 (Frontend to REST):** WIRED. loadConfirmedBuys -> /api/history, loadPriceChart -> /api/price-history/{link_b64} (URL-safe b64 encode/decode confirmed compatible), pollLogs -> /api/logs (level/search/n), pollStatus -> /api/status. Shapes consistent.
- **29 to 27 (Frontend to SSE):** WIRED. EventSource('/api/events') resolves via include_router prefix="/api" + @router.get("/events"). Event names 'status'/'log' match SseHub.broadcast emissions exactly.
- **27 producer chain:** WIRED. _poll_loop launched in lifespan via asyncio.create_task; reads get_status + tail_log_lines via asyncio.to_thread; frame format matches generator yield and browser expectation; generator unsubscribes in finally (no queue leak).
- **Health contract (28 / 26 / 27):** WIRED. get_snapshot() keys (status, heartbeat_age_secs, consecutive_errors, items_checked, orders_confirmed, last_error) all present and consumed; private _-prefixed keys stripped; last_error scrubbed to class name (SSE-03).
- **25 to 28 (design consumption):** WIRED. All component classes referenced by render functions exist in components.css; CSS color tokens read via getComputedStyle; uPlot global vendored and loaded synchronously in <head> before first new uPlot() call.
- **29.1 fixes:** WIRED. SSE idle watchdog (SSE_STALL_MS=8000) + guarded REST fallback timer + unconditional setIndicator('live') recovery; uPlot loader in <head>; _dedupNextLine one-shot boundary guard.

**E2E flows (5/5 complete):**
- A. Open dashboard -> one-shot REST backfill -> SSE connect -> live updates
- B. Bot start/stop -> HealthRegistry -> get_status -> _poll_loop -> status frame -> renderHealthCards
- C. Log write -> tail_log_lines cursor -> SSE log frame -> appendLogLine (no dup at backfill boundary)
- D. SSE stall (no close) -> idle watchdog -> REST fallback polls -> indicator reflects state
- E. No EventSource support -> polling fallback, no JS error (else branch references no SSE vars)

## Tech Debt (non-blocking)

### Integration warnings (2 open)

The 2026-06-28 audit had 4 warnings; 29.1 closed 3. Remaining:

1. **Warning A — UI-03 / Phase 25** (carried open). Jinja2 SSR items-table remove buttons have no click handler; dead if `loadItems()` fetch fails. Graceful-degradation gap, not an XSS vector (`data-link` attribute is safe). Not in 29.1 scope. Low severity.
2. **Warning B — SSE-03 spirit / Phase 27** (new this refresh). `get_status()` and SSE `status` frames include the raw `last_heartbeat` monotonic float because the key lacks the `_` prefix that `get_snapshot()` strips. Frontend ignores it; no credentials or sensitive data exposed; SSE-03's credential-scrub requirement is fully met. Cosmetic: rename to `_last_heartbeat` or add to a strip list in `core/health.py`.

### Closed since prior audit (3)

- **OBS-05 / Phase 28** — uPlot loader relocated to `<head>` synchronous; cold-load ReferenceError hazard removed (29.1-02).
- **OBS-07 / Phase 28** — `appendLogLine` blanket `_lastLogLine` equality drop replaced with `_dedupNextLine` one-shot backfill-boundary guard; repeated bot messages no longer suppressed (29.1-03).
- **SSE-01/02 / Phase 29** — SSE idle watchdog + REST fallback added; indicator no longer stuck "Live" on a stalled-but-open stream (29.1-04).

### Deferred live-UAT items (16)

Inherent un-automatable live-browser / live-socket checks, explicitly planned as operator
UAT and pre-accepted as debt (same posture as v4.0):
- **P27 (3):** keepalive 15s interval, real bot-thread running-flip, real TCP disconnect cleanup
- **P28 (5):** health cards render, header uptime, confirmed-orders table rows, price chart + empty state, log viewer color/filter/follow/500-cap
- **P29 (5):** one-stream-replaces-two-polls, live health update <2s, no-dup at backfill boundary, clean reconnect on tab close/reopen, polling fallback with no JS error
- **P29.1 (3):** cold-load chart no `ReferenceError: uPlot`, stall to fallback timing (~8-10s flip), repeated bot messages visible after filter repaint

### Nyquist coverage

| Phase | VALIDATION.md | nyquist_compliant | status field | Action |
|-------|---------------|-------------------|--------------|--------|
| 25 | exists | true | planned | doc-status lag only (flag true) |
| 26 | exists | true | planned | doc-status lag only (flag true) |
| 27 | exists | true | planned | doc-status lag only (flag true) |
| 28 | exists | true | validated | none (flipped 2026-06-30) |
| 29 | exists | true | validated | none (flipped 2026-06-30) |
| 29.1 | exists | true | passed | none |

All 6 phases `nyquist_compliant: true`. Phases 25/26/27 carry a stale `status: planned`
field but the Nyquist flag itself is true and their suites are GREEN. Doc-status lag, not
a coverage hole.

### Anti-patterns

None. Zero TODO/FIXME/XXX/stub/placeholder in any phase's production code. Only INFO-level
notes: a `str(exc)` reference inside an sse_hub.py docstring (executable path uses
`exc.__class__.__name__`), and two allowlisted clear-only/static `innerHTML` uses in
dashboard.html.

## Recommendation

No blockers prevent milestone completion. Both remaining warnings are low severity
(Warning A graceful-degradation, Warning B cosmetic with no security impact). The 16
live-UAT items match the v4.0 pre-accepted posture. Decision on this run: complete the
milestone now and track Warning A, Warning B, the 16 live-UAT items, and the Phase 28
SUMMARY-frontmatter nit as backlog.

*Audited 2026-06-30 by gsd-audit-milestone (refresh).*
