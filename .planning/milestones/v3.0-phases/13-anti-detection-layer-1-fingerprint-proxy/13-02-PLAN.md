---
phase: 13-anti-detection-layer-1-fingerprint-proxy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/config_schema.py
  - sample.config.yml
  - tests/test_proxy_config.py
autonomous: true
requirements: [ANTI-04]
must_haves:
  truths:
    - "Users can declare an opt-in proxy: section in config.yml with enabled, urls, max_failures, cooldown_secs"
    - "proxy.enabled defaults to False and proxy.urls defaults to empty (disabled by default)"
    - "ProxyConfig parses scheme://user:pass@host:port URLs from YAML into config.proxy.urls"
    - "sample.config.yml documents the proxy: section with a credential-safety warning"
  artifacts:
    - path: "core/config_schema.py"
      provides: "ProxyConfig Pydantic model nested under AppConfig"
      contains: "class ProxyConfig"
    - path: "sample.config.yml"
      provides: "Documented, disabled-by-default proxy: section"
      contains: "proxy:"
    - path: "tests/test_proxy_config.py"
      provides: "Unit coverage for proxy config parsing + defaults"
      contains: "def test_proxyconfig_defaults"
  key_links:
    - from: "AppConfig"
      to: "ProxyConfig"
      via: "proxy: ProxyConfig = ProxyConfig() field"
      pattern: "proxy:\\s*ProxyConfig"
---

<objective>
Add the opt-in `proxy:` config section. Define `ProxyConfig` as a nested Pydantic model under `AppConfig` (following the existing `NotificationsConfig`/`CredentialsConfig` conventions in `core/config_schema.py`), document a disabled-by-default `proxy:` block in `sample.config.yml`, and unit-test parsing + defaults.

Purpose: ANTI-04 requires an opt-in `proxy:` section (disabled by default) listing `scheme://[user:pass@]host:port` URLs with configurable retire/cooldown. This plan owns the config surface only; the pool/wiring is Plan 01/03.

Output: `ProxyConfig` model + `AppConfig.proxy` field, documented `sample.config.yml` section, `tests/test_proxy_config.py`. Suite stays green (baseline 359 passed, 2 skipped).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-CONTEXT.md
@.planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-RESEARCH.md

<interfaces>
<!-- Existing core/config_schema.py conventions the executor follows directly. -->
<!-- BaseModel nested models with defaults; Field(default_factory=list); model attached to AppConfig. -->

ProxyConfig shape (RESEARCH Pattern 5 — chosen contract Plan 01/03 expect):
  class ProxyConfig(BaseModel):
    enabled: bool = False
    urls: list[str] = Field(default_factory=list)
    max_failures: int = 3
    cooldown_secs: float = 300.0

AppConfig already has these sibling fields (add proxy alongside):
  selenium, debug, available, platforms, app, notifications, credentials
  -> add:  proxy: ProxyConfig = ProxyConfig()

AppConfig test-construction seam (from core/config_schema.py):
  AppConfig(yaml_file=<Path>)   # inject a tmp config.yml in tests
  model_config = SettingsConfigDict(extra="ignore", env_nested_delimiter="__")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProxyConfig model + AppConfig.proxy field</name>
  <files>core/config_schema.py, tests/test_proxy_config.py</files>
  <read_first>
    - core/config_schema.py (CredentialsConfig + NotificationsConfig conventions; AppConfig field list lines 217-233; yaml_file injection seam lines 235-271)
    - .planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-RESEARCH.md (Pattern 5 ProxyConfig)
    - tests/test_config_schema.py (existing config-parse patterns: AppConfig(yaml_file=tmp_path/...) injection)
  </read_first>
  <behavior>
    - test_proxyconfig_defaults: AppConfig() (no proxy section) -> cfg.proxy.enabled False, cfg.proxy.urls == [], cfg.proxy.max_failures == 3, cfg.proxy.cooldown_secs == 300.0.
    - test_proxyconfig_parses_from_yaml: tmp config.yml with proxy.enabled true + 2 urls + max_failures 5 + cooldown_secs 120 -> AppConfig(yaml_file=tmp) parses all four.
    - test_proxyconfig_urls_with_creds_parse: "http://u:p@1.2.3.4:8080" survives into cfg.proxy.urls unchanged (cred parsing happens in core/stealth, not here).
    - test_appconfig_has_proxy_field: hasattr(AppConfig(), "proxy") and isinstance(cfg.proxy, ProxyConfig).
  </behavior>
  <action>
    In `core/config_schema.py`, add `class ProxyConfig(BaseModel)` after `CredentialsConfig`, before `AppConfig`, with the 4 fields from the interfaces block (RESEARCH Pattern 5; ANTI-04). Docstring notes: opt-in, disabled by default, credentials in url strings are never logged. Then add `proxy: ProxyConfig = ProxyConfig()` to the AppConfig field block (alongside `credentials`). Do NOT add a model_validator that reads os.environ (proxy urls live in config.yml, not the credential store — RESEARCH Open Question 3). Create `tests/test_proxy_config.py` with the 4 named tests using the `AppConfig(yaml_file=...)` injection seam from tests/test_config_schema.py.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_proxy_config.py -q` exits 0 (all 4 tests green).
    - Source assertion: core/config_schema.py contains `class ProxyConfig` and `proxy: ProxyConfig`.
    - Behavior assertion: AppConfig().proxy.enabled is False by default; no os.environ read in ProxyConfig.
    - Full suite stays green (no regression vs baseline 359 passed, 2 skipped).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_proxy_config.py -q</automated>
  </verify>
  <done>ProxyConfig exists with enabled/urls/max_failures/cooldown_secs; AppConfig.proxy present; all 4 tests green. No os.environ read in ProxyConfig.</done>
</task>

<task type="auto">
  <name>Task 2: Document disabled-by-default proxy: section in sample.config.yml</name>
  <files>sample.config.yml</files>
  <read_first>
    - sample.config.yml (existing section style: platforms:, debug:, available:, notifications: — match indentation + comment style)
    - .planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-CONTEXT.md (locked: startup log line, disabled by default, scheme://[user:pass@]host:port format)
    - tests/test_no_plaintext.py (test_no_plaintext_secrets_in_config_yml — sample.config.yml must contain no real credentials)
  </read_first>
  <action>
    Add a top-level `proxy:` block to `sample.config.yml` with `enabled: false`, an empty `urls: []` plus a COMMENT example showing the `scheme://[user:pass@]host:port` format with a PLACEHOLDER host (e.g. `# - http://user:pass@proxy.example.com:8080`), `max_failures: 3`, `cooldown_secs: 300`. Add a comment warning: proxy URLs with user:pass are stored locally in config.yml (gitignored); credentials are never logged (only host:port + the line `Proxy rotation: enabled, pool_size=N`). Match the existing 2-space indentation and inline-comment style.
  </action>
  <acceptance_criteria>
    - `python -c "import yaml; d=yaml.safe_load(open('sample.config.yml')); assert d.get('proxy',{}).get('enabled') is False; print('OK')"` prints OK.
    - `python -m pytest tests/test_no_plaintext.py -q` exits 0 (no real credentials in sample).
    - Source assertion: sample.config.yml contains a `proxy:` key and a placeholder-host comment example only.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('sample.config.yml')); assert d.get('proxy',{}).get('enabled') is False; print('OK')"</automated>
  </verify>
  <done>sample.config.yml has a documented proxy: section, enabled:false, placeholder-only example URLs, retire/cooldown fields. test_no_plaintext_secrets_in_config_yml still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml → AppConfig.proxy | Untrusted local YAML (may contain proxy credentials) parsed into typed model |
| sample.config.yml → public repo | Committed example must never carry real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information Disclosure | sample.config.yml proxy example | mitigate | Example URLs use placeholder host (proxy.example.com); no real user:pass. test_no_plaintext_secrets_in_config_yml enforces this in CI. |
| T-13-05 | Information Disclosure | ProxyConfig.urls in config.yml | accept | config.yml is gitignored + local; proxy creds are lower-risk than account creds. RESEARCH Open Question 3 + CONTEXT lock keep them in config.yml. Warning comment added in sample. |
| T-13-06 | Tampering | proxy: YAML parsing | mitigate | Pydantic typed model (bool/list[str]/int/float); extra="ignore" blocks unknown-key injection; no eval/exec. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs. RESEARCH audit: no new packages. |
</threat_model>

<verification>
- `python -m pytest tests/test_proxy_config.py -q` exits 0.
- `python -m pytest tests/test_no_plaintext.py -q` exits 0 (sample.config.yml clean).
- `python -m pytest -q` exits 0, no regression vs baseline.
- `python -c "import yaml; yaml.safe_load(open('sample.config.yml'))"` parses without error.
</verification>

<success_criteria>
- AppConfig exposes a typed `proxy` section (ANTI-04 config surface), disabled by default.
- sample.config.yml documents the section with no real credentials.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/13-anti-detection-layer-1-fingerprint-proxy/13-02-SUMMARY.md` when done.
</output>
