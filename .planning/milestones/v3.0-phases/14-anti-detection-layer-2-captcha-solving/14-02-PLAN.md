---
phase: 14-anti-detection-layer-2-captcha-solving
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - core/service.py
  - core/orchestrator.py
  - core/registry.py
  - tests/test_captcha_wiring.py
autonomous: true
requirements: [ANTI-06, ANTI-07]
must_haves:
  truths:
    - "A CaptchaSolver is constructed fresh inside async_main from cfg.captcha + get_store() (not in BotService.__init__) so solve_count resets each run"
    - "When captcha.enabled is true, the startup balance check runs and logs (WARNING on low, balance status on zero) without ever logging the key"
    - "registry.assign_solver injects the solver onto each plugin as self._captcha_solver, mirroring assign_proxy"
    - "When captcha is disabled or no key is set, plugins receive self._captcha_solver = None (no crash)"
  artifacts:
    - path: "core/orchestrator.py"
      provides: "CaptchaSolver construction in async_main + balance check + pass to PluginRegistry"
      contains: "CaptchaSolver.from_config"
    - path: "core/registry.py"
      provides: "assign_solver(plugin) method mirroring assign_proxy"
      contains: "def assign_solver"
    - path: "core/service.py"
      provides: "startup INFO log when captcha solving is enabled (no key logged)"
      contains: "captcha"
    - path: "tests/test_captcha_wiring.py"
      provides: "wiring tests: assign_solver, disabled->None, balance check call"
  key_links:
    - from: "core/orchestrator.py async_main"
      to: "CaptchaSolver.from_config(cfg.captcha, get_store())"
      via: "fresh solver per run"
      pattern: "CaptchaSolver\\.from_config"
    - from: "core/registry.py PluginRegistry"
      to: "plugin._captcha_solver"
      via: "assign_solver injection"
      pattern: "_captcha_solver"
    - from: "core/orchestrator.py _staggered_setup"
      to: "registry.assign_solver(plugin)"
      via: "per-plugin injection before setup"
      pattern: "assign_solver"
---

<objective>
Wire the CaptchaSolver into the running bot exactly as Phase 13 wired ProxyPool:
construct the solver in async_main from cfg.captcha + the credential store, run the
startup balance check, pass it through PluginRegistry, and inject it onto each active
plugin via registry.assign_solver before setup(). Add a startup INFO log in BotService.

Purpose: Make the solver reachable by plugins (Plan 03) without any plugin ABC change,
while guaranteeing solve_count resets per run and the key is never logged.
Output: solver construction + balance check + assign_solver wiring + wiring unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-anti-detection-layer-2-captcha-solving/14-CONTEXT.md
@.planning/phases/14-anti-detection-layer-2-captcha-solving/14-RESEARCH.md
@.planning/phases/14-anti-detection-layer-2-captcha-solving/14-01-SUMMARY.md

<interfaces>
<!-- The ProxyPool wiring is the exact analog. Mirror it for CaptchaSolver. -->

From core/captcha.py (Plan 01 output):
```python
class CaptchaSolver:
    @classmethod
    def from_config(cls, cfg, store) -> "CaptchaSolver | None": ...  # None when disabled/no key
    def check_balance_at_startup(self) -> None: ...   # blocking HTTP; sets balance_ok
    def can_solve(self) -> bool: ...
    def solve_recaptcha(self, sitekey, pageurl) -> str: ...  # blocking
```

From core/orchestrator.py — the ProxyPool flow to mirror (async_main lines 221-237):
```python
proxy_pool = None
if getattr(getattr(cfg, "proxy", None), "enabled", False):
    proxy_pool = ProxyPool.from_urls(cfg.proxy.urls, cfg.proxy.max_failures, cfg.proxy.cooldown_secs)
registry = PluginRegistry(cfg, plugins_dir, proxy_pool=proxy_pool)
...
# _staggered_setup calls: registry.assign_proxy(plugin); await plugin.setup()
```

From core/registry.py — assign_proxy is the exact analog (lines 67-91):
```python
def __init__(self, config, plugins_dir, proxy_pool=None) -> None: ...
def assign_proxy(self, plugin) -> None:
    if self._proxy_pool is None:
        plugin._proxy_required = False; plugin._proxy = None; return
    ...
```

From core/service.py — proxy startup log to mirror (BotService.__init__ lines 44-46):
```python
if self._cfg.proxy.enabled:
    n = len(self._cfg.proxy.urls)
    writeLog(f"Proxy rotation: enabled, pool_size={n}", "INFO")
```

From core/credentials.py: `get_store()` returns the process-wide CredentialStore.
The blocking balance check must run via loop.run_in_executor (Pitfall 2: never block the loop).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct solver in async_main + balance check + PluginRegistry.assign_solver</name>
  <files>core/orchestrator.py, core/registry.py, tests/test_captcha_wiring.py</files>
  <read_first>
    - core/orchestrator.py (async_main lines 221-237; _staggered_setup lines 171-194) — where ProxyPool is built and assign_proxy is called
    - core/registry.py (PluginRegistry.__init__ + assign_proxy lines 60-91) — the method to mirror
    - core/captcha.py (Plan 01 CaptchaSolver API)
    - core/credentials.py (get_store)
    - .planning/phases/14-anti-detection-layer-2-captcha-solving/14-RESEARCH.md (Pitfall 2, Pitfall 7: fresh solver per run; Architecture diagram)
  </read_first>
  <behavior>
    - PluginRegistry accepts an optional captcha_solver param and stores it (self._captcha_solver)
    - assign_solver(plugin) sets plugin._captcha_solver = self._captcha_solver (or None when no solver)
    - async_main builds solver via CaptchaSolver.from_config(cfg.captcha, get_store()) only when cfg.captcha.enabled
    - When solver is not None, check_balance_at_startup runs via run_in_executor (not blocking the loop)
    - When captcha disabled, no solver constructed and assign_solver sets plugin._captcha_solver = None
    - _staggered_setup calls registry.assign_solver(plugin) alongside the existing assign_proxy call
  </behavior>
  <action>
    In core/registry.py: add captcha_solver=None to PluginRegistry.__init__ and store it as
    self._captcha_solver. Add assign_solver(plugin) mirroring assign_proxy: when
    self._captcha_solver is None, set plugin._captcha_solver = None and return; otherwise set
    plugin._captcha_solver = self._captcha_solver. In core/orchestrator.py async_main: after the
    existing proxy_pool block, build captcha_solver = None, then if
    getattr(getattr(cfg,"captcha",None),"enabled",False) call
    CaptchaSolver.from_config(cfg.captcha, get_store()) (import get_store + CaptchaSolver at top).
    If the returned solver is not None, await loop.run_in_executor(None, solver.check_balance_at_startup)
    BEFORE the TaskGroup starts (Pitfall 2: balance check is blocking HTTP — must use the executor).
    Pass captcha_solver=captcha_solver into PluginRegistry(...). In _staggered_setup, add
    registry.assign_solver(plugin) right after the existing registry.assign_proxy(plugin) line.
    Keep functions under 30 lines (extract a small _build_captcha_solver helper in orchestrator if
    needed). Write tests/test_captcha_wiring.py covering assign_solver injection, the disabled->None
    path, and (with a mocked CaptchaSolver) that check_balance_at_startup is invoked when enabled.
    Implements ANTI-06 (wiring) and ANTI-07 (startup balance check invocation).
  </action>
  <verify>
    <automated>python -m pytest tests/test_captcha_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_captcha_wiring.py -q` exits 0
    - `grep -n "def assign_solver" core/registry.py` returns a match
    - `grep -n "assign_solver" core/orchestrator.py` returns a match (called in _staggered_setup)
    - `grep -n "run_in_executor" core/orchestrator.py | grep -i balance` OR a test asserting check_balance_at_startup runs via the executor (not directly awaited) — balance check never runs inline on the loop
    - Test asserts a plugin gets _captcha_solver = None when no solver is configured
    - Test asserts CaptchaSolver.from_config is NOT called when cfg.captcha.enabled is False (fresh-per-run + disabled path)
  </acceptance_criteria>
  <done>PluginRegistry.assign_solver mirrors assign_proxy; async_main builds the solver fresh per run from cfg.captcha + get_store(), runs the startup balance check via run_in_executor, and injects the solver into every plugin during _staggered_setup; wiring tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: BotService startup log for enabled captcha solving (no key)</name>
  <files>core/service.py, tests/test_captcha_wiring.py</files>
  <read_first>
    - core/service.py (BotService.__init__ proxy log lines 33-46) — the exact log pattern to mirror
    - .planning/phases/14-anti-detection-layer-2-captcha-solving/14-CONTEXT.md (never log the key)
  </read_first>
  <action>
    In BotService.__init__ (after the existing proxy-enabled log block), add an INFO log when
    self._cfg.captcha.enabled is true, of the form
    "CAPTCHA solving: enabled, max_solves_per_run={n}, low_balance_threshold={t}" using only the
    integer cap and float threshold from cfg.captcha — NEVER the API key (the key is read later in
    async_main via get_store, not here). Do not construct the solver here (Pitfall 7: solver is built
    fresh in async_main). Extend tests/test_captcha_wiring.py with a caplog test asserting the INFO
    line appears when enabled and is absent when disabled, and that no secret-looking value is logged.
    Implements ANTI-06 (operator visibility that solving is on).
  </action>
  <verify>
    <automated>python -m pytest tests/test_captcha_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_captcha_wiring.py -q` exits 0
    - caplog test asserts the "CAPTCHA solving: enabled" INFO line is present when cfg.captcha.enabled=True
    - caplog test asserts the line is absent when cfg.captcha.enabled=False
    - Test asserts the logged line contains max_solves_per_run and low_balance_threshold but NO api key string
  </acceptance_criteria>
  <done>BotService logs an enabled-captcha startup line (cap + threshold only, never the key); test proves presence/absence by config and no key leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| credential store → async_main | API key value enters the orchestrator at solver construction |
| BotService.__init__ → logs | startup log must expose config knobs but never the key |
| async_main → event loop | blocking balance-check HTTP must not run on the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-log | Information Disclosure | core/service.py startup log | mitigate | Log only cap + threshold ints/floats; caplog test asserts no key string present |
| T-14-block | Denial of Service | async_main balance check | mitigate | check_balance_at_startup runs via run_in_executor; never awaited inline on the loop (Pitfall 2) |
| T-14-stale | Tampering | solver lifetime | mitigate | Solver constructed fresh in async_main (not BotService.__init__) so solve_count resets per run (Pitfall 7) |
| T-14-abc | Tampering | plugin contract | mitigate | Injection via assign_solver attribute only; no PLUGIN_API_VERSION bump, no ABC method added (Pitfall 8) |
</threat_model>

<verification>
- `python -m pytest tests/test_captcha_wiring.py -q` green
- Full suite `python -m pytest` no regression vs baseline (419 passed, 2 skipped)
- PLUGIN_API_VERSION unchanged (still 2); no new abstract method on RetailerPlugin
- API key never logged in BotService startup
</verification>

<success_criteria>
CaptchaSolver constructed fresh per run from cfg.captcha + credential store; startup
balance check runs off the event loop; assign_solver injects the solver onto every
plugin mirroring assign_proxy; disabled path yields None with no crash; no ABC bump.
</success_criteria>

<output>
Create `.planning/phases/14-anti-detection-layer-2-captcha-solving/14-02-SUMMARY.md` when done
</output>
