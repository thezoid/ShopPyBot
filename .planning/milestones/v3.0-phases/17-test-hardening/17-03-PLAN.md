---
phase: 17-test-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_price_history.py
  - tests/test_price_alert.py
autonomous: true
requirements: [STAB-03]

must_haves:
  truths:
    - "A hand-built v2.0 items DB (no v3.0 cols, no price_history) migrates in place: new cols + table added, legacy row survives, 2nd run is idempotent (PR-01) — THE criterion-3 migration fixture"
    - "The pct-helper denominator guards return 0.0 when target/last <= 0 (PR-02)"
    - "_evaluate_price_triggers early-returns when the item row is None or both config fields are None (PR-03)"
    - "get_price_alert_state / get_item_price_config return their row-None sentinels for a missing item (PR-04)"
    - "A get_price exception is caught/logged and the cycle continues; availability path still runs (AB-01)"
    - "get_price runs even when available is False, and is skipped when check_availability raises (AB-02)"
  artifacts:
    - path: "tests/test_price_history.py"
      provides: "PR-01 v2.0 migration fixture + PR-04 row-None sentinels"
      contains: "test_v2_schema_db_migrates_to_v3_in_place"
    - path: "tests/test_price_alert.py"
      provides: "PR-02/PR-03 trigger guards + AB-01/AB-02 get_price integration"
      contains: "test_get_price_error_is_isolated_and_does_not_propagate"
  key_links:
    - from: "tests/test_price_history.py"
      to: "models.py:48-81"
      via: "raw sqlite3 builds a v2.0 items table at models.DB_PATH then initialize_db() (no delete)"
      pattern: "PRAGMA table_info"
    - from: "tests/test_price_alert.py"
      to: "core/orchestrator.py:70,77,123,126,209-210"
      via: "_check_and_buy / _evaluate_price_triggers / _pct_* with a fake_plugin"
      pattern: "_check_and_buy|_evaluate_price_triggers|_pct_"
---

<objective>
Add the price-surface coverage tests (PR-01..PR-04) plus the two get_price()-alongside-check_availability integration tests (AB-01, AB-02) for STAB-03 criteria 3 and the integration half of criterion 4. The headline deliverable is PR-01: the v2.0-schema → v3.0 in-place migration fixture test (the criterion-3 required gap, currently MISSING). Every test targets a genuinely uncovered line/branch named in 17-RESEARCH.md and is deterministic + CI-safe (raw sqlite3 under tmp_data_dir; no browser/network).

Purpose: CI must catch regressions in the idempotent DB migration against a real v2.0 DB, the price-trigger denominator/early-return guards, the row-None sentinels, and the get_price hook's error-isolation + sequencing in the poll cycle.
Output: Extended tests/test_price_history.py and tests/test_price_alert.py with the named PR-01..PR-04, AB-01, AB-02 tests; full suite stays green (baseline 529 passed, 2 skipped; count rises).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-hardening/17-RESEARCH.md
@.planning/phases/17-test-hardening/17-CONTEXT.md

<interfaces>
<!-- Contracts under test. Use directly — no codebase exploration needed. -->

From models.py:
- `initialize_db(delete=False)`: with delete=False, does NOT drop the DB; runs the idempotent ALTER/CREATE block (models.py:48-81). The v3.0 items columns added only if absent (PRAGMA table_info guard): `last_seen_available`, `last_notified`, `target_price`, `price_drop_pct`, `price_alert_armed`, `price_last_notified`. `price_history` table is `CREATE TABLE IF NOT EXISTS` with cols id/item_link/price_cents/currency/scraped_at.
- `models.DB_PATH` is redirected by the `tmp_data_dir` fixture (sets SHOPBOT_DATA_DIR + monkeypatches models.DB_PATH to `<tmp>/shop_py_bot.db`). Build the v2.0 DB with raw `sqlite3.connect(models.DB_PATH)`.
- The v2.0 items schema (pre-v3.0) is exactly: `id INTEGER PRIMARY KEY, name TEXT NOT NULL, link TEXT NOT NULL UNIQUE, auto_buy BOOLEAN NOT NULL, quantity INTEGER NOT NULL, purchased BOOLEAN NOT NULL DEFAULT 0` — i.e. NONE of the six v3.0 columns and no price_history table.
- `get_price_alert_state_sync(link)` → `(bool, str|None)`; returns `(False, None)` when row is None (models.py:197-198).
- `get_item_price_config_sync(link)` → `(int|None, float|None) | None`; returns `None` when row is None (models.py:245-246).

From core/orchestrator.py:
- `_pct_from_target(price_cents, target_cents)` (orch:63-71): `if target_cents <= 0: return 0.0` (line 70 guard) else rounded pct.
- `_pct_drop_from_last(current_cents, last_cents)` (orch:74-78): `if last_cents <= 0: return 0.0` (line 77 guard).
- `async _evaluate_price_triggers(plugin, name, link, price_cents, prev_price, dispatcher, loop)` (orch:119-142): `item_row = get_item_price_config_sync(link)`; `if item_row is None: return` (line 123); `if target_price is None and price_drop_pct is None: return` (line 126); else evaluates triggers.
- `async _check_and_buy(plugin, name, link, auto_buy, write_queue, dispatcher=None)` (orch:196-238): awaits `check_availability` (on exception logs ERROR and returns BEFORE get_price, orch:201-203); then `price_cents = await plugin.get_price(link)` inside try/except that logs `get_price error: <class name>` and continues (orch:209-210). Price-history append + _evaluate_price_triggers happen only when `price_cents is not None and price_cents > 0`. Availability/notification path runs after, regardless of get_price outcome.

From tests/conftest.py:
- `tmp_data_dir` (redirects DB), `fake_plugin` factory (builds a no-browser RetailerPlugin with AsyncMock setup/teardown and configurable available/bought; you can attach AsyncMock get_price / check_availability with side effects). Use `asyncio.Queue()` for write_queue.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PR-01 v2.0-schema in-place migration fixture (THE criterion-3 gap) + PR-04 row-None sentinels</name>
  <files>tests/test_price_history.py</files>
  <read_first>
    - tests/test_price_history.py (existing 9 tests — reuse the `tmp_data_dir` fixture and raw `sqlite3.connect(models.DB_PATH)` + `PRAGMA table_info` assertion pattern already present)
    - models.py lines 33-81 (initialize_db + idempotent migration block) and lines 187-247 (get_price_alert_state_sync row-None at 197-198, get_item_price_config_sync row-None at 245-246)
    - tests/conftest.py (tmp_data_dir fixture semantics)
    - .planning/phases/17-test-hardening/17-RESEARCH.md rows PR-01, PR-04 (PR-01 is the highest-value entry)
  </read_first>
  <behavior>
    - PR-01 (HIGH VALUE — the criterion-3 required fixture): build, with raw sqlite3 at models.DB_PATH, a v2.0 `items` table containing ONLY the six v2.0 columns (no last_seen_available/last_notified/target_price/price_drop_pct/price_alert_armed/price_last_notified) and NO price_history table; insert one legacy row. Run `models.initialize_db()` (delete=False) to migrate in place. Assert: (a) all six v3.0 columns now exist on items (PRAGMA table_info), (b) the price_history table now exists with its 5 columns, (c) the pre-existing legacy row SURVIVED with its original name/link/auto_buy/quantity/purchased values (no data loss), (d) re-running initialize_db() a SECOND time is idempotent — no error, column set unchanged, legacy row still single and intact. Exercises models.py:48-81 against a genuine v2.0 DB (not the fresh-create path the existing tests use).
    - PR-04: with a freshly initialize_db(delete=True) DB and NO matching item inserted, assert `get_price_alert_state_sync("https://missing")` returns `(False, None)` (models.py:198) and `get_item_price_config_sync("https://missing")` returns `None` (models.py:246).
  </behavior>
  <action>
    In tests/test_price_history.py add `test_v2_schema_db_migrates_to_v3_in_place` (param `tmp_data_dir`): open `sqlite3.connect(models.DB_PATH)`, CREATE the v2.0 items table with exactly the six legacy columns (id, name, link UNIQUE, auto_buy, quantity, purchased) and INSERT one row (e.g. name="Legacy", link="https://legacy.test/x", auto_buy=0, quantity=1, purchased=0); commit + close; confirm via a pre-assert that none of the v3.0 columns and no price_history table exist yet. Call `models.initialize_db()` (no delete). Re-open and assert the six v3.0 columns are present on items, price_history exists with {id,item_link,price_cents,currency,scraped_at}, and the legacy row is unchanged (SELECT name,link,auto_buy,quantity,purchased WHERE link=...). Then call `models.initialize_db()` a second time and re-assert the column set is identical and exactly one legacy row remains (idempotent). This is THE criterion-3 migration fixture — give it the detailed multi-step body described here.
    Add `test_price_config_and_alert_state_for_missing_item` (param `tmp_data_dir`): `models.initialize_db(delete=True)`; assert `models.get_price_alert_state_sync("https://missing.test")` == `(False, None)` and `models.get_item_price_config_sync("https://missing.test")` is None. Exercises models.py:198 and 246. No fenced code blocks — directive prose + identifiers + the exact SQL column names listed above.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_price_history.py -k "v2_schema_db_migrates_to_v3_in_place or price_config_and_alert_state_for_missing_item"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_price_history.py` exits 0.
    - The two new test names exist and PASS: test_v2_schema_db_migrates_to_v3_in_place, test_price_config_and_alert_state_for_missing_item.
    - PR-01 proves: v2.0 DB gains all 6 v3.0 columns + price_history table, the legacy row survives with no data loss, and a 2nd initialize_db() run is idempotent.
    - Previously-uncovered targets now executed: models.py:48-81 ALTER branch against a real v2.0 fixture (not the fresh-create path), models.py:198 + 246 row-None sentinels.
    - Raw sqlite3 only, under tmp_data_dir; no browser/network. No new dependency.
  </acceptance_criteria>
  <done>Migration fixture + sentinel tests pass; models.py:48-81/198/246 executed under --cov; legacy data preserved + idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PR-02/PR-03 trigger guards + AB-01/AB-02 get_price()-alongside-check_availability integration</name>
  <files>tests/test_price_alert.py</files>
  <read_first>
    - tests/test_price_alert.py (existing 17 tests — reuse fake_plugin / asyncio.Queue / dispatcher-or-None patterns and any existing _check_and_buy or _evaluate_price_triggers tests)
    - core/orchestrator.py lines 63-78 (_pct_from_target line 70 guard, _pct_drop_from_last line 77 guard), 119-142 (_evaluate_price_triggers early returns at 123 + 126), 196-238 (_check_and_buy: check_availability exception return BEFORE get_price at 201-203; get_price try/except at 207-210)
    - tests/conftest.py (fake_plugin factory; how to attach AsyncMock check_availability / get_price)
    - .planning/phases/17-test-hardening/17-RESEARCH.md rows PR-02, PR-03, AB-01, AB-02
  </read_first>
  <behavior>
    - PR-02: `_pct_from_target(price, 0)` and `_pct_drop_from_last(price, 0)` (and a negative denominator) each return `0.0` via the `<=0` guards (orch:70, 77) — no ZeroDivisionError.
    - PR-03: `_evaluate_price_triggers` with `get_item_price_config_sync` returning None → early return, no alert, no arming (orch:123); and with config returning `(None, None)` (both fields None) → early return (orch:126). Use a fake dispatcher and assert dispatcher.notify was NOT awaited.
    - AB-01: in `_check_and_buy`, a plugin whose `get_price` raises → the exception is caught and logged as `get_price error: <class name>` (orch:209-210), `_check_and_buy` does NOT propagate, and the availability/notification path still runs (e.g. an available item still enqueues set_available). Assert no exception escaped and the availability branch executed.
    - AB-02: get_price IS invoked even when `check_availability` returns False (sequencing: get_price runs after a successful availability check regardless of the boolean); and get_price is NOT invoked when `check_availability` RAISES (orch:201-203 returns before reaching get_price). Assert get_price.call_count == 1 in the available=False case and == 0 in the check_availability-raises case.
  </behavior>
  <action>
    In tests/test_price_alert.py add `test_pct_helpers_guard_nonpositive_denominator`: import `_pct_from_target`, `_pct_drop_from_last` from core.orchestrator; assert each returns `0.0` for a 0 denominator and a negative denominator (orch:70, 77).
    Add `test_evaluate_price_triggers_skips_when_no_config` (async, param `tmp_data_dir`): build a fake_plugin and a fake dispatcher (MagicMock with `notify = AsyncMock()`); first sub-case patch/seed so `get_item_price_config_sync(link)` returns None (no item row) → `await _evaluate_price_triggers(...)` returns without calling dispatcher.notify (orch:123); second sub-case seed config `(None, None)` → returns without notify (orch:126). Assert `dispatcher.notify` not awaited in both.
    Add `test_get_price_error_is_isolated_and_does_not_propagate` (async, param `tmp_data_dir`): build a fake_plugin with `check_availability = AsyncMock(return_value=True)` and `get_price = AsyncMock(side_effect=RuntimeError("boom"))`; call `await _check_and_buy(plugin, name, link, auto_buy=False, write_queue=asyncio.Queue(), dispatcher=None)`; assert it does NOT raise and that the availability path still ran (e.g. the write_queue received a set_available tuple, or get_item_notification_state path executed). Exercises orch:209-210.
    Add `test_get_price_invoked_alongside_check_availability_sequencing` (async, param `tmp_data_dir`): sub-case A — `check_availability = AsyncMock(return_value=False)`, `get_price = AsyncMock(return_value=None)`; after `_check_and_buy`, assert `get_price.call_count == 1` (runs even though unavailable). Sub-case B (fresh plugin) — `check_availability = AsyncMock(side_effect=RuntimeError("x"))`, `get_price = AsyncMock()`; after `_check_and_buy`, assert `get_price.call_count == 0` (early return before get_price, orch:201-203) and no exception propagated. No fenced code blocks — identifiers + directive prose only.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_price_alert.py -k "pct_helpers_guard_nonpositive_denominator or evaluate_price_triggers_skips_when_no_config or get_price_error_is_isolated_and_does_not_propagate or get_price_invoked_alongside_check_availability_sequencing"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_price_alert.py` exits 0.
    - The four new test names exist and PASS: test_pct_helpers_guard_nonpositive_denominator, test_evaluate_price_triggers_skips_when_no_config, test_get_price_error_is_isolated_and_does_not_propagate, test_get_price_invoked_alongside_check_availability_sequencing.
    - Previously-uncovered targets now executed: orchestrator.py:70, 77 (denominator guards), 123, 126 (early returns), 209-210 (get_price error isolation), plus the sequencing branch where check_availability-raises returns before get_price.
    - Full suite `python -m pytest` green; no regressions vs 529-passed baseline (count rises). fake_plugin + asyncio.Queue + tmp_data_dir only; no browser/network. No new dependency.
  </acceptance_criteria>
  <done>All four tests pass; named orchestrator guards/branches executed under --cov; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds test code only. The DB is a temp SQLite file under tmp_data_dir; the browser/network boundaries are replaced by fake_plugin AsyncMocks. No production code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-NA | (all) | tests/test_price_history.py, tests/test_price_alert.py | accept | No new attack surface; test-only additions, no production code changed. PR-01 strengthens data-integrity assurance (idempotent migration with no data loss against a v2.0 DB). |
| T-17-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (sqlite3 stdlib; pytest/pytest-asyncio/pytest-cov already installed). No legitimacy gate required. |

Note: if PR-01 reveals a real migration bug (e.g. data loss or a non-idempotent ALTER), flag it as a production bug — but the primary deliverable is the tests.
</threat_model>

<verification>
- `python -m pytest -q tests/test_price_history.py tests/test_price_alert.py` exits 0 with the six new tests passing.
- Full suite `python -m pytest` green; no regressions vs 529-passed/2-skipped baseline (count rises).
- `python -m pytest --cov=core --cov=models --cov-report=term-missing` confirms models.py:48-81 (v2.0-fixture migration path), 198, 246, and orchestrator.py:70, 77, 123, 126, 209-210 are now executed.
</verification>

<success_criteria>
- PR-01..PR-04, AB-01, AB-02 all present, named exactly per RESEARCH, and passing.
- PR-01 v2.0-schema migration fixture: migrates in place, preserves the legacy row, idempotent on second run.
- STAB-03 criterion 3 (price comparison/threshold guards, price_history schema + v2.0 migration, dedup-state sentinels) and the integration half of criterion 4 (get_price alongside check_availability) mapped to named passing tests.
- Deterministic and CI-safe; raw sqlite3 + fake_plugin AsyncMocks; no browser/network; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/17-test-hardening/17-03-SUMMARY.md` when done.
</output>
