---
phase: 17-test-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_plugin_base.py
autonomous: true
requirements: [STAB-03]

must_haves:
  truths:
    - "A plugin subclass can override requires_captcha=True and difficulty='easy' and the values stick + validate (AB-03)"
    - "_handle_ban records a proxy failure on a ban phrase, makes no call on benign text, and is safe with no proxy configured (AB-04) — the ban-detection → per-instance proxy cooldown bridge"
  artifacts:
    - path: "tests/test_plugin_base.py"
      provides: "AB-03 metadata override permutation + AB-04 _handle_ban branch 53->55"
      contains: "test_handle_ban_records_failure_on_proxy_when_banned"
  key_links:
    - from: "tests/test_plugin_base.py"
      to: "core/plugin_base.py:43-55"
      via: "RetailerPlugin subclass instance calling _handle_ban with a stub _proxy/_pool"
      pattern: "_handle_ban"
---

<objective>
Add the two plugin-ABC coverage tests (AB-03, AB-04) for STAB-03 criterion 4 (the unit half — defaults/overrides of the registry metadata attrs, and the ban-detection bridge). The headline deliverable is AB-04: the `_handle_ban` branch 53→55 that bridges ban-detection to the per-instance proxy scoping/cooldown (`pool.record_failure(proxy)`), the most material STAB-03.1 sub-requirement gap (completeness critic G-01). Both tests target genuinely uncovered lines/branches named in 17-RESEARCH.md and are deterministic + CI-safe (no browser/network/proxy).

Purpose: CI must catch regressions in the additive class-attribute overrides and in the ban→cooldown plumbing that ties Phase 13 proxy scoping to Phase 15+16 plugin metadata.
Output: Extended tests/test_plugin_base.py with the named AB-03, AB-04 tests; full suite stays green (baseline 529 passed, 2 skipped; count rises).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-hardening/17-RESEARCH.md
@.planning/phases/17-test-hardening/17-CONTEXT.md

<interfaces>
<!-- Contracts under test. Use directly — no codebase exploration needed. -->

From core/plugin_base.py:
- `class RetailerPlugin(ABC)`: class attrs with defaults `difficulty: Literal["easy","medium","hard"] = "medium"`, `requires_proxy: bool = False`, `requires_captcha: bool = False` (lines 22-24). `_VALID_DIFFICULTY = frozenset({"easy","medium","hard"})`. `__init_subclass__` raises `ValueError` if `cls.difficulty` not in _VALID_DIFFICULTY (validation at class-definition time); a valid override like "easy" passes.
- `get_price` default returns None (ALREADY covered — do NOT re-assert it per AB-03 note).
- `_handle_ban(self, body_text) -> bool` (lines 43-55): `if not _is_ban_response(0, body_text): return False` (benign text → no proxy call, line 49-50); reads `proxy = getattr(self, "_proxy", None)` and `pool = getattr(self, "_pool", None)`; `if proxy and pool: pool.record_failure(proxy)` (lines 53-54); returns True. The 53→55 branch is: ban detected (status 0 + a ban phrase in body) AND (proxy/pool present → record_failure called) OR (proxy/pool absent → safe no-op, still returns True).
- `core/stealth.py:_is_ban_response(status_code, body_text)`: returns True if status in {403,429,503} OR a ban phrase appears in body_text (phrases include "access denied", "blocked", "bot detected", "unusual traffic", "automated access", "verify you are human"). `_handle_ban` always passes status 0, so detection here is body-phrase driven.
- Constructing a concrete subclass requires implementing `check_availability` and `auto_buy` (both abstract). The existing test file has a `MinimalPlugin(RetailerPlugin)` stub at module level — reuse it or define a focused subclass.

From tests/test_plugin_base.py (existing patterns):
- Module-level `MinimalPlugin(RetailerPlugin)` concrete stub with `domain_patterns=["example.com"]` and async check_availability/auto_buy. Sync tests assert class attrs directly; instantiate when calling instance methods like `_handle_ban`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AB-03 metadata override permutation + AB-04 _handle_ban branch 53->55 (ban → proxy cooldown bridge)</name>
  <files>tests/test_plugin_base.py</files>
  <read_first>
    - tests/test_plugin_base.py (existing 11 tests — reuse the module-level MinimalPlugin stub and the class-attr-assertion pattern; do NOT re-assert the get_price default, already covered)
    - core/plugin_base.py lines 8-55 (_VALID_DIFFICULTY, class-attr defaults 22-24, __init_subclass__ validation, _handle_ban 43-55)
    - core/stealth.py lines 99-119 (_is_ban_response: _BAN_STATUSES, _BAN_PHRASES — pick a real phrase like "access denied" for the ban body, and benign text like "normal page content" for the no-ban case)
    - .planning/phases/17-test-hardening/17-RESEARCH.md rows AB-03, AB-04 (AB-04 is the high-value bridge entry, critic G-01)
  </read_first>
  <behavior>
    - AB-03: define a RetailerPlugin subclass overriding `requires_captcha = True` and `difficulty = "easy"` (and a concrete check_availability/auto_buy); assert the class attrs reflect the overrides (requires_captcha True, difficulty "easy") and that __init_subclass__ accepted the valid difficulty (no ValueError). Do NOT re-assert the get_price default (already covered per RESEARCH).
    - AB-04 (HIGH VALUE — the ban→cooldown bridge, branch 53→55): three sub-cases on an instantiated plugin's `_handle_ban`:
      (1) ban path WITH proxy: attach `_proxy = <sentinel object>` and `_pool = MagicMock(record_failure=MagicMock())`; call `_handle_ban("Access Denied - bot detected")` (a body containing a real ban phrase so _is_ban_response(0, body) is True) → returns True AND `pool.record_failure` was called once with the proxy sentinel (lines 53-54).
      (2) benign path: same plugin, call `_handle_ban("normal page content")` → returns False AND record_failure was NOT called (line 49-50 early return; no proxy interaction).
      (3) no-proxy-configured safe path: a fresh plugin instance with neither `_proxy` nor `_pool` set; call `_handle_ban("access denied")` → returns True and raises nothing (getattr(...) defaults to None; the `if proxy and pool` guard is False, so no record_failure). This proves the 53→55 branch (ban detected but no proxy → skip record_failure, still return True).
  </behavior>
  <action>
    In tests/test_plugin_base.py add `test_requires_captcha_and_easy_difficulty_overrides`: define a local subclass of RetailerPlugin (concrete check_availability/auto_buy, domain_patterns=["ex.com"]) that sets `requires_captcha = True` and `difficulty = "easy"`; assert `Subclass.requires_captcha is True` and `Subclass.difficulty == "easy"` and that defining the class did NOT raise (valid difficulty accepted by __init_subclass__). Do not touch get_price. Exercises the override permutation on plugin_base lines 22-24 + validation.
    Add `test_handle_ban_records_failure_on_proxy_when_banned`: instantiate the MinimalPlugin stub (or a focused concrete subclass). Sub-case 1 — set `plugin._proxy = object()` (a sentinel) and `plugin._pool = MagicMock()`; call `result = plugin._handle_ban("Access Denied - bot detected")`; assert `result is True` and `plugin._pool.record_failure.assert_called_once_with(plugin._proxy)` (plugin_base.py:53-54). Sub-case 2 — call `plugin._handle_ban("normal page content")`; assert it returns False and `record_failure` total call_count is unchanged (benign early return, plugin_base.py:49-50). Sub-case 3 — a NEW MinimalPlugin instance with NO `_proxy`/`_pool` attributes set; call `plugin._handle_ban("access denied")`; assert it returns True and raises nothing (the `if proxy and pool` guard short-circuits — branch 53→55). This is the high-value ban→cooldown bridge test; give all three sub-cases. No fenced code blocks — identifiers + directive prose only.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_plugin_base.py -k "requires_captcha_and_easy_difficulty_overrides or handle_ban_records_failure_on_proxy_when_banned"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_plugin_base.py` exits 0.
    - The two new test names exist and PASS: test_requires_captcha_and_easy_difficulty_overrides, test_handle_ban_records_failure_on_proxy_when_banned.
    - AB-04 covers all three sub-cases: ban+proxy → record_failure called once with the proxy; benign → no call + returns False; no-proxy → returns True and no exception (branch 53→55 executed).
    - Previously-uncovered targets now executed: plugin_base.py:43-55 (specifically the 53→55 branch) and the requires_captcha/difficulty override permutation.
    - Full suite `python -m pytest` green; no regressions vs 529-passed baseline (count rises). MagicMock + sentinel only; no browser/network/proxy. No new dependency.
  </acceptance_criteria>
  <done>Both tests pass; plugin_base.py:43-55 (53→55 bridge) and metadata overrides executed under --cov; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds test code only. `_handle_ban`'s proxy interaction is exercised against a MagicMock pool + sentinel proxy; no real proxy, browser, or network. No production code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-NA | (all) | tests/test_plugin_base.py | accept | No new attack surface; test-only additions, no production code changed. AB-04 strengthens the anti-detection posture by locking in the ban→cooldown bridge that retires a proxy on a ban page (security-relevant behavior asserted, not weakened). |
| T-17-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pytest/pytest-cov already installed). No legitimacy gate required. |

Note: if AB-04 reveals a real bug in the ban→record_failure plumbing, flag it as a production bug — but the primary deliverable is the tests.
</threat_model>

<verification>
- `python -m pytest -q tests/test_plugin_base.py` exits 0 with the two new tests passing.
- Full suite `python -m pytest` green; no regressions vs 529-passed/2-skipped baseline (count rises).
- `python -m pytest --cov=core --cov-branch --cov-report=term-missing` confirms plugin_base.py:43-55 (the 53→55 branch) and the requires_captcha/difficulty override lines are now executed.
</verification>

<success_criteria>
- AB-03, AB-04 present, named exactly per RESEARCH, and passing.
- AB-04 ban→proxy-cooldown bridge: record_failure on ban+proxy, no call on benign, safe with no proxy (branch 53→55) — the criterion-4 unit gap closed.
- STAB-03 criterion 4 (plugin ABC metadata overrides + ban-detection bridge) mapped to named passing tests; the get_price-alongside-check_availability integration half lives in plan 17-03.
- Deterministic and CI-safe; MagicMock + sentinel only; no browser/network/proxy; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/17-test-hardening/17-04-SUMMARY.md` when done.
</output>
