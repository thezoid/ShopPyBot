---
phase: 16-price-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models.py
  - core/config_schema.py
  - tests/test_price_history.py
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-05]
must_haves:
  truths:
    - "initialize_db() creates an append-only price_history table and adds four new items columns without data loss on existing DBs"
    - "Running initialize_db() twice does not raise or duplicate any column"
    - "A per-item target_price (cents) and price_drop_pct can be set in config and seeded into the items table; NULL/absent both = monitoring off"
    - "The last recorded price for an item can be read before the current price is appended"
  artifacts:
    - path: "models.py"
      provides: "price_history table, append/read functions, price-alert dedup functions, price-config update function"
      contains: "price_history"
    - path: "core/config_schema.py"
      provides: "ItemConfig.target_price and ItemConfig.price_drop_pct optional fields"
      contains: "target_price"
    - path: "tests/test_price_history.py"
      provides: "Migration idempotency + models function unit tests"
  key_links:
    - from: "models.initialize_db"
      to: "items table + price_history table"
      via: "PRAGMA table_info guard + CREATE TABLE IF NOT EXISTS"
      pattern: "CREATE TABLE IF NOT EXISTS price_history"
    - from: "ItemConfig"
      to: "items table"
      via: "update_item_price_config_sync seed (called in Plan 03)"
      pattern: "update_item_price_config_sync"
---

<objective>
Build the SQLite + config foundation for price monitoring: an append-only `price_history` table, four new `items` dedup/config columns added via the existing idempotent migration pattern, the models `_sync` read/write functions, and the two optional `ItemConfig` fields.

Purpose: Every later plan (orchestrator wiring, CLI, alerts) reads from these schema objects and functions. This plan defines the data contract first so downstream plans build against it without exploration.
Output: Migrated `models.py`, extended `ItemConfig`, and a new `tests/test_price_history.py` proving migration idempotency and parameterized writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-price-monitoring/16-CONTEXT.md
@.planning/phases/16-price-monitoring/16-RESEARCH.md

<interfaces>
<!-- Existing migration + dedup pattern in models.py the executor must mirror exactly -->
From models.py (initialize_db, lines 33-60): items table CREATE, then
`existing = {row[1] for row in conn.execute("PRAGMA table_info(items)").fetchall()}`
guard followed by `ALTER TABLE items ADD COLUMN ...` for each new column.
Existing dedup functions to mirror (signatures, NOT to modify):
  get_item_notification_state_sync(link) -> (bool, str|None)   # stock dedup
  set_item_available_sync(link, notified_at)                   # stock dedup
  clear_item_available_sync(link)                              # stock dedup
get_db_connection() context manager: WAL, parameterized SQL only.

From core/config_schema.py (ItemConfig, lines 46-50):
  class ItemConfig(BaseModel): name; link; auto_buy: bool = False; quantity: int = 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing migration + models tests</name>
  <files>tests/test_price_history.py</files>
  <read_first>
    - models.py (initialize_db migration pattern lines 33-60; get_db_connection; existing _sync functions)
    - tests/conftest.py (tmp_data_dir fixture — redirects models.DB_PATH; use it in every test)
    - .planning/phases/16-price-monitoring/16-RESEARCH.md (§Validation Architecture key test patterns: test_migration_idempotent, integer-cents)
  </read_first>
  <behavior>
    - test_price_history_table_created: after initialize_db(delete=True), PRAGMA table_info(price_history) lists columns id, item_link, price_cents, currency, scraped_at.
    - test_items_price_columns_added: after initialize_db(delete=True), PRAGMA table_info(items) includes target_price, price_drop_pct, price_alert_armed, price_last_notified.
    - test_migration_idempotent: initialize_db(delete=True) then initialize_db() does not raise; each new column count == 1 via PRAGMA table_info.
    - test_append_and_get_price_history: append_price_history_sync inserts a row; get_price_history_sync(link, limit) returns it newest-first as (price_cents, currency, scraped_at) tuples.
    - test_get_last_price: get_last_price_sync returns the most recent price_cents or None when no history.
    - test_price_alert_state_roundtrip: get_price_alert_state_sync defaults (False, None); set_price_alert_armed_sync sets (True, ts); clear_price_alert_armed_sync resets armed to False.
    - test_price_dedup_independent_from_stock_dedup: set_price_alert_armed_sync does NOT change last_seen_available/last_notified, and set_item_available_sync does NOT change price_alert_armed/price_last_notified.
    - test_update_item_price_config: update_item_price_config_sync writes target_price + price_drop_pct; get_item_price_config_sync reads them back (and returns None pair when both unset).
  </behavior>
  <action>
    Create `tests/test_price_history.py`. Every test uses the existing `tmp_data_dir` fixture (do NOT add conftest fixtures — none are needed). Mirror `16-RESEARCH.md` §Validation Architecture test patterns (PRAGMA table_info assertions, integer cents). Import the new models functions named in Task 2: `append_price_history_sync`, `get_price_history_sync`, `get_last_price_sync`, `get_price_alert_state_sync`, `set_price_alert_armed_sync`, `clear_price_alert_armed_sync`, `update_item_price_config_sync`, `get_item_price_config_sync`. Seed items via existing `add_items_sync` before testing config/dedup functions. Tests fail (RED) until Task 2 lands the functions and migration. No fenced implementation here — assertions only.
  </action>
  <verify>
    <automated>python -m pytest tests/test_price_history.py -q</automated>
  </verify>
  <acceptance_criteria>
    File `tests/test_price_history.py` exists with the 8 tests above. Before Task 2, the run errors on missing imports / missing columns (RED). After Task 2, `python -m pytest tests/test_price_history.py -q` exits 0. No new conftest fixtures added.
  </acceptance_criteria>
  <done>tests/test_price_history.py created; collects; fails only because Task 2 code is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Migration + models price functions + ItemConfig fields</name>
  <files>models.py, core/config_schema.py</files>
  <read_first>
    - models.py (initialize_db lines 33-60; get_db_connection; set_item_available_sync / clear_item_available_sync / get_item_notification_state_sync to mirror style and to NOT touch their columns)
    - core/config_schema.py (ItemConfig lines 46-50)
    - .planning/phases/16-price-monitoring/16-RESEARCH.md (§Exact Integration Points 1 + 2: exact column names/types, function signatures, SQL)
    - .planning/phases/16-price-monitoring/16-CONTEXT.md (LOCKED: INTEGER cents; separate dedup columns; append-only no purge)
  </read_first>
  <action>
    In `models.py` `initialize_db()`, after the existing PRAGMA-guard block, add idempotent `ALTER TABLE items ADD COLUMN` guards (reuse the same `existing` set; re-read or extend it) for: `target_price INTEGER` (NULL=off, PRICE-01), `price_drop_pct REAL` (NULL=off, PRICE-05), `price_alert_armed INTEGER NOT NULL DEFAULT 0`, `price_last_notified TEXT`. Then `CREATE TABLE IF NOT EXISTS price_history (id INTEGER PRIMARY KEY, item_link TEXT NOT NULL, price_cents INTEGER NOT NULL, currency TEXT NOT NULL DEFAULT 'USD', scraped_at TEXT NOT NULL)`. All SQL inside the existing `get_db_connection()` context manager.
    Add these parameterized `_sync` functions (mirror existing `_sync` naming + the get_db_connection pattern; under 30 lines each): `append_price_history_sync(link, price_cents, scraped_at, currency='USD')` INSERT; `get_price_history_sync(link, limit=10)` SELECT price_cents, currency, scraped_at WHERE item_link=? ORDER BY scraped_at DESC LIMIT ?; `get_last_price_sync(link)` SELECT price_cents ... ORDER BY scraped_at DESC LIMIT 1 returning int|None; `get_price_alert_state_sync(link)` SELECT price_alert_armed, price_last_notified returning (bool, str|None) — return (False, None) when row missing; `set_price_alert_armed_sync(link, notified_at)` UPDATE items SET price_alert_armed=1, price_last_notified=? WHERE link=? — MUST NOT touch last_seen_available/last_notified (Pitfall 1); `clear_price_alert_armed_sync(link)` UPDATE items SET price_alert_armed=0 WHERE link=?; `update_item_price_config_sync(link, target_price, price_drop_pct)` UPDATE items SET target_price=?, price_drop_pct=? WHERE link=? (separate seed per CONTEXT — do NOT reshape add_items_sync); `get_item_price_config_sync(link)` SELECT target_price, price_drop_pct WHERE link=? returning tuple|None.
    Every parameter MUST be bound with `?` placeholders (no string interpolation in SQL) — threat T-16-SQLI.
    In `core/config_schema.py` `ItemConfig`, add `target_price: Optional[int] = None` and `price_drop_pct: Optional[float] = None` (import `Optional` from typing). Do not change other ItemConfig fields. Do not change `add_items_sync` (separate-update strategy per CONTEXT).
  </action>
  <verify>
    <automated>python -m pytest tests/test_price_history.py -q</automated>
  </verify>
  <acceptance_criteria>
    `python -m pytest tests/test_price_history.py -q` exits 0 (all Task-1 tests pass). Idempotency test proves each new column count == 1 after a double `initialize_db`. Stock-dedup-independence test passes (price functions never touch last_seen_available/last_notified and vice versa). `grep` of new models functions shows only `?`-parameterized SQL — no f-string/`%`/`.format` in any SQL literal. Full suite stays green: `python -m pytest -q` shows no new failures vs baseline 493 passed, 2 skipped.
  </acceptance_criteria>
  <done>Migration adds 4 items columns + price_history idempotently; 8 models functions added; ItemConfig has the two optional fields; targeted + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml → ItemConfig → items table | User-supplied per-item price targets cross into the DB |
| caller (orchestrator/CLI) → models SQL | item_link / price values cross into SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SQLI | Tampering | new models.py SQL (price_history insert/read, items config/dedup updates) | mitigate | Every statement uses `?` placeholders; no string interpolation. Acceptance test greps for f-string/format in SQL literals. |
| T-16-NEG | Tampering | append_price_history_sync price_cents | accept | Range/positive guard is enforced at the orchestrator boundary (Plan 03) before calling append; models stores what it is given. Documented split. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: none); nothing to verify. |
</threat_model>

<verification>
- `python -m pytest tests/test_price_history.py -q` exits 0
- `python -m pytest -q` shows no regression vs baseline (493 passed, 2 skipped)
- PRAGMA table_info(items) lists the 4 new columns exactly once each; price_history table exists
</verification>

<success_criteria>
PRICE-01 (per-item config fields + items columns), PRICE-05 (price_drop_pct field + last-price read), and the schema half of PRICE-02 (idempotent price_history migration + write/read functions) are in place and unit-proven.
</success_criteria>

<output>
Create `.planning/phases/16-price-monitoring/16-01-SUMMARY.md` when done
</output>
