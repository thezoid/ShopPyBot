---
phase: 16-price-monitoring
plan: 04
type: execute
wave: 3
depends_on: ["16-03"]
files_modified:
  - core/cli/items.py
  - core/cli/__init__.py
  - tests/test_cli_price_history.py
autonomous: true
requirements: [PRICE-06]
must_haves:
  truths:
    - "Running `shoppybot items price-history <name>` prints the last N recorded prices for that item as a plain aligned table with $X.XX prices"
    - "Default limit is 10; --limit N overrides it"
    - "An unknown item name prints a clear no-history message and exits 0"
    - "The command makes no network call (reads price_history via BotService only)"
  artifacts:
    - path: "core/cli/items.py"
      provides: "handle_items_price_history + _format_price_history_table"
      contains: "handle_items_price_history"
    - path: "core/cli/__init__.py"
      provides: "price-history subparser leaf under items_sub with name + --limit"
      contains: "price-history"
    - path: "tests/test_cli_price_history.py"
      provides: "CLI stdout + limit + no-item tests"
  key_links:
    - from: "handle_items_price_history"
      to: "BotService.get_price_history"
      via: "svc.get_price_history(args.name, args.limit)"
      pattern: "get_price_history"
    - from: "items_sub price-history parser"
      to: "handle_items_price_history"
      via: "set_defaults(func=...)"
      pattern: "set_defaults\\(func=handle_items_price_history\\)"
---

<objective>
Add the `shoppybot items price-history <name>` CLI leaf: a handler + table formatter in `core/cli/items.py` and the argparse subparser (with `--limit`, default 10) in `core/cli/__init__.py`. Reads via `BotService.get_price_history` (added in Plan 03); no network call.

Purpose: This delivers PRICE-06, the user-facing inspection command. It mirrors the Phase 15 `core/cli/plugins.py` pattern and the existing `_format_items_table` style.
Output: Extended `core/cli/items.py`, `core/cli/__init__.py`, and `tests/test_cli_price_history.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-price-monitoring/16-CONTEXT.md
@.planning/phases/16-price-monitoring/16-RESEARCH.md
@.planning/phases/16-price-monitoring/16-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 (core/service.py) -->
  BotService.get_price_history(name: str, limit: int = 10) -> list[(price_cents, currency, scraped_at)]
<!-- Existing CLI patterns to mirror (core/cli/items.py) -->
  _format_items_table(rows) -> str    # width-aligned table builder; clone for price history
  handle_items_list(args, svc) -> int # handler signature contract
  imports ONLY sys + BotService (MOD-02 boundary)
<!-- Existing subparser wiring (core/cli/__init__.py) -->
  items_sub = items_p.add_subparsers(...); list_p/add_p/remove_p use set_defaults(func=...)
  handlers imported at top: from core.cli.items import handle_items_list, handle_items_add, handle_items_remove
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing CLI price-history tests</name>
  <files>tests/test_cli_price_history.py</files>
  <read_first>
    - tests/test_cli_items.py (capsys, patch core.service.BotService, SystemExit assertions)
    - core/cli/items.py (_format_items_table style; handler return-int contract)
    - .planning/phases/16-price-monitoring/16-RESEARCH.md (§Validation Architecture CLI stdout pattern: mock BotService, capsys, $49.99 assertion)
  </read_first>
  <behavior>
    - test_cli_price_history: main(["items","price-history","Widget"]) with mocked BotService.get_price_history returning [(4999,"USD","2026-06-09T12:00:00+00:00")] exits 0; stdout contains "$49.99", "USD", and the recorded-at value.
    - test_cli_price_history_no_item: unknown name (get_price_history returns []) prints a no-history message and exits 0.
    - test_cli_price_history_limit: main(["items","price-history","Widget","--limit","3"]) passes limit=3 to BotService.get_price_history (assert call args); table shows at most 3 rows.
    - test_cli_price_history_no_network: the handler path calls only BotService.get_price_history (no registry/orchestrator/models import side effects) — assert via patching BotService and confirming no network/registry call.
  </behavior>
  <action>
    Create `tests/test_cli_price_history.py`. Mirror `tests/test_cli_items.py`: `patch("core.service.BotService", return_value=mock_svc)`, set `mock_svc.get_price_history.return_value`, call `core.service.main([...])`, assert `SystemExit.code == 0` and inspect `capsys.readouterr().out`. Reference the handler `handle_items_price_history` and formatter `_format_price_history_table` from Task 2. Assertions only; RED until Tasks 2-3.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_price_history.py -q</automated>
  </verify>
  <acceptance_criteria>
    File exists with the 4 tests; collects; RED before Tasks 2-3. After implementation, exits 0. No network, no live browser.
  </acceptance_criteria>
  <done>tests/test_cli_price_history.py created; fails only because implementation is absent.</done>
</task>

<task type="auto">
  <name>Task 2: handle_items_price_history + table formatter</name>
  <files>core/cli/items.py</files>
  <read_first>
    - core/cli/items.py (_format_items_table width-alignment; handle_items_* signature; imports sys + BotService only)
    - .planning/phases/16-price-monitoring/16-RESEARCH.md (§Exact Integration Points 6: _format_price_history_table + handler skeletons; Pitfall 8 case-sensitivity)
  </read_first>
  <action>
    Add `_format_price_history_table(name: str, rows: list) -> str`: when rows empty return `f"No price history recorded for: {name}"`; else build a left-aligned table (clone `_format_items_table` width logic) with headers `("Price", "Currency", "Recorded At")`, formatting each row's `price_cents` as `$X.XX` via `f"${r[0] / 100:.2f}"`, currency `r[1]`, recorded-at `r[2]`. Add `handle_items_price_history(args, svc: BotService) -> int`: `print(_format_price_history_table(args.name, svc.get_price_history(args.name, args.limit)))`; return 0. Keep the MOD-02 import boundary (only sys + BotService). Name matching is case-sensitive exact (consistent with existing items commands; Pitfall 8) — the no-history message covers a miss.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_price_history.py::test_cli_price_history tests/test_cli_price_history.py::test_cli_price_history_no_item -q</automated>
  </verify>
  <acceptance_criteria>
    Targeted tests pass: table shows `$49.99`/`USD`/recorded-at; unknown name prints the no-history message and exits 0. `core/cli/items.py` still imports only sys + BotService.
  </acceptance_criteria>
  <done>Formatter + handler render price history as a plain aligned $X.XX table via BotService.</done>
</task>

<task type="auto">
  <name>Task 3: Wire price-history subparser leaf</name>
  <files>core/cli/__init__.py</files>
  <read_first>
    - core/cli/__init__.py (items_sub structure lines 64-93; handler import line 12; remove_p pattern)
    - .planning/phases/16-price-monitoring/16-RESEARCH.md (§Exact Integration Points 6 subparser snippet)
  </read_first>
  <action>
    Add `handle_items_price_history` to the existing `from core.cli.items import ...` line. After `remove_p` in the items_sub group, add a parser: `ph_p = items_sub.add_parser("price-history", help="Show recorded price history for an item.")`; `ph_p.add_argument("name", help="Item name.")`; `ph_p.add_argument("--limit", type=int, default=10, help="Number of recent prices to show (default: 10).")`; `ph_p.set_defaults(func=handle_items_price_history)`. Do not change the bare-`items` usage-exit behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_price_history.py -q</automated>
  </verify>
  <acceptance_criteria>
    All CLI tests pass (including `--limit 3` passing limit=3 to BotService). `python -m pytest -q` no regression vs baseline (493 passed, 2 skipped). `shoppybot items price-history --help` lists name + --limit.
  </acceptance_criteria>
  <done>price-history leaf wired under items with name + --limit; dispatches to handle_items_price_history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → BotService.get_price_history | User-supplied name/limit cross into a read-only DB query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-CLI-SQLI | Tampering | get_price_history query (via Plan 01 models) | mitigate | Name/limit bound with `?` placeholders in the Plan-01 models function; limit is argparse `type=int`. No raw SQL in the CLI layer. |
| T-16-CLI-NET | Information Disclosure | price-history command | accept | Read-only, no network; MOD-02 keeps the handler to BotService only. Asserted by no-network test. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH audit: none). |
</threat_model>

<verification>
- `python -m pytest tests/test_cli_price_history.py -q` exits 0
- `python -m pytest -q` no regression vs baseline (493 passed, 2 skipped)
- `shoppybot items price-history <name>` prints a $X.XX table; `--limit N` bounds rows; unknown name → no-history message, exit 0
</verification>

<success_criteria>
PRICE-06 (`shoppybot items price-history <name>` showing the last N recorded prices, default 10, `--limit N`, plain table, no network) is delivered and unit-proven.
</success_criteria>

<output>
Create `.planning/phases/16-price-monitoring/16-04-SUMMARY.md` when done
</output>
