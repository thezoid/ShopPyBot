---
phase: 35-audit-fixes-doc-hygiene-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/health.py
  - core/cli/status.py
  - tests/test_health.py
  - tests/test_cli_status.py
  - tests/test_service.py
  - tests/test_orchestrator.py
  - tests/test_sse.py
  - tests/test_web_controls.py
autonomous: true
requirements: [AF-02]
must_haves:
  truths:
    - "get_status()['plugins'][name] contains no raw last_heartbeat field"
    - "The SSE 'status' frame contains no raw last_heartbeat field"
    - "heartbeat_age_secs is still present on both surfaces"
    - "The shoppybot status CLI table still renders a real elapsed heartbeat age (not always 'never') when a plugin is heartbeating"
  artifacts:
    - path: "core/health.py"
      provides: "get_snapshot() excludes raw last_heartbeat from the public per-plugin dict, still derives heartbeat_age_secs"
      contains: "heartbeat_age_secs"
    - path: "core/cli/status.py"
      provides: "CLI status table reads heartbeat_age_secs (lockstep with the AF-02 backend change)"
      contains: "heartbeat_age_secs"
  key_links:
    - from: "core/health.py get_snapshot()"
      to: "BotService.get_status() and web/sse_hub.py broadcast"
      via: "single shaping boundary consumed verbatim by both REST and SSE"
      pattern: "heartbeat_age_secs"
    - from: "core/cli/status.py _format_status_table"
      to: "get_status()['plugins'][name]['heartbeat_age_secs']"
      via: "CLI reads the derived age, not the removed raw float"
      pattern: "heartbeat_age_secs"
---

<objective>
Close audit warning AF-02: `get_status()` and the SSE status frames must NOT expose the raw `last_heartbeat` monotonic float — only the already-derived `heartbeat_age_secs`.

There is exactly ONE source: `HealthRegistry.get_snapshot()` (`core/health.py:91`), whose public-key filter currently passes `last_heartbeat` through. This one shaped dict feeds BOTH `BotService.get_status()` (passthrough) and the SSE producer (`web/sse_hub.py` broadcasts `get_status()` verbatim) — fixing the snapshot fixes both surfaces atomically.

**Critical lockstep (not optional):** `core/cli/status.py:36` reads the raw `last_heartbeat` directly from the same dict and will silently degrade to printing "never" for every plugin once the field disappears. It MUST be updated to read `heartbeat_age_secs` in the same plan, or the `shoppybot status` table regresses functionally.

Purpose: information-disclosure hygiene (remove an internal timing float from the public surface) with no CLI regression.
Output: one-line filter change in health.py, lockstep CLI change, and the test fan-out across the 5 files that touch `last_heartbeat`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/35-audit-fixes-doc-hygiene-cleanup/35-RESEARCH.md
@.planning/phases/35-audit-fixes-doc-hygiene-cleanup/35-CONTEXT.md

<interfaces>
<!-- Live source contracts. Fix ONE boundary; both public surfaces follow. -->

core/health.py:78-97 — get_snapshot(self) -> dict[str, dict]
  # Builds `public = {k: v for k, v in rec.items() if not k.startswith("_")}`  <-- FIX line 91: also exclude k == "last_heartbeat"
  # Keeps: lhb = rec["last_heartbeat"] (internal read only) -> public["heartbeat_age_secs"] = None if lhb == 0.0 else round(now - lhb, 1)  (UNCHANGED)

core/service.py:84 — BotService.get_status()["plugins"] = get_snapshot() passthrough (NO change needed)
web/sse_hub.py:97-98 — broadcasts svc.get_status() verbatim as the "status" event (NO change needed)

core/cli/status.py:30-42 — _format_status_table(status) LOCKSTEP CONSUMER (MUST change):
  now = time.monotonic()                                              # line 31 — remove (only use-site of import time)
  ... (f"{now - rec['last_heartbeat']:.1f}s ago" if rec.get("last_heartbeat", 0.0) > 0.0 else "never")   # line 36 — switch to heartbeat_age_secs
  import time                                                          # line 13 — remove (becomes unused)

Test files that reference last_heartbeat (grep-confirmed, 5 files / 7 occurrences):
  tests/test_health.py (3), tests/test_cli_status.py (1), tests/test_orchestrator.py (1), tests/test_service.py (1), tests/test_web_controls.py (1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scrub raw last_heartbeat at the snapshot source + CLI lockstep (AF-02)</name>
  <files>core/health.py, core/cli/status.py, tests/test_health.py, tests/test_cli_status.py</files>
  <read_first>
core/health.py:78-97 (get_snapshot — the single shaping boundary; fix the line-91 comprehension only, keep the heartbeat_age_secs derivation)
core/cli/status.py (full — the line-13 import, line-31 now=, line-36 raw read)
tests/test_health.py:18-22 (test_heartbeat_sets_last_heartbeat) and :101-110 (test_snapshot_public_keys_exact)
tests/test_cli_status.py (the _STATUS_PAYLOAD mock fixture hardcoding "last_heartbeat": 100.0, and the table-rendering test)
  </read_first>
  <behavior>
    - After a heartbeat, get_snapshot()["PluginA"] does NOT contain the key "last_heartbeat" but DOES contain "heartbeat_age_secs" (not None).
    - A never-heartbeated plugin still yields heartbeat_age_secs is None (unchanged edge case).
    - _format_status_table renders "{age}s ago" for a plugin whose heartbeat_age_secs is a non-null number, and "never" only when heartbeat_age_secs is None — i.e. a heartbeating plugin is NOT rendered as "never".
  </behavior>
  <action>
    In core/health.py line 91, change the public-dict comprehension to also drop the raw field: exclude keys where `k.startswith("_")` OR `k == "last_heartbeat"`. Keep the existing internal read `lhb = rec["last_heartbeat"]` and the `public["heartbeat_age_secs"] = None if lhb == 0.0 else round(now - lhb, 1)` derivation exactly as-is (that field is what AF-02 says to KEEP). In core/cli/status.py, update line 36 to read the derived age: render `f"{rec['heartbeat_age_secs']}s ago"` when `rec.get("heartbeat_age_secs") is not None`, else `"never"`; then remove the now-orphaned `now = time.monotonic()` (line 31) and the `import time` (line 13, whose only use-site was that computation). Update tests/test_health.py: in test_snapshot_public_keys_exact drop "last_heartbeat" from the expected key set; repurpose test_heartbeat_sets_last_heartbeat to assert `snap["PluginA"]["heartbeat_age_secs"] is not None` post-heartbeat; and ADD a permanent guard test_snapshot_excludes_last_heartbeat asserting `"last_heartbeat" not in snap["PluginA"]`. Update tests/test_cli_status.py: change the _STATUS_PAYLOAD fixture to supply `"heartbeat_age_secs": <non-null number>` instead of `"last_heartbeat"`, and assert the rendered table is NOT "never" for that plugin (this closes the trap where a stale mock masks the regression). No other behavior changes.
  </action>
  <acceptance_criteria>
    - core/health.py get_snapshot() public dict excludes `last_heartbeat` and still sets `heartbeat_age_secs`.
    - `grep -n "last_heartbeat" core/cli/status.py` returns 0 matches; `grep -n "heartbeat_age_secs" core/cli/status.py` returns a match; `import time` no longer present in core/cli/status.py.
    - tests/test_health.py has test_snapshot_excludes_last_heartbeat asserting the key's absence; test_snapshot_public_keys_exact no longer lists "last_heartbeat".
    - tests/test_cli_status.py fixture supplies heartbeat_age_secs and asserts the table shows a real age (not "never") for a heartbeating plugin.
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_health.py tests/test_cli_status.py -q</automated>
  </verify>
  <done>Raw last_heartbeat is gone from get_snapshot()'s public dict; the CLI reads heartbeat_age_secs (no "never" regression); health + cli tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Consumer test fan-out + SSE-frame absence proof (AF-02)</name>
  <files>tests/test_service.py, tests/test_orchestrator.py, tests/test_sse.py, tests/test_web_controls.py</files>
  <read_first>
tests/test_service.py:83-90 (test_get_status_shape_with_registry — the key tuple listing last_heartbeat)
tests/test_orchestrator.py (~line 1295 — assert snap[plugin_name]["last_heartbeat"] > 0.0)
tests/test_sse.py:186-213 (test_sse_no_credential_patterns — the _event_generator + SseHub broadcast pattern to mirror for the new SSE-frame absence test)
tests/test_web_controls.py (the one last_heartbeat fixture reference — optional non-breaking cleanup)
  </read_first>
  <action>
    Update tests/test_service.py test_get_status_shape_with_registry: remove "last_heartbeat" from the expected key tuple and add an explicit `assert "last_heartbeat" not in plugins[name]` (proves the REST/get_status surface is clean). Update tests/test_orchestrator.py's post-cycle health assertion (~line 1295) from `assert snap[plugin_name]["last_heartbeat"] > 0.0` to `assert snap[plugin_name]["heartbeat_age_secs"] is not None`. Add a new test to tests/test_sse.py that broadcasts a get_snapshot()-shaped status payload through the SSE `_event_generator`/`SseHub` (mirroring test_sse_no_credential_patterns) and asserts the serialized SSE "status" frame text does NOT contain "last_heartbeat" AND DOES contain "heartbeat_age_secs" (proves the SSE surface is clean — the second surface AF-02 requires). In tests/test_web_controls.py, update the single last_heartbeat fixture reference to heartbeat_age_secs if it feeds a get_status-shaped payload (optional, non-breaking cleanup — skip only if it references internal registry state rather than the public snapshot). Then run the FULL suite to catch any missed lockstep consumer.
  </action>
  <acceptance_criteria>
    - `grep -rn "last_heartbeat" tests/` returns matches ONLY where the key's ABSENCE is being asserted (no test asserts its presence on the public snapshot/get_status/SSE surface).
    - tests/test_sse.py has a test asserting "last_heartbeat" absent from and "heartbeat_age_secs" present in the SSE status frame.
    - tests/test_service.py asserts "last_heartbeat" not in the get_status plugins dict.
    - tests/test_orchestrator.py asserts heartbeat_age_secs is not None (not last_heartbeat > 0.0).
    - Full suite green: `.venv\Scripts\python.exe -m pytest -q`.
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q</automated>
  </verify>
  <done>Both public surfaces (get_status + SSE frame) proven free of raw last_heartbeat; heartbeat_age_secs retained; full suite green with no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HealthRegistry internal state → public per-plugin snapshot | The single shaping boundary where internal fields are filtered before crossing to any browser/CLI-facing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | Information Disclosure | Raw `last_heartbeat` monotonic float exposed on get_status() + SSE status frame | mitigate | This plan IS the remediation — filter the field once at `get_snapshot()`; new absence tests on both the REST and SSE surfaces are the permanent guard. Low severity (internal timing datum, no credential/PII), per the audit's own characterization |
| T-35-02-02 | Denial of Service (regression) | CLI `shoppybot status` silently showing "never" for all plugins if the lockstep consumer is missed | mitigate | core/cli/status.py updated to read heartbeat_age_secs in the SAME task; test_cli_status.py fixture updated to heartbeat_age_secs and asserts a real age renders (defeats the stale-mock masking trap) |
| T-35-02-SC | Tampering | Supply chain (package installs) | accept | No packages installed this plan; existing first-party code + tests only |

The credential-scrub boundary (`last_error` class-name-only) established in v4.1 is the same pattern reused here; no new trust boundary is introduced.
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/test_health.py tests/test_cli_status.py tests/test_service.py tests/test_orchestrator.py tests/test_sse.py -q` green.
- `grep -n "last_heartbeat" core/cli/status.py` returns 0; `import time` absent from core/cli/status.py.
- Full suite green: `.venv\Scripts\python.exe -m pytest -q` (baseline 932 passed, 2 skipped + AF-02 test changes).
</verification>

<success_criteria>
- Raw `last_heartbeat` absent from BOTH get_status()['plugins'] and the SSE "status" frame; `heartbeat_age_secs` present on both.
- core/cli/status.py reads heartbeat_age_secs (no "never" regression); `import time` removed.
- All 5 last_heartbeat-touching test files updated; no test asserts the raw field on a public surface.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/35-audit-fixes-doc-hygiene-cleanup/35-02-SUMMARY.md` when done.
</output>
