---
phase: 34-feature-completion
plan: 02
type: execute
wave: 2
depends_on: [34-01, 34-03]
files_modified:
  - web/log_reader.py
  - web/routes/api.py
  - web/routes/pages.py
  - core/service.py
  - web/templates/dashboard.html
  - tests/test_log_reader.py
  - tests/test_api_observability.py
autonomous: true
requirements: [FC-01]
must_haves:
  truths:
    - "GET /api/logs?plugin=amazon returns only [amazon]-tagged lines"
    - "the plugin filter AND-composes with the existing level and search filters"
    - "an absent plugin param returns all lines (byte-identical to existing behavior)"
    - "the dashboard log panel has a plugin dropdown populated from platform_keys, wired to the plugin query param"
  artifacts:
    - path: "web/log_reader.py"
      provides: "read_logs_filtered(n, level, search, plugin) plugin filter"
      contains: "plugin"
    - path: "web/routes/api.py"
      provides: "/logs plugin query param (validated) passed to read_logs_filtered"
      contains: "plugin"
    - path: "core/service.py"
      provides: "list_plugins() exposes platform_key for the dropdown"
      contains: "platform_key"
    - path: "web/templates/dashboard.html"
      provides: "#log-plugin-filter select + pollLogs plugin-param wiring"
      contains: "log-plugin-filter"
    - path: "tests/test_api_observability.py"
      provides: "updated arity asserts + /api/logs?plugin= integration test"
      contains: "plugin"
  key_links:
    - from: "web/templates/dashboard.html:#log-plugin-filter"
      to: "/api/logs?plugin="
      via: "pollLogs URLSearchParams"
      pattern: "log-plugin-filter"
    - from: "web/routes/api.py:/logs"
      to: "web/log_reader.py:read_logs_filtered"
      via: "positional (n, level, search, plugin)"
      pattern: "read_logs_filtered"
---

<objective>
Add a `plugin` filter to `/api/logs` and a dashboard log-panel dropdown that consumes it (FC-01, completes OBS-08). The filter reuses the existing `read_logs_filtered()` path — an optional `plugin` param AND-composes with the existing level + search filters; absent = all (no behavior change). The dropdown is populated from `platform_key` values and wired to the `plugin` query param, reusing the existing `.log-controls` styling with no new design language.

Purpose: Turns the guaranteed `[plugin]` tag from Plan 34-01 into an operator-usable filter — the deferred half of OBS-08.
Output: `read_logs_filtered` plugin param + validated route param; `list_plugins` platform_key; `#log-plugin-filter` dropdown + JS wiring; the two arity-assert fixes + a plugin-filter test.

Dependency note: depends_on 34-01 (needs the guaranteed `[plugin]` tag format the filter matches on) AND 34-03 (shared ownership of web/routes/api.py, core/service.py, web/templates/dashboard.html, tests/test_api_observability.py — 34-02 builds on 34-03's edits to those files; runs in the next wave to keep file ownership clean).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-feature-completion/34-RESEARCH.md
@.planning/phases/34-feature-completion/34-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

Current read_logs_filtered (web/log_reader.py:29-52) — filter-then-limit:
- signature TODAY: `read_logs_filtered(n=50, level=None, search=None) -> list[str]`
- level: keep lines where `line.startswith(f"[{level.upper()}]")`
- search: keep lines where `search.lower() in line.lower()`
- both AND-combined; returns `lines[-n:]`
- NOTE: after Plan 34-01 the line format is `[LEVEL][plugin][ts] message`, so a plugin tag is the substring `[<platform_key>]`

Current /logs route (web/routes/api.py:31-49):
- `async def get_logs(request, level=None, search=None, n=50)`; clamps `n = min(max(n,1),500)`
- calls `logs = await asyncio.to_thread(read_logs_filtered, n, level, search)`

The 2 arity asserts that BREAK (tests/test_api_observability.py:122, :132):
- test_logs_filtered_level_and_search asserts `mock_fn.assert_called_once_with(50, "ERROR", "captcha")`
- test_logs_no_params_default_behavior asserts `mock_fn.assert_called_once_with(50, None, None)`
- BOTH must gain a trailing `None`: `(50, "ERROR", "captcha", None)` and `(50, None, None, None)`

list_plugins (core/service.py:120-145) — returns dicts WITHOUT platform_key today:
- keys: name, domain_patterns, difficulty, requires_proxy, requires_captcha
- add one key: `"platform_key": getattr(plugin, "platform_key", None)`

Dashboard log-controls markup (web/templates/dashboard.html:171-186):
- `<div class="log-controls">` holds `<label for="log-level-filter">` + `<select id="log-level-filter">` + `<label for="log-search">` + `<input id="log-search">` + Follow/Refresh buttons

pollLogs JS (web/templates/dashboard.html:575-608):
- reads `#log-level-filter` + `#log-search` into a `URLSearchParams({ n: 500 })`, sets level/search if truthy, fetches `/api/logs?...`
- filter controls are wired: level `change` → pollLogs, search `input` (debounced 300ms) → pollLogs, refresh click → pollLogs

Dashboard page route (web/routes/pages.py:11-19):
- `templates.TemplateResponse(request, "dashboard.html", {"is_non_local": ..., "status": svc.get_status(), "items": svc.list_items()})`

platform_key set (research-verified): amazon, bestbuy, gamestop, newegg, squareenix, target, walmart (lowercase, no underscore)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read_logs_filtered plugin param + validated /api/logs param + test updates</name>
  <files>web/log_reader.py, web/routes/api.py, tests/test_log_reader.py, tests/test_api_observability.py</files>
  <read_first>
    - web/log_reader.py:29-52 (read_logs_filtered — the exact filter-then-limit chain)
    - web/routes/api.py:31-49 (the /logs route + the `asyncio.to_thread(read_logs_filtered, n, level, search)` call)
    - tests/test_api_observability.py:115-132 (the two arity-pinned tests that MUST be updated)
    - 34-RESEARCH.md "Pattern 2: plugin filter composes with level+search" + Pitfall 1 (the two breaking arity asserts) + Security Domain V5 (whitelist the plugin param defense-in-depth)
  </read_first>
  <behavior>
    - read_logs_filtered(50, None, None, "amazon") returns only lines containing "[amazon]"; lines tagged "[bestbuy]" are excluded
    - plugin AND-composes with level+search: read_logs_filtered(50, "ERROR", "captcha", "amazon") keeps only lines that are [ERROR]-prefixed AND contain "captcha" AND contain "[amazon]"
    - plugin=None → no plugin filtering (existing callers unchanged)
    - /api/logs?plugin=amazon calls read_logs_filtered(50, None, None, "amazon")
    - /api/logs with no params calls read_logs_filtered(50, None, None, None) — updated arity
    - /api/logs?level=ERROR&search=captcha&n=50 calls read_logs_filtered(50, "ERROR", "captcha", None) — updated arity
    - an invalid plugin param (not lowercase-alphanumeric, e.g. "../etc" or "a b") is dropped to None before calling read_logs_filtered (defense-in-depth whitelist)
  </behavior>
  <action>
    Add a `plugin: str | None = None` parameter to read_logs_filtered (web/log_reader.py) as the LAST positional, after search. Insert its filter block AFTER level and BEFORE (or after) search, AND-composed: when plugin is not None, `tag = f"[{plugin}]"`; keep only lines where `tag in line`. Preserve filter-then-limit (`return lines[-n:]` stays last). Update the docstring to mention the plugin filter. In web/routes/api.py add `plugin: str | None = None` to get_logs; validate it defense-in-depth: if plugin is provided and NOT `re.fullmatch(r"[a-z0-9]+", plugin)`, set plugin = None (an internal tag is always lowercase-alphanumeric; this blocks arbitrary substring probing beyond what `search` already allows). Pass plugin as the 4th positional to `asyncio.to_thread(read_logs_filtered, n, level, search, plugin)`. Update BOTH arity asserts in tests/test_api_observability.py to the 4-arg form (add a trailing None), and add an integration test that mocks read_logs_filtered, GETs /api/logs?plugin=amazon, and asserts `assert_called_once_with(50, None, None, "amazon")`. Add plugin-filter unit tests to tests/test_log_reader.py (monkeypatch _read_today_lines like the existing tests) covering the <behavior> cases (plugin-only, plugin+level+search compose, plugin=None passthrough). Do NOT change the filter-then-limit ordering; do NOT add a new endpoint.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_log_reader.py tests/test_api_observability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - read_logs_filtered signature ends with `plugin: str | None = None`; its filter keeps only lines where `f"[{plugin}]"` is a substring, AND-composed with level+search, filter-then-limit preserved
    - /logs route validates plugin against `re.fullmatch(r"[a-z0-9]+", ...)` and drops invalid values to None before calling read_logs_filtered
    - the TWO arity asserts now read `(50, "ERROR", "captcha", None)` and `(50, None, None, None)` — explicit criterion
    - a new test asserts /api/logs?plugin=amazon → `read_logs_filtered` called with `(50, None, None, "amazon")`
    - tests/test_log_reader.py has a plugin-only, a compose-with-level+search, and a plugin=None passthrough test
    - `.venv\Scripts\python.exe -m pytest tests/test_log_reader.py tests/test_api_observability.py -q` green
  </acceptance_criteria>
  <done>The plugin filter composes with level+search, absent=all, the route validates the param, and the two broken arity asserts are fixed with a new plugin-filter integration test.</done>
</task>

<task type="auto">
  <name>Task 2: platform_key in list_plugins + #log-plugin-filter dropdown wired to the param</name>
  <files>core/service.py, web/routes/pages.py, web/templates/dashboard.html</files>
  <read_first>
    - core/service.py:120-145 (list_plugins — add the platform_key key)
    - web/routes/pages.py:11-19 (the dashboard TemplateResponse context — add a plugins entry)
    - web/templates/dashboard.html:171-186 (the .log-controls block where the dropdown is added) + :575-608 (pollLogs + the filter-control wiring)
    - 34-RESEARCH.md Open Question 2 (server-side dropdown population via platform_key) + Pitfall 6 (squareenix no-underscore — use platform_key, never class name) + the FC-01 UI decision (reuse .log-controls styling, no new CSS)
  </read_first>
  <action>
    In core/service.py list_plugins, add `"platform_key": getattr(plugin, "platform_key", None)` to each returned dict. In web/routes/pages.py, add `"plugins": svc.list_plugins()` to the dashboard TemplateResponse context. In web/templates/dashboard.html, inside the existing `.log-controls` div (next to `#log-level-filter`), add a `<label for="log-plugin-filter">Plugin</label>` + `<select id="log-plugin-filter">` with a first `<option value="">All plugins</option>` followed by a Jinja2 loop rendering `<option value="{{ p.platform_key }}">{{ p.platform_key }}</option>` for each plugin in `plugins` that has a truthy platform_key (skip None). In pollLogs JS, read `#log-plugin-filter` value and `if (plugin) params.set('plugin', plugin)` alongside the existing level/search params. Wire the dropdown `change` event to pollLogs (mirror the existing level-filter `change` listener). Reuse the existing select styling (the components.css `select` rule already matches the level dropdown) — add NO new CSS. Use platform_key values ONLY (never class names).
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_web_dashboard.py tests/test_design_system.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "platform_key" core/service.py` shows the added key in list_plugins
    - web/routes/pages.py passes `plugins` into the template context
    - dashboard.html has `id="log-plugin-filter"` inside `.log-controls` with an "All plugins" default option and a Jinja2 loop over platform_key values
    - pollLogs sets the `plugin` query param when the dropdown has a value, and a change listener calls pollLogs
    - no new CSS class / hardcoded hex added (test_design_system green); Zero-Node preserved
    - `.venv\Scripts\python.exe -m pytest tests/test_web_dashboard.py tests/test_design_system.py -q` green
  </acceptance_criteria>
  <done>The dashboard log panel has a platform_key-populated plugin dropdown wired to the /api/logs plugin param, reusing existing control styling with zero new CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/logs?plugin= | untrusted query param reaches the log-file read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Information Disclosure | plugin param probing the log file | mitigate | The route whitelists plugin against `re.fullmatch(r"[a-z0-9]+", ...)` before use; the filter is a read-only substring match; no new file exposure beyond the existing `search` param (which already reads the whole day's file) |
| T-34-04 | Tampering | plugin param as a format/path token | mitigate | The param is used ONLY as `f"[{plugin}]"` substring-membership; it is never used in a path, format-eval, or SQL; invalid values are dropped to None |
| T-34-08 | Information Disclosure | dashboard renders log lines | accept | Log lines are already scrubbed at write time (exc.__class__.__name__ convention); the dashboard uses textContent (not innerHTML) for log content; the plugin filter adds no new data surface |
| T-34-SC | Tampering | new dependencies | accept | No package installs — uses stdlib re + existing fastapi/jinja2; Package Legitimacy Gate is N/A |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/test_log_reader.py tests/test_api_observability.py tests/test_web_dashboard.py tests/test_design_system.py -q` green
- Full suite green before phase gate: `.venv\Scripts\python.exe -m pytest -q`
- Manual: GET /api/logs?plugin=amazon returns only [amazon]-tagged lines; the dashboard dropdown lists platform_key values
</verification>

<success_criteria>
- /api/logs accepts a validated plugin param that AND-composes with level+search; absent=all
- The two arity asserts are updated and a plugin-filter integration test passes
- The dashboard log panel has a platform_key-populated plugin dropdown wired to the param, reusing existing components (near-zero new CSS, Zero-Node preserved)
</success_criteria>

<output>
Create `.planning/phases/34-feature-completion/34-02-SUMMARY.md` when done.
</output>
