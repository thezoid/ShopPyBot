---
phase: 33-config-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/config_schema.py
  - core/orchestrator.py
  - sample.config.yml
  - tests/test_config_schema.py
  - tests/test_orchestrator_jitter.py
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "A config using legacy min_delay/max_delay still loads and maps to canonical delay_seconds/delay_jitter (delay_seconds == min_delay, delay_jitter == max_delay - min_delay)"
    - "A config using canonical delay_seconds/delay_jitter loads directly for all 7 platforms"
    - "The 5 community plugins' effective poll-delay distribution is unchanged: _get_plugin_sleep still yields values in the same [min_delay, max_delay] range"
    - "Amazon/BestBuy availability polling is now jittered (delay_seconds + uniform(0, delay_jitter) = 30-40s by default) instead of flat poll_interval (Option A)"
    - "Explicit canonical values are never clobbered by the legacy shim; negative/inverted legacy values still raise ValidationError"
  artifacts:
    - path: "core/config_schema.py"
      provides: "5 community models declare delay_seconds/delay_jitter + shared _shim_legacy_delay_fields model_validator; Amazon/BestBuy delay fields gain Field(ge=0.0)"
      contains: "_shim_legacy_delay_fields"
    - path: "core/orchestrator.py"
      provides: "_get_plugin_sleep reads canonical delay_seconds/delay_jitter for all 7 platforms"
      contains: "delay_seconds"
    - path: "tests/test_config_schema.py"
      provides: "legacy-shim, canonical-load, precedence, and deprecation-warning tests"
      contains: "_legacy_delay_shim"
    - path: "tests/test_orchestrator_jitter.py"
      provides: "distribution-equivalence test + Amazon jitter-activation test"
      contains: "30.0 <= result <= 40.0"
    - path: "sample.config.yml"
      provides: "canonical field names documented + back-compat note near platforms:"
      contains: "delay_seconds"
  key_links:
    - from: "core/orchestrator.py:_get_plugin_sleep"
      to: "platform_cfg.delay_seconds / platform_cfg.delay_jitter"
      via: "getattr(platform_cfg, 'delay_seconds', None) then delay_seconds + random.uniform(0, delay_jitter)"
      pattern: "getattr\\(platform_cfg, \"delay_seconds\""
    - from: "5 community platform models"
      to: "_shim_legacy_delay_fields"
      via: "@model_validator(mode='before')"
      pattern: "model_validator\\(mode=\"before\"\\)"
---

<objective>
Harmonize the platform delay-config field names to a single canonical scheme (`delay_seconds`/`delay_jitter`) across all 7 platforms, with a back-compat shim that maps the legacy `min_delay`/`max_delay` names to canonical without changing the 5 community plugins' effective delay distribution. Update the sole runtime consumer (`_get_plugin_sleep`) to read canonical fields uniformly — which, per the accepted Option A decision, activates poll-cadence jitter for Amazon/BestBuy for the first time (flat 30s -> 30-40s).

Purpose: CFG-01 — one field name everywhere, old configs keep loading, community timing preserved exactly.
Output: renamed community models + shared shim + canonical `_get_plugin_sleep` + updated/added tests proving both old- and new-style configs load, distribution equivalence for community plugins, and the documented Amazon/BestBuy jitter activation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-config-refactor/33-CONTEXT.md
@.planning/phases/33-config-refactor/33-RESEARCH.md
@.planning/phases/33-config-refactor/33-VALIDATION.md

<interfaces>
<!-- Exact contracts the executor needs. Do not re-explore the codebase. -->

Legacy->canonical shim mapping (behavior-preserving, algebraically exact — RESEARCH.md "Exact shim mapping"):
  delay_seconds := min_delay
  delay_jitter  := max_delay - min_delay
Consumed as: delay_seconds + random.uniform(0, delay_jitter)  ==  random.uniform(min_delay, max_delay)

Current community model shape (core/config_schema.py:89-139) — 5 identical models
(Walmart/Target/GameStop/SquareEnix/Newegg):
  min_delay: float = Field(default=8.0, ge=0.0)
  max_delay: float = Field(default=15.0, ge=0.0)
  headless: bool = True
  user_agents: list[str] = Field(default_factory=list)
  session_persistence: bool = False

Current Amazon/BestBuy model shape (core/config_schema.py:67-86) — already canonical, KEEP field names:
  delay_seconds: float = 30.0    # -> add Field(default=30.0, ge=0.0)
  delay_jitter: float = 10.0     # -> add Field(default=10.0, ge=0.0)
  headless / user_agents / session_persistence unchanged

Current sole consumer (core/orchestrator.py:292-312) — reads min_delay/max_delay today:
  min_delay = getattr(platform_cfg, "min_delay", None)
  max_delay = getattr(platform_cfg, "max_delay", None)
  if min_delay is None or max_delay is None: return poll_interval
  return random.uniform(min_delay, max_delay)

Established precedents in this same file to mirror:
  - AppConfig.warn_legacy_keys (config_schema.py:309-325): module-level dict + warnings.warn(DeprecationWarning) + @model_validator(mode="before") — the shim mirrors this idiom.
  - core/retry.py:35-44 compute_delay: base + rng.uniform(0, jitter) — the canonical consumption formula.
  - pydantic already imported in config_schema.py: BaseModel, Field, field_validator, model_validator.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write/update failing tests for canonical fields, shim, distribution equivalence, and Amazon jitter activation</name>
  <files>tests/test_config_schema.py, tests/test_orchestrator_jitter.py</files>
  <read_first>
    - tests/test_config_schema.py (existing walmart/squareenix/negative-delay/legacy-key tests)
    - tests/test_orchestrator_jitter.py (existing walmart-range + amazon-fallback tests)
    - 33-RESEARCH.md sections "Regression Surface" (the two test-file tables) and "Exact shim mapping"
    - 33-VALIDATION.md "Per-Requirement Verification Map"
  </read_first>
  <behavior>
    - Legacy config: WalmartPlatformConfig(min_delay=8.0, max_delay=15.0) -> .delay_seconds == 8.0 and .delay_jitter == 7.0
    - Canonical config: WalmartPlatformConfig(delay_seconds=8.0, delay_jitter=7.0) -> same attributes
    - Precedence: WalmartPlatformConfig(min_delay=1, max_delay=2, delay_seconds=99, delay_jitter=55) -> .delay_seconds == 99.0, .delay_jitter == 55.0 (canonical never clobbered)
    - Legacy keys emit a DeprecationWarning mentioning min_delay/max_delay
    - Negative/inverted legacy values still raise ValidationError (delay_seconds or delay_jitter goes < 0 -> Field(ge=0.0) rejects)
    - Distribution equivalence: a legacy walmart config driven through _get_plugin_sleep yields values in [8.0, 15.0] over many iterations (identical to the legacy uniform(8,15) range)
    - Amazon jitter activation (Option A): AmazonPlatformConfig(delay_seconds=30.0, delay_jitter=10.0) through _get_plugin_sleep yields 30.0 <= result <= 40.0
  </behavior>
  <action>
    In tests/test_config_schema.py: update test_walmart_platform_fields_load_from_yaml to keep its legacy YAML input (min_delay:8, max_delay:15) but change its assertions to config.platforms.walmart.delay_seconds == 8.0 and config.platforms.walmart.delay_jitter == 7.0 (keep the headless/user_agents assertions). Update test_squareenix_platform_config_reachable the same way (min_delay:5, max_delay:10 -> delay_seconds == 5.0, delay_jitter == 5.0), and do NOT change the squareenix YAML key. Add test_legacy_delay_shim_maps_to_canonical constructing WalmartPlatformConfig(min_delay=8.0, max_delay=15.0) directly and asserting delay_seconds == 8.0 and delay_jitter == 7.0, plus a canonical-construction variant WalmartPlatformConfig(delay_seconds=8.0, delay_jitter=7.0) asserting the same. Add test_canonical_delay_precedence_over_legacy proving explicit canonical wins (delay_seconds == 99.0, delay_jitter == 55.0). Add test_legacy_delay_shim_emits_deprecation_warning mirroring the existing test_legacy_key_warning pattern (record warnings, assert a DeprecationWarning whose message contains "min_delay"). Leave test_negative_min_delay_raises_validation_error and test_negative_max_delay_raises_validation_error unchanged (they must stay green post-shim). In tests/test_orchestrator_jitter.py: keep test_jitter_in_range_for_walmart_config and test_jitter_stays_in_range_over_50_iterations (they pass through the shim) and add a canonical-construction sibling test_jitter_in_range_for_walmart_canonical_config using WalmartPlatformConfig(delay_seconds=8.0, delay_jitter=7.0) asserting the result is in [8.0, 15.0]. Rewrite test_fallback_for_amazon_shaped_config's body and docstring to assert the NEW behavior: build AmazonPlatformConfig(delay_seconds=30.0, delay_jitter=10.0), call _get_plugin_sleep, assert 30.0 <= result <= 40.0 (rename it to test_amazon_config_activates_poll_jitter for clarity). Add test_legacy_community_config_preserves_delay_distribution: load a legacy walmart config through the shim, assert delay_seconds == 8.0 / delay_jitter == 7.0, then call _get_plugin_sleep 200 times and assert every result is within [8.0, 15.0]. Run the tests to confirm they FAIL (RED) against the current unmodified schema/orchestrator. Do not touch config_schema.py or orchestrator.py in this task.
  </action>
  <acceptance_criteria>
    - test_legacy_delay_shim_maps_to_canonical exists and asserts delay_seconds == 8.0 AND delay_jitter == 7.0 for a legacy-key construction
    - test_canonical_delay_precedence_over_legacy asserts delay_seconds == 99.0 AND delay_jitter == 55.0
    - test_legacy_delay_shim_emits_deprecation_warning asserts a DeprecationWarning containing "min_delay"
    - The Amazon-shaped test asserts 30.0 <= result <= 40.0 (no remaining `== 30.0` assertion for the amazon-shaped config)
    - test_legacy_community_config_preserves_delay_distribution asserts all 200 results in [8.0, 15.0]
    - Running the two files RED: the new/updated tests fail because delay_seconds/delay_jitter do not yet exist on community models and _get_plugin_sleep still reads min_delay/max_delay
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_config_schema.py tests/test_orchestrator_jitter.py -q</automated>
  </verify>
  <done>Tests written and confirmed RED (targeted failures on the new canonical/shim/jitter assertions; no import errors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add shared legacy-delay shim, rename 5 community models to canonical fields, tighten Amazon/BestBuy bounds, document sample config</name>
  <files>core/config_schema.py, sample.config.yml</files>
  <read_first>
    - core/config_schema.py:34-150 (the _LEGACY_KEYS module const, the 5 community models, Amazon/BestBuy models, PlatformsConfig)
    - core/config_schema.py:309-325 (warn_legacy_keys precedent)
    - 33-RESEARCH.md sections "Pydantic v2 shim mechanism", "Full 5-model rename pattern", "Anti-Patterns to Avoid", Pitfalls 1-5
  </read_first>
  <action>
    Add a module-level function _shim_legacy_delay_fields(data) near the existing _LEGACY_KEYS constant. It must: return data unchanged if data is not a dict; compute has_legacy = "min_delay" in data or "max_delay" in data and has_canonical = "delay_seconds" in data or "delay_jitter" in data; only when has_legacy and not has_canonical, copy the dict, emit a warnings.warn(..., DeprecationWarning, stacklevel=2) message that names min_delay/max_delay as deprecated and points to delay_seconds/delay_jitter, pop min_delay (default 8.0) and max_delay (default 15.0) coerced through float(), then set data["delay_seconds"] = min_d and data["delay_jitter"] = max_d - min_d. Do NOT clamp the derived delay_jitter — let a negative derived value flow into Field(ge=0.0) and raise naturally (Pitfall 3). On each of the 5 community models (WalmartPlatformConfig, TargetPlatformConfig, GameStopPlatformConfig, SquareEnixPlatformConfig, NeweggPlatformConfig): replace the min_delay field with delay_seconds: float = Field(default=8.0, ge=0.0), replace max_delay with delay_jitter: float = Field(default=7.0, ge=0.0), leave headless/user_agents/session_persistence and every docstring untouched, and add a classmethod _legacy_delay_shim decorated with @model_validator(mode="before") that returns _shim_legacy_delay_fields(data). Do NOT rename the squareenix YAML key or the SquareEnix docstring's "no underscore" note (Pitfall 4). On AmazonPlatformConfig and BestBuyPlatformConfig only, change the bare delay_seconds: float = 30.0 / delay_jitter: float = 10.0 declarations to Field(default=30.0, ge=0.0) / Field(default=10.0, ge=0.0) for schema consistency (Assumption A2); do not add a shim to these two (they already use canonical names). Leave PlatformsConfig untouched in this plan. In sample.config.yml, add a comment block immediately above the existing platforms: key stating that all 7 platforms use the canonical delay_seconds/delay_jitter fields and that the legacy min_delay/max_delay names still load via a deprecated back-compat shim; do not invent a new community-platform example block.
  </action>
  <acceptance_criteria>
    - _shim_legacy_delay_fields exists as a module-level function guarded by `has_legacy and not has_canonical`, uses float() coercion, sets delay_seconds := min_delay and delay_jitter := (max_delay - min_delay), and does NOT clamp
    - All 5 community models declare delay_seconds and delay_jitter (Field ge=0.0) and NO longer declare min_delay/max_delay; each has an @model_validator(mode="before") calling _shim_legacy_delay_fields
    - AmazonPlatformConfig/BestBuyPlatformConfig delay_seconds/delay_jitter now use Field(..., ge=0.0)
    - The squareenix config key is still `squareenix` (no underscore), unchanged
    - sample.config.yml has a comment near platforms: documenting canonical names + the back-compat shim
    - No community model still DECLARES the legacy fields: `grep -E '^[[:space:]]*(min_delay|max_delay)[[:space:]]*:[[:space:]]*float' core/config_schema.py` returns 0. (The shim function legitimately still references the legacy key NAMES in its has_legacy check, its DeprecationWarning message text, and its data.pop("min_delay")/data.pop("max_delay") calls — a whole-file `grep -c 'min_delay\|max_delay'` therefore returns ~4, which is EXPECTED and correct; only field DECLARATIONS must be gone.)
    - tests/test_config_schema.py passes (config-schema half GREEN)
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_config_schema.py -q</automated>
  </verify>
  <done>All 5 community models renamed to canonical fields with the shared shim; Amazon/BestBuy bounds tightened; sample.config.yml documented; test_config_schema.py green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — update _get_plugin_sleep to consume canonical fields uniformly (Option A jitter activation)</name>
  <files>core/orchestrator.py</files>
  <read_first>
    - core/orchestrator.py:292-312 (_get_plugin_sleep) and its call sites at lines 343, 372
    - 33-RESEARCH.md sections "_get_plugin_sleep update (Option A, recommended)", "CRITICAL FINDING", Pitfall 6, Assumption A1
    - 33-CONTEXT.md the CORRECTION / Option A acceptance block
  </read_first>
  <behavior>
    - Community plugin (walmart, delay_seconds=8, delay_jitter=7): _get_plugin_sleep returns a float in [8.0, 15.0]
    - Amazon/BestBuy (delay_seconds=30, delay_jitter=10): _get_plugin_sleep returns a float in [30.0, 40.0] (was flat 30.0)
    - Missing platform_key, missing platform_cfg, or a platform_cfg lacking either canonical attribute -> returns poll_interval unchanged
  </behavior>
  <action>
    In _get_plugin_sleep, replace the two getattr reads of "min_delay"/"max_delay" with getattr(platform_cfg, "delay_seconds", None) and getattr(platform_cfg, "delay_jitter", None); keep the None-guard (if either is None, return poll_interval); change the return expression to delay_seconds + random.uniform(0, delay_jitter). Preserve the outer try/except Exception: return poll_interval and the earlier platform_key / platform_cfg None-guards exactly as they are (do not widen or narrow them — the explicit getattr None-checks, not the blanket except, are what detect an absent field per Pitfall 6). Update the docstring to state it reads canonical delay_seconds/delay_jitter and returns delay_seconds + random.uniform(0, delay_jitter), noting that Amazon/BestBuy now receive poll jitter (Option A) rather than a flat poll_interval fallback. Do not add any per-platform hardcoding or allowlist (that is the rejected Option B). This is the only functional min_delay/max_delay consumer in the repo.
  </action>
  <acceptance_criteria>
    - _get_plugin_sleep reads delay_seconds and delay_jitter via getattr (no remaining "min_delay"/"max_delay" reads in orchestrator.py: grep -c 'min_delay\|max_delay' core/orchestrator.py returns 0)
    - The return path is `delay_seconds + random.uniform(0, delay_jitter)`
    - The platform_key None-guard, platform_cfg None-guard, the delay-field None-guard, and the outer try/except are all preserved
    - No platform-name string literal (e.g. "amazon"/"walmart") is added inside _get_plugin_sleep
    - tests/test_orchestrator_jitter.py passes, including the rewritten Amazon jitter-activation test (30-40s) and the distribution-equivalence test
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_config_schema.py tests/test_orchestrator_jitter.py -q</automated>
  </verify>
  <done>_get_plugin_sleep consumes canonical fields for all 7 platforms; both targeted test files green; Amazon/BestBuy jitter activation proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yml -> PlatformsConfig | Operator-authored (or legacy) YAML crosses into typed pydantic validation; the delay shim transforms it before field validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | _shim_legacy_delay_fields | mitigate | `float(data.pop("min_delay"/"max_delay"))` raises ValueError/TypeError on non-numeric YAML (string/list) -> config load fails loudly at startup, not silent misbehavior |
| T-33-02 | Tampering | community model Field(ge=0.0) | mitigate | Inverted legacy range (max_delay < min_delay) -> derived delay_jitter < 0 -> Field(ge=0.0) raises ValidationError; shim MUST NOT clamp (Pitfall 3) — proven by the two negative-delay tests staying green |
| T-33-03 | Tampering | shim precedence guard | mitigate | `has_legacy and not has_canonical` guard prevents legacy keys from silently overwriting an explicit delay_seconds/delay_jitter (Pitfall 2) — proven by test_canonical_delay_precedence_over_legacy |
| T-33-04 | Repudiation/Info | _get_plugin_sleep except Exception | accept | Blanket except could mask a renamed-field bug as "no jitter"; mitigated by the distribution-equivalence + range tests asserting the actual jittered range (not merely "no exception") per Pitfall 6 |
| T-33-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase (internal refactor of already-pinned pydantic 2.13.3); slopcheck N/A |
</threat_model>

<verification>
- `.venv\Scripts\python.exe -m pytest tests/test_config_schema.py tests/test_orchestrator_jitter.py -q` green.
- Full suite `.venv\Scripts\python.exe -m pytest -q` green (baseline 889 passed, 2 skipped, plus the new CFG-01 tests) BEFORE CFG-02 (33-02) starts — CFG-01 must land clean.
- `grep -c 'min_delay\|max_delay' core/orchestrator.py` returns 0.
- Operator-UAT item to record in the SUMMARY: live Amazon/BestBuy availability-poll cadence is now jittered 30-40s (was flat 30s) — Option A behavior change, observable only against a live run (already logged in 33-VALIDATION.md Manual-Only Verifications).
</verification>

<success_criteria>
- Legacy (min_delay/max_delay) AND canonical (delay_seconds/delay_jitter) configs both load; shim mapping proven exact (delay_seconds == min_delay, delay_jitter == max_delay - min_delay).
- 5 community plugins' effective delay distribution preserved (test-proven range identity).
- Amazon/BestBuy jitter-activation test updated and green (30-40s).
- Negative/inverted legacy values still raise ValidationError; explicit canonical never clobbered.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/33-config-refactor/33-01-SUMMARY.md` when done. Record the Amazon/BestBuy poll-cadence operator-UAT item in the SUMMARY.
</output>
