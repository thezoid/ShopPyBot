---
phase: 30-breakfix-hardening
plan: 05
type: execute
wave: 3
depends_on: ["30-03", "30-04"]
files_modified:
  - plugins/shopbot_plugin_amazon.py
  - plugins/shopbot_plugin_bestbuy.py
  - tests/test_plugin_amazon.py
  - tests/test_plugin_bestbuy.py
autonomous: true
requirements: [BF-03]

must_haves:
  truths:
    - "Amazon login returns True only after _verify_login_generic confirms; save_session only fires on a confirmed login"
    - "BestBuy login returns True only after a redirect off /identity/signin"
    - "Both plugins set _checkout_stage='login' before login() and abort auto_buy (return False) on a failed login"
  artifacts:
    - path: "plugins/shopbot_plugin_amazon.py"
      provides: "login(self) -> bool with post-login verification + auto_buy abort-on-False"
      contains: "_verify_login_generic"
    - path: "plugins/shopbot_plugin_bestbuy.py"
      provides: "login(self) -> bool with redirect-off-signin verification + auto_buy abort-on-False"
      contains: "_verify_login_generic"
  key_links:
    - from: "plugins/shopbot_plugin_amazon.py:login"
      to: "core/plugin_base._verify_login_generic"
      via: "_verify_login_generic(tab, '/ap/signin', '#ap_email') before return True"
      pattern: "_verify_login_generic"
    - from: "plugins/shopbot_plugin_amazon.py:auto_buy"
      to: "self._checkout_stage / login() return"
      via: "_checkout_stage='login' then abort on False"
      pattern: "_checkout_stage = \"login\""
---

<objective>
Convert the two live-tested plugins' `login()` to the BF-03 `-> bool` contract with real post-login verification, and make `auto_buy()` abort on a failed login while setting the `_checkout_stage="login"` signal the orchestrator short-circuit (30-01, Task 3) reads. Depends on 30-03 (the shared `_verify_login_generic` helper + ABC contract) and lands after 30-04 (which also edits these two plugin files).

Purpose: Amazon/BestBuy — the pair with verifiable signals — never proceed to checkout on an unconfirmed login (D-13), and a failed login is surfaced (not looped).
Output: `login(self) -> bool` for Amazon and BestBuy, each with a verification call and `save_session()` moved after verification; `auto_buy` abort + stage signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-breakfix-hardening/30-CONTEXT.md
@.planning/phases/30-breakfix-hardening/30-RESEARCH.md
@.planning/phases/30-breakfix-hardening/30-PATTERNS.md
@.planning/phases/30-breakfix-hardening/30-VALIDATION.md
@.planning/phases/30-breakfix-hardening/30-03-SUMMARY.md

<interfaces>
<!-- Contracts extracted from live source + plan 30-03 output. Do not re-explore. -->

core/plugin_base.py (from plan 30-03):
  async def _verify_login_generic(self, tab, signin_url_fragment: str, form_selector: str) -> bool
    #   False if signin_url_fragment in tab.target.url; else True iff form_selector absent; any exception → False (never raises)
  async def login(self) -> bool   # ABC default returns True

plugins/shopbot_plugin_amazon.py:
  async def login(self) -> None    # :299-364 — convert to -> bool; every early bare/implicit-None return → return False;
                                   #   success path: after "Signed in to Amazon", verify then return True; save_session moves AFTER verify
  async def auto_buy(self, url)    # login() called at :389 — the ONLY plugin where login precedes DOM interaction (Pitfall 3)

plugins/shopbot_plugin_bestbuy.py:
  async def login(self) -> None    # :224-253 — convert to -> bool (same rules)
  async def auto_buy(self, url)    # login() called at :349 — AFTER add-to-cart/checkout-proceed (Pitfall 3)

Verification signals (D-12):
  Amazon  → _verify_login_generic(tab, "/ap/signin", "#ap_email")
  BestBuy → _verify_login_generic(tab, "/identity/signin", "#fld-e")   # tighter = redirect off /identity/signin
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amazon login() -> bool + verification + auto_buy abort</name>
  <files>plugins/shopbot_plugin_amazon.py, tests/test_plugin_amazon.py</files>
  <read_first>
    - plugins/shopbot_plugin_amazon.py (login :299-364; auto_buy login call :389)
    - core/plugin_base.py (_verify_login_generic, from 30-03)
    - .planning/phases/30-breakfix-hardening/30-PATTERNS.md (§amazon login — exact early-return sites; save_session-after-verify)
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Pitfall 3 — Amazon is the one plugin where login precedes DOM interaction; D-14/D-15)
  </read_first>
  <behavior>
    - login returns False on missing creds, missing #ap_email, missing #ap_password, missing #signInSubmit, or exception
    - login returns True only after _verify_login_generic(tab, "/ap/signin", "#ap_email") confirms
    - save_session() is called only after verification passes
    - auto_buy sets _checkout_stage="login" before login() and returns False (abort) when login() is False
  </behavior>
  <action>
    Convert Amazon `login(self) -> None` to `-> bool`. Every early bare/implicit-None `return` (missing AMZ_EMAIL/AMZ_PASSWORD, missing `#ap_email`, missing `#ap_password`, missing `#signInSubmit`) becomes `return False`; the `except Exception` handler returns `False`. After the final `writeLog("Signed in to Amazon", ...)`, call `verified = await self._verify_login_generic(tab, "/ap/signin", "#ap_email")`; if `not verified`, `writeLog` WARNING and `return False`. MOVE `await self.save_session()` to AFTER verification passes (only persist a confirmed login), then `return True`. In `auto_buy`, set `self._checkout_stage = "login"` immediately before `await self.login()` (amazon:389) and, when `login()` returns False, abort by returning False (Amazon is the one plugin where login precedes DOM interaction, but implement uniformly as "abort all remaining stages on False", Pitfall 3). Per D-12/D-13/D-14/D-15. Log with `exc.__class__.__name__` only (V7).
  </action>
  <acceptance_criteria>
    - Amazon `login` signature is `-> bool`; all early returns are `return False`; success path calls `_verify_login_generic(tab, "/ap/signin", "#ap_email")` before `return True`; `save_session()` is after verification
    - `auto_buy` sets `self._checkout_stage = "login"` before `await self.login()` and returns False when login() is False
    - `pytest tests/test_plugin_amazon.py -x -q` green (login False on missing creds/DOM/exception; True only when verified; auto_buy aborts before place-order on login False)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_plugin_amazon.py -x -q</automated>
  </verify>
  <done>Amazon login is verified before success; save_session gated on verification; auto_buy aborts and signals stage=login on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BestBuy login() -> bool + verification + auto_buy abort</name>
  <files>plugins/shopbot_plugin_bestbuy.py, tests/test_plugin_bestbuy.py</files>
  <read_first>
    - plugins/shopbot_plugin_bestbuy.py (login :224-253; auto_buy login call :349)
    - core/plugin_base.py (_verify_login_generic, from 30-03)
    - .planning/phases/30-breakfix-hardening/30-PATTERNS.md (§bestbuy login — redirect-off-/identity/signin signal; Pitfall 3 ordering note)
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Pitfall 3 — BestBuy runs login AFTER add-to-cart/checkout; D-15 = abort remaining stages, NOT no-add-to-cart)
  </read_first>
  <behavior>
    - login returns False on missing creds or exception
    - login returns True only after _verify_login_generic(tab, "/identity/signin", "#fld-e") confirms (redirect off /identity/signin)
    - save_session() only after verification passes
    - auto_buy sets _checkout_stage="login" before login() and returns False (abort remaining stages) when login() is False
  </behavior>
  <action>
    Convert BestBuy `login(self) -> None` to `-> bool`: missing BB_EMAIL/BB_PASSWORD → `return False`; the `except Exception` handler returns `False`. After `writeLog("Signed in to BestBuy", ...)`, call `await self._verify_login_generic(tab, "/identity/signin", "#fld-e")` (tighter signal = redirect off `/identity/signin`; the landing-page selector is unverified so URL-off-signin is the honest available signal, D-12); `return False` if not verified. MOVE `await self.save_session()` to AFTER verification, then `return True`. In `auto_buy`, set `self._checkout_stage = "login"` immediately before `await self.login()` (bestbuy:349) and return False (abort) when login() is False. IMPORTANT (Pitfall 3): BestBuy runs `login()` AFTER add-to-cart/checkout-proceed — implement D-15 as "return False immediately, aborting all remaining stages", NOT "no add-to-cart occurred"; do not gate the test on no-add-to-cart. Per D-12/D-13/D-14/D-15. Log with `exc.__class__.__name__` only (V7).
  </action>
  <acceptance_criteria>
    - BestBuy `login` signature is `-> bool`; early returns are `return False`; success path calls `_verify_login_generic(tab, "/identity/signin", "#fld-e")` before `return True`; `save_session()` after verification
    - `auto_buy` sets `self._checkout_stage = "login"` before `await self.login()` and returns False when login() is False
    - `pytest tests/test_plugin_bestbuy.py -x -q` green (acceptance NOT gated on no-add-to-cart)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_plugin_bestbuy.py -x -q</automated>
  </verify>
  <done>BestBuy login is verified before success; save_session gated; auto_buy aborts remaining stages and signals stage=login on failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retail sign-in page → plugin checkout flow | Post-submit DOM/URL state is untrusted; a submit click is not proof of authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Spoofing | Amazon/BestBuy proceeding to checkout on an unconfirmed login | mitigate | `login()` returns True only after `_verify_login_generic` confirms (Amazon `/ap/signin`+`#ap_email`; BestBuy redirect off `/identity/signin`); ambiguous → False (D-13); `save_session()` gated on verification |
| T-30-03c | Spoofing | a failed login being retried as if transient | mitigate | `auto_buy` sets `_checkout_stage="login"` + aborts on False, feeding the orchestrator login-failure short-circuit (30-01, Task 3) |
| T-30-V7 | Information Disclosure | credential leak via exception text | mitigate | all handlers log `exc.__class__.__name__` only; never `str(exc)`; never log credentials (enforced by source-grep tests) |

No package-manager installs occur in this plan — no `T-30-SC` checkpoint required.
</threat_model>

<verification>
- `pytest tests/test_plugin_amazon.py tests/test_plugin_bestbuy.py -x -q` green.
- Both logins return False on missing creds/DOM/exception and True only when verified; save_session gated on verification.
- Both auto_buy paths set stage=login and abort on failed login.
</verification>

<success_criteria>
- BF-03 success criterion 3 satisfied for the live-tested pair: a failed/ambiguous login is not reported successful; verification uses real post-login signals.
- Uniform D-15 abort implemented regardless of where login() sits in each flow.
</success_criteria>

<output>
Create `.planning/phases/30-breakfix-hardening/30-05-SUMMARY.md` when done.
</output>
