---
phase: 30-breakfix-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/shopbot_plugin_amazon.py
  - tests/test_captcha_plugin.py
autonomous: true
requirements: [BF-01]

must_haves:
  truths:
    - "Amazon WAF detection triggers a single 2captcha solve attempt when the solver is available"
    - "Amazon WAF falls back to the manual pause on decode failure, solver-unavailable, solve failure/timeout, or injection failure"
    - "The WAF voucher/token is json.dumps()-escaped and rejected on quote/backslash/newline before injection"
  artifacts:
    - path: "plugins/shopbot_plugin_amazon.py"
      provides: "_solve_or_pause WAF branch calling solve_amazon_waf + _inject_waf_token helper"
      contains: "solve_amazon_waf"
  key_links:
    - from: "plugins/shopbot_plugin_amazon.py:_solve_or_pause"
      to: "CaptchaSolver.solve_amazon_waf"
      via: "loop.run_in_executor under asyncio.timeout(120)"
      pattern: "solve_amazon_waf"
    - from: "plugins/shopbot_plugin_amazon.py:_inject_waf_token"
      to: "tab (Chromium)"
      via: "escaped injection after char rejection"
      pattern: "_inject_waf_token"
---

<objective>
Wire the Amazon AWS-WAF challenge path to the already-implemented, already-unit-tested `CaptchaSolver.solve_amazon_waf` solver, replacing the current "auto-solve deferred; falling back to manual pause" stub — while preserving the manual-pause fallback on every non-success path. This is a structural copy of the reCAPTCHA branch two sections down in the same function; the solver itself needs zero changes.

Purpose: Reduce unattended-run stalls when `captcha.enabled=true` with a funded 2captcha balance, without ever hard-failing an item.
Output: A rewired WAF branch in `_solve_or_pause`, a new `_inject_waf_token` injection helper, and two pre-fix tests rewritten to assert the new behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-breakfix-hardening/30-CONTEXT.md
@.planning/phases/30-breakfix-hardening/30-RESEARCH.md
@.planning/phases/30-breakfix-hardening/30-PATTERNS.md
@.planning/phases/30-breakfix-hardening/30-VALIDATION.md

<interfaces>
<!-- Contracts extracted from live source. Do not re-explore. -->

core/captcha.py:
  def can_solve(self) -> bool                                   # :151-153 (balance_ok AND _solve_count < _max_solves; shared reCAPTCHA/WAF budget)
  def solve_amazon_waf(self, key, iv, context, pageurl) -> dict # :169-204 — blocking; RAISES RuntimeError if can_solve() is False;
                                                                #   returns {"captcha_voucher": str, "existing_token": str}

plugins/shopbot_plugin_amazon.py:
  _WAF_PROBE_JS   # :59-63 — window.gokuProps probe → JSON {key, iv, context}
  async def _wait_user_action(self, event, message)   # :87-105 — the manual-pause fallback (sound + event/stdin listener, NOT input())
  async def _inject_token(self, tab, token: str)      # :114-126 — reCAPTCHA injection: _json.dumps(token) escape; REUSE this exact escaping shape
  async def _solve_or_pause(self, tab, pageurl)       # :128-195 — WAF branch to replace at :147-158; solve_recaptcha reference branch at :168-195
  # reCAPTCHA reference pattern (:168-187): loop.run_in_executor(None, solver.solve_recaptcha, sitekey, pageurl)
  #   wrapped in `async with asyncio.timeout(120):`, `except (asyncio.TimeoutError, Exception) as exc: _log.warning("...%s", exc.__class__.__name__)`
  #   then CR-02 validation: reject token containing `'`, `\`, or `\n` before _inject_token
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire the WAF branch to solve_amazon_waf + add _inject_waf_token</name>
  <files>plugins/shopbot_plugin_amazon.py</files>
  <read_first>
    - plugins/shopbot_plugin_amazon.py (_WAF_PROBE_JS :59-63; _wait_user_action :87-105; _inject_token :114-126; _solve_or_pause WAF branch :147-158 and solve_recaptcha branch :168-195)
    - core/captcha.py (can_solve :151-153; solve_amazon_waf :169-204 — note the RuntimeError-when-not-can_solve() guard and the {captcha_voucher, existing_token} return)
    - .planning/phases/30-breakfix-hardening/30-PATTERNS.md (§amazon — Pattern 4 WAF wiring + "Token/voucher injection safety")
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Assumption A1 — injection payload is LOW-confidence, live acceptance is operator debt)
  </read_first>
  <behavior>
    - WAF detected + solver available + solve succeeds: solve_amazon_waf is called once, token injected, NO manual pause
    - WAF detected + solver None or can_solve() False: manual pause, solve_amazon_waf NOT called
    - solve raises / times out: manual pause (graceful, no crash)
    - injection raises: manual pause (graceful)
    - voucher/token containing quote/backslash/newline: rejected before any tab injection → manual pause
  </behavior>
  <action>
    Replace the WAF branch in `_solve_or_pause` (amazon:147-158) that currently early-returns to manual pause (D-06: route the existing WAF branch to `solve_amazon_waf`). Keep the `_WAF_PROBE_JS` probe; decode `waf_raw` with `_json.loads` into `{key, iv, context}`, and on decode failure call `await self._wait_user_action(self.captcha_event, ...)` + return. Gate on solver availability per D-08/D-09: if the solver is None OR `not solver.can_solve()`, fall back to manual pause (do NOT call `solve_amazon_waf` — it raises `RuntimeError` when `can_solve()` is False). Otherwise call `solver.solve_amazon_waf(key, iv, context, pageurl)` via `await loop.run_in_executor(...)` wrapped in `async with asyncio.timeout(120):`, mirroring the solve_recaptcha branch — a SINGLE attempt, no retry loop (D-07: the ~30s gokuProps window makes a second solve stale). On any `(asyncio.TimeoutError, Exception) as exc` → `_log.warning("... %s", exc.__class__.__name__)` + manual pause + return. On success, call a new `_inject_waf_token(self, tab, solution: dict)` and, if it raises, fall back to manual pause + return; on clean success return WITHOUT a manual pause. Add `_inject_waf_token` mirroring `_inject_token` (amazon:114-126): reject either of `solution["captcha_voucher"]` / `solution["existing_token"]` if it contains `'`, `\`, or `\n` (V5/CR-02) before injecting, and escape values via `_json.dumps()` before any `tab.evaluate(...)` interpolation. Best-effort injection mechanism (CDP `tab.send` set-cookies is the recommended shape per RESEARCH.md A1; a JS-eval path is acceptable) — the exact live payload is LOW-confidence and live-challenge acceptance is operator debt (out of scope); the test asserts the WIRING, not the live payload. WAF solving must never hard-fail the item (D-08). Log with `exc.__class__.__name__` only; never log the 2captcha key (V7).
  </action>
  <acceptance_criteria>
    - shopbot_plugin_amazon.py contains a `solve_amazon_waf(` call site inside `_solve_or_pause`
    - shopbot_plugin_amazon.py defines `async def _inject_waf_token` that applies `_json.dumps()` escaping and rejects quote/backslash/newline before injection
    - the WAF branch falls back to `_wait_user_action` on decode failure, solver-unavailable/`can_solve()` False, solve failure/timeout, and injection failure
    - no `for attempt in range(` loop is added (single attempt, D-07)
    - `pytest tests/test_captcha_plugin.py -x -q` green (with Task 2's rewritten tests)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_captcha_plugin.py -x -q</automated>
  </verify>
  <done>WAF branch calls the solver once when available, injects the escaped voucher, and preserves manual pause on every non-success path.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the two pre-fix WAF tests + assert both paths</name>
  <files>tests/test_captcha_plugin.py</files>
  <read_first>
    - tests/test_captcha_plugin.py (test_amazon_waf_detected_falls_to_manual_pause :143; test_amazon_source_does_not_call_solve_amazon_waf :357; test_amazon_source_no_str_exc :386; CR-02 inject tests)
    - .planning/phases/30-breakfix-hardening/30-RESEARCH.md (Pitfall 1 — these assert PRE-FIX behavior; CI red here after wiring is EXPECTED, rewrite not supplement)
    - .planning/phases/30-breakfix-hardening/30-VALIDATION.md (D-10 — mock the 2captcha call, assert both paths)
  </read_first>
  <action>
    REWRITE `test_amazon_waf_detected_falls_to_manual_pause` (do not leave the old assertion): with a mocked solver where `can_solve()` is True and `solve_amazon_waf` returns a valid voucher dict, assert `solve_amazon_waf` IS called and the voucher is injected with NO manual pause. Add a companion case where the solver is unavailable (`can_solve()` False) or `solve_amazon_waf` raises → assert the manual pause is invoked and nothing crashes (D-10). INVERT the source-grep guard `test_amazon_source_does_not_call_solve_amazon_waf` → rename/rewrite to assert the `solve_amazon_waf(` call site now EXISTS in `shopbot_plugin_amazon.py`. Add or extend an injection-escaping test (`-k inject`) asserting a voucher containing a quote/backslash/newline is rejected before `tab.evaluate`/cookie injection. Keep `test_amazon_source_no_str_exc` green (no `str(exc)` in the new handlers). Mock the 2captcha network call — live-challenge acceptance stays operator debt.
  </action>
  <acceptance_criteria>
    - `test_amazon_waf_detected_falls_to_manual_pause` rewritten to assert `solve_amazon_waf` is called and no pause on the success path
    - the source-grep guard is inverted (call site asserted present, not absent)
    - an injection-escaping test asserts quote/backslash/newline rejection
    - `pytest tests/test_captcha_plugin.py -x -q` green; `test_amazon_source_no_str_exc` still green
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_captcha_plugin.py -x -q</automated>
  </verify>
  <done>Both WAF paths (solve-success, fallback) are asserted; the stale pre-fix guards are replaced; no-str-exc guard stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2captcha response → Chromium tab | The solver's `captcha_voucher`/`existing_token` strings are untrusted input interpolated into `tab.evaluate(...)` / a cookie value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | untrusted solver response interpolated into JS/cookie in `_inject_waf_token` | mitigate | reuse the reCAPTCHA `_inject_token` control: reject quote/backslash/newline + `_json.dumps()` escaping before any `tab.evaluate()` interpolation (V5) |
| T-30-01b | Denial of Service | a failing/slow solve stalling the item indefinitely | mitigate | single attempt under `asyncio.timeout(120)` (D-07); every non-success path degrades to the existing manual pause, never hard-fails (D-08) |
| T-30-V7 | Information Disclosure | 2captcha API key / credential leak via exception text | mitigate | log `exc.__class__.__name__` only; never `str(exc)`; never log the 2captcha key (enforced by `test_amazon_source_no_str_exc`) |

No package-manager installs occur in this plan — no `T-30-SC` checkpoint required.

**Operator debt (out of scope, documented):** the exact live WAF token injection payload shape is undocumented by 2captcha (RESEARCH.md Assumption A1); live-challenge acceptance within the ~30s gokuProps window is tracked operator debt. Tests assert wiring, not live payload correctness.
</threat_model>

<verification>
- `pytest tests/test_captcha_plugin.py -x -q` green.
- Solve-success path: `solve_amazon_waf` called once, voucher injected, no manual pause.
- Fallback path: solver unavailable/failing → manual pause, no crash.
- Voucher with quote/backslash/newline rejected before injection.
</verification>

<success_criteria>
- BF-01 success criterion 2 satisfied: WAF path calls the 2captcha solver when a challenge is detected, manual-pause fallback preserved; both paths asserted with a mocked 2captcha call.
- Single-attempt policy (D-07) and shared budget/balance gate (D-09) honored.
</success_criteria>

<output>
Create `.planning/phases/30-breakfix-hardening/30-02-SUMMARY.md` when done.
</output>
