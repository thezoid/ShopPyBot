---
phase: 32-release-automation-community-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [RH-06]
must_haves:
  truths:
    - "README documents all 7 platforms: Amazon, BestBuy, Walmart, Target, GameStop, NewEgg, Square Enix"
    - "README states the Python 3.11+ prerequisite (no 3.8+)"
    - "README documents `pip install -e .[web]` install and the `shoppybot` console entry point"
    - "README uses the real clone URL github.com/thezoid/ShopPyBot.git (no yourusername placeholder)"
    - "Every README badge targets a workflow file that exists (ci.yml, codeql-analysis.yml, optionally gitleaks.yml); the three dead app_*Build.yml badges are gone; no fabricated license badge"
    - "README documents web dashboard/observability, price monitoring, anti-detection, and encrypted session persistence"
    - "README no longer instructs storing account credentials in config.yml (aligns with the env-var/credential-store model in SECURITY.md)"
  artifacts:
    - path: "README.md"
      provides: "accurate contributor/operator-facing project documentation"
      contains: "pip install -e .[web]"
  key_links:
    - from: "README.md badges"
      to: ".github/workflows/ci.yml, codeql-analysis.yml"
      via: "badge URL references an existing workflow file"
      pattern: "actions/workflows/(ci|codeql-analysis)\\.yml/badge\\.svg"
    - from: "README.md install section"
      to: "pyproject.toml [project.optional-dependencies].web + [project.scripts]"
      via: "pip install -e .[web] + shoppybot entry point"
      pattern: "pip install -e \\.\\[web\\]"
    - from: "README.md credential guidance"
      to: "SECURITY.md Credentials and Secrets"
      via: "env-var/credential-store model (no config.yml credentials)"
      pattern: "\\.env"
---

<objective>
Rewrite README.md so it accurately documents the current project for a new contributor or operator (RH-06): 7-platform ecosystem, web dashboard/observability, price monitoring, anti-detection, encrypted session persistence, the correct Python 3.11+ prereq, the pyproject-based install (`pip install -e .[web]`) + `shoppybot` entry point, the real clone URL, and working badges.

Purpose: the current README describes a pre-refactor, two-platform, `python main.py` / `requirements.txt` project with a placeholder clone URL, three dead build badges, and a credential instruction that contradicts the project's own security policy. Every one of these is wrong today (confirmed in 32-RESEARCH State-of-the-Art table).

Output: an accurate README.md. No application code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-release-automation-community-readiness/32-RESEARCH.md
@.planning/phases/32-release-automation-community-readiness/32-CONTEXT.md
@README.md
@pyproject.toml
@SECURITY.md

<interfaces>
<!-- Ground-truth for every README claim is the 32-RESEARCH "State of the Art" gap table (lines 306-324).
     Each row = old (wrong) value → correct value → how it was verified. Executor treats it as the checklist. -->
- 11-row README gap table: 32-RESEARCH.md lines 306-324
- Badge correctness + no-LICENSE rule: 32-RESEARCH.md Pitfall 3 (lines 187-191)
- Existing workflows that DO exist (valid badge targets): .github/workflows/ci.yml, codeql-analysis.yml, gitleaks.yml
- Credential model to align with: SECURITY.md "Credentials and Secrets" (env vars / .env / credential store; never config.yml)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix header, badges, prerequisites, install, run, and clone URL</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 1-99 (badge block, Setup/Prerequisites/Installation, Configuration, Running the Bot)
    - 32-RESEARCH.md State-of-the-Art table rows for: Python prereq, install command, run command, clone URL, badges (lines 308-319)
    - 32-RESEARCH.md Pitfall 3 (lines 187-191): valid badge targets are ci.yml / codeql-analysis.yml / gitleaks.yml; NO license badge (no LICENSE file exists).
  </read_first>
  <action>
    Replace the six dead badge lines (the master + dev blocks pointing at app_linuxBuild.yml / app_macBuild.yml / app_windowsBuild.yml) with badges that target only workflows that exist: a CI badge for actions/workflows/ci.yml/badge.svg and a CodeQL badge for actions/workflows/codeql-analysis.yml/badge.svg (optionally a gitleaks badge for gitleaks.yml/badge.svg). Do NOT add a license/shields.io license badge — no LICENSE file exists in the repo, so any license badge would be fabricated. Under Prerequisites, change "Python 3.8+" to "Python 3.11+". In the Installation section: change the clone URL from github.com/yourusername/ShopPyBot.git to github.com/thezoid/ShopPyBot.git, and replace the `pip install -r requirements.txt` step with `pip install -e .[web]` (this installs the FastAPI/uvicorn/jinja2/python-multipart web extra and registers the `shoppybot` console script). In the Running the Bot section, document `shoppybot` as the primary command (note `python main.py` still works as an alternative). Preserve the Disclaimer section (README lines 16-24) and the "Changing the Alert Sound" section verbatim — they are load-bearing legal/usage text, not in scope for change. Do not fabricate a platform/support-matrix section (only add the cryptography 64-bit-only note if a support section already exists — it does not, so skip it).
  </action>
  <acceptance_criteria>
    - `rg -q "Python 3\.11" README.md` succeeds; `rg -q "Python 3\.8" README.md` fails (0 matches)
    - `rg -q "pip install -e \.\[web\]" README.md` succeeds; the primary install step is no longer `pip install -r requirements.txt`
    - `rg -q "shoppybot" README.md` succeeds (entry point documented)
    - `rg -q "github\.com/thezoid/ShopPyBot" README.md` succeeds; `rg -q "yourusername" README.md` fails (0 matches)
    - `rg -c "app_(linux|mac|windows)Build\.yml" README.md` == 0 (all three dead badges removed)
    - Each remaining badge target resolves to an existing file: for every `actions/workflows/<name>.yml/badge.svg` in README, `.github/workflows/<name>.yml` exists
    - No license badge added: `rg -qi "img.shields.io/.*license|/blob/.*/LICENSE" README.md` fails (0 matches)
  </acceptance_criteria>
  <verify>
    <automated>rg -q "Python 3\.11" README.md && ! rg -q "Python 3\.8" README.md && rg -q "pip install -e \.\[web\]" README.md && rg -q "shoppybot" README.md && rg -q "github\.com/thezoid/ShopPyBot" README.md && ! rg -q "yourusername" README.md && [ "$(rg -c 'app_(linux|mac|windows)Build\.yml' README.md || echo 0)" = "0" ] && ! rg -qi "img.shields.io/.*license" README.md && echo "task1 OK"</automated>
  </verify>
  <done>Badges point only to existing workflows (no dead app_*Build badges, no fabricated license badge), prereq is 3.11+, install is `pip install -e .[web]`, run documents `shoppybot`, and the clone URL is the real repo.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Overview/Features for the 7-platform ecosystem and correct the credential model</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 13-31 (Overview, Features) and lines 73-83 (Configuration + credential instruction)
    - 32-RESEARCH.md State-of-the-Art rows for: 7 platforms, web dashboard, price monitoring, anti-detection, session persistence, and the config.yml-credentials contradiction (lines 314-320)
    - SECURITY.md "Credentials and Secrets" (env vars only; .env gitignored; never config.yml)
  </read_first>
  <action>
    Rewrite the Overview to describe ShopPyBot as a drop-in plugin framework that monitors availability and can auto-purchase across 7 retail platforms — Amazon, BestBuy, Walmart, Target, GameStop, NewEgg, and Square Enix — naming all seven. Expand the Features list to include: the plugin framework (community adds a platform via one file in plugins/, no core changes); the optional web dashboard with observability (live SSE status stream, health surface, plugin/level log filtering); price monitoring (target price / price-drop percentage); anti-detection (fingerprint + proxy rotation and 2captcha CAPTCHA solving); and encrypted session persistence (Fernet + scrypt) so logins survive restarts. In the Configuration section, remove the instruction that says to edit config.yml with Amazon/BestBuy account details; replace it with the correct model: config.yml holds only non-secret item/behavior settings, and account credentials go in environment variables (copy `.env.example` to `.env`, which is gitignored) per SECURITY.md — never commit credentials. Keep the item-monitoring configuration guidance. Fix the malformed Credits link at the end of the file (the Square Enix Sound Fan Kit line is missing its opening parenthesis). Do NOT delete root-level amazon_bot.py/bestbuy_bot.py or the _deprecated/ directory, and do NOT edit CLAUDE.md — those are out of RH-06 scope (dead-code cleanup / CLAUDE.md accuracy are separate concerns; note them as adjacent issues in the SUMMARY only).
  </action>
  <acceptance_criteria>
    - All 7 platform names present: `rg -qi "Amazon" README.md`, `Best ?Buy`, `Walmart`, `Target`, `GameStop`, `NewEgg`, `Square ?Enix` each match
    - Feature coverage present: `rg -qi "dashboard|observability" README.md`, `rg -qi "price" README.md`, `rg -qi "anti-detection|fingerprint|2captcha|captcha" README.md`, `rg -qi "session" README.md` each succeed
    - Plugin framework mentioned: `rg -qi "plugin" README.md` succeeds
    - Credential model corrected: `rg -qi "\.env" README.md` succeeds AND the old instruction is gone — `rg -qi "config\.yml to include your Amazon" README.md` fails (0 matches)
    - Malformed Credits link fixed: no `]http` sequence remains — `rg -c "\]http" README.md` == 0
  </acceptance_criteria>
  <verify>
    <automated>for p in "Amazon" "Best ?Buy" "Walmart" "Target" "GameStop" "NewEgg" "Square ?Enix"; do rg -qi "$p" README.md || { echo "MISSING $p"; exit 1; }; done && rg -qi "dashboard" README.md && rg -qi "price" README.md && rg -qi "fingerprint|2captcha|anti-detection" README.md && rg -qi "session" README.md && rg -qi "\.env" README.md && ! rg -qi "config\.yml to include your Amazon" README.md && [ "$(rg -c '\]http' README.md || echo 0)" = "0" ] && echo "task2 OK"</automated>
  </verify>
  <done>Overview + Features accurately describe the 7-platform plugin ecosystem, dashboard/observability, price monitoring, anti-detection, and session persistence; the credential instruction matches SECURITY.md's env-var model; the Credits link is well-formed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README instructions → new operator/contributor | A reader copy-pastes install/config steps; wrong guidance can lead to an insecure setup or non-functional commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Information Disclosure | README credential guidance | mitigate | Remove the "edit config.yml with account details" instruction; document env-var/`.env` model matching SECURITY.md so readers never place secrets in a tracked/committed file (Task 2 acceptance asserts the old string is gone and `.env` is present) |
| T-32-05 | Spoofing / Tampering | placeholder clone URL + dead badges | mitigate | Replace `yourusername` with the real `thezoid/ShopPyBot` URL and point badges only at existing workflows, so readers clone the authentic repo and see real CI status (Task 1) |
| T-32-06 | Tampering | fabricated license badge | mitigate | No license badge added (no LICENSE file exists); Task 1 asserts absence, preventing a misleading legal claim |
| T-32-SC | Tampering | npm/pip installs | accept | No package installs in this plan (docs only) |
</threat_model>

<verification>
- Cross-check README against the 32-RESEARCH State-of-the-Art gap table (lines 306-324): every "old" value is replaced by its "correct" value.
- All grep assertions in both tasks pass.

**Operator action items / adjacent issues (surface in SUMMARY, do NOT action here):**
1. No LICENSE file exists (`gh api repos/thezoid/ShopPyBot/license` → 404). The README intentionally carries no license badge. Operator should add a LICENSE (e.g. MIT) before/at first public release if the project is meant to be open-source-licensed.
2. Root-level `amazon_bot.py` / `bestbuy_bot.py` and the `_deprecated/` directory appear superseded by `plugins/shopbot_plugin_*.py`. Out of RH-06 scope (README accuracy, not dead-code removal) — flag only.
3. CLAUDE.md's Architecture section still describes the pre-refactor single-process design. Out of scope — flag only, do not edit CLAUDE.md.
</verification>

<success_criteria>
- [ ] README lists all 7 platforms (RH-06).
- [ ] README states Python 3.11+, `pip install -e .[web]`, `shoppybot` (RH-06).
- [ ] README uses the real clone URL; no `yourusername` (RH-06).
- [ ] All badges resolve to existing workflow files; no dead app_*Build badges; no fabricated license badge (RH-06).
- [ ] README documents dashboard/observability, price monitoring, anti-detection, session persistence, plugin framework (RH-06).
- [ ] Credential guidance aligns with SECURITY.md env-var model (RH-06).
- [ ] Adjacent issues (LICENSE, dead code, CLAUDE.md) recorded in SUMMARY, not actioned.
</success_criteria>

<output>
Create `.planning/phases/32-release-automation-community-readiness/32-02-SUMMARY.md` when done.
</output>
