---
phase: 32-release-automation-community-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - release-please-config.json
  - .release-please-manifest.json
  - .github/workflows/release-please.yml
autonomous: true
requirements: [RH-03, RH-02]
must_haves:
  truths:
    - "pyproject.toml [project].version is 2.0.0 and is the sole version source in the repo"
    - "release-please-config.json declares release-type python for the root package '.'"
    - ".release-please-manifest.json seeds '.' to 2.0.0, matching pyproject exactly"
    - ".github/workflows/release-please.yml is valid YAML, triggers on push to master, grants only contents:write + pull-requests:write, and uses googleapis/release-please-action@v5"
    - "the full pytest suite stays green after the version bump (no regression)"
  artifacts:
    - path: "pyproject.toml"
      provides: "canonical package version, reconciled to 2.0.0"
      contains: 'version = "2.0.0"'
    - path: "release-please-config.json"
      provides: "manifest-mode release-please config, python release-type"
      contains: '"release-type": "python"'
    - path: ".release-please-manifest.json"
      provides: "release-please version seed at 2.0.0"
      contains: '"." : "2.0.0"'
    - path: ".github/workflows/release-please.yml"
      provides: "release automation workflow (release PR + tag + changelog)"
      contains: "googleapis/release-please-action@v5"
  key_links:
    - from: ".release-please-manifest.json"
      to: "pyproject.toml"
      via: "version equality — both must read 2.0.0"
      pattern: '"2\.0\.0"'
    - from: ".github/workflows/release-please.yml"
      to: "release-please-config.json"
      via: "action auto-discovers root-level config/manifest (manifest mode)"
      pattern: "release-please-action@v5"
---

<objective>
Reconcile the single version source of truth to 2.0.0 (RH-03) and land release-please automation seeded at 2.0.0 (RH-02) so the repo can cut its first public release from conventional-commit history.

Purpose: release-please needs a correct pyproject source-of-truth and a matching manifest seed from its very first run; seeding at 2.0.0 makes release-please treat 2.0.0 as already-released and compute the next bump from commit history (it does NOT re-tag 2.0.0).

Output: `pyproject.toml` version = 2.0.0; new `release-please-config.json`, `.release-please-manifest.json`, and `.github/workflows/release-please.yml`.

Scope boundary: this plan LANDS the automation only. Actually cutting/publishing the first GitHub release (operator merges the release PR) is out of scope per CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-release-automation-community-readiness/32-RESEARCH.md
@.planning/phases/32-release-automation-community-readiness/32-CONTEXT.md
@pyproject.toml

<interfaces>
<!-- Exact file contents to author are in 32-RESEARCH.md "Code Examples" (lines 207-257).
     Executor uses those verbatim — no design decisions to make here. -->
- release-please-config.json shape: 32-RESEARCH.md lines 208-218
- .release-please-manifest.json shape: 32-RESEARCH.md lines 222-226
- release-please.yml shape: 32-RESEARCH.md lines 229-247
- pyproject reconcile: 32-RESEARCH.md lines 250-257
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile pyproject version to 2.0.0 (RH-03) and prove no regression</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (the [project] table — only the version line changes)
    - 32-RESEARCH.md "pyproject.toml version reconcile (RH-03)" (lines 249-257) and Summary (lines 8-9): pyproject is the ONLY version source; no __version__/setup.py/setup.cfg/VERSION exists.
  </read_first>
  <action>
    Change the single line `version = "0.1.0"` under `[project]` in pyproject.toml to `version = "2.0.0"`. Do NOT touch any other line: name, requires-python, dependencies, optional-dependencies, scripts, setuptools config, and pytest config all stay byte-for-byte identical. This is the sole version source of truth (per RH-03, D-RH-03 locked); do not create a setup.py/setup.cfg/VERSION/__version__ to "help" release-please — the python release-type updates pyproject.toml natively and skips missing files. After editing, run the full pytest suite against the venv python to confirm the version bump did not break packaging/import.
  </action>
  <acceptance_criteria>
    - `.venv\Scripts\python.exe -c "import tomllib; assert tomllib.load(open('pyproject.toml','rb'))['project']['version']=='2.0.0'"` exits 0
    - No occurrence of `version = "0.1.0"` remains in pyproject.toml
    - No new setup.py / setup.cfg / VERSION file was created; `git grep __version__ -- '*.py'` still returns nothing (pyproject remains the sole version source)
    - Full suite green: `.venv\Scripts\python.exe -m pytest -q` reports >= 889 passed, 2 skipped, 0 failed
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import tomllib; v=tomllib.load(open('pyproject.toml','rb'))['project']['version']; assert v=='2.0.0', v; print('version', v)" && .venv\Scripts\python.exe -m pytest -q</automated>
  </verify>
  <done>pyproject [project].version reads 2.0.0, no other version source exists, and the full suite is green with no regression.</done>
</task>

<task type="auto">
  <name>Task 2: Create release-please config + manifest seeded at 2.0.0 (RH-02)</name>
  <files>release-please-config.json, .release-please-manifest.json</files>
  <read_first>
    - 32-RESEARCH.md "Code Examples" release-please-config.json (lines 207-218) and .release-please-manifest.json (lines 221-226)
    - 32-RESEARCH.md Pattern 1 (lines 112-132): manifest-seeding semantics — seeded value is the baseline, not a pending tag.
  </read_first>
  <action>
    Create release-please-config.json at repo root using the exact shape in 32-RESEARCH.md lines 208-218: a `$schema` pointing at the release-please config schema, `"release-type": "python"`, and a `"packages"` object with a single key `"."` whose value is an object `{ "package-name": "shoppybot" }`. Create .release-please-manifest.json at repo root containing exactly one key `"."` with string value `"2.0.0"` (matching the pyproject version from Task 1 — this is the seed baseline). Do NOT add an `extra-files` entry (the python strategy already updates pyproject.toml because it has a [project].version key — a redundant extra-files entry risks a double-write). Do NOT pass release-type anywhere except this config file. Both files must be valid JSON.
  </action>
  <acceptance_criteria>
    - `.venv\Scripts\python.exe -c "import json; c=json.load(open('release-please-config.json')); assert c['release-type']=='python'; assert list(c['packages'])==['.']; assert c['packages']['.']['package-name']=='shoppybot'"` exits 0
    - `.venv\Scripts\python.exe -c "import json; m=json.load(open('.release-please-manifest.json')); assert m=={'.':'2.0.0'}, m"` exits 0
    - The manifest `"."` value (2.0.0) equals pyproject [project].version (2.0.0) — single source of truth honored
    - release-please-config.json contains no `extra-files` key
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import json; c=json.load(open('release-please-config.json')); m=json.load(open('.release-please-manifest.json')); assert c['release-type']=='python'; assert list(c['packages'])==['.']; assert c['packages']['.']['package-name']=='shoppybot'; assert 'extra-files' not in c; assert m=={'.':'2.0.0'}, m; print('config+manifest OK')"</automated>
  </verify>
  <done>Both JSON files parse, release-type is python for package ".", and the manifest seed equals the pyproject version (2.0.0).</done>
</task>

<task type="auto">
  <name>Task 3: Create the release-please workflow (RH-02)</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - 32-RESEARCH.md "Code Examples" release-please.yml (lines 228-247) and Pattern 2 notes (lines 134-158)
    - 32-RESEARCH.md Pitfall 1 (lines 175-179): third-party actions are currently blocked by the repo Actions allowlist — this is documented operator debt, NOT a blocker for landing the workflow.
    - Existing .github/workflows/ci.yml for the repo's workflow style (runs-on, permissions convention).
  </read_first>
  <action>
    Create .github/workflows/release-please.yml using the exact shape in 32-RESEARCH.md lines 229-247: workflow name `release-please`; trigger `on: push` restricted to `branches: [master]` (master is the repo default/release branch — not dev); a top-level `permissions` block granting exactly `contents: write` and `pull-requests: write` (no `actions: write`, no `id-token: write` — least privilege per the STRIDE register below); one job `release-please` on `ubuntu-latest` with a single step `uses: googleapis/release-please-action@v5` and `with: token: ${{ secrets.GITHUB_TOKEN }}`. Do NOT pass `release-type` as an action input — that is legacy single-package mode and conflicts with the manifest-mode config from Task 2; the action auto-discovers the root-level config/manifest filenames. The file must be valid YAML.
  </action>
  <acceptance_criteria>
    - `.venv\Scripts\python.exe -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml'))"` exits 0 (valid YAML)
    - File contains the literal `googleapis/release-please-action@v5`
    - File contains `contents: write` and `pull-requests: write`; it does NOT contain `id-token: write` or `actions: write`
    - File triggers on push to `master` (the token `master` is present; the token `dev` is absent — no dev-branch trigger)
    - File does NOT contain a `release-type:` action input (manifest mode only)
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml')); t=open('.github/workflows/release-please.yml',encoding='utf-8').read(); assert 'googleapis/release-please-action@v5' in t; assert 'contents: write' in t and 'pull-requests: write' in t; assert 'id-token: write' not in t and 'actions: write' not in t; assert 'master' in t and 'dev' not in t; assert 'release-type' not in t; print('workflow OK')"</automated>
  </verify>
  <done>release-please.yml is valid YAML, uses release-please-action@v5, is scoped to contents+pull-requests write on push to master, and passes no conflicting release-type input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit → release automation | Conventional-commit history drives semver/changelog; a bad seed or config could tag/publish the wrong version |
| repo → GitHub Actions runner | release-please.yml runs with `GITHUB_TOKEN`; over-broad token perms are a supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Elevation of Privilege | release-please.yml `GITHUB_TOKEN` scope | mitigate | Workflow-level `permissions:` limited to exactly `contents: write` + `pull-requests: write`; no `actions:write`/`id-token:write` (Task 3 acceptance asserts their absence) |
| T-32-02 | Tampering | version seed drift | mitigate | Manifest `"."` and pyproject version both asserted == 2.0.0 (Task 1 + Task 2 cross-check); python release-type updates pyproject natively, no `extra-files` double-write |
| T-32-03 | Elevation of Privilege (supply chain) | Actions-permissions allowlist fix | accept (operator-gated) | Third-party actions blocked today (Pitfall 1). Operator debt: prefer narrow pattern allowlist `googleapis/release-please-action@*` over "allow all". Flagged in verification notes; NOT toggled autonomously. |
| T-32-SC | Tampering | npm/pip installs | accept | No package installs this phase. `googleapis/release-please-action` is a GitHub Action from the googleapis org (approved in 32-RESEARCH Package Legitimacy Audit); not a package-registry artifact, no slopcheck gate applies. |
</threat_model>

<verification>
- pyproject version == 2.0.0 (tomllib assertion); no other version source in the repo.
- release-please-config.json + .release-please-manifest.json parse as JSON; release-type python, package ".", manifest seed 2.0.0.
- release-please.yml parses as YAML; action@v5, least-privilege permissions, push→master trigger, no release-type input.
- Full pytest suite green (>= 889 passed, 2 skipped).

**Operator action items (CI-verification debt — NOT blockers, NOT autonomous tasks; surface in SUMMARY):**
1. Repo Actions allowlist is `selected` with no third-party patterns, so `googleapis/release-please-action` (and the Phase 31 `gitleaks/gitleaks-action`, already `startup_failure`) cannot run. Until the operator adds the pattern(s) in Settings → Actions → General (narrow pattern preferred over "allow all"), the release-please.yml run will be `startup_failure`. This is expected, pre-flagged CI debt — the workflow is still correct and lands.
2. Clarify in SUMMARY (Pitfall 2): seeding the manifest at 2.0.0 does NOT make release-please tag `v2.0.0`; it treats 2.0.0 as already-released and proposes the NEXT bump from commit history. Cutting the actual first release is operator-gated and out of scope.
</verification>

<success_criteria>
- [ ] `pyproject.toml` [project].version == "2.0.0"; pyproject is the sole version source (RH-03).
- [ ] `release-please-config.json` valid JSON, release-type python, package "." named shoppybot, no extra-files (RH-02).
- [ ] `.release-please-manifest.json` valid JSON == `{".":"2.0.0"}`, matching pyproject (RH-02).
- [ ] `.github/workflows/release-please.yml` valid YAML, action@v5, contents+pull-requests write only, push→master (RH-02).
- [ ] Full pytest suite green (no regression).
- [ ] Operator action items (Actions allowlist; seed-vs-tag clarification) recorded in SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/32-release-automation-community-readiness/32-01-SUMMARY.md` when done.
</output>
