---
phase: 31-ci-security-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_captcha.py
  - tests/test_no_tracked_secrets.py
  - .github/workflows/gitleaks.yml
autonomous: true
requirements: [RH-01]

must_haves:
  truths:
    - "gitleaks full-history scan reports zero findings (the one test-fixture false positive is suppressed)"
    - "No sensitive path (config.yml, data/*.db, *creds.bin, sessions/*.bin) is git-tracked"
    - ".gitignore demonstrably covers config.yml and data/* (source of coverage for db + creds + sessions)"
    - "A gitleaks CI job runs on every push/PR and fails the job on any finding"
  artifacts:
    - path: "tests/test_no_tracked_secrets.py"
      provides: "Fast local guard: asserts no sensitive path is git-tracked and .gitignore covers them"
      contains: "git ls-files"
    - path: "tests/test_captcha.py"
      provides: "Suppressed false positive on the sentinel fixture line"
      contains: "#gitleaks:allow"
    - path: ".github/workflows/gitleaks.yml"
      provides: "CI secret-scan job (full-history, fails on findings)"
      contains: "gitleaks/gitleaks-action@v3"
  key_links:
    - from: "tests/test_captcha.py:381"
      to: "gitleaks generic-api-key rule"
      via: "inline #gitleaks:allow comment"
      pattern: "sentinel_key.*#gitleaks:allow"
    - from: ".github/workflows/gitleaks.yml"
      to: "full git history"
      via: "checkout fetch-depth: 0 + gitleaks-action@v3"
      pattern: "fetch-depth:\\s*0"
---

<objective>
Close RH-01: make the repo's secret-scan surface clean and self-enforcing without any destructive history rewrite.

Purpose: The phase goal requires "no tracked secrets or credential artifacts" plus an ongoing scan baked into CI. Research executed a live gitleaks scan (964 commits) and found exactly ONE finding — an intentional test fixture at tests/test_captcha.py:381 — and confirmed .gitignore already excludes every real sensitive path. This plan suppresses that single false positive, adds a fast local guard mirroring the existing test_no_committed_sessions.py pattern, and bakes gitleaks into CI.

Output: An inline suppression comment, a new tests/test_no_tracked_secrets.py guard, and a new .github/workflows/gitleaks.yml CI job.

Non-destructive constraint (LOCKED, CONTEXT.md): NEVER git filter-repo / rebase -i / force-push in this phase. If a real secret were found (none was), the deliverable is surface + stop tracking + flag rotation as operator debt — not history rewrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-ci-security-infrastructure/31-RESEARCH.md
@.planning/phases/31-ci-security-infrastructure/31-CONTEXT.md

<interfaces>
<!-- Existing pattern to mirror for the new guard (extracted from codebase). -->
<!-- Executor should copy this shape directly — no exploration needed. -->

tests/test_no_committed_sessions.py (existing convention):
- _REPO_ROOT = Path(__file__).resolve().parent.parent   (anchors to repo root)
- Uses subprocess.run(["git", "-C", str(_REPO_ROOT), "ls-files", <paths>], capture_output=True, text=True, check=False)
- pytest.skip(...) on FileNotFoundError (git absent) and on non-zero returncode (not a repo) — never fail in non-git envs
- Asserts tracked == [] with a message listing any offending tracked paths
- Second test reads .gitignore text and asserts the covering rule ("data/*") is present

.gitignore covering rules (verified live in research, do NOT edit unless a gap is found):
- line 7: /config.yml
- line 8: data/*            (covers data/*.db, data/creds.bin, data/sessions/*.bin)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress the test-fixture false positive and add the local no-tracked-secrets guard</name>
  <files>tests/test_captcha.py, tests/test_no_tracked_secrets.py</files>
  <read_first>
    - tests/test_captcha.py (line 381 — the sentinel_key fixture assignment)
    - tests/test_no_committed_sessions.py (the exact pattern to mirror: repo-root anchor, git ls-files subprocess, skip-on-no-git, .gitignore content assertion)
    - 31-RESEARCH.md "Pattern 3: Suppressing a confirmed test-fixture false positive" and "Pattern 4: Local pytest guard" and "RH-01 proof commands"
  </read_first>
  <action>
    Append the exact trailing comment token #gitleaks:allow to tests/test_captcha.py line 381 so the line reads: sentinel_key = "sentinel-api-key-cp03-xyzzy"  #gitleaks:allow — do NOT change the string value (the CP-03 test depends on it) and do NOT add a scope-widening .gitleaks.toml allowlist (Anti-Pattern in research: a tests/ path exemption would silently suppress future real leaks).

    Create tests/test_no_tracked_secrets.py mirroring tests/test_no_committed_sessions.py: anchor _REPO_ROOT to the repo root via Path(__file__).resolve().parent.parent. Write test_no_tracked_sensitive_paths() that runs git -C <root> ls-files against the sensitive path set config.yml, data/, *.db, *creds.bin, *sessions/*.bin using subprocess.run(..., check=False); pytest.skip on FileNotFoundError (git absent) and on non-zero returncode; assert the tracked list is empty with a message naming any offenders. Add a second test test_gitignore_covers_sensitive_paths() that reads .gitignore text and asserts both "/config.yml" (or "config.yml") and "data/*" are present, skipping if .gitignore is absent. Keep the two tests skip-safe so the suite stays green in non-git environments, exactly like the existing sessions guard.

    Verify (do not edit) that .gitignore already excludes the sensitive paths via git check-ignore; only add a rule if a genuine gap is found (research confirmed none — /config.yml on line 7 and data/* on line 8 cover db, creds.bin, and sessions/*.bin).
  </action>
  <verify>
    <automated>pytest tests/test_no_tracked_secrets.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '#gitleaks:allow' tests/test_captcha.py` returns the sentinel_key line (line 381) and no other unrelated line.
    - The sentinel string value "sentinel-api-key-cp03-xyzzy" is unchanged (grep still finds it verbatim).
    - `pytest tests/test_no_tracked_secrets.py -q` passes: both test_no_tracked_sensitive_paths and test_gitignore_covers_sensitive_paths are collected and green (not skipped, since git is available).
    - `git ls-files config.yml 'data/*.db' '*creds.bin' '*sessions/*.bin'` returns no lines (no real artifact tracked).
    - No `.gitleaks.toml` file is created; no `[[allowlists]]` path exemption is added anywhere.
  </acceptance_criteria>
  <done>The one gitleaks finding is suppressed inline, a skip-safe local guard asserts no sensitive path is tracked and .gitignore covers them, and pytest tests/test_no_tracked_secrets.py is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the gitleaks CI secret-scan workflow</name>
  <files>.github/workflows/gitleaks.yml</files>
  <read_first>
    - .github/workflows/ci.yml (existing on: push/pull_request branches [master, dev] convention to match)
    - 31-RESEARCH.md "Pattern 2: Minimal gitleaks CI job" and "Standard Stack" (gitleaks-action@v3, checkout@v6 versions) and "Anti-Patterns to Avoid" (do NOT use deprecated `gitleaks detect`)
  </read_first>
  <action>
    Create .github/workflows/gitleaks.yml named "gitleaks". Trigger on push and pull_request to branches [master, dev] plus workflow_dispatch, matching the existing ci.yml / codeql-analysis.yml branch convention. Define a single job scan on runs-on ubuntu-latest with two steps: (1) actions/checkout@v6 with fetch-depth: 0 (full history is REQUIRED — a shallow clone breaks the history scan); (2) gitleaks/gitleaks-action@v3 with env GITHUB_TOKEN set to ${{ secrets.GITHUB_TOKEN }}. Do NOT add a GITLEAKS_LICENSE (research confirmed this is a personal-account public repo, no org license needed). Do NOT add a permissions block (repo default_workflow_permissions is already write). The action fails the job on any finding by default — that satisfies the "fail on findings" requirement; do not weaken it.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/gitleaks.yml')); print('valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/gitleaks.yml` parses as valid YAML (the python yaml.safe_load command prints "valid").
    - `grep -nE 'actions/checkout@v6' .github/workflows/gitleaks.yml` matches (checkout is v6, not v4/v2).
    - `grep -nE 'gitleaks/gitleaks-action@v3' .github/workflows/gitleaks.yml` matches.
    - `grep -nE 'fetch-depth:\s*0' .github/workflows/gitleaks.yml` matches (full-history scan enabled).
    - `grep -nE 'gitleaks detect' .github/workflows/gitleaks.yml` returns no lines (deprecated verb not used).
    - `grep -nE 'workflow_dispatch' .github/workflows/gitleaks.yml` matches (manual trigger present).
  </acceptance_criteria>
  <done>A valid gitleaks.yml CI job scans full history on push/PR/dispatch using checkout@v6 (fetch-depth 0) + gitleaks-action@v3 and fails the job on any finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree / git history → public GitHub | Any tracked file (history included) is world-readable once the repo is public; a leaked credential crosses here |
| CI workflow definition → GitHub Actions runner | The gitleaks job's config determines whether the scan actually enforces anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | tracked secret in git history (config.yml, creds.bin, sessions, api key) | mitigate | gitleaks-action@v3 full-history scan (fetch-depth 0) on every push/PR; local pytest guard asserts no sensitive path tracked |
| T-31-02 | Repudiation | gitleaks job fails open (scans nothing / passes on findings) | mitigate | fetch-depth: 0 enforces full-history scan; gitleaks-action@v3 fails the job on findings by default (not weakened); YAML validity asserted pre-push |
| T-31-03 | Information Disclosure | scope-widening allowlist masks a real future leak in tests/ | mitigate | fingerprint-precise inline #gitleaks:allow on the single reviewed fixture line only; no .gitleaks.toml path exemption (Anti-Pattern) |
| T-31-SC | Tampering | GitHub Action supply chain (gitleaks-action, checkout) | accept | first-party/official publishers pinned to verified major tags (gitleaks/gitleaks-action@v3, actions/checkout@v6) resolved live in research; no third-party forks; no pip/npm install in this plan |
</threat_model>

<verification>
- pytest tests/test_no_tracked_secrets.py -q is green (both guards run, not skipped).
- .github/workflows/gitleaks.yml is valid YAML with checkout@v6 + gitleaks-action@v3 + fetch-depth 0.
- tests/test_captcha.py line 381 carries #gitleaks:allow; string value unchanged.
- CI-verification debt (post-push, per CONTEXT.md live-CI boundary): the gitleaks Actions run reports leaks found: 0. If the runner is unavailable this session, record as CI debt — not a code gap.
</verification>

<success_criteria>
- RH-01 local-verifiable half complete: no sensitive path tracked, .gitignore covers them, false positive suppressed, gitleaks CI job in place and valid.
- No history rewrite, force-push, or secret rotation performed (non-destructive constraint honored).
</success_criteria>

<output>
Create `.planning/phases/31-ci-security-infrastructure/31-01-SUMMARY.md` when done. Record the gitleaks-clean evidence (or CI-verification debt note if the runner was unavailable).
</output>
