# Copy this file to .env (gitignored) or export these in your shell.
# Real credentials must NEVER be placed in config.yml or committed to git.
# The CVV is intentionally absent: it is collected at runtime via a hidden
# prompt (getpass) and never stored on disk.

AMZ_EMAIL=
AMZ_PWD=
BB_EMAIL=
BB_PASSWORD=
